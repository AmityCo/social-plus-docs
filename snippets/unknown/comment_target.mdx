{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: comment_target | updated: 2026-04-30 */}

<CodeGroup>

```swift iOS
switch comment.target {
        /// If the comment is in community then SDK also provide `communityId` and `creatorMember` of that comment
        /// `creatorMember` type is `AmityCommunityMember?`
        /// It will return as null if the comment creator is not member of community
        case .community(let communityId, let creatorMember):
            print("CommunityId:", communityId)
            
            /// Example usage of creatorMember
            /// Check if comment creator is moderator insdie
            if let isModerator = creatorMember?.roles.contains(where: { $0 == "moderator" || $0 == "community-moderator" }), isModerator {
                print("The creator is moderator")
            } else {
                print("The creator is not moderator")
            }
            
        /// If the comment is created to a post in user's feed, we provide the user id of that target user.
        case .user(let userId):
            print("Userfeed id", userId)
            
        /// If the comment is created to a post in content, we provide the contet id.
        case .content(let contentId):
            print("Content id", contentId)
            
        case .unknown:
            break
        default:
            break
        }
```

</CodeGroup>
