{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function:  | updated: 2026-04-30 */}

<CodeGroup>

```kotlin Android
// Using loader pattern for manual pagination with custom pageSize
    fun loadCommentsWithPageSize(
        commentRepository: AmityCommentRepository,
        postId: String,
        parentCommentId: String
    ) {
        // Build a comment query with pageSize=5 and get the loader
        val loader: AmityCommentLoader = commentRepository.getComments()
            .post(postId = postId)
            .parentId(parentId = parentCommentId)
            .sortBy(AmityCommentSortOption.LAST_CREATED)
            .includeDeleted(false)
            .pageSize(5) // 5 comments per page
            .build()
            .loader()

        // Subscribe to cumulative results
        loader.getResult()
            .subscribe { comments: List<AmityComment> ->
                // Update UI with the accumulated comments
            }

        // Trigger first page load
        loader.load()
            .subscribe()

        // Load next page when user requests more
        if (loader.hasMore()) {
            loader.load()
                .subscribe()
        }
    }
```

</CodeGroup>
