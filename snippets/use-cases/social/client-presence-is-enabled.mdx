{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: client-presence-is-enabled | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun isPresenceEnabled() {
        AmityCoreClient.presence()
            .isEnabled()
            .doOnSuccess { isEnabled: Boolean ->
                // isEnabled
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
do {
            let isEnabled = try await client.presence.isEnabled()
            print("Presence enabled: \(isEnabled)")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void checkPresenceEnabled() {
    AmityCoreClient.presence()
        .isEnabled()
        .then((bool isEnabled) {
          // isEnabled indicates whether presence is active
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { Client } from '@amityco/ts-sdk'

function checkPresenceEnabled() {
  const client = Client.getActiveClient()
  const isEnabled = client.isPresenceEnabled()
  console.log('Presence enabled:', isEnabled)
}
```

</CodeGroup>
