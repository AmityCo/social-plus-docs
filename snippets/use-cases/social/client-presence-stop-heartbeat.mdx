{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: client-presence-stop-heartbeat | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun stopHeartbeat() {
        AmityCoreClient.presence()
            .stopHeartbeat()
            .doOnComplete {
                // heartbeat stopped
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
client.presence.stopHeartbeat()
```

```dart Flutter
void stopHeartbeat() {
    AmityCoreClient.presence()
        .stopHeartbeat()
        .then((_) {
          // heartbeat stopped; user is marked as offline
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { Client } from '@amityco/ts-sdk'

async function stopPresenceHeartbeat() {
  const client = Client.getActiveClient()
  await client.stopPresenceHeartbeat()
}
```

</CodeGroup>
