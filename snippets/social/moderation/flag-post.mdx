<CodeGroup>
```swift iOS
do {
    let success = try await postRepository.flagPost(withId: postId, reason: .spam)
} catch {
    // Handle error here
}
```

```kotlin Android
post.report()
    .flag(AmityContentFlagReason.SPAM)
    .doOnComplete {
        // Post flagged successfully
    }
    .doOnError { error ->
        // Handle error
    }
    .subscribe()
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk';

const isFlagged = await PostRepository.flagPost(postId, reasonReport);
```

```dart Flutter
post.report().flag().then((value) {
    // Post flagged successfully
}).onError((error, stackTrace) {
    // Handle error
});
```
</CodeGroup>
