```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk';

// Semantic (AI) search — natural language queries
const unsubscriber = PostRepository.semanticSearchPosts(
  {
    query: 'healthy breakfast ideas',
    targetId: 'communityId',   // optional: scope to a community
    targetType: 'community',   // optional: 'community' | 'user'
    dataTypes: ['text', 'image'],
    matchingOnlyParentPost: true,
  },
  ({ data: posts, onNextPage, hasNextPage, loading }) => {
    if (posts) { /* render post results */ }
  },
);

// Hashtag / keyword search — exact tag matching
const unsubscriber2 = PostRepository.searchPostsByHashtag(
  { query: 'recipe', limit: 20 },
  ({ data: posts, onNextPage, hasNextPage, loading }) => {
    if (posts) { /* render post results */ }
  },
);
```
