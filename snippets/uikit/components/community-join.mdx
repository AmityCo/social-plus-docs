{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-join | updated: 2026-05-01 */}

<CodeGroup>

```kotlin Android
fun joinCommunity(
        communityRepository: AmityCommunityRepository,
        community: AmityCommunity
    ) {
        communityRepository
            .joinCommunity(communityId = community.getCommunityId())
            .doOnComplete {
                // Void
            }
            .doOnError { throwable: Throwable ->
                //Amity Error example
                when {
                    AmityError.from(throwable) == AmityError.ITEM_NOT_FOUND -> {
                        // Community doesn't exist
                    }
                    AmityError.from(throwable) == AmityError.USER_IS_BANNED -> {
                        // User is banned
                    }
                }
            }
            .subscribe()
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository(client: client)
        do {
            let success = try await communityRepository.joinCommunity(withId: "<community-id>")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void joinCommunity(String communityId) {
    AmitySocialClient.newCommunityRepository()
        .joinCommunity(communityId)
        .then((value) => {
              //handle result
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function joinCommunity() {
  await CommunityRepository.joinCommunity('communityId')
}
```

</CodeGroup>
