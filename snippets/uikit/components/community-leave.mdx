{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-leave | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun leaveCommunity(
        communityRepository: AmityCommunityRepository,
        community: AmityCommunity
    ) {
        communityRepository
            .leaveCommunity(communityId = community.getCommunityId())
            .doOnComplete {
                // Void
            }
            .doOnError { throwable: Throwable ->
                //Amity Error example
                when {
                    AmityError.from(throwable) == AmityError.ITEM_NOT_FOUND -> {
                        // Community doesn't exist
                    }
                    AmityError.from(throwable) == AmityError.PERMISSION_DENIED -> {
                        // Permission denied
                    }
                }
            }
            .subscribe()
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository(client: client)
        do {
            let success = try await communityRepository.leaveCommunity(withId: "<community-id>")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void leaveCommunity(String communityId) {
    AmitySocialClient.newCommunityRepository()
        .leaveCommunity(communityId)
        .then((value) => {
              //handle result
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function leaveCommunity() {
  await CommunityRepository.leaveCommunity('communityId')
}
```

</CodeGroup>
