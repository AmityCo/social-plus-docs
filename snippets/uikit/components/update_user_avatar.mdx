{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: update_user_avatar | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun updateUserAvatar() {
        // Update operation for update_user_avatar
        AmityCoreClient.newUserRepository()
            .updateUser()
            .displayName("New Display Name")
            .build()
            .update()
            .subscribe({ user -> }, { error -> })
    }
```

```swift iOS
// Build the current user data
        let builder = AmityUserUpdateOptions()
        builder.setAvatar(imageData)
        // Update the current user from the builder
        do {
            let result = try await client.editUser(builder)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void updateUserAvatar() {
    // First upload the image, then set it as avatar
    AmityCoreClient.newFileRepository()
        .uploadImage(file)
        .stream
        .listen((state) {
          state.when(
            progress: (uploadInfo, cancelToken) {},
            complete: (file) {
              AmityCoreClient.newUserRepository()
                  .updateUser(AmityCoreClient.getUserId())
                  .avatar(file)
                  .update();
            },
            error: (error) => print('Upload error: \$error'),
          );
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function updateUserAvatar() {
  const fileUrl = await FileRepository.uploadImage({ uri: 'path/to/avatar.jpg' })
  const { data: updatedUser } = await UserRepository.updateUser({
    userId: 'userId',
    avatarFileId: fileUrl.fileId,
  })
  console.log('Avatar updated:', updatedUser)
}
```

</CodeGroup>
