{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: search_users_partial | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun searchUsersPartial() {
        AmityCoreClient.newUserRepository()
            .searchUsers(keyword = "keyword")
            .build()
            .query()
            .subscribe()
    }
```

```swift iOS
func partialUserSearchExample() {
        let liveCollection = userRepository.searchUsers("<display-name>", sortBy: .displayName, matchType: .partial)
        token = liveCollection.observe { collection, change, error in
            // Handle live collection notification here
        }
    }
```

```dart Flutter
void searchUsersPartial() {
    AmityCoreClient.newUserRepository()
        .searchUsers('ali')
        .build()
        .query()
        .listen((users) {
          for (final user in users) {
            print('User found: \${user.displayName}');
          }
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function searchUsersPartial() {
  import { UserRepository } from '@amityco/ts-sdk';

  const unsubscribe = UserRepository.searchUserByDisplayName(
    { displayName: 'jo', limit: 20 },
    ({ data: users, loading, error }) => {
      if (!loading && users) {
        console.log('Matching users:', users);
      }
    },
  );
  return unsubscribe;
}
```

</CodeGroup>
