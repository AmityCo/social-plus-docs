{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create_user_repository | updated: 2026-05-02 */}

<CodeGroup>

```swift iOS
let userRepository = AmityUserRepository(client: client)
```

```dart Flutter
void createUserRepository() {
    final userRepo = AmitySocialClient.newUserRepository();
    userRepo
        .getUser('userId')
        .then((AmityUser user) {
          final displayName = user.displayName;
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

function createUserRepository() {
  const liveCollection = UserRepository.queryUsers({ displayName: '' })
  liveCollection.on('dataUpdated', (users) => {
    console.log('Users:', users)
  })
}
```

</CodeGroup>
