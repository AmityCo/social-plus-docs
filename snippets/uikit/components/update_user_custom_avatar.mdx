{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: update_user_custom_avatar | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun updateUserCustomAvatar() {
        // Update operation for update_user_custom_avatar
        AmityCoreClient.newUserRepository()
            .updateUser()
            .displayName("New Display Name")
            .build()
            .update()
            .subscribe({ user -> }, { error -> })
    }
```

```swift iOS
// Build the current user data
        let builder = AmityUserUpdateOptions()
        builder.setAvatarCustomUrl("https://myapp.com/avatar/xyz.jpg")
        // Update the current user from the builder
        do {
            let result = try await client.editUser(builder)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void updateUserCustomAvatar() {
    AmityCoreClient.newUserRepository()
        .updateUser(AmityCoreClient.getUserId())
        .avatarCustomUrl('https://example.com/avatar.jpg')
        .update()
        .then((user) => print('Custom avatar updated'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function updateUserCustomAvatar() {
  const { data: user } = await runQuery(
    UserRepository.getUser({ userId: 'userId' })
  )
  console.log('User avatar:', user?.avatarCustomUrl || user?.avatarFileId)
}
```

</CodeGroup>
