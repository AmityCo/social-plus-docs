{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: observe_user_presence | updated: 2026-04-30 */}

<CodeGroup>

```swift iOS
// Example 1:
    func observeUserPresence1() {
        // Observe user presence update
        userPresenceRepo.getSyncingUserPresence().sink { completion in
            // Handle completion
        } receiveValue: { presences in
            // When presence information is retrieved from server, this publisher publishes it
            let onlinePresences = presences.filter { $0.isOnline }
            
            // You can use this onlinePresences & map it with your users list to determine
            // list of online users to show.
            let onlineUsers = self.users.filter { user in
                let isOnlineUserPresent = onlinePresences.contains { $0.userId == user.userId }
                return isOnlineUserPresent
            }
            
            self.users = onlineUsers
            self.tableView.reloadData()
            
        }.store(in: &cancellables)
    }
    
    
    // Example 2:
    func observeUserPresence2() {
        
        // Observe user presence update
        userPresenceRepo.getSyncingUserPresence().sink { completion in
            // Handle completion
        } receiveValue: { presences in
            // Reload tableview
            self.tableView.reloadData()
            
        }.store(in: &cancellables)
    }
    
    // Tableview where you display data
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        let cell = tableView.dequeueReusableCell(withIdentifier: "MyCell")!
        
        let user = users[indexPath.row]
        cell.textLabel?.text = user.displayName
        
        // Access online presence through lastHeartbeat
        if let userHeartbeat = user.lastHeartbeat, Date().timeIntervalSince(userHeartbeat) < 60 {
            // User is online
            cell.detailTextLabel?.text = "ONLINE"
        } else {
            // User is offline.
            cell.detailTextLabel?.text = "OFFLINE"
        }
        
        return cell
    }
```

</CodeGroup>
