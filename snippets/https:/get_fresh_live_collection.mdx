{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_fresh_live_collection | updated: 2026-04-30 */}

<CodeGroup>

```swift iOS
var token: AmityNotificationToken?
    
    func example() {
        // observe block
        token = liveCollection.observe { [weak self] liveCollection, change, error in
            // We just want to access fresh collection
            guard liveCollection.dataStatus == .fresh else {
                return
            }
            // We don't want to observe further changes to collection
            self?.token?.invalidate()
            // Using the count property we loop through collection data
            for i in 0..<liveCollection.count() {
                // We access individual data
                if let channel = liveCollection.object(at: i) {
                    // Access channel object property as normal object i.e AmityChannel
                    let channelId = channel.channelId
                    let channelDisplayName = channel.displayName
                }
            }
        }
    }
```

</CodeGroup>
