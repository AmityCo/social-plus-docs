{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: follow_user | updated: 2026-04-30 */}

<CodeGroup>

```swift iOS
do {
            let result = try await userRelationship.follow(withUserId: "<user-id>")
            let status = result.status
            
            switch status {
            case .accepted:
                print("follow status: accepted")
            case .pending:
                print("follow status: pending")
            case .none:
                print("follow status: none")
            case .blocked:
                print("follow status: blocked")
            @unknown default:
                print("follow status: unknown")
            }
        } catch {
            // Handle error here
        }
```

</CodeGroup>
