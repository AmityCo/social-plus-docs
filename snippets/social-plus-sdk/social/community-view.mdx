{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-view | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun communityView() {
        // Retrieve a community live object by ID
        AmitySocialClient.newCommunityRepository()
            .getCommunity(communityId = "communityId")
            .subscribe({ community -> println(community.getDisplayName()) }, {})
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository(client: AmityClient.shared)
        let liveObject = communityRepository.getCommunity(withId: "communityId")
        liveObject.observe { liveObject, error in
            guard let community = liveObject.object else { return }
            print("Display Name: \(community.displayName ?? "")")
            print("Description: \(community.communityDescription ?? "")")
            print("Members Count: \(community.membersCount)")
        }
```

```dart Flutter
void getCommunity(String communityId) {
    AmitySocialClient.newCommunityRepository()
        .getCommunity(communityId)
        .then((value) => {
              //handle result
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }

  //example of using AmityCommunity with StreamBuilder
  void observeCommunity(AmityCommunity community) {
    StreamBuilder<AmityCommunity>(
        stream: community.listen.stream,
        builder: (context, snapshot) {
          // update widget
          // eg. widget.text = community.displayName
          return Container();
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function communityView() {
  const client = createClient('YOUR_API_KEY')
  const liveObject = client.communityRepository.communityForId('communityId123')
  liveObject.on('dataUpdated', (community) => {
    console.log('Community name:', community.displayName)
    console.log('Members:', community.membersCount)
  })
}
```

</CodeGroup>
