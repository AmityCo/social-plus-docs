{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: reaction-remove | updated: 2026-04-30 */}

<CodeGroup>

```kotlin Android
fun removeReaction(
        reactionRepository: AmityReactionRepository,
        post: AmityPost
    ) {
        reactionRepository.removeReaction(
            reactionReference = AmityReactionReference.POST(postId = post.getPostId()),
            reactionName = "like"
        )
            .doOnComplete {
                // Void
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```typescript TypeScript
import { ReactionRepository } from '@amityco/ts-sdk'

async function removeReaction() {
  await ReactionRepository.removeReaction('post', 'postId', 'like')
}
```

</CodeGroup>
