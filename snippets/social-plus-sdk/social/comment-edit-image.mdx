{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: comment-edit-image | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun editComment(
        commentRepository: AmityCommentRepository,
        comment: AmityComment,
        images : List<AmityImage>
    ) {

        val attachments = images.map { image ->
            AmityComment.Attachment.IMAGE(fileId = image.getFileId(), null)
        }.toTypedArray()

        commentRepository.editComment(comment.getCommentId())
            .text(text = "Hello")
            .attachments(*attachments)
            .build()
            .apply()
            .doOnSuccess {
                // AmityComment
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```dart Flutter
void editCommentWithImage(
      String commentId, String newText, List<AmityImage> images) {
    AmitySocialClient.newCommentRepository()
        .editComment(commentId: commentId)
        .text(newText)
        .images(images)
        .build()
        .apply()
        .then((AmityComment updatedComment) {
          // Comment with image edited successfully
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }
```

</CodeGroup>
