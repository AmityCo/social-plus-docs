{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-moderation-ban-members | updated: 2026-04-30 */}

<CodeGroup>

```kotlin Android
fun banMembers(communityId: String, userIds: List<String>) {
        AmitySocialClient.newCommunityRepository()
            .moderation(communityId = communityId)
            .banMembers(userIds = userIds)
            .doOnComplete {
                // success
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let moderation = AmityCommunityModeration(client: client, communityId: "<community-id>")
        do {
            try await moderation.banMembers(["<user-id>"])
            print("Members banned")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void banMembers(String communityId, List<String> userIds) {
    AmitySocialClient.newCommunityRepository()
        .moderation(communityId)
        .banMember(userIds)
        .then((value) => {
              //success
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function banCommunityMembers() {
  await CommunityRepository.banMembers('communityId', ['userId1', 'userId2'])
}
```

</CodeGroup>
