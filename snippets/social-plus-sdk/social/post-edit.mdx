{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-edit | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun postEdit() {
        // TODO: implement post edit using Social Plus SDK
    }
```

```swift iOS
func postEdit() {
        let feedRepository = AmityFeedRepository(client: AmityManager.shared.client!)
        let builder = AmityPostUpdateBuilder()
        builder.setPostId("postId")
        builder.setText("Updated post text")
        
        feedRepository.updatePost(builder) { post, error in
            if let error = error {
                print("Post edit failed: \(error)")
                return
            }
            print("Post updated: \(post?.postId ?? "")")
        }
    }
```

```dart Flutter
void editPost(AmityPost post, String newText) {
    AmitySocialClient.newPostRepository()
        .editPost(postId: post.postId!)
        .text(newText)
        .edit()
        .then((AmityPost updatedPost) {
          // post updated
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function editPost() {
  const { data: post } = await PostRepository.updatePost('postId', {
    data: { text: 'Updated post text' },
  })
  console.log(post)
}
```

</CodeGroup>
