{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: reply_comment_text | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun replyCommentText() {
        // perform reply comment text operation
        AmitySocialClient.newCommentRepository()
            .createComment()
            .post("postId")
            .with()
            .text("This is a comment")
            .build()
            .comment()
            .subscribe()
    }
```

```swift iOS
func replyToCommentWithText(commentRepository: AmityCommentRepository,
                                postId: String,
                                commentId: String) async {
        
        let createOptions = AmityCommentCreateOptions(referenceId: postId,
                                                      referenceType: .post,
                                                      text: "<Comment-Text>",
                                                      parentId: commentId)
       
        do {
           let comment = try await commentRepository.createComment(with: createOptions)
        } catch {
            // Handle error here
        }
    }
```

```dart Flutter
void replyCommentText() {
    AmityCoreClient.newSocialRepository()
        .comment
        .createComment()
        .post(postId: 'postId123')
        .parentId('parentCommentId')
        .text('This is a reply comment')
        .create()
        .then((comment) => print('Text reply created: \${comment.commentId}'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function replyCommentText() {
  await CommentRepository.createComment({
    referenceType: 'post',
    referenceId: 'postId123',
    parentId: 'parentCommentId123',
    data: { text: 'This is a reply to the comment.' },
  });
  console.log('Text reply comment created');
}
```

</CodeGroup>
