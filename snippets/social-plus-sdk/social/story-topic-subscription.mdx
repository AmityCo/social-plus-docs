{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: story-topic-subscription | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun storyTopicSubscription() {
        // Subscribe to story topic for real-time updates
        AmitySocialClient.newStoryRepository()
            .getActiveStoriesByTarget(
                targetType = com.amityco.socialcloud.sdk.model.social.story.AmityStory.TargetType.COMMUNITY,
                targetId = "communityId"
            )
            .build()
            .query()
            .subscribe()
    }
```

```swift iOS
let topic = AmityTopic.story(target: .community(communityId: "communityId"))
        AmityEventBus.shared.subscribe(topic: topic, andNotify: self) { event in
            print("Story topic event: \(event)")
        }
```

```dart Flutter
void subscribeToStoryTopic(AmityStory story) {
    story
        .subscription()
        .subscribeTopic()
        .then((value) {
          // Event Subscription successfull 
        })
        .onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function storyTopicSubscription() {
  const topic = getStoryTopic({ targetType: 'community', targetId: 'communityId' });
  const unsubscribe = subscribeTopic(topic, (event) => {
    console.log('Story subscription event:', event);
  });
  return unsubscribe;
}
```

</CodeGroup>
