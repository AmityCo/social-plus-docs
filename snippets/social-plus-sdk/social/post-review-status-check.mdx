{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-review-status-check | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun checkReviewStatus(
        reviewStatus: AmityReviewStatus
    ) {
        when(reviewStatus) {
            AmityReviewStatus.PUBLISHED -> {
              // published
            }
            AmityReviewStatus.UNDER_REVIEW -> {
              // under review
            }
            AmityReviewStatus.DECLINED -> {
                // declined
            }
        }
    }
```

```swift iOS
let client = AmityClient.shared
        let postRepository = AmityPostRepository(client: client)
        let _ = postRepository.getPost(withId: "<postId>") { post, error in
            if let post = post {
                print("Review status: \(post.reviewStatus?.rawValue ?? "unknown")")
            }
        }
```

```dart Flutter
void checkReviewStatus(AmityPost post) {
    final reviewStatus = post.reviewStatus;
    switch (reviewStatus) {
      case AmityReviewStatus.PUBLISHED:
        // post is published and visible
        break;
      case AmityReviewStatus.UNDER_REVIEW:
        // post is awaiting moderator review
        break;
      case AmityReviewStatus.DECLINED:
        // post was declined by a moderator
        break;
      default:
        break;
    }
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function postReviewStatusCheck() {
  const { PostRepository } = Amity;
  const { data: post } = await PostRepository.getPost('postId');
  console.log('Post review status:', post.reviewStatus);
  console.log('Is reviewing:', post.reviewStatus === 'reviewing');
}
```

</CodeGroup>
