{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-member-list | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun communityMemberList() {
        // TODO: implement Community member list
    }
```

```swift iOS
let memberRepository = AmityCommunityMembershipRepository(client: AmityClient.shared)
        let liveCollection = memberRepository.getMembers(
            communityId: "communityId",
            filter: .member,
            sortBy: .lastCreated,
            roles: []
        )
        liveCollection.observe { collection, _, error in
            guard error == nil else { return }
            print("Member count: \(collection.count())")
        }
```

```dart Flutter
void communityMemberList() {
    // List members of a community using a paging controller
    final controller = PagingController<AmityCommunityMember>(
      pageFuture: (token) => AmitySocialClient.newCommunityRepository()
          .membership('communityId')
          .getMembers()
          .filter(AmityCommunityMembershipFilter.member)
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    );
    controller.addListener(() {
      final members = controller.itemList ?? [];
      print('Members: \${members.length}');
    });
    controller.fetchNextPage();
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function communityMemberList() {
  const client = createClient('YOUR_API_KEY')
  const liveCollection = client.communityRepository.searchMembers({
    communityId: 'communityId123',
    memberships: ['member'],
  })
  liveCollection.on('dataUpdated', (members) => {
    console.log('Community members:', members)
  })
}
```

</CodeGroup>
