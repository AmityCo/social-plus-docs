{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: story-impression | updated: 2026-05-02 */}

<CodeGroup>

```swift iOS
let storyRepository = AmityStoryRepository(client: client)
        var token: AmityNotificationToken?
        token = storyRepository.getStory(storyId: "<story-id>").observe { liveObject, error in
            guard let story = liveObject.object else { return }
            story.analytics.markAsViewed()
        }
```

```dart Flutter
void storyImpression() {
    AmitySocialClient.newStoryRepository()
        .markStoryAsSeen(storyId: 'storyId')
        .then((_) {
          print('Story impression recorded.');
        });
  }
```

```typescript TypeScript
import { StoryRepository } from '@amityco/ts-sdk'

async function markStoryImpression() {
  await StoryRepository.markAsSeen('storyId');
  console.log('Story impression recorded');
}
```

</CodeGroup>
