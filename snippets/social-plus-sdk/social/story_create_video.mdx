{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: story_create_video | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void storyCreateVideo() async {
    final story = await AmitySocialClient.newStoryRepository()
        .createStory()
        .targetCommunity('communityId')
        .video(
          filePath: '/path/to/video.mp4',
          items: [],
        )
        .create();
    print('Video story created: \${story.storyId}');
  }
```

```typescript TypeScript
import { StoryRepository } from '@amityco/ts-sdk'

async function createVideoStory(targetId: string, videoFileId: string) {
  const story = await StoryRepository.createVideoStory({
    targetId,
    targetType: 'community',
    fileId: videoFileId,
    metadata: {},
  })

  console.log('Video story created:', story.storyId)
  return story
}
```

</CodeGroup>
