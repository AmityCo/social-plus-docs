{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-poll-get | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getPoll(
        post: AmityPost
    ) {
        val data = post.getData()
        when(data) {
            is AmityPost.Data.POLL -> {
                data.getPoll()
                    .doOnNext {
                        // AmityPoll
                    }
                    .doOnError {
                        // Exception
                    }
                    .subscribe()
            }
            else -> {}
        }
    }
```

```swift iOS
let client = AmityClient.shared
        let pollRepository = AmityPollRepository(client: client)
        let liveObject = pollRepository.getPoll("<pollId>")
        liveObject.observe { livePoll, error in
            if let error = error { print("Error: \(error)"); return }
            print("Poll question: \(livePoll.object?.question ?? "")")
        }
```

```dart Flutter
void getPollFromPost(AmityPost post) {
    final data = post.data;
    if (data is AmityPollData) {
      final pollId = data.pollId;
      AmitySocialClient.newPollRepository()
          .getPoll(pollId)
          .then((AmityPoll poll) {
            final question = poll.question;
            final answers = poll.answers;
            final isClosed = poll.closedAt != null;
            // use poll data
          })
          .onError((error, stackTrace) {
            // handle error
          });
    }
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function postPollGet() {
  const { PostRepository } = Amity;
  const { data: post } = await PostRepository.getPost('pollPostId');
  const pollData = post.data as Amity.PollData;
  console.log('Poll question:', pollData.question);
  console.log('Poll answers:', pollData.answers);
  console.log('Poll closed:', pollData.isClosed);
}
```

</CodeGroup>
