{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: comment-soft-delete | updated: 2026-05-01 */}

<CodeGroup>

```kotlin Android
fun softDeleteComment(commentId: String) {
        AmitySocialClient.newCommentRepository()
            .softDeleteComment(commentId = commentId)
            .doOnComplete {
                // success
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let commentRepository = AmityCommentRepository(client: client)
        do {
            try await commentRepository.softDeleteComment(withId: "<comment-id>")
            print("Comment soft deleted")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void softDeleteComment(AmityComment comment) {
    comment
        .delete()
        .then((value) => {
              //success - comment is soft deleted
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

```typescript TypeScript
import { CommentRepository } from '@amityco/ts-sdk'

async function softDeleteComment() {
  await CommentRepository.deleteComment('commentId')
}
```

</CodeGroup>
