{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: invitation-get-invitations | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getInvitations() {
        AmityCoreClient.newInvitationRepository()
            .getMyCommunityInvitations()
            .doOnNext { invitations ->
                // PagingData<AmityInvitation>
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository(client: client)
        var token: AmityNotificationToken?
        token = communityRepository.getMyPendingInvitations().observe { collection, change, error in
            guard error == nil else { return }
            print("Pending invitations: \(collection.count())")
        }
```

```dart Flutter
void invitationGetInvitations() async {
    final communityRepo = AmitySocialClient.newCommunityRepository();
    final result = communityRepo.getInvitations(communityId: 'communityId123');
    result.listen((invitations) {
      for (final inv in invitations) {
        print('Invitation: \${inv.invitationId} - \${inv.status}');
      }
    });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function invitationGetInvitations() {
  const liveCollection = CommunityRepository.getInvitations({ limit: 20 })
  const unsubscribe = liveCollection.subscribe(({ data: invitations, loading }) => {
    if (!loading) console.log('Invitations:', invitations)
  })
  return unsubscribe
}
```

</CodeGroup>
