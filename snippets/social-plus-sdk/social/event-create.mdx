{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: event-create | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createEvent(
        title: String,
        description: String,
        type: AmityEventType,
        isInviteOnly: Boolean,
        startTime: DateTime,
        endTime: DateTime,
        originType: AmityEventOriginType,
        originId: String,
        location: String,
        externalUrl: String,
        coverImageFileId: String,
        tags: List<String>
    ) {
        val eventRepository = AmitySocialClient.newEventRepository()

        eventRepository.createEvent()
            .setTitle(title)
            .setDescription(description)
            .setType(type)
            .setIsInviteOnly(isInviteOnly)
            .setStartTime(startTime)
            .setEndTime(endTime)
            .setOriginType(originType)
            .setOriginId(originId)
            .setLocation(location)
            .setExternalUrl(externalUrl)
            .setCoverImageFileId(coverImageFileId)
            .setTags(tags)
            .build()
            .create()
            .subscribe(
                { createdEvent: AmityEvent ->
                    // Event created successfully
                    println("Event created: ${createdEvent.getTitle()}")
                    println("Type: ${createdEvent.getType()}")
                    println("Origin: ${createdEvent.getOriginType()}")
                },
                { error: Throwable ->
                    // Handle error
                    println("Error creating event: ${error.message}")
                }
            )
    }
```

```dart Flutter
void createEvent({
    required String title,
    required String description,
    required DateTime startTime,
    required DateTime endTime,
    required String originId,
    String? location,
    String? externalUrl,
    String? coverImageFileId,
  }) {
    AmitySocialClient.newEventRepository()
        .createEvent()
        .setTitle(title)
        .setDescription(description)
        .setStartTime(startTime)
        .setEndTime(endTime)
        .setOriginId(originId)
        .setLocation(location ?? '')
        .setExternalUrl(externalUrl ?? '')
        .build()
        .create()
        .then((AmityEvent event) {
          // event created successfully
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { EventRepository } from '@amityco/ts-sdk'

async function createEvent() {
  const { data: event } = await EventRepository.createEvent({
    communityId: 'communityId',
    displayName: 'My Event',
    description: 'Event description',
    startAt: new Date().toISOString(),
    endAt: new Date(Date.now() + 3600000).toISOString(),
  })
  console.log(event)
}
```

</CodeGroup>
