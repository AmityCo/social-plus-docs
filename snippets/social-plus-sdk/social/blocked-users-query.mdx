{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: blocked-users-query | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun queryBlockedUsers() {
        AmityCoreClient.newUserRepository()
            .getBlockedUsers()
            .build()
            .query()
            .subscribe { pagingData: PagingData<AmityUser> -> }
    }
```

```swift iOS
let collection = userRepository.getBlockedUsers()
        collection.observe { liveCollection, _, error in
            guard error == nil else { return }
            print("Total blocked users: \(liveCollection.snapshots.count)")
        }
```

```dart Flutter
void blockedUsersQuery() {
    AmitySocialClient.newUserRepository()
        .relationship
        .getBlockedUsers()
        .getPagingData(token: null, limit: 20);
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function queryBlockedUsers() {
  const unsubscribe = UserRepository.getBlockedUsers((users, error) => {
    if (error) return console.error(error)
    console.log('Blocked users:', users)
    unsubscribe()
  })
}
```

</CodeGroup>
