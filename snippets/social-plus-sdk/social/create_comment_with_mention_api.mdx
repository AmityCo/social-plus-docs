{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create_comment_with_mention_api | updated: 2026-05-02 */}

<CodeGroup>

```swift iOS
do {
            let success = try await commentRepository.createComment(with: amityCommentCreateOptions)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void createCommentWithMentionApi() {
    final metadata = {
      'mentioned': [
        {'type': 'user', 'userId': 'userId', 'index': 6, 'length': 9}
      ]
    };
    AmitySocialClient.newCommentRepository()
        .createComment()
        .post(postId: 'postId')
        .create()
        .text('Hello @username')
        .metadata(metadata)
        .build()
        .send()
        .then((AmityComment comment) {
          // Comment with mention API created
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }
```

```typescript TypeScript
import { CommentRepository } from '@amityco/ts-sdk'

async function createCommentWithMentionApi() {
  const comment = await CommentRepository.createComment({
    referenceId: 'postId',
    referenceType: 'post',
    data: { text: 'Hello @user!' },
    mentionees: [{ type: 'user', userIds: ['targetUserId'] }],
    metadata: {
      mentioned: [{ type: 'user', userId: 'targetUserId', index: 6, length: 5 }],
    },
  })
  console.log('Comment with mention API:', comment)
}
```

</CodeGroup>
