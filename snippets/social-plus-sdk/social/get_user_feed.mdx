{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_user_feed | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getUserFeed() {
        AmitySocialClient.newFeedRepository()
            .getGlobalFeed()
            .build()
            .query()
            .subscribe()
    }
```

```swift iOS
func getUserFeed() {
        let feedRepository = AmityFeedRepository(client: AmityManager.shared.client!)
        let collection = feedRepository.getUserFeed("userId123", sortBy: .lastCreated, includeDeleted: false, feedType: .published)
        collection.observe { collection, _, error in
            guard error == nil else { return }
            collection.snapshots.forEach { post in
                print("Post id: \(post.postId)")
            }
        }
    }
```

```dart Flutter
void getUserFeed() async {
    AmitySocialClient.newPostRepository()
        .getPosts()
        .targetUser(userId: 'userId')
        .getPagingData(token: null, limit: 20)
        .asStream()
        .listen((result) {
      final posts = result.data;
    });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

function getUserFeed() {
  const userId = 'user123'
  const liveCollection = PostRepository.getPosts({ targetId: userId, targetType: 'user', limit: 20 })
  liveCollection.on('dataUpdated', (posts) => {
    console.log('User feed:', posts)
  })
}
```

</CodeGroup>
