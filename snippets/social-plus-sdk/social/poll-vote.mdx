{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: poll-vote | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun votePoll(pollId: String, answerIds: List<String>) {
        AmitySocialClient.newPollRepository()
            .vote(pollId = pollId, answerIds = answerIds)
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let pollRepository = AmityPollRepository(client: client)
        do {
            let result = try await pollRepository.votePoll(withId: "<poll-id>", answerIds: ["<answer-id>"])
        } catch {
            // Handle error here
        }
```

```dart Flutter
void pollVote() async {
    await AmitySocialClient.newPollRepository()
        .votePoll(
          pollId: 'pollId',
          answerIds: ['answerId1'],
        );
  }
```

```typescript TypeScript
import { PollRepository } from '@amityco/ts-sdk'

async function voteOnPoll(pollId: string, answerIds: string[]) {
  await PollRepository.votePoll(pollId, answerIds)
  console.log(`Voted on poll ${pollId} with answers:`, answerIds)
}
```

</CodeGroup>
