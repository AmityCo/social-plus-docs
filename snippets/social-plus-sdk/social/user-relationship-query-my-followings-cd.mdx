{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-relationship-query-my-followings-cd | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun userRelationshipQueryMyFollowingsCd() {
        // Observe live list of users the current user is following
        AmitySocialClient.newUserRepository()
            .relationship()
            .me()
            .getFollowings()
            .build()
            .query()
            .subscribe { pagingData -> }
    }
```

```swift iOS
let userRepository = AmityUserRepository(client: client)
        var token: AmityNotificationToken?
        token = userRepository.userRelationship.getMyFollowings(with: .all).observe { collection, change, error in
            guard error == nil else { return }
            print("My followings: \(collection.count())")
        }
```

```dart Flutter
void userRelationshipQueryMyFollowingsCd() {
    AmitySocialClient.newUserRepository()
        .relationship
        .getMyFollowings()
        .status(AmityFollowStatus.ACCEPTED)
        .getPagingData()
        .listen((result) => print('My followings: \${result.data.length}'))
        ..onError((error) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function userRelationshipQueryMyFollowingsCd() {
  const userId = 'target-user-id';
  const { data: followStatus } = await UserRepository.relationship.follow(userId);
  console.log('Follow status:', followStatus);
}
```

</CodeGroup>
