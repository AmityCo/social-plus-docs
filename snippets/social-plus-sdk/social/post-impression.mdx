{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-impression | updated: 2026-05-02 */}

<CodeGroup>

```swift iOS
let postRepository = AmityPostRepository(client: client)
        var token: AmityNotificationToken?
        token = postRepository.getPost(withId: "<post-id>").observe { liveObject, error in
            guard let post = liveObject.object else { return }
            post.analytics.markAsViewed()
        }
```

```dart Flutter
void markPostAsViewed(AmityPost post) {
    post.analytics.markAsViewed();
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function markPostImpression() {
  await PostRepository.markPostAsViewed('postId')
}
```

</CodeGroup>
