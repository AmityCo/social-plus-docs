{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_poll_from_post | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getPollFromPost() {
        // Retrieve poll data attached to a post
        AmitySocialClient.newPostRepository()
            .getPost("postId")
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(io.reactivex.rxjava3.android.schedulers.AndroidSchedulers.mainThread())
            .subscribe({ post ->
                val pollData = post.getData() as? com.amityco.socialcloud.sdk.model.social.post.AmityPost.Data.POLL
                val poll = pollData?.getPoll()
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
func getPoll(from post: AmityPost) {
        let _ = post.getPollInfo()
    }
```

```dart Flutter
void getPollFromPost() {
    AmitySocialClient.newPostRepository()
        .getPost('postId')
        .asStream()
        .listen((post) {
      final pollData = post.data as AmityPollData;
      print('Poll question: \${pollData.poll?.question}');
      for (final answer in pollData.poll?.answers ?? []) {
        print('Answer: \${answer.data}, Votes: \${answer.voteCount}');
      }
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function getPollFromPost() {
  const liveObject = PostRepository.getPost('postId123');
  liveObject.on('dataUpdated', (post) => {
    const pollChild = post.children?.find((c: any) => c.dataType === 'poll');
    if (pollChild) {
      console.log('Poll question:', pollChild.data?.question, 'closed:', pollChild.data?.closedAt);
    }
  });
}
```

</CodeGroup>
