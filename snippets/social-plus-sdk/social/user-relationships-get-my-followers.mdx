{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-relationships-get-my-followers | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
final _followers = <AmityFollowRelationship>[];
  late PagingController<AmityFollowRelationship> _controller;

  void getMyFollowers() {
    _controller = PagingController(
      pageFuture: (token) => AmityCoreClient.newUserRepository()
          .relationship()
          .getMyFollowers()
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    )..addListener(() {
        if (_controller.error == null) {
          _followers.clear();
          _followers.addAll(_controller.loadedItems);
          // update widgets
        } else {
          // handle error
        }
      });
  }
```

</CodeGroup>
