{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-recommendation | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void getRecommendation() async {
    AmitySocialClient.newCommunityRepository()
        .getRecommendedCommunities()
        .getPagingData(token: null, limit: 20)
        .asStream()
        .listen((result) {
      final communities = result.data;
    });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

function getRecommendation() {
  const liveCollection = CommunityRepository.getRecommendedCommunities({ limit: 10 })
  liveCollection.on('dataUpdated', (communities) => {
    console.log('Recommended communities:', communities)
  })
}
```

</CodeGroup>
