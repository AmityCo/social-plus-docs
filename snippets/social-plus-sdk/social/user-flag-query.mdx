{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-flag-query | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun userFlagQuery() {
        AmitySocialClient.newUserRepository()
            .isFlaggedByMe("userId123")
            .subscribe { isFlagged ->
                println("Flagged by me: \$isFlagged")
            }
    }
```

```swift iOS
let collection = AmitySDK.queryFlaggedUsers()
        let token = collection.observe { liveCollection, change, error in
            if let error = error {
                print("Failed to query flagged users: \(error)")
                return
            }
            let users = liveCollection.allObjects()
            print("Flagged users loaded: \(users.count)")
        }
```

```dart Flutter
void userFlagQuery() {
    AmitySocialClient.newUserRepository()
        .flag('userId')
        .isFlaggedByMe()
        .then((bool flagged) {
          print('User flagged by me: \$flagged');
        });
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function queryFlaggedUsers() {
  const unsubscribe = UserRepository.getUsers(
    { isFlaggedByMe: true },
    (result) => {
      if (result.data) {
        console.log('Flagged users:', result.data);
      }
    },
  );
  return unsubscribe;
}
```

</CodeGroup>
