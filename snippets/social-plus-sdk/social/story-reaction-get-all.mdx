{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: story-reaction-get-all | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun storyReactionGetAll() {
        AmitySocialClient.newReactionRepository()
            .addReaction(
                referenceType = com.amityco.socialcloud.sdk.model.core.reaction.AmityReactionReferenceType.STORY,
                referenceId = "storyId",
                reactionName = "like"
            )
            .subscribe()
    }
```

```swift iOS
let reactionRepository = AmityReactionRepository(client: AmityManager.shared.client!)
        let collection = reactionRepository.getReactions(referenceId: "storyId", referenceType: .story, reactionName: nil)
        collection.observe { liveCollection, _, error in
            print("Story reactions: \(liveCollection.count())")
        }
```

```dart Flutter
final _reactions = <AmityReaction>[];
  late PagingController<AmityReaction> _controller;

  void getAllStoryReactions(String storyId) {
    _controller = PagingController(
      pageFuture: (token) => AmityCoreClient.newReactionRepository()
          .getReactions(AmityReactionReferenceType.STORY, storyId)
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    )..addListener(() {
        if (_controller.error == null) {
          _reactions
            ..clear()
            ..addAll(_controller.loadedItems);
          // update UI
        } else {
          // handle error
        }
      });

    _controller.fetchNextPage();
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function storyReactionGetAll() {
  const storyRepository = new StoryRepository();
  const impressions = await storyRepository.getStoryImpressions('storyId');
  console.log('Story impressions:', impressions);
}
```

</CodeGroup>
