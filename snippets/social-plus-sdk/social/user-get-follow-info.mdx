{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-get-follow-info | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun userGetFollowInfo() {
        // Get and observe user-get-follow-info
        AmityCoreClient.newUserRepository()
            .relationship()
            .getFollowInfo("userId")
            .asFlowable()
            .subscribe({ followInfo -> }, { error -> })
    }
```

```swift iOS
let userId = "userId"
        AmitySDK.getFollowInfo(userId: userId) { followInfo, error in
            if let error = error {
                print("Failed to get follow info: \(error)")
                return
            }
            print("Followers: \(followInfo?.followersCount ?? 0), Following: \(followInfo?.followingCount ?? 0)")
        }
```

```dart Flutter
Future<void> getUserFollowInfo(String userId) async {
    try {
      final followInfo = await AmityCoreClient.newUserRepository()
          .relationship()
          .getFollowInfo(userId);
      final followerCount = followInfo.followerCount;
      final followingCount = followInfo.followingCount;
      final followStatus = followInfo.status;
      // use follow info data
    } catch (error) {
      // handle error
    }
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function userGetFollowInfo() {
  const { data: followInfo } = await UserRepository.Relationships.getFollowInfo({ userId: 'userId' })
  console.log('Follow info:', followInfo)
}
```

</CodeGroup>
