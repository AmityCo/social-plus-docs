{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-query-recommended-communities | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun queryRecommendedCommunities() {
        AmitySocialClient.newCommunityRepository()
            .getRecommendedCommunities()
            .build()
            .query()
            .doOnNext { communities: PagingData<AmityCommunity> ->
                // PagingData<AmityCommunity>
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository(client: client)
        var token: AmityNotificationToken?
        token = communityRepository.getRecommendedCommunities().observe { collection, change, error in
            guard error == nil else { return }
            print("Recommended communities: \(collection.count())")
        }
```

```dart Flutter
void getRecommendedCommunities() {
    AmitySocialClient.newCommunityRepository()
        .getRecommendedCommunities()
        .then((List<AmityCommunity> communities) => {
              //handle result - returns maximum 5 recommended communities
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function queryRecommendedCommunities() {
  const liveCollection = CommunityRepository.getRecommendedCommunities()
  liveCollection.on('dataUpdated', (communities) => {
    console.log(communities)
  })
}
```

</CodeGroup>
