{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: follow-get-my-info | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getMyInfo() {
        AmityCoreClient.newUserRepository()
            .relationship()
            .getMyFollowInfo()
            .doOnNext { followInfo: AmityMyFollowInfo ->
                val followerCount = followInfo.getFollowerCount()
                val followingCount = followInfo.getFollowingCount()
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let userRepository = AmityUserRepository(client: AmityManager.shared.client!)
        let liveObject = userRepository.getMyFollowInfo()
        liveObject.observe { liveObject, error in
            guard let info = liveObject.object else { return }
            print("Followers: \(info.followerCount), Following: \(info.followingCount)")
        }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

function getMyFollowInfo() {
  const unsubscribe = UserRepository.Relationship.getMyFollowInfo(
    ({ data: followInfo, loading, error }) => {
      if (loading) return
      if (error) throw error
      console.log('Followers:', followInfo.followerCount)
      console.log('Following:', followInfo.followingCount)
    },
  )
  return unsubscribe
}
```

</CodeGroup>
