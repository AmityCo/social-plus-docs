{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_following | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getFollowing() {
        // Query the current user's following list
        AmitySocialClient.newFollowRepository()
            .getMyFollowings()
            .build()
            .query()
            .subscribe { followings -> /* handle following list */ }
    }
```

```swift iOS
// Get the following list for the current user
        let userRepository = AmityUserRepository(client: AmityManager.shared.client!)
        var token: AmityNotificationToken?
        let currentUserId = AmityManager.shared.client?.currentUserId ?? ""
        let collection = userRepository.getFollowList(withUserId: currentUserId, with: .following)
        token = collection.observe { liveCollection, _, error in
            if let error = error {
                print("Failed to get following list: \(error.localizedDescription)")
                return
            }
            let following = (0..<liveCollection.count()).compactMap { liveCollection.object(at: $0) }
            print("Following count: \(following.count)")
        }
```

```dart Flutter
void getFollowing() async {
    AmitySocialClient.newUserRepository()
        .relationship()
        .getFollowing(userId: 'userId')
        .getPagingData(token: null, limit: 20)
        .asStream()
        .listen((result) {
      final following = result.data;
    });
  }
```

```typescript TypeScript
import { FollowRepository } from '@amityco/ts-sdk'

function getFollowing() {
  const userId = 'user123'
  const liveCollection = FollowRepository.getFollowings({ userId, limit: 20 })
  liveCollection.on('dataUpdated', (followings) => {
    console.log('Following:', followings)
  })
}
```

</CodeGroup>
