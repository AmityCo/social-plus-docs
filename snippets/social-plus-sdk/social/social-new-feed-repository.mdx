{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: social-new-feed-repository | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun socialNewFeedRepository() {
        AmitySocialClient.newFeedRepository()
            .getGlobalFeed()
            .build()
            .query()
            .subscribe()
    }
```

```swift iOS
let feedRepository = AmityFeedRepository(client: AmityManager.shared.client!)
        let collection = feedRepository.getGlobalFeed()
        collection.observe { liveCollection, _, error in
            print("Feed posts: \(liveCollection.count())")
        }
```

```dart Flutter
void newFeedRepository() {
    final feedRepository = AmitySocialClient.newFeedRepository();
    // use feedRepository to query user or global feeds
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function socialNewFeedRepository() {
  import { PostRepository } from '@amityco/ts-sdk';

  const unsubscribe = PostRepository.getPosts(
    { targetId: 'community-id-123', targetType: 'community', limit: 20 },
    ({ data: posts, loading, error, hasNextPage, onNextPage }) => {
      if (!loading && posts) {
        console.log('Feed posts:', posts);
      }
    },
  );
  return unsubscribe;
}
```

</CodeGroup>
