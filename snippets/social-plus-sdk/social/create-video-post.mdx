{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create-video-post | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createVideoPost() {
        // TODO: implement Create video post
    }
```

```swift iOS
let postRepository = AmityPostRepository(client: AmityClient.shared)
        let builder = AmityVideoPostBuilder()
        // Attach uploaded video data objects before creating
        builder.setText("My video post")
        postRepository.createPost(builder, targetId: "communityId", targetType: .community) { post, error in
            if let error = error {
                print("Error creating video post: \(error)")
            } else {
                print("Video post created: \(post?.postId ?? "")")
            }
        }
```

```dart Flutter
void createVideoPost() {
    AmitySocialClient.newPostRepository()
        .createPost()
        .targetMe()
        .video([AmityUploadedVideo(fileId: 'fileId')])
        .build()
        .post()
        .then((AmityPost post) {
          final postId = post.postId;
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function createVideoPost() {
  const post = await PostRepository.createPost({
    targetType: 'community',
    targetId: 'communityId',
    dataType: 'video',
    data: { fileId: 'uploadedVideoFileId' },
  })
  console.log('Video post created:', post)
}
```

</CodeGroup>
