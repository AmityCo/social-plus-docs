{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create-video-post | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createVideoPost(communityId: String, videoUris: List<Uri>, caption: String) {
        val target = AmityPost.Target.COMMUNITY.create(communityId = communityId)
        AmitySocialClient.newPostRepository()
            .createVideoPost(target = target, attachments = videoUris, text = caption)
            .doOnSuccess { post: AmityPost ->
                // AmityPost video created
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```dart Flutter
void createVideoPost() {
    AmitySocialClient.newPostRepository()
        .createPost()
        .targetMe()
        .video([AmityUploadedVideo(fileId: 'fileId')])
        .build()
        .post()
        .then((AmityPost post) {
          final postId = post.postId;
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function createVideoPost() {
  const post = await PostRepository.createPost({
    targetType: 'community',
    targetId: 'communityId',
    dataType: 'video',
    data: { fileId: 'uploadedVideoFileId' },
  })
  console.log('Video post created:', post)
}
```

</CodeGroup>
