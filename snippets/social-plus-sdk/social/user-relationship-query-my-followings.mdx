{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-relationship-query-my-followings | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun queryMyFollowings() {
        AmityCoreClient.newUserRepository()
            .relationship()
            .getMyFollowings()
            .status(AmityFollowStatusFilter.ACCEPTED)
            .build()
            .query()
            .doOnNext { followings: PagingData<AmityFollowRelationship> ->
                // PagingData<AmityFollowRelationship>
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```dart Flutter
final _followings = <AmityUser>[];
  late PagingController<AmityUser> _controller;

  void queryMyFollowings() {
    _controller = PagingController(
      pageFuture: (token) => AmityCoreClient.newUserRepository()
          .relationship()
          .getMyFollowings()
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    )..addListener(
        () {
          if (_controller.error == null) {
            _followings.clear();
            _followings.addAll(_controller.loadedItems);
            //update widgets
          } else {
            //error on pagination controller
          }
        },
      );
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function queryMyFollowings() {
  const liveCollection = UserRepository.getMyFollowings()
  liveCollection.on('dataUpdated', (followings) => {
    console.log(followings)
  })
}
```

</CodeGroup>
