{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: access-post-and-its-children | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void accessPostAndChildren(AmityPost post) {
    // Access post data
    final postId = post.postId;
    final data = post.data;

    // Access children posts (e.g. image/video attachments)
    final children = post.children;
    for (final child in children ?? []) {
      final childType = child.type;
      // handle each child post
    }
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

function accessPostAndChildren(postId: string) {
  const unsubscribe = PostRepository.getPost(postId, ({ data: post, loading, error }) => {
    if (loading) return
    if (error) {
      console.error('Error loading post:', error)
      return
    }

    console.log('Post ID:', post.postId)
    console.log('Post text:', post.data?.text)

    // Access child posts (e.g. image/video attachments)
    const children = post.children ?? []
    console.log(`Child posts (${children.length}):`)
    children.forEach((child) => {
      console.log(' - Child post ID:', child.postId, '| type:', child.dataType)
    })
  })

  return unsubscribe
}
```

</CodeGroup>
