{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-flag-by-me | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun userFlagByMe() {
        val userId = "userId"
        AmitySocialClient.newUserRepository()
            .flagUser(userId)
            .subscribe()
    }
```

```swift iOS
let userFlagRepository = AmityUserFlagRepository(userId: "targetUserId")
        userFlagRepository.isFlaggedByMe { isFlagged, error in
            if let error = error {
                print("Error checking user flag status: \(error)")
            } else {
                print("User flagged by me: \(isFlagged)")
            }
        }
```

```dart Flutter
void userFlagByMe() {
    AmitySocialClient.newUserRepository()
        .isFlaggedByMe(userId: 'userId')
        .then((isFlagged) => print('Is flagged by me: \$isFlagged'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function userFlagByMe() {
  const isFlagged = await UserRepository.isFlaggedByMe({ userId: 'userId' })
  console.log('Flagged by me:', isFlagged)
}
```

</CodeGroup>
