{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-create-clip-post | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createClipPost(targetCommunityId: String, clip: AmityVideo, text: String = "") {
        val target = AmityPost.Target.COMMUNITY.create(communityId = targetCommunityId)
        AmitySocialClient.newPostRepository()
            .createClipPost(target = target, clip = clip, text = text)
            .doOnSuccess { post: AmityPost ->
                // AmityPost
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let fileRepository = AmityFileRepository(client: AmityClient.shared)
        let videoURL = URL(fileURLWithPath: "/path/to/clip.mp4")
        fileRepository.uploadVideo(fromLocalURL: videoURL, progress: { progress in
            print("Upload progress: \(progress * 100)%")
        }) { videoData, error in
            if let error = error {
                print("Video upload failed: \(error.localizedDescription)")
                return
            }
            guard let videoData = videoData else { return }
            let postRepository = AmityPostRepository(client: AmityClient.shared)
            let builder = AmityVideoPostBuilder()
            builder.setVideos([videoData])
            postRepository.createPost(builder, targetId: nil, targetType: .user) { post, error in
                if let error = error {
                    print("Failed to create clip post: \(error.localizedDescription)")
                    return
                }
                print("Clip post created: \(post?.postId ?? "")")
            }
        }
```

```dart Flutter
void postCreateClipPost() async {
        // Upload video file for clip
        final uploadResult = await AmityCoreClient.newFileRepository()
            .uploadVideo(filePath: '/path/to/clip.mp4');

        // Create clip post
        final post = await AmitySocialClient.newPostRepository()
            .createPost()
            .targetCommunity('communityId')
            .video(fileIds: [uploadResult.fileId])
            .text('Check out this video clip!')
            .post();
        print('Clip post created: \${post.postId}');
  }
```

```typescript TypeScript
import { PostRepository, FileRepository } from '@amityco/ts-sdk'

async function createClipPost(videoFile) {
  const { data: uploadedFile } = await FileRepository.uploadVideo(videoFile)
  const { data: post } = await PostRepository.createPost({
    targetType: 'community',
    targetId: 'communityId',
    dataType: 'video',
    data: { fileId: uploadedFile.fileId },
  })
  console.log(post)
}
```

</CodeGroup>
