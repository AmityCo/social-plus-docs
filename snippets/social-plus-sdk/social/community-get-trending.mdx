{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-get-trending | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun communityGetTrending() {
        AmitySocialClient.newCommunityRepository()
            .getTrendingCommunities()
            .build()
            .query()
            .subscribe(
                { communities -> println("Trending communities: ${communities.size}") },
                { error -> println("Error: ${error.message}") }
            )
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository(client: AmityClient.shared)
        let token = communityRepository.getTrendingCommunities().observe { collection, _, error in
            guard error == nil else { return }
            print("Trending communities: \(collection.count())")
            for i in 0..<collection.count() {
                if let community = collection.object(at: UInt(i)) {
                    print("- \(community.displayName)")
                }
            }
        }
```

```dart Flutter
void getTrendingCommunities() {
    AmitySocialClient.newCommunityRepository()
        .getTrendingCommunities()
        .then((List<AmityCommunity> communities) {
          for (final community in communities) {
            final communityId = community.communityId;
            final displayName = community.displayName;
            final membersCount = community.membersCount;
          }
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function getTrendingCommunities() {
  const { data: communities } = await CommunityRepository.getTrendingCommunities({ limit: 10 })
  console.log(communities)
}
```

</CodeGroup>
