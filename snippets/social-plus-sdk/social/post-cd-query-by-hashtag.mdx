{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-cd-query-by-hashtag | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun postCdQueryByHashtag() {
        // Query posts as a live collection with real-time updates
        AmitySocialClient.newPostRepository()
            .getPosts()
            .targetMe()
            .build()
            .query()
            .subscribe({ pagingData ->
                // Handle paged post data
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
let postRepository = AmityPostRepository(client: client)
        var token: AmityNotificationToken?
        token = postRepository.getPostsByHashtag(hashtag: "<hashtag>", sortBy: .lastCreated).observe { collection, change, error in
            guard error == nil else { return }
            print("Posts with hashtag: \(collection.count())")
        }
```

```dart Flutter
void postCdQueryByHashtag() async {
        final result = await AmitySocialClient.newPostRepository()
            .getPosts()
            .hashtag('flutter')
            .getPagingData();
        print('Posts with #flutter: \${result.data.length}');
        for (final post in result.data) {
          print('Post: \${post.postId}');
        }
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function postCdQueryByHashtag() {
  const liveCollection = PostRepository.getPosts({ targetId: 'communityId123', targetType: 'community', tags: ['#trending'] });
  liveCollection.on('dataUpdated', (posts) => {
    console.log('Posts by hashtag:', posts);
  });
}
```

</CodeGroup>
