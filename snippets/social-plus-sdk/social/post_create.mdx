{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post_create | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void postCreate() async {
    final postRepo = AmitySocialClient.newPostRepository();
    final post = await postRepo
        .createPost()
        .targetMe()
        .text('Hello from Amity!')
        .post();
    print('Post created: \${post.postId}');
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function postCreate(targetId: string, text: string) {
  const post = await PostRepository.createPost({
    targetId,
    targetType: 'community',
    dataType: 'text',
    data: { text },
  })
  console.log('Post created:', post.data.postId)
}
```

</CodeGroup>
