{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: query_comment | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun queryComment() {
        // Query comments for a post
        AmitySocialClient.newCommentRepository()
            .getComments()
            .post("postId")
            .build()
            .query()
            .asLiveData()
    }
```

```swift iOS
let commentRepository = AmityCommentRepository(client: AmityUIKitManagerInternal.shared.client)
        let queryOptions = AmityCommentQueryOptions(referenceId: "postId", referenceType: .post, filterByParentId: false, parentId: nil, orderBy: .descending, includeDeleted: false)
        let collection = commentRepository.getComments(with: queryOptions)
        collection.observe { liveCollection, _, error in
            guard error == nil else { return }
            let comments = liveCollection.snapshots
            print("Loaded \(comments.count) comments")
        }
```

```dart Flutter
void queryComment() async {
    final commentRepository = AmitySocialClient.newCommentRepository();
    commentRepository
        .getComments()
        .post('postId')
        .includeDeleted(false)
        .asStream()
        .listen((comments) {
      print('Comments: \${comments.length}');
    });
  }
```

```typescript TypeScript
import { CommentRepository } from '@amityco/ts-sdk'

async function queryCommentsUnderscore() {
  const params = {
    referenceId: 'postId123',
    referenceType: 'post' as const,
    sortBy: 'lastCreated' as const,
    limit: 20,
  };

  const liveCollection = CommentRepository.getComments(params);
  liveCollection.on('dataUpdated', comments => {
    console.log('Comments:', comments.map(c => c.commentId));
  });
  liveCollection.on('dataError', err => console.error('Error querying comments:', err));
}
```

</CodeGroup>
