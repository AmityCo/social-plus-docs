{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: event-query-live | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun eventQueryLive(communityId: String) {
        AmitySocialClient.newEventRepository()
            .getEvents()
            .communityId(communityId = communityId)
            .build()
            .getLiveCollection()
            .doOnNext { events: PagingData<AmityEvent> ->
                // handle live events
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let eventRepository = AmityEventRepository(client: client)
        var token: AmityNotificationToken?
        token = eventRepository.getEvents().observe { collection, change, error in
            guard error == nil else { return }
            print("Events count: \(collection.count())")
        }
```

```dart Flutter
void eventQueryLive() {
    final eventRepo = AmitySocialClient.newEventRepository();
    eventRepo
        .getEvents()
        .getLiveCollection()
        .asStream()
        .listen((events) => print('Live events: \${events.length}'))
        ..onError((error) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function eventQueryLive() {
  const liveCollection = runQuery(EventRepository.getEvents({
    communityId: 'communityId123',
    filter: 'upcoming',
    limit: 10,
  }));
  liveCollection.on('dataUpdated', (events) => {
    console.log('Live events:', events);
  });
}
```

</CodeGroup>
