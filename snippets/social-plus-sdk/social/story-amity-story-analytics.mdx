{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: story-amity-story-analytics | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun storyAmityStoryAnalytics() {
        AmitySocialClient.newStoryRepository()
            .markStoryAsSeen(storyId = "storyId")
            .subscribe()
    }
```

```swift iOS
let storyRepository = AmityStoryRepository(client: AmityManager.shared.client!)
        let analytics = storyRepository.getStoryAnalytics(storyId: "storyId")
        analytics.observe { result, error in
            print("Story analytics: \(String(describing: result))")
        }
```

```dart Flutter
import 'package:amity_sdk/amity_sdk.dart';

void trackStoryAnalytics(AmityStory story) {
  final analytics = AmityStoryAnalytics(story);

  // Mark story as seen when user views it
  analytics.markAsSeen();

  // Mark story link as clicked when user taps a hyperlink
  analytics.markLinkAsClicked();
}
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function storyAmityStoryAnalytics() {
  import { StoryRepository } from '@amityco/ts-sdk';

  const unsubscribe = StoryRepository.getStoriesByTarget(
    { targetId: 'community-id-123', targetType: 'community' },
    ({ data: stories, loading, error }) => {
      if (!loading && stories) {
        stories.forEach((story) => {
          console.log('Story analytics:', {
            storyId: story.storyId,
            impressionCount: story.impressionCount,
            reactionCount: story.reactionCount,
          });
        });
      }
    },
  );
  return unsubscribe;
}
```

</CodeGroup>
