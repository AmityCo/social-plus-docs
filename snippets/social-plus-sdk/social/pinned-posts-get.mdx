{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: pinned-posts-get | updated: 2026-05-01 */}

<CodeGroup>

```kotlin Android
fun getPinnedPosts(
        postRepository: AmityPostRepository,
        communityId: String
    ) {
        // 1. Can use "announcement" as a placement
        postRepository.getPinnedPosts(communityId = communityId, placement = "default")
            .doOnNext { pinnedPost: PagingData<AmityPinnedPost> ->
                // AmityPinnedPost.post : AmityPost
            }
            .doOnError {
                // Exception
            }
            .subscribe()
        
        // 2. Can use ${AmityPinnedPost.PinPlacement.DEFAULT.value} as a placement
        postRepository.getPinnedPosts(communityId = communityId, placement = AmityPinnedPost.PinPlacement.DEFAULT.value)
            .doOnNext { pinnedPost: PagingData<AmityPinnedPost> ->
                // AmityPinnedPost.post : AmityPost
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

</CodeGroup>
