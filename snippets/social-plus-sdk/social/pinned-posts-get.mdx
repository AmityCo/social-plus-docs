{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: pinned-posts-get | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getPinnedPosts(
        postRepository: AmityPostRepository,
        communityId: String
    ) {
        // 1. Can use "announcement" as a placement
        postRepository.getPinnedPosts(communityId = communityId, placement = "default")
            .doOnNext { pinnedPost: PagingData<AmityPinnedPost> ->
                // AmityPinnedPost.post : AmityPost
            }
            .doOnError {
                // Exception
            }
            .subscribe()
        
        // 2. Can use ${AmityPinnedPost.PinPlacement.DEFAULT.value} as a placement
        postRepository.getPinnedPosts(communityId = communityId, placement = AmityPinnedPost.PinPlacement.DEFAULT.value)
            .doOnNext { pinnedPost: PagingData<AmityPinnedPost> ->
                // AmityPinnedPost.post : AmityPost
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let feedRepository = AmityFeedRepository(client: AmityClient.shared)
        let communityId = "community_id"
        let liveCollection = feedRepository.getPinnedPosts(withCommunityId: communityId)
        liveCollection.observe { collection, _, error in
            if let error = error {
                print("Failed to get pinned posts: \(error.localizedDescription)")
                return
            }
            let posts = collection.allObjects()
            print("Pinned posts count: \(posts.count)")
        }
```

```dart Flutter
void pinnedPostsGet() async {
        AmitySocialClient.newCommunityRepository()
            .getPinnedPosts(communityId: 'communityId')
            .asStream()
            .listen((posts) {
              print('Pinned posts count: \${posts.length}');
              for (final post in posts) {
                print('Post: \${post.postId}, pinned at: \${post.pinnedAt}');
              }
            });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function pinnedPostsGet() {
  const liveCollection = PostRepository.getPinnedPosts({ targetId: 'communityId123', targetType: 'community' });
  liveCollection.on('dataUpdated', (posts) => {
    console.log('Pinned posts:', posts);
  });
}
```

</CodeGroup>
