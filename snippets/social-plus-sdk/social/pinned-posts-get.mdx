{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: pinned-posts-get | updated: 2026-04-30 */}

<CodeGroup>

```kotlin Android
fun getAnnouncementPost(
        postRepository: AmityPostRepository,
        communityId: String
    ) {
        // 1. Can use "announcement" as a placement
        postRepository.getPinnedPosts(communityId = communityId, placement = "announcement")
            .doOnNext { pinnedPost: PagingData<AmityPinnedPost> ->
                // AmityPinnedPost.post : AmityPost
            }
            .doOnError {
                // Exception
            }
            .subscribe()
        
        // 2. Can use ${AmityPinnedPost.PinPlacement.ANNOUNCEMENT.value} as a placement
        postRepository.getPinnedPosts(communityId = communityId, placement = AmityPinnedPost.PinPlacement.ANNOUNCEMENT.value)
            .doOnNext { pinnedPost: PagingData<AmityPinnedPost> ->
                // AmityPinnedPost.post : AmityPost
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

</CodeGroup>
