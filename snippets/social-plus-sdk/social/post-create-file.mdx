{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-create-file | updated: 2026-04-30 */}

<CodeGroup>

```kotlin Android
fun createFilePost(targetId: String, fileUri: Uri) {
        AmitySocialClient.newPostRepository()
            .createPost()
            .targetUser(userId = targetId)
            .file(fileUri = fileUri)
            .build()
            .post()
            .doOnSuccess { post: AmityPost ->
                // handle created post
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let postRepository = AmityPostRepository(client: client)
        let builder = AmityFilePostBuilder()
        let fileURL = URL(string: "file:///path/to/document.pdf")!
        builder.setAttachments([fileURL])
        do {
            let post = try await postRepository.createFilePost(builder, targetId: "<community-id>", targetType: .community, metadata: nil, mentionees: nil)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void createFilePost(String communityId, File file) {
    AmityCoreClient.newFileRepository()
        .uploadFile(file)
        .stream
        .listen((amityUploadResult) {
      amityUploadResult.when(
        progress: (uploadInfo, cancelToken) {},
        complete: (AmityFile amityFile) {
          AmitySocialClient.newPostRepository()
              .createPost()
              .targetCommunity(communityId)
              .file([amityFile])
              .post()
              .then((AmityPost post) {
                // handle result
              })
              .onError((error, stackTrace) {
                // handle error
              });
        },
        error: (error) {
          // handle upload error
        },
        cancel: () {},
      );
    });
  }
```

</CodeGroup>
