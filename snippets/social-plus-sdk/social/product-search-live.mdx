{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: product-search-live | updated: 2026-05-02 */}

<CodeGroup>

```swift iOS
let productRepository = AmityProductRepository(client: client)
        var token: AmityNotificationToken?
        token = productRepository.searchProducts(query: "<search-query>").observe { collection, change, error in
            guard error == nil else { return }
            print("Products found: \(collection.count())")
        }
```

```dart Flutter
List<AmityProduct> _products = [];
  late ProductLiveCollection _liveCollection;

  void searchProductsLive(String keyword) {
    _liveCollection = AmityProductRepository()
        .searchProductsLive(keyword)
        .getLiveCollection();

    _liveCollection.getStreamController().stream.listen((products) {
      _products = products;
      // update UI
    }, onError: (error) {
      // handle error
    });

    _liveCollection.loadNext();
  }
```

</CodeGroup>
