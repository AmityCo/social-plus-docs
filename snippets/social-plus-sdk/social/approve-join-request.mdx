{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: approve-join-request | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun approveJoinRequest(communityId: String, userId: String) {
        AmitySocialClient.newCommunityRepository()
            .moderation(communityId)
            .acceptJoinRequest(userIds = listOf(userId))
            .doOnComplete { }
            .doOnError { }
            .subscribe()
    }
```

```swift iOS
communityRepository.acceptMemberRequest(withUserId: "userId", communityId: "communityId") { success, error in
            if let error = error {
                print("Error approving request: \(error)")
            } else {
                print("Join request approved: \(success)")
            }
        }
```

```dart Flutter
void approveJoinRequest(String communityId, String userId) {
    AmitySocialClient.newCommunityRepository()
        .moderation(communityId)
        .approveJoinRequest([userId])
        .then((value) {
          // join request approved
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function approveJoinRequest(communityId: string, userId: string) {
  try {
    await CommunityRepository.Membership.approveJoinRequest(communityId, [userId]);
    console.log('Join request approved for user:', userId);
  } catch (error) {
    console.error('Failed to approve join request:', error);
  }
}
```

</CodeGroup>
