{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-a-community | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getACommunity() {
        AmitySocialClient.newCommunityRepository()
            .getCommunity("communityId")
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(AndroidSchedulers.mainThread())
            .subscribe({ community ->
                val name = community.getDisplayName()
                val memberCount = community.getMembersCount()
            }, { error ->
                error.printStackTrace()
            })
    }
```

```swift iOS
func getACommunity() {
        let communityRepository = AmityCommunityRepository(client: AmityManager.shared.client!)
        let liveObject = communityRepository.getCommunity(withId: "communityId")
        liveObject.observe { community, error in
            if let error = error {
                print("Error: \(error)")
                return
            }
            print("Community: \(community?.object?.displayName ?? "")")
        }
    }
```

```dart Flutter
void getACommunity() async {
    final community = await AmitySocialClient.newCommunityRepository()
        .getCommunity(communityId: 'communityId');
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

function getACommunity() {
  const communityId = 'community123'
  const liveObject = CommunityRepository.getCommunity(communityId)
  liveObject.on('dataUpdated', (community) => {
    console.log('Community:', community)
  })
}
```

</CodeGroup>
