{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-ban-unban | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun userBanUnban() {
        // Perform user-ban-unban operation
        AmitySocialClient.newCommunityRepository()
            .moderation("communityId")
            .banMember("userId")
            .subscribe({ /* banned */ }, { error -> })
    }
```

```swift iOS
let userId = "userId"
        AmitySDK.banUser(userId: userId) { success, error in
            if let error = error {
                print("Failed to ban/unban user: \(error)")
                return
            }
            print("User ban status updated")
        }
```

```dart Flutter
void banMembers(String communityId, List<String> userIds) {
    AmitySocialClient.newCommunityRepository()
        .moderation(communityId)
        .banMember(userIds)
        .then((value) {
          // users banned
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }

  void unbanMembers(String communityId, List<String> userIds) {
    AmitySocialClient.newCommunityRepository()
        .moderation(communityId)
        .unbanMember(userIds)
        .then((value) {
          // users unbanned
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function userBanUnban() {
  // Ban a user
  await UserRepository.banUser({ userId: 'userId' })
  // Unban a user
  await UserRepository.unbanUser({ userId: 'userId' })
}
```

</CodeGroup>
