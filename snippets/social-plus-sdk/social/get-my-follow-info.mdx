{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-my-follow-info | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getMyFollowInfo() {
        AmityCoreClient.newUserRepository()
            .relationship()
            .getMyFollowInfo()
            .doOnNext { followInfo: AmityMyFollowInfo ->
                val followerCount = followInfo.getFollowerCount()
                val followingCount = followInfo.getFollowingCount()
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let userRepository = AmityUserRepository(client: AmityManager.shared.client!)
        let liveObject = userRepository.getMyFollowInfo()
        liveObject.observe { liveObject, error in
            guard let info = liveObject.object else { return }
            print("My followers: \(info.followerCount), following: \(info.followingCount), pending: \(info.pendingCount)")
        }
```

```dart Flutter
void getMyFollowInfo() {
    final followInfo = await AmityCoreClient.newUserRepository()
        .relationship()
        .me()
        .getFollowInfo();
    print('My followers: \${followInfo.followerCount}, following: \${followInfo.followingCount}');
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

function getMyFollowInfo() {
  const unsubscribe = UserRepository.Relationship.getMyFollowInfo(
    ({ data: followInfo, loading, error }) => {
      if (loading) return
      if (error) throw error
      console.log('My follower count:', followInfo.followerCount)
      console.log('My following count:', followInfo.followingCount)
      console.log('Pending requests:', followInfo.pendingCount)
    },
  )
  return unsubscribe
}
```

</CodeGroup>
