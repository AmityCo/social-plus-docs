{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: story-get-story-target | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getStoryTarget(targetType: AmityStory.TargetType, targetId: String) {
        AmitySocialClient.newStoryRepository()
            .getStoryTarget(
                targetType = targetType,
                targetId = targetId
            )
            .doOnNext { storyTarget: AmityStoryTarget ->
                // handle story target
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
liveObject = storyRepository.getStoryTarget(targetType: .community, targetId: "<community-id>")
        liveObject?.observe { object, error in
            if let error = error {
                // Handle error here
                print("Error: \(error)")
                return
            }
            if let target = object.object {
                print("Story target ID: \(target.targetId)")
                print("Has unseen stories: \(target.hasUnseen)")
            }
        }
```

```dart Flutter
void storyGetStoryTarget() {
    AmityCoreClient.newSocialRepository()
        .story
        .getStoryTarget(
          targetType: AmityStoryTargetType.community,
          targetId: 'communityId123',
        )
        .then((target) => print('Story target: \${target?.targetId}'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function storyGetStoryTarget() {
  import { StoryRepository } from '@amityco/ts-sdk';

  const unsubscribe = StoryRepository.getStoryTarget(
    { targetId: 'community-id-123', targetType: 'community' },
    ({ data: target, loading, error }) => {
      if (!loading && target) {
        console.log('Story target:', target);
      }
    },
  );
  return unsubscribe;
}
```

</CodeGroup>
