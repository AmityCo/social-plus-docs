{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: story-get-story-target | updated: 2026-05-01 */}

<CodeGroup>

```kotlin Android
fun getStoryTarget(targetType: AmityStory.TargetType, targetId: String) {
        AmitySocialClient.newStoryRepository()
            .getStoryTarget(
                targetType = targetType,
                targetId = targetId
            )
            .doOnNext { storyTarget: AmityStoryTarget ->
                // handle story target
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
liveObject = storyRepository.getStoryTarget(targetType: .community, targetId: "<community-id>")
        liveObject?.observe { object, error in
            if let error = error {
                // Handle error here
                print("Error: \(error)")
                return
            }
            if let target = object.object {
                print("Story target ID: \(target.targetId)")
                print("Has unseen stories: \(target.hasUnseen)")
            }
        }
```

</CodeGroup>
