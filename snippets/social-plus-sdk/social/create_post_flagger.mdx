{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create_post_flagger | updated: 2026-05-02 */}

<CodeGroup>

```swift iOS
let _ = AmityPostFlagger(client: client, postId: "<post-id>")
```

```dart Flutter
void createPostFlagger() {
    AmitySocialClient.newPostRepository()
        .getPost('postId')
        .then((AmityPost post) {
          post
              .report()
              .flag()
              .then((_) {
                // Post flagged
              })
              .onError((error, stackTrace) {
                // Handle error
              });
        });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function createPostFlagger() {
  await PostRepository.flagPost('postId')
  console.log('Post flagged for moderation')
}
```

</CodeGroup>
