{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: member-add-community-member | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun addCommunityMember(communityId: String, userIds: List<String>) {
        AmitySocialClient.newCommunityRepository()
            .membership(communityId = communityId)
            .addMembers(userIds = userIds)
            .doOnComplete {
                // members added
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository(client: AmityUIKitManagerInternal.shared.client)
        let communityId = "community123"
        let userIds = ["user1", "user2"]
        communityRepository.addMembers(userIds, communityId: communityId) { success, error in
            print("Community members added: \(success)")
        }
```

```dart Flutter
void addCommunityMembers(String communityId, List<String> userIds) {
    AmitySocialClient.newCommunityRepository()
        .membership(communityId)
        .addMembers(userIds)
        .then((members) {
          // members added successfully
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function memberAddCommunityMember() {
  await CommunityRepository.addMembers('communityId', ['userId1', 'userId2'])
  console.log('Members added successfully')
}
```

</CodeGroup>
