{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-edit-custom-post | updated: 2026-05-01 */}

<CodeGroup>

```kotlin Android
fun editCustomPost(postId: String, newData: JSONObject) {
        AmitySocialClient.newPostRepository()
            .editPost(postId = postId)
            .data(data = newData)
            .build()
            .apply()
            .doOnSuccess { post: AmityPost ->
                // AmityPost
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function editCustomPost() {
  const { data: post } = await PostRepository.updatePost('postId', {
    data: { customField: 'updatedValue' },
  })
  console.log(post)
}
```

</CodeGroup>
