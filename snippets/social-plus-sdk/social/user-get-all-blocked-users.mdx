{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-get-all-blocked-users | updated: 2026-04-30 */}

<CodeGroup>

```kotlin Android
fun getAllBlockedUsers() {
        AmityCoreClient.newUserRepository()
            .relationship()
            .getBlockedUsers()
            .build()
            .query()
            .doOnNext { blockedUsers: PagingData<AmityUser> ->
                // PagingData<AmityUser>
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let userRepository = AmityUserRepository(client: client)
        var token: AmityNotificationToken?
        token = userRepository.userRelationship.getBlockedUsers().observe { collection, change, error in
            guard error == nil else { return }
            print("Blocked users: \(collection.count())")
        }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function getAllBlockedUsers() {
  const liveCollection = UserRepository.getAllBlockedUsers()
  liveCollection.on('dataUpdated', (users) => {
    console.log(users)
  })
}
```

</CodeGroup>
