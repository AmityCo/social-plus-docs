{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: invitation-create | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createInvitation(communityId: String, userId: String) {
        AmitySocialClient.newCommunityRepository()
            .invitation()
            .create(communityId = communityId, userId = userId)
            .doOnComplete {
                // invitation created
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository(client: AmityUIKitManagerInternal.shared.client)
        let communityId = "community123"
        let userId = "user123"
        communityRepository.createInvitation(userId: userId, communityId: communityId) { success, error in
            print("Invitation created: \(success)")
        }
```

```dart Flutter
void createInvitation(String communityId, List<String> userIds) {
    AmitySocialClient.newCommunityRepository()
        .invitation(communityId)
        .sendInvitation(userIds: userIds)
        .then((_) {
          // invitations created and sent
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function invitationCreate() {
  const communityId = 'community-id-123'
  const userId = 'user-id-456'
  const { data: invitation, error } = await CommunityRepository.createInvitation({
    communityId,
    userId,
  })
  if (error) {
    console.error('Failed to create invitation:', error)
  } else {
    console.log('Invitation created:', invitation?.invitationId)
  }
}
```

</CodeGroup>
