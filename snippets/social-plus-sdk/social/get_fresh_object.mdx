{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_fresh_object | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getFreshObject() {
        // Retrieve a fresh live object and observe for updates
        AmitySocialClient.newPostRepository()
            .getPost(postId = "postId")
            .subscribe { post ->
                /* observe live object for real-time changes */
            }
    }
```

```dart Flutter
void getFreshObject() {
    // TODO: implement SDK operation
    throw UnimplementedError();
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function getFreshObject() {
  const liveObject = PostRepository.getPost('postId123');
  await liveObject.refresh();
  liveObject.on('dataUpdated', (post) => {
    console.log('Fresh post text:', post.data?.text);
  });
}
```

</CodeGroup>
