{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create_post_with_mention_api | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createPostWithMentionApi() {
        // Create a text post that mentions a user
        AmitySocialClient.newPostRepository()
            .createPost()
            .targetUser(userId = "userId")
            .text(text = "Hello @user!")
            .mentionUsers(userIds = listOf("mentionedUserId"))
            .build()
            .subscribe()
    }
```

```swift iOS
do {
            let post = try await postRepository.createTextPost(amityPostBuilder, targetId: nil, targetType: targetType, metadata: nil, mentionees: amityMentioneesBuilder)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void createPostWithMentionApi() {
    final metadata = {
      'mentioned': [
        {'type': 'user', 'userId': 'userId', 'index': 6, 'length': 9}
      ]
    };
    AmitySocialClient.newPostRepository()
        .createPost()
        .targetMe()
        .text('Hello @username')
        .metadata(metadata)
        .build()
        .post()
        .then((AmityPost post) {
          // Post with mention API created
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function createPostWithMentionApi() {
  const post = await PostRepository.createPost({
    targetType: 'community',
    targetId: 'communityId',
    dataType: 'text',
    data: { text: 'Congrats @user on the milestone!' },
    mentionees: [{ type: 'user', userIds: ['targetUserId'] }],
    metadata: {
      mentioned: [{ type: 'user', userId: 'targetUserId', index: 8, length: 5 }],
    },
  })
  console.log('Post with mention API:', post)
}
```

</CodeGroup>
