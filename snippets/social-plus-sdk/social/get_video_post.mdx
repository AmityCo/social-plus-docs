{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_video_post | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getVideoPost() {
        val videoUri = android.net.Uri.parse("content://path/to/video.mp4")
        AmitySocialClient.newPostRepository()
            .createPost()
            .targetMe()
            .video(listOf(videoUri))
            .build()
            .post()
            .subscribe()
    }
```

```swift iOS
func getVideoPost() {
        let postRepository = AmityPostRepository(client: AmityManager.shared.client!)
        let liveObject = postRepository.getPost(withId: "videoPostId123")
        liveObject.observe { liveObject, error in
            guard let post = liveObject.object, post.dataType == "video" else { return }
            print("Video post children: \(post.childrenPosts?.count ?? 0)")
        }
    }
```

```dart Flutter
void getVideoPost() async {
    final post = await AmitySocialClient.newPostRepository()
        .getPost('postId');
    if (post.dataType == AmityDataType.VIDEO) {
      final videoData = post.data as VideoData;
      print('Video file ID: \${videoData.fileId}');
    }
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function getVideoPost(postId: string) {
  const liveObject = PostRepository.getPost(postId)
  liveObject.on('dataUpdated', (post) => {
    if (post.dataType === 'video') {
      console.log('Video post:', post)
    }
  })
  return liveObject
}
```

</CodeGroup>
