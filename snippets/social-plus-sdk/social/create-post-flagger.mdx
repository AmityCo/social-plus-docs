{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create-post-flagger | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createPostFlagger(postId: String) {
        AmitySocialClient.newPostRepository()
            .flagPost(postId = postId)
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```dart Flutter
void createPostFlagger() {
    AmitySocialClient.newPostRepository()
        .flagPost('postId')
        .then((_) { })
        .onError((error, stackTrace) { });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function createPostFlagger(postId: string) {
  try {
    await PostRepository.flagPost(postId);
    console.log('Post flagged for moderation review:', postId);
  } catch (error) {
    console.error('Failed to flag post:', error);
  }
}
```

</CodeGroup>
