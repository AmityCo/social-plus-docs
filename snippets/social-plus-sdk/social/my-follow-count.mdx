{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: my-follow-count | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun myFollowCount() {
        // Follow/unfollow user operation
        AmityCoreClient.newUserRepository()
            .getFollowInfo(userId = AmityCoreClient.getUserId())
            .asLiveData()
            .observe(lifecycleOwner) { followInfo ->
                val followerCount = followInfo.getFollowerCount()
                val followingCount = followInfo.getFollowingCount()
            }
    }
```

```swift iOS
let userRepo = AmityUserRepository(client: AmityManager.shared.client!)
        let liveObject = userRepo.getFollowInfo(userId: AmityManager.shared.client!.currentUserId!)
        liveObject.observe { liveObject, error in
            guard let info = liveObject.object else { return }
            print("Following: \(info.followingCount), Followers: \(info.followerCount)")
        }
```

```dart Flutter
void getFollowInfo() {
    AmityCoreClient.newUserRepository()
        .relationship()
        .getMyFollowInfo()
        .then((myFollowInfo) => {
              //my follower count
              myFollowInfo.followerCount,
              //my following count
              myFollowInfo.followingCount
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

```typescript TypeScript
import { FollowRepository } from '@amityco/ts-sdk'

function getMyFollowCount() {
  const liveObject = FollowRepository.getMyFollowInfo()
  liveObject.on('dataUpdated', (followInfo) => {
    const followingCount = followInfo.followingCount
    const followerCount = followInfo.followerCount
    console.log('Following:', followingCount, 'Followers:', followerCount)
  })
  return liveObject
}
```

</CodeGroup>
