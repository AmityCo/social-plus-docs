{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post_review | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun postReview() {
        // Review (approve/decline) a pending post
        AmitySocialClient.newPostRepository()
            .reviewPost(postId = "postId")
            .approve()
            .subscribe(
                { /* post approved */ },
                { error -> /* handle error */ }
            )
    }
```

```swift iOS
let client = AmityClient.shared
        let postRepository = AmityPostRepository(client: client)
        let _ = postRepository.getPost(withId: "<postId>") { post, error in
            if let post = post {
                print("Review status: \(post.reviewStatus?.rawValue ?? "unknown")")
            }
        }
```

```dart Flutter
void postReview() async {
    final postRepository = AmitySocialClient.newPostRepository();
    // Approve a pending post
    await postRepository
        .approvePost('postId');
    // Or decline a pending post
    // await postRepository.declinePost('postId');
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function reviewPostUnderscore() {
  const approvePost = async (postId: string) => {
    await PostRepository.approvePost({ postId });
    console.log('Post approved:', postId);
  };

  const declinePost = async (postId: string) => {
    await PostRepository.declinePost({ postId });
    console.log('Post declined:', postId);
  };

  await approvePost('postId123');
}
```

</CodeGroup>
