{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: event-rsvp-get | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
Future<void> getRsvp(String eventId) async {
    final rsvp = await AmitySocialClient.newEventRepository()
        .getRsvp(eventId: eventId);
    final status = rsvp?.status;
    // status reflects the current RSVP response (e.g., going, notGoing, maybe)
  }
```

```typescript TypeScript
import { EventRepository } from '@amityco/ts-sdk'

async function getMyRsvp() {
  const { data: rsvp } = await EventRepository.getMyRSVP('eventId')
  console.log('My RSVP status:', rsvp.status)
}
```

</CodeGroup>
