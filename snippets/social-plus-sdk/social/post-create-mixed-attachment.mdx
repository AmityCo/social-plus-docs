{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-create-mixed-attachment | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createMixedAttachmentPost(
        targetType: AmityPost.TargetType,
        targetId: String,
        attachments: Set<AmityAttachment>,
        text: String = ""
    ) {
        AmitySocialClient.newPostRepository()
            .createMixedAttachmentPost(
                targetType = targetType,
                targetId = targetId,
                attachments = attachments,
                text = text
            )
            .doOnSuccess { post: AmityPost ->
                // handle created post
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let postRepository = AmityPostRepository(client: AmityClient.shared)
        let builder = AmityTextPostBuilder()
        builder.setText("Check out these attachments!")
        // Mixed attachment post with multiple media types
        postRepository.createPost(builder, targetId: "community_id", targetType: .community) { post, error in
            if let error = error {
                print("Failed to create mixed attachment post: \(error.localizedDescription)")
                return
            }
            print("Mixed attachment post created: \(post?.postId ?? "")")
        }
```

```dart Flutter
void postCreateMixedAttachment() async {
        // Upload image and file attachments
        final imageResult = await AmityCoreClient.newFileRepository()
            .uploadImage(filePath: '/path/to/image.jpg');
        final fileResult = await AmityCoreClient.newFileRepository()
            .uploadFile(filePath: '/path/to/document.pdf');

        final post = await AmitySocialClient.newPostRepository()
            .createPost()
            .targetCommunity('communityId')
            .image(fileIds: [imageResult.fileId])
            .file(fileIds: [fileResult.fileId])
            .text('Mixed attachment post')
            .post();
        print('Mixed attachment post created: \${post.postId}');
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function postCreateMixedAttachment() {
  const post = await PostRepository.createPost({
    targetId: 'communityId123',
    targetType: 'community',
    data: { text: 'Mixed attachment post' },
    dataType: 'text',
    attachments: [
      { fileId: 'imageFileId123', type: 'image' },
      { fileId: 'fileId456', type: 'file' },
    ],
  });
  console.log('Mixed attachment post created:', post.postId);
}
```

</CodeGroup>
