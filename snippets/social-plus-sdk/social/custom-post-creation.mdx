{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: custom-post-creation | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createCustomPost(
        postRepository: AmityPostRepository,
        customData: JsonObject
    ) {
        // To create a post on a user feed, create a user target
        val userTarget = AmityPost.Target.USER.create(userId = "user1")

        // To create a post on a community feed, create a community target
        val communityTarget = AmityPost.Target.COMMUNITY.create(communityId = "community1")

        // define custom dataType
        val dataType = "amity.custom" // custom dataType must be in this pattern "*.*"

        postRepository.createCustomPost(
            target = userTarget,
            customDataType = dataType,
            data = customData
        )
            .doOnSuccess { post: AmityPost ->
                // AmityPost
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let postRepository = AmityPostRepository()
        let builder = AmityCustomPostBuilder()
        builder.setData(["customKey": "customValue", "type": "poll"])
        builder.setText("Custom post content")
        postRepository.createPost(builder, targetId: "communityId", targetType: .community) { post, error in
            if let error = error {
                print("Failed to create custom post: \(error.localizedDescription)")
            } else {
                print("Custom post created: \(post?.postId ?? "")")
            }
        }
```

```dart Flutter
void customPostCreation() {
    AmitySocialClient.newPostRepository()
        .createPost()
        .targetMe()
        .customPost(
          dataType: 'custom.post.type',
          data: {'key': 'value', 'extra': 'data'},
        )
        .build()
        .post()
        .then((AmityPost post) {
          final postId = post.postId;
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function customPostCreation() {
  const post = await PostRepository.createPost({
    targetType: 'community',
    targetId: 'communityId',
    dataType: 'custom',
    data: {
      customType: 'article',
      title: 'My Custom Article',
      content: 'Article content here',
    },
  })
  console.log('Custom post created:', post)
}
```

</CodeGroup>
