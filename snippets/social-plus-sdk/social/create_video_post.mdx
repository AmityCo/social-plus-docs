{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create_video_post | updated: 2026-05-02 */}

<CodeGroup>

```swift iOS
func createVideoPostExample(videos: [AmityVideoData], text: String) async {
        // Build your post structure
        let builder = AmityVideoPostBuilder()
        builder.setText(text)
        builder.setVideos(videos)
        // Create a post from the builder
        do {
            let post = try await postRepository.createVideoPost(builder, targetId: nil, targetType: .user, metadata: nil, mentionees: nil)
        } catch {
            // Handle error here
        }
    }
```

```dart Flutter
void createVideoPost() {
    AmitySocialClient.newPostRepository()
        .createPost()
        .targetMe()
        .video([AmityUploadedVideo(fileId: 'fileId')])
        .build()
        .post()
        .then((AmityPost post) {
          final postId = post.postId;
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function createVideoPost() {
  const post = await PostRepository.createPost({
    targetType: 'community',
    targetId: 'communityId',
    dataType: 'video',
    data: { fileId: 'uploadedVideoFileId' },
  })
  console.log('Video post created:', post)
}
```

</CodeGroup>
