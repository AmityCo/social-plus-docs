{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-create | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createCommunity(displayName: String, isPublic: Boolean) {
        AmitySocialClient.newCommunityRepository()
            .createCommunity(displayName = displayName)
            .isPublic(isPublic = isPublic)
            .build()
            .create()
            .doOnSuccess { community: AmityCommunity ->
                // handle created community
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository(client: client)
        let builder = AmityCommunityCreateOptions()
        builder.setDisplayName("My Community")
        builder.setCommunityDescription("A great community")
        builder.setIsPublic(true)
        do {
            let community = try await communityRepository.createCommunity(with: builder)
            print("Community created: \(community.communityId)")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void communityCreate() async {
    final communityRepo = AmitySocialClient.newCommunityRepository();
    final community = await communityRepo
        .createCommunity('My Community')
        .isPublic(true)
        .description('A great community')
        .create();
    print('Community created: \${community.communityId}');
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function communityCreate(displayName: string, description: string, isPublic: boolean) {
  const community = await CommunityRepository.createCommunity({
    displayName,
    description,
    isPublic,
  })
  console.log('Community created:', community.data.communityId)
}
```

</CodeGroup>
