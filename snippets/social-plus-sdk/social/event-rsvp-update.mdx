{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: event-rsvp-update | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun eventRsvpUpdate(eventId: String, newStatus: AmityEventRsvpStatus) {
        AmitySocialClient.newEventRepository()
            .updateRsvp(eventId = eventId, status = newStatus)
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let eventRepository = AmityEventRepository()
        eventRepository.updateRsvp(eventId: "eventId", status: .notGoing) { rsvp, error in
            if let error = error {
                print("Failed to update RSVP: \(error.localizedDescription)")
            } else {
                print("RSVP updated to: \(rsvp?.status.rawValue ?? "")")
            }
        }
```

```dart Flutter
void updateRsvp(String eventId, AmityEventRsvpStatus status) {
    AmitySocialClient.newEventRepository()
        .rsvp(eventId)
        .setStatus(status)
        .submit()
        .then((_) {
          // RSVP updated successfully
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { EventRepository } from '@amityco/ts-sdk'

async function updateRsvp() {
  const { data: rsvp } = await EventRepository.upsertRSVP({
    eventId: 'eventId',
    status: 'going',
  })
  console.log('Updated RSVP:', rsvp.status)
}
```

</CodeGroup>
