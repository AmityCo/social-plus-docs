{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: query_comments | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun queryComments() {
        // Query comments for a post
        AmitySocialClient.newCommentRepository()
            .getComments()
            .post("postId")
            .build()
            .query()
            .asLiveData()
    }
```

```swift iOS
var token: AmityNotificationToken?
    
    func commentQueryExample() {
        let queryOptions = AmityCommentQueryOptions(referenceId: "post-id", referenceType: .post, filterByParentId: false, orderBy: .descending, includeDeleted: false)
        token = commentRepository.getComments(with: queryOptions)
            .observe { collection, changes, error in
            // Handle the result
        }
    }
    
    func commentQueryWithPageSizeExample() {
        // Fetch 5 comments per page (useful for reply threads in multi-level comments)
        let queryOptions = AmityCommentQueryOptions(
            referenceId: "post-id",
            referenceType: .post,
            filterByParentId: true,
            parentId: "parent-comment-id",
            orderBy: .ascending,
            includeDeleted: false,
            pageSize: 5
        )
        token = commentRepository.getComments(with: queryOptions)
            .observe { collection, changes, error in
            // Handle the result
        }
    }
```

```dart Flutter
late PagingController<AmityComment> _commentController;

    void queryComments(String postId) {
      _commentController = PagingController(
        pageFuture: (token) => AmitySocialClient.newCommentRepository()
            .getComments()
            .post(postId)
            .orderBy(AmityCommentOrderByOption.lastCreated)
            .getPagingData(token: token, limit: 20),
        pageSize: 20,
      )..addListener(() {
          if (_commentController.error == null) {
            // Update UI with _commentController.loadedItems
          } else {
            // Handle error
          }
        });
    }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function queryComments() {
  const unsub = runQuery(
    CommentRepository.queryComments({
      referenceType: 'post',
      referenceId: 'postId123',
      sortBy: 'lastCreated',
      includeDeleted: false,
    }),
    ({ data: comments, hasNextPage, onNextPage }) => {
      console.log('Comments count:', comments.length);
      if (hasNextPage) onNextPage();
    },
  );
  return unsub;
}
```

</CodeGroup>
