{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: mark_story_as_seen | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun markStoryAsSeen() {
        // Mark a story as seen by the current user
        AmitySocialClient.newStoryRepository()
            .markStoryAsSeen("storyId")
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(io.reactivex.rxjava3.android.schedulers.AndroidSchedulers.mainThread())
            .subscribe({
                // Story marked as seen
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
// storyObject is a type of `AmityStory`.
        storyObject.analytics.markAsSeen()
```

```dart Flutter
void markStoryAsSeen() {
    final storyId = 'storyId';
    AmityCoreClient.newSocialRepository()
        .story()
        .markAsSeen(storyId: storyId)
        .then((_) => print('Story marked as seen'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function markStoryAsSeen() {
  await StoryRepository.markStoriesAsSeen({ storyIds: ['storyId1', 'storyId2'] })
  console.log('Stories marked as seen')
}
```

</CodeGroup>
