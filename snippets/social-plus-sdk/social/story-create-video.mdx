{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: story-create-video | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createVideoStory(targetId: String, targetType: AmityStory.TargetType, videoUri: Uri) {
        AmitySocialClient.newStoryRepository()
            .createVideoStory(
                targetType = targetType,
                targetId = targetId,
                videoUri = videoUri
            )
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let storyRepository = AmityStoryRepository(client: client)
        let options = AmityVideoStoryCreateOptions(targetType: .community, tartgetId: "<community-id>", videoFileURL: URL(string: "file:///path/to/video.mp4")!, metadata: nil, items: [])
        do {
            let post = try await storyRepository.createVideoStory(options: options)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void storyCreateVideo() async {
    final story = await AmitySocialClient.newStoryRepository()
        .createStory()
        .targetCommunity('communityId')
        .video(
          filePath: '/path/to/video.mp4',
          items: [],
        )
        .create();
    print('Video story created: \${story.storyId}');
  }
```

```typescript TypeScript
import { StoryRepository } from '@amityco/ts-sdk'

async function createVideoStory(targetId: string, videoFileId: string) {
  const story = await StoryRepository.createVideoStory({
    targetId,
    targetType: 'community',
    fileId: videoFileId,
    metadata: {},
  })

  console.log('Video story created:', story.storyId)
  return story
}
```

</CodeGroup>
