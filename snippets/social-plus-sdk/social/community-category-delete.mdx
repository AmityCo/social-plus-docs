{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-category-delete | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun deleteCategory(categoryId: String) {
        AmitySocialClient.newCommunityRepository()
            .deleteCategory(categoryId = categoryId)
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let categoryRepository = AmityCommunityCategoryRepository(client: AmityClient.shared)
        categoryRepository.deleteCategory(withId: "categoryId") { success, error in
            if success {
                print("Category deleted successfully")
            } else if let error = error {
                print("Delete failed: \(error)")
            }
        }
```

```dart Flutter
void deleteCategory(String categoryId) {
    AmitySocialClient.newCommunityRepository()
        .deleteCategory(categoryId)
        .then((_) {
          // Category deleted successfully
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function communityCategoryDelete() {
  const categoryId = 'category-id-here';
  await CommunityCategoryRepository.deleteCategory(categoryId);
  console.log('Community category deleted successfully');
}
```

</CodeGroup>
