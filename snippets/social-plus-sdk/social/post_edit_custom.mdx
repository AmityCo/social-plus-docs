{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post_edit_custom | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void postEditCustom() async {
    final postRepository = AmitySocialClient.newPostRepository();
    await postRepository
        .editPost('postId')
        .customData({'key': 'value', 'updated': true})
        .apply();
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function editCustomPost() {
  const updateData = {
    postId: 'postId123',
    data: { customField: 'updatedValue', anotherField: 42 },
    metadata: { updatedAt: new Date().toISOString() },
  };

  const livePost = PostRepository.updatePost(updateData);
  livePost.on('dataUpdated', post => console.log('Custom post updated:', post.postId));
  livePost.on('dataError', err => console.error('Error updating custom post:', err));
}
```

</CodeGroup>
