{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: edit_comment_with_mention_api | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun editCommentWithMentionApi() {
        // Edit a comment and include user mentions
        AmitySocialClient.newCommentRepository()
            .editComment(commentId = "commentId")
            .text("Updated text with @mention")
            .mentionUsers(listOf("userId1", "userId2"))
            .build()
            .update()
            .subscribe()
    }
```

```swift iOS
let options = AmityCommentUpdateOptions(text: "text")
        
        do {
            let comment = try await commentRepository.editComment(withId: "<comment-id>", options: options)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void editCommentWithMentionApi() {
    final metadata = {
      'mentioned': [
        {'type': 'user', 'userId': 'userId', 'index': 8, 'length': 9}
      ]
    };
    AmitySocialClient.newCommentRepository()
        .getComment(commentId: 'commentId')
        .then((AmityComment comment) {
          comment
              .edit()
              .text('Updated @username mention')
              .metadata(metadata)
              .build()
              .apply()
              .then((_) {
                // Comment with mention updated via API
              });
        });
  }
```

```typescript TypeScript
import { CommentRepository } from '@amityco/ts-sdk'

async function editCommentWithMentionApi() {
  const updated = await CommentRepository.editComment({
    commentId: 'commentId',
    data: { text: 'Updating with mention for @user' },
    mentionees: [{ type: 'user', userIds: ['targetUserId'] }],
    metadata: {
      mentioned: [{ type: 'user', userId: 'targetUserId', index: 24, length: 5 }],
    },
  })
  console.log('Comment mention API updated:', updated)
}
```

</CodeGroup>
