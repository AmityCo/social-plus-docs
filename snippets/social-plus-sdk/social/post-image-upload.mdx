{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-image-upload | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun updateImage(image: AmityImage, altText: String) {
        AmityCoreClient.newFileRepository()
            .updateAltText(image.getFileId(), altText)
            .doOnComplete {
                // Complete handling
            }
            .doOnError {
                // Error handling
            }
            .subscribe()
    }
```

```dart Flutter
void uploadImageAndCreatePost(String communityId, File imageFile) {
    AmityCoreClient.newFileRepository()
        .uploadImage(imageFile)
        .stream
        .listen((uploadResult) {
      uploadResult.when(
        progress: (uploadInfo, cancelToken) {
          // track upload progress
        },
        complete: (AmityImage image) {
          AmitySocialClient.newPostRepository()
              .createPost()
              .targetCommunity(communityId)
              .image([image])
              .post()
              .then((AmityPost post) {
                // image post created
              })
              .onError((error, stackTrace) {
                // handle error
              });
        },
        error: (error) {
          // handle upload error
        },
        cancel: () {},
      );
    });
  }
```

</CodeGroup>
