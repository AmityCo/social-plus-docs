{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-category-create | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createCategory(name: String) {
        AmitySocialClient.newCommunityRepository()
            .createCategory(name = name)
            .build()
            .create()
            .doOnSuccess { category: AmityCommunityCategory ->
                // handle created category
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let categoryRepository = AmityCommunityCategoryRepository(client: AmityClient.shared)
        let builder = AmityCommunityCategoryBuilder()
        builder.setName("Technology")
        categoryRepository.createCategory(builder: builder) { category, error in
            if let error = error {
                print("Category creation failed: \(error)")
                return
            }
            print("Category created: \(category?.categoryId ?? "")")
        }
```

```dart Flutter
void createCategory(String categoryName) {
    AmitySocialClient.newCommunityRepository()
        .createCategory(categoryName)
        .build()
        .create()
        .then((AmityCommunityCategory category) {
          final categoryId = category.categoryId;
          final name = category.name;
          // Category created successfully
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function communityCategoryCreate() {
  const category = await CommunityCategoryRepository.createCategory({
    name: 'Technology',
    avatarFileId: 'avatar-file-id',
  });
  console.log('Community category created:', category.categoryId);
}
```

</CodeGroup>
