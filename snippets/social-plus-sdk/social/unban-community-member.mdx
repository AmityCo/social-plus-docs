{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: unban-community-member | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun unbanCommunityMember() {
        // TODO: Implement Unban community member
    }
```

```swift iOS
let moderation = AmityCommunityModeration(client: AmityManager.shared.client!, andCommunityId: "communityId")
        moderation.unban(userId: "userId") { success, error in
            if success {
                print("User unbanned from community")
            } else if let error = error {
                print("Error unbanning user: \(error)")
            }
        }
```

```dart Flutter
void unbanCommunityMember() {
    AmitySocialClient.newCommunityRepository()
        .moderation('communityId')
        .unbanMember('userId')
        .then((_) {
          print('Member unbanned successfully.');
        })
        .onError((error, stackTrace) {
          print('Error: \$error');
        });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function unbanCommunityMember() {
  await CommunityRepository.unbanMembers('communityId', ['userId']);
  console.log('Community member unbanned successfully');
}
```

</CodeGroup>
