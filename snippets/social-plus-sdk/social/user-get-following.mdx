{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-get-following | updated: 2026-05-01 */}

<CodeGroup>

```kotlin Android
fun getFollowing(userId: String) {
        AmitySocialClient.newUserRepository()
            .relationship()
            .getFollowing(userId = userId)
            .build()
            .query()
            .doOnNext { following: PagedList<AmityFollowRelationship> ->
                // handle following list
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let userRepository = AmityUserRepository(client: client)
        var token: AmityNotificationToken?
        token = userRepository.userRelationship.getFollowing(withUserId: "<user-id>").observe { collection, change, error in
            // Handle collection updates
        }
```

```dart Flutter
List<AmityFollowRelationship> _following = [];
  late PagingController<AmityFollowRelationship> _controller;
  final scrollController = ScrollController();

  void getFollowing(String userId) {
    _controller = PagingController(
      pageFuture: (token) => AmityCoreClient.newUserRepository()
          .relationship()
          .getFollowing(userId)
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    )..addListener(() {
        if (_controller.error == null) {
          _following
            ..clear()
            ..addAll(_controller.loadedItems);
        }
      });

    scrollController.addListener(() {
      if (scrollController.position.pixels ==
              scrollController.position.maxScrollExtent &&
          _controller.hasMoreItems) {
        _controller.fetchNextPage();
      }
    });
  }
```

</CodeGroup>
