{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post_image_upload | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void postImageUpload() async {
    final fileRepository = AmityFileRepository();
    final uploadedImage = await fileRepository
        .uploadImage('path/to/image.jpg');
    print('Uploaded image fileId: \${uploadedImage.fileId}');
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function createImagePostUnderscore() {
  const postData = {
    targetId: 'communityId',
    targetType: 'community' as const,
    data: { text: 'Check out this image!' },
    attachments: [{ type: 'image' as const, fileId: 'uploadedFileId' }],
  };

  const livePost = PostRepository.createPost(postData);
  livePost.on('dataUpdated', post => console.log('Image post created:', post.postId));
  livePost.on('dataError', err => console.error('Error creating image post:', err));
}
```

</CodeGroup>
