{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: comment-query | updated: 2026-05-01 */}

<CodeGroup>

```kotlin Android
fun queryCommentsOnPost(
        commentRepository: AmityCommentRepository,
        postId: String,
        commentId: String
    ) {
        // To query for all first level comments, pass parentId = null
        commentRepository.getComments()
            .post(postId = postId) //or .content(contentId)
            .includeDeleted(includeDeleted = true)
            .build()
            .query()
            .doOnNext { pagingData: PagingData<AmityComment> ->
                //results
            }
            .subscribe()
        //
        // To query for replies on a comment, pass the commentId as a parentId
        commentRepository.getComments()
            .post(postId = postId) //or .content(contentId)
            .parentId(parentId = commentId)
            .includeDeleted(includeDeleted = true)
            .build()
            .query()
            .doOnNext { pagingData: PagingData<AmityComment> ->
                // results
            }
            .subscribe()

        //  To query with match exact data types filter
        val filterMatchExactDataTypes = AmityCommentDataTypeFilter.Exact(
            dataTypes = listOf(AmityComment.DataType.IMAGE)
        )
        commentRepository.getComments()
            .post(postId = postId)
            .dataTypes(filterMatchExactDataTypes)
            .build()
            .query()

        //  To query with match any data types filter
        val filterMatchAnyDataTypes = AmityCommentDataTypeFilter.Any(
            dataTypes = listOf(AmityComment.DataType.IMAGE, AmityComment.DataType.TEXT)
        )
        commentRepository.getComments()
            .post(postId = postId)
            .dataTypes(filterMatchAnyDataTypes)
            .build()
            .query()

    }

    fun queryCommentsOnStory(
        commentRepository: AmityCommentRepository,
        storyId: String,
        commentId: String
    ) {
        // To query for all first level comments, pass parentId = null
        commentRepository.getComments()
            .story(storyId = storyId) //or .content(contentId)
            .includeDeleted(includeDeleted = true)
            .build()
            .query()
            .doOnNext { pagingData: PagingData<AmityComment> ->
                //results
            }
            .subscribe()
        //
        // To query for replies on a comment, pass the commentId as a parentId
        commentRepository.getComments()
            .story(storyId = storyId) //or .content(contentId)
            .parentId(parentId = commentId)
            .includeDeleted(includeDeleted = true)
            .build()
            .query()
            .doOnNext { pagingData: PagingData<AmityComment> ->
                // results
            }
            .subscribe()

        //  To query with match exact data types filter
        val filterMatchExactDataTypes = AmityCommentDataTypeFilter.Exact(
            dataTypes = listOf(AmityComment.DataType.IMAGE)
        )
        commentRepository.getComments()
            .story(storyId = storyId)
            .dataTypes(filterMatchExactDataTypes)
            .build()
            .query()

        //  To query with match any data types filter
        val filterMatchAnyDataTypes = AmityCommentDataTypeFilter.Any(
            dataTypes = listOf(AmityComment.DataType.IMAGE, AmityComment.DataType.TEXT)
        )
        commentRepository.getComments()
            .story(storyId = storyId)
            .dataTypes(filterMatchAnyDataTypes)
            .build()
            .query()

    }

    // Query with custom page size (e.g., 5 for reply thread batches)
    fun queryCommentsWithPageSize(
        commentRepository: AmityCommentRepository,
        postId: String,
        parentCommentId: String
    ) {
        // Load reply threads in batches of 5 using Paging 3
        commentRepository.getComments()
            .post(postId = postId)
            .parentId(parentId = parentCommentId)
            .includeDeleted(includeDeleted = false)
            .pageSize(5) // 5 replies per page
            .build()
            .query()
            .doOnNext { pagingData: PagingData<AmityComment> ->
                // results — Paging 3 handles pagination automatically
            }
            .subscribe()
    }
```

```swift iOS
let commentRepository = AmityCommentRepository(client: client)
        var token: AmityNotificationToken?
        token = commentRepository.getComments(referenceId: "<post-id>", referenceType: .post, filterByParentId: false, parentId: nil, orderBy: .descending, includeDeleted: false)
            .observe { collection, change, error in
                // Handle collection updates
            }
```

```dart Flutter
final amityComments = <AmityComment>[];
  late PagingController<AmityComment> _commentController;

  // To query for all first level comments without parentId
  void queryComments(String postId) {
    _commentController = PagingController(
      pageFuture: (token) => AmitySocialClient.newCommentRepository()
          .getComments()
          .post(postId)
          .includeDeleted(true) //optional
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    )..addListener(
        () {
          if (_commentController.error == null) {
            //handle results, we suggest to clear the previous items
            //and add with the latest _controller.loadedItems
            amityComments.clear();
            amityComments.addAll(_commentController.loadedItems);
            //update widgets
          } else {
            //error on pagination controller
            //update widgets
          }
        },
      );
  }

  final amityRepliedComments = <AmityComment>[];
  late PagingController<AmityComment> _repliedCommentController;

  // To query for replies on a comment, pass the commentId as a parentId
  void queryRepliedComments(String postId, String commentParentId) {
    _repliedCommentController = PagingController(
      pageFuture: (token) => AmitySocialClient.newCommentRepository()
          .getComments()
          .post(postId)
          .parentId(commentParentId)
          .includeDeleted(true) //optional
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    )..addListener(
        () {
          if (_repliedCommentController.error == null) {
            //handle results, we suggest to clear the previous items
            //and add with the latest _controller.loadedItems
            amityComments.clear();
            amityComments.addAll(_repliedCommentController.loadedItems);
            //update widgets
          } else {
            //error on pagination controller
            //update widgets
          }
        },
      );
  }
```

```typescript TypeScript
import { CommentRepository } from '@amityco/ts-sdk'

async function queryComments() {
  const liveCollection = CommentRepository.getComments({
    referenceType: 'post',
    referenceId: 'postId',
    sortBy: 'lastCreated',
  })
  liveCollection.on('dataUpdated', (comments) => {
    console.log(comments)
  })
}
```

</CodeGroup>
