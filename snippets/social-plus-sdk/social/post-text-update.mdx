{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-text-update | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun updateTextPost(
        postRepository: AmityPostRepository,
        post: AmityPost
    ) {
        postRepository.editPost(postId = post.getPostId())
                .text(text = "hello!")
                .build()
                .apply()
                .subscribe()

    }
```

```dart Flutter
void updatePostText(String postId, String newText) {
    AmitySocialClient.newPostRepository()
        .editPost(postId: postId)
        .text(newText)
        .edit()
        .then((AmityPost updatedPost) {
          // post text updated successfully
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

</CodeGroup>
