{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: comment_flag | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun commentFlag() {
        AmitySocialClient.newCommentRepository()
            .flagComment("commentId")
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(AndroidSchedulers.mainThread())
            .subscribe({
                // comment flagged
            }, { error ->
                error.printStackTrace()
            })
    }
```

```dart Flutter
void commentFlag() async {
    final commentRepo = AmitySocialClient.newCommentRepository();
    final comment = await commentRepo.getComment('commentId').first;
    await comment.report().flag();
    print('Comment flagged');
  }
```

```typescript TypeScript
import { CommentRepository } from '@amityco/ts-sdk'

async function commentFlag(commentId: string) {
  await CommentRepository.flagComment(commentId)
  console.log('Comment flagged')
}
```

</CodeGroup>
