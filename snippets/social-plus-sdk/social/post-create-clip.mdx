{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-create-clip | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun postCreateClip() {
        // Create a text post targeting the current user's feed
        AmitySocialClient.newPostRepository()
            .createPost()
            .targetMe()
            .text("Hello from Social Plus SDK!")
            .build()
            .subscribe({ post ->
                // Post created successfully
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
let fileRepository = AmityFileRepository(client: AmityClient.shared)
        let videoURL = URL(fileURLWithPath: "/path/to/clip.mp4")
        fileRepository.uploadVideo(fromLocalURL: videoURL, progress: { progress in
            print("Upload progress: \(progress * 100)%")
        }) { videoData, error in
            if let error = error {
                print("Video upload failed: \(error.localizedDescription)")
                return
            }
            guard let videoData = videoData else { return }
            let postRepository = AmityPostRepository(client: AmityClient.shared)
            let builder = AmityVideoPostBuilder()
            builder.setVideos([videoData])
            postRepository.createPost(builder, targetId: nil, targetType: .user) { post, error in
                if let error = error {
                    print("Failed to create clip post: \(error.localizedDescription)")
                    return
                }
                print("Clip post created: \(post?.postId ?? "")")
            }
        }
```

```dart Flutter
void createClipPost(String communityId, AmityVideo clip) {
    AmitySocialClient.newPostRepository()
        .createPost()
        .targetCommunity(communityId: communityId)
        .video([clip])
        .build()
        .post()
        .then((AmityPost post) {
          // clip post created
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function postCreateClip() {
  const post = await PostRepository.createPost({
    targetId: 'userId123',
    targetType: 'user',
    data: { text: 'My new clip' },
    dataType: 'text',
    attachments: [{ fileId: 'clipFileId123', type: 'clip' }],
  });
  console.log('Clip post created:', post.postId);
}
```

</CodeGroup>
