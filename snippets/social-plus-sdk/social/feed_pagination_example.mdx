{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: feed_pagination_example | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun feedPaginationExample() {
        // Paginate through global news feed posts
        AmitySocialClient.newFeedRepository()
            .getGlobalFeed()
            .build()
            .query()
            .subscribe { liveCollection -> /* handle paged results */ }
    }
```

```dart Flutter
void feedPaginationExample() {
    final feedRepo = AmitySocialClient.newFeedRepository();
    String? nextPageToken;
    final result = await feedRepo
        .getMyFeed()
        .getPagingData(token: nextPageToken, limit: 20);
    nextPageToken = result.token;
    print('Posts: \${result.data.length}, hasMore: \${result.token != null}');
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function feedPaginationExample() {
  const params = { targetType: 'globalFeed' as const, limit: 10 };
  const liveCollection = runQuery(PostRepository.getPosts(params));
  liveCollection.on('dataUpdated', (posts) => {
    console.log('Current page:', posts);
  });
  if (liveCollection.hasNextPage) {
    liveCollection.nextPage();
  }
}
```

</CodeGroup>
