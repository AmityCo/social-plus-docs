{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-relationship-decline-follow | updated: 2026-04-30 */}

<CodeGroup>

```kotlin Android
fun declineFollow(userId: String) {
        AmityCoreClient.newUserRepository()
            .relationship()
            .declineMyFollower(userId = userId)
            .doOnComplete {
                // success
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let userRepository = AmityUserRepository(client: client)
        do {
            try await userRepository.userRelationship.declineMyFollower(withUserId: "<user-id>")
            print("Follow request declined")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void declineFollowRequest(String userId) {
    AmityCoreClient.newUserRepository()
        .relationship()
        .decline(userId)
        .then((value) => {
              //success
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function declineFollowRequest() {
  await UserRepository.declineFollowRequest('userId')
}
```

</CodeGroup>
