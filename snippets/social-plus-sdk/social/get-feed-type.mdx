{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-feed-type | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getFeedType(feedRepository: AmityFeedRepository) {
        feedRepository
            .getGlobalFeed()
            .build()
            .query()
            .doOnNext { posts ->
                // PagingData<AmityPost>
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
// Determine feed type from a feed collection
        let feedRepository = AmityFeedRepository(client: AmityManager.shared.client!)
        var token: AmityNotificationToken?
        let collection = feedRepository.getGlobalFeed()
        token = collection.observe { liveCollection, _, error in
            if let error = error {
                print("Error loading feed: \(error.localizedDescription)")
                return
            }
            print("Feed type: global, posts count: \(liveCollection.count())")
        }
```

```dart Flutter
void getFeedType() {
    // Get global feed
    final globalFeed = AmitySocialClient.newFeedRepository()
        .getGlobalFeed()
        .getPagingData();

    // Get community feed
    final communityFeed = AmitySocialClient.newFeedRepository()
        .getCommunityFeed(communityId: 'communityId')
        .getPagingData();

    globalFeed.asStream().listen((posts) => print('Global: \${posts.length}'));
    communityFeed.asStream().listen((posts) => print('Community: \${posts.length}'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function getFeedType() {
  const liveObject = runQuery(PostRepository.postForPostId('postId123'));
  liveObject.on('dataUpdated', (post) => {
    console.log('Feed type:', post.feedType);
  });
}
```

</CodeGroup>
