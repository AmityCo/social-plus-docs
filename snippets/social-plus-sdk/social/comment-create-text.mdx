{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: comment-create-text | updated: 2026-04-30 */}

<CodeGroup>

```kotlin Android
fun createTextComment(postId: String, text: String) {
        AmitySocialClient.newCommentRepository()
            .createComment()
            .post(postId = postId)
            .with()
            .text(text = text)
            .build()
            .comment()
            .doOnSuccess { comment: AmityComment ->
                // handle created comment
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let commentRepository = AmityCommentRepository(client: client)
        let options = AmityCommentCreateOptions(referenceId: "<post-id>", referenceType: .post, text: "Great post!", parentId: nil)
        do {
            let comment = try await commentRepository.createComment(with: options)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void createComment(String postId) {
    AmitySocialClient.newCommentRepository()
        .createComment()
        .post(postId)
        .create()
        .text('Great post!')
        .send()
        .then((AmityComment comment) {
          // handle result
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

</CodeGroup>
