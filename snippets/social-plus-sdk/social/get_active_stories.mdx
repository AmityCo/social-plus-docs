{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_active_stories | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getActiveStories() {
        // Query currently active stories
        AmitySocialClient.newStoryRepository()
            .getActiveStories(targetId = "communityId", targetType = AmityStoryTargetType.COMMUNITY)
            .subscribe { stories -> /* handle active stories */ }
    }
```

```swift iOS
token = storyRepository.getActiveStoriesByTarget(targetType: .community, targetId: "targetId", sortOption: .firstCreated).observe({ collection, _, error in
            if let error {
                print(error)
                return
            }
            
            for story in collection.snapshots {
                // Get list of story in the collection
                print(story)
            }
        })
```

```dart Flutter
void getActiveStories() {
    AmitySocialClient.newStoryRepository()
        .getActiveStories(targetId: 'communityId', targetType: AmityStoryTargetType.COMMUNITY)
        .asStream()
        .listen((stories) {
      for (final story in stories) {
        print('Story: \${story.storyId}');
      }
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function getActiveStories() {
  const liveCollection = StoryRepository.getActiveStories({ targetType: 'community', targetId: 'communityId123' });
  liveCollection.on('dataUpdated', (stories) => {
    console.log('Active stories:', stories.map((s: any) => s.storyId));
  });
}
```

</CodeGroup>
