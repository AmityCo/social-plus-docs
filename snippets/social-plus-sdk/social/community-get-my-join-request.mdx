{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-get-my-join-request | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun communityGetMyJoinRequest(communityId: String) {
        AmitySocialClient.newCommunityRepository()
            .getMyJoinRequest(communityId = communityId)
            .subscribe(
                { request -> println("Join request status: ${request?.getStatus()}") },
                { error -> println("Error: ${error.message}") }
            )
    }
```

```swift iOS
let memberRepository = AmityCommunityMembershipRepository(client: AmityClient.shared)
        let currentUserId = AmityClient.shared.currentUserId ?? ""
        let token = memberRepository.getJoinRequest(
            userId: currentUserId,
            communityId: "communityId"
        ).observe { liveObject, error in
            guard let request = liveObject.object else { return }
            print("My join request status: \(request.status)")
        }
```

```dart Flutter
void getMyJoinRequest(String communityId) {
    AmitySocialClient.newCommunityRepository()
        .getMyJoinRequest(communityId: communityId)
        .then((AmityCommunityJoinRequest? request) {
          if (request != null) {
            final status = request.status;
            final createdAt = request.createdAt;
          } else {
            // No pending join request
          }
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function getMyJoinRequest() {
  const liveObject = CommunityRepository.getMyJoinRequest('communityId')
  liveObject.on('dataUpdated', (joinRequest) => {
    console.log(joinRequest)
  })
}
```

</CodeGroup>
