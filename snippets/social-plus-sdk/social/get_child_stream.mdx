{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_child_stream | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getChildStream() {
        // Retrieve child streams of a live stream
        AmityVideoClient.newStreamRepository()
            .getChildStreams(streamId = "streamId")
            .subscribe { streams -> /* handle child streams */ }
    }
```

```dart Flutter
void getChildStream() {
    // TODO: implement SDK operation
    throw UnimplementedError();
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function getChildStream() {
  const liveCollection = StreamRepository.getStreams({ parentStreamId: 'parentStreamId123' });
  liveCollection.on('dataUpdated', (streams) => {
    console.log('Child streams:', streams.map((s: any) => s.streamId));
  });
}
```

</CodeGroup>
