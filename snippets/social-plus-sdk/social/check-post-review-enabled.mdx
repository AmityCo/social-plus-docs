{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: check-post-review-enabled | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun checkPostReviewEnabled(communityId: String) {
        AmitySocialClient.newCommunityRepository()
            .getCommunity(communityId = communityId)
            .doOnSuccess { community: AmityCommunity ->
                val isReviewEnabled = community.isPostReviewEnabled()
            }
            .doOnError { }
            .subscribe()
    }
```

```swift iOS
community.postSettings
```

```dart Flutter
void checkPostReviewEnabled(AmityCommunity community) {
    final postSettings = community.postSettings;
    // postSettings indicates whether admin review is required before posts are published
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function checkPostReviewEnabled() {
  const communityId = 'community-id-here';
  const liveObject = CommunityRepository.getCommunity(communityId);
  liveObject.on('dataUpdated', (community) => {
    console.log('Post review enabled:', community.isPostReviewEnabled);
  });
}
```

</CodeGroup>
