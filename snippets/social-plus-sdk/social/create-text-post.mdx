{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create-text-post | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createTextPost(communityId: String, text: String) {
        val target = AmityPost.Target.COMMUNITY.create(communityId = communityId)
        AmitySocialClient.newPostRepository()
            .createTextPost(target = target, text = text)
            .doOnSuccess { post: AmityPost ->
                // AmityPost created
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let postRepository = AmityPostRepository()
        let builder = AmityTextPostBuilder()
        builder.setText("This is my text post content")
        postRepository.createPost(builder, targetId: "communityId", targetType: .community) { post, error in
            if let error = error {
                print("Failed to create text post: \(error.localizedDescription)")
            } else {
                print("Text post created: \(post?.postId ?? "")")
            }
        }
```

```dart Flutter
void createTextPost() {
    AmitySocialClient.newPostRepository()
        .createPost()
        .targetMe()
        .text('Hello World')
        .build()
        .post()
        .then((AmityPost post) {
          final postId = post.postId;
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function createTextPost() {
  const post = await PostRepository.createPost({
    targetType: 'community',
    targetId: 'communityId',
    dataType: 'text',
    data: { text: 'This is a text post' },
  })
  console.log('Text post created:', post)
}
```

</CodeGroup>
