{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-review | updated: 2026-05-02 */}

<CodeGroup>

```swift iOS
let moderationRepository = AmityPostModerationRepository()
        moderationRepository.approvePost(postId: "postId") { error in
            if let error = error {
                print("Error approving post: \(error)")
            } else {
                print("Post approved")
            }
        }
```

```dart Flutter
void postReview() async {
    final postRepository = AmitySocialClient.newPostRepository();
    // Approve a pending post
    await postRepository
        .approvePost('postId');
    // Or decline a pending post
    // await postRepository.declinePost('postId');
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function reviewPost() {
  const approve = async (postId: string) => {
    await PostRepository.approvePost({ postId });
    console.log('Post approved:', postId);
  };

  const decline = async (postId: string) => {
    await PostRepository.declinePost({ postId });
    console.log('Post declined:', postId);
  };

  await approve('postId123');
  await decline('postId456');
}
```

</CodeGroup>
