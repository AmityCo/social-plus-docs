{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: event-rsvp-create | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun eventRsvpCreate(eventId: String, status: AmityEventRsvpStatus) {
        AmitySocialClient.newEventRepository()
            .createRsvp(eventId = eventId, status = status)
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let eventRepository = AmityEventRepository()
        eventRepository.createRsvp(eventId: "eventId", status: .going) { rsvp, error in
            if let error = error {
                print("Failed to create RSVP: \(error.localizedDescription)")
            } else {
                print("RSVP created with status: \(rsvp?.status.rawValue ?? "")")
            }
        }
```

```dart Flutter
void rsvpEvent(String eventId, AmityEventRsvpStatus status) {
    AmitySocialClient.newEventRepository()
        .rsvp(eventId)
        .setStatus(status)
        .submit()
        .then((value) {
          // RSVP submitted successfully
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { EventRepository } from '@amityco/ts-sdk'

async function rsvpEvent() {
  await EventRepository.setRsvp('eventId', 'going')
}
```

</CodeGroup>
