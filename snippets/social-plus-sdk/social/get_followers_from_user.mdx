{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_followers_from_user | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getFollowersFromUser() {
        // Query a user's follower list
        AmitySocialClient.newFollowRepository()
            .getFollowers(userId = "userId")
            .build()
            .query()
            .subscribe { followers -> /* handle followers */ }
    }
```

```dart Flutter
void getFollowersFromUser() {
    AmitySocialClient.newUserRepository()
        .relationship()
        .user('userId')
        .getFollowers()
        .getPagingData()
        .listen((followers) {
      for (final follower in followers) {
        print('Follower: \${follower.sourceUserId}');
      }
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function getFollowersFromUser() {
  const liveCollection = UserRepository.getFollowers('userId123');
  liveCollection.on('dataUpdated', (followers) => {
    console.log('Followers:', followers.map((f: any) => f.displayName));
  });
}
```

</CodeGroup>
