{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: pin-get-live-pinned-posts | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun pinGetLivePinnedPosts() {
        // Query live posts as a live collection
        AmitySocialClient.newPostRepository()
            .getPosts()
            .targetMe()
            .build()
            .query()
            .subscribe({ pagingData ->
                // Observe live post updates
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
let postRepository = AmityPostRepository(client: client)
        var token: AmityNotificationToken?
        token = postRepository.getPinnedPosts(communityId: "<community-id>").observe { collection, change, error in
            guard error == nil else { return }
            print("Pinned posts: \(collection.count())")
        }
```

```dart Flutter
void pinGetLivePinnedPosts() async {
        AmitySocialClient.newCommunityRepository()
            .getPinnedPosts(communityId: 'communityId')
            .asStream()
            .listen((posts) {
              print('Pinned posts: \${posts.length}');
              for (final post in posts) {
                print('Pinned post: \${post.postId}');
              }
            });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function pinGetLivePinnedPosts() {
  const liveCollection = PostRepository.getPinnedPosts({ targetId: 'communityId123', targetType: 'community' });
  liveCollection.on('dataUpdated', (posts) => {
    console.log('Pinned posts:', posts);
  });
}
```

</CodeGroup>
