{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-comment-creation | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun postCommentCreation() {
        // Perform the operation using the appropriate SDK repository
        AmitySocialClient.newPostRepository()
            .getPosts()
            .targetMe()
            .build()
            .query()
            .subscribe({ pagingData ->
                // Handle live collection updates
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
let commentRepository = AmityCommentRepository(client: AmityClient.shared)
        let postId = "post_id"
        let builder = AmityCommentCreationDataBuilder()
        builder.setText("This is a comment on the post.")
        commentRepository.createComment(
            withReferenceId: postId,
            referenceType: .post,
            parentId: nil,
            dataBuilder: builder
        ) { comment, error in
            if let error = error {
                print("Failed to create comment: \(error.localizedDescription)")
                return
            }
            print("Comment created: \(comment?.commentId ?? "")")
        }
```

```dart Flutter
void postCommentCreation() async {
    await AmitySocialClient.newCommentRepository()
        .createComment()
        .post('postId')
        .create(text: 'Great post!');
    print('Comment created');
  }
```

```typescript TypeScript
import { CommentRepository } from '@amityco/ts-sdk'

async function postCommentCreation(postId: string, text: string) {
  const comment = await CommentRepository.createComment({
    referenceType: 'post',
    referenceId: postId,
    dataType: 'text',
    data: { text },
  })
  console.log('Comment created:', comment.commentId)
  return comment
}
```

</CodeGroup>
