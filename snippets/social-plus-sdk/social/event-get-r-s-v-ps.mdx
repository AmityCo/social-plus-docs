{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: event-get-r-s-v-ps | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun eventGetRSVPs(eventId: String) {
        AmitySocialClient.newEventRepository()
            .getEventRsvps(eventId = eventId)
            .build()
            .query()
            .doOnNext { rsvps: PagingData<AmityEventRsvp> ->
                // handle RSVPs
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let eventRepository = AmityEventRepository()
        let collection = eventRepository.getRsvps(eventId: "eventId")
        collection.observe { liveCollection, _, error in
            guard error == nil else { return }
            let rsvps = liveCollection.allObjects()
            print("RSVPs for event: \(rsvps.count)")
        }
```

```typescript TypeScript
import { EventRepository } from '@amityco/ts-sdk'

function watchRSVPs() {
  const unsubscribe = EventRepository.getRSVPs(
    { eventId: 'eventId' },
    ({ data: rsvps }) => {
      console.log(rsvps)
    },
  )
  return unsubscribe
}
```

</CodeGroup>
