{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-member-unban | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun communityMemberUnban() {
        // TODO: implement community member unban using Social Plus SDK
    }
```

```swift iOS
func communityMemberUnban() {
        let client = AmityClient.shared
        let communityModeration = AmityCommunityModeration(client: client, communityId: "community_id")
        communityModeration.unbanMembers(["user_id"]) { success, error in
            if success {
                print("Community member unbanned")
            }
        }
    }
```

```dart Flutter
void communityMemberUnban() {
    // Unban a previously banned community member
    AmitySocialClient.newCommunityRepository()
        .moderation('communityId')
        .unbanMembers(userIds: ['userId'])
        .then((_) => print('Member unbanned'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function communityMemberUnban() {
  const client = createClient('YOUR_API_KEY')
  await client.communityRepository.unbanMembers('communityId123', ['userId1'])
  console.log('Member unbanned successfully')
}
```

</CodeGroup>
