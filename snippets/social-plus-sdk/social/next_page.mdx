{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: next_page | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun nextPage() {
        // Execute next page operation
        // Obtain the next page token from the current live collection
        val liveCollection = AmitySocialClient.newPostRepository()
            .getPosts()
            .targetGlobal()
            .build()
            .query()
        // Call loadNextPage() on the PagingData to load more items
        liveCollection.subscribe { pagingData ->
            // adapter.submitData(pagingData)
        }
    }
```

```dart Flutter
void nextPage() {
    // Using PagingController for paginated live collections
    final pagingController = PagingController<AmityPost>(
      pageSize: 20,
    );
    pagingController.fetchNextPage();
    print('Next page fetched');
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function nextPage() {
  let onNextPage: (() => void) | undefined
  
  const liveCollection = PostRepository.getPosts(
    { targetId: 'communityId', targetType: 'community' },
    ({ data: posts, loading, onNextPage: nextPageFn, hasNextPage }) => {
      onNextPage = nextPageFn
      console.log('Posts loaded:', posts.length, 'Has more:', hasNextPage)
    },
  )
  
  // Load the next page when needed
  onNextPage?.()
}
```

</CodeGroup>
