{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-membership-query | updated: 2026-05-01 */}

<CodeGroup>

```kotlin Android
fun queryMembers(communityId: String) {
        AmitySocialClient.newCommunityRepository()
            .membership(communityId = communityId)
            .getMembers()
            .build()
            .query()
            .doOnNext { members: PagingData<AmityCommunityMember> ->
                // PagingData<AmityCommunityMember>
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```dart Flutter
final _members = <AmityCommunityMember>[];
  late PagingController<AmityCommunityMember> _controller;

  void queryCommunityMembers(String communityId) {
    _controller = PagingController(
      pageFuture: (token) => AmitySocialClient.newCommunityRepository()
          .membership(communityId)
          .getMembers()
          .filter(AmityCommunityMembershipFilter.MEMBER)
          .sortBy(AmityCommunityMembershipSortOption.LAST_CREATED)
          .includeDeleted(false)
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    )..addListener(
        () {
          if (_controller.error == null) {
            _members.clear();
            _members.addAll(_controller.loadedItems);
            //update widgets
          } else {
            //error on pagination controller
          }
        },
      );
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function queryCommunityMembers() {
  const liveCollection = CommunityRepository.queryMembers({
    communityId: 'communityId',
    sortBy: 'lastCreated',
    roles: [],
  })
  liveCollection.on('dataUpdated', (members) => {
    console.log(members)
  })
}
```

</CodeGroup>
