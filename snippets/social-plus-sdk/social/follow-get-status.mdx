{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: follow-get-status | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void getFollowStatus(String userId) {
    AmityCoreClient.newUserRepository()
        .relationship()
        .getFollowInfo(userId: userId)
        .then((AmityUserFollowInfo followInfo) {
          final status = followInfo.status;
          // status reflects the follow state: NONE, PENDING, ACCEPTED
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

function watchFollowStatus() {
  const unsubscribe = UserRepository.getFollowInfo('userId', ({ data: followInfo }) => {
    console.log('Follow status:', followInfo.status)
  })
  return unsubscribe
}
```

</CodeGroup>
