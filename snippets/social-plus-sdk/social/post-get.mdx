{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-get | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun postGet() {
        // TODO: implement post get using Social Plus SDK
    }
```

```swift iOS
func postGet() {
        let feedRepository = AmityFeedRepository(client: AmityManager.shared.client!)
        let liveObject = feedRepository.getPost(withPostId: "postId")
        
        let token = liveObject.observe { post, error in
            guard let post = post.object else { return }
            print("Post: \(post.postId), text: \(post.data?["text"] as? String ?? "")")
        }
        // Retain token as long as observation is needed
    }
```

```dart Flutter
void getPost(String postId) {
    AmitySocialClient.newPostRepository()
        .getPost(postId)
        .then((AmityPost post) {
      //handle result
    }).onError<AmityException>((error, stackTrace) {
      //handle error
    });
  }

  //example of using AmityPost with StreamBuilder
  void observePost(AmityPost post) {
    StreamBuilder<AmityPost>(
        stream: post.listen.stream,
        builder: (context, snapshot) {
          // update widget
          // eg. widget.text = post.postId
          return Container();
        });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

function getPost(postId: string) {
  const disposer = PostRepository.getPost(postId, ({ data: post, loading, error }) => {
    if (loading) return
    if (error) {
      console.error('Failed to fetch post', error)
      return
    }
    console.log('Post:', post)
  })

  return disposer
}
```

</CodeGroup>
