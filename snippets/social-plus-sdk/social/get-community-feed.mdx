{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-community-feed | updated: 2026-05-02 */}

<CodeGroup>

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

function getCommunityFeed() {
  const liveCollection = PostRepository.getPosts({
    targetType: 'community',
    targetId: 'communityId',
    sortBy: 'lastCreated',
  })

  liveCollection.on('dataUpdated', (posts) => {
    console.log('Community posts:', posts)
  })

  liveCollection.on('dataError', (error) => {
    console.error('Failed to get community feed:', error)
  })

  return () => liveCollection.dispose()
}
```

</CodeGroup>
