{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-feed-permission-check | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun checkCommunityReviewPermission(
        community: AmityCommunity
    ) {
        val permission: AmityPermission = AmityPermission.CREATE_PRIVILEGED_POST
        AmityCoreClient.hasPermission(permission = permission)
            .atCommunity(communityId = community.getCommunityId())
            .check()
            .doOnNext { hasPermission: Boolean ->
                // Boolean
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```dart Flutter
void checkCommunityFeedPermission(String communityId) {
    AmityCoreClient.hasPermission(AmityPermission.createPrivilegedPost)
        .atCommunity(communityId: communityId)
        .check()
        .then((bool hasPermission) {
          if (hasPermission) {
            // User can create privileged posts in this community
          } else {
            // User does not have this permission
          }
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }
```

</CodeGroup>
