{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-story-tab-feed | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun communityStoryTabFeed(communityId: String) {
        AmitySocialClient.newStoryRepository()
            .getGlobalStoryTargets()
            .subscribe(
                { targets -> println("Story targets: ${targets.size}") },
                { error -> println("Error: ${error.message}") }
            )
    }
```

```swift iOS
let storyRepository = AmityStoryRepository()
        let collection = storyRepository.getGlobalStoryTargets(options: .smart)
        collection.observe { liveCollection, _, error in
            guard error == nil else { return }
            let targets = liveCollection.allObjects()
            print("Story targets: \(targets.count)")
        }
```

```dart Flutter
void communityStoryTabFeed() {
    final liveCollection = AmitySocialClient.newStoryRepository()
        .getActiveStoriesByTargetIds(targetType: AmityStoryTargetType.community, targetIds: ['communityId'])
        .getLiveCollection();
    liveCollection.observe().listen((targets) { });
  }
```

```typescript TypeScript
import { StoryRepository } from '@amityco/ts-sdk'

function getCommunityStoryTabFeed(communityIds: string[]) {
  const liveCollection = StoryRepository.getGlobalStoryTargets(
    { seenState: 'all', sortBy: 'lastCreated' },
    ({ data: targets, loading, error }) => {
      if (error) { console.error(error); return; }
      if (!loading) {
        targets.forEach(target => {
          console.log('Target:', target.targetId, 'Has unseen:', target.hasUnseen);
        });
      }
    }
  );
  return liveCollection;
}
```

</CodeGroup>
