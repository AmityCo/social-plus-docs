{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-invitation-handle | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun invitationNetworkSetting() {
        AmitySocialClient.getSettings()
            .doOnNext { socialSettings ->
                val membershipAcceptanceType = socialSettings.getMembershipAcceptanceType()
                when (membershipAcceptanceType) {
                    AmityMembershipAcceptanceType.AUTOMATIC -> {}
                    AmityMembershipAcceptanceType.INVITATION -> {}
                    AmityMembershipAcceptanceType.UNKNOWN -> {}
                }
            }
            .doOnError { error ->
                // Handle error
            }
            .subscribe()
    }
```

```swift iOS
let memberRepository = AmityCommunityMembershipRepository(client: AmityClient.shared)
        memberRepository.acceptInvitation(
            withCommunityId: "communityId"
        ) { success, error in
            if success {
                print("Invitation accepted")
            } else {
                memberRepository.declineInvitation(withCommunityId: "communityId") { success, error in
                    print("Invitation declined: \(success)")
                }
            }
        }
```

```dart Flutter
void createInvitation(String communityId, List<String> userIds) {
    AmitySocialClient.newCommunityRepository()
        .createInvitations(communityId: communityId, userIds: userIds)
        .then((_) {
          // Invitations sent successfully
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }

  void acceptInvitation(String communityId) {
    AmitySocialClient.newCommunityRepository()
        .acceptInvitation(communityId: communityId)
        .then((_) {
          // Invitation accepted - user is now a member
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }

  void rejectInvitation(String communityId) {
    AmitySocialClient.newCommunityRepository()
        .declineInvitation(communityId: communityId)
        .then((_) {
          // Invitation rejected successfully
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function communityInvitationHandle() {
  const client = createClient('YOUR_API_KEY')
  const invitationId = 'invitationId123'
  const shouldAccept = true
  if (shouldAccept) {
    await client.communityRepository.acceptInvitation(invitationId)
    console.log('Invitation accepted')
  } else {
    await client.communityRepository.declineInvitation(invitationId)
    console.log('Invitation declined')
  }
}
```

</CodeGroup>
