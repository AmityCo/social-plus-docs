{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-query-blocked | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun userQueryBlocked() {
        // Query live collection for user-query-blocked
        AmityCoreClient.newUserRepository()
            .relationship()
            .blockUser("userId")
            .subscribe({ /* blocked */ }, { error -> })
    }
```

```swift iOS
let client = AmityClient.shared
        let userRepository = AmityUserRepository(client: client)
        let collection = userRepository.getBlockedUsers()
        print("Blocked users collection: \(collection)")
```

```dart Flutter
void userQueryBlocked() {
    AmitySocialClient.newUserRepository()
        .relationship
        .getBlockedUsers()
        .getPagingData()
        .listen((result) {
          for (final user in result.data) {
            print('Blocked: \${user.displayName}');
          }
        })
        ..onError((error) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function queryBlockedUsers() {
  const { data: users, total } = await UserRepository.queryBlockedUsers({ limit: 10 })
  console.log(users, total)
}
```

</CodeGroup>
