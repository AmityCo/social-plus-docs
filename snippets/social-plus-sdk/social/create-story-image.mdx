{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create-story-image | updated: 2026-05-02 */}

<CodeGroup>

```typescript TypeScript
import { StoryRepository } from '@amityco/ts-sdk'

async function createImageStory(communityId: string, imageFile: File) {
  const formData = new FormData()
  formData.append('file', imageFile)

  const metadata = {
    url: 'https://example.com',
  }

  const { data: story } = await StoryRepository.createImageStory(
    'community',
    communityId,
    formData,
    metadata,
    'fit',
  )

  console.log('Image story created:', story.storyId)
  return story
}
```

</CodeGroup>
