{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: cancel_join_request | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun cancelJoinRequest() {
        AmitySocialClient.newCommunityRepository()
            .membership("communityId")
            .cancelJoinRequest()
            .subscribe()
    }
```

```swift iOS
let communityRepository = AmitySocialClient.newCommunityRepository()
        communityRepository.cancelJoinRequest(communityId: "communityId") { success, error in
            if let error = error { print("Error cancelling join request: \(error)"); return }
            print("Join request cancelled")
        }
```

```dart Flutter
void cancelJoinRequest() {
    // Cancel a pending request to join a community
    AmitySocialClient.newCommunityRepository()
        .leaveCommunity('communityId')
        .then((_) => print('Join request cancelled'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function cancelJoinRequest() {
  const client = createClient('apiKey', 'region')
  await client.login({ userId: 'user123', displayName: 'User 123' })
  await client.community.cancelJoinRequest({ communityId: 'community123' })
  console.log('Join request cancelled')
}
```

</CodeGroup>
