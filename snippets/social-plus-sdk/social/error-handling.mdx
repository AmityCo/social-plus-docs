{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: error-handling | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun errorHandling() {
        AmitySocialClient.newPostRepository()
            .createPost()
            .targetUser("userId")
            .text("Hello!")
            .build()
            .post()
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(AndroidSchedulers.mainThread())
            .subscribe({ post ->
                val postId = post.getPostId()
            }, { error ->
                when (error) {
                    is com.amityco.socialcloud.sdk.core.error.AmityException -> {
                        val code = error.code
                        val message = error.message
                    }
                    else -> error.printStackTrace()
                }
            })
    }
```

```swift iOS
func errorHandling() {
        let postRepository = AmityPostRepository(client: AmityManager.shared.client!)
        let builder = AmityTextPostBuilder()
        builder.setText("Test post")
        postRepository.createPost(builder, targetId: nil, targetType: .user) { post, error in
            if let amityError = error as? AmityError {
                switch amityError.code {
                case .unauthorized:
                    print("Unauthorized: \(amityError.localizedDescription)")
                case .itemNotFound:
                    print("Not found: \(amityError.localizedDescription)")
                default:
                    print("Error \(amityError.code.rawValue): \(amityError.localizedDescription)")
                }
                return
            }
            print("Post created: \(post?.postId ?? "")")
        }
    }
```

```dart Flutter
void errorHandling() {
    try {
      final postRepo = AmitySocialClient.newPostRepository();
      await postRepo.getPost('postId');
    } on AmityException catch (e) {
      print('Error \${e.code}: \${e.message}');
    } catch (e) {
      print('Unexpected error: \$e');
    }
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function errorHandling() {
  try {
    await CommunityRepository.joinCommunity('communityId123');
  } catch (error) {
    const sdkError = error as AmityError;
    switch (sdkError.code) {
      case 400300: console.error('User is banned'); break;
      case 500000: console.error('Server error'); break;
      default: console.error('Unexpected error:', sdkError.message);
    }
  }
}
```

</CodeGroup>
