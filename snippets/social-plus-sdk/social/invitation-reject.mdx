{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: invitation-reject | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun rejectInvitation(invitationId: String) {
        AmitySocialClient.newCommunityRepository()
            .invitation()
            .reject(invitationId = invitationId)
            .doOnComplete {
                // invitation rejected
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository()
        communityRepository.declineInvitation(invitationId: "invitationId") { error in
            if let error = error {
                print("Error rejecting invitation: \(error)")
            } else {
                print("Invitation rejected")
            }
        }
```

```typescript TypeScript
import { InvitationRepository } from '@amityco/ts-sdk'

async function rejectInvitation() {
  await InvitationRepository.declineInvitation('invitationId')
}
```

</CodeGroup>
