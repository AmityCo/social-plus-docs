{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: remove-community-member | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun removeCommunityMember() {
        // perform remove community member operation
        AmitySocialClient.newCommunityRepository()
            .membership("communityId")
            .removeMembers(listOf("userId1", "userId2"))
            .subscribe()
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository(client: AmityUIKitManagerInternal.shared.client)
        communityRepository.removeMembers(withMemberIds: ["userId"], from: "communityId") { success, error in
            if success {
                print("Member removed from community")
            } else if let error = error {
                print("Failed to remove member: \(error.localizedDescription)")
            }
        }
```

```dart Flutter
void removeCommunityMember(String communityId, List<String> userIds) {
    AmitySocialClient.newCommunityRepository()
        .membership(communityId)
        .removeMembers(userIds)
        .then((_) {
          // member removed successfully
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function removeCommunityMember() {
  await CommunityRepository.removeMembers({
    communityId: 'communityId123',
    userIds: ['userId123'],
  });
  console.log('Member removed from community');
}
```

</CodeGroup>
