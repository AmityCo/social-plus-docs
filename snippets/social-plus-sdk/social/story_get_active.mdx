{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: story_get_active | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void storyGetActive() {
    AmitySocialClient.newStoryRepository()
        .getActiveStories(targetId: 'communityId', targetType: AmityStoryTargetType.COMMUNITY)
        .then((List<AmityStory> stories) {
          for (final story in stories) {
            print('Active story: \${story.storyId}');
          }
        });
  }
```

```typescript TypeScript
import { StoryRepository } from '@amityco/ts-sdk'

async function getActiveStories() {
  const unsubscribe = StoryRepository.getActiveStoriesByTarget(
    { targetType: 'community', targetId: 'communityId' },
    (result) => {
      if (result.data) {
        console.log('Active stories:', result.data);
      }
    },
  );
  return unsubscribe;
}
```

</CodeGroup>
