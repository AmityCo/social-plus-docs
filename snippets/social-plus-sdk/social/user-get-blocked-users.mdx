{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-get-blocked-users | updated: 2026-05-01 */}

<CodeGroup>

```kotlin Android
fun getBlockedUsers() {
        AmitySocialClient.newUserRepository()
            .relationship()
            .getBlockedUsers()
            .build()
            .query()
            .doOnNext { users: PagedList<AmityUser> ->
                // handle blocked users list
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let userRepository = AmityUserRepository(client: client)
        var token: AmityNotificationToken?
        token = userRepository.userRelationship.getBlockedUsers().observe { collection, change, error in
            // Handle collection updates
        }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function getBlockedUsers() {
  const liveCollection = UserRepository.getBlockedUsers()
  liveCollection.on('dataUpdated', (users) => {
    console.log(users)
  })
}
```

</CodeGroup>
