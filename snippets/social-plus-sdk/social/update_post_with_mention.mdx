{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: update_post_with_mention | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun updatePostWithMention() {
        // Update operation for update_post_with_mention
        AmitySocialClient.newPostRepository()
            .updatePost("postId")
            .text("Updated text")
            .build()
            .update()
            .subscribe({ post -> }, { error -> })
    }
```

```swift iOS
@MainActor
    func updatePostWithMentionExample() async {
        // 1. We build the post structure containing the message.
        let postBuilder = AmityTextPostBuilder()
        postBuilder.setText("Hi @john.")
        // 2. We create post.metadata that contains mention object, via AmityMentionMapper helper class.
        let metadata = AmityMentionMapper.metadata(from: [
            AmityMention(type: .user, index: 3, length: 4, userId: "<john-user-id>")
        ])
        // 3. We create mentionees.
        let mentionees = AmityMentioneesBuilder()
        mentionees.mentionUsers(userIds: ["<john-user-id>"])
        // Finally we pass (1) (2) and (3) to create a post with mention.
        do {
            let post = try await postRepository.editPost(withId: "<post-id>", builder: postBuilder, metadata: metadata, mentionees: mentionees)
            testLabel.attributedText = getAttributedString(from: post)
        } catch {
            // Handle error here
        }
    }
```

```dart Flutter
void updatePostWithMention() {
    final mentionees = AmityMentioneesBuilder()
        .users(['userId1', 'userId2'])
        .build();
    AmitySocialClient.newPostRepository()
        .updatePost('postId')
        .text('Hello @user1 and @user2!')
        .mentionees(mentionees)
        .update()
        .then((post) => print('Post with mention updated'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function updatePostWithMention() {
  const { data: post } = await PostRepository.updatePost({
    postId: 'postId',
    data: { text: 'Hello @userId! Updated content.' },
    metadata: {
      mentioned: [{ userId: 'userId', index: 6, length: 7 }],
    },
  })
  console.log('Post with mention updated:', post.postId)
}
```

</CodeGroup>
