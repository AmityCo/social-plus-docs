{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: event-query | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
final _events = <AmityEvent>[];
  late PagingController<AmityEvent> _controller;

  void queryEvents(String communityId) {
    _controller = PagingController(
      pageFuture: (token) => AmitySocialClient.newEventRepository()
          .getEvents()
          .communityId(communityId)
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    )..addListener(() {
        if (_controller.error == null) {
          _events.clear();
          _events.addAll(_controller.loadedItems);
          // update widgets
        } else {
          // handle error
        }
      });
  }
```

```typescript TypeScript
import { EventRepository } from '@amityco/ts-sdk'

function watchCommunityEvents() {
  const unsubscribe = EventRepository.getEvents(
    { communityId: 'communityId' },
    ({ data: events }) => {
      console.log(events)
    },
  )
  return unsubscribe
}
```

</CodeGroup>
