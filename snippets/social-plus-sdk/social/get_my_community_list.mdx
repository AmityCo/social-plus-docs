{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_my_community_list | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getMyCommunityList() {
        // Query communities the current user has joined as a live collection
        AmitySocialClient.newCommunityRepository()
            .getCommunities()
            .filter(com.amityco.socialcloud.sdk.api.social.community.query.AmityCommunityFilter.MEMBER)
            .build()
            .query()
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(io.reactivex.rxjava3.android.schedulers.AndroidSchedulers.mainThread())
            .subscribe({ pagedList ->
                // Update UI with community list
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
// Get all communities the current user belongs to
        let communityRepository = AmityCommunityRepository(client: AmityManager.shared.client!)
        var token: AmityNotificationToken?
        let collection = communityRepository.getCommunities(
            displayName: nil,
            filter: .userIsMember,
            sortBy: .lastCreated,
            categoryId: nil,
            includeDeleted: false
        )
        token = collection.observe { liveCollection, _, error in
            if let error = error {
                print("Failed to get my community list: \(error.localizedDescription)")
                return
            }
            let communities = (0..<liveCollection.count()).compactMap { liveCollection.object(at: $0) }
            print("My communities count: \(communities.count)")
        }
```

```dart Flutter
void getMyCommunityList() async {
    AmitySocialClient.newCommunityRepository()
        .getCommunities()
        .filter(AmityCommunityQueryFilter.member)
        .getPagingData(token: null, limit: 20)
        .asStream()
        .listen((result) {
      final communities = result.data;
    });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

function getMyCommunityList() {
  const liveCollection = CommunityRepository.getCommunities({ membership: 'member', limit: 20 })
  liveCollection.on('dataUpdated', (communities) => {
    console.log('My community list:', communities)
  })
}
```

</CodeGroup>
