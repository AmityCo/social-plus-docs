{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-relationship-get-follow-info | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getFollowInfo(userId: String) {
        AmityCoreClient.newUserRepository()
            .relationship()
            .getFollowInfo(userId = userId)
            .doOnNext { followInfo: AmityUserFollowInfo ->
                val followingCount = followInfo.getFollowingCount()
                val followerCount = followInfo.getFollowerCount()
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let client = AmityClient.shared
        let followManager = AmityFollowManager(client: client)
        followManager.getFollowInfo(withUserId: "userId") { followInfo, error in
            if let info = followInfo {
                print("Followers: \(info.followerCount), Following: \(info.followingCount)")
            }
        }
```

```dart Flutter
void getFollowInfo(String userId) {
    AmityCoreClient.newUserRepository()
        .relationship()
        .getFollowInfo(userId)
        .then((AmityFollowInfo followInfo) => {
              //handle result
              //followInfo.followingCount - number of users this user follows
              //followInfo.followerCount - number of users following this user
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function getFollowInfo() {
  const liveObject = UserRepository.getFollowInfo('userId')
  liveObject.on('dataUpdated', (info) => {
    console.log(info)
  })
}
```

</CodeGroup>
