{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-service-locator | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun postServiceLocator() {
        // Use service locator to access post creation services
        AmitySocialClient.newPostRepository()
            .createPost()
            .targetMe()
            .build()
            .post()
            .subscribe(
                { post -> /* post created via service locator */ },
                { error -> /* handle error */ }
            )
    }
```

```swift iOS
let client = AmityClient.shared
        let fileRepository = AmityFileRepository(client: client)
        let postRepository = AmityPostRepository(client: client)
        print("Service locator ready. fileRepo: \(fileRepository), postRepo: \(postRepository)")
```

```dart Flutter
void accessPostRepository() {
    final postRepository = AmitySocialClient.newPostRepository();
    // use postRepository to create, get, update, or delete posts
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function postServiceLocator() {
  const { PostRepository } = Amity;
  const { data: post } = await PostRepository.getPost('postId');
  console.log('Post service located, post type:', post.dataType);
}
```

</CodeGroup>
