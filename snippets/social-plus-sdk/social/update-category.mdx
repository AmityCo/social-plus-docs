{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: update-category | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun updateCategory() {
        // Update operation for update-category
        AmitySocialClient.newCommunityRepository()
            .updateCommunity("communityId")
            .displayName("New Name")
            .build()
            .update()
            .subscribe({ community -> }, { error -> })
    }
```

```swift iOS
let categoryId = "categoryId"
        let builder = AmityCategoryUpdateBuilder()
        builder.setName("Updated Category Name")
        AmitySDK.updateCommunityCategory(categoryId: categoryId, builder: builder) { category, error in
            if let error = error {
                print("Failed to update category: \(error)")
                return
            }
            print("Category updated: \(category?.categoryId ?? "")")
        }
```

```dart Flutter
void updateCategory() {
    final categoryRepo = AmityCategoryRepository();
    categoryRepo
        .updateCategory(categoryId: 'categoryId')
        .displayName('Updated Category Name')
        .update()
        .then((category) => print('Category updated: \${category.name}'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function updateCommunityCategory() {
  try {
    await CommunityRepository.updateCategory('categoryId', { name: 'Updated Category Name' })
    console.log('Community category updated')
  } catch (error) {
    console.error('Failed to update category:', error)
  }
}
```

</CodeGroup>
