{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user_posts_get | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void userPostsGet() {
    AmitySocialClient.newPostRepository()
        .getPosts()
        .targetUser('userId')
        .build()
        .query();
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function getUserPosts() {
  const unsubscribe = PostRepository.getPosts(
    { targetType: 'user', targetId: 'userId', sortBy: 'lastCreated' },
    (result) => {
      if (result.data) {
        console.log('User posts:', result.data);
      }
    },
  );
  return unsubscribe;
}
```

</CodeGroup>
