{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_user_following | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getUserFollowing() {
        AmitySocialClient.newUserRepository()
            .relationship()
            .user("userId")
            .getFollowers()
            .build()
            .query()
            .subscribe()
    }
```

```swift iOS
func getUserFollowing() {
        let userRepository = AmityUserRepository(client: AmityManager.shared.client!)
        let collection = userRepository.getFollowing(userId: "userId123", status: .accepted)
        collection.observe { collection, _, error in
            guard error == nil else { return }
            collection.snapshots.forEach { relationship in
                print("Following userId: \(relationship.targetUserId)")
            }
        }
    }
```

```dart Flutter
void getUserFollowing() {
    AmitySocialClient.newUserRepository()
        .relationship()
        .user('userId')
        .getFollowings()
        .getPagingData()
        .listen((followings) {
      for (final following in followings) {
        print('Following: \${following.destinationUserId}');
      }
    });
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function getUserFollowing() {
  const userId = 'user-id-123'
  const liveCollection = UserRepository.getFollowings({
    userId,
    limit: 20,
  })
  const unsubscribe = liveCollection.subscribe(({ data: following, loading }) => {
    if (!loading) console.log('Following:', following)
  })
  return unsubscribe
}
```

</CodeGroup>
