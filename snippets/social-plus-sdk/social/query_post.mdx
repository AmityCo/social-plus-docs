{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: query_post | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun queryPost() {
        // Query posts in a community
        AmitySocialClient.newPostRepository()
            .getPosts()
            .targetCommunity("communityId")
            .build()
            .query()
            .asLiveData()
    }
```

```swift iOS
let postRepository = AmityPostRepository(client: AmityUIKitManagerInternal.shared.client)
        let object = postRepository.getPost(withPostId: "postId")
        object.observe { liveObject, error in
            guard error == nil, let post = liveObject.snapshot else { return }
            print("Post loaded: \(post.postId)")
        }
```

```dart Flutter
void queryPost() async {
    final postRepository = AmitySocialClient.newPostRepository();
    postRepository
        .getPosts()
        .targetUser('userId')
        .sortBy(AmityPostSortOption.LAST_CREATED)
        .asStream()
        .listen((posts) {
      print('Posts: \${posts.length}');
    });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function queryPostUnderscore() {
  const params = {
    targetId: 'userId123',
    targetType: 'user' as const,
    sortBy: 'lastCreated' as const,
    feedType: 'published' as const,
  };

  const liveCollection = PostRepository.getPosts(params);
  liveCollection.on('dataUpdated', posts => {
    console.log('Posts:', posts.map(p => p.postId));
  });
  liveCollection.on('dataError', err => console.error('Error querying posts:', err));
}
```

</CodeGroup>
