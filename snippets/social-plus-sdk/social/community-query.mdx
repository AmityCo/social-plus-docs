{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-query | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun communityQuery() {
        AmitySocialClient.newCommunityRepository()
            .getCommunities()
            .filter(com.amityco.socialcloud.sdk.api.social.community.query.AmityCommunityFilter.MEMBER)
            .sortBy(com.amityco.socialcloud.sdk.model.social.community.AmityCommunitySortOption.DISPLAY_NAME)
            .build()
            .query()
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(AndroidSchedulers.mainThread())
            .subscribe({ pagedList ->
                // update UI
            }, { error ->
                error.printStackTrace()
            })
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository(client: client)
        var token: AmityNotificationToken?
        let query = AmityCommunityQuery()
        query.filter = .userIsMember
        token = communityRepository.getCommunities(with: query).observe { collection, change, error in
            // Handle collection updates
        }
```

```dart Flutter
void communityQuery() async {
    final communities = AmitySocialClient.newCommunityRepository()
        .getCommunities()
        .filter(AmityCommunityFilter.ALL)
        .sortBy(AmityCommunitySortOption.DISPLAY_NAME)
        .build()
        .asStream();
    communities.listen((list) {
      print('Communities: \${list.length}');
    });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

function communityQuery(filter: { categoryId?: string; displayName?: string }) {
  const disposer = CommunityRepository.getCommunities(
    { ...filter, limit: 20, sortBy: 'displayName' },
    ({ data: communities, loading, error }) => {
      if (loading) return
      if (error) {
        console.error('Failed to query communities', error)
        return
      }
      console.log('Communities:', communities)
    },
  )

  return disposer
}
```

</CodeGroup>
