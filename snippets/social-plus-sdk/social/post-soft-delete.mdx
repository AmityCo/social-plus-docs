{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-soft-delete | updated: 2026-05-01 */}

<CodeGroup>

```kotlin Android
fun softDeletePost(postId: String) {
        AmitySocialClient.newPostRepository()
            .softDeletePost(postId = postId)
            .doOnComplete {
                // success
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let postRepository = AmityPostRepository(client: client)
        do {
            try await postRepository.softDeletePost(postId: "<post-id>")
            print("Post soft deleted")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void softDeletePost(AmityPost post) {
    AmitySocialClient.newPostRepository()
        .deletePost(postId: post.postId!)
        .then((value) => {
              //success - post is soft deleted
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function softDeletePost() {
  await PostRepository.deletePost('postId')
}
```

</CodeGroup>
