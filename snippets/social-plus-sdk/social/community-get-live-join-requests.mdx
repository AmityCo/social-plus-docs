{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-get-live-join-requests | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun communityGetLiveJoinRequests(communityId: String) {
        AmitySocialClient.newCommunityRepository()
            .getJoinRequests(communityId = communityId)
            .status(AmityJoinRequestStatus.PENDING)
            .build()
            .getLiveCollection()
            .subscribe(
                { page -> println("Pending requests count: ${page.size}") },
                { error -> println("Error: ${error.message}") }
            )
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository(client: client)
        var token: AmityNotificationToken?
        token = communityRepository.getJoinRequests(communityId: "<community-id>").observe { collection, change, error in
            guard error == nil else { return }
            print("Pending join requests: \(collection.count())")
        }
```

```dart Flutter
StreamSubscription? subscription;

  void observeLiveJoinRequests(String communityId) {
    subscription = AmitySocialClient.newCommunityRepository()
        .moderation(communityId)
        .getJoinRequests()
        .status(AmityCommunityJoinRequestStatus.pending)
        .build()
        .query()
        .asStream()
        .listen((List<AmityCommunityJoinRequest> requests) {
          for (final request in requests) {
            final userId = request.userId;
            final displayName = request.user?.displayName;
          }
        })
        ..onError((error) {
          // Handle error
        });
  }

  void dispose() {
    subscription?.cancel();
  }
```

</CodeGroup>
