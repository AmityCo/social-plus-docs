{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: feed-query-community-feed | updated: 2026-04-30 */}

<CodeGroup>

```kotlin Android
fun queryCommunityFeed(feedRepository: AmityFeedRepository, communityId: String) {
        feedRepository
            .getCommunityFeed(communityId = communityId)
            .reviewStatus(reviewStatus = AmityReviewStatus.PUBLISHED)
            .build()
            .query()
            .doOnNext { posts: PagingData<AmityPost> ->
                // PagingData<AmityPost>
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let feedRepository = AmityFeedRepository(client: client)
        var token: AmityNotificationToken?
        token = feedRepository.getCommunityFeed(communityId: "<community-id>", sortBy: .lastCreated, includeDeleted: false, feedType: .published).observe { collection, change, error in
            guard error == nil else { return }
            print("Community feed posts: \(collection.count())")
        }
```

```dart Flutter
final _posts = <AmityPost>[];
  late PagingController<AmityPost> _controller;

  void queryCommunityFeed(String communityId) {
    _controller = PagingController(
      pageFuture: (token) => AmitySocialClient.newFeedRepository()
          .getCommunityFeed(communityId)
          .includeDeleted(false)
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    )..addListener(
        () {
          if (_controller.error == null) {
            _posts.clear();
            _posts.addAll(_controller.loadedItems);
            //update widgets
          } else {
            //error on pagination controller
          }
        },
      );
  }
```

```typescript TypeScript
import { FeedRepository } from '@amityco/ts-sdk'

async function queryCommunityFeed() {
  const liveCollection = FeedRepository.getCommunityFeed('communityId', 'published')
  liveCollection.on('dataUpdated', (posts) => {
    console.log(posts)
  })
}
```

</CodeGroup>
