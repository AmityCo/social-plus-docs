{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create-text-comment | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createTextComment(postId: String, text: String) {
        AmitySocialClient.newCommentRepository()
            .createComment()
            .post(postId = postId)
            .with()
            .text(text = text)
            .build()
            .comment()
            .doOnSuccess { comment: AmityComment ->
                // AmityComment created
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let commentRepository = AmityCommentRepository()
        let builder = AmityCommentCreationDataBuilder()
        builder.setText("Great post!")
        commentRepository.createComment(builder, referenceId: "postId", referenceType: .post, parentId: nil) { comment, error in
            if let error = error {
                print("Failed to create comment: \(error.localizedDescription)")
            } else {
                print("Comment created: \(comment?.commentId ?? "")")
            }
        }
```

```typescript TypeScript
import { CommentRepository } from '@amityco/ts-sdk'

async function createTextComment(postId: string, text: string) {
  const { data: comment } = await CommentRepository.createComment({
    referenceType: 'post',
    referenceId: postId,
    data: { text },
  })

  console.log('Comment created:', comment.commentId)
  return comment
}

// Example usage
createTextComment('postId', 'This is a great post!')
```

</CodeGroup>
