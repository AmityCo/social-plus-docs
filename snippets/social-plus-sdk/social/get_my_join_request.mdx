{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_my_join_request | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getMyJoinRequest() {
        AmitySocialClient.newCommunityRepository()
            .moderation("communityId")
            .getPendingJoinRequests()
            .subscribe()
    }
```

```swift iOS
func getMyJoinRequest() {
        let communityRepository = AmityCommunityRepository(client: AmityManager.shared.client!)
        let liveCollection = communityRepository.getJoinRequests(communityId: "communityId", status: .pending)
        liveCollection.observe { collection, _, error in
            if let error = error { print("Error: \(error)"); return }
            print("Pending join requests: \(collection.count())")
        }
    }
```

```dart Flutter
void getMyJoinRequest() {
    AmitySocialClient.newCommunityRepository()
        .moderation('communityId')
        .getMyJoinRequest()
        .asStream()
        .listen((request) {
      print('Join request status: \${request?.status}');
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function getMyJoinRequest() {
  const liveCollection = CommunityRepository.getJoinRequests('communityId123', { status: 'pending' });
  liveCollection.on('dataUpdated', (requests) => {
    const client = Client.getActiveClient();
    const mine = requests.find((r: any) => r.userId === client.currentUser?.userId);
    console.log('My join request status:', mine?.status);
  });
}
```

</CodeGroup>
