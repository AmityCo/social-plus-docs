{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-query-trending-communities | updated: 2026-05-01 */}

<CodeGroup>

```kotlin Android
fun queryTrendingCommunities() {
        AmitySocialClient.newCommunityRepository()
            .getTrendingCommunities()
            .build()
            .query()
            .doOnNext { communities: PagingData<AmityCommunity> ->
                // PagingData<AmityCommunity>
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository(client: client)
        var token: AmityNotificationToken?
        token = communityRepository.getTrendingCommunities().observe { collection, change, error in
            guard error == nil else { return }
            print("Trending communities: \(collection.count())")
        }
```

```dart Flutter
void getTrendingCommunities() {
    AmitySocialClient.newCommunityRepository()
        .getTrendingCommunities()
        .then((List<AmityCommunity> communities) => {
              //handle result - returns maximum 5 trending communities
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function queryTrendingCommunities() {
  const liveCollection = CommunityRepository.getTrendingCommunities()
  liveCollection.on('dataUpdated', (communities) => {
    console.log(communities)
  })
}
```

</CodeGroup>
