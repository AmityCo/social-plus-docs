{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create-poll-post | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createPollPost(communityId: String, pollId: String) {
        val target = AmityPost.Target.COMMUNITY.create(communityId = communityId)
        AmitySocialClient.newPostRepository()
            .createPollPost(target = target, pollId = pollId)
            .doOnSuccess { post: AmityPost ->
                // AmityPost poll created
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```dart Flutter
void createPollPost() {
    AmitySocialClient.newPollRepository()
        .createPoll()
        .question('What is your favorite?')
        .answers(['Option A', 'Option B'])
        .closedIn(86400)
        .create()
        .then((poll) {
          AmitySocialClient.newPostRepository()
              .createPost()
              .targetMe()
              .poll(poll.pollId!)
              .post();
        })
        .onError((error, stackTrace) { });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function createPollPost(targetId: string, question: string, answers: string[]) {
  try {
    const { data: poll } = await PollRepository.createPoll({
      question,
      answers: answers.map(text => ({ dataType: 'text', data: { text } })),
      timeToClosePoll: 86400,
      answerType: 'single',
    });
    const { data: post } = await PostRepository.createPost({
      targetType: 'community',
      targetId,
      dataType: 'poll',
      data: { pollId: poll.pollId },
    });
    console.log('Poll post created:', post.postId, 'pollId:', poll.pollId);
    return { post, poll };
  } catch (error) {
    console.error('Failed to create poll post:', error);
  }
}
```

</CodeGroup>
