{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-create-custom-post | updated: 2026-05-01 */}

<CodeGroup>

```kotlin Android
fun createCustomPost(targetCommunityId: String, customData: JSONObject) {
        val target = AmityPost.Target.COMMUNITY.create(communityId = targetCommunityId)
        AmitySocialClient.newPostRepository()
            .createCustomPost(target = target, data = customData)
            .doOnSuccess { post: AmityPost ->
                // AmityPost
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let postRepository = AmityPostRepository(client: client)
        let builder = AmityCustomPostBuilder()
        builder.setData(["key": "value"])
        do {
            let post = try await postRepository.createCustomPost(builder, targetId: "<community-id>", targetType: .community, metadata: nil, mentionees: nil)
            print("Created custom post: \(post.postId)")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void createCustomPost(String targetId, AmityPostTargetType targetType) {
    final customData = <String, dynamic>{
      'type': 'custom',
      'data': 'Custom post data',
    };

    AmitySocialClient.newPostRepository()
        .createPost()
        .targetType(targetType)
        .targetId(targetId)
        .data(customData)
        .post()
        .then((AmityPost post) => {
              //handle result
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function createCustomPost() {
  const { data: post } = await PostRepository.createPost({
    targetType: 'community',
    targetId: 'communityId',
    dataType: 'custom',
    data: { customField: 'customValue' },
  })
  console.log(post)
}
```

</CodeGroup>
