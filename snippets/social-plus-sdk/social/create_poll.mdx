{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create_poll | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createPoll() {
        AmitySocialClient.newPollRepository()
            .createPoll("What is your favourite language?")
            .answers(listOf(
                com.amityco.socialcloud.sdk.api.social.poll.AmityPollAnswer.Text("Kotlin"),
                com.amityco.socialcloud.sdk.api.social.poll.AmityPollAnswer.Text("Java")
            ))
            .timeToClosePoll(3600)
            .build()
            .create()
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(AndroidSchedulers.mainThread())
            .subscribe({ poll -> }, { error -> error.printStackTrace() })
    }
```

```swift iOS
func createPoll() {
        let postRepository = AmityPostRepository(client: AmityManager.shared.client!)
        let pollRepository = AmityPollRepository(client: AmityManager.shared.client!)
        let question = "What is your favorite feature?"
        let answers = ["Live Objects", "Real-time Chat", "Communities"]
        pollRepository.createPoll(question: question, answers: answers, closedIn: 86400, multipleSelection: false) { poll, error in
            guard let poll = poll, error == nil else { return }
            let builder = AmityPollPostBuilder()
            builder.setPollId(poll.pollId)
            postRepository.createPost(builder, targetId: nil, targetType: .user) { post, error in
                print("Poll post created: \(post?.postId ?? "")")
            }
        }
    }
```

```dart Flutter
void createPoll() {
    AmitySocialClient.newPollRepository()
        .createPoll(
          question: 'What is your favorite color?',
          answers: [
            AmityPollAnswer(dataType: AmityPollAnswerDataType.TEXT, data: 'Red'),
            AmityPollAnswer(dataType: AmityPollAnswerDataType.TEXT, data: 'Blue'),
          ],
          answerType: AmityPollAnswerType.SINGLE,
          closedIn: 86400000,
        )
        .then((AmityPoll poll) {
          final pollId = poll.pollId;
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }
```

```typescript TypeScript
import { PollRepository } from '@amityco/ts-sdk'

async function createPoll() {
  const poll = await PollRepository.createPoll({
    question: 'What is your favorite color?',
    answers: [{ data: 'Red' }, { data: 'Blue' }, { data: 'Green' }],
    closedIn: 86400000,
    dataType: 'single',
  })
  console.log('Poll created:', poll)
}
```

</CodeGroup>
