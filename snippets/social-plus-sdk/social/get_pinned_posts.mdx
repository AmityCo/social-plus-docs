{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_pinned_posts | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getPinnedPosts() {
        // Retrieve pinned posts for a target (community or user)
        AmitySocialClient.newPostRepository()
            .getPinnedPosts("communityId")
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(io.reactivex.rxjava3.android.schedulers.AndroidSchedulers.mainThread())
            .subscribe({ pinnedPosts ->
                // Display pinned posts at top of feed
            }, { error ->
                // Handle error
            })
    }
```

```dart Flutter
void getPinnedPosts() {
    // TODO: implement SDK operation
    throw UnimplementedError();
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function getPinnedPosts() {
  const liveCollection = PostRepository.getPosts({ targetId: 'communityId123', targetType: 'community', feedType: 'pin' });
  liveCollection.on('dataUpdated', (posts) => {
    console.log('Pinned posts:', posts.map((p: any) => p.postId));
  });
}
```

</CodeGroup>
