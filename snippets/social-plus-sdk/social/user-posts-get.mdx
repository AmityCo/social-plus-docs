{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-posts-get | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun userPostsGet() {
        // Get and observe user-posts-get
        AmityCoreClient.newUserRepository()
            .searchUser("keyword")
            .build()
            .query()
            .subscribe({ liveCollection -> }, { error -> })
    }
```

```swift iOS
let client = AmityClient.shared
        let postRepository = AmityPostRepository(client: client)
        let queryOptions = AmityPostQueryOptions(userId: "userId", sortBy: .lastCreated, deletedOption: .notDeleted, filterByParentPost: false)
        let collection = postRepository.getPosts(queryOptions)
        print("Querying posts for user: \(collection)")
```

```dart Flutter
void userPostsGet() {
    AmitySocialClient.newPostRepository()
        .getPosts()
        .targetUser('userId')
        .query()
        .then((List<AmityPost> posts) {
          for (final post in posts) {
            print('Post: \${post.postId}');
          }
        });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function getUserPosts() {
  const unsubscribe = PostRepository.getPosts(
    { targetType: 'user', targetId: 'userId', sortBy: 'lastCreated' },
    (result) => {
      if (result.data) {
        console.log('User posts:', result.data);
      }
    },
  );
  return unsubscribe;
}
```

</CodeGroup>
