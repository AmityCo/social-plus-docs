{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-moderation-approve-post | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun approvePost(communityId: String, postId: String) {
        AmitySocialClient.newCommunityRepository()
            .moderation(communityId = communityId)
            .approvePost(postId = postId)
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let communityModeration = AmityCommunityModeration(client: client, andCommunity: "<community-id>")
        do {
            let success = try await communityModeration.approvePost(withId: "<post-id>")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void approvePost(String communityId, String postId) {
    AmitySocialClient.newCommunityRepository()
        .moderation(communityId)
        .approvePost(postId)
        .then((_) {
          // post approved
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function approvePost() {
  await PostRepository.approvePost('postId')
}
```

</CodeGroup>
