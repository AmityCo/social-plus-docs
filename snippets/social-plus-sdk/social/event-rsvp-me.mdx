{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: event-rsvp-me | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun eventRsvpMe(eventId: String) {
        AmitySocialClient.newEventRepository()
            .getMyRsvp(eventId = eventId)
            .doOnSuccess { rsvp: AmityEventRsvp ->
                val status = rsvp.getStatus()
                // handle my RSVP status
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let eventRepository = AmityEventRepository()
        let liveObject = eventRepository.getMyRsvp(eventId: "eventId")
        liveObject.observe { rsvp, error in
            guard error == nil else { return }
            if let myRsvp = rsvp.object {
                print("My RSVP: \(myRsvp.status.rawValue)")
            } else {
                print("No RSVP found for current user")
            }
        }
```

```dart Flutter
void eventRsvpMe() {
    final eventRepo = AmitySocialClient.newEventRepository();
    eventRepo
        .getEventRsvps('eventId')
        .getPagingData(token: null, limit: 20)
        .then((rsvps) => print('RSVPs count: \${rsvps.data.length}'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { EventRepository } from '@amityco/ts-sdk'

async function getMyRsvp() {
  const liveObject = EventRepository.getMyRsvp('eventId')
  liveObject.on('dataUpdated', (rsvp) => {
    console.log(rsvp)
  })
}
```

</CodeGroup>
