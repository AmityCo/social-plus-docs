{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: update_text_post | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun updateTextPost() {
        // Update operation for update_text_post
        AmitySocialClient.newPostRepository()
            .createPost()
            .targetMe()
            .text("Hello World")
            .build()
            .post()
            .subscribe({ post -> 
                // Post created: ${post.getPostId()}
            }, { error -> })
    }
```

```swift iOS
func updateTextPostExample(postId: String, newText: String) async {
        let builder = AmityTextPostBuilder()
        builder.setText(newText)
        do {
            let post = try await postRepository.editPost(withId: postId, builder: builder)
        } catch {
            // Handle error here
        }
    }
```

```dart Flutter
void updateTextPost() {
    AmitySocialClient.newPostRepository()
        .updatePost('postId')
        .text('This is the updated post text.')
        .update()
        .then((post) => print('Text post updated'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function updateTextPost() {
  const { data: post } = await PostRepository.updatePost({
    postId: 'postId',
    data: { text: 'Updated text post content' },
  })
  console.log('Text post updated:', post.postId)
}
```

</CodeGroup>
