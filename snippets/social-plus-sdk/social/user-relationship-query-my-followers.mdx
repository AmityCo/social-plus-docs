{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-relationship-query-my-followers | updated: 2026-05-01 */}

<CodeGroup>

```kotlin Android
fun queryMyFollowers() {
        AmityCoreClient.newUserRepository()
            .relationship()
            .getMyFollowers()
            .status(AmityFollowStatusFilter.ACCEPTED)
            .build()
            .query()
            .doOnNext { followers: PagingData<AmityFollowRelationship> ->
                // PagingData<AmityFollowRelationship>
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```dart Flutter
final _followers = <AmityUser>[];
  late PagingController<AmityUser> _controller;

  void queryMyFollowers() {
    _controller = PagingController(
      pageFuture: (token) => AmityCoreClient.newUserRepository()
          .relationship()
          .getMyFollowers()
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    )..addListener(
        () {
          if (_controller.error == null) {
            _followers.clear();
            _followers.addAll(_controller.loadedItems);
            //update widgets
          } else {
            //error on pagination controller
          }
        },
      );
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function queryMyFollowers() {
  const liveCollection = UserRepository.getMyFollowers()
  liveCollection.on('dataUpdated', (followers) => {
    console.log(followers)
  })
}
```

</CodeGroup>
