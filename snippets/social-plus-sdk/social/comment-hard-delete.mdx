{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: comment-hard-delete | updated: 2026-04-30 */}

<CodeGroup>

```kotlin Android
fun hardDeleteComment(commentId: String) {
        AmitySocialClient.newCommentRepository()
            .hardDeleteComment(commentId = commentId)
            .doOnComplete {
                // success
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let commentRepository = AmityCommentRepository(client: client)
        do {
            try await commentRepository.hardDeleteComment(withId: "<comment-id>")
            print("Comment permanently deleted")
        } catch {
            // Handle error here
        }
```

```typescript TypeScript
import { CommentRepository } from '@amityco/ts-sdk'

async function hardDeleteComment() {
  await CommentRepository.deleteComment('commentId', true)
}
```

</CodeGroup>
