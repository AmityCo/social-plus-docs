{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: update_post | updated: 2026-05-02 */}

<CodeGroup>

```swift iOS
func updateTextPostExample() async throws {
        // Update your post structure
        let builder = AmityTextPostBuilder()
        builder.setText("new-text")
        // Update a post from the builder
        do {
            let post = try await postRepository.editPost(withId: "<post-id>", builder: builder)
        } catch {
            // Handle error here
        }
    }
```

```dart Flutter
void updatePost() async {
    final postRepo = AmitySocialClient.newPostRepository();
    await postRepo
        .updatePost('postId')
        .text('Updated post content')
        .update();
    print('Post updated');
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function updatePost(postId: string, text: string) {
  const updated = await PostRepository.updatePost(postId, {
    data: { text },
    dataType: 'text',
  })
  console.log('Post updated:', updated.data.postId)
}
```

</CodeGroup>
