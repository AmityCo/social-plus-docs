{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create_post_with_mention | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createPostWithMention() {
        // Create a text post that mentions a user
        AmitySocialClient.newPostRepository()
            .createPost()
            .targetUser(userId = "userId")
            .text(text = "Hello @user!")
            .mentionUsers(userIds = listOf("mentionedUserId"))
            .build()
            .subscribe()
    }
```

```swift iOS
@MainActor
    func createPostWithMentionExample() async {
        // 1. We build the post structure containing the message.
        let postBuilder = AmityTextPostBuilder()
        postBuilder.setText("Hi @john and @alice.")
        // To add mention to the post, we need extra steps.
        // In this example, we want to mention two users "john" and "alice".
        //
        // 2. We create post.metadata that contains mention object, via AmityMentionMapper helper class.
        //
        // Notice how `index` and `length` is set.
        let metadata = AmityMentionMapper.metadata(from: [
            AmityMention(type: .user, index: 3, length: 4, userId: "<john-user-id>"),
            AmityMention(type: .user, index: 13, length: 5, userId: "<alice-user-id>")
        ])
        // 3. We create mentionees.
        let mentionees = AmityMentioneesBuilder()
        mentionees.mentionUsers(userIds: ["<john-user-id>", "<alice-user-id>"])
        // Finally we pass (1) (2) and (3) to create a post with mention.
        do {
            let post = try await postRepository.createTextPost(
                postBuilder,
                targetId: nil,
                targetType: .user,
                metadata: metadata,
                mentionees: mentionees
            )
            testLabel.attributedText = getAttributedString(from: post)
        } catch {
            // Handle error here
        }
    }
```

```dart Flutter
void createPostWithMention() {
    AmitySocialClient.newPostRepository()
        .createPost()
        .targetMe()
        .text('Hello @username')
        .mentionUsers(['userId'])
        .build()
        .post()
        .then((AmityPost post) {
          // Post with mention created
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function createPostWithMention() {
  const post = await PostRepository.createPost({
    targetType: 'community',
    targetId: 'communityId',
    dataType: 'text',
    data: { text: 'Hey @username, welcome to the community!' },
    mentionees: [{ type: 'user', userIds: ['userId'] }],
    metadata: { mentioned: [{ type: 'user', userId: 'userId', index: 4, length: 9 }] },
  })
  console.log('Post with mention created:', post)
}
```

</CodeGroup>
