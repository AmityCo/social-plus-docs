{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: revoke-invitations | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void revokeInvitations() async {
    final communityRepository = AmitySocialClient.newCommunityRepository();
    await communityRepository
        .membership('communityId')
        .revokeInvitation(userId: 'userId');
    print('Invitation revoked');
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function revokeInvitations() {
  const revokeInvitation = async (communityId: string, userId: string) => {
    await CommunityRepository.revokeInvitation({ communityId, userId });
    console.log(`Invitation revoked for user ${userId} in community ${communityId}`);
  };

  await revokeInvitation('communityId123', 'userId456');
}
```

</CodeGroup>
