{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-all-categories | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getAllCategories() {
        // Query all community categories
        AmitySocialClient.newCommunityRepository()
            .getCategories()
            .build()
            .query()
            .subscribe { categories -> /* handle categories */ }
    }
```

```swift iOS
// Query all community categories
        let communityRepository = AmityCommunityRepository(client: AmityManager.shared.client!)
        var token: AmityNotificationToken?
        let collection = communityRepository.getCategories(sortBy: .displayName, includeDeleted: false)
        token = collection.observe { liveCollection, _, error in
            if let error = error {
                print("Failed to get categories: \(error.localizedDescription)")
                return
            }
            let categories = (0..<liveCollection.count()).compactMap { liveCollection.object(at: $0) }
            print("Categories loaded — count: \(categories.count)")
        }
```

```dart Flutter
void getAllCategories() async {
    final controller = PagingController<AmityCommunityCategory>(
      pageSize: 20,
    );
    AmitySocialClient.newCommunityRepository()
        .getCategories()
        .getPagingData(token: null, limit: 20)
        .asStream()
        .listen((result) {
      final categories = result.data;
    });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

function getAllCategories() {
  const liveCollection = CommunityRepository.getCategories({ sortBy: 'name', includeDeleted: false })
  liveCollection.on('dataUpdated', (categories) => {
    console.log('Categories:', categories)
  })
}
```

</CodeGroup>
