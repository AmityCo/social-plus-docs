{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: flag-comment | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun flagComment(commentId: String) {
        AmitySocialClient.newCommentRepository()
            .flagComment(commentId = commentId)
            .doOnComplete {
                // Comment flagged successfully
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let commentRepository = AmityCommentRepository(client: AmityManager.shared.client!)
        commentRepository.flagComment(withId: "commentId") { _, error in
            if let error = error {
                print("Error flagging comment: \(error)")
            } else {
                print("Comment flagged successfully")
            }
        }
```

```dart Flutter
void flagComment(AmityComment comment) {
    comment.report()
        .flag()
        .then((_) {
          // comment flagged
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

</CodeGroup>
