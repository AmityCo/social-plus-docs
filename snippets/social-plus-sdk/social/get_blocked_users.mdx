{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_blocked_users | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getBlockedUsers() {
        // Query users blocked by the current user
        AmityCoreClient.newUserRepository()
            .getBlockedUsers()
            .build()
            .query()
            .subscribe { users -> /* handle blocked users */ }
    }
```

```swift iOS
// Get all users blocked by the current user
        let userRepository = AmityUserRepository(client: AmityManager.shared.client!)
        var token: AmityNotificationToken?
        let collection = userRepository.getBlockedUsers()
        token = collection.observe { liveCollection, _, error in
            if let error = error {
                print("Failed to get blocked users: \(error.localizedDescription)")
                return
            }
            let users = (0..<liveCollection.count()).compactMap { liveCollection.object(at: $0) }
            print("Blocked users count: \(users.count)")
        }
```

```dart Flutter
void getBlockedUsers() async {
    AmitySocialClient.newUserRepository()
        .getBlockedUsers()
        .asStream()
        .listen((users) {
      // handle blocked users list
    });
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

function getBlockedUsers() {
  const liveCollection = UserRepository.getBlockedUsers()
  liveCollection.on('dataUpdated', (users) => {
    console.log('Blocked users:', users)
  })
}
```

</CodeGroup>
