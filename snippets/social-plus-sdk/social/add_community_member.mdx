{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: add_community_member | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun addCommunityMember() {
        AmitySocialClient.newCommunityRepository()
            .membership("communityId")
            .addMembers(userIds = listOf("userId1", "userId2"))
            .subscribe()
    }
```

```swift iOS
let communityRepository = AmitySocialClient.newCommunityRepository()
        communityRepository.addMembers(["userId1", "userId2"], communityId: "communityId") { success, error in
            if let error = error { print("Error adding members: \(error)"); return }
            print("Members added successfully")
        }
```

```dart Flutter
void addCommunityMember() {
    AmitySocialClient.newCommunityRepository()
        .moderation('communityId')
        .addMembers(['userId1', 'userId2'])
        .then((_) { })
        .onError((error, stackTrace) { });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function addCommunityMember(communityId: string, userIds: string[]) {
  try {
    await CommunityRepository.addMembers(communityId, userIds);
    console.log('Members added successfully:', userIds);
  } catch (error) {
    console.error('Failed to add members:', error);
  }
}
```

</CodeGroup>
