{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: poll-observe | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun pollObserve() {
        // TODO: implement poll observe
    }
```

```swift iOS
func pollObserve() {
        let client = AmityClient.shared
        let pollRepository = AmityPollRepository(client: client)
        let liveObject = pollRepository.getPoll("poll_id")
        liveObject.observe { poll, error in
            if let p = poll {
                print("Poll: \(p.pollId), closed: \(p.isClosed), total votes: \(p.voteCount)")
            }
        }
    }
```

```dart Flutter
void pollObserve() async {
        AmitySocialClient.newPollRepository()
            .getPoll('pollId')
            .asStream()
            .listen((poll) {
              print('Poll question: \${poll?.question}');
              print('Total votes: \${poll?.voteCount}');
              for (final answer in poll?.answers ?? []) {
                print('Option: \${answer.dataValue}, votes: \${answer.voteCount}');
              }
            });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function pollObserve() {
  const liveObject = PostRepository.getPost('pollPostId123');
  liveObject.on('dataUpdated', (post) => {
    console.log('Poll updated:', post?.data);
  });
}
```

</CodeGroup>
