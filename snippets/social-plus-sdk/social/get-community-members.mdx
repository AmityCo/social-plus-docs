{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-community-members | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getCommunityMembers() {
        // TODO: implement get community members using Social Plus SDK
    }
```

```swift iOS
func getCommunityMembers() {
        let communityRepository = AmityCommunityRepository(client: AmityManager.shared.client!)
        let membershipRepository = communityRepository.membershipParticipation(forCommunityId: "communityId")
        
        let queryOptions = AmityCommunityMemberQueryOptions()
        let liveCollection = membershipRepository?.getMembers(options: queryOptions)
        
        let token = liveCollection?.observe { collection, changes, error in
            guard error == nil else { return }
            print("Community members count: \(collection.count())")
            for member in collection.allObjects() {
                print("Member: \(member.displayName ?? ""), role: \(member.roles)")
            }
        }
        // Retain token as long as observation is needed
    }
```

```dart Flutter
void getCommunityMembers() async {
    AmitySocialClient.newCommunityRepository()
        .membership(communityId: 'communityId')
        .getMembers()
        .getPagingData(token: null, limit: 20)
        .asStream()
        .listen((result) {
      final members = result.data;
    });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

function getCommunityMembers() {
  const communityId = 'community123'
  const liveCollection = CommunityRepository.getCommunityMembers({ communityId, limit: 20 })
  liveCollection.on('dataUpdated', (members) => {
    console.log('Community members:', members)
  })
}
```

</CodeGroup>
