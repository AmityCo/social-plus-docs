{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-edit-image | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void editImagePost(AmityPost post, File newImageFile) {
    AmityCoreClient.newFileRepository()
        .uploadImage(newImageFile)
        .stream
        .listen((amityUploadResult) {
      amityUploadResult.when(
        progress: (uploadInfo, cancelToken) {},
        complete: (AmityImage image) {
          AmitySocialClient.newPostRepository()
              .editPost(postId: post.postId!)
              .image([image])
              .edit()
              .then((AmityPost updatedPost) {
                // post image updated
              })
              .onError((error, stackTrace) {
                // handle error
              });
        },
        error: (error) {
          // handle upload error
        },
        cancel: () {},
      );
    });
  }
```

</CodeGroup>
