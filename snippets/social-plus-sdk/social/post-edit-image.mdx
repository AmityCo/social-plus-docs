{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-edit-image | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun postEditImage() {
        // Edit an existing post's text content
        AmitySocialClient.newPostRepository()
            .editPost(postId = "post123")
            .text("Updated post content")
            .build()
            .subscribe({ post ->
                // Post updated successfully
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
let postRepository = AmityPostRepository(client: AmityClient.shared)
        let postId = "post_id"
        let fileRepository = AmityFileRepository(client: AmityClient.shared)
        let imageURL = URL(fileURLWithPath: "/path/to/new_image.jpg")
        fileRepository.uploadImage(fromLocalURL: imageURL, progress: nil) { imageData, error in
            if let error = error {
                print("Image upload failed: \(error.localizedDescription)")
                return
            }
            guard let imageData = imageData else { return }
            let builder = AmityImagePostBuilder()
            builder.setImages([imageData])
            postRepository.updatePost(withPostId: postId, builder: builder) { post, error in
                if let error = error {
                    print("Failed to edit image post: \(error.localizedDescription)")
                    return
                }
                print("Image post updated: \(post?.postId ?? "")")
            }
        }
```

```dart Flutter
void postEditImage() async {
    final fileInfo = await AmityCoreClient.newFileRepository()
        .uploadImage('path/to/new_image.jpg');
    await AmitySocialClient.newPostRepository()
        .editPost('postId')
        .image([fileInfo])
        .apply();
    print('Image post edited');
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function postEditImage(postId: string, text: string, imageFileIds: string[]) {
  const post = await PostRepository.updatePost({
    postId,
    dataType: 'image',
    data: { text },
    attachments: imageFileIds.map(fileId => ({ type: 'image', fileId })),
  })
  console.log('Image post edited:', post.postId)
  return post
}
```

</CodeGroup>
