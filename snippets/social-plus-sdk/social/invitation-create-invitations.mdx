{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: invitation-create-invitations | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createInvitations(communityId: String, userIds: List<String>) {
        userIds.forEach { userId ->
            AmitySocialClient.newCommunityRepository()
                .invitation()
                .create(communityId = communityId, userId = userId)
                .doOnComplete {
                    // invitation created for userId
                }
                .doOnError {
                    // handle error
                }
                .subscribe()
        }
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository(client: AmityUIKitManagerInternal.shared.client)
        let communityId = "community123"
        let userIds = ["user1", "user2", "user3"]
        communityRepository.createInvitations(userIds: userIds, communityId: communityId) { success, error in
            print("Invitations created: \(success)")
        }
```

```dart Flutter
void invitationCreateInvitations() async {
    final communityRepo = AmitySocialClient.newCommunityRepository();
    await communityRepo.createInvitation(
      communityId: 'communityId123',
      userIds: ['userId1', 'userId2'],
    );
    print('Invitations sent successfully.');
  }
```

```typescript TypeScript
import { InvitationRepository } from '@amityco/ts-sdk'

async function createInvitations() {
  await InvitationRepository.createInvitations('communityId', ['userId1', 'userId2'])
}
```

</CodeGroup>
