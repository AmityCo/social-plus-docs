{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: video-post-creation | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun videoPostCreation() {
        val videoUri: android.net.Uri = TODO("provide video uri")
        AmitySocialClient.newPostRepository()
            .createPost()
            .targetMe()
            .video(videoUri)
            .build()
            .post()
            .subscribe { post ->
                println("Video post created: \${post.getPostId()}")
            }
    }
```

```swift iOS
func videoPostCreation() {
        let client = AmityClient.shared
        let postRepo = AmityPostRepository(client: client)
        // First upload the video file
        let fileRepo = AmityFileRepository(client: client)
        let videoData = Data() // Your video data here
        fileRepo.uploadVideo(videoData, progress: nil) { videoData, error in
            guard let videoFileData = videoData, error == nil else { return }
            let builder = AmityVideoPostBuilder()
            builder.setText("Check out this video!")
            builder.setVideoFileData([videoFileData])
            postRepo.createPost(builder, targetId: "communityId", targetType: .community) { post, error in
                if let post = post {
                    print("Video post created: \(post.postId)")
                }
            }
        }
    }
```

```dart Flutter
void videoPostCreation() {
    AmitySocialClient.newPostRepository()
        .createPost()
        .targetMe()
        .video(['videoFileId1'])
        .build()
        .post();
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function createVideoPost() {
  const unsubscribe = PostRepository.createPost({
    targetType: 'community',
    targetId: 'community123',
    dataType: 'video',
    data: { text: 'My new video' },
    metadata: { videoIds: ['vid001'] },
  }, (post, error) => {
    if (error) return console.error(error)
    console.log('Video post created:', post)
    unsubscribe()
  })
}
```

</CodeGroup>
