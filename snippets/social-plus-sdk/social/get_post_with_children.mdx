{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_post_with_children | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getPostWithChildren() {
        // Get a post along with its children (child posts)
        AmitySocialClient.newPostRepository()
            .getPost("postId")
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(io.reactivex.rxjava3.android.schedulers.AndroidSchedulers.mainThread())
            .subscribe({ post ->
                val children = post.getChildren()
                // Render parent and child post content
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
// Get a post and its associated child posts
        let postRepository = AmityPostRepository(client: AmityManager.shared.client!)
        var token: AmityNotificationToken?
        let liveObject = postRepository.getPost(withPostId: "postId")
        token = liveObject.observe { post, error in
            if let error = error {
                print("Failed to get post with children: \(error.localizedDescription)")
                return
            }
            guard let post = post.object else { return }
            let children = post.getChildrenPosts() ?? []
            print("Post '\(post.postId)' has \(children.count) children")
            children.forEach { child in
                print("  Child type: \(child.dataType ?? ""), ID: \(child.postId)")
            }
        }
```

```dart Flutter
void getPostWithChildren() async {
    final post = await AmitySocialClient.newPostRepository()
        .getPost(postId: 'postId');
    final children = post.children;
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

function getPostWithChildren() {
  const postId = 'post123'
  const liveObject = PostRepository.getPost(postId)
  liveObject.on('dataUpdated', (post) => {
    console.log('Post:', post)
    console.log('Children:', post.children)
  })
}
```

</CodeGroup>
