{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-create | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void postCreate() async {
    final post = await AmitySocialClient.newPostRepository()
        .createPost()
        .targetMe()
        .text('Hello Amity!')
        .post();
    print('Created post: \${post.postId}');
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function createPost(targetId: string, targetType: 'community' | 'user') {
  const newPost = await PostRepository.createPost({
    targetId,
    targetType,
    data: { text: 'Hello, Social Plus!' },
    dataType: 'text',
  })

  console.log('Post created:', newPost.postId)
  return newPost
}
```

</CodeGroup>
