{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-pin-product | updated: 2026-05-02 */}

<CodeGroup>

```swift iOS
let postRepository = AmityPostRepository(client: client)
        do {
            try await postRepository.pinProduct(postId: "<post-id>", productId: "<product-id>")
            print("Product pinned to post")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void pinProductToPost(String postId, String productId) {
    AmitySocialClient.newPostRepository()
        .pinProduct(postId: postId, productId: productId)
        .then((_) {
          // product pinned to post
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function pinProduct() {
  await PostRepository.pinProduct('postId', 'productId')
}
```

</CodeGroup>
