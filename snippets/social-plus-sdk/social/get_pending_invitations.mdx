{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_pending_invitations | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getPendingInvitations() {
        AmitySocialClient.newCommunityRepository()
            .moderation("communityId")
            .getPendingInvitations()
            .subscribe()
    }
```

```swift iOS
func getPendingInvitations() {
        let communityRepository = AmityCommunityRepository(client: AmityManager.shared.client!)
        let liveCollection = communityRepository.getPendingInvitations(communityId: "communityId")
        liveCollection.observe { collection, _, error in
            if let error = error { print("Error: \(error)"); return }
            print("Pending invitations: \(collection.count())")
        }
    }
```

```dart Flutter
void getPendingInvitations() {
    AmitySocialClient.newCommunityRepository()
        .membership('communityId')
        .getPendingInvitations()
        .query();
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function getPendingInvitations() {
  const unsubscribe = CommunityRepository.getInvitations(
    { status: 'pending' },
    (invitations, error) => {
      if (error) return console.error(error)
      console.log('Pending invitations:', invitations)
      unsubscribe()
    }
  )
}
```

</CodeGroup>
