{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_fresh_live_collection | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getFreshLiveCollection() {
        // Refresh and observe a live collection for real-time updates
        AmitySocialClient.newPostRepository()
            .getPosts(targetId = "targetId", targetType = AmityPost.TargetType.USER)
            .build()
            .query()
            .subscribe { liveCollection ->
                liveCollection.refresh()
                /* handle updated collection */
            }
    }
```

```dart Flutter
void getFreshLiveCollection() {
    // TODO: implement SDK operation
    throw UnimplementedError();
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function getFreshLiveCollection() {
  const liveCollection = PostRepository.getPosts({ targetId: 'communityId123', targetType: 'community' });
  await liveCollection.refresh();
  liveCollection.on('dataUpdated', (posts) => {
    console.log('Fresh posts count:', posts.length);
  });
}
```

</CodeGroup>
