{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-pinned-posts | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getPinnedPosts(communityId: String) {
        AmitySocialClient.newPostRepository()
            .getPinnedPosts(targetId = communityId)
            .doOnNext { pagedList ->
                // Handle paged list of pinned posts
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let postRepository = AmityPostRepository(client: AmityManager.shared.client!)
        let liveCollection = postRepository.getPinnedPosts(targetId: "communityId", targetType: .community)
        liveCollection.observe { collection, _, error in
            guard error == nil else { return }
            print("Pinned posts: \(collection.count())")
        }
```

```dart Flutter
void getPinnedPosts() {
    final liveCollection = AmitySocialClient.newPostRepository()
        .getPinnedPosts('communityId')
        .getPagingData();
    liveCollection.asStream().listen((List<AmityPost> pinnedPosts) {
      print('Pinned posts: \${pinnedPosts.length}');
    });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

function getPinnedPosts() {
  const unsubscribe = PostRepository.getPinnedPosts(
    { communityId: 'communityId' },
    ({ data: posts }) => {
      console.log('Pinned posts:', posts)
    },
  )

  return unsubscribe
}
```

</CodeGroup>
