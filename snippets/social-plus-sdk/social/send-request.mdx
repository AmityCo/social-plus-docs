{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: send-request | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun sendRequest() {
        // TODO: Implement operation for send-request
    }
```

```swift iOS
let userRepository = AmityUserRepository(client: AmityManager.shared.client!)
        userRepository.follow(withUserId: "targetUserId") { status, error in
            print("Follow request sent, status: \(String(describing: status))")
        }
```

```dart Flutter
void sendFollowRequest(String userId) {
    AmityCoreClient.newUserRepository()
        .relationship()
        .follow(userId)
        .then((AmityFollowStatus followStatus) {
          // follow request sent or user followed
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function sendRequest() {
  import { UserRepository } from '@amityco/ts-sdk';

  await UserRepository.follow('user-id-456');
  console.log('Follow request sent');
}
```

</CodeGroup>
