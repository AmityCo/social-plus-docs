{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: update_post_with_mention_api | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun updatePostWithMentionApi() {
        // Update operation for update_post_with_mention_api
        AmitySocialClient.newPostRepository()
            .updatePost("postId")
            .text("Updated text")
            .build()
            .update()
            .subscribe({ post -> }, { error -> })
    }
```

```swift iOS
do {
            let post = try await postRepository.editPost(withId: "<post-id>", builder: amityPostBuilder, metadata: nil, mentionees: amityMentioneesBuilder)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void updatePostWithMentionApi() {
    final mentionees = AmityMentioneesBuilder()
        .users(['userId1'])
        .build();
    AmitySocialClient.newPostRepository()
        .updatePost('postId')
        .text('Hey @user1, check this out!')
        .mentionees(mentionees)
        .update()
        .then((post) => print('Post with mention updated via API'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function updatePostWithMentionApi() {
  const { data: post } = await PostRepository.updatePost({
    postId: 'postId',
    data: { text: 'Updated: Hello @userId!' },
    metadata: {
      mentioned: [{ userId: 'userId', index: 9, length: 7 }],
    },
  })
  console.log('Post with mention updated:', post.postId)
}
```

</CodeGroup>
