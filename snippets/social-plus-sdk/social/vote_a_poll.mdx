{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: vote_a_poll | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun voteAPoll() {
        // Vote on a poll with selected answer IDs
        AmitySocialClient.newPollRepository()
            .vote(pollId = "pollId", answerIds = listOf("answerId1", "answerId2"))
            .subscribe()
    }
```

```swift iOS
do {
            let result = try await pollRepository.votePoll(withId: "<poll-id>", answerIds: ["<answer-id>"])
        } catch {
            // Handle error here
        }
```

```dart Flutter
Future<void> voteAPoll() async {
    final postId = 'pollPostId';
    final post = await AmitySocialClient.newPostRepository()
        .getPost(postId)
        .future;
    final poll = post.data as AmityPostPollData?;
    if (poll != null) {
      await AmitySocialClient.newPollRepository()
          .votePoll(
            pollId: poll.pollId!,
            answerIds: ['answerId1'],
          );
    }
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function voteAPoll() {
  const pollId = 'poll-id';
  const answerIds = ['answer-id-1'];
  await PollRepository.vote({ pollId, answerIds });
  console.log('Voted on poll:', pollId);
}
```

</CodeGroup>
