{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: check_post_flag | updated: 2026-05-02 */}

<CodeGroup>

```swift iOS
do {
            let isFlagged = try await postRepository.isFlaggedByMe(withId: "<post-id>")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void checkPostFlag() {
    AmitySocialClient.newPostRepository()
        .getPost('postId')
        .then((post) {
          final isFlaggedByMe = post.isFlaggedByMe;
        })
        .onError((error, stackTrace) { });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function checkPostFlag(postId: string) {
  try {
    const isFlagged = await PostRepository.isFlaggedByMe(postId);
    console.log('Post flagged by me:', isFlagged);
    return isFlagged;
  } catch (error) {
    console.error('Failed to check post flag status:', error);
  }
}
```

</CodeGroup>
