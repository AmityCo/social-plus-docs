{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-relationships-service-locator | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun userRelationshipsServiceLocator() {
        // Access the user relationship service via the repository
        val userRelationshipRepository = AmitySocialClient.newUserRepository()
            .relationship()
    }
```

```swift iOS
let client = AmityClient.shared
        let followManager = AmityFollowManager(client: client)
        print("AmityFollowManager initialized with client: \(client)")
```

```dart Flutter
void accessUserRelationships() {
    final relationship = AmityCoreClient.newUserRepository().relationship();
    // use relationship to follow, unfollow, block, or query followers/followings
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function userRelationshipsServiceLocator() {
  const client = createClient('YOUR_API_KEY');
  const userRepo = client.getUserRepository();
  console.log('UserRepository initialized:', userRepo);
}
```

</CodeGroup>
