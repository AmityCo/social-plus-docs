{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: comment_get | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void commentGet() async {
    final commentRepo = AmitySocialClient.newCommentRepository();
    final liveCollection = commentRepo
        .getComments()
        .post('postId')
        .parentId(null)
        .getLiveCollection();
    liveCollection.asStream().listen((comments) {
      print('Comments fetched: \${comments.length}');
    });
  }
```

```typescript TypeScript
import { CommentRepository } from '@amityco/ts-sdk'

function commentGet(postId: string) {
  const unsubscribe = CommentRepository.getComments(
    { referenceId: postId, referenceType: 'post' },
    ({ data: comments, loading, error }) => {
      if (loading) return
      if (error) { console.error(error); return }
      console.log('Comments:', comments)
    }
  )
  return unsubscribe
}
```

</CodeGroup>
