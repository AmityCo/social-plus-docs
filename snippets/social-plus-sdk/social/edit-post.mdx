{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: edit-post | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun editPost(postId: String, newText: String) {
        AmitySocialClient.newPostRepository()
            .editPost(postId = postId)
            .text(text = newText)
            .build()
            .apply()
            .doOnSuccess { post: AmityPost ->
                // AmityPost updated
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let postRepository = AmityPostRepository()
        let builder = AmityTextPostBuilder()
        builder.setText("Updated post content")
        postRepository.updatePost(withId: "postId", builder: builder) { post, error in
            if let error = error {
                print("Failed to edit post: \(error.localizedDescription)")
            } else {
                print("Post updated: \(post?.postId ?? "")")
            }
        }
```

```dart Flutter
void editPost() {
    AmitySocialClient.newPostRepository()
        .getPost('postId')
        .then((AmityPost post) {
          post
              .edit()
              .text('Updated post content')
              .build()
              .apply()
              .then((_) {
                // Post edited
              })
              .onError((error, stackTrace) {
                // Handle error
              });
        });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function editPost() {
  const updated = await PostRepository.editPost({
    postId: 'postId',
    data: { text: 'Updated post content' },
  })
  console.log('Post edited:', updated)
}
```

</CodeGroup>
