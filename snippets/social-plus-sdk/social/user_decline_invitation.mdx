{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user_decline_invitation | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun userDeclineInvitation() {
        // Decline a community invitation
        AmitySocialClient.newCommunityRepository()
            .membership(communityId = "communityId")
            .declineInvitation()
            .subscribe()
    }
```

```swift iOS
let client = AmityClient.shared
        let communityRepository = AmityCommunityRepository(client: client)
        communityRepository.declineInvitation(withCommunityId: "communityId") { success, error in
            if success { print("Invitation declined successfully") }
        }
```

```dart Flutter
void userDeclineInvitation() async {
    await AmitySocialClient.newCommunityRepository()
        .membership('communityId')
        .declineInvitation();
    print('Community invitation declined');
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function userDeclineInvitation(communityId: string) {
  await CommunityRepository.declineInvitation(communityId)
  console.log(`Invitation to community ${communityId} declined.`)
}
```

</CodeGroup>
