{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: query-user-feed | updated: 2026-05-02 */}

<CodeGroup>

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

function queryUserFeed() {
  const liveCollection = PostRepository.getPosts({
    targetType: 'user',
    targetId: 'userId',
    sortBy: 'lastCreated',
  })

  liveCollection.on('dataUpdated', (posts) => {
    console.log('User feed posts:', posts)
  })

  liveCollection.on('dataError', (error) => {
    console.error('Failed to query user feed:', error)
  })

  return () => liveCollection.dispose()
}
```

</CodeGroup>
