{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: story-create-image | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun storyCreateImage() {
        val targetId = "communityId"
        val imageUri = android.net.Uri.parse("file:///path/to/image.jpg")
        AmitySocialClient.newStoryRepository()
            .createImageStory(
                targetType = com.amityco.socialcloud.sdk.model.social.story.AmityStory.TargetType.COMMUNITY,
                targetId = targetId,
                imageUri = imageUri,
                imageDisplayMode = com.amityco.socialcloud.sdk.model.social.story.AmityStoryImageDisplayMode.FILL,
                items = emptyList()
            )
            .subscribe()
    }
```

```swift iOS
let storyRepository = AmityStoryRepository(client: client)
        let options = AmityImageStoryCreateOptions(targetType: .community, tartgetId: "<community-id>", imageFileURL: URL(string: "file:///path/to/image.jpg")!, metadata: nil, items: [], imageDisplayMode: .fit)
        do {
            let post = try await storyRepository.createImageStory(options: options)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void storyCreateImage() async {
    final story = await AmitySocialClient.newStoryRepository()
        .createStory()
        .targetCommunity('communityId')
        .image(
          filePath: '/path/to/image.jpg',
          items: [],
        )
        .create();
    print('Image story created: \${story.storyId}');
  }
```

```typescript TypeScript
import { StoryRepository } from '@amityco/ts-sdk'

async function createImageStory(targetId: string, imageFileId: string) {
  const story = await StoryRepository.createImageStory({
    targetId,
    targetType: 'community',
    fileId: imageFileId,
    imageDisplayMode: 'fit',
    metadata: {},
  })

  console.log('Image story created:', story.storyId)
  return story
}
```

</CodeGroup>
