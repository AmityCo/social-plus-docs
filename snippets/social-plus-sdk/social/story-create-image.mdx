{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: story-create-image | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createImageStory(targetId: String, targetType: AmityStory.TargetType, imageUri: Uri) {
        AmitySocialClient.newStoryRepository()
            .createImageStory(
                targetType = targetType,
                targetId = targetId,
                imageUri = imageUri
            )
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let storyRepository = AmityStoryRepository(client: client)
        let options = AmityImageStoryCreateOptions(targetType: .community, tartgetId: "<community-id>", imageFileURL: URL(string: "file:///path/to/image.jpg")!, metadata: nil, items: [], imageDisplayMode: .fit)
        do {
            let post = try await storyRepository.createImageStory(options: options)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void createImageStory(String communityId, File imageFile) {
    AmitySocialClient.newStoryRepository()
        .createImageStory(
            targetType: AmityStoryTargetType.COMMUNITY,
            targetId: communityId,
            imageFile: imageFile,
            imageDisplayMode: AmityStoryImageDisplayMode.FIT)
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { StoryRepository } from '@amityco/ts-sdk'

async function createImageStory(file) {
  const formData = new FormData()
  formData.append('files', file)

  const { data: story } = await StoryRepository.createImageStory(
    'community',
    'communityId',
    formData,
    { customKey: 'value' },
    'fit',
  )
  console.log(story)
}
```

</CodeGroup>
