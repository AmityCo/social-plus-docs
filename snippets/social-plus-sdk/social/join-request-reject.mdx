{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: join-request-reject | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun rejectJoinRequest(communityId: String, userId: String) {
        AmitySocialClient.newCommunityRepository()
            .joinRequest(communityId = communityId)
            .reject(userId = userId)
            .doOnComplete {
                // join request rejected
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository()
        communityRepository.rejectJoinRequest(communityId: "communityId", userId: "userId") { error in
            if let error = error {
                print("Error rejecting join request: \(error)")
            } else {
                print("Join request rejected")
            }
        }
```

```dart Flutter
void joinRequestReject() async {
    final communityRepo = AmitySocialClient.newCommunityRepository();
    await communityRepo.rejectJoinRequest(
      communityId: 'communityId123',
      userId: 'userId123',
    );
    print('Join request rejected.');
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function rejectJoinRequest() {
  await CommunityRepository.declineJoinRequest('communityId', 'userId')
}
```

</CodeGroup>
