{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: comment_target | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun commentTarget() {
        // Query comments for a specific post target
        AmitySocialClient.newCommentRepository()
            .getComments()
            .post(postId = "postId")
            .build()
            .query()
            .subscribe()
    }
```

```dart Flutter
void commentTarget() {
    // Specify the target for a comment (post or content target)
    final commentTarget = AmityCommentTarget.post(postId: 'postId');
    AmitySocialClient.newCommentRepository()
        .createComment()
        .target(commentTarget)
        .create()
        .text(text: 'Great post!')
        .send()
        .then((comment) => print('Comment on target: \${comment.commentId}'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function commentTarget() {
  // Comment on a post
  const postComments = CommentRepository.getComments({
    referenceId: 'post-id-here',
    referenceType: 'post',
  });
  // Comment on a content
  const contentComments = CommentRepository.getComments({
    referenceId: 'content-id-here',
    referenceType: 'content',
  });
  console.log('Comment targets configured');
}
```

</CodeGroup>
