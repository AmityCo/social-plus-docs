{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: delete-community | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun deleteCommunity(communityId: String) {
        AmitySocialClient.newCommunityRepository()
            .deleteCommunity(communityId = communityId)
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository()
        communityRepository.deleteCommunity(withId: "communityId") { error in
            if let error = error {
                print("Failed to delete community: \(error.localizedDescription)")
            } else {
                print("Community deleted successfully")
            }
        }
```

```dart Flutter
void deleteCommunity() async {
    await AmitySocialClient.newCommunityRepository()
        .deleteCommunity('communityId');
    print('Community deleted');
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function deleteCommunity(communityId: string) {
  await CommunityRepository.deleteCommunity(communityId)
  console.log(`Community ${communityId} has been deleted.`)
}
```

</CodeGroup>
