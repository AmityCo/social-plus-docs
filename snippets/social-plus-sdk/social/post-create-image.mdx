{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-create-image | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createImagePost(targetId: String, imageUri: Uri) {
        AmitySocialClient.newPostRepository()
            .createPost()
            .targetUser(userId = targetId)
            .image(imageUri = imageUri)
            .build()
            .post()
            .doOnSuccess { post: AmityPost ->
                // handle created post
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let postRepository = AmityPostRepository(client: client)
        let fileRepository = AmityFileRepository(client: client)
        let builder = AmityImagePostBuilder()
        // Upload image first, then attach
        let imageURL = URL(string: "file:///path/to/image.jpg")!
        fileRepository.uploadImage(with: imageURL, isFullImage: true, progress: { _ in }) { [weak builder] data, error in
            guard let data = data else { return }
            builder?.setAttachments([data])
        }
        do {
            let post = try await postRepository.createImagePost(builder, targetId: "<community-id>", targetType: .community, metadata: nil, mentionees: nil)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void createImagePost(String communityId, File imageFile) {
    AmityCoreClient.newFileRepository()
        .uploadImage(imageFile)
        .stream
        .listen((amityUploadResult) {
      amityUploadResult.when(
        progress: (uploadInfo, cancelToken) {},
        complete: (AmityImage image) {
          AmitySocialClient.newPostRepository()
              .createPost()
              .targetCommunity(communityId)
              .image([image])
              .post()
              .then((AmityPost post) {
                // handle result
              })
              .onError((error, stackTrace) {
                // handle error
              });
        },
        error: (error) {
          // handle upload error
        },
        cancel: () {},
      );
    });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function createImagePost() {
  const { data: post } = await PostRepository.createPost({
    targetType: 'user',
    targetId: 'userId',
    dataType: 'image',
    data: { fileId: 'uploadedImageFileId' },
  })
  console.log(post)
}
```

</CodeGroup>
