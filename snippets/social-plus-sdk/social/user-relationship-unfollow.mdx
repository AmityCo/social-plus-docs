{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-relationship-unfollow | updated: 2026-04-30 */}

<CodeGroup>

```kotlin Android
fun unfollowUser(userId: String) {
        AmityCoreClient.newUserRepository()
            .relationship()
            .unfollow(userId = userId)
            .doOnComplete {
                // success
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let userRepository = AmityUserRepository(client: client)
        do {
            try await userRepository.userRelationship.unfollow(withUserId: "<user-id>")
            print("User unfollowed")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void unfollowUser(String userId) {
    AmityCoreClient.newUserRepository()
        .relationship()
        .unfollow(userId)
        .then((value) => {
              //success
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function unfollowUser() {
  await UserRepository.unfollow('userId')
}
```

</CodeGroup>
