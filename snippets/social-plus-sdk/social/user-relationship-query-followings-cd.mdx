{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-relationship-query-followings-cd | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun userRelationshipQueryFollowingsCd() {
        // Query live collection for user-relationship-query-followings-cd
        AmityCoreClient.newUserRepository()
            .relationship()
            .getFollowers("userId")
            .build()
            .query()
            .subscribe({ liveCollection -> }, { error -> })
    }
```

```swift iOS
let userRepository = AmityUserRepository(client: client)
        var token: AmityNotificationToken?
        token = userRepository.userRelationship.getFollowings(userId: "<user-id>", with: .all).observe { collection, change, error in
            guard error == nil else { return }
            print("Followings: \(collection.count())")
        }
```

```dart Flutter
void userRelationshipQueryFollowingsCd() {
    AmitySocialClient.newUserRepository()
        .relationship
        .getFollowings('targetUserId')
        .status(AmityFollowStatus.ACCEPTED)
        .getPagingData()
        .listen((result) => print('Followings: \${result.data.length}'))
        ..onError((error) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function userRelationshipQueryFollowingsCd() {
  const userId = 'target-user-id';
  const { data: followStatus } = await UserRepository.relationship.follow(userId);
  console.log('Follow status:', followStatus);
}
```

</CodeGroup>
