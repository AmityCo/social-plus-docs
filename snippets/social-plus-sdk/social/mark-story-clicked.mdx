{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: mark-story-clicked | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun markStoryAsSeen(
        story: AmityStory
    ) {
        // available as extension function
        story.analytics()
            .markLinkAsClicked()
    }
```

```swift iOS
let storyRepository = AmityStoryRepository(client: AmityUIKitManagerInternal.shared.client)
        let storyId = "story123"
        storyRepository.markStoryAsClicked(storyId: storyId) { success, error in
            print("Story click marked: \(success)")
        }
```

```dart Flutter
void markStoryClicked() async {
    final storyRepo = AmitySocialClient.newStoryRepository();
    await storyRepo.markStoryAsClicked(storyId: 'storyId123');
    print('Story marked as clicked.');
  }
```

```typescript TypeScript
import { StoryRepository } from '@amityco/ts-sdk'

async function markStoryLinkAsClicked() {
  await StoryRepository.markStoryLinkAsClicked('storyId')
}
```

</CodeGroup>
