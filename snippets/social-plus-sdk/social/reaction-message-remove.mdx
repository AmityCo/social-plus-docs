{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: reaction-message-remove | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun reactionMessageRemove() {
        // perform reaction message remove operation
        AmityChatClient.newMessageRepository()
            .addReaction("messageId", "like")
            .subscribe()
    }
```

```swift iOS
let reactionRepository = AmityReactionRepository(client: AmityUIKitManagerInternal.shared.client)
        reactionRepository.removeReaction("like", referenceId: "messageId", referenceType: .message) { success, error in
            if success {
                print("Reaction removed from message")
            } else if let error = error {
                print("Failed to remove reaction: \(error.localizedDescription)")
            }
        }
```

```dart Flutter
void removeMessageReaction(AmityMessage comment) {
    comment.react().removeReaction('like').then((value) => {
          //success
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function reactionMessageRemove() {
  await MessageRepository.removeReaction({
    messageId: 'messageId123',
    reactionName: 'thumbsup',
  });
  console.log('Reaction removed from message');
}
```

</CodeGroup>
