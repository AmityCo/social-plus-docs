{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: story-reach | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun storyReach() {
        val storyId = "storyId"
        AmitySocialClient.newStoryRepository()
            .getStory(storyId)
            .firstOrError()
            .subscribe { story ->
                val reachCount = story.getReach()
            }
    }
```

```swift iOS
let storyRepository = AmityStoryRepository()
        let liveObject = storyRepository.getStory(withId: "storyId")
        liveObject.observe { liveObject, error in
            guard let story = liveObject.object else { return }
            let reachCount = story.viewerCount
            print("Story reach count: \(reachCount)")
        }
```

```dart Flutter
void storyReach() {
    AmitySocialClient.newStoryRepository()
        .getStoryReach(storyId: 'storyId')
        .then((int reach) {
          print('Story reach: \$reach');
        });
  }
```

```typescript TypeScript
import { StoryRepository } from '@amityco/ts-sdk'

async function getStoryReach() {
  const unsubscribe = StoryRepository.getStoryByStoryId(
    'storyId',
    (result) => {
      if (result.data) {
        console.log('Story reach:', result.data.reach);
      }
    },
  );
  return unsubscribe;
}
```

</CodeGroup>
