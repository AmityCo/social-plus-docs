{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: search-membership-with-keyword | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun searchMembershipWithKeyword() {
        AmitySocialClient.newCommunityRepository()
            .membership(communityId = "communityId")
            .getMembers()
            .build()
            .query()
            .subscribe()
    }
```

```swift iOS
func searchMember(keyword: String,
                      filter: [AmityCommunityMembership.SearchFilter],
                      roles: [String],
                      sortBy: AmityCommunityMembershipSortOption) {
        
        let liveCollection = communityMembership.searchMembers(
            keyword: keyword,
            filter: filter,
            roles: roles,
            sortBy: sortBy,
            includeDeleted: false
        )
        
        token = liveCollection.observe { collection, change, error in
            for _ in collection.allObjects() {
                // List of user with related keyword.
            }
        }
        
    }
```

```dart Flutter
void searchMembershipWithKeyword() {
    AmityCoreClient.newSocialRepository()
        .community
        .searchMembers('communityId123', keyword: 'john')
        .build()
        .query()
        .listen((members) {
          for (final m in members) {
            print('Member: \${m.user?.displayName}');
          }
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function searchMembershipWithKeyword() {
  import { CommunityRepository } from '@amityco/ts-sdk';

  const unsubscribe = CommunityRepository.searchCommunityMembers(
    { communityId: 'community-id-123', displayName: 'john', limit: 20 },
    ({ data: members, loading, error }) => {
      if (!loading && members) {
        console.log('Matching members:', members);
      }
    },
  );
  return unsubscribe;
}
```

</CodeGroup>
