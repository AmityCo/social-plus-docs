{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: stream-by-id | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun streamById() {
        AmitySocialClient.newStreamRepository()
            .getStreamById("streamId")
            .subscribe({ stream ->
                // Handle stream object
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
let streamRepository = AmityStreamRepository(client: AmityManager.shared.client!)
        let token = streamRepository.getStream(streamId: "streamId").observe { stream, error in
            print("Stream: \(stream.object?.streamId ?? "")")
        }
```

```dart Flutter
AmityStream? _amityStream;

  void getAmityStreamById(String streamId) {
    AmityVideoClient.newStreamRepository()
        .getStream(streamId)
        .then((value) => _amityStream = value);
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function streamById() {
  const streamRepository = new StreamRepository();
  const liveObject = streamRepository.getStreamById('streamId');
  liveObject.on('dataUpdated', (stream) => {
    console.log('Stream:', stream);
  });
}
```

</CodeGroup>
