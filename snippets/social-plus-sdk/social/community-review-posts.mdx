{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-review-posts | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun communityReviewPosts() {
        // TODO: implement community review posts using Social Plus SDK
    }
```

```swift iOS
func communityReviewPosts() {
        let client = AmityClient.shared
        let feedRepository = AmityFeedRepository(client: client)
        let liveCollection = feedRepository.getCommunityFeed("community_id", sortBy: .lastCreated, includeDeleted: false, feedType: .reviewing)
        liveCollection.observe { collection, _, error in
            guard error == nil else { return }
            print("Posts pending review: \(collection.count())")
        }
    }
```

```dart Flutter
void communityReviewPosts() {
    // Query posts pending review in a community
    final controller = PagingController<AmityPost>(
      pageFuture: (token) => AmitySocialClient.newFeedRepository()
          .getCommunityFeed('communityId')
          .feedType(AmityFeedType.reviewing)
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    );
    controller.addListener(() {
      final posts = controller.itemList ?? [];
      print('Posts pending review: \${posts.length}');
    });
    controller.fetchNextPage();
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function communityReviewPosts() {
  const client = createClient('YOUR_API_KEY')
  const liveCollection = client.postRepository.getPendingPosts('communityId123')
  liveCollection.on('dataUpdated', (posts) => {
    console.log('Posts pending review:', posts)
  })
}
```

</CodeGroup>
