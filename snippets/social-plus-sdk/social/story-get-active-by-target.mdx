{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: story-get-active-by-target | updated: 2026-05-02 */}

<CodeGroup>

```swift iOS
collection = storyRepository.getActiveStoriesByTarget(
            targetType: .community,
            targetId: "<community-id>",
            sortOption: .lastCreated
        )
        collection?.observe { liveCollection, _, error in
            if let error = error {
                // Handle error here
                print("Error: \(error)")
                return
            }
            for i in 0..<liveCollection.count() {
                if let story = liveCollection.object(at: i) {
                    print("Story ID: \(story.storyId)")
                }
            }
        }
```

```typescript TypeScript
import { StoryRepository } from '@amityco/ts-sdk'

function watchActiveStories() {
  const unsubscribe = StoryRepository.getActiveStoriesByTarget(
    { targetId: 'communityId', targetType: 'community' },
    ({ data: stories }) => {
      console.log(stories)
    },
  )
  return unsubscribe
}
```

</CodeGroup>
