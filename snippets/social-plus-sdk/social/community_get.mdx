{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community_get | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void communityGet() async {
    final communityRepo = AmitySocialClient.newCommunityRepository();
    final community = await communityRepo.getCommunity('communityId').first;
    print('Community fetched: \${community.communityId}');
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

function communityGet(communityId: string) {
  const unsubscribe = CommunityRepository.getCommunity(communityId, ({ data: community, loading, error }) => {
    if (loading) return
    if (error) { console.error(error); return }
    console.log('Community:', community)
    unsubscribe()
  })
}
```

</CodeGroup>
