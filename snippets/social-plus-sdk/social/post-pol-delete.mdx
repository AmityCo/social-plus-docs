{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-pol-delete | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun deletePoll(
        pollRepository: AmityPollRepository,
        poll: AmityPoll
    ) {
        pollRepository.deletePoll(pollId = poll.getPollId())
            .doOnComplete {
                // Void
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let client = AmityClient.shared
        let postRepository = AmityPostRepository(client: client)
        postRepository.deletePost(withId: "<pollPostId>", parentId: nil) { success, error in
            if let error = error { print("Error: \(error)"); return }
            print("Poll post deleted: \(success)")
        }
```

```dart Flutter
void deletePoll(String pollId) {
    AmitySocialClient.newPollRepository()
        .deletePoll(pollId)
        .then((value) {
          // poll deleted successfully
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function postPolDelete() {
  const { PostRepository } = Amity;
  await PostRepository.deletePost('pollPostId');
  console.log('Poll post deleted successfully');
}
```

</CodeGroup>
