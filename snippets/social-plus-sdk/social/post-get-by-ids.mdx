{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-get-by-ids | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun postGetByIds() {
        AmitySocialClient.newPostRepository()
                .getPosts(listOf("postId1", "postId2"))
                .subscribe({ posts ->
                    // Handle multiple posts
                }, { error ->
                    // Handle error
                })
    }
```

```swift iOS
func postGetByIds() {
        let postRepository = AmityPostRepository(client: AmityClient.shared)
        let postIds = ["postId1", "postId2", "postId3"]
        for postId in postIds {
            let livePost = postRepository.getPost(postId)
            let token = livePost.observe { liveObject, error in
                guard let post = liveObject.object else { return }
                print("Post retrieved: \(post.postId)")
            }
        }
    }
```

```dart Flutter
void getPostsByIds(List<String> postIds) {
    for (final postId in postIds) {
      AmitySocialClient.newPostRepository()
          .getPost(postId)
          .then((AmityPost post) => {
                //handle result
              })
          .onError((error, stackTrace) => {
                //handle error
              });
    }
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function getPostsByIds() {
  const liveCollection = PostRepository.getPostsByIds(['postId1', 'postId2'])
  liveCollection.on('dataUpdated', (posts) => {
    console.log(posts)
  })
}
```

</CodeGroup>
