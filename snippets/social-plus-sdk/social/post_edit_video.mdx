{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post_edit_video | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun postEditVideo() {
        AmitySocialClient.newPostRepository()
                .editPost("postId")
                .video(listOf(newVideoFile))
                .build()
                .edit()
                .subscribe({ post ->
                    // Video post updated
                }, { error ->
                    // Handle error
                })
    }
```

```swift iOS
func postEditVideo() {
        let postRepository = AmityPostRepository(client: AmityClient.shared)
        let builder = AmityPostUpdateBuilder()
        builder.setText("Updated video post text")
        postRepository.updatePost(withId: "postId", builder: builder) { post, error in
            if let error = error {
                print("Failed to edit video post: \(error.localizedDescription)")
                return
            }
            print("Video post updated: \(post?.postId ?? "")")
        }
    }
```

```dart Flutter
void postEditVideo() async {
    final postRepository = AmitySocialClient.newPostRepository();
    await postRepository
        .editPost('postId')
        .video([AmityVideo(fileId: 'newVideoFileId')])
        .apply();
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function editVideoPost() {
  const updateData = {
    postId: 'videoPostId123',
    data: { text: 'Updated description for video post' },
    attachments: [{ type: 'video' as const, fileId: 'newVideoFileId' }],
  };

  const livePost = PostRepository.updatePost(updateData);
  livePost.on('dataUpdated', post => console.log('Video post updated:', post.postId));
  livePost.on('dataError', err => console.error('Error updating video post:', err));
}
```

</CodeGroup>
