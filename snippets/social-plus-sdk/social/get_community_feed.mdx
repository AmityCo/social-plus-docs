{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_community_feed | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getCommunityFeed() {
        // Query the post feed for a community
        AmitySocialClient.newFeedRepository()
            .getCommunityFeed(communityId = "communityId")
            .build()
            .query()
            .subscribe { feed -> /* handle community feed posts */ }
    }
```

```dart Flutter
void getCommunityFeed() {
    AmitySocialClient.newFeedRepository()
        .getCommunityFeed(communityId: 'communityId')
        .getPagingData()
        .listen((posts) {
      for (final post in posts) {
        print('Post: \${post.postId}');
      }
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function getCommunityFeed() {
  const liveCollection = PostRepository.getPosts({ targetId: 'communityId123', targetType: 'community' });
  liveCollection.on('dataUpdated', (posts) => {
    console.log('Community feed posts:', posts.length);
  });
}
```

</CodeGroup>
