{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-global-feed-posts | updated: 2026-05-02 */}

<CodeGroup>

```typescript TypeScript
import { FeedRepository } from '@amityco/ts-sdk'

function getGlobalFeedPosts() {
  const unsubscribe = FeedRepository.getGlobalFeed(
    {},
    (response) => {
      if (response.error) throw response.error
      if (!response.loading) {
        console.log('Global feed posts:', response.data.length)
        response.data.forEach((post) => {
          console.log('Post ID:', post.postId)
        })
      }
    },
  )
  return unsubscribe
}
```

</CodeGroup>
