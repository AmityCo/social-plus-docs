{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create-post-repository | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createPostRepository(): AmityPostRepository {
        return AmitySocialClient.newPostRepository()
    }
```

```swift iOS
let postRepository = AmityPostRepository(client: AmityClient.shared)
        print("AmityPostRepository initialized: \(postRepository)")
```

```dart Flutter
void createPostRepository() {
    final postRepository = AmitySocialClient.newPostRepository();
    postRepository
        .createPost()
        .targetMe()
        .text('Post text')
        .post()
        .then((post) { })
        .onError((error, stackTrace) { });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function createPostRepository(targetId: string, text: string) {
  try {
    const { data: post } = await PostRepository.createPost({
      targetType: 'community',
      targetId,
      dataType: 'text',
      data: { text },
    });
    console.log('Post repository created post:', post.postId);
    return post;
  } catch (error) {
    console.error('Post repository operation failed:', error);
  }
}
```

</CodeGroup>
