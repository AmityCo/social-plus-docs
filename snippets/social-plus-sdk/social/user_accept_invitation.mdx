{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user_accept_invitation | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun userAcceptInvitation() {
        // Accept a community invitation
        AmitySocialClient.newCommunityRepository()
            .membership(communityId = "communityId")
            .acceptInvitation()
            .subscribe()
    }
```

```swift iOS
let client = AmityClient.shared
        let communityRepository = AmityCommunityRepository(client: client)
        communityRepository.acceptInvitation(withCommunityId: "communityId") { success, error in
            if success { print("Invitation accepted successfully") }
        }
```

```dart Flutter
void userAcceptInvitation() async {
    await AmitySocialClient.newCommunityRepository()
        .membership('communityId')
        .acceptInvitation();
    print('Community invitation accepted');
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function userAcceptInvitation(communityId: string) {
  await CommunityRepository.acceptInvitation(communityId)
  console.log(`Invitation to community ${communityId} accepted.`)
}
```

</CodeGroup>
