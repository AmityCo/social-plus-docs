{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_feed_type | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getFeedType() {
        // Get the feed type (published or reviewing) for a post target
        AmitySocialClient.newFeedRepository()
            .getCommunityFeed(communityId = "communityId")
            .feedType(AmityFeedType.PUBLISHED)
            .build()
            .query()
            .subscribe { feed -> /* handle feed by type */ }
    }
```

```swift iOS
let feedType = post.getFeedType()
```

```dart Flutter
void getFeedType() {
    // TODO: implement SDK operation
    throw UnimplementedError();
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function getFeedType() {
  const liveCollection = PostRepository.getPosts({ targetId: 'communityId123', targetType: 'community', feedType: 'reviewing' });
  liveCollection.on('dataUpdated', (posts) => {
    console.log('Posts under review:', posts.length, 'feedType:', posts[0]?.feedType);
  });
}
```

</CodeGroup>
