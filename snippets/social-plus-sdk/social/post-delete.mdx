{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-delete | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun postDelete() {
        // TODO: implement post delete using Social Plus SDK
    }
```

```swift iOS
func postDelete() {
        let feedRepository = AmityFeedRepository(client: AmityManager.shared.client!)
        
        feedRepository.deletePost(withPostId: "postId") { success, error in
            if let error = error {
                print("Post deletion failed: \(error)")
                return
            }
            print("Post deleted successfully: \(success)")
        }
    }
```

```dart Flutter
void postDelete() async {
    await AmitySocialClient.newPostRepository()
        .deletePost(postId: 'postId');
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function deletePost(postId: string) {
  await PostRepository.deletePost(postId)
  console.log(`Post ${postId} has been deleted.`)
}
```

</CodeGroup>
