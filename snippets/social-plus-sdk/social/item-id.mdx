{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: item-id | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getItemId(notificationItem: AmityNotificationItem): String {
        return notificationItem.getItemId()
    }
```

```swift iOS
let notificationRepository = AmityNotificationRepository(client: AmityUIKitManagerInternal.shared.client)
        let collection = notificationRepository.getNotifications()
        collection.observe { liveCollection, _, _ in
            let items = liveCollection.allObjects()
            for item in items {
                print("Notification item ID: \(item.notificationId)")
            }
        }
```

```dart Flutter
void itemId() async {
    final notificationRepo = AmitySocialClient.newNotificationRepository();
    final items = await notificationRepo.getNotificationItems();
    for (final item in items) {
      print('Notification item ID: \${item.notificationId}');
    }
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function getNotificationItemId() {
  const liveCollection = UserRepository.getNotificationTray()
  liveCollection.on('dataUpdated', (items) => {
    items.forEach((item) => {
      console.log(item.notificationId)
    })
  })
}
```

</CodeGroup>
