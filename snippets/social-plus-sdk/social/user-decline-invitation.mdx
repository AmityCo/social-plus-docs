{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-decline-invitation | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun userDeclineInvitation() {
        // Perform user-decline-invitation operation
        AmityCoreClient.newUserRepository()
            .getUser("userId")
            .asFlowable()
            .subscribe({ user -> }, { error -> })
    }
```

```swift iOS
let communityId = "communityId"
        AmitySDK.declineCommunityInvitation(communityId: communityId) { success, error in
            if let error = error {
                print("Failed to decline invitation: \(error)")
                return
            }
            print("Community invitation declined")
        }
```

```dart Flutter
void userDeclineInvitation() async {
    await AmitySocialClient.newCommunityRepository()
        .membership('communityId')
        .declineInvitation();
    print('Community invitation declined');
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function userDeclineInvitation(communityId: string) {
  await CommunityRepository.declineInvitation(communityId)
  console.log(`Invitation to community ${communityId} declined.`)
}
```

</CodeGroup>
