{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-relationship-block | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun blockUser(userId: String) {
        AmityCoreClient.newUserRepository()
            .relationship()
            .blockUser(userId = userId)
            .doOnComplete {
                // success
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let userRepository = AmityUserRepository(client: client)
        do {
            try await userRepository.userRelationship.blockUser(withUserId: "<user-id>")
            print("User blocked")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void userRelationshipBlock() {
    AmitySocialClient.newUserRepository()
        .relationship
        .blockUser('targetUserId')
        .then((_) => print('User blocked successfully'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function blockUser() {
  await UserRepository.blockUser('userId')
}
```

</CodeGroup>
