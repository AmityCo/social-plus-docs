{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: list_communities | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun listCommunities() {
        AmitySocialClient.newCommunityRepository()
            .getCommunityCollection()
            .filter(com.amityco.socialcloud.sdk.model.social.community.AmityCommunityFilter.MEMBER)
            .build()
            .query()
            .subscribe()
    }
```

```swift iOS
func listCommunities() {
        let communityRepository = AmityCommunityRepository(client: AmityManager.shared.client!)
        let query = AmityCommunityQuery()
        query.filter = .all
        query.sortBy = .displayName
        let collection = communityRepository.getCommunities(with: query)
        collection.observe { collection, _, error in
            guard error == nil else { return }
            collection.snapshots.forEach { community in
                print("Community: \(community.displayName ?? "")")
            }
        }
    }
```

```dart Flutter
void listCommunities() async {
    final communityRepo = AmitySocialClient.newCommunityRepository();
    final query = communityRepo
        .getCommunities()
        .filter(AmityCommunityFilter.ALL)
        .sortBy(AmityCommunitySortOption.DISPLAY_NAME)
        .build()
        .getLiveCollection();
    query.stream.listen((communities) {
      for (final community in communities) {
        print('Community: \${community.displayName}');
      }
    });
    query.loadNext();
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function listCommunities() {
  const liveCollection = CommunityRepository.getCommunities({
    filter: 'all',
    sortBy: 'displayName',
    limit: 20,
  })
  const unsubscribe = liveCollection.subscribe(({ data: communities, loading, hasNextPage }) => {
    if (!loading) {
      console.log('Communities:', communities)
      console.log('Has next page:', hasNextPage)
      if (hasNextPage) liveCollection.nextPage()
    }
  })
  return unsubscribe
}
```

</CodeGroup>
