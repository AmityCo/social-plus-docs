{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: join-request-approve | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun approveJoinRequest(communityId: String, userId: String) {
        AmitySocialClient.newCommunityRepository()
            .joinRequest(communityId = communityId)
            .approve(userId = userId)
            .doOnComplete {
                // join request approved
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository()
        communityRepository.approveJoinRequest(communityId: "communityId", userId: "userId") { error in
            if let error = error {
                print("Error approving join request: \(error)")
            } else {
                print("Join request approved")
            }
        }
```

```dart Flutter
void joinRequestApprove() async {
    final communityRepo = AmitySocialClient.newCommunityRepository();
    await communityRepo.approveJoinRequest(
      communityId: 'communityId123',
      userId: 'userId123',
    );
    print('Join request approved.');
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function approveJoinRequest() {
  await CommunityRepository.approveJoinRequest('communityId', 'userId')
}
```

</CodeGroup>
