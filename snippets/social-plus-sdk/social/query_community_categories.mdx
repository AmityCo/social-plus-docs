{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: query_community_categories | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun queryCommunityCategories() {
        // Query community categories
        AmitySocialClient.newCommunityRepository()
            .getCategories()
            .build()
            .query()
            .asLiveData()
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository(client: AmityUIKitManagerInternal.shared.client)
        let collection = communityRepository.getCategories(sortBy: .displayName, includeDeleted: false)
        collection.observe { liveCollection, _, error in
            guard error == nil else { return }
            let categories = liveCollection.snapshots
            print("Loaded \(categories.count) categories")
        }
```

```dart Flutter
void queryCommunityCategories() async {
    final communityRepository = AmitySocialClient.newCommunityRepository();
    communityRepository
        .getCategories()
        .sortBy(AmityCommunityCategorySortOption.NAME)
        .includeDeleted(false)
        .asStream()
        .listen((categories) {
      print('Categories: \${categories.length}');
    });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function queryCommunityCategoresUnderscore() {
  const params = {
    sortBy: 'name' as const,
    includeDeleted: false,
  };

  const liveCollection = CommunityRepository.getCategories(params);
  liveCollection.on('dataUpdated', categories => {
    console.log('Categories:', categories.map(cat => cat.name));
  });
  liveCollection.on('dataError', err => console.error('Error querying community categories:', err));
}
```

</CodeGroup>
