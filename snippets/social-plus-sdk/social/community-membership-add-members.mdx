{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-membership-add-members | updated: 2026-05-01 */}

<CodeGroup>

```kotlin Android
fun addMembers(communityId: String, userIds: List<String>) {
        AmitySocialClient.newCommunityRepository()
            .membership(communityId = communityId)
            .addMembers(userIds = userIds)
            .doOnComplete {
                // success
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository(client: client)
        let moderation = AmityCommunityModeration(client: client, communityId: "<community-id>")
        do {
            try await moderation.addMembers(["<user-id-1>", "<user-id-2>"])
            print("Members added successfully")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void addMembers(String communityId, List<String> userIds) {
    AmitySocialClient.newCommunityRepository()
        .membership(communityId)
        .addMembers(userIds)
        .then((value) => {
              //success
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function addCommunityMembers() {
  await CommunityRepository.addMembers('communityId', ['userId1', 'userId2'])
}
```

</CodeGroup>
