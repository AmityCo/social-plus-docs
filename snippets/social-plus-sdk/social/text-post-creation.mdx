{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: text-post-creation | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun textPostCreation() {
        val communityId = "communityId"
        AmitySocialClient.newPostRepository()
            .createPost()
            .targetCommunity(communityId)
            .text("This is a text post.")
            .build()
            .post()
            .subscribe()
    }
```

```swift iOS
let postRepository = AmityPostRepository()
        let builder = AmityTextPostBuilder()
        builder.setText("This is my new text post!")
        postRepository.createPost(
            builder,
            targetId: "communityId",
            targetType: .community
        ) { post, error in
            if let post = post {
                print("Text post created: \(post.postId)")
            } else if let error = error {
                print("Error creating text post: \(error)")
            }
        }
```

```dart Flutter
void textPostCreation() {
    AmitySocialClient.newPostRepository()
        .createPost()
        .targetCommunity(communityId: 'communityId')
        .text('Hello, Social Plus!')
        .build()
        .post()
        .then((AmityPost post) {
      // Text post created: post.postId
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function textPostCreation() {
  const postRepository = new PostRepository();
  const { data: post } = await postRepository.createPost({
    targetType: 'community',
    targetId: 'communityId',
    dataType: 'text',
    data: { text: 'Hello, Social Plus!' },
  });
  console.log('Text post created:', post.postId);
}
```

</CodeGroup>
