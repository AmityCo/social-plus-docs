{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create-poll | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createPoll(question: String, answers: List<String>) {
        AmitySocialClient.newPollRepository()
            .createPoll(question = question)
            .answers(answers = answers)
            .build()
            .create()
            .doOnSuccess { poll: AmityPoll ->
                // AmityPoll created
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```dart Flutter
void createPoll() {
    AmitySocialClient.newPollRepository()
        .createPoll()
        .question('What is your favorite color?')
        .answers(['Red', 'Blue', 'Green'])
        .closedIn(7 * 24 * 60 * 60)
        .create()
        .then((poll) { })
        .onError((error, stackTrace) { });
  }
```

```typescript TypeScript
import { PollRepository } from '@amityco/ts-sdk'

async function createPoll(question: string, answers: string[], closingInSeconds: number) {
  try {
    const { data: poll } = await PollRepository.createPoll({
      question,
      answers: answers.map(text => ({ dataType: 'text', data: { text } })),
      timeToClosePoll: closingInSeconds,
      answerType: 'single',
    });
    console.log('Poll created:', poll.pollId, 'Question:', poll.question);
    return poll;
  } catch (error) {
    console.error('Failed to create poll:', error);
  }
}
```

</CodeGroup>
