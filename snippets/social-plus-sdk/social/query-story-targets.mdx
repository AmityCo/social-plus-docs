{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: query-story-targets | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun queryStoryTargets() {
        // TODO: Implement Query story targets
    }
```

```swift iOS
func queryStoryTargets() {
        let storyRepository = AmityStoryRepository(client: AmityClient.shared)
        let collection = storyRepository.getStoryTargets(targetType: .community)
        token = collection.observe { liveCollection, change, error in
            print("Story targets count: \(liveCollection.count())")
        }
    }
```

```dart Flutter
void queryStoryTargets() async {
    final targets = AmitySocialClient.newStoryRepository()
        .getActiveStoryTargets()
        .build()
        .asStream();
    targets.listen((list) {
      print('Story targets: \${list.length}');
    });
  }
```

```typescript TypeScript
import { StoryRepository } from '@amityco/ts-sdk'

function queryStoryTargets() {
  const disposer = StoryRepository.getStoryTargets(
    { limit: 20 },
    ({ data: targets, loading, error }) => {
      if (loading) return
      if (error) {
        console.error('Failed to query story targets', error)
        return
      }
      console.log('Story targets:', targets)
    },
  )

  return disposer
}
```

</CodeGroup>
