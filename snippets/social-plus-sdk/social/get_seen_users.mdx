{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_seen_users | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getSeenUsers() {
        // Get the list of users who have seen/viewed a story
        AmitySocialClient.newStoryRepository()
            .getStoryViewers("storyId")
            .build()
            .query()
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(io.reactivex.rxjava3.android.schedulers.AndroidSchedulers.mainThread())
            .subscribe({ pagedList ->
                // Display viewer list
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
token = userRepository.getReachedUsers(viewedType: .story, viewedId: "story-id").observe { Collection, change, error in
            // Handle list of reach users here.
        }
```

```dart Flutter
void getSeenUsers() {
    AmitySocialClient.newStoryRepository()
        .getSeenUsers('storyId')
        .getPagingData()
        .listen((users) {
      for (final user in users) {
        print('Seen by: \${user.userId}');
      }
    });
  }
```

```typescript TypeScript
import { StoryRepository } from '@amityco/ts-sdk'

async function getSeenUsers() {
  const storyId = 'story-id-123'
  const { data: story } = await StoryRepository.getStory(storyId)
  const impressions = story?.impressionCount ?? 0
  console.log('Seen users count:', impressions)

  // Live collection of users who have seen the story
  const liveCollection = StoryRepository.getSeenUsersByStoryId(storyId)
  const unsubscribe = liveCollection.subscribe(({ data: users }) => {
    console.log('Users who have seen the story:', users)
  })
  return unsubscribe
}
```

</CodeGroup>
