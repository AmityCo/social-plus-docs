{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: play_video_example | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun playVideoExample() {
        // Get a video post stream for playback
        AmitySocialClient.newPostRepository()
            .getPost(postId = "post123")
            .subscribe({ post ->
                // Extract video stream URL for playback
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
guard let videoPath = videoData.getVideo(resolution: resolution),
                let url = URL(string: videoPath) else { return }
        let player = AVPlayer(url: url)
        let controller = AVPlayerViewController()
        controller.player = player
        present(controller, animated: true) {
            player.play()
        }
```

```dart Flutter
void playVideoExample() async {
        final videoController = AmityVideoPlayerController();
        await videoController.initialize(url: 'https://example.com/video.mp4');
        videoController.play();

        // Dispose when done
        // videoController.dispose();
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function playVideoExample() {
  const liveObject = PostRepository.getPost('postId123');
  liveObject.on('dataUpdated', (post) => {
    const videoFileId = post?.data?.videoFileId?.original;
    console.log('Playing video with file ID:', videoFileId);
  });
}
```

</CodeGroup>
