{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: comment-update-text | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun updateComment(commentId: String, text: String) {
        AmitySocialClient.newCommentRepository()
            .editComment(commentId = commentId)
            .text(text = text)
            .build()
            .apply()
            .doOnSuccess { comment: AmityComment ->
                // handle updated comment
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let commentRepository = AmityCommentRepository(client: client)
        let options = AmityCommentUpdateOptions(text: "Updated comment text")
        do {
            let comment = try await commentRepository.editComment(withId: "<comment-id>", options: options)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void updateComment(AmityComment comment) {
    AmitySocialClient.newCommentRepository()
        .editComment(commentId: comment.commentId!)
        .text('Updated comment text')
        .edit()
        .then((AmityComment updated) {
          // handle result
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { CommentRepository } from '@amityco/ts-sdk'

async function updateCommentText() {
  const { data: comment } = await CommentRepository.updateComment('commentId', {
    data: { text: 'Updated comment text' },
  })
  console.log(comment)
}
```

</CodeGroup>
