{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: live_collection_usage | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun liveCollectionUsage() {
        // Demonstrate live collection usage with automatic UI updates
        AmitySocialClient.newFeedRepository()
            .getGlobalFeed()
            .build()
            .query()
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(io.reactivex.rxjava3.android.schedulers.AndroidSchedulers.mainThread())
            .subscribe({ pagedList ->
                // Live collection auto-updates; refresh adapter
            }, { error ->
                // Handle error
            })
    }
```

```dart Flutter
void liveCollectionUsage() async {
    final postRepo = AmitySocialClient.newPostRepository();
    final liveCollection = postRepo
        .getPosts()
        .targetCommunity('communityId123')
        .build()
        .getLiveCollection();
    liveCollection.stream.listen((posts) {
      print('Live collection updated: \${posts.length} posts');
    });
    liveCollection.loadNext();
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function liveCollectionUsage() {
  const { data: posts, onNextPage, hasNextPage } = PostRepository.getPosts(
    { targetId: 'communityId', targetType: 'community' },
    ({ data, loading, error }) => {
      if (!loading && !error) {
        console.log('Posts:', data)
      }
    },
  )
}
```

</CodeGroup>
