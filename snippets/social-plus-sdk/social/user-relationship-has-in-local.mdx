{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-relationship-has-in-local | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun hasInLocal(userId: String): Boolean {
        return UserRelationshipRepository().hasInLocal(id = userId)
    }
```

```swift iOS
let client = AmityClient.shared
        let followManager = AmityFollowManager(client: client)
        let isFollowing = followManager.hasLocalFollowRelationship(withUserId: "userId")
        print("Has local follow relationship: \(isFollowing)")
```

```dart Flutter
void userRelationshipHasInLocal() {
    final hasLocal = AmitySocialClient.newUserRepository()
        .relationship
        .hasLocalFollowInfo(userId: 'targetUserId');
    print('Has local follow info: \$hasLocal');
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function userRelationshipHasInLocal() {
  const userId = 'target-user-id';
  const isInLocal = UserRepository.relationship.isFollowingInLocal(userId);
  console.log('Is following (local):', isInLocal);
}
```

</CodeGroup>
