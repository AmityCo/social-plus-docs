{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: pin-get-live-global-pinned-posts | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun pinGetLiveGlobalPinnedPosts() {
        // Query live posts as a live collection
        AmitySocialClient.newPostRepository()
            .getPosts()
            .targetMe()
            .build()
            .query()
            .subscribe({ pagingData ->
                // Observe live post updates
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
let postRepository = AmityPostRepository(client: client)
        var token: AmityNotificationToken?
        token = postRepository.getGlobalPinnedPosts().observe { collection, change, error in
            guard error == nil else { return }
            print("Global pinned posts: \(collection.count())")
        }
```

```dart Flutter
void pinGetLiveGlobalPinnedPosts() async {
        AmitySocialClient.newCommunityRepository()
            .getGlobalPinnedPosts()
            .asStream()
            .listen((posts) {
              print('Global pinned posts: \${posts.length}');
              for (final post in posts) {
                print('Post ID: \${post.postId}');
              }
            });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function pinGetLiveGlobalPinnedPosts() {
  const liveCollection = PostRepository.getPinnedPosts({ feedType: 'global' });
  liveCollection.on('dataUpdated', (posts) => {
    console.log('Global pinned posts:', posts);
  });
}
```

</CodeGroup>
