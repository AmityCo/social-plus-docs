{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-update | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun editCommunity(
        communityRepository: AmityCommunityRepository,
        avatar: AmityImage
    ) {
        communityRepository
            .editCommunity(communityId = "communityId1")
            .avatar(avatar = avatar)
            .isPublic(isPublic = true)
            .description(description = "hello and welcome!")
            .categoryIds(categoryIds = listOf("categoryId1", "categoryId2"))
            .postSettings(AmityCommunityPostSettings.ANYONE_CAN_POST)
            .storySettings(AmityCommunityStorySettings(allowComment = false))
            .build()
            .apply()
            .doOnSuccess { community: AmityCommunity ->
                // AmityCommunity
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository(client: client)
        let options = AmityCommunityUpdateOptions()
        options.setDisplayName("Updated Community Name")
        options.setCommunityDescription("Updated description")
        do {
            let community = try await communityRepository.editCommunity(withId: "<community-id>", options: options)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void updateCommunity(String communityId, AmityImage updatingAvatar) {
    AmitySocialClient.newCommunityRepository()
        .updateCommunity(communityId)
        .avatar(updatingAvatar)
        .displayName('updated name')
        .description('updated description')
        .tags(['tag1', 'tag2'])
        .categoryIds(['categoryId1', 'categoryId2'])
        .isPublic(false)
        .postSetting(AmityCommunityPostSettings.ANYONE_CAN_POST)
        .metadata({'updateKey': 'updateValue'})
        .update()
        .then((value) => {
              //handle result
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function updateCommunity() {
  const { data: community } = await CommunityRepository.updateCommunity('communityId', {
    displayName: 'Updated Community Name',
    description: 'New description',
  })
  console.log(community)
}
```

</CodeGroup>
