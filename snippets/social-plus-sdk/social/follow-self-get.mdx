{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: follow-self-get | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getMyFollowInfo() {
        val userRepository = AmityCoreClient.newUserRepository()
        userRepository
            .relationship()
            .getMyFollowInfo()
            .doOnNext { followInfo: AmityMyFollowInfo ->
                // AmityMyFollowInfo
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

function getMyFollowInfo() {
  const unsubscribe = UserRepository.Relationship.getMyFollowInfo(({ data: followInfo, loading, error }) => {
    if (loading) return
    if (error) {
      console.error('Error fetching my follow info:', error)
      return
    }

    console.log('My follower count:', followInfo.followerCount)
    console.log('My following count:', followInfo.followingCount)
    console.log('My pending requests:', followInfo.pendingCount)
  })

  return unsubscribe
}
```

</CodeGroup>
