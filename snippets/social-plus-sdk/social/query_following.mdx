{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: query_following | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void queryFollowing() async {
    final userRepository = AmitySocialClient.newUserRepository();
    userRepository
        .relationship()
        .user('userId')
        .getFollowings()
        .asStream()
        .listen((followings) {
      print('Following: \${followings.length}');
    });
  }
```

```typescript TypeScript
import { FollowRepository } from '@amityco/ts-sdk'

async function queryFollowingUnderscore() {
  const params = {
    userId: 'userId123',
    limit: 20,
  };

  const liveCollection = FollowRepository.getFollowings(params);
  liveCollection.on('dataUpdated', following => {
    console.log('Following:', following.map(f => f.userId));
  });
  liveCollection.on('dataError', err => console.error('Error querying following:', err));
}
```

</CodeGroup>
