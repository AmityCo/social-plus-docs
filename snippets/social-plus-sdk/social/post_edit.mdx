{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post_edit | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun postEdit() {
        AmitySocialClient.newPostRepository()
            .editPost("postId")
            .text("Updated post text")
            .build()
            .edit()
            .subscribe({ post ->
                // Post updated
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
func postEdit() {
        let postRepository = AmityPostRepository(client: AmityClient.shared)
        let builder = AmityPostUpdateBuilder()
        builder.setText("Updated post text")
        postRepository.updatePost(withId: "postId", builder: builder) { post, error in
            if let error = error {
                print("Failed to edit post: \(error.localizedDescription)")
                return
            }
            print("Post updated: \(post?.postId ?? "")")
        }
    }
```

```dart Flutter
void editPost(String postId, String newText) {
      AmitySocialClient.newPostRepository()
          .editPost(postId: postId)
          .text(newText)
          .build()
          .update()
          .then((AmityPost post) {
        // Post updated successfully
      }).onError((error, stackTrace) {
        // Handle error
      });
    }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function postEdit() {
  const { PostRepository } = Amity;
  await PostRepository.updatePost('postId', {
    data: { text: 'Edited post content' },
  });
  console.log('Post edited successfully');
}
```

</CodeGroup>
