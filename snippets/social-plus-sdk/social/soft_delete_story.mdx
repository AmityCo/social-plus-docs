{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: soft_delete_story | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun softDeleteStory() {
        AmitySocialClient.newStoryRepository()
            .hardDeleteStory(storyId = "storyId")
            .subscribe()
    }
```

```swift iOS
do {
            try await storyRepository.softDeleteStory(storyId: "storyId")
            // story successfully soft deleted
        } catch {
            // Catch error when deleting
        }
```

```dart Flutter
void softDeleteStory() {
    AmityCoreClient.newSocialRepository()
        .story
        .softDeleteStory(storyId: 'storyId123')
        .then((_) => print('Story soft-deleted successfully'))
        .onError((error, _) => print('Error soft-deleting story: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function softDeleteStory() {
  import { StoryRepository } from '@amityco/ts-sdk';

  await StoryRepository.softDeleteStory('story-id-123');
  console.log('Story soft deleted');
}
```

</CodeGroup>
