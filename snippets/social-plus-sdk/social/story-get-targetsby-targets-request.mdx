{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: story-get-targetsby-targets-request | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun storyGetTargetsbyTargetsRequest() {
        AmitySocialClient.newStoryRepository()
            .getStory(storyId = "storyId")
            .subscribe()
    }
```

```swift iOS
let storyRepository = AmityStoryRepository(client: AmityManager.shared.client!)
        let collection = storyRepository.getStoryTargets(targetIds: ["communityId1", "communityId2"], targetType: .community)
        collection.observe { liveCollection, _, error in
            print("Story targets: \(liveCollection.count())")
        }
```

```dart Flutter
import 'package:amity_sdk/amity_sdk.dart';

void getStoryTargetsByTargets() {
  final targets = [
    StoryTargetSearchInfo(
      targetId: 'communityId123',
      targetType: AmityStoryTargetType.COMMUNITY,
    ),
  ];

  AmitySocialClient.newStoryRepository()
      .getStoryTargets(targets: targets)
      .query()
      .listen((result) {
    print('Story targets: $result');
  });
}
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function storyGetTargetsbyTargetsRequest() {
  import { StoryRepository } from '@amityco/ts-sdk';

  const targets = [
    { targetId: 'community-id-123', targetType: 'community' as const },
    { targetId: 'community-id-456', targetType: 'community' as const },
  ];

  const unsubscribe = StoryRepository.getStoryTargetsByTargets(
    { targets },
    ({ data: storyTargets, loading, error }) => {
      if (!loading && storyTargets) {
        console.log('Story targets by request:', storyTargets);
      }
    },
  );
  return unsubscribe;
}
```

</CodeGroup>
