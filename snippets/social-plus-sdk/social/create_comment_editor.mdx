{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create_comment_editor | updated: 2026-05-02 */}

<CodeGroup>

```swift iOS
let editor = AmityCommentEditor(client: client, commentId: "<comment-id>")
```

```dart Flutter
void createCommentEditor() {
    AmitySocialClient.newCommentRepository()
        .getComment(commentId: 'commentId')
        .then((AmityComment comment) {
          comment
              .edit()
              .text('Updated comment text')
              .build()
              .apply()
              .then((_) {
                // Comment edited
              })
              .onError((error, stackTrace) {
                // Handle error
              });
        });
  }
```

```typescript TypeScript
import { CommentRepository } from '@amityco/ts-sdk'

async function createCommentEditor() {
  const comment = await CommentRepository.createComment({
    referenceId: 'postId',
    referenceType: 'post',
    data: { text: 'Initial comment text' },
  })
  const updated = await CommentRepository.editComment({
    commentId: comment.commentId,
    data: { text: 'Edited comment text' },
  })
  console.log('Comment edited:', updated)
}
```

</CodeGroup>
