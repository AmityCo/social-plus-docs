{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-create-image-post | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createImagePost(targetCommunityId: String, images: List<AmityImage>, text: String = "") {
        val target = AmityPost.Target.COMMUNITY.create(communityId = targetCommunityId)
        AmitySocialClient.newPostRepository()
            .createImagePost(target = target, images = images, text = text)
            .doOnSuccess { post: AmityPost ->
                // AmityPost
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let fileRepository = AmityFileRepository(client: client)
        let postRepository = AmityPostRepository(client: client)
        let builder = AmityImagePostBuilder()
        do {
            let imageData = AmityImageData()
            builder.setImages([imageData])
            builder.setText("Check out this photo!")
            let post = try await postRepository.createImagePost(builder, targetId: "<community-id>", targetType: .community, metadata: nil, mentionees: nil)
            print("Created image post: \(post.postId)")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void createImagePost(
      String targetId, AmityPostTargetType targetType, List<File> imageFiles) async {
    final uploadedImages = <AmityImage>[];

    for (final imageFile in imageFiles) {
      await AmityCoreClient.newFileRepository()
          .uploadImage(imageFile)
          .stream
          .firstWhere((result) => result is AmityUploadComplete)
          .then((result) {
        result.when(
          complete: (image) => uploadedImages.add(image),
          progress: (_, __) {},
          error: (_) {},
          cancel: () {},
        );
      });
    }

    AmitySocialClient.newPostRepository()
        .createPost()
        .targetType(targetType)
        .targetId(targetId)
        .image(uploadedImages)
        .post()
        .then((AmityPost post) => {
              //handle result
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function createImagePost() {
  const { data: post } = await PostRepository.createPost({
    targetType: 'community',
    targetId: 'communityId',
    dataType: 'image',
    data: {},
    attachments: [{ type: 'image', fileId: 'uploadedFileId' }],
  })
  console.log(post)
}
```

</CodeGroup>
