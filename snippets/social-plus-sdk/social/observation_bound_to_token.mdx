{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: observation_bound_to_token | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun observationBoundToToken() {
        // Perform the operation using the appropriate SDK repository
        AmitySocialClient.newPostRepository()
            .getPosts()
            .targetMe()
            .build()
            .query()
            .subscribe({ pagingData ->
                // Handle live collection updates
            }, { error ->
                // Handle error
            })
    }
```

```dart Flutter
void observationBoundToToken() async {
        // Live objects are automatically cleaned up when the token is disposed
        final token = AmityCoreClient.newUserRepository()
            .getUser('userId')
            .asLiveObject();

        final subscription = token.asStream().listen((user) {
          print('User updated: \${user?.displayName}');
        });

        // Cancel subscription to release the live object
        // subscription.cancel();
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function observationBoundToToken() {
  // TODO: implement SDK operation
  console.log('observation_bound_to_token operation');
}
```

</CodeGroup>
