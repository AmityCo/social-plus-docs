{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_global_feed_ranking | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getGlobalFeedRanking() {
        // Query global feed with custom ranking options
        AmitySocialClient.newFeedRepository()
            .getGlobalFeed()
            .build()
            .query()
            .subscribe { feed -> /* handle global ranked feed */ }
    }
```

```dart Flutter
void getGlobalFeedRanking() {
    // TODO: implement SDK operation
    throw UnimplementedError();
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function getGlobalFeedRanking() {
  const liveCollection = PostRepository.getPosts({ targetType: 'globalFeed', sortBy: 'score' });
  liveCollection.on('dataUpdated', (posts) => {
    console.log('Ranked global feed posts:', posts.map((p: any) => p.postId));
  });
}
```

</CodeGroup>
