{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-get-live-member-invitations | updated: 2026-05-02 */}

<CodeGroup>

```swift iOS
let communityRepository = AmityCommunityRepository(client: client)
        var token: AmityNotificationToken?
        token = communityRepository.getInvitations(communityId: "<community-id>").observe { collection, change, error in
            guard error == nil else { return }
            print("Pending invitations: \(collection.count())")
        }
```

```dart Flutter
StreamSubscription? subscription;

  void observeLiveMemberInvitations(String communityId) {
    subscription = AmitySocialClient.newCommunityRepository()
        .getInvitations(communityId: communityId)
        .status(AmityCommunityInvitationStatus.pending)
        .build()
        .getLiveCollection()
        .asStream()
        .listen((List<AmityCommunityInvitation> invitations) {
          for (final invitation in invitations) {
            final inviteeId = invitation.inviteeId;
            final status = invitation.status;
          }
        })
        ..onError((error) {
          // Handle error
        });
  }

  void dispose() {
    subscription?.cancel();
  }
```

</CodeGroup>
