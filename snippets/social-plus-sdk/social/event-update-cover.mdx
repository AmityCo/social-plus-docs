{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: event-update-cover | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun eventUpdateCover(eventId: String, coverImageFileId: String) {
        AmitySocialClient.newEventRepository()
            .editEvent(eventId = eventId)
            .setCoverImageFileId(coverImageFileId = coverImageFileId)
            .build()
            .apply()
            .doOnSuccess { event: AmityEvent ->
                // AmityEvent updated
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let eventRepository = AmityEventRepository()
        let fileRepository = AmityFileRepository()
        let imageData = UIImage(named: "eventCover")?.jpegData(compressionQuality: 0.8) ?? Data()
        fileRepository.uploadImage(imageData, progress: nil) { uploadedImage, error in
            guard let uploadedImage = uploadedImage, error == nil else { return }
            let builder = AmityEventUpdateDataBuilder()
            builder.setCoverImage(uploadedImage)
            eventRepository.updateEvent(withId: "eventId", builder: builder) { event, error in
                if let error = error {
                    print("Failed to update event cover: \(error.localizedDescription)")
                } else {
                    print("Event cover updated successfully")
                }
            }
        }
```

```dart Flutter
void updateEventCover(String eventId, File coverImageFile) {
    AmityCoreClient.newFileRepository()
        .uploadImage(coverImageFile)
        .stream
        .listen((amityUploadResult) {
      amityUploadResult.when(
        progress: (uploadInfo, cancelToken) {},
        complete: (AmityImage image) {
          AmitySocialClient.newEventRepository()
              .updateEvent(eventId)
              .setCoverImageFileId(image.fileId ?? '')
              .build()
              .apply()
              .then((AmityEvent event) {
                // cover updated successfully
              })
              .onError((error, stackTrace) {
                // handle error
              });
        },
        error: (error) {
          // handle upload error
        },
        cancel: () {},
      );
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function eventUpdateCover() {
  await EventRepository.updateEvent({
    eventId: 'eventId123',
    coverImageFileId: 'newCoverImageId456',
  });
  console.log('Event cover image updated');
}
```

</CodeGroup>
