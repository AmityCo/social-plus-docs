{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-story-feed | updated: 2026-05-02 */}

<CodeGroup>

```typescript TypeScript
import { StoryRepository } from '@amityco/ts-sdk'

function getCommunityStoryFeed(communityId: string) {
  const unsubscribe = StoryRepository.getActiveStoriesByTarget(
    { targetType: 'community', targetId: communityId },
    (response) => {
      if (response.error) throw response.error
      if (!response.loading) {
        console.log('Active stories:', response.data)
      }
    },
  )

  return unsubscribe
}
```

</CodeGroup>
