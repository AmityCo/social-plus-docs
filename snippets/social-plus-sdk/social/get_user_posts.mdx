{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_user_posts | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getUserPosts() {
        // Query posts created by a specific user as a live collection
        AmitySocialClient.newFeedRepository()
            .getUserFeed("userId")
            .build()
            .query()
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(io.reactivex.rxjava3.android.schedulers.AndroidSchedulers.mainThread())
            .subscribe({ pagedList ->
                // Display user's posts
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
let postRepository = AmityPostRepository(client: AmityUIKitManagerInternal.shared.client)
        let userId = "user123"
        let query = AmityUserFeedQueryOptions(userId: userId, sortBy: .lastCreated, includeDeleted: false)
        let collection = postRepository.getUserFeed(query)
        collection.observe { liveCollection, _, _ in
            let posts = liveCollection.allObjects()
            print("User posts count: \(posts.count)")
        }
```

```dart Flutter
void getUserPosts() async {
    AmitySocialClient.newPostRepository()
        .getPosts()
        .targetUser(userId: 'userId')
        .getPagingData(token: null, limit: 20)
        .asStream()
        .listen((result) {
      final posts = result.data;
    });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

function getUserPosts() {
  const userId = 'user123'
  const liveCollection = PostRepository.getPosts({ targetId: userId, targetType: 'user', limit: 20 })
  liveCollection.on('dataUpdated', (posts) => {
    console.log('User posts:', posts)
  })
}
```

</CodeGroup>
