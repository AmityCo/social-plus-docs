{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-user-following-communities | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void getUserFollowingCommunities() async {
    AmitySocialClient.newCommunityRepository()
        .getCommunities()
        .filter(AmityCommunityQueryFilter.member)
        .userId('userId')
        .getPagingData(token: null, limit: 20)
        .asStream()
        .listen((result) {
      final communities = result.data;
    });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

function getUserFollowingCommunities() {
  const liveCollection = CommunityRepository.getCommunities({ membership: 'member', limit: 20 })
  liveCollection.on('dataUpdated', (communities) => {
    console.log('User following communities:', communities)
  })
}
```

</CodeGroup>
