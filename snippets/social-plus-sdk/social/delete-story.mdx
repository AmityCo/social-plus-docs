{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: delete-story | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun deleteStory(storyId: String) {
        AmitySocialClient.newStoryRepository()
            .softDeleteStory(storyId = storyId)
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let storyRepository = AmityStoryRepository()
        storyRepository.softDeleteStory(withId: "storyId") { error in
            if let error = error {
                print("Failed to delete story: \(error.localizedDescription)")
            } else {
                print("Story deleted successfully")
            }
        }
```

```dart Flutter
void deleteStory(String storyId) {
    AmitySocialClient.newStoryRepository()
        .hardDeleteStory(storyId: storyId)
        .then((_) {
          // story deleted
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

</CodeGroup>
