{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_community_members | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void getCommunityMembers() async {
    AmitySocialClient.newCommunityRepository()
        .membership(communityId: 'communityId')
        .getMembers()
        .getPagingData(token: null, limit: 20)
        .asStream()
        .listen((result) {
      final members = result.data;
    });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

function getCommunityMembers() {
  const communityId = 'community123'
  const liveCollection = CommunityRepository.getCommunityMembers({ communityId, limit: 20 })
  liveCollection.on('dataUpdated', (members) => {
    console.log('Community members:', members)
  })
}
```

</CodeGroup>
