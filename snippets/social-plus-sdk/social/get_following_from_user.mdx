{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_following_from_user | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getFollowingFromUser() {
        // Query the list of users a specific user is following
        AmitySocialClient.newFollowRepository()
            .getFollowings(userId = "userId")
            .build()
            .query()
            .subscribe { followings -> /* handle followings */ }
    }
```

```dart Flutter
void getFollowingFromUser() {
    AmitySocialClient.newUserRepository()
        .relationship()
        .user('userId')
        .getFollowings()
        .getPagingData()
        .listen((followings) {
      for (final following in followings) {
        print('Following: \${following.destinationUserId}');
      }
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function getFollowingFromUser() {
  const liveCollection = UserRepository.getFollowings('userId123');
  liveCollection.on('dataUpdated', (followings) => {
    console.log('Following:', followings.map((f: any) => f.displayName));
  });
}
```

</CodeGroup>
