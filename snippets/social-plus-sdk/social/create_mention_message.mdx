{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create_mention_message | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createMentionMessage() {
        // Send a message mentioning all channel members
        AmityChatClient.newMessageRepository()
            .createMessage(channelId = "channelId")
            .with()
            .text(text = "@all attention!")
            .mentionAll()
            .build()
            .subscribe()
    }
```

```dart Flutter
void createMentionMessage() {
    final messageRepo = AmityChatClient.newMessageRepository();
    messageRepo
        .createMessage('channelId')
        .with(AmityMentionMetadata(
          userIds: ['userId1'],
          channelMention: false,
        ))
        .text('Hello @user1!')
        .send()
        .then((msg) => print('Message sent: \${msg.messageId}'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function createMentionMessage() {
  await MessageRepository.createMessage({
    subChannelId: 'subChannelId123',
    dataType: 'text',
    data: { text: 'Hello @john!' },
    mentionees: [{ type: 'user', userIds: ['userId123'] }],
  });
}
```

</CodeGroup>
