{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-membership-query | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun userMembershipQuery() {
        // Query live collection for user-membership-query
        AmityCoreClient.newUserRepository()
            .searchUser("keyword")
            .build()
            .query()
            .subscribe({ liveCollection -> }, { error -> })
    }
```

```swift iOS
let communityId = "communityId"
        let queryBuilder = AmityMembershipQueryBuilder()
        queryBuilder.setCommunityId(communityId)
        let collection = AmitySDK.queryCommunityMembers(builder: queryBuilder)
        let token = collection.observe { liveCollection, change, error in
            if let error = error {
                print("Failed to query memberships: \(error)")
                return
            }
            let members = liveCollection.allObjects()
            print("Community members: \(members.count)")
        }
```

```dart Flutter
void userMembershipQuery() {
    AmitySocialClient.newCommunityRepository()
        .membership('communityId')
        .getMembers()
        .query()
        .then((List<AmityCommunityMember> members) {
          for (final member in members) {
            print('Member: \${member.userId}');
          }
        });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function queryUserMembership() {
  const unsubscribe = CommunityRepository.getCommunityMembers(
    { communityId: 'communityId', memberships: ['member'] },
    (result) => {
      if (result.data) {
        console.log('Community members:', result.data);
      }
    },
  );
  return unsubscribe;
}
```

</CodeGroup>
