{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-search | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun queryCommunity(communityRepository: AmityCommunityRepository) {
        //Available sort options
        AmityCommunitySortOption.DISPLAY_NAME
        AmityCommunitySortOption.LAST_CREATED
        AmityCommunitySortOption.FIRST_CREATED
        //Available filter options
        AmityCommunityFilter.ALL
        AmityCommunityFilter.MEMBER
        AmityCommunityFilter.NOT_MEMBER
        //
        //Query communities
        communityRepository
            .searchCommunities(keyword = "keyword")
            .sortBy(sortBy = AmityCommunitySortOption.LAST_CREATED)
            .filter(filter = AmityCommunityFilter.ALL)
            .categoryId(categoryId = "categoryId")
            .includeDeleted(includeDeleted = true)
            .build()
            .query()
            .doOnNext { communities: PagingData<AmityCommunity> ->
                // PagingData<AmityCommunity>
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository()
        let queryOptions = AmityCommunityQueryOptions(filter: .all, sortBy: .displayName, categoryId: nil, includeDeleted: false)
        let collection = communityRepository.searchCommunities(displayName: "keyword", queryOptions: queryOptions)
        collection.observe { liveCollection, _, error in
            guard error == nil else { return }
            let communities = liveCollection.allObjects()
            print("Search results: \(communities.count)")
        }
```

```dart Flutter
void communitySearch() {
    final query = AmitySocialClient.newCommunityRepository()
        .searchCommunities(keyword: 'keyword')
        .filter(AmityCommunityFilter.all)
        .build();
    final controller = PagingController(pageFuture: (token) => query.getPagingData(token: token, limit: 20), pageSize: 20);
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

function searchCommunity(keyword: string) {
  const liveCollection = CommunityRepository.searchCommunity(
    { displayName: keyword, sortBy: 'displayName' },
    ({ data: communities, loading, error, hasNextPage, onNextPage }) => {
      if (error) { console.error(error); return; }
      if (!loading) {
        communities.forEach(c => console.log(c.communityId, c.displayName, c.membersCount));
        if (hasNextPage) onNextPage();
      }
    }
  );
  return liveCollection;
}
```

</CodeGroup>
