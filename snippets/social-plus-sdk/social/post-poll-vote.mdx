{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-poll-vote | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun votePoll(pollRepository: AmityPollRepository) {
        pollRepository.votePoll(
            pollId = "pollId",
            answerIds = listOf("answer_id_1", "answer_id_2")
        )
            .doOnComplete {
                // Void
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let client = AmityClient.shared
        let pollRepository = AmityPollRepository(client: client)
        pollRepository.votePoll(withId: "<pollId>", answerIds: ["<answerId>"]) { success, error in
            if let error = error { print("Error: \(error)"); return }
            print("Vote submitted: \(success)")
        }
```

```dart Flutter
void voteOnPoll(String pollId, List<String> answerIds) {
    AmitySocialClient.newPollRepository()
        .vote(pollId: pollId, answerIds: answerIds)
        .then((value) {
          // vote submitted successfully
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function postPollVote() {
  const { PostRepository } = Amity;
  await PostRepository.vote({ pollId: 'pollId', answerIds: ['answerId1'] });
  console.log('Vote submitted successfully');
}
```

</CodeGroup>
