{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: event-delete | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun deleteEvent(eventId: String): Disposable {
        val eventRepository = AmitySocialClient.newEventRepository()

        return eventRepository.deleteEvent(eventId)
            .subscribeOn(Schedulers.io())
            .observeOn(AndroidSchedulers.mainThread())
            .subscribe(
                {
                    // Event deleted successfully
                    println("✅ Event deleted successfully")
                },
                { error: Throwable ->
                    // Handle error
                    println("❌ Error deleting event: ${error.message}")
                }
            )
    }
```

```typescript TypeScript
import { EventRepository } from '@amityco/ts-sdk'

async function deleteEvent() {
  await EventRepository.deleteEvent('eventId')
}
```

</CodeGroup>
