{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: video-post-upload | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun videoPostUpload() {
        val communityId = "communityId"
        val videoUri = android.net.Uri.parse("file:///path/to/video.mp4")
        AmitySocialClient.newPostRepository()
            .createPost()
            .targetCommunity(communityId)
            .video(listOf(videoUri))
            .build()
            .post()
            .subscribe()
    }
```

```swift iOS
let fileRepository = AmityFileRepository()
        guard let videoUrl = Bundle.main.url(forResource: "sample", withExtension: "mp4") else { return }
        fileRepository.uploadVideo(videoUrl, progress: { progress in
            print("Upload progress: \(progress * 100)%")
        }) { videoData, error in
            guard let videoData = videoData, error == nil else {
                print("Video upload failed: \(error!)")
                return
            }
            let postRepository = AmityPostRepository()
            let builder = AmityVideoPostBuilder()
            builder.setVideos([videoData])
            builder.setText("Watch this video!")
            postRepository.createPost(
                builder,
                targetId: "communityId",
                targetType: .community
            ) { post, error in
                if let post = post {
                    print("Video post created: \(post.postId)")
                }
            }
        }
```

</CodeGroup>
