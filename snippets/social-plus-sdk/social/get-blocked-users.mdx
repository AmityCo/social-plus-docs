{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-blocked-users | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getBlockedUsers() {
        AmityCoreClient.newUserRepository()
            .getBlockedUsers()
            .doOnNext { pagedList ->
                // Handle paged list of blocked users
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let userRepository = AmityUserRepository(client: AmityManager.shared.client!)
        let liveCollection = userRepository.getBlockedUsers()
        liveCollection.observe { collection, _, error in
            guard error == nil else { return }
            for i in 0..<collection.count() {
                if let user = collection.object(at: i) {
                    print("Blocked user: \(user.userId)")
                }
            }
        }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

function getBlockedUsers() {
  const liveCollection = UserRepository.getBlockedUsers()

  liveCollection.on('dataUpdated', (users) => {
    console.log('Blocked users:', users.length)
    users.forEach((user) => {
      console.log(' -', user.userId, user.displayName)
    })
  })

  liveCollection.on('dataError', (error) => {
    console.error('Error fetching blocked users:', error)
  })
}
```

</CodeGroup>
