{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-moderation-remove-roles | updated: 2026-04-30 */}

<CodeGroup>

```kotlin Android
fun removeRoles(communityId: String, roles: List<String>, userIds: List<String>) {
        AmitySocialClient.newCommunityRepository()
            .moderation(communityId = communityId)
            .removeRole(role = roles.first(), userIds = userIds)
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let communityModeration = AmityCommunityModeration(client: client, andCommunity: "<community-id>")
        do {
            let success = try await communityModeration.removeRole("<role>", userIds: ["<user-id>"])
        } catch {
            // Handle error here
        }
```

```dart Flutter
void removeMemberRoles(String communityId, String userId, List<String> roles) {
    AmitySocialClient.newCommunityRepository()
        .moderation(communityId)
        .removeRoles(roles, [userId])
        .then((value) => {
              //success
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function removeCommunityRoles() {
  await CommunityRepository.removeRoles('communityId', ['moderator'], ['userId1'])
}
```

</CodeGroup>
