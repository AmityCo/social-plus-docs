{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-story-by-target | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getStoryByTarget(targetId: String, targetType: AmityStoryTarget.TargetType) {
        AmitySocialClient.newStoryRepository()
            .getStories(
                targetId = targetId,
                targetType = targetType
            )
            .doOnNext { pagedList ->
                // Handle paged list of stories for this target
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let storyRepository = AmityStoryRepository(client: AmityManager.shared.client!)
        let liveCollection = storyRepository.getActiveStories(targetId: "communityId", targetType: .community)
        liveCollection.observe { collection, _, error in
            guard error == nil else { return }
            print("Stories for target: \(collection.count())")
        }
```

```dart Flutter
void getStoryByTarget() {
    final liveCollection = AmitySocialClient.newStoryRepository()
        .getActiveStories(
          targetId: 'communityId',
          targetType: AmityStoryTargetType.COMMUNITY,
        )
        .getPagingData();
    liveCollection.asStream().listen((List<AmityStory> stories) {
      print('Stories for target: \${stories.length}');
    });
  }
```

```typescript TypeScript
import { StoryRepository } from '@amityco/ts-sdk'

function getStoriesByTarget() {
  const liveCollection = StoryRepository.getActiveStoriesByTarget({
    targetId: 'communityId',
    targetType: 'community',
  })

  liveCollection.on('dataUpdated', (stories) => {
    console.log('Active stories:', stories)
  })

  liveCollection.on('dataError', (error) => {
    console.error('Failed to get stories:', error)
  })

  return () => liveCollection.dispose()
}
```

</CodeGroup>
