{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: handle-join-request | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun approveJoinRequest(communityId: String, userId: String) {
        AmitySocialClient.newCommunityRepository()
            .joinRequest(communityId = communityId)
            .approve(userId = userId)
            .doOnComplete {
                // join request approved
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }

    fun rejectJoinRequest(communityId: String, userId: String) {
        AmitySocialClient.newCommunityRepository()
            .joinRequest(communityId = communityId)
            .reject(userId = userId)
            .doOnComplete {
                // join request rejected
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository(client: AmityUIKitManagerInternal.shared.client)
        let communityId = "community123"
        let userId = "user123"
        communityRepository.acceptJoinRequest(communityId: communityId, userId: userId) { success, error in
            print("Join request handled: \(success)")
        }
```

```dart Flutter
void approveJoinRequest(String communityId, String userId) {
    AmitySocialClient.newCommunityRepository()
        .moderation(communityId)
        .approveJoinRequest([userId])
        .then((value) {
          // join request approved
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }

  void declineJoinRequest(String communityId, String userId) {
    AmitySocialClient.newCommunityRepository()
        .moderation(communityId)
        .declineJoinRequest([userId])
        .then((value) {
          // join request declined
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function handleJoinRequest() {
  const communityId = 'community-id-123'
  const userId = 'user-id-123'

  // Approve a join request
  const { data: accepted, error: acceptError } = await CommunityRepository.acceptJoinRequest({
    communityId,
    userId,
  })
  if (acceptError) {
    console.error('Failed to accept join request:', acceptError)
  } else {
    console.log('Join request accepted for user:', userId)
  }

  // Decline a join request
  const { data: declined, error: declineError } = await CommunityRepository.declineJoinRequest({
    communityId,
    userId,
  })
  if (declineError) {
    console.error('Failed to decline join request:', declineError)
  } else {
    console.log('Join request declined for user:', userId)
  }
}
```

</CodeGroup>
