{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: category-get | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getCategory(
        communityRepository: AmityCommunityRepository,
        categoryId: String
    ) {
        communityRepository
            .getCategory(categoryId = categoryId)
            .doOnNext { category: AmityCommunityCategory ->
                // AmityCommunity
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```dart Flutter
void getCategoryById(String categoryId) {
    AmitySocialClient.newCommunityRepository()
        .getCategory(categoryId)
        .then((AmityCommunityCategory category) {
          // handle category
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

</CodeGroup>
