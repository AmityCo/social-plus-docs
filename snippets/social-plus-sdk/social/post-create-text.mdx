{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-create-text | updated: 2026-04-30 */}

<CodeGroup>

```kotlin Android
fun createTextPost(targetId: String, targetType: AmityPost.TargetType, text: String) {
        AmitySocialClient.newPostRepository()
            .createPost()
            .targetUser(userId = targetId)
            .text(text = text)
            .build()
            .post()
            .doOnSuccess { post: AmityPost ->
                // handle created post
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let postRepository = AmityPostRepository(client: client)
        let builder = AmityTextPostBuilder()
        builder.setText("Hello World!")
        do {
            let post = try await postRepository.createTextPost(builder, targetId: nil, targetType: .user, metadata: nil, mentionees: nil)
        } catch {
            // Handle error here
        }
```

```dart Flutter
late PagingController<AmityPost> _controller;

  void createTextPost(String communityId) {
    AmitySocialClient.newPostRepository()
        .createPost()
        .targetCommunity(communityId)
        .text('Hello from Flutter!')
        .post()
        .then((AmityPost post) {
          _controller.add(post);
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

</CodeGroup>
