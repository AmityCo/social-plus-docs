{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-creation-conversation | updated: 2026-04-30 */}

<CodeGroup>

```dart Flutter
void createConversationChannel() {
    // create channel and let SDK handle channelId generation
    AmityChatClient.newChannelRepository()
        .createChannel()
        .conversationType()
        .withUserId('myFriendId')
        .displayName('Chat with my BFF') //Optional
        .metadata({'key': 'value'}) //Optional
        .tags(['friends', 'New Arrival']) //Optional
        .create()
        .then((AmityChannel channel) => {
              //handle result
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

</CodeGroup>
