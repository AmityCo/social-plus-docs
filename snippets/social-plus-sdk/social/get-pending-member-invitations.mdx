{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-pending-member-invitations | updated: 2026-05-02 */}

<CodeGroup>

```swift iOS
var token: AmityNotificationToken!
        token = community.getMemberInvitations().observe({ collection, change, error in
            // Observe changes in the collection
        })
```

```dart Flutter
void getPendingMemberInvitations(String communityId) {
    final liveCollection = AmitySocialClient.newCommunityRepository()
        .getInvitations(communityId: communityId)
        .status(AmityCommunityInvitationStatus.pending)
        .build()
        .getLiveCollection();

    liveCollection.getStreamController().stream.listen(
      (List<AmityCommunityInvitation> invitations) {
        // handle pending invitations list
      },
      onError: (error) {
        // handle error
      },
    );
    liveCollection.loadNext();
  }
```

</CodeGroup>
