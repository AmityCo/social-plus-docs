{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-storytarget | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getStorytarget() {
        // Query story targets to display story rings
        AmitySocialClient.newStoryRepository()
            .getStoryTargets()
            .build()
            .query()
            .subscribe { targets -> /* handle story targets */ }
    }
```

```swift iOS
// Get a specific story target by ID and type
        let storyRepository = AmityStoryRepository(client: AmityManager.shared.client!)
        var token: AmityNotificationToken?
        let liveObject = storyRepository.getStoryTarget(targetId: "communityId", targetType: .community)
        token = liveObject.observe { target, error in
            if let error = error {
                print("Failed to get story target: \(error.localizedDescription)")
                return
            }
            guard let target = target.object else { return }
            print("Story target: \(target.targetId), hasUnseen: \(target.hasUnseen)")
        }
```

```dart Flutter
void observeTarget(AmityStoryTargetType targetType , String targetId) {
    StreamBuilder<AmityStoryTarget>(
        stream: AmitySocialClient.newStoryRepository().live.getStoryTaregt(targetType: targetType, targetId: targetId),
        builder: (context, snapshot) {
          // update widget
          // eg. widget.text = storyTarget.uniqueId
          return Container();
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function getStorytarget() {
  const liveObject = StoryRepository.getStoryTarget('community', 'communityId123');
  liveObject.on('dataUpdated', (target) => {
    console.log('Story target:', target.targetId, 'hasUnseen:', target.hasUnseen);
  });
}
```

</CodeGroup>
