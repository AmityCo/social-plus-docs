{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: product-search | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
final List<AmityProduct> _products = [];
  late PagingController<AmityProduct> _controller;
  final scrollController = ScrollController();

  void searchProducts(String keyword) {
    _controller = PagingController(
      pageFuture: (token) => AmityProductRepository()
          .searchProducts(keyword)
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    )..addListener(() {
        if (_controller.error == null) {
          _products
            ..clear()
            ..addAll(_controller.loadedItems);
          // update UI
        } else {
          // handle error
        }
      });

    scrollController.addListener(() {
      if (scrollController.position.pixels ==
              scrollController.position.maxScrollExtent &&
          _controller.hasMoreItems) {
        _controller.fetchNextPage();
      }
    });
  }
```

```typescript TypeScript
import { ProductRepository } from '@amityco/ts-sdk'

async function searchProducts() {
  const liveCollection = ProductRepository.searchProducts({ keyword: 'sneakers' })
  liveCollection.on('dataUpdated', (products) => {
    console.log(products)
  })
}
```

</CodeGroup>
