{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-comment-by-ids | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getCommentByIds(
        commentRepository: AmityCommentRepository,
        commentIds: Set<String>
    ) {
        commentRepository.getCommentByIds(commentIds = commentIds)
            .doOnNext { comments: List<AmityComment> ->
                // List<AmityComment>
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let commentRepository = AmityCommentRepository(client: AmityManager.shared.client!)
        let commentIds = ["commentId1", "commentId2", "commentId3"]
        for commentId in commentIds {
            let liveObject = commentRepository.getComment(withId: commentId)
            liveObject.observe { liveObject, error in
                guard let comment = liveObject.object else { return }
                print("Comment: \(comment.commentId)")
            }
        }
```

```dart Flutter
void getCommentByIds() {
    final comment = await AmitySocialClient.newCommentRepository()
        .getComment('commentId');
    print('Comment: \${comment.data}');
  }
```

```typescript TypeScript
import { CommentRepository } from '@amityco/ts-sdk'

async function getCommentByIds(commentIds: string[]) {
  const comments = await CommentRepository.getCommentByIds(commentIds)
  comments.forEach((comment) => {
    console.log('Comment ID:', comment.commentId)
    console.log('Comment text:', comment.data?.text)
  })
  return comments
}

// Example usage
getCommentByIds(['commentId1', 'commentId2'])
```

</CodeGroup>
