{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: comment-reply | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun commentReply() {
        val postId = "postId"
        val parentCommentId = "parentCommentId"
        AmitySocialClient.newCommentRepository()
            .createComment()
            .post(postId)
            .parentId(parentCommentId)
            .with()
            .text("This is a reply!")
            .build()
            .comment()
            .subscribe()
    }
```

```swift iOS
let commentRepository = AmityCommentRepository(client: AmityClient.shared)
        let builder = AmityCommentCreateDataBuilder()
        builder.setText("This is a reply!")
        commentRepository.createComment(
            withReferenceId: "postId",
            referenceType: .post,
            parentId: "parentCommentId",
            builder: builder
        ) { comment, error in
            print("Reply created: \(comment?.commentId ?? "")")
        }
```

```dart Flutter
void replyToComment(String postId, String parentCommentId, String replyText) {
    AmitySocialClient.newCommentRepository()
        .createComment()
        .post(postId: postId)
        .parentId(parentCommentId)
        .with_()
        .text(text: replyText)
        .build()
        .send()
        .then((AmityComment reply) {
          // Reply created successfully
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function commentReply() {
  const parentCommentId = 'parent-comment-id';
  const reply = await CommentRepository.createComment({
    referenceId: 'post-id-here',
    referenceType: 'post',
    parentId: parentCommentId,
    data: { text: 'This is a reply to the comment.' },
    metadata: {},
  });
  console.log('Reply created:', reply.commentId);
}
```

</CodeGroup>
