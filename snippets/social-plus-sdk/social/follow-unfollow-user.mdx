{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: follow-unfollow-user | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun followUnfollowUser() {
        // Follow user
        AmitySocialClient.newUserRepository()
            .relationship()
            .follow("userId")
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(AndroidSchedulers.mainThread())
            .subscribe({
                // now following user
            }, { error -> error.printStackTrace() })

        // Unfollow user
        AmitySocialClient.newUserRepository()
            .relationship()
            .unfollow("userId")
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(AndroidSchedulers.mainThread())
            .subscribe({
                // unfollowed user
            }, { error -> error.printStackTrace() })
    }
```

```swift iOS
// Follow or unfollow a user by their user ID
        let userRepository = AmityUserRepository(client: AmityManager.shared.client!)
        // Follow a user
        userRepository.followUser(withUserId: "targetUserId") { status, error in
            if let error = error {
                print("Failed to follow user: \(error.localizedDescription)")
                return
            }
            print("Follow status: \(status?.status.rawValue ?? "unknown")")
        }
        // Unfollow a user
        userRepository.unfollowUser(withUserId: "targetUserId") { status, error in
            if let error = error {
                print("Failed to unfollow user: \(error.localizedDescription)")
                return
            }
            print("Unfollowed user successfully")
        }
```

```dart Flutter
void followUnfollowUser() {
    // Follow a user
    await AmityCoreClient.newUserRepository()
        .relationship()
        .user('targetUserId')
        .follow();

    // Unfollow a user
    await AmityCoreClient.newUserRepository()
        .relationship()
        .user('targetUserId')
        .unfollow();
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function followUnfollowUser() {
  // Follow a user
  await UserRepository.followUser('targetUserId');
  console.log('Now following user');
  // Unfollow a user
  await UserRepository.unfollowUser('targetUserId');
  console.log('Unfollowed user');
}
```

</CodeGroup>
