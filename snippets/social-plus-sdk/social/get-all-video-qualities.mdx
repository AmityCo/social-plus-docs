{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-all-video-qualities | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getAllVideoQualities(post: AmityPost) {
        val videoData = post.getData() as? AmityPost.Data.VIDEO
        videoData?.getVideo()?.getVideoUrl()?.let { videoUrl ->
            // access video URL
        }
        videoData?.let {
            // iterate over children to get video qualities
            post.getChildren().forEach { childPost ->
                val childVideo = childPost.getData() as? AmityPost.Data.VIDEO
                childVideo?.getVideo()?.let { video ->
                    // handle each quality
                }
            }
        }
    }
```

</CodeGroup>
