{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-invitation-accept | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun communityInvitationAccept() {
        // TODO: implement Community invitation accept
    }
```

```swift iOS
let invitationRepository = AmityCommunityInvitationRepository(client: AmityClient.shared)
        invitationRepository.acceptInvitation(withId: "invitationId") { success, error in
            if success {
                print("Invitation accepted")
            } else if let error = error {
                print("Accept failed: \(error)")
            }
        }
```

```dart Flutter
void communityInvitationAccept() {
    // Accept an invitation to join a community
    AmitySocialClient.newCommunityRepository()
        .acceptInvitation(communityId: 'communityId')
        .then((_) => print('Invitation accepted'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function communityInvitationAccept() {
  const client = createClient('YOUR_API_KEY')
  await client.communityRepository.acceptInvitation('invitationId123')
  console.log('Invitation accepted')
}
```

</CodeGroup>
