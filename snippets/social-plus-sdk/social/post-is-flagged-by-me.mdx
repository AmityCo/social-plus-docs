{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-is-flagged-by-me | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun isFlaggedByMe(post: AmityPost) {
        val isFlagged = post.isFlaggedByMe()
        // use isFlagged boolean
    }
```

```swift iOS
let postRepository = AmityPostRepository(client: client)
        var token: AmityNotificationToken?
        token = postRepository.getPost(withId: "<post-id>").observe { liveObject, error in
            guard let post = liveObject.object else { return }
            print("Flagged by me: \(post.isFlaggedByMe)")
        }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function checkPostIsFlaggedByMe() {
  const isFlagged = await PostRepository.isPostFlaggedByMe('postId')
  console.log('Is flagged by me:', isFlagged)
}
```

</CodeGroup>
