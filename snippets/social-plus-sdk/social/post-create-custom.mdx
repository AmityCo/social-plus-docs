{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-create-custom | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createCustomPost(targetId: String, data: Map<String, Any>, dataType: String) {
        AmitySocialClient.newPostRepository()
            .createPost()
            .targetUser(userId = targetId)
            .custom(data = data, dataType = dataType)
            .build()
            .post()
            .doOnSuccess { post: AmityPost ->
                // handle custom post
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let postRepository = AmityPostRepository(client: client)
        let builder = AmityCustomPostBuilder()
        builder.setDataType("custom.mytype")
        builder.setData(["key": "value"])
        do {
            let post = try await postRepository.createCustomPost(builder, targetId: nil, targetType: .user, metadata: nil, mentionees: nil)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void createCustomPost(String targetUserId, Map<String, dynamic> data, String dataType) {
    AmitySocialClient.newPostRepository()
        .createPost()
        .targetUser(userId: targetUserId)
        .customPost(data: data, dataType: dataType)
        .build()
        .post()
        .then((AmityPost post) {
          // custom post created
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function createCustomPost() {
  const { data: post } = await PostRepository.createPost({
    targetType: 'community',
    targetId: 'communityId',
    dataType: 'customType',
    data: { customKey: 'customValue' },
  })
  console.log(post)
}
```

</CodeGroup>
