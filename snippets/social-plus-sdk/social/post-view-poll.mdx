{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-view-poll | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getPollInfo(
        post: AmityPost,
    ) {
        val data = post.getData()
        when (data) {
            is AmityPost.Data.POLL -> {
                data.getPoll()
                    .doOnNext { poll ->
                        val pollQuestion = poll.getQuestion()
                        val pollAnswerType = poll.getAnswerType()
                        val pollAnswers = poll.getAnswers()
                        val isPollOpen = poll.getStatus() == AmityPoll.Status.OPEN
                        val pollClosingTime = poll.getClosedAt()
                        val hasVoted = poll.isVoted()
                    }
                    .doOnError {
                        // Exception
                    }
                    .subscribe()
            }

            else -> {}
        }
    }
```

```dart Flutter
void viewPollPost(AmityPost post) {
    final data = post.data;
    if (data is AmityPollData) {
      final pollId = data.pollId;
      AmitySocialClient.newPollRepository()
          .getPoll(pollId)
          .then((AmityPoll poll) {
            final question = poll.question;
            final answers = poll.answers;
            final totalVotes = poll.voteCount;
            final isClosed = poll.closedAt != null;
            // render poll UI with question, answers, and vote counts
          })
          .onError((error, stackTrace) {
            // handle error
          });
    }
  }
```

</CodeGroup>
