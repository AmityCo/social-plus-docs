{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: query-community-with-filter | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun queryCommunityWithFilter() {
        // TODO: Implement Query community with filter
    }
```

```swift iOS
func queryCommunityWithFilter() {
        let communityRepository = AmityCommunityRepository(client: AmityClient.shared)
        let query = AmityCommunityQuery()
        query.filter = .member
        query.sortBy = .displayName
        query.categoryId = "categoryId"
        let collection = communityRepository.getCommunities(with: query)
        token = collection.observe { liveCollection, change, error in
            print("Filtered communities count: \(liveCollection.count())")
        }
    }
```

```dart Flutter
late PagingController<AmityCommunity> _communityController;

    void queryCommunityWithFilter(AmityCommunityFilter filter) {
      _communityController = PagingController(
        pageFuture: (token) => AmitySocialClient.newCommunityRepository()
            .getCommunities()
            .filter(filter)
            .getPagingData(token: token, limit: 20),
        pageSize: 20,
      )..addListener(() {
          if (_communityController.error == null) {
            // Update UI with _communityController.loadedItems
          } else {
            // Handle error
          }
        });
    }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function queryCommunityWithFilter() {
  const { CommunityRepository } = Amity;
  const { data: communities } = await CommunityRepository.getCommunities({
    filter: 'member',
    sortBy: 'lastCreated',
    includeDeleted: false,
  });
  console.log('Filtered communities:', communities.map((c: any) => c.displayName));
}
```

</CodeGroup>
