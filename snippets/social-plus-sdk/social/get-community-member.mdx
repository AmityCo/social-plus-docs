{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-community-member | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getCommunityMember(communityId: String, userId: String) {
        AmitySocialClient.newCommunityRepository()
            .membership(communityId)
            .getMember(userId = userId)
            .doOnNext { member: AmityCommunityMember ->
                val displayName = member.getDisplayName()
                val roles = member.getRoles()
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let memberRepository = AmityCommunityMembersRepository(client: AmityManager.shared.client!)
        memberRepository.getMember(communityId: "communityId", userId: "userId") { member, error in
            if let error = error {
                print("Error: \(error)")
            } else if let member = member {
                print("Member: \(member.userId), role: \(member.roles)")
            }
        }
```

```dart Flutter
void getCommunityMember(String communityId, String userId) {
    AmitySocialClient.newCommunityRepository()
        .membership(communityId)
        .getMember(userId: userId)
        .then((AmityCommunityMember member) {
          // use member details
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function getCommunityMember() {
  const liveObject = runQuery(CommunityRepository.getCommunityMember({
    communityId: 'communityId123',
    userId: 'userId456',
  }));
  liveObject.on('dataUpdated', (member) => console.log('Member:', member));
}
```

</CodeGroup>
