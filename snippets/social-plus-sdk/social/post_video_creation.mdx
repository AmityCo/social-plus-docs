{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post_video_creation | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun postVideoCreation() {
        AmitySocialClient.newPostRepository()
            .createPost()
            .targetMe()
            .video(listOf(videoFile))
            .build()
            .post()
            .subscribe({ post ->
                // Video post created
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
func postVideoCreation() {
        let videoRepository = AmityVideoRepository(client: AmityClient.shared)
        let videoURL = URL(fileURLWithPath: "/path/to/video.mp4")
        videoRepository.uploadVideo(videoURL, progress: nil) { videoData, error in
            guard let videoData = videoData, error == nil else { return }
            let postRepository = AmityPostRepository(client: AmityClient.shared)
            let builder = AmityPostCreationBuilder()
            builder.setTargetType(.community, targetId: "communityId")
            builder.setVideoIds([videoData.fileId])
            postRepository.createPost(builder) { post, error in
                print("Video post created: \(post?.postId ?? "")")
            }
        }
    }
```

```dart Flutter
void createVideoPost(String targetId, String videoFilePath) async {
      final uploadResult = await AmityVideoClient.newVideoRepository()
          .uploadVideo(videoFilePath);

      if (uploadResult is AmityUploadComplete<AmityVideo>) {
        final video = uploadResult.getResult;
        await AmitySocialClient.newPostRepository()
            .createPost()
            .targetCommunity(targetId)
            .video([video])
            .post()
            .then((AmityPost post) {
          // Video post created successfully
        }).onError((error, stackTrace) {
          // Handle error
        });
      }
    }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function postVideoCreation() {
  const { PostRepository, uploadFile } = Amity;
  const videoFile = new File([''], 'video.mp4', { type: 'video/mp4' });
  const { data: uploaded } = await uploadFile(videoFile);
  const { data: post } = await PostRepository.createPost({
    targetId: 'userId',
    targetType: 'user',
    dataType: 'video',
    data: { videoFileId: uploaded.fileId },
  });
  console.log('Video post created:', post.postId);
}
```

</CodeGroup>
