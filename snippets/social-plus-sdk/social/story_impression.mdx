{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: story_impression | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun storyImpression() {
        AmitySocialClient.newStoryRepository()
            .markStoryAsSeen(storyId = "storyId")
            .subscribe()
    }
```

```swift iOS
let storyRepository = AmityStoryRepository(client: AmityManager.shared.client!)
        storyRepository.markStoryAsSeen(storyId: "storyId") { success, error in
            print("Story impression marked: \(success)")
        }
```

```dart Flutter
void storyImpression() {
    AmitySocialClient.newStoryRepository()
        .markStoryAsSeen(storyId: 'storyId')
        .then((_) {
          print('Story impression recorded.');
        });
  }
```

```typescript TypeScript
import { StoryRepository } from '@amityco/ts-sdk'

async function markStoryImpression() {
  await StoryRepository.markAsSeen('storyId');
  console.log('Story impression recorded');
}
```

</CodeGroup>
