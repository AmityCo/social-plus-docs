{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-membership-observe | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun communityMembershipObserve() {
        val communityId = "communityId"
        AmitySocialClient.newCommunityRepository()
            .membership(communityId)
            .getMembers()
            .build()
            .query()
            .subscribe { members ->
                val count = members.size
            }
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository()
        let memberLiveCollection = communityRepository.getMembers(
            communityId: "communityId",
            filter: .all,
            sortBy: .displayName,
            includeDeleted: false
        )
        memberLiveCollection.observe { liveCollection, error in
            guard let members = liveCollection.allObjects() else { return }
            print("Community member count: \(members.count)")
            for member in members {
                print("Member: \(member.displayName ?? "")")
            }
        }
```

```dart Flutter
void communityMembershipObserve() {
    // Observe real-time changes to community membership
    final liveMembership = AmitySocialClient.newCommunityRepository()
        .membership('communityId')
        .getMember('userId');
    liveMembership.stream.listen(
      (member) => print('Membership updated: \${member.userId}'),
      onError: (error) => print('Error: \$error'),
    );
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function communityMembershipObserve() {
  const client = createClient('YOUR_API_KEY')
  const liveCollection = client.communityRepository.searchMembers({
    communityId: 'communityId123',
    memberships: ['member'],
  })
  liveCollection.on('dataUpdated', (members) => {
    console.log('Membership update:', members.length, 'members')
  })
}
```

</CodeGroup>
