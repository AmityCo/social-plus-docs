{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: member-management-get-list | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun memberManagementGetList() {
        // Observe live collection with pagination
        AmitySocialClient.newCommunityRepository()
            .membership(communityId = "communityId")
            .getMembers()
            .build()
            .query()
            .subscribe { pagingData ->
                // bind member list
            }
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository(client: AmityUIKitManagerInternal.shared.client)
        let communityId = "community123"
        let query = AmityCommunityMembershipQueryOptions(communityId: communityId, roles: [], sortBy: .lastCreated, includeDeleted: false, memberships: [.member, .moderator])
        let collection = communityRepository.getMembers(query)
        collection.observe { liveCollection, _, _ in
            print("Member list count: \(liveCollection.count())")
        }
```

```dart Flutter
void memberManagementGetList() {
    AmitySocialClient.newCommunityRepository()
        .membership('communityId')
        .getMembers()
        .asStream()
        .listen((members) {
      for (final member in members) {
        print('Member: \${member.userId}');
      }
    });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function memberManagementGetList(communityId: string) {
  const { data: members, onNextPage } = await CommunityRepository.getCommunityMembers({
    communityId,
    memberships: ['member'],
  })
  console.log('Community members:', members)
  return { members, onNextPage }
}
```

</CodeGroup>
