{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: member-management-get-list | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void memberManagementGetList() {
    AmitySocialClient.newCommunityRepository()
        .membership('communityId')
        .getMembers()
        .asStream()
        .listen((members) {
      for (final member in members) {
        print('Member: \${member.userId}');
      }
    });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function memberManagementGetList(communityId: string) {
  const { data: members, onNextPage } = await CommunityRepository.getCommunityMembers({
    communityId,
    memberships: ['member'],
  })
  console.log('Community members:', members)
  return { members, onNextPage }
}
```

</CodeGroup>
