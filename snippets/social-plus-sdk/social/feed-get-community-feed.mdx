{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: feed-get-community-feed | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getCommunityFeed(communityId: String) {
        AmitySocialClient.newFeedRepository()
            .getCommunityFeed(communityId = communityId)
            .build()
            .getPagingData()
            .doOnNext { posts: PagedList<AmityPost> ->
                // handle community feed posts
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let feedRepository = AmityFeedRepository(client: client)
        var token: AmityNotificationToken?
        token = feedRepository.getCommunityFeed("<community-id>", sortBy: .lastCreated, includeDeleted: false, feedType: .published)
            .observe { collection, change, error in
                // Handle collection updates
            }
```

```dart Flutter
final _posts = <AmityPost>[];
  late PagingController<AmityPost> _controller;
  final scrollController = ScrollController();

  void getCommunityFeed(String communityId) {
    _controller = PagingController(
      pageFuture: (token) => AmitySocialClient.newPostRepository()
          .getPosts()
          .targetCommunity(communityId)
          .feedType(AmityFeedType.PUBLISHED)
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    )..addListener(() {
        if (_controller.error == null) {
          _posts
            ..clear()
            ..addAll(_controller.loadedItems);
        }
      });

    scrollController.addListener(() {
      if (scrollController.position.pixels ==
              scrollController.position.maxScrollExtent &&
          _controller.hasMoreItems) {
        _controller.fetchNextPage();
      }
    });
  }
```

```typescript TypeScript
import { FeedRepository } from '@amityco/ts-sdk'

function getCommunityFeed() {
  const unsubscribe = FeedRepository.getCommunityFeed(
    { communityId: 'communityId', feedType: 'published' },
    (response) => {
      if (response.error) throw response.error
      if (!response.loading) {
        console.log('community feed posts:', response.data)
      }
    },
  )
  return unsubscribe
}
```

</CodeGroup>
