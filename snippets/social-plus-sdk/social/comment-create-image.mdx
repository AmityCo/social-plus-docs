{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: comment-create-image | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createImageCommentOnPost(
        commentRepository: AmityCommentRepository,
        postId: String
    ) {
        val attachments: List<AmityComment.Attachment> = listOf(
            AmityComment.Attachment.IMAGE(fileId = "uploaded-image-id1", image = null),
            AmityComment.Attachment.IMAGE(fileId = "uploaded-image-id2", image = null)
        )
        commentRepository.createComment()
            .post(postId = postId)
            .with()
            .attachments(*attachments.toTypedArray())
            .text(text = "Hello world!")
            .build()
            .send()
            .doOnSuccess { comment: AmityComment ->
                // AmityComment
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }

    fun createImageCommentOnStory(
        commentRepository: AmityCommentRepository,
        storyId: String
    ) {
        val attachments: List<AmityComment.Attachment> = listOf(
            AmityComment.Attachment.IMAGE(fileId = "uploaded-image-id1", image = null),
            AmityComment.Attachment.IMAGE(fileId = "uploaded-image-id2", image = null)
        )
        commentRepository.createComment()
            .story(storyId = storyId)
            .with()
            .attachments(*attachments.toTypedArray())
            .text(text = "Hello world!")
            .build()
            .send()
            .doOnSuccess { comment: AmityComment ->
                // AmityComment
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let commentRepository = AmityCommentRepository(client: client)
        let imageURL = URL(string: "file:///path/to/image.jpg")!
        let options = AmityCommentCreateOptions(referenceId: "<post-id>", referenceType: .post, text: "Check this out!", parentId: nil)
        options.setAttachments([.image(fileURL: imageURL)])
        do {
            let comment = try await commentRepository.createComment(with: options)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void createImageComment(String postId, String uploadedImageFileId) {
    AmitySocialClient.newCommentRepository()
        .createComment()
        .post(postId)
        .create()
        .text('Check this out!')
        .attachImage([AmityImage(fileId: uploadedImageFileId)])
        .send()
        .then((AmityComment comment) {
          // image comment created
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { CommentRepository, FileRepository } from '@amityco/ts-sdk'

async function createImageComment(imageFile) {
  const { data: uploadedFile } = await FileRepository.uploadImage(imageFile)
  const { data: comment } = await CommentRepository.createComment({
    referenceType: 'post',
    referenceId: 'postId',
    data: { text: '' },
    attachments: [{ fileId: uploadedFile.fileId, type: 'image' }],
  })
  console.log(comment)
}
```

</CodeGroup>
