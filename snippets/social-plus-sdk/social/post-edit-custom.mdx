{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-edit-custom | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void postEditCustom() async {
    await AmitySocialClient.newPostRepository()
        .editPost('postId')
        .custom({'key': 'updatedValue'})
        .apply();
    print('Custom post edited');
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function postEditCustom(postId: string, customData: Record<string, unknown>) {
  const post = await PostRepository.updatePost({
    postId,
    dataType: 'custom',
    data: customData,
  })
  console.log('Custom post edited:', post.postId)
  return post
}
```

</CodeGroup>
