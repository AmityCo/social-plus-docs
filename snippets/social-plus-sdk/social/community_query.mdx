{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community_query | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun communityQuery() {
        AmitySocialClient.newCommunityRepository()
            .getCommunities()
            .filter(com.amityco.socialcloud.sdk.api.social.community.query.AmityCommunityFilter.ALL)
            .build()
            .query()
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(AndroidSchedulers.mainThread())
            .subscribe({ pagedList ->
                // display communities
            }, { error ->
                error.printStackTrace()
            })
    }
```

```swift iOS
let communityRepository = AmitySocialClient.newCommunityRepository()
        let queryOptions = AmityCommunityQueryOptions()
        queryOptions.filter = .all
        queryOptions.sortBy = .displayName
        let liveCollection = communityRepository.getCommunities(with: queryOptions)
        liveCollection.observe { collection, _, error in
            guard error == nil else { return }
            print("Communities: \(collection.count())")
            for i in 0..<collection.count() {
                if let community = collection.object(at: i) { print("Community: \(community.communityId)") }
            }
        }
```

```dart Flutter
void communityQuery() async {
    final communities = AmitySocialClient.newCommunityRepository()
        .getCommunities()
        .filter(AmityCommunityFilter.ALL)
        .sortBy(AmityCommunitySortOption.DISPLAY_NAME)
        .build()
        .asStream();
    communities.listen((list) {
      print('Communities: \${list.length}');
    });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

function communityQuery(filter: { categoryId?: string; displayName?: string }) {
  const disposer = CommunityRepository.getCommunities(
    { ...filter, limit: 20, sortBy: 'displayName' },
    ({ data: communities, loading, error }) => {
      if (loading) return
      if (error) {
        console.error('Failed to query communities', error)
        return
      }
      console.log('Communities:', communities)
    },
  )

  return disposer
}
```

</CodeGroup>
