{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: comment-delete | updated: 2026-04-30 */}

<CodeGroup>

```kotlin Android
fun deleteComment(
        commentRepository: AmityCommentRepository,
        comment: AmityComment
    ) {

        commentRepository.hardDeleteComment(commentId = comment.getCommentId())
            .doOnComplete {
                // Void
            }
            .doOnError {
                // Exception
            }
            .subscribe()

        commentRepository.hardDeleteComment(commentId = comment.getCommentId())
            .doOnComplete {
                // Void
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let commentRepository = AmityCommentRepository(client: client)
        do {
            let success = try await commentRepository.deleteComment(withId: "<comment-id>")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void deleteComment(String commentId) {
    AmitySocialClient.newCommentRepository()
        .deleteComment(commentId: commentId)
        .then((value) {
          // comment deleted
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

</CodeGroup>
