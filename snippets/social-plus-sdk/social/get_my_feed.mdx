{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_my_feed | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getMyFeed() {
        // Query the current user's feed as a live collection
        AmitySocialClient.newFeedRepository()
            .getMyFeed()
            .build()
            .query()
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(io.reactivex.rxjava3.android.schedulers.AndroidSchedulers.mainThread())
            .subscribe({ pagedList ->
                // Update UI with feed posts
            }, { error ->
                // Handle error
            })
    }
```

```dart Flutter
void getMyFeed() {
    AmitySocialClient.newFeedRepository()
        .getCustomRankingGlobalFeed()
        .getPagingData()
        .listen((posts) {
      for (final post in posts) {
        print('Post: \${post.postId}');
      }
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function getMyFeed() {
  const client = Client.getActiveClient();
  const myUserId = client.currentUser?.userId ?? '';
  const liveCollection = PostRepository.getPosts({ targetId: myUserId, targetType: 'user', feedType: 'myFeed' });
  liveCollection.on('dataUpdated', (posts) => {
    console.log('My feed posts:', posts.length);
  });
}
```

</CodeGroup>
