{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: global-feed-get-post-with-children | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void globalFeedGetPostWithChildren() {
    AmitySocialClient.newFeedRepository()
        .getGlobalFeed()
        .includeChildren(true)
        .asStream()
        .listen((posts) {
      for (final post in posts) {
        print('Post \${post.postId} has \${post.children?.length} children');
      }
    });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function getGlobalFeedPostWithChildren() {
  const { data: posts } = await PostRepository.getPosts({
    targetType: 'globalFeed',
    targetId: 'globalFeed',
    includeDeleted: false,
  })
  for (const post of posts) {
    if (post.children && post.children.length > 0) {
      console.log('Post with children:', post.postId, post.children)
    }
  }
  return posts
}
```

</CodeGroup>
