{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: add-roles | updated: 2026-05-02 */}

<CodeGroup>

```swift iOS
do {
            let success = try await communityModeratiom.addRoles(roles, userIds: userIds)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void addRoles(String communityId) {
    AmitySocialClient.newCommunityRepository()
        .moderation(communityId)
        .addRole('community-moderator', ['userId1', 'userId2'])
        .then((value) {
          // roles added
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function addRoleToCommunityMembers(communityId: string, role: string, userIds: string[]) {
  await CommunityRepository.addRole(communityId, role, userIds)
  console.log(`Role '${role}' assigned to members in community ${communityId}`)
}

// Example usage
addRoleToCommunityMembers('communityId', 'community-moderator', ['userId1'])
```

</CodeGroup>
