{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: latest-comment-get | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getLatestComment(
        commentRepository: AmityCommentRepository,
        postId: String
    ) {
        commentRepository.getLatestComment()
            .post(postId = postId)
            .build()
            .query()
            .doOnSuccess { comment: AmityComment ->
                // AmityComment
            }
            .doOnError {
                // AmityException
            }
            .subscribe()
    }
```

```swift iOS
let commentRepository = AmityCommentRepository(client: AmityUIKitManagerInternal.shared.client)
        let referenceId = "post123"
        let queryOptions = AmityCommentQueryOptions(referenceId: referenceId, referenceType: .post, filterByParentId: false, parentId: nil, orderBy: .lastCreated, includeDeleted: false)
        let collection = commentRepository.getComments(queryOptions)
        collection.observe { liveCollection, _, _ in
            if let latestComment = liveCollection.allObjects().first {
                print("Latest comment: \(latestComment.commentId)")
            }
        }
```

```dart Flutter
void latestCommentGet() async {
    final commentRepo = AmitySocialClient.newCommentRepository();
    final comments = commentRepo
        .getComments()
        .post('postId123')
        .sortBy(AmityCommentSortOption.LAST_CREATED)
        .build()
        .getLiveCollection();
    comments.loadNext();
  }
```

```typescript TypeScript
import { CommentRepository } from '@amityco/ts-sdk'

function getLatestComment(postId: string) {
  const liveCollection = CommentRepository.getComments(
    { referenceId: postId, referenceType: 'post', limit: 1, sortBy: 'lastCreated' },
  )

  liveCollection.on('dataUpdated', (comments) => {
    if (comments.length > 0) {
      const latestComment = comments[0]
      console.log('Latest comment ID:', latestComment.commentId)
      console.log('Latest comment text:', latestComment.data?.text)
    }
  })

  liveCollection.on('dataError', (error) => {
    console.error('Error fetching latest comment:', error)
  })

  return () => liveCollection.dispose()
}
```

</CodeGroup>
