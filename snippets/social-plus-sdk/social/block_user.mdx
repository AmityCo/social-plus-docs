{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: block_user | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun blockUser() {
        AmitySocialClient.newUserRepository()
            .relationship()
            .blockUser(userId = "userId")
            .subscribe()
    }
```

```swift iOS
let userRepository = AmityUserRepository(client: AmityClient.shared)
        userRepository.blockUser(withId: "userId") { success, error in
            if let error = error { print("Error blocking user: \(error)"); return }
            print("User blocked successfully")
        }
```

```dart Flutter
void blockUser() {
    AmitySocialClient.newUserRepository()
        .relationship
        .blockUser('userId');
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function blockUser() {
  const userId = 'user456'
  UserRepository.blockUser(userId)
    .then(() => console.log('User blocked'))
    .catch(console.error)
}
```

</CodeGroup>
