{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-get | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun communityGet() {
        AmitySocialClient.newCommunityRepository()
            .getCommunity("communityId")
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(AndroidSchedulers.mainThread())
            .subscribe({ community ->
                val name = community.getDisplayName()
            }, { error ->
                error.printStackTrace()
            })
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository(client: client)
        var token: AmityNotificationToken?
        token = communityRepository.getCommunity(withId: "<community-id>").observe { liveObject, error in
            guard let community = liveObject.object else { return }
            print("Community: \(community.displayName ?? "")")
        }
```

```dart Flutter
void getCommunity(String communityId) {
    AmitySocialClient.newCommunityRepository()
        .getCommunity(communityId)
        .then((AmityCommunity community) {
          // handle result
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

function communityGet(communityId: string) {
  const unsubscribe = CommunityRepository.getCommunity(communityId, ({ data: community, loading, error }) => {
    if (loading) return
    if (error) { console.error(error); return }
    console.log('Community:', community)
    unsubscribe()
  })
}
```

</CodeGroup>
