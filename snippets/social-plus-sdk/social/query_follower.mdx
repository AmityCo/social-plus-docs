{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: query_follower | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void queryFollower() async {
    final userRepository = AmitySocialClient.newUserRepository();
    userRepository
        .relationship()
        .user('userId')
        .getFollowers()
        .asStream()
        .listen((followers) {
      print('Followers: \${followers.length}');
    });
  }
```

```typescript TypeScript
import { FollowRepository } from '@amityco/ts-sdk'

async function queryFollowersUnderscore() {
  const params = {
    userId: 'userId123',
    limit: 20,
  };

  const liveCollection = FollowRepository.getFollowers(params);
  liveCollection.on('dataUpdated', followers => {
    console.log('Followers:', followers.map(f => f.userId));
  });
  liveCollection.on('dataError', err => console.error('Error querying followers:', err));
}
```

</CodeGroup>
