{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-file-get | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun postFileGet() {
        // Query posts as a live collection with real-time updates
        AmitySocialClient.newPostRepository()
            .getPosts()
            .targetMe()
            .build()
            .query()
            .subscribe({ pagingData ->
                // Handle paged post data
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
let fileRepository = AmityFileRepository(client: AmityClient.shared)
        let fileId = "file_id"
        fileRepository.getFile(withFileId: fileId) { file, error in
            if let error = error {
                print("Failed to get file: \(error.localizedDescription)")
                return
            }
            if let file = file {
                print("File URL: \(file.fileURL?.absoluteString ?? "")")
            }
        }
```

```dart Flutter
void getFile(AmityPost post) {
    final AmityPostData? amityPostData = post.data;
    if (amityPostData != null) {
      final fileData = amityPostData as FileData;
      final AmityFile amityFile = fileData.file!;
      final String? fileUrl = amityFile.fileUrl;
    }
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function postFileGet() {
  const liveObject = PostRepository.getPost('postId123');
  liveObject.on('dataUpdated', (post) => {
    const fileId = post?.data?.fileId;
    console.log('Post file ID:', fileId);
  });
}
```

</CodeGroup>
