{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: handle_error_from_client_delegate | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun handleErrorFromClientDelegate() {
        // Handle errors globally, including ban errors, from the SDK client
        AmityCoreClient.setErrorHandler { throwable ->
            when (throwable) {
                is com.amityco.socialcloud.sdk.core.exception.AmityException -> {
                    // Handle specific SDK exception (e.g., global ban)
                    val code = throwable.code
                }
                else -> {
                    // Handle unexpected errors
                }
            }
        }
    }
```

```dart Flutter
void handleErrorFromClientDelegate() {
    AmityUIKitManager.setup(
      apiKey: 'YOUR_API_KEY',
      region: AmityRegionalHttpEndpoint.SG,
    );
    // Handle errors via error callback
    AmityUIKitManager.onError((error, stackTrace) {
      print('SDK Error: \${error.message}');
      print('Error code: \${error.code}');
    });
  }
```

```typescript TypeScript
import { createClient } from '@amityco/ts-sdk'

async function handleErrorFromClientDelegate() {
  const client = createClient('your-api-key')
  client.onError = (error) => {
    const { code, message } = error
    console.error(`SDK Error [${code}]: ${message}`)
    if (code === 400301) {
      console.warn('User is globally banned')
    }
  }
  await client.login({ userId: 'user-id-123', displayName: 'John Doe' })
}
```

</CodeGroup>
