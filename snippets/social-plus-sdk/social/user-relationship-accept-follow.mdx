{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-relationship-accept-follow | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun userRelationshipAcceptFollow() {
        // TODO: implement User relationship accept follow
    }
```

```swift iOS
let userRelationshipRepository = AmityUserRelationshipRepository(client: AmityClient.shared)
        userRelationshipRepository.acceptFollowRequest(withUserId: "userId") { success, error in
            if success {
                print("Follow request accepted via relationship API")
            } else if let error = error {
                print("Accept failed: \(error)")
            }
        }
```

```dart Flutter
void acceptFollowRequest(String userId) {
    AmityCoreClient.newUserRepository()
        .relationship()
        .accept(userId)
        .then((value) => {
              //success
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function acceptFollowRequest() {
  await UserRepository.acceptFollowRequest('userId')
}
```

</CodeGroup>
