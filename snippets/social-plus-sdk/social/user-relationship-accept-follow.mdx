{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-relationship-accept-follow | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun acceptFollow(userId: String) {
        AmityCoreClient.newUserRepository()
            .relationship()
            .acceptMyFollower(userId = userId)
            .doOnComplete {
                // success
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let userRepository = AmityUserRepository(client: client)
        do {
            try await userRepository.userRelationship.acceptMyFollower(withUserId: "<user-id>")
            print("Follow request accepted")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void acceptFollowRequest(String userId) {
    AmityCoreClient.newUserRepository()
        .relationship()
        .accept(userId)
        .then((value) => {
              //success
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function acceptFollowRequest() {
  await UserRepository.acceptFollowRequest('userId')
}
```

</CodeGroup>
