{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: follow-user | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun followUser(userId: String) {
        AmityCoreClient.newUserRepository()
            .relationship()
            .with(userId)
            .follow()
            .doOnComplete {
                // User followed successfully
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let userRepository = AmityUserRepository(client: AmityManager.shared.client!)
        userRepository.follow(userId: "targetUserId") { _, error in
            if let error = error {
                print("Failed to follow user: \(error)")
            } else {
                print("Now following user")
            }
        }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function followUser(userId: string) {
  await UserRepository.follow(userId)
  console.log(`Now following user ${userId}`)
}

// Example usage
followUser('userId')
```

</CodeGroup>
