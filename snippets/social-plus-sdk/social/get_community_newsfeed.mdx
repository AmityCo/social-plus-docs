{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_community_newsfeed | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getCommunityNewsfeed() {
        // Query the news feed for a specific community
        AmitySocialClient.newFeedRepository()
            .getCommunityFeed(communityId = "communityId")
            .build()
            .query()
            .subscribe { feed -> /* handle community newsfeed posts */ }
    }
```

```swift iOS
// Get community news feed posts
        let feedRepository = AmityFeedRepository(client: AmityManager.shared.client!)
        var token: AmityNotificationToken?
        let collection = feedRepository.getCommunityFeed(
            withCommunityId: "communityId",
            sortBy: .lastCreated,
            includeDeleted: false,
            feedType: .published
        )
        token = collection.observe { liveCollection, _, error in
            if let error = error {
                print("Failed to get community newsfeed: \(error.localizedDescription)")
                return
            }
            print("Community newsfeed posts: \(liveCollection.count())")
        }
```

```dart Flutter
void getCommunityNewsfeed() async {
    AmitySocialClient.newPostRepository()
        .getPosts()
        .targetCommunity(communityId: 'communityId')
        .getPagingData(token: null, limit: 20)
        .asStream()
        .listen((result) {
      final posts = result.data;
    });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

function getCommunityNewsfeed() {
  const communityId = 'community123'
  const liveCollection = PostRepository.getPosts({ targetId: communityId, targetType: 'community', limit: 20 })
  liveCollection.on('dataUpdated', (posts) => {
    console.log('Community newsfeed:', posts)
  })
}
```

</CodeGroup>
