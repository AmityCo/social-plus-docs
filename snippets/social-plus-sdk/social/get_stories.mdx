{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_stories | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void getStories() async {
    final stories = AmitySocialClient.newStoryRepository()
        .getStories(
          targetId: 'communityId',
          targetType: AmityStoryTargetType.COMMUNITY,
        )
        .build()
        .asStream();
    stories.listen((list) {
      print('Stories: \${list.length}');
    });
  }
```

```typescript TypeScript
import { StoryRepository } from '@amityco/ts-sdk'

function getStories(targetId: string, targetType: 'community' | 'user') {
  const disposer = StoryRepository.getStoriesByTarget(
    { targetId, targetType },
    ({ data: stories, loading, error }) => {
      if (loading) return
      if (error) {
        console.error('Failed to fetch stories', error)
        return
      }
      console.log('Stories:', stories)
    },
  )

  return disposer
}
```

</CodeGroup>
