{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-get-reach-users | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
final _reachedUsers = <AmityUser>[];
  late PagingController<AmityUser> _controller;

  void getPostReachedUsers(String postId) {
    _controller = PagingController(
      pageFuture: (token) => AmitySocialClient.newPostRepository()
          .getReachedUsers(postId: postId)
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    )..addListener(() {
        if (_controller.error == null) {
          _reachedUsers
            ..clear()
            ..addAll(_controller.loadedItems);
          // update UI
        } else {
          // handle error
        }
      });

    _controller.fetchNextPage();
  }
```

</CodeGroup>
