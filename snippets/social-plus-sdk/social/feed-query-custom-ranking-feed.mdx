{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: feed-query-custom-ranking-feed | updated: 2026-04-30 */}

<CodeGroup>

```kotlin Android
fun queryCustomRankingFeed(feedRepository: AmityFeedRepository) {
        feedRepository
            .getCustomRankingGlobalFeed()
            .build()
            .query()
            .doOnNext { posts: PagingData<AmityPost> ->
                // PagingData<AmityPost>
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let feedRepository = AmityFeedRepository(client: client)
        var token: AmityNotificationToken?
        token = feedRepository.getCustomRankingGlobalFeed().observe { collection, change, error in
            guard error == nil else { return }
            print("Custom ranking feed posts: \(collection.count())")
        }
```

```typescript TypeScript
import { FeedRepository } from '@amityco/ts-sdk'

async function queryCustomRankingFeed() {
  const liveCollection = FeedRepository.getCustomRankingGlobalFeed()
  liveCollection.on('dataUpdated', (posts) => {
    console.log(posts)
  })
}
```

</CodeGroup>
