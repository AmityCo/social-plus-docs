{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: add-community-member | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun addCommunityMember(communityId: String, userIds: List<String>) {
        AmitySocialClient.newCommunityRepository()
            .membership(communityId)
            .addMembers(userIds = userIds)
            .doOnComplete { }
            .doOnError { }
            .subscribe()
    }
```

```swift iOS
communityRepository.addMembers(["userId1", "userId2"], communityId: "communityId") { success, error in
            if let error = error {
                print("Error adding member: \(error)")
            } else {
                print("Members added successfully")
            }
        }
```

```dart Flutter
void addCommunityMembers(String communityId) {
    AmitySocialClient.newCommunityRepository()
        .membership(communityId)
        .addMembers(['userId1', 'userId2'])
        .then((members) {
          // members added
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function addCommunityMembers(communityId: string, userIds: string[]) {
  await CommunityRepository.addMembers(communityId, userIds)
  console.log(`Added ${userIds.length} member(s) to community ${communityId}`)
}

// Example usage
addCommunityMembers('communityId', ['userId1', 'userId2'])
```

</CodeGroup>
