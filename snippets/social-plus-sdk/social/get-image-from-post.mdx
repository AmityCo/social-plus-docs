{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-image-from-post | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getImageFromPost(post: AmityPost) {
        val imageData = post.getData() as? AmityPost.Data.IMAGE
        imageData?.getImage()?.let { image ->
            val imageUrl = image.getUrl()
            // handle image data
        }
        // or iterate children for multiple images
        post.getChildren().forEach { child ->
            val childImage = child.getData() as? AmityPost.Data.IMAGE
            childImage?.getImage()?.let { image ->
                val imageUrl = image.getUrl()
            }
        }
    }
```

```swift iOS
// Extract image data from a post's children posts
        let postRepository = AmityPostRepository(client: AmityManager.shared.client!)
        var token: AmityNotificationToken?
        let liveObject = postRepository.getPost(withPostId: "postId")
        token = liveObject.observe { post, error in
            if let error = error {
                print("Failed to get post: \(error.localizedDescription)")
                return
            }
            guard let post = post.object else { return }
            let imagePosts = post.getChildrenPosts()?.filter { $0.dataType == "image" } ?? []
            imagePosts.forEach { imagePost in
                if let imageData = imagePost.data as? AmityImageData {
                    print("Image URL: \(imageData.largeFileURL?.absoluteString ?? "")")
                }
            }
        }
```

```dart Flutter
void getImageFromPost() {
    final post = await AmitySocialClient.newPostRepository()
        .getPost('postId');
    final imageData = post.data as AmityImageData;
    final imageUrl = imageData.fileInfo?.fileUrl;
    print('Image URL: \$imageUrl');
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function getImageFromPost() {
  const liveObject = PostRepository.getPost('postId123');
  liveObject.on('dataUpdated', (post) => {
    const imageChildren = post.children?.filter((c: any) => c.dataType === 'image') ?? [];
    imageChildren.forEach((child: any) => {
      console.log('Image fileId:', child.data?.fileId, 'fileUrl:', child.data?.fileUrl);
    });
  });
}
```

</CodeGroup>
