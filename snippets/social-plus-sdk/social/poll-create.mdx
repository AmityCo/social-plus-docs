{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: poll-create | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createPoll(question: String, answers: List<String>) {
        AmitySocialClient.newPollRepository()
            .createPoll(question = question)
            .answers(answers = answers.map { AmityPoll.Answer.Text(it) })
            .build()
            .create()
            .doOnSuccess { poll: AmityPoll ->
                // handle created poll
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let pollRepository = AmityPollRepository(client: client)
        let options = AmityPollCreateOptions()
        options.setQuestion("What is your favourite season?")
        options.setAnswers(["Spring", "Summer", "Autumn", "Winter"])
        options.setMultipleSelection(false)
        do {
            let pollId = try await pollRepository.createPoll(options)
            print("Poll created: \(pollId)")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void pollCreate() async {
    final poll = await AmitySocialClient.newPollRepository()
        .createPoll(
          question: 'What is your favorite color?',
          answers: ['Red', 'Blue', 'Green'],
          answerType: AmityPollAnswerType.SINGLE,
          closedIn: 86400,
        );
    print('Created poll: \${poll.pollId}');
  }
```

```typescript TypeScript
import { PollRepository } from '@amityco/ts-sdk'

async function createPoll() {
  const newPoll = await PollRepository.createPoll({
    question: 'What is your favorite programming language?',
    answers: [
      { dataType: 'text', data: 'TypeScript' },
      { dataType: 'text', data: 'Python' },
      { dataType: 'text', data: 'Rust' },
    ],
    answerType: 'single',
    closedIn: 86400000, // 24 hours in ms
  })

  console.log('Poll created:', newPoll.pollId)
  return newPoll
}
```

</CodeGroup>
