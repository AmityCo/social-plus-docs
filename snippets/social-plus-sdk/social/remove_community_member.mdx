{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: remove_community_member | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun removeCommunityMember() {
        // perform remove community member operation
        AmitySocialClient.newCommunityRepository()
            .membership("communityId")
            .removeMembers(listOf("userId1", "userId2"))
            .subscribe()
    }
```

```swift iOS
do {
            let isRemoved = try await communityMembership.removeMembers(["user1", "user2"])
        } catch {
            // Handle error here
        }
```

```dart Flutter
void removeCommunityMember() {
    AmityCoreClient.newCommunityRepository()
        .moderation(communityId: 'communityId123')
        .removeMembers(['userId1', 'userId2'])
        .then((_) => print('Members removed'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function removeCommunityMember() {
  await CommunityRepository.removeMembers({
    communityId: 'communityId123',
    userIds: ['userId123', 'userId456'],
  });
  console.log('Community members removed successfully');
}
```

</CodeGroup>
