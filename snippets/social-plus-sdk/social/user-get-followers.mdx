{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-get-followers | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun userGetFollowers() {
        // TODO: implement user get followers using Social Plus SDK
    }
```

```swift iOS
func userGetFollowers() {
        let followManager = AmityManager.shared.client?.followManager
        let liveCollection = followManager?.getFollowers(userId: "userId")
        
        let token = liveCollection?.observe { collection, changes, error in
            guard error == nil else { return }
            print("Followers count: \(collection.count())")
            for relationship in collection.allObjects() {
                print("Follower: \(relationship.sourceUser?.displayName ?? "")")
            }
        }
        // Retain token as long as observation is needed
    }
```

```dart Flutter
List<AmityFollowRelationship> _followers = [];
  late PagingController<AmityFollowRelationship> _controller;
  final scrollController = ScrollController();

  void getFollowers(String userId) {
    _controller = PagingController(
      pageFuture: (token) => AmityCoreClient.newUserRepository()
          .relationship()
          .getFollowers(userId)
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    )..addListener(() {
        if (_controller.error == null) {
          _followers
            ..clear()
            ..addAll(_controller.loadedItems);
        }
      });

    scrollController.addListener(() {
      if (scrollController.position.pixels ==
              scrollController.position.maxScrollExtent &&
          _controller.hasMoreItems) {
        _controller.fetchNextPage();
      }
    });
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

function watchUserFollowers() {
  const liveCollection = UserRepository.getFollowers('userId')
  liveCollection.on('dataUpdated', (followers) => {
    console.log(followers)
  })
  return liveCollection
}
```

</CodeGroup>
