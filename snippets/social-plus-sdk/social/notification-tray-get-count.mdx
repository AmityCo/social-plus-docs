{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: notification-tray-get-count | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getNotificationTrayCount() {
        AmitySocialClient.newNotificationTrayRepository()
            .getUnseenCount()
            .doOnNext { count: Int ->
                // handle unseen count
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
var token: AmityNotificationToken?
        token = client.notificationTray.getNotificationTraySeen().observe { liveObject, error in
            guard let snapshot = liveObject.snapshot else { return }
            print("Is seen: \(snapshot.isSeen)")
            print("Last tray occurred at: \(String(describing: snapshot.lastTrayOccurredAt))")
        }
```

```dart Flutter
void notificationTrayGetCount() {
    final channelId = 'channelId';
    AmityCoreClient.newChannelRepository()
        .channel(channelId)
        .subscription(AmityChannelEvents.MESSAGES)
        .subscribeTopic()
        .then((_) => print('Subscribed to channel events'));
    final count = AmityCoreClient.newChannelRepository()
        .channel(channelId)
        .getUnreadCount();
    print('Unread count: \$count');
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function getNotificationTrayCount() {
  const liveObject = UserRepository.getNotificationTrayUnseenCount()
  liveObject.on('dataUpdated', (count) => {
    console.log('Unseen count:', count)
  })
}
```

</CodeGroup>
