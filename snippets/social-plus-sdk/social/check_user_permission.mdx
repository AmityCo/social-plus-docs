{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: check_user_permission | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun checkUserPermission() {
        AmitySocialClient.newCommunityRepository()
            .moderation("communityId")
            .hasPermission(AmityPermission.EDIT_USER)
            .subscribe { hasPermission ->
                if (hasPermission) { /* proceed */ }
            }
    }
```

```swift iOS
AmityClient.shared.hasPermission(.editCommunity, forCommunity: "communityId") { hasPermission in
            print("User has community edit permission: \(hasPermission)")
        }
```

```dart Flutter
void checkUserPermission() {
    AmitySocialClient.newUserRepository()
        .getUser('userId')
        .then((user) {
          final roles = user.roles;
        })
        .onError((error, stackTrace) { });
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function checkUserPermission(userId: string) {
  try {
    const { data: user } = await UserRepository.getUser(userId);
    console.log('User ID:', user.userId);
    console.log('Display name:', user.displayName);
    console.log('Roles:', user.roles);
    const hasPermission = user.roles?.includes('global-admin') || user.roles?.includes('super-moderator');
    console.log('Has elevated permission:', hasPermission);
    return hasPermission;
  } catch (error) {
    console.error('Failed to check user permission:', error);
  }
}
```

</CodeGroup>
