{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: story-impression-mark | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun markImpression(story: AmityStory) {
        AmitySocialClient.newStoryRepository()
            .markAsSeen(storyId = story.getStoryId())
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let storyRepository = AmityStoryRepository(client: AmityManager.shared.client!)
        storyRepository.markStoryAsSeen(storyId: "storyId") { success, error in
            print("Story impression marked: \(success)")
        }
```

```dart Flutter
void storyImpressionMark() {
    AmityCoreClient.newSocialRepository()
        .story
        .markAsSeen(storyId: 'storyId123')
        .then((_) => print('Story impression marked'))
        .onError((error, _) => print('Error marking impression: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function storyImpressionMark() {
  import { StoryRepository } from '@amityco/ts-sdk';

  await StoryRepository.markStoryAsViewed({ storyId: 'story-id-123' });
  console.log('Story impression marked');
}
```

</CodeGroup>
