{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-get-blocked | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun userGetBlocked() {
        // TODO: implement user get blocked using Social Plus SDK
    }
```

```swift iOS
func userGetBlocked() {
        let userRepository = AmityUserRepository(client: AmityManager.shared.client!)
        let liveCollection = userRepository.getBlockedUsers()
        
        let token = liveCollection.observe { collection, changes, error in
            guard error == nil else { return }
            let blockedUsers = collection.allObjects()
            print("Blocked users count: \(blockedUsers.count)")
            for user in blockedUsers {
                print("Blocked user: \(user.displayName ?? ""), id: \(user.userId)")
            }
        }
        // Retain token as long as observation is needed
    }
```

```dart Flutter
final _blockedUsers = <AmityUser>[];
  late PagingController<AmityUser> _controller;

  void getBlockedUsers() {
    _controller = PagingController(
      pageFuture: (token) => AmityCoreClient.newUserRepository()
          .getBlockedUsers()
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    )..addListener(() {
        if (_controller.error == null) {
          _blockedUsers.clear();
          _blockedUsers.addAll(_controller.loadedItems);
          // update widgets
        } else {
          // handle error
        }
      });
  }
```

</CodeGroup>
