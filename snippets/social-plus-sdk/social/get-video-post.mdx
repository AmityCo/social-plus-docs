{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-video-post | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getVideoPost() {
        // Retrieve a video post by post ID
        AmitySocialClient.newPostRepository()
            .getPost(postId = "postId")
            .subscribe { post -> /* handle video post data */ }
    }
```

```swift iOS
// Get a video post by ID
        let postRepository = AmityPostRepository(client: AmityManager.shared.client!)
        var token: AmityNotificationToken?
        let liveObject = postRepository.getPost(withPostId: "postId")
        token = liveObject.observe { post, error in
            if let error = error {
                print("Failed to get video post: \(error.localizedDescription)")
                return
            }
            guard let post = post.object else { return }
            let videoPosts = post.getChildrenPosts()?.filter { $0.dataType == "video" } ?? []
            print("Video post has \(videoPosts.count) video attachment(s)")
        }
```

```dart Flutter
void getVideoPost() async {
    final post = await AmitySocialClient.newPostRepository()
        .getPost(postId: 'videoPostId');
    final videoData = post.data as AmityVideoPostData;
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

function getVideoPost() {
  const postId = 'videoPost123'
  const liveObject = PostRepository.getPost(postId)
  liveObject.on('dataUpdated', (post) => {
    const videoData = post.data
    console.log('Video post:', post, 'Video data:', videoData)
  })
}
```

</CodeGroup>
