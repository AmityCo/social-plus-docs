{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: join-community | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun joinCommunity(communityId: String) {
        AmitySocialClient.newCommunityRepository()
            .joinCommunity(communityId = communityId)
            .doOnComplete {
                // joined community successfully
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository()
        communityRepository.joinCommunity(withId: "communityId") { error in
            if let error = error {
                print("Error joining community: \(error)")
            } else {
                print("Joined community successfully")
            }
        }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function joinCommunity(communityId: string) {
  await CommunityRepository.joinCommunity(communityId)
  console.log(`Joined community: ${communityId}`)
}

// Example usage
joinCommunity('communityId')
```

</CodeGroup>
