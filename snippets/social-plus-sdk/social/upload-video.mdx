{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: upload-video | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void uploadVideo() {
    final fileRepo = AmityCoreClient.newFileRepository();
    fileRepo
        .uploadVideo(AmityUploadableFile.fromPath('/path/to/video.mp4'))
        .then((AmityVideoInfo videoInfo) {
          print('Video uploaded. FileId: \${videoInfo.fileId}');
        })
        .onError((error, stackTrace) {
          print('Error: \$error');
        });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function uploadVideo() {
  const client = Client.getActiveClient();
  const formData = new FormData();
  formData.append('file', videoBlob, 'video.mp4');
  const uploadedVideo = await client.uploadVideo(formData);
  const post = await PostRepository.createPost({
    targetType: 'community',
    targetId: 'communityId',
    dataType: 'video',
    data: { fileId: uploadedVideo.fileId },
  });
  console.log('Video post created:', post.postId);
}
```

</CodeGroup>
