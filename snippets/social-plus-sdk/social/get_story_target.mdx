{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_story_target | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getStoryTarget() {
        // Retrieve story target info for a community
        AmitySocialClient.newStoryRepository()
            .getStoryTarget(
                targetType = com.amityco.socialcloud.sdk.model.social.story.AmityStory.TargetType.COMMUNITY,
                targetId = "communityId"
            )
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(io.reactivex.rxjava3.android.schedulers.AndroidSchedulers.mainThread())
            .subscribe({ storyTarget ->
                val hasUnseen = storyTarget.hasUnseen()
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
let token = storyRepository.getStoryTarget(targetType: .community, targetId: "targetId").observe { object, error in
            if let error {
                print(error)
                return
            }
            
            guard let storyTarget = object.snapshot else { return}
            print(storyTarget)
            // Use storyTarget
        }
```

```dart Flutter
void getStoryTarget() {
    AmitySocialClient.newStoryRepository()
        .getStoryTarget(
          targetId: 'communityId',
          targetType: AmityStoryTargetType.COMMUNITY,
        )
        .asStream()
        .listen((target) {
      print('Target ID: \${target.targetId}');
      print('Has unseen: \${target.hasUnseen}');
    });
  }
```

```typescript TypeScript
import { StoryRepository } from '@amityco/ts-sdk'

async function getStoryTarget() {
  const liveObject = StoryRepository.getStoryTarget({
    targetType: 'community',
    targetId: 'community-id-123',
  })
  const unsubscribe = liveObject.subscribe(({ data: target, loading, error }) => {
    if (error) { console.error(error); return }
    if (!loading) console.log('Story target:', target)
  })
  return unsubscribe
}
```

</CodeGroup>
