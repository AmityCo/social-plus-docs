{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: notification-tray-seen-get | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getNotificationTraySeen() {
        AmityCoreClient
            .notificationTray()
            .getNotificationTraySeen()
            .doOnNext { notificationTraySeen ->
                // AmityNotificationTraySeen
            }
            .doOnError {
                // Exception
            }.subscribe()
    }
```

```swift iOS
let notificationTrayRepository = AmityNotificationTrayRepository(client: AmityClient.shared)
        notificationTrayRepository.getSeenStatus { isSeen, error in
            if let error = error {
                print("Failed to get seen status: \(error.localizedDescription)")
                return
            }
            print("Notification tray seen status: \(isSeen)")
        }
```

```dart Flutter
void notificationTraySeenGet() async {
        final liveObject = AmityNotificationTray()
            .getItem(notificationId: 'notif123')
            .asLiveObject();

        liveObject.asStream().listen((item) {
          print('Seen status: \${item?.isSeen}');
        });
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

function getNotificationTraySeen() {
  const liveObject = UserRepository.getNotificationTraySeen()
  liveObject.on('dataUpdated', (seen) => {
    console.log('Notification tray seen:', seen)
  })
  return liveObject
}
```

</CodeGroup>
