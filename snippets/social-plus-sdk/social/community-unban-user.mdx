{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-unban-user | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun communityUnbanUser(communityId: String, userId: String) {
        AmitySocialClient.newCommunityRepository()
            .moderation(communityId = communityId)
            .unbanMembers(userIds = listOf(userId))
            .subscribe(
                { println("User unbanned: $userId") },
                { error -> println("Unban failed: ${error.message}") }
            )
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository()
        let moderation = AmityCommunityModeration(client: AmityUIKitManagerInternal.shared.client, andCommunityId: "communityId")
        moderation.unbanMembers(["userId"]) { error in
            if let error = error {
                print("Failed to unban user: \(error.localizedDescription)")
            } else {
                print("User unbanned successfully")
            }
        }
```

```dart Flutter
void communityUnbanUser() {
    AmitySocialClient.newCommunityRepository()
        .moderation('communityId')
        .unbanMember(['userId'])
        .then((_) { })
        .onError((error, stackTrace) { });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function communityUnbanUser(communityId: string, userId: string) {
  try {
    await CommunityRepository.Moderation.unbanMembers(communityId, [userId]);
    console.log('User unbanned from community:', userId);
  } catch (error) {
    console.error('Failed to unban user from community:', error);
  }
}
```

</CodeGroup>
