{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: comment-edit-with-links | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun editCommentWithLinks(
        commentRepository: AmityCommentRepository,
        comment: AmityComment
    ) {
        val links = listOf(
            AmityLink(
                index = 6,
                length = 18,
                url = "https://amity.co",
                renderPreview = false,
                domain = null,
                title = null,
                imageUrl = null,
            )
        )

        commentRepository.editComment(comment.getCommentId())
            .text(text = "Visit https://amity.co for more info")
            .links(links = links)
            .build()
            .apply()
            .doOnSuccess { updatedComment: AmityComment ->
                val commentLinks = updatedComment.getLinks()
                // list of AmityLink or null for legacy comments
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```dart Flutter
void editCommentWithLinks(String commentId) {
    final newText = 'Updated: Visit https://amity.co for more info!';
    final links = [
      AmityLinkPreview(
        url: 'https://amity.co',
        title: 'Amity',
        description: 'Amity Social Cloud',
      ),
    ];

    AmitySocialClient.newCommentRepository()
        .editComment(commentId: commentId)
        .text(newText)
        .links(links: links)
        .build()
        .apply()
        .then((AmityComment updatedComment) {
          // Comment with links edited successfully
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }
```

</CodeGroup>
