{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: posts_query_by_id | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun postsQueryById() {
        // Query posts by ID
        AmitySocialClient.newPostRepository()
            .getPosts()
            .targetUser("userId")
            .build()
            .query()
            .asLiveData()
    }
```

```swift iOS
var token: AmityNotificationToken?
        
        token = postRepository.getPosts(postIds: ["post-id-1", "post-id-2"]).observe { collection, changes, error in
            // Observe live collection here
        }
```

```dart Flutter
void queryPostsById(List<String> postIds) {
      for (final postId in postIds) {
        AmitySocialClient.newPostRepository()
            .getPost(postId)
            .first
            .then((AmityPost post) {
          // Process each post
        }).onError((error, stackTrace) {
          // Handle error
        });
      }
    }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function postsQueryById() {
  const { PostRepository } = Amity;
  const postIds = ['postId1', 'postId2', 'postId3'];
  const posts = await Promise.all(postIds.map((id) => PostRepository.getPost(id)));
  console.log('Posts by ID:', posts.map((p) => p.data.postId));
}
```

</CodeGroup>
