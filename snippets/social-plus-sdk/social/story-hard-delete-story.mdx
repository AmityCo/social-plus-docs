{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: story-hard-delete-story | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun storyHardDeleteStory() {
        AmitySocialClient.newStoryRepository()
            .hardDeleteStory(storyId = "storyId")
            .subscribe()
    }
```

```swift iOS
let storyRepository = AmityStoryRepository(client: AmityManager.shared.client!)
        storyRepository.hardDeleteStory(withId: "storyId") { success, error in
            print("Story hard-deleted: \(success)")
        }
```

```dart Flutter
void hardDeleteStory(AmityStory story) {
    story
        .hardDelete()
        .then((value) => {
              //success - story is permanently deleted
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function storyHardDeleteStory() {
  import { StoryRepository } from '@amityco/ts-sdk';

  await StoryRepository.hardDeleteStory('story-id-123');
  console.log('Story permanently deleted');
}
```

</CodeGroup>
