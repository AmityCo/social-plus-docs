{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: blocked-user-query | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun queryBlockedUsers(
        userRepository: AmityUserRepository
    ) {
        userRepository
            .getBlockedUsers()
            .doOnNext { pagingData: PagingData<AmityUser> ->
                // PagingData<AmityUser>
            }
            .subscribe()
    }
```

```swift iOS
let collection = userRepository.getBlockedUsers()
        collection.observe { liveCollection, _, error in
            if let error = error {
                print("Error: \(error)")
                return
            }
            let users = liveCollection.snapshots
            print("Blocked users count: \(users.count)")
            users.forEach { print("Blocked: \($0.userId)") }
        }
```

```dart Flutter
final _blockedUsers = <AmityUser>[];
  late PagingController<AmityUser> _controller;

  void queryBlockedUsers() {
    _controller = PagingController(
      pageFuture: (token) => AmityCoreClient.newUserRepository()
          .getBlockedUsers()
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    )..addListener(() {
        if (_controller.error == null) {
          _blockedUsers.clear();
          _blockedUsers.addAll(_controller.loadedItems);
          // update widgets
        } else {
          // handle error
        }
      });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function blockedUserQuery() {
  const client = createClient('apiKey', 'region')
  await client.login({ userId: 'user123', displayName: 'User 123' })
  const liveCollection = client.user.queryBlockedUsers()
  liveCollection.on('dataUpdated', (users) => {
    users.forEach((u) => console.log('Blocked:', u.userId, u.displayName))
  })
}
```

</CodeGroup>
