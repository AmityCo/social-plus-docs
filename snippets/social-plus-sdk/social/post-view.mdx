{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-view | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun postView() {
        // Mark post as viewed to track impressions
        AmitySocialClient.newPostRepository()
            .markPostAsViewed("postId")
            .subscribe(
                { /* post marked as viewed */ },
                { error -> /* handle error */ }
            )
    }
```

```swift iOS
let client = AmityClient.shared
        let postRepository = AmityPostRepository(client: client)
        postRepository.markPostAsViewed(withId: "<postId>") { success, error in
            if let error = error { print("Error: \(error)"); return }
            print("Post view recorded: \(success)")
        }
```

```dart Flutter
void markPostAsViewed(String postId) {
      AmitySocialClient.newPostRepository()
          .getPost(postId)
          .first
          .then((AmityPost post) {
        post.analytics().markAsViewed();
        // Post impression recorded
      }).onError((error, stackTrace) {
        // Handle error
      });
    }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

function viewPost(postId: string) {
  const unsubscribe = PostRepository.getPost(
    postId,
    ({ data: post, loading, error }) => {
      if (loading) return
      if (error) throw error
      console.log('Viewing post:', post.postId)
      console.log('View count:', post.viewCount)
      console.log('Reaction count:', post.reactionsCount)
    },
  )
  return unsubscribe
}
```

</CodeGroup>
