{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: unflag-community | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun unflagCommunity() {
        AmitySocialClient.newCommunityRepository()
            .unflagCommunity("communityId")
            .subscribe({
                // Community unflagged successfully
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
let communityId = "communityId"
        AmitySDK.unflagCommunity(communityId: communityId) { success, error in
            if let error = error {
                print("Failed to unflag community: \(error)")
                return
            }
            print("Community unflagged successfully")
        }
```

```dart Flutter
void unflagCommunity(String communityId) {
    AmitySocialClient.newCommunityRepository()
        .unflagCommunity(communityId: communityId)
        .then((_) {
          // community unflagged
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function unflagCommunity() {
  const communityRepository = new CommunityRepository();
  await communityRepository.unflagCommunity('communityId');
  console.log('Community unflagged');
}
```

</CodeGroup>
