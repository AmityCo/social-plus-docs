{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: ban_community_members | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun banCommunityMembers() {
        AmitySocialClient.newCommunityRepository()
            .moderation("communityId")
            .banMembers(userIds = listOf("userId1", "userId2"))
            .subscribe()
    }
```

```swift iOS
let communityRepository = AmitySocialClient.newCommunityRepository()
        communityRepository.banMembers(["userId1", "userId2"], communityId: "communityId") { success, error in
            if let error = error { print("Error banning members: \(error)"); return }
            print("Community members banned")
        }
```

```dart Flutter
void banCommunityMembers() {
    AmitySocialClient.newCommunityRepository()
        .moderation('communityId')
        .banMember(['userId'])
        .then((_) { })
        .onError((error, stackTrace) { });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function banCommunityMembers(communityId: string, userIds: string[]) {
  try {
    await CommunityRepository.Moderation.banMembers(communityId, userIds);
    console.log('Members banned:', userIds);
  } catch (error) {
    console.error('Failed to ban community members:', error);
  }
}
```

</CodeGroup>
