{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: reply_comment_image | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun replyCommentImage() {
        // perform reply comment image operation
        AmitySocialClient.newCommentRepository()
            .createComment()
            .post("postId")
            .with()
            .text("This is a comment")
            .build()
            .comment()
            .subscribe()
    }
```

```swift iOS
func replyToCommentWithImage(commentRepository: AmityCommentRepository,
                                 postId: String,
                                 commentId: String) async {
        
        let createOptions = AmityCommentCreateOptions(referenceId: postId,
                                                      referenceType: .post,
                                                      text: "",
                                                      attachments: [.image(fileId: "4f22aca58098487daf30fe3b30891e25")],
                                                      parentId: commentId)
        
        do {
           let comment = try await commentRepository.createComment(with: createOptions)
        } catch {
            // Handle error here
        }
    }
```

```dart Flutter
void replyCommentImage() {
    AmityCoreClient.newSocialRepository()
        .comment
        .createComment()
        .post(postId: 'postId123')
        .parentId('parentCommentId')
        .image(imageFile: imageFile)
        .create()
        .then((comment) => print('Image reply created: \${comment.commentId}'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function replyCommentImage() {
  const imageFile = new File([imageBlob], 'reply-image.jpg', { type: 'image/jpeg' });
  await CommentRepository.createComment({
    referenceType: 'post',
    referenceId: 'postId123',
    parentId: 'parentCommentId123',
    data: { text: 'Check this out!' },
    attachments: [{ fileId: await uploadImage(imageFile), type: 'image' }],
  });
  console.log('Image reply comment created');
}
```

</CodeGroup>
