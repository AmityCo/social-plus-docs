{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-stories-by-target | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getStoriesByTarget() {
        // Query stories for a specific target (user or community)
        AmitySocialClient.newStoryRepository()
            .getStories(targetId = "communityId", targetType = AmityStoryTargetType.COMMUNITY)
            .build()
            .query()
            .subscribe { stories -> /* handle stories by target */ }
    }
```

```swift iOS
// Get active stories for a specific target ID and type
        let storyRepository = AmityStoryRepository(client: AmityManager.shared.client!)
        var token: AmityNotificationToken?
        let collection = storyRepository.getActiveStories(
            targetId: "communityId",
            targetType: .community
        )
        token = collection.observe { liveCollection, _, error in
            if let error = error {
                print("Failed to get stories: \(error.localizedDescription)")
                return
            }
            let stories = (0..<liveCollection.count()).compactMap { liveCollection.object(at: $0) }
            print("Stories count for target: \(stories.count)")
        }
```

```dart Flutter
void getStoriesByTarget() {
    AmitySocialClient.newStoryRepository()
        .getStories(
          targetId: 'targetId',
          targetType: AmityStoryTargetType.COMMUNITY,
        )
        .query();
  }
```

```typescript TypeScript
import { StoryRepository } from '@amityco/ts-sdk'

async function getStoriesByTarget() {
  const unsubscribe = StoryRepository.getStoriesByTarget(
    { targetType: 'community', targetId: 'community123' },
    (stories, error) => {
      if (error) return console.error(error)
      console.log('Stories:', stories)
      unsubscribe()
    }
  )
}
```

</CodeGroup>
