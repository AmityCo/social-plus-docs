{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: hashtag-get-feed | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun hashtagGetFeed(hashtag: String) {
        AmitySocialClient.newHashtagRepository()
            .getHashtagFeed(hashtag = hashtag)
            .build()
            .query()
            .doOnNext { posts: PagingData<AmityPost> ->
                // PagingData<AmityPost>
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let postRepository = AmityPostRepository()
        let query = AmityPostQuery()
        query.hashtag = "trending"
        let liveCollection = postRepository.getPosts(with: query)
        liveCollection.observe { collection, _, error in
            print("Hashtag feed count: \(collection.count())")
        }
```

```dart Flutter
void hashtagGetFeed() {
    AmitySocialClient.newFeedRepository()
        .getPostsByHashtag('#flutter')
        .getPagingData()
        .listen((posts) {
      for (final post in posts) {
        print('Post: \${post.postId}');
      }
    });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

function getHashtagFeed(hashtag: string) {
  const posts = []

  const unsubscribe = PostRepository.searchPostsByHashtag(
    { hashtags: [hashtag] },
    (response) => {
      if (response.error) throw response.error
      if (!response.loading) {
        posts.length = 0
        posts.push(...response.data)
        console.log(`Posts with #${hashtag}:`, posts)
      }
    },
  )

  return unsubscribe
}
```

</CodeGroup>
