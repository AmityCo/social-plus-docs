{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-review-approve | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun approvePost(
        postRepository: AmityPostRepository,
        post: AmityPost
    ) {
        postRepository.approvePost(postId = post.getPostId())
            .doOnComplete {
                // Void
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let client = AmityClient.shared
        let postRepository = AmityPostRepository(client: client)
        postRepository.approvePost(withId: "<postId>") { success, error in
            if let error = error { print("Error: \(error)"); return }
            print("Post approved: \(success)")
        }
```

```dart Flutter
late PagingController<AmityPost> _controller;

  void approvePost(String postId) {
    AmitySocialClient.newPostRepository()
        .reviewPost(postId: postId)
        .approve()
        .then((value) {
          // post approved - optionally remove from pending list
          _controller.removeWhere((element) => element.postId == postId);
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function postReviewApprove() {
  const { PostRepository } = Amity;
  await PostRepository.approvePost('postId');
  console.log('Post approved successfully');
}
```

</CodeGroup>
