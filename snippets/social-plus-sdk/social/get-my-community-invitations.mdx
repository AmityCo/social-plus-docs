{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-my-community-invitations | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getMyCommunityInvitations() {
        AmitySocialClient.newCommunityRepository()
            .getMyInvitations()
            .doOnNext { pagedList ->
                // Handle paged list of my invitations
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
var token: AmityNotificationToken?
        let repository = AmityInvitationRepository(client: client)
        token = repository.getMyCommunityInvitations().observe({ collection, change, error in
            // Observe changes in the collection
        })
```

```dart Flutter
void getMyCommunityInvitations() {
    final liveCollection = AmitySocialClient.newCommunityRepository()
        .getMyInvitations()
        .getPagingData();
    liveCollection.asStream().listen((List<AmityCommunityInvitation> invitations) {
      print('My community invitations: \${invitations.length}');
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function getMyCommunityInvitations() {
  const liveCollection = CommunityRepository.getMyInvitations();
  liveCollection.on('dataUpdated', (invitations) => {
    console.log('Pending invitations count:', invitations.length);
    invitations.forEach((inv: any) => console.log('communityId:', inv.communityId));
  });
}
```

</CodeGroup>
