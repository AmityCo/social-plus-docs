{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: verify-url | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun verifyUrl() {
        // Validate URLs against the moderation allowlist
        AmitySocialClient.newContentModerationRepository()
            .validateUrls(urls = listOf("https://example.com"))
            .subscribe { result -> }
    }
```

```swift iOS
let client = AmityClient.shared
        let contentValidator = AmityContentValidator(client: client)
        contentValidator.validateURL(URL(string: "https://example.com")!) { isValid, error in
            print("URL validation result: \(isValid)")
        }
```

```dart Flutter
void verifyUrl( List<String> urls ){
    AmityCoreClient().validateUrls(urls).then((value) {
      // Passes validation
    }).then((value){
      // Fails validation
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function verifyUrl() {
  const url = 'https://example.com';
  const isValid = await ContentModerationRepository.verifyUrl({ url });
  console.log('URL verification result:', isValid);
}
```

</CodeGroup>
