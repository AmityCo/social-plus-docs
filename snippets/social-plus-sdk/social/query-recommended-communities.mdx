{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: query-recommended-communities | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun queryRecommendedCommunities() {
        // Query recommended communities for discovery
        AmitySocialClient.newCommunityRepository()
            .getRecommendedCommunities()
            .build()
            .query()
            .asLiveData()
    }
```

```swift iOS
var token: AmityNotificationToken?
    
    func queryCommunitiesExample() {
        token = communityRepository.getRecommendedCommunities().observe { collection, change, error in
            for community in collection.allObjects() {
                // For example, to handle each community in the list.
            }
        }
    }
```

```dart Flutter
void getRecommendedCommunities() {
    AmitySocialClient.newCommunityRepository()
        .getRecommendedCommunities()
        .then((List<AmityCommunity> communities) {
          // handle result - returns up to 5 recommended communities
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function queryRecommendedCommunities() {
  const { CommunityRepository } = Amity;
  const { data: communities } = await CommunityRepository.getRecommendedCommunities();
  console.log('Recommended communities:', communities.map((c: any) => c.displayName));
}
```

</CodeGroup>
