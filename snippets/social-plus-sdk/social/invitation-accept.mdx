{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: invitation-accept | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun acceptInvitation(invitationId: String) {
        AmitySocialClient.newCommunityRepository()
            .invitation()
            .accept(invitationId = invitationId)
            .doOnComplete {
                // invitation accepted
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository()
        communityRepository.acceptInvitation(invitationId: "invitationId") { error in
            if let error = error {
                print("Error accepting invitation: \(error)")
            } else {
                print("Invitation accepted")
            }
        }
```

```dart Flutter
void invitationAccept() async {
    final communityRepo = AmitySocialClient.newCommunityRepository();
    await communityRepo.acceptInvitation(invitationId: 'invitationId123');
    print('Invitation accepted successfully.');
  }
```

```typescript TypeScript
import { InvitationRepository } from '@amityco/ts-sdk'

async function acceptInvitation() {
  await InvitationRepository.acceptInvitation('invitationId')
}
```

</CodeGroup>
