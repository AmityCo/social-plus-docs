{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: comment_image_creation_with_mention | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun commentImageCreationWithMention() {
        val mentionedUsers = listOf(
            com.amityco.socialcloud.sdk.helper.core.mention.AmityMentionMetadata.USER(
                userId = "userId1",
                index = 0,
                length = 5
            )
        )
        AmitySocialClient.newCommentRepository()
            .createComment()
            .post("postId")
            .with()
            .text("Hello @user1")
            .metadata(
                com.amityco.socialcloud.sdk.helper.core.mention.AmityMentionMetadataCreator(mentionedUsers).create()
            )
            .mentionUsers(listOf("userId1"))
            .build()
            .send()
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(AndroidSchedulers.mainThread())
            .subscribe({ comment -> }, { error -> error.printStackTrace() })
    }
```

```swift iOS
let commentRepository = AmityCommentRepository(client: AmityClient.shared)
        let imageData = AmityImageData()
        imageData.setImage(UIImage(named: "sampleImage")!)
        let builder = AmityImageCommentBuilder()
        builder.setImages([imageData])
        builder.setText("Check this out @jane!")
        let mentionees = [AmityMentioneesBuilder()]
        mentionees[0].setUser(userId: "janeUserId")
        builder.setMentionees(mentionees.map { $0.build() })
        commentRepository.createComment(
            referenceId: "postId",
            referenceType: .post,
            parentId: nil,
            builder: builder
        ) { comment, error in
            if let comment = comment {
                print("Image comment created: \(comment.commentId)")
            }
        }
```

```dart Flutter
void commentImageCreationWithMention() {
    // Create an image comment that mentions a user
    AmitySocialClient.newCommentRepository()
        .createComment()
        .post('postId')
        .create()
        .image(imageId: 'uploadedImageId')
        .mentionUsers(userIds: ['userId'])
        .send()
        .then((comment) => print('Image comment with mention: \${comment.commentId}'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function commentImageCreationWithMention() {
  const comment = await CommentRepository.createComment({
    referenceId: 'post-id-here',
    referenceType: 'post',
    data: { text: '@user123 check out this image!' },
    attachments: [{ type: 'image', fileId: 'image-file-id' }],
    mentionees: [{ type: 'user', userIds: ['user123'] }],
    metadata: {},
  });
  console.log('Image comment with mention created:', comment.commentId);
}
```

</CodeGroup>
