{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-latest-comment | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getLatestComment(postId: String) {
        AmitySocialClient.newCommentRepository()
            .getComments()
            .post(postId = postId)
            .build()
            .query()
            .doOnNext { comments ->
                // PagingData<AmityComment>
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
// Get the latest comment on a post
        let commentRepository = AmityCommentRepository(client: AmityManager.shared.client!)
        var token: AmityNotificationToken?
        let collection = commentRepository.getComments(
            referenceId: "postId",
            referenceType: .post,
            filterByParentId: false,
            parentId: nil,
            orderBy: .descending,
            includeDeleted: false
        )
        token = collection.observe { liveCollection, _, error in
            if let error = error {
                print("Failed to get latest comment: \(error.localizedDescription)")
                return
            }
            if let latestComment = liveCollection.object(at: 0) {
                print("Latest comment: \(latestComment.data?["text"] as? String ?? "")")
            }
        }
```

```dart Flutter
void getLatestComment() {
    final liveCollection = AmitySocialClient.newCommentRepository()
        .getComments(
          referenceId: 'postId',
          referenceType: AmityCommentReferenceType.POST,
        )
        .sortBy(AmityCommentSortOption.LAST_CREATED)
        .getPagingData();
    liveCollection.asStream().listen((List<AmityComment> comments) {
      if (comments.isNotEmpty) {
        print('Latest comment: \${comments.first.data}');
      }
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function getLatestComment() {
  const liveObject = CommentRepository.getLatestComment({ referenceId: 'postId123', referenceType: 'post' });
  liveObject.on('dataUpdated', (comment) => {
    console.log('Latest comment text:', comment.data?.text);
  });
}
```

</CodeGroup>
