{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-video-upload | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun uploadVideo(videoUri: Uri) {
        AmityCoreClient.newFileRepository()
            .uploadVideo(uri = videoUri, contentFeedType = AmityContentFeedType.POST)
            .doOnNext { uploadResult: AmityUploadResult<AmityVideo> ->
                when (uploadResult) {
                    is AmityUploadResult.PROGRESS -> {
                        // upload progress event
                        val progress = uploadResult.getUploadInfo().getProgressPercentage()
                    }
                    is AmityUploadResult.COMPLETE -> {
                        // completion event
                        val image: AmityVideo = uploadResult.getFile()
                    }
                    is AmityUploadResult.ERROR -> {
                        // error event
                        val error = AmityError.from(uploadResult.getError())
                    }
                    is AmityUploadResult.CANCELLED -> {
                        // cancelled upload event
                    }
                }
            }
            .subscribe()
    }
```

```dart Flutter
void postVideoUpload() async {
    final fileRepository = AmityFileRepository();
    final uploadedVideo = await fileRepository
        .uploadVideo('path/to/video.mp4');
    print('Uploaded video fileId: \${uploadedVideo.fileId}');
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function createVideoPost() {
  const postData = {
    targetId: 'communityId',
    targetType: 'community' as const,
    data: { text: 'Check out this video!' },
    attachments: [{ type: 'video' as const, fileId: 'uploadedVideoFileId' }],
  };

  const livePost = PostRepository.createPost(postData);
  livePost.on('dataUpdated', post => console.log('Video post created:', post.postId));
  livePost.on('dataError', err => console.error('Error creating video post:', err));
}
```

</CodeGroup>
