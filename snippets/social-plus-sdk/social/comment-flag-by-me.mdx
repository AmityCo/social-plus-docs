{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: comment-flag-by-me | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun commentFlagByMe() {
        val commentId = "commentId"
        AmitySocialClient.newCommentRepository()
            .getComment(commentId)
            .subscribe { comment ->
                val isFlaggedByMe = comment.isFlaggedByMe()
            }
    }
```

```swift iOS
let commentFlagRepository = AmityCommentFlagRepository(commentId: "commentId")
        commentFlagRepository.isFlaggedByMe { isFlagged, error in
            if let error = error {
                print("Error checking flag status: \(error)")
            } else {
                print("Comment flagged by me: \(isFlagged)")
            }
        }
```

```dart Flutter
void commentFlagByMe() {
    // Check whether the current user has flagged a comment
    AmitySocialClient.newCommentRepository()
        .getComment('commentId')
        .then((comment) {
      final isFlagged = comment.isFlaggedByMe ?? false;
      print('Flagged by me: \$isFlagged');
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function commentFlagByMe() {
  const commentId = 'comment-id-here';
  const liveObject = CommentRepository.getComment(commentId);
  liveObject.on('dataUpdated', (comment) => {
    console.log('Flagged by me:', comment.isFlaggedByMe);
  });
}
```

</CodeGroup>
