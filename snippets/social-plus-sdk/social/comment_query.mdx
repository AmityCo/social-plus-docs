{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: comment_query | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun commentQuery() {
        AmitySocialClient.newCommentRepository()
            .getComments()
            .post("postId")
            .includeDeleted(false)
            .build()
            .query()
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(AndroidSchedulers.mainThread())
            .subscribe({ pagedList ->
                // update UI
            }, { error ->
                error.printStackTrace()
            })
    }
```

```swift iOS
let commentRepository = AmityCommentRepository(client: AmityClient.shared)
        let collection = commentRepository.getComments(
            referenceId: "postId",
            referenceType: .post,
            filterByParentId: false,
            parentId: nil,
            orderBy: .descending,
            includeDeleted: false
        )
        collection.observe { liveCollection, _, error in
            for i in 0..<liveCollection.count() {
                if let comment = liveCollection.object(at: i) {
                    print("Comment: \(comment.commentId)")
                }
            }
        }
```

```dart Flutter
void commentQuery() {
    // Query comments for a post using a paging controller
    final controller = PagingController<AmityComment>(
      pageFuture: (token) => AmitySocialClient.newCommentRepository()
          .getComments()
          .post('postId')
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    );
    controller.addListener(() {
      final comments = controller.itemList ?? [];
      print('Comments loaded: \${comments.length}');
    });
    controller.fetchNextPage();
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function commentQuery() {
  const liveCollection = CommentRepository.getComments({
    referenceId: 'post-id-here',
    referenceType: 'post',
    orderBy: 'lastCreated',
    includeDeleted: false,
  });
  liveCollection.on('dataUpdated', (comments) => {
    console.log('Queried comments:', comments.length);
  });
}
```

</CodeGroup>
