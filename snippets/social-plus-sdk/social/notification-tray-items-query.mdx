{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: notification-tray-items-query | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun queryNotificationTrayItems() {
        AmityCoreClient
            .notificationTray()
            .getNotificationTrayItems()
            .doOnNext { queryPagingData ->
                // Observe the changes here.
            }
            .subscribe()
    }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

function queryNotificationTrayItems() {
  const liveCollection = UserRepository.queryNotificationTrayItems({})
  liveCollection.on('dataUpdated', (items) => {
    console.log('Notification tray items:', items)
  })
  return liveCollection
}
```

</CodeGroup>
