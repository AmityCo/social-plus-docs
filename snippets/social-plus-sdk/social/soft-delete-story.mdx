{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: soft-delete-story | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun softDeleteStory(
        storyRepository: AmityStoryRepository,
        story: AmityStory
    ) {
        storyRepository.softDeleteStory(storyId = story.getStoryId())
            .doOnComplete {
                // Complete
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let storyRepository = AmityStoryRepository(client: AmityManager.shared.client!)
        storyRepository.softDeleteStory(withId: "storyId") { success, error in
            print("Story soft-deleted: \(success)")
        }
```

```dart Flutter
void softDeleteStory() {
    AmityCoreClient.newSocialRepository()
        .story
        .softDeleteStory(storyId: 'storyId123')
        .then((_) => print('Story soft-deleted'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function softDeleteStory() {
  import { StoryRepository } from '@amityco/ts-sdk';

  await StoryRepository.softDeleteStory('story-id-123');
  console.log('Story soft deleted');
}
```

</CodeGroup>
