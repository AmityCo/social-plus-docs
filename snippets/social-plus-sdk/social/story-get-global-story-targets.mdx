{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: story-get-global-story-targets | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun storyGetGlobalStoryTargets() {
        // TODO: Implement Story get global story targets
    }
```

```swift iOS
let storyRepository = AmityStoryRepository(client: AmityManager.shared.client!)
        let collection = storyRepository.getGlobalStoryTargets(orderBy: .lastCreated)
        collection.observe { liveCollection, _, error in
            guard error == nil else { return }
            let targets = liveCollection.allObjects()
            print("Global story targets: \(targets.count)")
        }
```

```dart Flutter
void storyGetGlobalStoryTargets() {
    AmityCoreClient.newSocialRepository()
        .story
        .getGlobalStoryTargets(
          seenState: AmityStorySeenState.unseen,
        )
        .listen((targets) {
          print('Global story targets: \${targets.length}');
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function storyGetGlobalStoryTargets() {
  import { StoryRepository } from '@amityco/ts-sdk';

  const unsubscribe = StoryRepository.getGlobalStoryTargets(
    { limit: 20 },
    ({ data: targets, loading, error, hasNextPage, onNextPage }) => {
      if (!loading && targets) {
        console.log('Global story targets:', targets);
      }
    },
  );
  return unsubscribe;
}
```

</CodeGroup>
