{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: story-get-global-story-targets | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getGlobalStoryTargets(queryOption: AmityGlobalStoryTargetsQueryOption) {
        AmitySocialClient.newStoryRepository()
            .getGlobalStoryTargets(queryOption = queryOption)
            .doOnNext { pagingData: PagingData<AmityStoryTarget> ->
                // submit pagingData to PagingDataAdapter
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
collection = storyRepository.getGlobalStoryTargets(option: .unseen)
        collection?.observe { liveCollection, _, error in
            if let error = error {
                // Handle error here
                print("Error: \(error)")
                return
            }
            for i in 0..<liveCollection.count() {
                if let target = liveCollection.object(at: i) {
                    print("Global story target: \(target.targetId)")
                }
            }
        }
```

</CodeGroup>
