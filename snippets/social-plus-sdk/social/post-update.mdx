{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-update | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun updatePost(postId: String, text: String) {
        AmitySocialClient.newPostRepository()
            .updatePost(postId = postId)
            .text(text = text)
            .build()
            .update()
            .doOnSuccess { post: AmityPost ->
                // handle updated post
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let postRepository = AmityPostRepository(client: client)
        let builder = AmityTextPostBuilder()
        builder.setText("Updated post text")
        do {
            let post = try await postRepository.editPost(withId: "<post-id>", builder: builder)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void updatePost(AmityPost post) {
    post
        .edit()
        .text('updated post content')
        .metadata({'key': 'value'}) //for extra metadata
        .build()
        .update()
        .then((value) {
          //success
        })
        .onError((error, stackTrace) {
          //handle error
        });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function updatePost() {
  const { data: post } = await PostRepository.updatePost('postId', {
    data: { text: 'Updated post text' },
  })
  console.log(post)
}
```

</CodeGroup>
