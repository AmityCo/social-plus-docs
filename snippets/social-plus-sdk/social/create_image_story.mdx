{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create_image_story | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createImageStory() {
        // Create a image story in a community target
        AmitySocialClient.newStoryRepository()
            .createStory()
            .targetCommunity(communityId = "communityId")
            .image(uri = android.net.Uri.EMPTY)
            .build()
            .subscribe()
    }
```

```swift iOS
// Video create options
        
        var items: [AmityStoryItem] = []
    
        // If you want the story to contain some items
        items.append(AmityHyperLinkItem(url: "someURL", customText: "someCustomText"))
        
        // The default for image display mode is `fit`
        let options = AmityImageStoryCreateOptions(targetType: .community, tartgetId: "targetId", imageFileURL: URL(string: "pathToYourURL")!, metadata: nil, items: items, imageDisplayMode: .fill)
        // Create a image story
        do {
            let post = try await storyRepository.createImageStory(options: options)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void createImageStory() {
    final storyRepo = AmitySocialClient.newStoryRepository();
    storyRepo
        .createImageStory(
          targetId: 'communityId',
          targetType: AmityStoryTargetType.COMMUNITY,
          imageFile: AmityStoryImageFile(filePath: '/path/to/image.jpg'),
          storyItems: [],
        )
        .then((AmityStory story) {
          final storyId = story.storyId;
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }
```

```typescript TypeScript
import { StoryRepository } from '@amityco/ts-sdk'

async function createImageStory() {
  const story = await StoryRepository.createStory({
    targetType: 'community' as const,
    targetId: 'communityId',
    imageData: { fileId: 'uploadedImageFileId' },
    storyItems: [],
  })
  console.log('Image story created:', story)
}
```

</CodeGroup>
