{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-get-current-user-roles | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun communityGetCurrentUserRoles(communityId: String) {
        AmitySocialClient.newCommunityRepository()
            .membership(communityId = communityId)
            .getMember(userId = AmityCoreClient.getUserId())
            .subscribe(
                { member -> println("Roles: ${member.getRoles()}") },
                { error -> println("Error: ${error.message}") }
            )
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository(client: AmityClient.shared)
        let token = communityRepository.getMember(
            withUserId: AmityClient.shared.currentUserId ?? "",
            communityId: "communityId"
        ).observe { liveObject, error in
            guard let member = liveObject.object else { return }
            print("Current user roles: \(member.roles)")
        }
```

```dart Flutter
void getCurrentUserRole(String communityId) async {
    List<String>? currentUserRoles =
        await AmitySocialClient.newCommunityRepository()
            .getCurrentUserRoles(communityId);
    if (currentUserRoles == null) {
      // Not a memeber
    } else {
      if (currentUserRoles.isEmpty) {
        // just a Member
      } else {
        // Have Roles (eg. Mod)
      }
    }
  }
```

</CodeGroup>
