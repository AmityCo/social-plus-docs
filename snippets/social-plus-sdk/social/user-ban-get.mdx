{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-ban-get | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void userBanGet() {
    AmitySocialClient.newUserRepository()
        .isBanned('userId')
        .then((bool isBanned) {
          print('User is banned: \$isBanned');
        })
        .onError((error, stackTrace) {
          print('Error: \$error');
        });
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function getUserBanStatus() {
  const unsubscribe = UserRepository.getUser(
    'userId',
    (result) => {
      if (result.data) {
        console.log('User ban status:', result.data.isBanned);
      }
    },
  );
  return unsubscribe;
}
```

</CodeGroup>
