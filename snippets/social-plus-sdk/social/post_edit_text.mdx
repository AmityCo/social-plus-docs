{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post_edit_text | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void postEditText() async {
    final postRepository = AmitySocialClient.newPostRepository();
    await postRepository
        .editPost('postId')
        .text('Updated post text content')
        .apply();
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function editTextPost() {
  const updateData = {
    postId: 'textPostId123',
    data: { text: 'Updated post content with new information.' },
  };

  const livePost = PostRepository.updatePost(updateData);
  livePost.on('dataUpdated', post => console.log('Text post updated:', post.postId));
  livePost.on('dataError', err => console.error('Error updating text post:', err));
}
```

</CodeGroup>
