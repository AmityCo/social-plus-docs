{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-invitation-status | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getInvitation(community: AmityCommunity) {
        community.getInvitation()
            .doOnSuccess { invitations ->
                invitations.map { invitation ->
                    val status = invitation.getStatus()
                    when(status){
                        AmityInvitationStatus.PENDING -> {
                            // Handle pending invitation
                        }
                        AmityInvitationStatus.APPROVED -> {
                            // Handle approved invitation
                        }
                        AmityInvitationStatus.REJECTED -> {
                            // Handle rejected invitation
                        }
                        AmityInvitationStatus.CANCELLED -> {
                            // Handle cancelled invitation
                        }
                    }
                }
            }
            .doOnError { error ->
                // Handle error
            }
            .subscribe()
    }
```

```dart Flutter
void checkInvitationStatus(AmityCommunityInvitation invitation) {
    final status = invitation.status;
    switch (status) {
      case AmityCommunityInvitationStatus.pending:
        // Invitation is still pending
        break;
      case AmityCommunityInvitationStatus.approved:
        // Invitation was accepted
        break;
      case AmityCommunityInvitationStatus.rejected:
        // Invitation was rejected
        break;
      case AmityCommunityInvitationStatus.cancelled:
        // Invitation was cancelled
        break;
      default:
        break;
    }
  }
```

</CodeGroup>
