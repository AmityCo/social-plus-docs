{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: role-remove | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun roleRemove() {
        // TODO: Implement Role remove
    }
```

```swift iOS
let moderation = AmityCommunityModeration(client: AmityManager.shared.client!, andCommunityId: "communityId")
        moderation.removeRole("moderator", userIds: ["userId"]) { success, error in
            if success {
                print("Role removed from member")
            } else if let error = error {
                print("Error removing role: \(error)")
            }
        }
```

```dart Flutter
void roleRemove() {
    AmityCoreClient.newCommunityRepository()
        .moderation(communityId: 'communityId123')
        .removeRole(userId: 'userId123', roleName: 'moderator')
        .then((_) => print('Role removed successfully'))
        .onError((error, _) => print('Error removing role: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function roleRemove() {
  await CommunityRepository.removeRoles({
    communityId: 'communityId123',
    userIds: ['userId123'],
    roles: ['moderator'],
  });
  console.log('Moderator role removed from user');
}
```

</CodeGroup>
