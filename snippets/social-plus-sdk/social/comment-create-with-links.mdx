{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: comment-create-with-links | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createCommentWithLinks(
        commentRepository: AmityCommentRepository,
        postId: String
    ) {
        val links = listOf(
            AmityLink(
                index = 0,
                length = 18,
                url = "https://amity.co",
                renderPreview = false,
                domain = null,
                title = null,
                imageUrl = null,
            )
        )

        commentRepository.createComment()
            .post(postId = postId)
            .with()
            .text(text = "Check out https://amity.co")
            .links(links = links)
            .build()
            .send()
            .doOnSuccess { comment: AmityComment ->
                val commentLinks = comment.getLinks()
                // list of AmityLink or null for legacy comments
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let commentRepository = AmityCommentRepository(client: AmityClient.shared)
        let builder = AmityCommentCreateDataBuilder()
        builder.setText("Check this out: https://example.com")
        commentRepository.createComment(
            withReferenceId: "postId",
            referenceType: .post,
            parentId: nil,
            builder: builder
        ) { comment, error in
            if let error = error {
                print("Failed to create comment with links: \(error)")
                return
            }
            print("Comment with links created: \(comment?.commentId ?? "")")
        }
```

```dart Flutter
void createCommentWithLinks(String postId) {
    final commentText = 'Check out https://amity.co for more info!';
    final links = [
      AmityLinkPreview(
        url: 'https://amity.co',
        title: 'Amity',
        description: 'Amity Social Cloud',
      ),
    ];

    AmitySocialClient.newCommentRepository()
        .createComment()
        .post(postId: postId)
        .with_()
        .text(text: commentText)
        .links(links: links)
        .build()
        .send()
        .then((AmityComment comment) {
          // Comment with links created successfully
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }
```

</CodeGroup>
