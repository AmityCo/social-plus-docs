{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_member_list | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getMemberList() {
        AmitySocialClient.newCommunityRepository()
            .membership("communityId")
            .getMembers()
            .build()
            .query()
            .subscribe()
    }
```

```swift iOS
func getMemberList() {
        let communityRepository = AmityCommunityRepository(client: AmityManager.shared.client!)
        let liveCollection = communityRepository.getMembers(communityId: "communityId", filter: .all, roles: [], sortBy: .firstCreated)
        liveCollection.observe { collection, _, error in
            if let error = error { print("Error: \(error)"); return }
            print("Member count: \(collection.count())")
        }
    }
```

```dart Flutter
void getMemberList() {
    AmitySocialClient.newCommunityRepository()
        .moderation('communityId')
        .getMembers()
        .getPagingData()
        .listen((members) {
      for (final member in members) {
        print('Member: \${member.userId}');
      }
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function getMemberList() {
  const liveCollection = CommunityRepository.getMembers('communityId123', { limit: 20, sortBy: 'displayName' });
  liveCollection.on('dataUpdated', (members) => {
    console.log('Member list:', members.map((m: any) => m.displayName));
    if (liveCollection.hasNextPage) liveCollection.nextPage();
  });
}
```

</CodeGroup>
