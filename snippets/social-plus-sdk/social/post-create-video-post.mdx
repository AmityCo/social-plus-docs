{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-create-video-post | updated: 2026-05-01 */}

<CodeGroup>

```kotlin Android
fun createVideoPost(targetCommunityId: String, videos: List<AmityVideo>, text: String = "") {
        val target = AmityPost.Target.COMMUNITY.create(communityId = targetCommunityId)
        AmitySocialClient.newPostRepository()
            .createVideoPost(target = target, videos = videos, text = text)
            .doOnSuccess { post: AmityPost ->
                // AmityPost
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let postRepository = AmityPostRepository(client: client)
        let builder = AmityVideoPostBuilder()
        do {
            let videoData = AmityVideoData()
            builder.setVideos([videoData])
            builder.setText("Watch this video!")
            let post = try await postRepository.createVideoPost(builder, targetId: "<community-id>", targetType: .community, metadata: nil, mentionees: nil)
            print("Created video post: \(post.postId)")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void createVideoPost(
      String targetId, AmityPostTargetType targetType, List<File> videoFiles) async {
    final uploadedVideos = <AmityVideo>[];

    for (final videoFile in videoFiles) {
      await AmityCoreClient.newFileRepository()
          .uploadVideo(videoFile)
          .stream
          .firstWhere((result) => result is AmityUploadComplete)
          .then((result) {
        result.when(
          complete: (video) => uploadedVideos.add(video),
          progress: (_, __) {},
          error: (_) {},
          cancel: () {},
        );
      });
    }

    AmitySocialClient.newPostRepository()
        .createPost()
        .targetType(targetType)
        .targetId(targetId)
        .video(uploadedVideos)
        .post()
        .then((AmityPost post) => {
              //handle result
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function createVideoPost() {
  const { data: post } = await PostRepository.createPost({
    targetType: 'community',
    targetId: 'communityId',
    dataType: 'video',
    data: {},
    attachments: [{ type: 'video', fileId: 'uploadedVideoFileId' }],
  })
  console.log(post)
}
```

</CodeGroup>
