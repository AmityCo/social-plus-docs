{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_user_follow_info | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getUserFollowInfo() {
        AmitySocialClient.newUserRepository()
            .relationship()
            .user("userId")
            .getFollowInfo()
            .subscribe { followInfo ->
                // Follow info retrieved
            }
    }
```

```swift iOS
func getUserFollowInfo() {
        let userRepository = AmityUserRepository(client: AmityManager.shared.client!)
        userRepository.getFollowInfo(withUserId: "userId123") { followInfo, error in
            guard let info = followInfo else { return }
            print("Followers: \(info.followerCount), Following: \(info.followingCount)")
        }
    }
```

```dart Flutter
void getUserFollowInfo() {
    AmitySocialClient.newUserRepository()
        .relationship()
        .user('userId')
        .getFollowInfo()
        .asStream()
        .listen((followInfo) {
      print('Follower count: \${followInfo.followerCount}');
      print('Following count: \${followInfo.followingCount}');
    });
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function getUserFollowInfo() {
  const userId = 'user-id-123'
  const liveObject = UserRepository.getFollowInfo(userId)
  const unsubscribe = liveObject.subscribe(({ data: followInfo, loading, error }) => {
    if (error) { console.error(error); return }
    if (!loading && followInfo) {
      console.log('Follow status:', followInfo.status)
      console.log('Followers:', followInfo.followerCount)
      console.log('Following:', followInfo.followingCount)
    }
  })
  return unsubscribe
}
```

</CodeGroup>
