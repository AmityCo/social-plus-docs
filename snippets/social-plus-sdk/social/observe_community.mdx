{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: observe_community | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void observeCommunity() {
    AmitySocialClient.newCommunityRepository()
        .getCommunity('communityId')
        .asStream()
        .listen((community) {
      print('Community updated: \${community.displayName}');
    });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function observeCommunity(communityId: string) {
  const liveObject = CommunityRepository.getCommunity(communityId)
  liveObject.on('dataUpdated', (community) => {
    console.log('Community updated:', community)
  })
  liveObject.on('dataError', (error) => {
    console.error('Community error:', error)
  })
  return liveObject
}
```

</CodeGroup>
