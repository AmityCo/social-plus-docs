{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create-video-story | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createVideoStory(
        storyRepository: AmityStoryRepository,
        targetType: AmityStory.TargetType,
        targetId: String,
        videoUri: Uri,
        hyperlink: String,
        linkAlias: String
    ) {
        storyRepository.createVideoStory(
            targetType = targetType,
            targetId = targetId,
            videoUri = videoUri,
            storyItems = listOf(
                AmityStoryItem.HYPERLINK(
                    url = hyperlink,
                    customText = linkAlias
                )
            )
        )
            .doOnComplete {
                // Complete
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let storyRepository = AmityStoryRepository(client: AmityClient.shared)
        let builder = AmityVideoStoryBuilder()
        if let videoURL = Bundle.main.url(forResource: "story", withExtension: "mp4") {
            builder.setVideo(videoURL)
        }
        storyRepository.createStory(builder, targetId: "communityId", targetType: .community) { story, error in
            if let story = story {
                print("Video story created: \(story.storyId)")
            }
        }
```

```dart Flutter
void createVideoStory() {
    // Create a video story within a community
    AmitySocialClient.newStoryRepository()
        .createStory()
        .targetCommunity('communityId')
        .video(fileUri: Uri.file('/path/to/video.mp4'))
        .create()
        .then((story) => print('Video story created: \${story.storyId}'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function createVideoStory() {
  const client = createClient('YOUR_API_KEY')
  const videoFile = await client.fileRepository.uploadVideo({ file: videoBlob })
  await client.storyRepository.createStory({
    targetId: client.currentUserId!,
    targetType: 'user',
    dataType: 'video',
    data: { fileId: videoFile.fileId },
    metadata: {},
  })
  console.log('Video story created successfully')
}
```

</CodeGroup>
