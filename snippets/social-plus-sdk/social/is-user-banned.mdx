{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: is-user-banned | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun isUserBanned(communityId: String, userId: String) {
        AmitySocialClient.newCommunityRepository()
            .membership(communityId = communityId)
            .getMember(userId = userId)
            .doOnNext { member: AmityCommunityMember ->
                val isBanned = member.isBanned()
                // handle ban status
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let userRepository = AmityUserRepository(client: AmityUIKitManagerInternal.shared.client)
        let userId = "user123"
        let liveObject = userRepository.getUser(userId)
        liveObject.observe { liveObject, _ in
            if let user = liveObject.object {
                print("User is banned: \(user.isBanned)")
            }
        }
```

```dart Flutter
void checkIsUserBanned(String communityId, String userId) {
    AmitySocialClient.newCommunityRepository()
        .membership(communityId)
        .getMember(userId: userId)
        .then((AmityCommunityMember member) {
          final isBanned = member.isBanned;
          // use isBanned boolean
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function isUserBanned() {
  const communityId = 'community-id-123'
  const userId = 'user-id-123'
  const liveObject = CommunityRepository.getCommunityMember({ communityId, userId })
  const unsubscribe = liveObject.subscribe(({ data: member, loading }) => {
    if (!loading && member) {
      console.log('Is user banned:', member.isBanned)
    }
  })
  return unsubscribe
}
```

</CodeGroup>
