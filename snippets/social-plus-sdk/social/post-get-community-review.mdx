{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-get-community-review | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun postGetCommunityReview() {
        // Query posts as a live collection with real-time updates
        AmitySocialClient.newPostRepository()
            .getPosts()
            .targetMe()
            .build()
            .query()
            .subscribe({ pagingData ->
                // Handle paged post data
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
let feedRepository = AmityFeedRepository(client: AmityClient.shared)
        let communityId = "community_id"
        let liveCollection = feedRepository.getCommunityFeed(
            withCommunityId: communityId,
            sortBy: .lastCreated,
            includeDeleted: false,
            feedType: .reviewing
        )
        liveCollection.observe { collection, _, error in
            if let error = error {
                print("Failed to get posts under review: \(error.localizedDescription)")
                return
            }
            print("Posts under review: \(collection.count())")
        }
```

```dart Flutter
List<AmityPost> _posts = [];
  late PagingController<AmityPost> _controller;

  void getPostsUnderReview(String communityId) {
    _controller = PagingController(
      pageFuture: (token) => AmitySocialClient.newPostRepository()
          .getPosts()
          .targetCommunity(communityId: communityId)
          .reviewStatus(AmityReviewStatus.UNDER_REVIEW)
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    )..addListener(() {
        if (_controller.error == null) {
          _posts
            ..clear()
            ..addAll(_controller.loadedItems);
          // update UI
        } else {
          // handle error
        }
      });

    _controller.fetchNextPage();
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function postGetCommunityReview() {
  const liveCollection = PostRepository.getPosts({ targetId: 'communityId123', targetType: 'community', feedType: 'reviewing' });
  liveCollection.on('dataUpdated', (posts) => {
    console.log('Posts pending review:', posts);
  });
}
```

</CodeGroup>
