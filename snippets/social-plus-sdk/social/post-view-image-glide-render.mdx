{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-view-image-glide-render | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun renderPostImageByGlide(
        context: Context,
        imageView: ImageView,
        post: AmityPost
    ) {
        val data = post.getData()
        when (data) {
            is AmityPost.Data.IMAGE -> {
                val image = data.getImage()
                val imageUrl = image?.getUrl()
                Glide.with(context)
                    .load(imageUrl)
                    .into(imageView)
            }
            else -> {}
        }
    }
```

```dart Flutter
String? getPostImageUrl(AmityPost post) {
    final data = post.data;
    if (data is AmityImageData) {
      // Use the image URL with a Flutter Image widget or cached_network_image
      final imageUrl = data.getUrl(AmityImageSize.FULL);
      return imageUrl;
    }
    return null;
  }
```

</CodeGroup>
