{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-view-image-glide-render | updated: 2026-05-01 */}

<CodeGroup>

```kotlin Android
fun renderPostImageByGlide(
        context: Context,
        imageView: ImageView,
        post: AmityPost
    ) {
        val data = post.getData()
        when (data) {
            is AmityPost.Data.IMAGE -> {
                val image = data.getImage()
                val imageUrl = image?.getUrl()
                Glide.with(context)
                    .load(imageUrl)
                    .into(imageView)
            }
            else -> {}
        }
    }
```

</CodeGroup>
