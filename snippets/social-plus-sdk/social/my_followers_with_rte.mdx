{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: my_followers_with_rte | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun myFollowersWithRte() {
        // Follow/unfollow user operation
        AmityCoreClient.newUserRepository()
            .relationship()
            .getFollowers(userId = AmityCoreClient.getUserId())
            .build()
            .query()
            .subscribe { pagingData ->
                // observe followers with real-time events
            }
    }
```

```dart Flutter
void myFollowersWithRte() {
    AmityCoreClient.newUserRepository()
        .relationship()
        .getMyFollowers()
        .asStream()
        .listen(
          (followers) => print('Followers: \${followers.length}'),
          onError: (error) => print('Error: \$error'),
        );
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function myFollowersWithRte() {
  const unsubscribe = UserRepository.getFollowers(
    { userId: 'myUserId' },
    ({ data: followers, loading, error }) => {
      if (!loading && !error) {
        console.log('My followers:', followers.length)
        followers.forEach(f => console.log(f.userId, f.displayName))
      }
    },
  )
  
  return unsubscribe
}
```

</CodeGroup>
