{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: comment-report | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun commentReport() {
        val commentId = "commentId"
        AmitySocialClient.newCommentRepository()
            .flagComment(commentId)
            .subscribe()
    }
```

```swift iOS
let commentFlagRepository = AmityCommentFlagRepository(commentId: "commentId")
        commentFlagRepository.flagComment { error in
            if let error = error {
                print("Error reporting comment: \(error)")
            } else {
                print("Comment reported successfully")
            }
        }
```

```dart Flutter
void commentReport() {
    // Report (flag) a comment for moderation review
    AmitySocialClient.newCommentRepository()
        .flagComment('commentId')
        .then((_) => print('Comment reported'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function commentReport() {
  const commentId = 'comment-id-here';
  await CommentRepository.flagComment(commentId);
  console.log('Comment reported/flagged successfully');
}
```

</CodeGroup>
