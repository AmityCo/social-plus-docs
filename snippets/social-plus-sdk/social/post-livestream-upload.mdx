{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-livestream-upload | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createLiveStream(
        streamRepository: AmityStreamRepository,
        metadata: JsonObject,
        thumbnailImage: AmityImage,
    ) {
        val metadata = JsonObject()
        streamRepository.createStream(
                title = "some title",
                description = "some description",
                resolution = AmityBroadcastResolution.HD_720P,
                metadata = metadata,
                thumbnailImage = thumbnailImage
            )
            .doOnSuccess { stream: AmityStream ->
                // AmityStream
                val streamId = stream.getStreamId()
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let client = AmityClient.shared
        let streamRepository = AmityStreamRepository(client: client)
        // Upload/attach recorded stream
        streamRepository.createStream(title: "My Stream", thumbnailFileId: nil) { stream, error in
            if let error = error { print("Error: \(error)"); return }
            print("Stream created for upload: \(stream?.streamId ?? "")")
        }
```

```dart Flutter
void createLivestreamPost(String communityId) async {
    // Step 1: Create a stream
    final stream = await AmityVideoClient.newStreamRepository()
        .createStream(title: 'My Live Stream', description: 'Live broadcast');

    final streamId = stream.streamId;

    // Step 2: Create a post with the stream
    AmitySocialClient.newPostRepository()
        .createPost()
        .targetCommunity(communityId)
        .liveStream(streamId)
        .post()
        .then((AmityPost post) {
          // livestream post created, use post.postId
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function postLivestreamUpload() {
  const uploadResult = await FileRepository.uploadFile({ file: videoFile, fileName: 'stream-recording.mp4' });
  const post = await PostRepository.createPost({
    targetId: 'communityId123',
    targetType: 'community',
    data: { text: 'Stream recording' },
    dataType: 'text',
    attachments: [{ fileId: uploadResult.fileId, type: 'video' }],
  });
  console.log('Livestream upload post created:', post.postId);
}
```

</CodeGroup>
