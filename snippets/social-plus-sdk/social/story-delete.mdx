{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: story-delete | updated: 2026-05-01 */}

<CodeGroup>

```kotlin Android
fun deleteStory(storyId: String) {
        AmitySocialClient.newStoryRepository()
            .softDeleteStory(storyId = storyId)
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let storyRepository = AmityStoryRepository(client: client)
        do {
            let success = try await storyRepository.deleteStory(storyId: "<story-id>")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void deleteStory(String storyId) {
    AmitySocialClient.newStoryRepository()
        .hardDeleteStory(storyId: storyId)
        .then((value) {
          // story deleted
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

</CodeGroup>
