{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: member-management-add-role | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun memberManagementAddRole() {
        // Community member/role management
        AmitySocialClient.newCommunityRepository()
            .moderation(communityId = "communityId")
            .addRoles(roles = listOf("moderator"), userIds = listOf("userId"))
            .subscribe()
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository(client: AmityUIKitManagerInternal.shared.client)
        let communityId = "community123"
        let userId = "user123"
        let roles = ["moderator"]
        communityRepository.addRoles(roles, userId: userId, communityId: communityId) { success, error in
            print("Role added to member: \(success)")
        }
```

```dart Flutter
void memberManagementAddRole() async {
    await AmitySocialClient.newCommunityRepository()
        .moderation('communityId')
        .addRole('community-moderator', ['userId1', 'userId2']);
    print('Role added to members');
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function memberManagementAddRole(communityId: string, userId: string, role: string) {
  await CommunityRepository.addRoles({
    communityId,
    roles: [role],
    userIds: [userId],
  })
  console.log(`Role '${role}' added to user ${userId} in community ${communityId}`)
}
```

</CodeGroup>
