{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-my-communities | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getMyCommunities() {
        AmitySocialClient.newCommunityRepository()
            .getCommunities()
            .filter(onlyMine = true)
            .sortBy(AmityCommunitySortOption.DISPLAY_NAME)
            .build()
            .query()
            .doOnNext { pagedList ->
                // Handle paged list of my communities
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository(client: AmityManager.shared.client!)
        let queryOptions = AmityCommunityQueryOptions()
        queryOptions.filter = .member
        let liveCollection = communityRepository.getCommunities(with: queryOptions)
        liveCollection.observe { collection, _, error in
            guard error == nil else { return }
            print("My communities: \(collection.count())")
        }
```

```dart Flutter
List<AmityCommunity> _communities = [];
  late PagingController<AmityCommunity> _controller;
  final scrollController = ScrollController();

  void getMyCommunities() {
    _controller = PagingController(
      pageFuture: (token) => AmitySocialClient.newCommunityRepository()
          .getCommunities()
          .filter(AmityCommunityFilter.MEMBER)
          .sortBy(AmityCommunitySortOption.LAST_CREATED)
          .includeDeleted(false)
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    )..addListener(() {
        if (_controller.error == null) {
          _communities
            ..clear()
            ..addAll(_controller.loadedItems);
          // update UI
        } else {
          // handle error
        }
      });

    _controller.fetchNextPage();

    scrollController.addListener(() {
      if (scrollController.position.pixels ==
              scrollController.position.maxScrollExtent &&
          _controller.hasMoreItems) {
        _controller.fetchNextPage();
      }
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function getMyCommunities() {
  const liveCollection = CommunityRepository.getMyCommunities({ membership: 'member' });
  liveCollection.on('dataUpdated', (communities) => {
    console.log('My communities:', communities.map((c: any) => c.displayName));
  });
}
```

</CodeGroup>
