{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: reject_join_request | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun rejectJoinRequest() {
        // perform reject join request operation
        AmitySocialClient.newCommunityRepository()
            .membership("communityId")
            .declineJoinRequest(listOf("userId"))
            .subscribe()
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository(client: AmityUIKitManagerInternal.shared.client)
        communityRepository.rejectJoinRequest(communityId: "communityId", userId: "userId") { success, error in
            if success {
                print("Join request rejected")
            } else if let error = error {
                print("Failed to reject join request: \(error.localizedDescription)")
            }
        }
```

```dart Flutter
void rejectJoinRequest() async {
    final communityRepository = AmitySocialClient.newCommunityRepository();
    await communityRepository
        .membership('communityId')
        .declineJoinRequest(userId: 'userId');
    print('Join request rejected');
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function rejectJoinRequest() {
  const rejectRequest = async (communityId: string, userId: string) => {
    await CommunityRepository.declineRequest({ communityId, userId });
    console.log(`Join request rejected for user ${userId} in community ${communityId}`);
  };

  await rejectRequest('communityId123', 'userId456');
}
```

</CodeGroup>
