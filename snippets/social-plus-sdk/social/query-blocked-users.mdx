{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: query-blocked-users | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun queryBlockedUsers() {
        // Query the list of blocked users
        AmityCoreClient.newUserRepository()
            .getBlockedUsers()
            .build()
            .query()
            .asLiveData()
    }
```

```swift iOS
let client = AmityClient.shared
        let userRepository = AmityUserRepository(client: client)
        let collection = userRepository.getBlockedUsers()
        collection.observe { liveCollection, _, error in
            if let error = error { print("Error: \(error)"); return }
            print("Blocked users count: \(liveCollection.count())")
        }
```

```dart Flutter
void queryBlockedUsers() async {
    final userRepository = AmitySocialClient.newUserRepository();
    final blockedUsers = await userRepository
        .getBlockedUsers()
        .getPagingData();
    print('Blocked users count: \${blockedUsers.data.length}');
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function queryBlockedUsers() {
  const liveCollection = UserRepository.getBlockedUsers({});
  liveCollection.on('dataUpdated', users => {
    console.log('Blocked users:', users.map(u => u.userId));
  });
  liveCollection.on('dataError', err => console.error('Error querying blocked users:', err));
}
```

</CodeGroup>
