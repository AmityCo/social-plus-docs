{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: unfollow_user | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun unfollowUser() {
        // TODO: Implement Unfollow user
    }
```

```swift iOS
let userRepository = AmityUserRepository(client: AmityManager.shared.client!)
        userRepository.unfollowUser("userId") { success, error in
            if success {
                print("User unfollowed successfully")
            } else if let error = error {
                print("Error unfollowing user: \(error)")
            }
        }
```

```dart Flutter
void unfollowUser() {
    AmitySocialClient.newUserRepository()
        .relationship()
        .unfollow('userId')
        .then((_) {
          print('User unfollowed successfully.');
        })
        .onError((error, stackTrace) {
          print('Error: \$error');
        });
  }
```

```typescript TypeScript
import { FollowRepository } from '@amityco/ts-sdk'

async function unfollowUser() {
  await FollowRepository.unfollow('userId');
  console.log('User unfollowed successfully');
}
```

</CodeGroup>
