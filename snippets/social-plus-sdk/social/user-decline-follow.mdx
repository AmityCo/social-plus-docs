{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-decline-follow | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun declineFollow(userId: String) {
        AmitySocialClient.newUserRepository()
            .relationship()
            .decline(userId = userId)
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let userRepository = AmityUserRepository(client: client)
        do {
            let success = try await userRepository.userRelationship.declineFollow(withUserId: "<user-id>")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void userDeclineFollow() {
    AmitySocialClient.newUserRepository()
        .relationship
        .declineMyFollower('userId')
        .then((_) => print('Follow request declined'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function userDeclineFollow() {
  const { data: followStatus } = await UserRepository.Relationships.declineFollowRequest({ userId: 'followerId' })
  console.log('Follow request declined:', followStatus)
}
```

</CodeGroup>
