{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: accept-request | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun acceptRequest(userId: String) {
        AmityCoreClient.newUserRepository()
            .relationship()
            .me()
            .accept(userId = userId)
            .doOnComplete { }
            .doOnError { }
            .subscribe()
    }
```

```swift iOS
userRepository.acceptMyFollower(withUserId: "userId") { success, error in
            if let error = error {
                print("Error accepting request: \(error)")
            } else {
                print("Follow request accepted: \(success)")
            }
        }
```

```dart Flutter
void acceptRequest() {
    AmitySocialClient.newUserRepository()
        .relationship
        .accept('userId');
  }
```

```typescript TypeScript
import { FollowRepository } from '@amityco/ts-sdk'

async function acceptFollowRequest(userId: string) {
  const followRepository = new FollowRepository()
  const result = await followRepository.acceptMyFollower(userId)
  return result
}
```

</CodeGroup>
