{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: comment-thread | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun commentThread() {
        val commentId = "commentId"
        AmitySocialClient.newCommentRepository()
            .getComment(commentId)
            .subscribe { comment ->
                val childCount = comment.getChildrenNumber()
                val text = (comment.getData() as? com.amityco.socialcloud.sdk.model.social.comment.AmityComment.Data.TEXT)?.getText()
            }
    }
```

```swift iOS
let commentRepository = AmityCommentRepository()
        let queryOptions = AmityCommentQueryOptions(
            referenceId: "postId",
            referenceType: .post,
            filterByParentId: false,
            parentId: nil,
            orderBy: .descending,
            includeDeleted: false
        )
        let liveCollection = commentRepository.getComments(with: queryOptions)
        liveCollection.observe { liveCollection, error in
            guard let comments = liveCollection.allObjects() else { return }
            print("Comments count: \(comments.count)")
            for comment in comments {
                print("Comment: \(comment.data?["text"] ?? ""), replies: \(comment.childrenNumber)")
            }
        }
```

```dart Flutter
void commentThread() {
    // Retrieve a comment thread (replies) for a specific comment
    final controller = PagingController<AmityComment>(
      pageFuture: (token) => AmitySocialClient.newCommentRepository()
          .getComments()
          .post('postId')
          .parentId('commentId')
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    );
    controller.addListener(() {
      final replies = controller.itemList ?? [];
      print('Thread replies: \${replies.length}');
    });
    controller.fetchNextPage();
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function commentThread() {
  const parentCommentId = 'parent-comment-id';
  const threadCollection = CommentRepository.getComments({
    referenceId: 'post-id-here',
    referenceType: 'post',
    parentId: parentCommentId,
  });
  threadCollection.on('dataUpdated', (replies) => {
    console.log('Thread replies:', replies.length);
  });
}
```

</CodeGroup>
