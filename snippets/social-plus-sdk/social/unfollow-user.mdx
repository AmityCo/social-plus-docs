{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: unfollow-user | updated: 2026-05-02 */}

<CodeGroup>

```swift iOS
let userRepository = AmityUserRepository()
        userRepository.unfollow(userId: "userId") { error in
            if let error = error {
                print("Error unfollowing user: \(error)")
            } else {
                print("User unfollowed successfully")
            }
        }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function unfollowUser(userId: string) {
  await UserRepository.unfollow(userId)
  console.log(`Unfollowed user ${userId}`)
}

// Example usage
unfollowUser('userId')
```

</CodeGroup>
