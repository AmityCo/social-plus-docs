{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-poll-upload | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createPoll(
        postRepository: AmityPostRepository,
        pollRepository: AmityPollRepository
    ) {
        pollRepository.createPoll(question = "What are your favorite songs") // a question
            .answers(
                answers = listOf(
                    AmityPollAnswer.Data.TEXT("First date"),
                    AmityPollAnswer.Data.TEXT("All of the small things"),
                    AmityPollAnswer.Data.TEXT("The rock show")
                )
            )
            .answerType(answerType = AmityPoll.AnswerType.MULTIPLE) // allow users to vote on multiple choces.
            .closedIn(
                closedIn = Minutes.minutes(60).toStandardDuration().millis // open for 60 minutes
            )
            .build()
            .create()
            .flatMap { pollId: String ->
                // To create a poll post, use pollId
                val userTarget = AmityPost.Target.USER.create(userId = "user1")
                postRepository.createPollPost(
                    target = userTarget,
                    pollId = pollId,
                    text = "new video post!"
                )
            }
            .subscribe()
    }
```

```swift iOS
let client = AmityClient.shared
        let fileRepository = AmityFileRepository(client: client)
        // Upload image to attach to poll post
        fileRepository.uploadImage(UIImage(named: "pollBanner")!, progress: nil) { imageData, error in
            if let error = error { print("Error: \(error)"); return }
            print("Uploaded image ID: \(imageData?.fileId ?? "")")
        }
```

```dart Flutter
void createPollPost(String communityId) async {
    // Step 1: Create the poll
    final poll = await AmitySocialClient.newPollRepository().createPoll(
      question: 'What are your favorite songs?',
      answers: ['First Date', 'Yellow', 'Bohemian Rhapsody'],
      answerType: AmityPollAnswerType.MULTIPLE,
      closedIn: 3600000, // 1 hour in milliseconds
    );

    // Step 2: Create the post with the poll
    AmitySocialClient.newPostRepository()
        .createPost()
        .targetCommunity(communityId)
        .poll(poll.pollId!)
        .post()
        .then((AmityPost post) {
          // poll post created
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function postPollUpload() {
  const { PostRepository } = Amity;
  const { data: post } = await PostRepository.createPost({
    targetId: 'communityId',
    targetType: 'community',
    dataType: 'poll',
    data: {
      question: 'Rate this feature?',
      answers: [{ data: { text: 'Great' } }, { data: { text: 'Good' } }, { data: { text: 'Okay' } }],
      closedIn: 3 * 24 * 60 * 60 * 1000,
      answerType: 'single',
    },
  });
  console.log('Poll uploaded and created:', post.postId);
}
```

</CodeGroup>
