{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: community-invitation-create | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun communityInvitationCreate() {
        // TODO: implement Community invitation create
    }
```

```swift iOS
let invitationRepository = AmityCommunityInvitationRepository(client: AmityClient.shared)
        invitationRepository.sendInvitation(
            toUserId: "userId",
            communityId: "communityId"
        ) { success, error in
            if success {
                print("Invitation sent")
            } else if let error = error {
                print("Send invitation failed: \(error)")
            }
        }
```

```dart Flutter
void communityInvitationCreate() {
    // Invite a user to join a community
    AmitySocialClient.newCommunityRepository()
        .createInvitation(
          communityId: 'communityId',
          userIds: ['userId1', 'userId2'],
        )
        .then((_) => print('Invitations sent'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function communityInvitationCreate() {
  const client = createClient('YOUR_API_KEY')
  await client.communityRepository.createInvitation({
    communityId: 'communityId123',
    userId: 'targetUserId',
  })
  console.log('Invitation sent')
}
```

</CodeGroup>
