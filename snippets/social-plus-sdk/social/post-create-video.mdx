{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-create-video | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createVideoPost(targetId: String, videoUri: Uri) {
        AmitySocialClient.newPostRepository()
            .createPost()
            .targetUser(userId = targetId)
            .video(videoUri = videoUri)
            .build()
            .post()
            .doOnSuccess { post: AmityPost ->
                // handle created post
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let postRepository = AmityPostRepository(client: client)
        let builder = AmityVideoPostBuilder()
        let videoURL = URL(string: "file:///path/to/video.mp4")!
        builder.setAttachments([videoURL])
        do {
            let post = try await postRepository.createVideoPost(builder, targetId: "<community-id>", targetType: .community, metadata: nil, mentionees: nil)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void createVideoPost(String communityId, File videoFile) {
    AmityCoreClient.newFileRepository()
        .uploadVideo(videoFile)
        .stream
        .listen((amityUploadResult) {
      amityUploadResult.when(
        progress: (uploadInfo, cancelToken) {},
        complete: (AmityVideo video) {
          AmitySocialClient.newPostRepository()
              .createPost()
              .targetCommunity(communityId)
              .video([video])
              .post()
              .then((AmityPost post) {
                // handle result
              })
              .onError((error, stackTrace) {
                // handle error
              });
        },
        error: (error) {
          // handle upload error
        },
        cancel: () {},
      );
    });
  }
```

</CodeGroup>
