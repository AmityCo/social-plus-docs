{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_my_follow | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getMyFollow() {
        AmitySocialClient.newUserRepository()
            .relationship()
            .user("userId")
            .getFollowers()
            .build()
            .query()
            .subscribe()
    }
```

```swift iOS
func getMyFollow() {
        let followManager = AmityFollowManager(client: AmityManager.shared.client!)
        let liveObject = followManager.getMyFollowInfo()
        liveObject.observe { followInfo, error in
            if let error = error { print("Error: \(error)"); return }
            print("My followers: \(followInfo?.object?.followerCount ?? 0)")
        }
    }
```

```dart Flutter
void getMyFollow() {
    // TODO: implement SDK operation
    throw UnimplementedError();
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function getMyFollow() {
  const client = Client.getActiveClient();
  const myUserId = client.currentUser?.userId ?? '';
  const followings = UserRepository.getFollowings(myUserId);
  followings.on('dataUpdated', (list) => {
    console.log('I am following:', list.length, 'users');
  });
}
```

</CodeGroup>
