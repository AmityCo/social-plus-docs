{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: flag-post | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun flagPost(postId: String) {
        AmitySocialClient.newPostRepository()
            .flagPost(postId = postId)
            .doOnComplete {
                // Post flagged successfully
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let postRepository = AmityPostRepository(client: AmityManager.shared.client!)
        postRepository.flagPost(withId: "postId") { _, error in
            if let error = error {
                print("Error flagging post: \(error)")
            } else {
                print("Post flagged successfully")
            }
        }
```

```dart Flutter
void flagPost(AmityPost post) {
    post.report()
        .flag()
        .then((_) {
          // post flagged
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

</CodeGroup>
