{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: unban_user | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun unbanUser() {
        AmitySocialClient.newCommunityRepository()
            .moderation("communityId")
            .unbanMembers(listOf("userId"))
            .subscribe({
                // User unbanned from community
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
let communityId = "communityId"
        let userId = "userId"
        AmitySDK.unbanCommunityMember(communityId: communityId, userId: userId) { success, error in
            if let error = error {
                print("Failed to unban user: \(error)")
                return
            }
            print("User unbanned successfully")
        }
```

```dart Flutter
void unbanUser() async {
    final communityRepo = AmitySocialClient.newCommunityRepository();
    await communityRepo
        .moderation('communityId')
        .unbanMembers(['userId']);
    print('User unbanned from community');
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function unbanUser(communityId: string, userId: string) {
  await CommunityRepository.Moderation.unbanMember(communityId, [userId])
  console.log('User unbanned from community')
}
```

</CodeGroup>
