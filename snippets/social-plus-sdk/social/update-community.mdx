{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: update-community | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun updateCommunity() {
        // TODO: Implement Update community
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository(client: AmityManager.shared.client!)
        let builder = AmityCommunityUpdateDataBuilder()
        builder.setDisplayName("Updated Community Name")
        builder.setCommunityDescription("Updated description")
        communityRepository.updateCommunity("communityId", builder: builder) { community, error in
            if let community = community {
                print("Community updated: \(community.displayName)")
            } else if let error = error {
                print("Error updating community: \(error)")
            }
        }
```

```dart Flutter
void updateCommunity() async {
    final communityRepo = AmitySocialClient.newCommunityRepository();
    await communityRepo
        .updateCommunity('communityId')
        .displayName('Updated Community Name')
        .description('New description')
        .update();
    print('Community updated');
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function updateCommunity(communityId: string, displayName: string, description: string) {
  const updated = await CommunityRepository.updateCommunity(communityId, {
    displayName,
    description,
  })
  console.log('Community updated:', updated.data.communityId)
}
```

</CodeGroup>
