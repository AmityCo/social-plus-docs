{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_latest_comment | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getLatestComment() {
        // Query the latest comment for a post
        AmitySocialClient.newCommentRepository()
            .getComments(referenceId = "postId", referenceType = AmityComment.ReferenceType.POST)
            .sortBy(AmityCommentSortOption.LAST_CREATED)
            .build()
            .query()
            .subscribe { comments -> /* handle latest comment */ }
    }
```

```swift iOS
var token: AmityNotificationToken?
    
    func getLatestCommentExample() async {
        do {
            let comment = try await commentRepository.getLatestComment(
                withReferenceId: "<post-id>",
                referenceType: .post,
                includeReplies: true
            )
        } catch {
            // Handle error here
        }
    }
```

```dart Flutter
void getLatestComment() {
    AmitySocialClient.newCommentRepository()
        .getComments()
        .post('postId')
        .includeDeleted(false)
        .getPagingData()
        .listen((comments) {
      if (comments.isNotEmpty) {
        print('Latest comment: \${comments.first.data}');
      }
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function getLatestComment() {
  const liveCollection = CommentRepository.getComments({ referenceId: 'postId123', referenceType: 'post', last: 1 });
  liveCollection.on('dataUpdated', (comments) => {
    const latest = comments[0];
    if (latest) console.log('Latest comment:', latest.data?.text);
  });
}
```

</CodeGroup>
