{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: block-user | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun blockUser(
        userRepository: AmityUserRepository,
        userId: String
    ) {
        userRepository
            .relationship()
            .blockUser(userId = userId)
            .doOnComplete {
                //  success
            }
            .doOnError {
                //  handle exception
            }
            .subscribe()
    }
```

```swift iOS
userRepository.blockUser(withUserId: "userId") { success, error in
            if let error = error {
                print("Error blocking user: \(error)")
            } else {
                print("User blocked: \(success)")
            }
        }
```

```dart Flutter
void blockUser(String userId) {
    AmityCoreClient.newUserRepository()
        .relationship()
        .blockUser(userId)
        .then((value) {
          // user blocked
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function blockUser(userId: string) {
  await UserRepository.blockUser(userId)
  console.log(`User ${userId} has been blocked`)
}

// Example usage
blockUser('userId')
```

</CodeGroup>
