{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-edit-text | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun postEditText() {
        // Edit an existing post's text content
        AmitySocialClient.newPostRepository()
            .editPost(postId = "post123")
            .text("Updated post content")
            .build()
            .subscribe({ post ->
                // Post updated successfully
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
let postRepository = AmityPostRepository(client: AmityClient.shared)
        let postId = "post_id"
        let builder = AmityTextPostBuilder()
        builder.setText("Updated post text content")
        postRepository.updatePost(withPostId: postId, builder: builder) { post, error in
            if let error = error {
                print("Failed to edit post: \(error.localizedDescription)")
                return
            }
            print("Post updated: \(post?.postId ?? "")")
        }
```

```dart Flutter
void postEditText() async {
    await AmitySocialClient.newPostRepository()
        .editPost('postId')
        .text('Updated post text content')
        .apply();
    print('Text post edited');
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function postEditText(postId: string, newText: string) {
  const post = await PostRepository.updatePost({
    postId,
    dataType: 'text',
    data: { text: newText },
  })
  console.log('Text post edited:', post.postId)
  return post
}
```

</CodeGroup>
