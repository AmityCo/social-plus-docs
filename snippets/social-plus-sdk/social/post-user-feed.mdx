{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-user-feed | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun postUserFeed() {
        AmitySocialClient.newPostRepository()
            .createPost()
            .targetMe()
            .text("My new post!")
            .build()
            .post()
            .subscribe()
    }
```

```swift iOS
let postRepository = AmityPostRepository()
        let builder = AmityTextPostBuilder()
        builder.setText("Sharing an update!")
        postRepository.createPost(
            builder,
            targetId: "myUserId",
            targetType: .user
        ) { post, error in
            if let post = post {
                print("Posted to user feed: \(post.postId)")
            } else if let error = error {
                print("Error posting to user feed: \(error)")
            }
        }
```

```dart Flutter
late PagingController<AmityPost> _userFeedController;

    void getUserFeedPosts(String userId) {
      _userFeedController = PagingController(
        pageFuture: (token) => AmitySocialClient.newPostRepository()
            .getPosts()
            .targetUser(userId)
            .feedType(AmityFeedType.published)
            .getPagingData(token: token, limit: 20),
        pageSize: 20,
      )..addListener(() {
          if (_userFeedController.error == null) {
            // Update UI with _userFeedController.loadedItems
          } else {
            // Handle error
          }
        });
    }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function postUserFeed() {
  const { FeedRepository } = Amity;
  const { data: posts, onNextPage, hasNextPage } = await FeedRepository.getUserFeed({
    userId: 'userId',
    sortBy: 'lastCreated',
  });
  console.log('User feed posts:', posts.map((p: any) => p.postId));
  if (hasNextPage) onNextPage?.();
}
```

</CodeGroup>
