{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-user-global-feed | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getUserGlobalFeed() {
        AmitySocialClient.newFeedRepository()
            .getGlobalFeed()
            .build()
            .getPagingData()
            .doOnNext { pagedList ->
                // Handle paged list of user global feed posts
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let feedRepository = AmityFeedRepository()
        let liveCollection = feedRepository.getGlobalFeed()
        liveCollection.observe { collection, _, error in
            print("Global feed posts count: \(collection.count())")
        }
```

```dart Flutter
final _posts = <AmityPost>[];
  late PagingController<AmityPost> _controller;
  final scrollController = ScrollController();

  void getUserGlobalFeed(String userId) {
    _controller = PagingController(
      pageFuture: (token) => AmitySocialClient.newFeedRepository()
          .getUserFeed(userId)
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    )..addListener(
        () {
          if (_controller.error == null) {
            _posts.clear();
            _posts.addAll(_controller.loadedItems);
            // update UI
          } else {
            // handle error
          }
        },
      );

    scrollController.addListener(() {
      if (scrollController.position.pixels ==
              scrollController.position.maxScrollExtent &&
          _controller.hasMoreItems) {
        _controller.fetchNextPage();
      }
    });
  }
```

</CodeGroup>
