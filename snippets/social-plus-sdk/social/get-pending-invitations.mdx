{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-pending-invitations | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getPendingInvitations() {
        // Query pending community invitations for the current user
        AmitySocialClient.newCommunityRepository()
            .getInvitations()
            .build()
            .query()
            .subscribe { invitations -> /* handle pending invitations */ }
    }
```

```swift iOS
// Get pending community invitations for the current user
        let communityRepository = AmityCommunityRepository(client: AmityManager.shared.client!)
        var token: AmityNotificationToken?
        let collection = communityRepository.getJoinRequests(communityId: "communityId", status: .pending)
        token = collection.observe { liveCollection, _, error in
            if let error = error {
                print("Failed to get pending invitations: \(error.localizedDescription)")
                return
            }
            print("Pending invitations count: \(liveCollection.count())")
        }
```

```dart Flutter
void getPendingInvitations() {
    AmitySocialClient.newCommunityRepository()
        .membership('communityId')
        .getPendingInvitations()
        .query();
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function getPendingInvitations() {
  const unsubscribe = CommunityRepository.getInvitations(
    { status: 'pending' },
    (invitations, error) => {
      if (error) return console.error(error)
      console.log('Pending invitations:', invitations)
      unsubscribe()
    }
  )
}
```

</CodeGroup>
