{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-member-query-live | updated: 2026-05-02 */}

<CodeGroup>

```swift iOS
let channelRepository = AmityChannelRepository(client: client)
        var token: AmityNotificationToken?
        token = channelRepository.getMembers(channelId: "<channel-id>", membership: .member, roles: [], sortBy: .displayName).observe { collection, change, error in
            guard error == nil else { return }
            print("Member count: \(collection.count())")
        }
```

```dart Flutter
StreamSubscription? subscription;

  void queryChannelMembers(String channelId) {
    subscription = AmityChatClient.newChannelRepository()
        .membership(channelId)
        .getMembers()
        .membershipFilter(AmityChannelMembershipFilter.member)
        .build()
        .query()
        .asStream()
        .listen((List<AmityChannelMember> members) {
          for (final member in members) {
            final userId = member.userId;
            final displayName = member.displayName;
          }
        })
        ..onError((error) {
          // Handle error
        });
  }

  void dispose() {
    subscription?.cancel();
  }
```

</CodeGroup>
