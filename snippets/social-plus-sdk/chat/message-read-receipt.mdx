{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-read-receipt | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun messageReadReceipt() {
        val channelId = "channelId"
        AmityChatClient.newMessageRepository()
            .getReadReceiptForChannel(channelId = channelId)
            .subscribe { receipts ->
                receipts.forEach { receipt ->
                    val userId = receipt.getUserId()
                    val readToPosition = receipt.getReadToPosition()
                }
            }
    }
```

```swift iOS
let subChannelRepository = AmitySubChannelRepository()
        subChannelRepository.startMessageReceiptSync(subChannelId: "subChannelId") { error in
            if let error = error {
                print("Error starting receipt sync: \(error)")
            } else {
                print("Receipt sync started")
            }
        }
```

```dart Flutter
void messageReadReceipt() {
    final messageId = 'messageId';
    final channelId = 'channelId';
    AmityCoreClient.newChannelRepository()
        .message(channelId)
        .getReadUsers(messageId: messageId)
        .then((users) => print('Read by: \${users.length} users'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function messageReadReceipt() {
  await MessageRepository.markMessageAsRead('messageId')
  console.log('Message marked as read')
}
```

</CodeGroup>
