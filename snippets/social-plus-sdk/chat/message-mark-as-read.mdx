{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-mark-as-read | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun markMessageAsRead(subChannelId: String, messageId: String) {
        AmityChatClient.newMessageRepository()
            .markMessageAsRead(subChannelId = subChannelId, messageId = messageId)
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let channelRepository = AmityChannelRepository(client: client)
        var token: AmityNotificationToken?
        token = channelRepository.getChannel("<channel-id>").observe { liveObject, error in
            guard let channel = liveObject.object else { return }
            Task {
                try await channel.markAsRead()
            }
        }
```

```dart Flutter
void markAsRead(AmityMessage message) {
    message.markRead();
  }
```

```typescript TypeScript
import { SubChannelRepository } from '@amityco/ts-sdk'

async function markMessageAsRead() {
  SubChannelRepository.startMessageReceiptSync('subChannelId')
}
```

</CodeGroup>
