{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-avatar-update | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun uploadAvatar(imageUri: Uri) {
        // upload image
        AmityCoreClient.newFileRepository()
            .uploadImage(uri = imageUri)
            .doOnNext { result: AmityUploadResult<AmityImage> ->
                when (result) {
                    is AmityUploadResult.COMPLETE -> {
                        // upload is complete
                        val uploadedImage: AmityImage = result.getFile()
                        updateUserAvatar(uploadedImage)
                    }
                    is AmityUploadResult.ERROR -> {
                        // handle upload error
                        val error = AmityError.from(result.getError())
                    }
                    is AmityUploadResult.PROGRESS -> {
                        // upload in progress
                        val progressPercent = result.getUploadInfo().getProgressPercentage()
                    }
                    is AmityUploadResult.CANCELLED -> {
                        // upload is cancelled
                    }
                }
            }
            //terminate the pipeline if the upload progress is completed
            .filter { it is AmityUploadResult.COMPLETE }
            .flatMapCompletable { Completable.complete() }
            .subscribe()
    }
    //
    fun updateUserAvatar(uploadedImage: AmityImage) {
        AmityCoreClient.editUser()
            .avatar(avatar = uploadedImage)
            .build()
            .apply()
            .doOnSuccess { updatedUser: AmityUser ->
                // AmityUser
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

</CodeGroup>
