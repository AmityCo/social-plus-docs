{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: send-image-message | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void sendImageMessage() async {
    final message = await AmityChatClient.newMessageRepository()
        .createMessage('subChannelId')
        .image(filePath: '/path/to/image.jpg')
        .send();
    print('Sent image message: \${message.messageId}');
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function sendImageMessage(subChannelId: string, imageFileId: string) {
  const message = await MessageRepository.createMessage({
    subChannelId,
    dataType: 'image',
    data: { fileId: imageFileId },
  })

  console.log('Image message sent:', message.messageId)
  return message
}
```

</CodeGroup>
