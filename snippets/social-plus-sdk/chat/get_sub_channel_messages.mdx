{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_sub_channel_messages | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getSubChannelMessages() {
        // Query messages in a sub-channel as a live collection
        AmityChatClient.newMessageRepository()
            .getMessages("subChannelId")
            .build()
            .query()
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(io.reactivex.rxjava3.android.schedulers.AndroidSchedulers.mainThread())
            .subscribe({ pagedList ->
                // Update UI with messages list
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
let messageRepository = AmityMessageRepository(client: AmityUIKitManagerInternal.shared.client)
        let subChannelId = "subChannel123"
        let queryOptions = AmityMessageQueryOptions(subChannelId: subChannelId, filterByParentId: false, parentId: nil, orderBy: .lastCreated, includeDeleted: false)
        let collection = messageRepository.getMessages(queryOptions: queryOptions)
        collection.observe { liveCollection, _, _ in
            let messages = liveCollection.allObjects()
            print("Messages count: \(messages.count)")
        }
```

```dart Flutter
void getSubChannelMessages() async {
    AmityChatClient.newMessageRepository()
        .getMessages(subChannelId: 'subChannelId')
        .getPagingData(token: null, limit: 20)
        .asStream()
        .listen((result) {
      final messages = result.data;
    });
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

function getSubChannelMessages() {
  const subChannelId = 'subChannel123'
  const liveCollection = MessageRepository.getMessages({ subChannelId, limit: 20 })
  liveCollection.on('dataUpdated', (messages) => {
    console.log('Sub-channel messages:', messages)
  })
}
```

</CodeGroup>
