{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-unmute-user | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun unMuteChannelUsers(
        channelRepository: AmityChannelRepository,
        channel: AmityChannel
    ) {
        val userIds = listOf("user1")
        channelRepository.moderation(channelId = channel.getChannelId())
            .unmuteMembers(userIds = userIds)
            .doOnComplete {
                // Void
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let moderation = channelRepository.moderation(forChannel: "channelId")
        moderation.unmuteMembers(["userId"]) { success, error in
            if let error = error {
                print("Error unmuting user: \(error)")
            } else {
                print("User unmuted: \(success)")
            }
        }
```

```dart Flutter
void unmuteUser(String channelId, String userId) {
    AmityChatClient.newChannelRepository()
        .moderation(channelId)
        .muteMembers([userId], 0)
        .then((value) {
          // user unmuted
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

</CodeGroup>
