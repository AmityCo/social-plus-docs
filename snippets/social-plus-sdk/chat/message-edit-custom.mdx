{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-edit-custom | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun messageEditCustom() {
        // Update operation via builder pattern
        val updatedData = mapOf("key" to "updatedValue")
        AmityChatClient.newMessageRepository()
            .editMessage(messageId = "messageId")
            .custom(updatedData)
            .build()
            .apply()
            .subscribe()
    }
```

```swift iOS
let messageRepository = AmityMessageRepository(client: client)
        let options = AmityCustomMessageEditOptions(customData: ["type": "updated", "value": 42])
        do {
            let message = try await messageRepository.editCustomMessage(withId: "<message-id>", options: options)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void messageEditCustom() {
    final messageId = 'messageId';
    AmityCoreClient.newChannelRepository()
        .message('')
        .messageId(messageId)
        .edit()
        .text('Updated message text')
        .apply()
        .then((message) => print('Message edited: \${message.messageId}'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function editCustomMessage() {
  const { data: updatedMessage } = await MessageRepository.updateMessage({
    messageId: 'messageId',
    dataType: 'custom',
    data: { url: 'https://docs.amity.co/updated' },
  })
  console.log(updatedMessage)
}
```

</CodeGroup>
