{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: subchannel-stop-message-receipt-sync | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun subchannelStopMessageReceiptSync() {
        AmityChatClient.newSubChannelRepository()
            .stopMessageReceiptSync("subChannelId")
            .subscribe({
                // Message receipt sync stopped
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
let subChannelId = "subChannelId"
        AmitySDK.stopMessageReceiptSync(subChannelId: subChannelId) { success, error in
            if let error = error {
                print("Failed to stop message receipt sync: \(error)")
                return
            }
            print("Message receipt sync stopped")
        }
```

```dart Flutter
void subchannelStopMessageReceiptSync() {
    AmityChatClient.newSubChannelRepository()
        .stopMessageReceiptSync(subChannelId: 'subChannelId')
        .then((_) {
      // Message receipt sync stopped
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { SubChannelRepository } from '@amityco/ts-sdk'

async function stopMessageReceiptSync() {
  SubChannelRepository.stopMessageReceiptSync('subChannelId')
}
```

</CodeGroup>
