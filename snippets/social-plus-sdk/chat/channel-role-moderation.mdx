{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-role-moderation | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun addAndRemoveRoles(channelRepository: AmityChannelRepository) {
        //Add role
        channelRepository
            .moderation(channelId = "channelId")
            .addRole(role = "moderator", listOf("userId1, userId2"))
            .doOnComplete {
                // Void
            }
            .doOnError {
                // Exception
            }
            .subscribe()
        //Remove role
        channelRepository
            .moderation(channelId = "channelId")
            .removeRole(role = "moderator", listOf("userId1"))
            .doOnComplete {
                // Void
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let participation = channelRepository.membership(forChannel: "channelId")
        participation.addRole("moderator", userIds: ["userId"]) { success, error in
            print("Role added: \(success), error: \(String(describing: error))")
        }
        participation.removeRole("moderator", userIds: ["userId"]) { success, error in
            print("Role removed: \(success), error: \(String(describing: error))")
        }
```

```dart Flutter
void addRole(String channelId) {
    AmityChatClient.newChannelRepository()
        .moderation(channelId)
        .addRole('channel-moderator', ['userId1', 'userId2'])
        .then((value) {
          // role added
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }

  void removeRole(String channelId) {
    AmityChatClient.newChannelRepository()
        .moderation(channelId)
        .removeRole('channel-moderator', ['userId1'])
        .then((value) {
          // role removed
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function promoteToModerator(channelId: string, userIds: string[]) {
  await ChannelRepository.addRole(channelId, 'channel-moderator', userIds)
  console.log(`Promoted ${userIds.length} user(s) to moderator in channel ${channelId}`)
}

async function demoteFromModerator(channelId: string, userIds: string[]) {
  await ChannelRepository.removeRole(channelId, 'channel-moderator', userIds)
  console.log(`Demoted ${userIds.length} user(s) from moderator in channel ${channelId}`)
}

// Example usage
promoteToModerator('channelId', ['userId1', 'userId2'])
demoteFromModerator('channelId', ['userId1'])
```

</CodeGroup>
