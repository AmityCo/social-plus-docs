{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-unmute | updated: 2026-04-30 */}

<CodeGroup>

```swift iOS
let channelRepository = AmityChannelRepository(client: client)
        let moderation = AmityChannelModeration(client: client, andChannelId: "<channel-id>")
        do {
            try await moderation.muteChannel(false)
            print("Channel unmuted successfully")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void unmuteChannel(String channelId) {
    AmityChatClient.newChannelRepository()
        .muteChannel(channelId, mutePeriod: AmityMutePeriod.MUTE_PERIOD_NONE)
        .then((value) => {
              //success - channel is unmuted
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function unmuteChannel() {
  await ChannelRepository.unmuteChannel('channelId')
}
```

</CodeGroup>
