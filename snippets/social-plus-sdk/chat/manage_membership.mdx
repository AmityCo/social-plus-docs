{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: manage_membership | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun manageMembership() {
        // Manage channel membership: add or remove members
        AmityChatClient.newChannelRepository()
            .membership("channelId")
            .addMembers(listOf("userId1", "userId2"))
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(io.reactivex.rxjava3.android.schedulers.AndroidSchedulers.mainThread())
            .subscribe({
                // Members added successfully
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
// Add 'user1' and 'user2' to this channel
        do {
            let success = try await channelMembership.addMembers(["user1", "user2"])
        } catch {
            // Handle error here
        }
        

        // Remove 'user3' from this channel
        do {
            let success = try await channelMembership.removeMembers(["user3"])
        } catch {
            // Handle error here
        }

        // Leave this channel
        Task {
            do {
                try await channelRepository.leaveChannel(channelId: "channel-id")
            } catch {
                // Handle error here
            }
        }
```

```dart Flutter
void manageMembership() async {
    final memberRepo = AmityChatClient.newChannelRepository();
    await memberRepo.membership('channelId123').addMembers(['userId1', 'userId2']);
    await memberRepo.membership('channelId123').removeMembers(['userId3']);
    print('Channel membership updated.');
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function manageMembership() {
  // Add user to channel
  await ChannelRepository.addMembers('channelId', ['userId1', 'userId2'])
  console.log('Members added to channel')
  
  // Remove user from channel
  await ChannelRepository.removeMembers('channelId', ['userId1'])
  console.log('Member removed from channel')
}
```

</CodeGroup>
