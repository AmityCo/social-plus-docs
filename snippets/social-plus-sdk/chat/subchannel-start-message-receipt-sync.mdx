{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: subchannel-start-message-receipt-sync | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun subchannelStartMessageReceiptSync() {
        AmityChatClient.newSubChannelRepository()
            .startMessageReceiptSync("subChannelId")
            .subscribe({
                // Message receipt sync started
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
let subChannelId = "subChannelId"
        AmitySDK.startMessageReceiptSync(subChannelId: subChannelId) { success, error in
            if let error = error {
                print("Failed to start message receipt sync: \(error)")
                return
            }
            print("Message receipt sync started")
        }
```

```dart Flutter
void subchannelStartMessageReceiptSync() {
    AmityChatClient.newSubChannelRepository()
        .startMessageReceiptSync(subChannelId: 'subChannelId')
        .then((_) {
      // Message receipt sync started
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { SubChannelRepository } from '@amityco/ts-sdk'

async function startMessageReceiptSync() {
  SubChannelRepository.startMessageReceiptSync('subChannelId')
}
```

</CodeGroup>
