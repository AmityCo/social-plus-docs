{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: query-sub-channels | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun querySubChannels() {
        // TODO: Implement Query sub channels
    }
```

```swift iOS
func querySubChannels() {
        let channelRepository = AmityChannelRepository(client: AmityClient.shared)
        let collection = channelRepository.getSubChannels(channelId: "channelId")
        token = collection.observe { liveCollection, change, error in
            print("Sub-channels count: \(liveCollection.count())")
        }
    }
```

```dart Flutter
void querySubChannels(String channelId) {
      final scrollController = ScrollController();
      final LiveCollectionStream<AmityChannel> collections =
          AmityChatClient.newChannelRepository()
              .getSubChannels(channelId: channelId);

      collections.getStream().listen((event) {
        final List<AmityChannel> subChannels = event.data;
        // Update UI with sub-channels
      });

      scrollController.addListener(() {
        if (scrollController.position.pixels ==
                scrollController.position.maxScrollExtent &&
            collections.hasNextPage()) {
          collections.loadNext();
        }
      });
    }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function querySubChannels() {
  const unsub = runQuery(
    SubChannelRepository.querySubChannels({ channelId: 'channelId123' }),
    ({ data: subChannels }) => {
      console.log('Sub-channels:', subChannels.map((sc) => sc.channelId));
    },
  );
  return unsub;
}
```

</CodeGroup>
