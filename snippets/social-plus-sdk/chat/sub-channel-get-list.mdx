{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: sub-channel-get-list | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void subChannelGetList() async {
    final subChannelRepo = AmityChatClient.newSubChannelRepository();
    final liveCollection = subChannelRepo
        .getSubChannels('channelId')
        .getLiveCollection();
    liveCollection.asStream().listen((subChannels) {
      print('Sub-channels: \${subChannels.length}');
    });
  }
```

```typescript TypeScript
import { SubChannelRepository } from '@amityco/ts-sdk'

function subChannelGetList(channelId: string) {
  const query = SubChannelRepository.getSubChannels({ channelId })
  const unsubscribe = query.on('dataUpdated', (subChannels) => {
    console.log('Sub-channels:', subChannels)
  })
  return unsubscribe
}
```

</CodeGroup>
