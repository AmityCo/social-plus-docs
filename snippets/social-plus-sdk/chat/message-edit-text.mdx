{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-edit-text | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun messageEditText() {
        // Update operation via builder pattern
        AmityChatClient.newMessageRepository()
            .editMessage(messageId = "messageId")
            .text("Updated message text")
            .build()
            .apply()
            .subscribe()
    }
```

```swift iOS
let messageRepository = AmityMessageRepository(client: client)
        let options = AmityTextMessageEditOptions(text: "Updated message text")
        do {
            let message = try await messageRepository.editTextMessage(withId: "<message-id>", options: options)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void editTextMessage(AmityMessage message) {
    AmityChatClient.newMessageRepository()
        .editMessage(message.messageId!)
        .text('Updated message text')
        .edit()
        .then((AmityMessage updated) {
          // handle result
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function editTextMessage() {
  const { data: updatedMessage } = await MessageRepository.updateMessage({
    messageId: 'messageId',
    dataType: 'text',
    data: { text: 'Updated message text' },
  })
  console.log(updatedMessage)
}
```

</CodeGroup>
