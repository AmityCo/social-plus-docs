{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-create-video | updated: 2026-05-01 */}

<CodeGroup>

```swift iOS
let messageRepository = AmityMessageRepository(client: client)
        let videoURL = URL(string: "file:///path/to/video.mp4")!
        let options = AmityVideoMessageCreateOptions(subChannelId: "<sub-channel-id>", attachment: .localURL(url: videoURL), fileName: nil, tags: nil, parentId: nil)
        do {
            let message = try await messageRepository.createVideoMessage(options: options)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void createVideoMessage(String channelId, Uri videoUri) {
    AmityChatClient.newMessageRepository()
        .createMessage(channelId)
        .video(videoUri)
        .caption('Video caption')
        .send()
        .then((value) {
          // message sent successfully
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

</CodeGroup>
