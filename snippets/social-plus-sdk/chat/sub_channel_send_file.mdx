{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: sub_channel_send_file | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun subChannelSendFile() {
        AmityChatClient.newSubChannelRepository()
            .createMessage("subChannelId")
            .file()
            .attachFile(fileUri)
            .build()
            .send()
            .subscribe({ message ->
                // File message sent successfully
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
let subChannelId = "subChannelId"
        let fileURL = URL(fileURLWithPath: "/path/to/document.pdf")
        AmitySDK.createFileMessage(subChannelId: subChannelId, fileURL: fileURL) { message, error in
            if let error = error {
                print("Failed to send file message: \(error)")
                return
            }
            print("File message sent: \(message?.messageId ?? "")")
        }
```

```dart Flutter
void subChannelSendFile() {
    final messageRepo = AmityChatClient.newMessageRepository();
    messageRepo
        .createMessage('subChannelId')
        .file(fileId: 'uploadedFileId')
        .send()
        .then((AmityMessage message) {
          print('File sent to sub-channel: \${message.messageId}');
        });
  }
```

```typescript TypeScript
import { SubChannelRepository, MessageRepository } from '@amityco/ts-sdk'

async function subChannelSendFile() {
  const messageCreator = MessageRepository.createMessage('subChannelId', {
    dataType: 'file',
    fileId: 'uploaded-file-id',
  });
  messageCreator.on('dataUpdated', (message) => {
    console.log('File message sent to sub-channel:', message);
  });
}
```

</CodeGroup>
