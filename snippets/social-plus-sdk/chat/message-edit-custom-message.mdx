{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-edit-custom-message | updated: 2026-04-30 */}

<CodeGroup>

```kotlin Android
fun editCustomMessage(messageId: String, data: Map<String, Any>) {
        AmityChatClient.newMessageRepository()
            .editMessage(messageId = messageId)
            .custom(data = data)
            .build()
            .apply()
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let messageRepository = AmityMessageRepository(client: client)
        let editor = AmityMessageEditor(client: client, messageId: "<message-id>")
        do {
            let message = try await editor.editCustomMessage(["key": "updated-value"])
            print("Custom message updated: \(message.messageId)")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void editCustomMessage(AmityMessage message) {
    final updatedData = <String, dynamic>{
      'type': 'custom',
      'value': 'Updated custom message content',
    };

    message
        .edit()
        .custom(updatedData)
        .update()
        .then((AmityMessage updatedMessage) => {
              //handle result
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function editCustomMessage() {
  const { data: message } = await MessageRepository.updateMessage('messageId', {
    data: { type: 'sticker', stickerId: 'sticker456' },
  })
  console.log(message)
}
```

</CodeGroup>
