{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: add-remove-role-member | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void addRole(String channelId) {
    AmityChatClient.newChannelRepository()
        .moderation(channelId)
        .addRole('channel-moderator', ['userId1'])
        .then((value) {
          // role added
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }

  void removeRole(String channelId) {
    AmityChatClient.newChannelRepository()
        .moderation(channelId)
        .removeRole('channel-moderator', ['userId1'])
        .then((value) {
          // role removed
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function addRoleToMembers(channelId: string, role: string, userIds: string[]) {
  await ChannelRepository.addRole(channelId, role, userIds)
  console.log(`Role '${role}' added to members in channel ${channelId}`)
}

async function removeRoleFromMembers(channelId: string, role: string, userIds: string[]) {
  await ChannelRepository.removeRole(channelId, role, userIds)
  console.log(`Role '${role}' removed from members in channel ${channelId}`)
}

// Example usage
addRoleToMembers('channelId', 'channel-moderator', ['userId1', 'userId2'])
removeRoleFromMembers('channelId', 'channel-moderator', ['userId1'])
```

</CodeGroup>
