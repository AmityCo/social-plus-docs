{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-creation-community | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun channelCreationCommunity() {
        AmityChatClient.newChannelRepository()
            .createChannel("communityChannelId")
            .communityType()
            .build()
            .create()
            .subscribe()
    }
```

```swift iOS
let channelRepository = AmityChannelRepository()
        let builder = AmityCommunityChannelBuilder()
        builder.setId("my-community-channel")
        builder.setDisplayName("My Community Channel")
        channelRepository.createChannel(with: builder) { channel, error in
            if let error = error { print("Error: \(error!)"); return }
            print("Community channel: \(channel?.channelId ?? "")")
        }
```

```dart Flutter
void createCommunityChannel() {
    // create channel by specifying channelId
    AmityChatClient.newChannelRepository()
        .createChannel()
        .communityType()
        .withChannelId("sorbh1234")
        .displayName('Weekly Promo') //Optional
        .metadata({'key': 'value'}) //Optional
        .tags(['Promotion', 'New Arrival']) //Optional
        .create()
        .then((AmityChannel channel) => {
              //handle result
            })
        .onError((error, stackTrace) => {
              //handle error
            });
    // create channel and let SDK handle channelId generation
    AmityChatClient.newChannelRepository()
        .createChannel()
        .communityType()
        .withDisplayName('Weekly Promo')
        .userIds(["userIds"])
        .metadata({'key': 'value'}) //Optional
        .tags(['Promotion', 'New Arrival']) //Optional
        .create()
        .then((AmityChannel channel) => {
              //handle result
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function channelCreationCommunity() {
  const client = createClient('apiKey', 'region')
  await client.login({ userId: 'user123', displayName: 'User 123' })
  const channel = await client.channel.createChannel({
    type: 'community',
    channelId: 'my-community-channel',
    displayName: 'My Community Channel',
    metadata: { topic: 'general' },
  })
  console.log('Community channel created:', channel.channelId)
}
```

</CodeGroup>
