{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: send_image_message | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun sendImageMessage() {
        val imageUri = android.net.Uri.parse("content://path/to/image.jpg")
        AmityChatClient.newMessageRepository()
            .createMessage(channelId = "channelId")
            .with()
            .image(imageUri)
            .build()
            .send()
            .subscribe()
    }
```

```swift iOS
let messageRepository = AmityMessageRepository(client: AmityManager.shared.client!)
        let image = UIImage(named: "example")!
        let createOptions = AmityMessageCreateOptions(channelId: "channelId", image: image, caption: "My photo")
        messageRepository.createMessage(with: createOptions) { message, error in
            print("Image message sent: \(message?.messageId ?? "")")
        }
```

```dart Flutter
void sendImageMessage() {
    final messageRepo = AmityChatClient.newMessageRepository();
    messageRepo
        .createMessage('channelId')
        .image(fileId: 'uploadedImageFileId')
        .send()
        .then((AmityMessage message) {
          print('Image message sent: \${message.messageId}');
        });
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function sendImageMessage() {
  const messageCreator = MessageRepository.createMessage('channelId', {
    dataType: 'image',
    fileId: 'uploaded-image-id',
    caption: 'Image caption',
  });
  messageCreator.on('dataUpdated', (message) => {
    console.log('Image message sent:', message);
  });
}
```

</CodeGroup>
