{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-delete | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun messageDelete() {
        // TODO: implement Message delete
    }
```

```swift iOS
func messageDelete() {
        let client = AmityClient.shared
        let messageRepo = AmityMessageRepository(client: client)
        messageRepo.deleteMessage(withMessageId: "messageId", isHardDelete: false) { success, error in
            if success {
                print("Message deleted")
            } else if let error = error {
                print("Error: \(error.localizedDescription)")
            }
        }
    }
```

```dart Flutter
void messageDelete() async {
    await AmityChatClient.newMessageRepository()
        .deleteMessage(messageId: 'messageId');
    print('Message deleted');
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function messageDelete(messageId: string) {
  await MessageRepository.deleteMessage(messageId)
  console.log('Message deleted:', messageId)
}
```

</CodeGroup>
