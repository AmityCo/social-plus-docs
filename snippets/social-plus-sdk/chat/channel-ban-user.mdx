{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-ban-user | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun banChannelMembers(
        channelRepository: AmityChannelRepository,
        channel: AmityChannel
    ) {
        // ban channel members
        val userIds = listOf("user1", "user2")
        channelRepository.moderation(channelId = channel.getChannelId())
            .banMembers(userIds = userIds)
            .doOnComplete {
                // Void
            }
            .doOnError {
                // Exception
            }
            .subscribe()

        // unban channel members
        channelRepository.moderation(channelId = channel.getChannelId())
            .unbanMembers(userIds = userIds)
            .doOnComplete {
                // Void
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```dart Flutter
void banUser(String channelId, String userId) {
    AmityChatClient.newChannelRepository()
        .moderation(channelId)
        .banMembers([userId])
        .then((value) {
          // user banned
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function banUserFromChannel(channelId: string, userId: string) {
  await ChannelRepository.banMembers(channelId, [userId])
  console.log(`User ${userId} has been banned from channel ${channelId}`)
}

// Example usage
banUserFromChannel('channelId', 'userId')
```

</CodeGroup>
