{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: add_remove_role_member | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun addRemoveRoleMember() {
        AmityChatClient.newChannelRepository()
            .membership("channelId")
            .addRole(role = "moderator", userIds = listOf("userId"))
            .subscribe()
    }
```

```swift iOS
let channelModeration = AmityChannelModeration(client: client, andChannel: "<channel-id>")
        
        // Add a role to member
        do {
            let success = try await channelModeration.addRole("<role>", userIds: ["<user-id>"])
        } catch {
            // Handle error here
        }
        
        // Remove a role from member
        do {
            let success = try await channelModeration.removeRole("<role>", userIds: ["<user-id>"])
        } catch {
            // Handle error here
        }
```

```dart Flutter
void addRemoveRoleMember() {
    AmityChatClient.newChannelRepository()
        .moderation('channelId')
        .addRole('moderator', ['userId'])
        .then((_) { })
        .onError((error, stackTrace) { });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function addRemoveRoleMember(channelId: string, userId: string, role: string, action: 'add' | 'remove') {
  try {
    if (action === 'add') {
      await ChannelRepository.Membership.addRole(channelId, role, [userId]);
      console.log(`Role '${role}' added to user ${userId}`);
    } else {
      await ChannelRepository.Membership.removeRole(channelId, role, [userId]);
      console.log(`Role '${role}' removed from user ${userId}`);
    }
  } catch (error) {
    console.error('Failed to update role:', error);
  }
}
```

</CodeGroup>
