{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message_update_image | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun messageUpdateImage() {
        AmityChatClient.newMessageRepository()
            .getMessage("messageId")
            .flatMap { message ->
                message.edit()
                    .text("Updated message text")
                    .build()
                    .apply()
            }
            .subscribe({ updatedMessage ->
                // Message updated
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
func messageUpdateImage() {
        let messageRepository = AmityMessageRepository(client: AmityManager.shared.client!)
        guard let image = UIImage(named: "updated_image"),
              let data = image.jpegData(compressionQuality: 0.8) else { return }
        let builder = AmityImageMessageBuilder(imageData: data)
        messageRepository.editMessage(
            withId: "messageId123",
            builder: builder
        ) { message, error in
            guard let message = message else { return }
            print("Image message updated: \(message.messageId)")
        }
    }
```

```dart Flutter
void messageUpdateImage() {
    final messageId = 'messageId';
    final imageFile = File('path/to/image.jpg');
    AmityCoreClient.newChannelRepository()
        .message('')
        .messageId(messageId)
        .edit()
        .image(imageFile)
        .apply()
        .then((message) => print('Image message updated'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function messageUpdateImage() {
  const fileUrl = await FileRepository.uploadImage(imageFile)
  
  const updatedMessage = await MessageRepository.updateMessage('messageId', {
    data: { fileId: fileUrl.fileId },
  })
  console.log('Image message updated:', updatedMessage.data.messageId)
}
```

</CodeGroup>
