{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: sub-channel-update | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun updateSubChannel(subChannelRepository: AmitySubChannelRepository, subChannelId: String) {
        subChannelRepository.editSubChannel(subChannelId, "displayName")
            .doOnComplete {
                // Void
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let channelRepository = AmityChannelRepository()
        let builder = AmitySubChannelUpdateBuilder()
        builder.setDisplayName("Updated Sub Channel")
        channelRepository.updateSubChannel(withId: "subChannelId", builder: builder) { subChannel, error in
            if let error = error {
                print("Error updating sub-channel: \(error)")
            } else {
                print("Sub-channel updated: \(subChannel?.displayName ?? "")")
            }
        }
```

```dart Flutter
Future<void> updateSubChannel(String subChannelId, String displayName) async {
    try {
      await AmityChatClient.newSubChannelRepository()
          .editSubChannel(subChannelId, displayName);
      // sub-channel updated
    } catch (error) {
      // handle error
    }
  }
```

</CodeGroup>
