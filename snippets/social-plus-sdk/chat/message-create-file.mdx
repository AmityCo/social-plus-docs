{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-create-file | updated: 2026-04-30 */}

<CodeGroup>

```swift iOS
let messageRepository = AmityMessageRepository(client: client)
        let fileURL = URL(string: "file:///path/to/document.pdf")!
        let options = AmityFileMessageCreateOptions(subChannelId: "<sub-channel-id>", attachment: .localURL(url: fileURL), fileName: "document.pdf", tags: nil, parentId: nil)
        do {
            let message = try await messageRepository.createFileMessage(options: options)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void createFileMessage(String channelId, Uri fileUri) {
    AmityChatClient.newMessageRepository()
        .createMessage(channelId)
        .file(fileUri)
        .caption('File caption')
        .send()
        .then((value) {
          // message sent successfully
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

</CodeGroup>
