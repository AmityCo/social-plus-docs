{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-join | updated: 2026-05-01 */}

<CodeGroup>

```kotlin Android
fun joinChannel(channelRepository: AmityChannelRepository) {
        channelRepository.joinChannel(channelId = "channel1")
            .doOnSuccess { channel: AmityChannel ->
                // AmityChannel
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let channelRepository = AmityChannelRepository(client: client)
        do {
            try await channelRepository.joinChannel(channelId: "<channel-id>")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void joinChannel(String channelId) {
    AmityChatClient.newChannelRepository()
        .joinChannel(channelId)
        .then((value) => {
              //handle result
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function joinChannel() {
  await ChannelRepository.joinChannel('channelId')
}
```

</CodeGroup>
