{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-send-image | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun messageSendImage() {
        val imageUri = android.net.Uri.parse("content://path/to/image.jpg")
        AmityChatClient.newMessageRepository()
            .createMessage("channelId")
            .with()
            .image(imageUri)
            .build()
            .send()
            .subscribe()
    }
```

```swift iOS
func messageSendImage() {
        let messageRepository = AmityMessageRepository(client: AmityManager.shared.client!)
        guard let image = UIImage(named: "sample"),
              let data = image.jpegData(compressionQuality: 0.8) else { return }
        let builder = AmityImageMessageBuilder(imageData: data)
        messageRepository.createMessage(
            withChannelId: "channelId123",
            builder: builder
        ) { message, error in
            guard let message = message else { return }
            print("Image message sent: \(message.messageId)")
        }
    }
```

```dart Flutter
void sendImageMessage(String channelId, File imageFile) {
    AmityCoreClient.newFileRepository()
        .uploadImage(imageFile)
        .stream
        .listen((amityUploadResult) {
      amityUploadResult.when(
        progress: (uploadInfo, cancelToken) {},
        complete: (AmityImage image) {
          AmityChatClient.newMessageRepository()
              .createMessage(channelId)
              .image(image)
              .send()
              .then((AmityMessage message) {
                // image message sent
              })
              .onError((error, stackTrace) {
                // handle error
              });
        },
        error: (error) {
          // handle upload error
        },
        cancel: () {},
      );
    });
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function messageSendImage(channelId: string, imageFileId: string, caption?: string) {
  const message = await MessageRepository.createMessage({
    channelId,
    dataType: 'image',
    data: {
      fileId: imageFileId,
      caption,
    },
  })
  console.log('Image message sent:', message.messageId)
  return message
}
```

</CodeGroup>
