{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-presence-get | updated: 2026-05-02 */}

<CodeGroup>

```swift iOS
import Combine
        let channelPresenceRepo = AmityChannelPresenceRepository(client: client)
        var cancellables = Set<AnyCancellable>()
        channelPresenceRepo.getChannelPresence(channelId: "<channel-id>").sink { completion in
            // Handle completion
        } receiveValue: { presence in
            print("Is any member online: \(presence?.isAnyMemberOnline ?? false)")
        }.store(in: &cancellables)
```

```dart Flutter
void getChannelPresence(String channelId) {
    AmityChatClient.newChannelRepository()
        .presence(channelId)
        .getPresence()
        .asStream()
        .listen((presence) {
          final isAnyMemberOnline = presence?.isAnyMemberOnline ?? false;
          // update UI with presence info
        });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

function getChannelPresence(channelId: string) {
  const unsubscribe = ChannelRepository.getChannel(channelId, ({ data: channel, loading }) => {
    if (loading) return

    console.log('Channel ID:', channel.channelId)
    console.log('Member Count:', channel.memberCount)
    console.log('Active Member Count:', channel.activeMemberCount)
  })

  return unsubscribe
}
```

</CodeGroup>
