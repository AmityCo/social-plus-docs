{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: live-channel-member-unmute | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun liveChannelMemberUnmute() {
        AmityChatClient.newChannelRepository()
            .membership("channelId")
            .unmuteMembers(listOf("userId"))
            .build()
            .apply()
            .subscribe()
    }
```

```swift iOS
let channelRepository = AmityChannelRepository(client: AmityUIKitManagerInternal.shared.client)
        let channelId = "channel123"
        let userIds = ["user1"]
        channelRepository.unmuteMembers(userIds, channelId: channelId) { success, error in
            print("Members unmuted: \(success)")
        }
```

```dart Flutter
void unmuteMembers(String channelId, List<String> userIds) {
    AmityChatClient.newChannelRepository()
        .moderation(channelId)
        .unmuteMembers(userIds)
        .then((_) {
          // members unmuted in live channel
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function liveChannelMemberUnmute() {
  const channelId = 'channel-id-123'
  const userId = 'user-id-to-unmute'
  const { data, error } = await ChannelRepository.unmuteMembers({
    channelId,
    userIds: [userId],
  })
  if (error) {
    console.error('Failed to unmute member:', error)
  } else {
    console.log('Member unmuted:', userId)
  }
}
```

</CodeGroup>
