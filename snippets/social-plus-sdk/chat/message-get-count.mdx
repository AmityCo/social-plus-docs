{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-get-count | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun messageGetCount() {
        AmityChatClient.newMessageRepository()
            .getMessages("subChannelId123")
            .build()
            .query()
            .subscribe { messages ->
                println("Message count: \${messages.size}")
            }
    }
```

```swift iOS
func messageGetCount() {
        let client = AmityClient.shared
        let channelRepo = AmityChannelRepository(client: client)
        let token = channelRepo.getChannel(withChannelId: "channelId")
            .observe { liveObject, error in
                guard let channel = liveObject.object else { return }
                print("Message count: \(channel.messageCount)")
            }
    }
```

```dart Flutter
void messageGetCount() {
    final channelId = 'channelId';
    AmityCoreClient.newChannelRepository()
        .channel(channelId)
        .subscription(AmityChannelEvents.MESSAGES)
        .subscribeTopic()
        .then((_) => print('Subscribed to channel events'));
    final count = AmityCoreClient.newChannelRepository()
        .channel(channelId)
        .getUnreadCount();
    print('Unread count: \$count');
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

function getMessageCount(channelId: string) {
  const unsubscribe = ChannelRepository.getChannel(
    channelId,
    ({ data: channel, loading, error }) => {
      if (loading) return
      if (error) throw error
      console.log('Unread message count:', channel.unreadCount)
      console.log('Total member count:', channel.memberCount)
    },
  )
  return unsubscribe
}
```

</CodeGroup>
