{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-member-get-my-membership | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getMyMembership(channelId: String) {
        AmityCoreClient.newChannelRepository()
            .membership(channelId)
            .getMyMembership()
            .doOnSuccess { membership: AmityChannelMember ->
                val roles = membership.getRoles()
                val isMuted = membership.isMuted()
            }
            .doOnError { }
            .subscribe()
    }
```

```swift iOS
let channelRepository = AmityChannelRepository(client: client)
        var token: AmityNotificationToken?
        token = channelRepository.getChannel("<channel-id>").observe { liveObject, error in
            guard let channel = liveObject.object else { return }
            print("My membership: \(channel.currentUserMembership)")
        }
```

```dart Flutter
StreamSubscription? subscription;

  void getMyMembership(String channelId) {
    subscription = AmityChatClient.newChannelRepository()
        .getChannel(channelId)
        .asStream()
        .listen((AmityChannel channel) {
          final myMembership = channel.currentUserMembership;
          if (myMembership != null) {
            final isMember = myMembership.isMember;
            final isMuted = myMembership.isMuted;
            final roles = myMembership.roles;
          }
        })
        ..onError((error) {
          // Handle error
        });
  }

  void dispose() {
    subscription?.cancel();
  }
```

</CodeGroup>
