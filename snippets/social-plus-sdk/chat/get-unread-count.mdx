{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-unread-count | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getUnreadCount() {
        AmityChatClient.newChannelRepository()
            .getChannels()
            .build()
            .query()
            .subscribe { channels ->
                val totalUnread = channels.sumOf { it.getUnreadCount() }
            }
    }
```

```swift iOS
let channelRepository = AmityChannelRepository()
        let liveObject = channelRepository.getTotalChannelsUnread()
        liveObject.observe { liveObject, error in
            guard let unreadInfo = liveObject.object else { return }
            print("Total unread channels: \(unreadInfo.channelCount)")
            print("Total unread messages: \(unreadInfo.messageCount)")
        }
```

</CodeGroup>
