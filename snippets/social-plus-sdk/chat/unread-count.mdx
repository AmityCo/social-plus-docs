{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: unread-count | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun sampleMarkRead(adapter: PagingDataAdapter<AmityMessage, RecyclerView.ViewHolder>, recyclerView: RecyclerView) {
		// adapter extends from PagingDataAdapter
		adapter.registerAdapterDataObserver(object : RecyclerView.AdapterDataObserver() {
			override fun onItemRangeInserted(positionStart: Int, itemCount: Int) {
				super.onItemRangeInserted(positionStart, itemCount)
				adapter.snapshot().items.maxBy { it.getSegment() }.markRead()
			}
		})
	}
```

```swift iOS
let channelId = "channelId"
        let token = AmitySDK.observeUnreadCount(channelId: channelId) { count in
            print("Unread count updated: \(count)")
        }
        // Retain token to keep observing
```

```dart Flutter
void unreadCount() {
    AmityChatClient.newChannelRepository()
        .getChannel('channelId')
        .then((channel) {
      final unreadCount = channel.unreadCount;
      // Display unreadCount in the channel list UI
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function unreadCount() {
  const { data: unreadInfo } = await runQuery(
    ChannelRepository.getTotalUnreadCount()
  )
  console.log('Total unread count:', unreadInfo?.totalUnreadCount)
}
```

</CodeGroup>
