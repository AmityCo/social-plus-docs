{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-get-reactions | updated: 2026-05-02 */}

<CodeGroup>

```swift iOS
collection = messageRepository.getReactions("<message-id>")
        collection?.observe { liveCollection, _, error in
            if let error = error {
                // Handle error here
                print("Error: \(error)")
                return
            }
            for i in 0..<liveCollection.count() {
                if let reaction = liveCollection.object(at: i) {
                    print("Reaction: \(reaction.reactionName) by \(reaction.userId)")
                }
            }
        }
```

```dart Flutter
void getMessageReactions(AmityMessage message, {String reactionName = 'like'}) {
    message.react()
        .getReactions(reactionName)
        .then((List<AmityReaction> reactions) {
          // use reactions list
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { ReactionRepository } from '@amityco/ts-sdk'

function getMessageReactions() {
  const liveCollection = ReactionRepository.queryReactions({
    referenceType: 'message',
    referenceId: 'messageId',
    reactionName: 'like',
  })
  liveCollection.on('dataUpdated', (reactions) => {
    reactions.forEach((reaction) => {
      console.log(`Reaction: ${reaction.reactionName} by ${reaction.userId}`)
    })
  })
  return liveCollection
}
```

</CodeGroup>
