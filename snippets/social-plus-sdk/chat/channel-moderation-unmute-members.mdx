{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-moderation-unmute-members | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun unmuteMembers(channelId: String, userIds: List<String>) {
        AmityChatClient.newChannelRepository()
            .moderation(channelId = channelId)
            .unmuteMembers(userIds = userIds)
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let channelModeration = AmityChannelModeration(client: client, andChannel: "<channel-id>")
        do {
            let success = try await channelModeration.unmuteMembers(["<user-id>"])
        } catch {
            // Handle error here
        }
```

```dart Flutter
void unmuteMembers(String channelId) {
    AmityChatClient.newChannelRepository()
        .moderation(channelId)
        .muteMembers(['userId1', 'userId2'], 0)
        .then((value) {
          // handle result
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function unmuteChannelMembers() {
  await ChannelRepository.unmuteMembers('channelId', ['userId1', 'userId2'])
}
```

</CodeGroup>
