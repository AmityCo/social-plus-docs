{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: join_channel | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun joinChannel() {
        AmityChatClient.newChannelRepository()
            .joinChannel("channelId")
            .subscribe { channel ->
                // Joined channel
            }
    }
```

```swift iOS
func joinChannel() {
        let channelRepository = AmityChannelRepository(client: AmityManager.shared.client!)
        channelRepository.joinChannel(withId: "channelId123") { success, error in
            if success {
                print("Successfully joined channel")
            }
        }
    }
```

```dart Flutter
void joinChannel() {
    AmityChatClient.newChannelRepository()
        .joinChannel('channelId');
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function joinChannel() {
  const channelId = 'channel123'
  ChannelRepository.joinChannel(channelId)
    .then(() => console.log('Joined channel'))
    .catch(console.error)
}
```

</CodeGroup>
