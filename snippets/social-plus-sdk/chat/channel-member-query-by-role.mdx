{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-member-query-by-role | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun channelMemberQueryByRole() {
        // TODO: implement Channel member query by role
    }
```

```swift iOS
func channelMemberQueryByRole() {
        let client = AmityClient.shared
        let channelRepo = AmityChannelRepository(client: client)
        let membershipRepo = channelRepo.membershipParticipation(forChannelId: "channelId")
        let collection = membershipRepo?.getMembers(
            filter: .all,
            sortBy: .displayName,
            roles: ["moderator"]
        )
    }
```

```dart Flutter
final _members = <AmityChannelMember>[];
  late PagingController<AmityChannelMember> _controller;

  void queryMembersByRole(String channelId) {
    _controller = PagingController(
      pageFuture: (token) => AmityChatClient.newChannelRepository()
          .membership(channelId)
          .getMembers()
          .roles(['channel-moderator'])
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    )..addListener(() {
        if (_controller.error == null) {
          _members.clear();
          _members.addAll(_controller.loadedItems);
          // update widgets
        } else {
          // handle error
        }
      });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

function queryChannelMembersByRole(channelId: string, roles: string[]) {
  const liveCollection = ChannelRepository.queryMembers({ channelId, roles })

  liveCollection.on('dataUpdated', (members) => {
    console.log(`Members with role(s) [${roles.join(', ')}] in channel ${channelId}:`)
    members.forEach((member) => {
      console.log(' -', member.userId)
    })
  })

  liveCollection.on('dataError', (error) => {
    console.error('Error querying members by role:', error)
  })
}

// Example usage
queryChannelMembersByRole('channelId', ['channel-moderator'])
```

</CodeGroup>
