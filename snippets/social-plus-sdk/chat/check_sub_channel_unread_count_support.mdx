{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: check_sub_channel_unread_count_support | updated: 2026-05-02 */}

<CodeGroup>

```swift iOS
print("Is this SubChannel support unread count = \(subChannel.isUnreadCountSupported)")
```

```dart Flutter
void checkSubChannelUnreadCountSupport() {
    AmityChatClient.newChannelRepository()
        .getChannel('channelId')
        .then((channel) {
          final unreadCount = channel.unreadCount;
        })
        .onError((error, stackTrace) { });
  }
```

```typescript TypeScript
import { SubChannelRepository } from '@amityco/ts-sdk'

function checkSubChannelUnreadCountSupport(subChannelId: string) {
  const unsubscribe = SubChannelRepository.getSubChannel(
    subChannelId,
    ({ data: subChannel, loading, error }) => {
      if (error) {
        console.error('Error fetching sub-channel:', error)
        return
      }
      if (!loading && subChannel) {
        console.log('Sub-channel unread count:', subChannel.unreadCount)
        console.log('Membership unread count supported:', subChannel.messageUnreadCount !== undefined)
        unsubscribe()
      }
    },
  )
}
```

</CodeGroup>
