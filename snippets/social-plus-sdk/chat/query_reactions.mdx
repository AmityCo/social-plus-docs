{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: query_reactions | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun queryReactions() {
        // Query reactions on a post
        AmitySocialClient.newReactionRepository()
            .getReactions(referenceType = AmityReactionReferenceType.POST, referenceId = "postId")
            .asLiveData()
    }
```

```dart Flutter
late PagingController<AmityReaction> _reactionController;

    void queryReactions(String referenceId, AmityReactionReferenceType referenceType) {
      _reactionController = PagingController(
        pageFuture: (token) => AmitySocialClient.newReactionRepository()
            .getReactions()
            .referenceId(referenceId)
            .referenceType(referenceType)
            .getPagingData(token: token, limit: 20),
        pageSize: 20,
      )..addListener(() {
          if (_reactionController.error == null) {
            // Update UI with _reactionController.loadedItems
          } else {
            // Handle error
          }
        });
    }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function queryReactions() {
  const unsub = runQuery(
    ReactionRepository.queryReactions({
      referenceType: 'post',
      referenceId: 'postId123',
      reactionName: undefined,
    }),
    ({ data: reactions }) => {
      console.log('Reactions:', reactions.map((r) => `${r.userId}:${r.reactionName}`));
    },
  );
  return unsub;
}
```

</CodeGroup>
