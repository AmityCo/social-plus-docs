{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-observe | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun messageObserve() {
        // TODO: implement message observe
    }
```

```swift iOS
func messageObserve() {
        let client = AmityClient.shared
        let messageRepository = AmityMessageRepository(client: client)
        let liveObject = messageRepository.getMessage("message_id")
        liveObject.observe { message, error in
            if let msg = message {
                print("Message updated: \(msg.messageId), text: \(msg.text ?? "")")
            }
        }
    }
```

```dart Flutter
void messageObserve() {
    final channelId = 'channelId';
    AmityCoreClient.newChannelRepository()
        .message(channelId)
        .getMessages()
        .asStream()
        .listen(
          (messages) => print('Messages updated: \${messages.length}'),
          onError: (error) => print('Error: \$error'),
        );
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function messageObserve() {
  const unsubscribe = MessageRepository.getMessage('messageId', ({ data: message, loading, error }) => {
    if (!loading && !error) {
      console.log('Message content:', message?.data)
      console.log('Message reactions:', message?.reactions)
    }
  })
  
  return unsubscribe
}
```

</CodeGroup>
