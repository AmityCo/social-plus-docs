{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: sub-channel-unread-count | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun subChannelUnreadCount() {
        val subChannelId = "subChannelId"
        AmityChatClient.newSubChannelRepository()
            .getSubChannel(subChannelId)
            .subscribe { subChannel ->
                val unreadCount = subChannel.getUnreadCount()
            }
    }
```

```swift iOS
let subChannelRepository = AmitySubChannelRepository()
        let liveObject = subChannelRepository.getSubChannel(withId: "subChannelId")
        liveObject.observe { liveObject, error in
            guard let subChannel = liveObject.object else { return }
            let unreadCount = subChannel.unreadCount
            print("Sub-channel unread count: \(unreadCount)")
        }
```

```dart Flutter
void subChannelUnreadCount() {
    AmityChatClient.newSubChannelRepository()
        .getSubChannel(subChannelId: 'subChannelId')
        .then((subChannel) {
      final unreadCount = subChannel.unreadCount;
      // Display unreadCount in UI
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function subChannelUnreadCount() {
  const channelRepository = new ChannelRepository();
  const unreadCount = await channelRepository.getSubChannelUnreadCount('subChannelId');
  console.log('Unread message count:', unreadCount);
}
```

</CodeGroup>
