{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message_update | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun messageUpdate() {
        AmityChatClient.newMessageRepository()
            .getMessage("messageId")
            .flatMap { message ->
                message.edit()
                    .text("Updated message text")
                    .build()
                    .apply()
            }
            .subscribe({ updatedMessage ->
                // Message updated
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
func messageUpdate() {
        let messageRepository = AmityMessageRepository(client: AmityManager.shared.client!)
        let builder = AmityTextMessageBuilder(text: "Edited message text")
        messageRepository.editMessage(
            withId: "messageId123",
            builder: builder
        ) { message, error in
            guard let message = message else { return }
            print("Message updated: \(message.messageId)")
        }
    }
```

```dart Flutter
void messageUpdate() {
    final messageId = 'messageId';
    AmityCoreClient.newChannelRepository()
        .message('')
        .messageId(messageId)
        .edit()
        .text('Updated text content')
        .apply()
        .then((message) => print('Message updated: \${message.messageId}'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function messageUpdate() {
  const updatedMessage = await MessageRepository.updateMessage('messageId', {
    data: { text: 'Updated message text' },
    metadata: { edited: true },
  })
  console.log('Message updated:', updatedMessage.data.messageId)
}
```

</CodeGroup>
