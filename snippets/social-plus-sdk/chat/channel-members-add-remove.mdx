{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-members-add-remove | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun addAndRemoveMembers(channelRepository: AmityChannelRepository) {
        //Add members
        channelRepository
            .membership(channelId = "channelId")
            .addMembers(userIds = listOf("userId1, userId2"))
            .doOnComplete {
                // Void
            }
            .doOnError {
                // Exception
            }
            .subscribe()
        //Remove members
        channelRepository
            .membership(channelId = "channelId")
            .removeMembers(userIds = listOf("userId1, userId2"))
            .doOnComplete {
                // Void
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let participation = channelRepository.membership(forChannel: "channelId")
        participation.addMembers(["userId1", "userId2"]) { success, error in
            print("Members added: \(success)")
        }
        participation.removeMembers(["userId3"]) { success, error in
            print("Members removed: \(success)")
        }
```

```dart Flutter
void addMembers(String channelId) {
    AmityChatClient.newChannelRepository()
        .membership(channelId)
        .addMembers(['userId1', 'userId2'])
        .then((value) {
          // members added
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }

  void removeMembers(String channelId) {
    AmityChatClient.newChannelRepository()
        .membership(channelId)
        .removeMembers(['userId1', 'userId2'])
        .then((value) {
          // members removed
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

</CodeGroup>
