{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-create-custom | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createCustomMessage(subChannelId: String, customData: Map<String, Any>) {
        AmityChatClient.newMessageRepository()
            .createMessage(subChannelId = subChannelId)
            .with()
            .custom(data = customData)
            .build()
            .send()
            .doOnComplete {
                // custom message sent
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let messageRepository = AmityMessageRepository(client: client)
        let options = AmityCustomMessageCreateOptions(subChannelId: "<sub-channel-id>", customData: ["type": "location", "lat": 13.0, "lng": 100.0], tags: nil, parentId: nil)
        do {
            let message = try await messageRepository.createCustomMessage(options: options)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void createCustomMessage(String channelId) {
    AmityChatClient.newMessageRepository()
        .createMessage(channelId)
        .custom({'type': 'poll', 'pollId': 'abc123'})
        .send()
        .then((value) {
          // message sent successfully
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function createCustomMessage() {
  const { data: message } = await MessageRepository.createMessage({
    subChannelId: 'subChannelId',
    dataType: 'custom',
    data: { customKey: 'customValue' },
  })
  console.log(message)
}
```

</CodeGroup>
