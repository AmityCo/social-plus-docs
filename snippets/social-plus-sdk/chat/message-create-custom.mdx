{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-create-custom | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun messageCreateCustom() {
        // TODO: implement message create custom using Social Plus SDK
    }
```

```swift iOS
func messageCreateCustom() {
        let channelId = "channelId"
        let customData: [String: Any] = ["type": "location", "latitude": 37.7749, "longitude": -122.4194]
        
        let repository = AmityMessageRepository(client: AmityManager.shared.client!)
        let builder = AmityCustomMessageCreationBuilder()
        builder.setCustomData(customData)
        
        repository.createMessage(withChannelId: channelId, builder: builder) { message, error in
            if let error = error {
                print("Error creating custom message: \(error)")
                return
            }
            print("Custom message created: \(message?.messageId ?? "")")
        }
    }
```

```dart Flutter
void createCustomMessage(String channelId) {
    AmityChatClient.newMessageRepository()
        .createMessage(channelId)
        .custom({'type': 'poll', 'pollId': 'abc123'})
        .send()
        .then((value) {
          // message sent successfully
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function createCustomMessage() {
  const { data: message } = await MessageRepository.createMessage({
    subChannelId: 'subChannelId',
    dataType: 'custom',
    data: { customKey: 'customValue' },
  })
  console.log(message)
}
```

</CodeGroup>
