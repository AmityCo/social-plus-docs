{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-member-remove | updated: 2026-04-30 */}

<CodeGroup>

```kotlin Android
fun removeMembers(channelId: String, userIds: List<String>) {
        AmityChatClient.newChannelRepository()
            .membership(channelId = channelId)
            .removeMembers(userIds = userIds)
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let channelMembership = AmityChannelMembership(client: client, andChannel: "<channel-id>")
        do {
            let success = try await channelMembership.removeMembers(["<user-id>"])
        } catch {
            // Handle error here
        }
```

```dart Flutter
void removeMembers(String channelId) {
    AmityChatClient.newChannelRepository()
        .membership(channelId)
        .removeMembers(['userId1', 'userId2'])
        .then((value) {
          // handle result
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function removeChannelMembers() {
  await ChannelRepository.removeMembers('channelId', ['userId1', 'userId2'])
}
```

</CodeGroup>
