{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: live-channel-member-mute | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun muteMember(channelId: String, userId: String, mutePeriodSeconds: Long = -1) {
        AmityChatClient.newChannelRepository()
            .moderation(channelId = channelId)
            .muteMembers(userIds = listOf(userId), mutePeriod = mutePeriodSeconds)
            .doOnComplete {
                // member muted
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```dart Flutter
void muteMembers(String channelId, List<String> userIds, {int mutePeriod = 300}) {
    AmityChatClient.newChannelRepository()
        .moderation(channelId)
        .muteMembers(userIds, mutePeriod)
        .then((_) {
          // members muted in live channel
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

</CodeGroup>
