{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: edit_message_with_mentions | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun editMessageWithMentions() {
        // Edit a chat message with user mentions
        AmityChatClient.newMessageRepository()
            .editMessage(messageId = "messageId")
            .text("Updated message with @mention")
            .mentionUsers(listOf("userId1"))
            .build()
            .update()
            .subscribe()
    }
```

```dart Flutter
void editMessageWithMentions() {
    final messageRepo = AmityChatClient.newMessageRepository();
    messageRepo
        .editMessage('messageId')
        .text('Updated message @user1')
        .withMentions([AmityMentionee.user('userId1')])
        .apply()
        .then((msg) => print('Message edited: \${msg.messageId}'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function editMessageWithMentions() {
  await MessageRepository.updateMessage({
    messageId: 'messageId123',
    data: { text: 'Updated message mentioning @jane' },
    mentionees: [{ type: 'user', userIds: ['userId456'] }],
  });
}
```

</CodeGroup>
