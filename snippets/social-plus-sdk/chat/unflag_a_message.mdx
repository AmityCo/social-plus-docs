{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: unflag_a_message | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun unflagAMessage() {
        AmityChatClient.newSubChannelRepository()
            .unflagMessage(messageId = "messageId")
            .subscribe({
                // Message unflagged successfully
            }, { error ->
                // Handle error
            })
    }
```

```dart Flutter
void unflagAMessage() {
    AmityChatClient.newMessageRepository()
        .unflagMessage(messageId: 'messageId')
        .then((_) {
      // Message unflagged
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function unflagAMessage() {
  await MessageRepository.unflagMessage({ messageId: 'messageId' })
  console.log('Message unflagged successfully')
}
```

</CodeGroup>
