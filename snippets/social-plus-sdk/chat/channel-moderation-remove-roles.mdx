{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-moderation-remove-roles | updated: 2026-05-01 */}

<CodeGroup>

```kotlin Android
fun removeRoles(channelId: String, roles: List<String>, userIds: List<String>) {
        AmityChatClient.newChannelRepository()
            .moderation(channelId = channelId)
            .removeRole(role = roles.first(), userIds = userIds)
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let channelModeration = AmityChannelModeration(client: client, andChannel: "<channel-id>")
        do {
            let success = try await channelModeration.removeRole("<role>", userIds: ["<user-id>"])
        } catch {
            // Handle error here
        }
```

```dart Flutter
void removeRoles(String channelId) {
    AmityChatClient.newChannelRepository()
        .moderation(channelId)
        .removeRole('channel-moderator', ['userId1', 'userId2'])
        .then((value) {
          // handle result
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function removeChannelRoles() {
  await ChannelRepository.removeRole('channelId', 'moderator', ['userId1'])
}
```

</CodeGroup>
