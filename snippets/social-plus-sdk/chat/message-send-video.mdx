{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-send-video | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun messageSendVideo() {
        val videoUri = android.net.Uri.parse("content://path/to/video.mp4")
        AmityChatClient.newMessageRepository()
            .createMessage("channelId")
            .with()
            .video(videoUri)
            .build()
            .send()
            .subscribe()
    }
```

```swift iOS
func messageSendVideo() {
        let messageRepository = AmityMessageRepository(client: AmityManager.shared.client!)
        guard let videoUrl = Bundle.main.url(forResource: "sample_video", withExtension: "mp4") else { return }
        let builder = AmityVideoMessageBuilder(videoFileUrl: videoUrl)
        messageRepository.createMessage(
            withChannelId: "channelId123",
            builder: builder
        ) { message, error in
            guard let message = message else { return }
            print("Video message sent: \(message.messageId)")
        }
    }
```

```dart Flutter
void messageSendVideo() async {
    final fileInfo = await AmityCoreClient.newFileRepository()
        .uploadVideo('path/to/video.mp4');
    await AmityChatClient.newMessageRepository()
        .createMessage('channelId')
        .video(fileInfo)
        .send();
    print('Video message sent');
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function messageSendVideo(channelId: string, videoFileId: string, thumbnailFileId?: string) {
  const message = await MessageRepository.createMessage({
    channelId,
    dataType: 'video',
    data: {
      fileId: videoFileId,
      thumbnailFileId,
    },
  })
  console.log('Video message sent:', message.messageId)
  return message
}
```

</CodeGroup>
