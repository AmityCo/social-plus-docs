{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: unsync_all_channel_presence | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun unsyncAllChannelPresence() {
        // Perform unsync_all_channel_presence operation
        AmityChatClient.newChannelRepository()
            .getChannel("channelId")
            .asFlowable()
            .subscribe({ channel -> }, { error -> })
    }
```

```swift iOS
override func viewWillDisappear(_ animated: Bool) {
        super.viewWillDisappear(animated)
        
        // Stop syncing presence for all users as user
        // is navigating to another screen.
        channelPresenceRepo.unsyncAllChannelPresence()
    }
```

```dart Flutter
void unsyncAllChannelPresence() {
    AmityChatClient.newChannelRepository()
        .unsyncAllPresence()
        .then((_) {
      // All channel presences unsynced
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function unsyncAllChannelPresence() {
  ChannelRepository.Presence.unsyncAllChannelPresence()
}
```

</CodeGroup>
