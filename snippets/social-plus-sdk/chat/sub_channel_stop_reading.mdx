{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: sub_channel_stop_reading | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun subChannelStopReading() {
        AmityChatClient.newSubChannelRepository()
            .stopReadingSubChannel("subChannelId")
            // Stops syncing read receipts for the sub-channel
    }
```

```dart Flutter
void subChannelStopReading() {
    AmityChatClient.newSubChannelRepository()
        .stopReading(subChannelId: 'subChannelId')
        .then((_) {
      // Stopped reading sub-channel
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function subChannelStopReading() {
  const channelRepository = new ChannelRepository();
  await channelRepository.stopReadingSubChannel('subChannelId');
  console.log('Stopped reading sub-channel');
}
```

</CodeGroup>
