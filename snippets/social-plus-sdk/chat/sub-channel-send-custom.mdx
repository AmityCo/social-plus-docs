{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: sub-channel-send-custom | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void subChannelSendCustom() {
    final messageRepo = AmityChatClient.newMessageRepository();
    messageRepo
        .createMessage('subChannelId')
        .custom(data: {'key': 'value'})
        .send()
        .then((AmityMessage message) {
          print('Custom message sent to sub-channel: \${message.messageId}');
        });
  }
```

```typescript TypeScript
import { SubChannelRepository, MessageRepository } from '@amityco/ts-sdk'

async function subChannelSendCustom() {
  const messageCreator = MessageRepository.createMessage('subChannelId', {
    dataType: 'custom',
    data: { type: 'location', lat: 13.756, lng: 100.501 },
  });
  messageCreator.on('dataUpdated', (message) => {
    console.log('Custom message sent to sub-channel:', message);
  });
}
```

</CodeGroup>
