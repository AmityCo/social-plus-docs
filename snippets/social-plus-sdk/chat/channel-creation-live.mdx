{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-creation-live | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun channelCreationLive() {
        AmityChatClient.newChannelRepository()
            .createChannel("liveChannelId")
            .liveType()
            .build()
            .create()
            .subscribe()
    }
```

```swift iOS
let channelRepository = AmityChannelRepository()
        let builder = AmityLiveChannelBuilder()
        builder.setId("my-live-channel")
        builder.setDisplayName("My Live Channel")
        channelRepository.createChannel(with: builder) { channel, error in
            if let error = error { print("Error: \(error!)"); return }
            print("Live channel: \(channel?.channelId ?? "")")
        }
```

```dart Flutter
void createLiveChannel() {
    // create channel by specifying channelId
    AmityChatClient.newChannelRepository()
        .createChannel()
        .liveType()
        .withChannelId("sorbh1234")
        .displayName('Weekly Promo') //Optional
        .metadata({'key': 'value'}) //Optional
        .tags(['Promotion', 'New Arrival']) //Optional
        .create()
        .then((AmityChannel channel) => {
              //handle result
            })
        .onError((error, stackTrace) => {
              //handle error
            });
    // create channel and let SDK handle channelId generation
    AmityChatClient.newChannelRepository()
        .createChannel()
        .liveType()
        .withDisplayName('Weekly Promo')
        .metadata({'key': 'value'}) //Optional
        .tags(['Promotion', 'New Arrival']) //Optional
        .create()
        .then((AmityChannel channel) => {
              //handle result
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function channelCreationLive() {
  const client = createClient('apiKey', 'region')
  await client.login({ userId: 'user123', displayName: 'User 123' })
  const channel = await client.channel.createChannel({
    type: 'live',
    channelId: 'my-live-channel',
    displayName: 'Live Stream',
  })
  console.log('Live channel created:', channel.channelId)
}
```

</CodeGroup>
