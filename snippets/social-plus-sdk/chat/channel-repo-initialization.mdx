{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-repo-initialization | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun channelRepoInitialization() {
        val channelRepository = AmityChatClient.newChannelRepository()
    }
```

```swift iOS
let channelRepository = AmityChannelRepository()
        let liveObject = channelRepository.getChannel("channelId")
        liveObject.observe { liveObject, error in
            guard let channel = liveObject.object else { return }
            print("Channel initialized: \(channel.channelId)")
        }
```

```dart Flutter
void initChannelRepo() {
    final channelRepository = AmityChatClient.newChannelRepository();
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function channelRepoInitialization() {
  const client = createClient('apiKey', 'region')
  await client.login({ userId: 'user123', displayName: 'User 123' })
  const channelRepo = client.channel
  console.log('Channel repository initialized:', !!channelRepo)
}
```

</CodeGroup>
