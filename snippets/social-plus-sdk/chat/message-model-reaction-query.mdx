{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-model-reaction-query | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun messageModelReactionQuery() {
        // TODO: implement Message model reaction query
    }
```

```swift iOS
func messageModelReactionQuery() {
        let client = AmityClient.shared
        let messageRepo = AmityMessageRepository(client: client)
        let collection = messageRepo.getReactions(
            referenceId: "messageId",
            referenceType: .message,
            reactionName: nil
        )
    }
```

```dart Flutter
void messageModelReactionQuery() {
    final messageId = 'messageId';
    AmityCoreClient.newChannelRepository()
        .message('')
        .messageId(messageId)
        .react()
        .getReactions()
        .asStream()
        .listen((reactions) => print('Reactions: \${reactions.length}'));
  }
```

```typescript TypeScript
import { ReactionRepository } from '@amityco/ts-sdk'

function queryMessageReactions() {
  const liveCollection = ReactionRepository.queryReactions({
    referenceType: 'message',
    referenceId: 'messageId',
    reactionName: 'like',
  })
  liveCollection.on('dataUpdated', (reactions) => {
    console.log(reactions)
  })
  return liveCollection
}
```

</CodeGroup>
