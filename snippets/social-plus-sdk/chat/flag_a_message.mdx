{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: flag_a_message | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun flagAMessage() {
        // Flag a message for moderation review
        AmityChatClient.newMessageRepository()
            .flagMessage(messageId = "messageId")
            .subscribe()
    }
```

```dart Flutter
void flagAMessage() {
    final messageRepo = AmityChatClient.newMessageRepository();
    messageRepo
        .flagMessage('messageId')
        .then((_) => print('Message flagged'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function flagAMessage() {
  await MessageRepository.flagMessage('messageId123');
  console.log('Message flagged successfully');
}
```

</CodeGroup>
