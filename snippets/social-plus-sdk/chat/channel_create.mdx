{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel_create | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun channelCreate() {
        AmityChatClient.newChannelRepository()
            .createChannel("channelId")
            .communityType()
            .displayName("My Channel")
            .build()
            .create()
            .subscribe()
    }
```

```swift iOS
let channelRepository = AmityChannelRepository()
        let builder = AmityCommunityChannelBuilder()
        builder.setId("new-channel-id")
        builder.setDisplayName("New Channel")
        channelRepository.createChannel(with: builder) { channel, error in
            if let error = error { print("Error creating channel: \(error)"); return }
            print("Channel created: \(channel?.channelId ?? "")")
        }
```

```dart Flutter
void channelCreate() async {
    final channelRepo = AmityChatClient.newChannelRepository();
    final channel = await channelRepo
        .createChannel()
        .publicChannel()
        .displayName('My Channel')
        .create();
    print('Channel created: \${channel.channelId}');
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function channelCreate(displayName: string, userIds: string[]) {
  const channel = await ChannelRepository.createChannel({
    displayName,
    type: 'community',
    userIds,
  })
  console.log('Channel created:', channel.data.channelId)
}
```

</CodeGroup>
