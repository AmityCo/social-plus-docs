{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_specific_channel | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getSpecificChannel() {
        AmityChatClient.newChannelRepository()
            .getChannel("channelId")
            .subscribe { channel ->
                // Channel retrieved successfully
            }
    }
```

```swift iOS
func getSpecificChannel() {
        let channelRepository = AmityChannelRepository(client: AmityManager.shared.client!)
        let liveObject = channelRepository.getChannel("channelId123")
        liveObject.observe { liveObject, error in
            guard let channel = liveObject.object else { return }
            print("Channel display name: \(channel.displayName ?? "")")
        }
    }
```

</CodeGroup>
