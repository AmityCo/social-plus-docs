{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-channel-unread-count | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getChannelUnreadCount(channelId: String) {
        AmityChatClient.newChannelRepository()
            .getChannel(channelId = channelId)
            .doOnNext { channel: AmityChannel ->
                val unreadCount = channel.getUnreadCount()
                val mentionCount = channel.getUnreadMentionCount()
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let channelRepository = AmityChannelRepository(client: AmityManager.shared.client!)
        let liveObject = channelRepository.getChannelLive(withId: "channelId")
        liveObject.observe { liveObject, error in
            guard let channel = liveObject.object else { return }
            print("Unread count: \(channel.unreadCount)")
        }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

function getChannelUnreadCount() {
  ChannelRepository.getChannel('channelId', ({ data: channel }) => {
    console.log('Unread count:', channel.unreadCount)
  })
}
```

</CodeGroup>
