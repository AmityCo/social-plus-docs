{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: check_message_sending_status | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun checkMessageSendingStatus() {
        AmityChatClient.newMessageRepository()
            .getMessage("messageId")
            .subscribe { message ->
                val syncState = message.getSyncState()
                val isSent = syncState == AmitySyncState.SYNCED
            }
    }
```

```swift iOS
let messageRepository = AmityMessageRepository(client: AmityClient.shared)
        let liveObject = messageRepository.getMessage(withId: "messageId")
        liveObject.observe { liveObject, error in
            guard let message = liveObject.object else { return }
            print("Message sync state: \(message.syncState)")
        }
```

```dart Flutter
void checkMessageSendingStatus() {
    AmityChatClient.newMessageRepository()
        .getMessage('messageId')
        .then((message) {
          final syncState = message.syncState;
        })
        .onError((error, stackTrace) { });
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

function checkMessageSendingStatus(channelId: string) {
  const liveCollection = MessageRepository.getMessages(
    { channelId, limit: 20 },
    ({ data: messages, loading, error }) => {
      if (error) { console.error(error); return; }
      if (!loading) {
        messages.forEach(msg => {
          console.log('Message:', msg.messageId, 'Sync state:', msg.syncState);
        });
      }
    }
  );
  return liveCollection;
}
```

</CodeGroup>
