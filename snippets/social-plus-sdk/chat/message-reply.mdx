{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-reply | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun replyMessage(messageRepository: AmityMessageRepository) {
        messageRepository.createTextMessage(
            subChannelId = "subChannelId",
            text = "What's up Social Cloud!!"
        )
            .parentId(parentId = "parentMessageId")
            .build()
            .send()
            .doOnComplete {
                // Void
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let messageRepository = AmityMessageRepository()
        messageRepository.createTextMessage(withChannelId: "channelId", subChannelId: "subChannelId", text: "This is a reply", tags: nil, parentId: "parentMessageId") { message, error in
            if let error = error {
                print("Error creating reply: \(error)")
            } else {
                print("Reply created: \(message?.messageId ?? "")")
            }
        }
```

```dart Flutter
void messageReply() async {
    await AmityChatClient.newMessageRepository()
        .createMessage('channelId')
        .text('This is a reply')
        .parentId('parentMessageId')
        .send();
    print('Reply sent successfully');
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function messageReply(channelId: string, parentMessageId: string, text: string) {
  const message = await MessageRepository.createMessage({
    channelId,
    dataType: 'text',
    data: { text },
    parentId: parentMessageId,
  })
  console.log('Reply sent:', message.messageId)
  return message
}
```

</CodeGroup>
