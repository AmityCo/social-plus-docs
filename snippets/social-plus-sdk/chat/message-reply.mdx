{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-reply | updated: 2026-04-30 */}

<CodeGroup>

```kotlin Android
fun replyMessage(messageRepository: AmityMessageRepository) {
        messageRepository.createTextMessage(
            subChannelId = "subChannelId",
            text = "What's up Social Cloud!!"
        )
            .parentId(parentId = "parentMessageId")
            .build()
            .send()
            .doOnComplete {
                // Void
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```dart Flutter
void replyMessage() {
    // Reply message
    AmityChatClient.newMessageRepository()
        .createMessage('channelId')
        .parentId('parentMessageId')
        .text("What's up Social Cloud!!")
        .send()
        .then((_) {
      //handle result
    }).onError((error, stackTrace) {
      //handle error
    });
  }
```

</CodeGroup>
