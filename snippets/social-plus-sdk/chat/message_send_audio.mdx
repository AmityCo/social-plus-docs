{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message_send_audio | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun messageSendAudio() {
        // Create/send operation via repository
        val audioFile = java.io.File("audio.aac")
        AmityChatClient.newMessageRepository()
            .createMessage(channelId = "channelId")
            .with()
            .audio(audioFile)
            .build()
            .send()
            .subscribe()
    }
```

```swift iOS
let messageRepo = AmityMessageRepository(client: AmityManager.shared.client!)
        let builder = AmityAudioMessageCreationBuilder()
        builder.setAudio(fileURL: URL(fileURLWithPath: "/path/to/audio.m4a"))
        messageRepo.createMessage(channelId: "channelId123", builder: builder) { message, error in
            if let error = error { print("Error: \(error)"); return }
            print("Audio message created: \(message?.messageId ?? "")")
        }
```

```dart Flutter
void messageSendAudio() async {
    final fileInfo = await AmityCoreClient.newFileRepository()
        .uploadFile('path/to/audio.mp3');
    await AmityChatClient.newMessageRepository()
        .createMessage('channelId')
        .audio(fileInfo)
        .send();
    print('Audio message sent');
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function messageSendAudio(channelId: string, audioFileId: string, durationMs: number) {
  const message = await MessageRepository.createMessage({
    channelId,
    dataType: 'audio',
    data: {
      fileId: audioFileId,
      duration: durationMs,
    },
  })
  console.log('Audio message sent:', message.messageId)
  return message
}
```

</CodeGroup>
