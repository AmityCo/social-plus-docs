{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: jump_to_message | updated: 2026-05-02 */}

<CodeGroup>

```swift iOS
var queryOptions: AmityMessageQueryOptions!

        // Step 1: Query Messages from a Subchannel

        // Create initial query options to fetch messages from a subchannel, sorted by the last created.
        queryOptions = AmityMessageQueryOptions(subChannelId: "subChannel-123", sortOption: .lastCreated)

        // Perform the initial query to load messages.
        queryMessages(queryOptions: queryOptions)
        
        
        // Step 2: Jump to a Specific Message

        // If you want to jump from a reply message to the parent message, set the parentMessageId as the aroundMessageId to jump.
        // If you want to jump to a specific message, use the messageId you want to jump as the aroundMessageId.
        queryOptions = AmityMessageQueryOptions(subChannelId: "subChannel-123", aroundMessageId: "messageId-to-jump", sortOption: .lastCreated)

        // Call the queryMessages function with the updated query options to jump to the desired message.
        queryMessages(queryOptions: queryOptions)

        // Function to Query Messages with Provided Options

        /**
         Queries messages using the provided query options and observes changes in the message collection.

         - Parameters:
            - queryOptions: The query options specifying the criteria for fetching messages.
         */
        func queryMessages(queryOptions: AmityMessageQueryOptions) {
            // Invalidate the existing observer token to stop the previous observation.
            token.invalidate()
            
            // Fetch messages using the specified query options and observe changes.
            token = messageRepository.getMessages(options: queryOptions).observe({ collection, change, error in
                if let error = error {
                    print("Error: \(error).")
                    return
                }
                
                // Use the 'collection' as the data source for the message table view.
                // Reload the message table view when the data source changes due to new messages or updates.
            })
        }
```

</CodeGroup>
