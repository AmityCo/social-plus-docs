{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: sub-channel-send-text | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void subChannelSendText() {
    final messageRepo = AmityChatClient.newMessageRepository();
    messageRepo
        .createMessage('subChannelId')
        .text('Hello sub-channel!')
        .send()
        .then((AmityMessage message) {
          print('Text sent to sub-channel: \${message.messageId}');
        });
  }
```

```typescript TypeScript
import { SubChannelRepository, MessageRepository } from '@amityco/ts-sdk'

async function subChannelSendText() {
  const messageCreator = MessageRepository.createMessage('subChannelId', {
    dataType: 'text',
    data: { text: 'Hello from sub-channel!' },
  });
  messageCreator.on('dataUpdated', (message) => {
    console.log('Text message sent to sub-channel:', message);
  });
}
```

</CodeGroup>
