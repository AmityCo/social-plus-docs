{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create-a-channel | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createAChannel(userId: String) {
        AmityChatClient.newChannelRepository()
            .createChannel(displayName = "My Channel")
            .conversation(userId = userId)
            .build()
            .create()
            .doOnSuccess { channel: AmityChannel ->
                // AmityChannel created
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```dart Flutter
void createAChannel() {
    AmityChatClient.newChannelRepository()
        .createChannel()
        .communityType()
        .withChannelId('channelId')
        .withDisplayName('My Channel')
        .create()
        .then((channel) { })
        .onError((error, stackTrace) { });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function createChannel(channelId: string, displayName: string) {
  try {
    const { data: channel } = await ChannelRepository.createChannel({
      channelId,
      displayName,
      type: 'community',
      tags: ['public'],
      metadata: { topic: 'general' },
    });
    console.log('Channel created:', channel.channelId, channel.displayName);
    return channel;
  } catch (error) {
    console.error('Failed to create channel:', error);
  }
}
```

</CodeGroup>
