{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-set-tags | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun messageSetTags() {
        // Chat message operation
        AmityChatClient.newMessageRepository()
            .editMessage(messageId = "messageId")
            .tags(listOf("tag1", "tag2"))
            .build()
            .apply()
            .subscribe()
    }
```

```swift iOS
do {
            try await messageRepository.setTags(messageId: "<message-id>", tags: ["important", "pinned"])
        } catch {
            // Handle error here
        }
```

```dart Flutter
void messageSetTags() {
    final channelId = 'channelId';
    AmityCoreClient.newChannelRepository()
        .message(channelId)
        .text()
        .text('Tagged message')
        .tags(['tag1', 'tag2'])
        .send()
        .then((message) => print('Tagged message sent'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function setMessageTags() {
  await MessageRepository.setTags('messageId', ['important', 'pinned'])
  console.log('Tags set successfully')
}
```

</CodeGroup>
