{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-moderation-add-roles | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun addRoles(channelId: String, roles: List<String>, userIds: List<String>) {
        AmityChatClient.newChannelRepository()
            .moderation(channelId = channelId)
            .addRole(role = roles.first(), userIds = userIds)
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let channelModeration = AmityChannelModeration(client: client, andChannel: "<channel-id>")
        do {
            let success = try await channelModeration.addRole("<role>", userIds: ["<user-id>"])
        } catch {
            // Handle error here
        }
```

```dart Flutter
void addRoles(String channelId) {
    AmityChatClient.newChannelRepository()
        .moderation(channelId)
        .addRole('channel-moderator', ['userId1', 'userId2'])
        .then((value) {
          // handle result
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function addChannelRoles() {
  await ChannelRepository.addRole('channelId', 'moderator', ['userId1'])
}
```

</CodeGroup>
