{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-flag | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun flagPost(
        postRepository: AmityPostRepository,
        post: AmityPost,
        reason: AmityContentFlagReason,
    ) {
        postRepository.flagPost(
            postId = post.getPostId(),
            reason = reason
        )
            .doOnComplete {
                // Void
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let postRepository = AmityPostRepository(client: client)
        do {
            let success = try await postRepository.flagPost(withId: "<post-id>")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void postFlag() async {
    final postRepository = AmitySocialClient.newPostRepository();
    await postRepository
        .flag('postId')
        .flag();
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function postFlag(postId: string) {
  await PostRepository.flagPost(postId)
  console.log('Post flagged:', postId)
}
```

</CodeGroup>
