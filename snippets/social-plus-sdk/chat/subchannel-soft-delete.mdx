{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: subchannel-soft-delete | updated: 2026-04-30 */}

<CodeGroup>

```kotlin Android
fun softDeleteSubchannel(subChannelId: String) {
        AmityChatClient.newSubChannelRepository()
            .softDeleteSubChannel(subChannelId = subChannelId)
            .doOnComplete {
                // success
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let subChannelRepository = AmitySubChannelRepository(client: client)
        do {
            try await subChannelRepository.softDeleteSubChannel(subChannelId: "<subchannel-id>")
            print("Subchannel soft deleted")
        } catch {
            // Handle error here
        }
```

```typescript TypeScript
import { SubChannelRepository } from '@amityco/ts-sdk'

async function softDeleteSubchannel() {
  await SubChannelRepository.deleteSubChannel('subChannelId')
}
```

</CodeGroup>
