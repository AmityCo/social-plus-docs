{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: update_channel | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun updateChannel() {
        // TODO: Implement Update channel
    }
```

```swift iOS
let channelRepository = AmityChannelRepository(client: AmityManager.shared.client!)
        let builder = AmityChannelUpdateDataBuilder()
        builder.setDisplayName("Updated Channel")
        channelRepository.updateChannel("channelId", builder: builder) { channel, error in
            if let channel = channel {
                print("Channel updated: \(channel.displayName ?? "")")
            } else if let error = error {
                print("Error updating channel: \(error)")
            }
        }
```

```dart Flutter
void updateChannel() {
    AmityChatClient.newChannelRepository()
        .updateChannel('channelId')
        .displayName('Updated Channel Name')
        .update()
        .then((AmityChannel channel) {
          print('Channel updated: \${channel.channelId}');
        })
        .onError((error, stackTrace) {
          print('Error: \$error');
        });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function updateChannel() {
  const updatedChannel = await ChannelRepository.updateChannel('channelId', {
    displayName: 'Updated Channel Name',
    metadata: { topic: 'General Discussion' },
  });
  console.log('Channel updated:', updatedChannel);
}
```

</CodeGroup>
