{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel_get | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void channelGet() async {
    final channelRepo = AmityChatClient.newChannelRepository();
    final channel = await channelRepo.getChannel('channelId').first;
    print('Channel fetched: \${channel.channelId}');
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

function channelGet(channelId: string) {
  const unsubscribe = ChannelRepository.getChannel(channelId, ({ data: channel, loading, error }) => {
    if (loading) return
    if (error) { console.error(error); return }
    console.log('Channel:', channel)
    unsubscribe()
  })
}
```

</CodeGroup>
