{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: live-channel-member-role | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun addRole(channelId: String, userId: String, role: String) {
        AmityChatClient.newChannelRepository()
            .moderation(channelId = channelId)
            .addRole(role = role, userIds = listOf(userId))
            .doOnComplete {
                // role added
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }

    fun removeRole(channelId: String, userId: String, role: String) {
        AmityChatClient.newChannelRepository()
            .moderation(channelId = channelId)
            .removeRole(role = role, userIds = listOf(userId))
            .doOnComplete {
                // role removed
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```dart Flutter
void addRoleToMember(String channelId, String role, List<String> userIds) {
    AmityChatClient.newChannelRepository()
        .moderation(channelId)
        .addRole(role, userIds)
        .then((_) {
          // role added to members
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }

  void removeRoleFromMember(String channelId, String role, List<String> userIds) {
    AmityChatClient.newChannelRepository()
        .moderation(channelId)
        .removeRole(role, userIds)
        .then((_) {
          // role removed from members
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

</CodeGroup>
