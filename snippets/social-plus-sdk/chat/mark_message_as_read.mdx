{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: mark_message_as_read | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun markMessageAsRead() {
        // Mark a specific message as read
        AmityChatClient.newMessageRepository()
            .markMessageAsRead("subChannelId", "messageId")
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(io.reactivex.rxjava3.android.schedulers.AndroidSchedulers.mainThread())
            .subscribe({
                // Message marked as read
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
let channelRepository = AmityChannelRepository(client: AmityUIKitManagerInternal.shared.client)
        let subChannelId = "subChannel123"
        let messageId = "message123"
        channelRepository.markMessageAsRead(subChannelId: subChannelId, messageId: messageId) { success, error in
            print("Message marked as read: \(success)")
        }
```

```dart Flutter
void markMessageAsRead() async {
    await AmityChatClient.newMessageRepository()
        .markMessageAsRead(messageId: 'messageId');
    print('Message marked as read');
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function markMessageAsRead(channelId: string, messageId: string) {
  await MessageRepository.markMessageRead({ channelId, messageId })
  console.log('Message marked as read:', messageId)
}
```

</CodeGroup>
