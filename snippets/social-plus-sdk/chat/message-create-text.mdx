{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-create-text | updated: 2026-04-30 */}

<CodeGroup>

```swift iOS
let messageRepository = AmityMessageRepository(client: client)
        let options = AmityTextMessageCreateOptions(subChannelId: "<sub-channel-id>", text: "Hello!", tags: nil, parentId: nil, metadata: nil, mentioneesBuilder: nil)
        do {
            let message = try await messageRepository.createTextMessage(options: options)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void createTextMessage(String channelId) {
    AmityChatClient.newMessageRepository()
        .createMessage(channelId)
        .text('Hello from Flutter!')
        .send()
        .then((value) {
          // message sent successfully
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

</CodeGroup>
