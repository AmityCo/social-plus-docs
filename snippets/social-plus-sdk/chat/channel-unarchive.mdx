{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-unarchive | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun channelUnarchive() {
        // TODO: implement Channel unarchive
    }
```

```swift iOS
let channelRepository = AmityChannelRepository()
        channelRepository.unarchiveChannel(withId: "channelId") { success, error in
            if let error = error { print("Error unarchiving channel: \(error)"); return }
            print("Channel unarchived successfully")
        }
```

```dart Flutter
Future<void> unarchiveChannel(String channelId) async {
    await AmityChatClient.newChannelRepository().unarchiveChannel(channelId);
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function channelUnarchive() {
  const client = createClient('apiKey', 'region')
  await client.login({ userId: 'user123', displayName: 'User 123' })
  await client.channel.unarchiveChannel({ channelId: 'channel123' })
  console.log('Channel unarchived')
}
```

</CodeGroup>
