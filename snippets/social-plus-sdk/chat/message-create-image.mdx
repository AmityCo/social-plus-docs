{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-create-image | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createImageMessage(subChannelId: String, attachment: AmityMessageAttachment) {
        AmityChatClient.newMessageRepository()
            .createImageMessage(
                subChannelId = subChannelId,
                attachment = attachment
            )
            .build()
            .send()
            .doOnComplete { /* message sent */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let messageRepository = AmityMessageRepository(client: client)
        let imageURL = URL(string: "file:///path/to/image.jpg")!
        let options = AmityImageMessageCreateOptions(subChannelId: "<sub-channel-id>", attachment: .localURL(url: imageURL), caption: nil, fullImage: true, tags: nil, parentId: nil)
        do {
            let message = try await messageRepository.createImageMessage(options: options)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void createImageMessage(String channelId, Uri imageUri) {
    AmityChatClient.newMessageRepository()
        .createMessage(channelId)
        .image(imageUri)
        .caption('Image caption')
        .send()
        .then((value) {
          // message sent successfully
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function createImageMessage() {
  const { data: message } = await MessageRepository.createMessage({
    subChannelId: 'subChannelId',
    dataType: 'image',
    fileId: 'uploadedImageFileId',
  })
  console.log(message)
}
```

</CodeGroup>
