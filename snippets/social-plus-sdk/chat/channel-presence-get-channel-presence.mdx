{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-presence-get-channel-presence | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getChannelPresence(channelId: String) {
        AmityChatClient.newChannelRepository()
            .presence(channelId = channelId)
            .getPresence()
            .doOnNext { onlineCount ->
                // handle online count
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```dart Flutter
void getPresence(String channelId) {
    AmityChatClient.newChannelRepository()
        .presence(channelId)
        .getPresence()
        .then((presence) {
          final isAnyMemberOnline = presence?.isAnyMemberOnline ?? false;
          // use presence data
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

function getSpecificChannelPresence(channelId: string) {
  const unsubscribe = ChannelRepository.getChannel(channelId, ({ data: channel, loading }) => {
    if (loading) return

    // Presence-related fields on the channel object
    const presenceData = {
      channelId: channel.channelId,
      memberCount: channel.memberCount,
      activeMemberCount: channel.activeMemberCount,
      lastActivity: channel.lastActivity,
    }

    console.log('Channel presence data:', presenceData)
  })

  return unsubscribe
}
```

</CodeGroup>
