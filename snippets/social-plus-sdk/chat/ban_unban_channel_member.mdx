{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: ban_unban_channel_member | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun banUnbanChannelMember() {
        AmityChatClient.newChannelRepository()
            .moderation("channelId")
            .banMembers(userIds = listOf("userId"))
            .andThen(
                AmityChatClient.newChannelRepository()
                    .moderation("channelId")
                    .unbanMembers(userIds = listOf("userId"))
            )
            .subscribe()
    }
```

```swift iOS
let channelRepository = AmityChannelRepository()
        // Ban member
        channelRepository.banMembers(["userId"], channelId: "channelId") { success, error in
            if let error = error { print("Error banning: \(error)"); return }
            print("Member banned")
        }
        // Unban member
        channelRepository.unbanMembers(["userId"], channelId: "channelId") { success, error in
            if let error = error { print("Error unbanning: \(error)"); return }
            print("Member unbanned")
        }
```

```dart Flutter
void banUnbanChannelMember() {
    AmityChatClient.newChannelRepository()
        .moderation('channelId')
        .banMember('userId')
        .then((_) { })
        .onError((error, stackTrace) { });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function banUnbanChannelMember(channelId: string, userId: string, action: 'ban' | 'unban') {
  try {
    if (action === 'ban') {
      await ChannelRepository.Membership.banMembers(channelId, [userId]);
      console.log('User banned from channel:', userId);
    } else {
      await ChannelRepository.Membership.unbanMembers(channelId, [userId]);
      console.log('User unbanned from channel:', userId);
    }
  } catch (error) {
    console.error('Failed to ban/unban member:', error);
  }
}
```

</CodeGroup>
