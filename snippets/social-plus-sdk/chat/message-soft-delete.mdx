{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-soft-delete | updated: 2026-04-30 */}

<CodeGroup>

```kotlin Android
fun softDeleteMessage(messageId: String) {
        AmityChatClient.newMessageRepository()
            .softDeleteMessage(messageId = messageId)
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let messageRepository = AmityMessageRepository(client: client)
        do {
            let success = try await messageRepository.deleteMessage(withId: "<message-id>")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void softDeleteMessage(String messageId) {
    AmityChatClient.newMessageRepository()
        .softDeleteMessage(messageId: messageId)
        .then((value) {
          // message deleted
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function softDeleteMessage() {
  await MessageRepository.deleteMessage('messageId')
}
```

</CodeGroup>
