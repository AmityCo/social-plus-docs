{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message_send_video | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void messageSendVideo() async {
    final fileInfo = await AmityCoreClient.newFileRepository()
        .uploadVideo('path/to/video.mp4');
    await AmityChatClient.newMessageRepository()
        .createMessage('channelId')
        .video(fileInfo)
        .send();
    print('Video message sent');
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function messageSendVideo(channelId: string, videoFileId: string, thumbnailFileId?: string) {
  const message = await MessageRepository.createMessage({
    channelId,
    dataType: 'video',
    data: {
      fileId: videoFileId,
      thumbnailFileId,
    },
  })
  console.log('Video message sent:', message.messageId)
  return message
}
```

</CodeGroup>
