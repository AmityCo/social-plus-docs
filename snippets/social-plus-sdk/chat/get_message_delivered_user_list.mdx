{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_message_delivered_user_list | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getMessageDeliveredUserList() {
        // Query users who delivered/read a specific message
        AmityChatClient.newMessageRepository()
            .getMessage("messageId")
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(io.reactivex.rxjava3.android.schedulers.AndroidSchedulers.mainThread())
            .subscribe({ message ->
                // Access delivery/read receipts from message
            }, { error ->
                // Handle error
            })
    }
```

```dart Flutter
void getMessageDeliveredUserList() {
    AmityChannelParticipation('channelId')
        .getMessageDeliveredUserList('messageId')
        .getPagingData()
        .listen((users) {
      for (final user in users) {
        print('Delivered to: \${user.userId}');
      }
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function getMessageDeliveredUserList() {
  const liveCollection = MessageRepository.getMessageDeliveredToUsers('messageId123');
  liveCollection.on('dataUpdated', (users) => {
    console.log('Message delivered to:', users.map((u: any) => u.userId));
  });
}
```

</CodeGroup>
