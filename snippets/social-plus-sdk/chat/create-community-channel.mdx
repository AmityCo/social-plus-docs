{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create-community-channel | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void createCommunityChannel() {
    AmityChatClient.newChannelRepository()
        .createChannel()
        .communityType()
        .withChannelId('communityChannelId')
        .withDisplayName('Community Channel')
        .create()
        .then((channel) { })
        .onError((error, stackTrace) { });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function createCommunityChannel(communityId: string, channelName: string) {
  try {
    const { data: channel } = await ChannelRepository.createChannel({
      displayName: channelName,
      type: 'community',
      tags: [communityId],
      metadata: { linkedCommunityId: communityId },
    });
    console.log('Community channel created:', channel.channelId);
    return channel;
  } catch (error) {
    console.error('Failed to create community channel:', error);
  }
}
```

</CodeGroup>
