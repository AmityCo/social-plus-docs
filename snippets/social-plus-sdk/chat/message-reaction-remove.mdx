{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-reaction-remove | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun messageReactionRemove() {
        // TODO: implement message reaction remove
    }
```

```swift iOS
func messageReactionRemove() {
        let client = AmityClient.shared
        let reactionRepository = AmityReactionRepository(client: client)
        reactionRepository.removeReaction("love", referenceId: "message_id", referenceType: .message) { success, error in
            if success {
                print("Message reaction removed")
            }
        }
    }
```

```dart Flutter
void removeMessageReaction(AmityMessage message) {
    message.react()
        .removeReaction('like')
        .then((value) {
          // success
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { ReactionRepository } from '@amityco/ts-sdk'

async function removeMessageReaction() {
  await ReactionRepository.removeReaction('message', 'messageId', 'like')
}
```

</CodeGroup>
