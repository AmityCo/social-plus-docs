{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-reaction-remove | updated: 2026-05-01 */}

<CodeGroup>

```kotlin Android
fun removeReaction(
        reactionRepository: AmityReactionRepository,
        message: AmityMessage
    ) {
        // Remove a reaction
        reactionRepository.removeReaction(
            reactionReference = AmityReactionReference.MESSAGE(messageId = message.getMessageId()),
            reactionName = "like"
        )
            .doOnComplete {
                // Void
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let reactionRepository = AmityReactionRepository(client: client)
        do {
            let result = try await reactionRepository.removeReaction("like", referenceId: "<message-id>", referenceType: .message)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void removeMessageReaction(AmityMessage message) {
    message.react()
        .removeReaction('like')
        .then((value) {
          // success
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

</CodeGroup>
