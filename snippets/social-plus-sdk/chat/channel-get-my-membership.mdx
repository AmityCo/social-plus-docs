{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-get-my-membership | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
StreamSubscription? subscription;

  void observeMyMembership(String channelId) {
    subscription = AmityChatClient.newChannelRepository()
        .getChannel(channelId)
        .asStream()
        .listen((AmityChannel channel) {
          final membership = channel.currentUserMembership;
          final isMember = membership?.isMember ?? false;
          final isBanned = membership?.isBanned ?? false;
        })
        ..onError((error) {
          // Handle error
        });
  }

  void dispose() {
    subscription?.cancel();
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

function getMyMembership() {
  const unsubscribe = ChannelRepository.getMyMembership('channelId', ({ data: membership }) => {
    console.log(membership)
  })
  return unsubscribe
}
```

</CodeGroup>
