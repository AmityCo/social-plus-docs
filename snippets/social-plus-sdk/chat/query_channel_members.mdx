{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: query_channel_members | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun queryChannelMembers() {
        // Query members of a channel
        AmityChatClient.newChannelRepository()
            .membership("channelId")
            .getMembers()
            .build()
            .query()
            .asLiveData()
    }
```

```swift iOS
let client = AmityClient.shared
        let channelRepository = AmityChannelRepository(client: client)
        let collection = channelRepository.getMembers(channelId: "<channelId>", memberships: [.member], roles: [])
        collection.observe { liveCollection, _, error in
            if let error = error { print("Error: \(error)"); return }
            print("Channel members count: \(liveCollection.count())")
        }
```

```dart Flutter
void queryChannelMembers() async {
    final channelRepository = AmityChatClient.newChannelRepository();
    channelRepository
        .membership('channelId')
        .getMembers()
        .asStream()
        .listen((members) {
      print('Members: \${members.length}');
    });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function queryChannelMembersUnderscore() {
  const params = {
    channelId: 'channelId123',
    filter: 'all' as const,
    sortBy: 'displayName' as const,
  };

  const liveCollection = ChannelRepository.getMembers(params);
  liveCollection.on('dataUpdated', members => {
    console.log('Channel members:', members.map(m => m.userId));
  });
  liveCollection.on('dataError', err => console.error('Error querying channel members:', err));
}
```

</CodeGroup>
