{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: sub_channel_start_reading | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun subChannelStartReading() {
        AmityChatClient.newSubChannelRepository()
            .startReadingSubChannel("subChannelId")
            // Begins syncing read receipts for the sub-channel
    }
```

```dart Flutter
void subChannelStartReading() {
    AmityChatClient.newSubChannelRepository()
        .startReading(subChannelId: 'subChannelId')
        .then((_) {
      // Started reading sub-channel
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function subChannelStartReading() {
  const channelRepository = new ChannelRepository();
  await channelRepository.startReadingSubChannel('subChannelId');
  console.log('Started reading sub-channel');
}
```

</CodeGroup>
