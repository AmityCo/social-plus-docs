{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: kotlin-extensions | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
suspend fun login(
        userId: String,
        sessionHandler: SessionHandler
    ) {
        AmityCoreClient.login(userId = userId, sessionHandler)
            .build()
            .submit()
            .await()
    }
```

```swift iOS
// Note: Demonstrates equivalent iOS async patterns using Combine
        let userRepository = AmityUserRepository(client: AmityUIKitManagerInternal.shared.client)
        let userId = "user123"
        let liveObject = userRepository.getUser(userId)
        liveObject.observe { liveObject, _ in
            if let user = liveObject.object {
                print("User updated: \(user.displayName ?? "")")
            }
        }
```

```dart Flutter
void kotlinExtensions() async {
    // Flutter SDK extension utilities for Social Plus SDK
    final userRepo = AmityCoreClient.newUserRepository();
    final liveObject = userRepo.getUser('userId123');
    liveObject.stream.listen((user) {
      print('User updated: \${user.displayName}');
    });
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function kotlinExtensions() {
  // Update user profile information
  const { data: updatedUser, error } = await UserRepository.updateUser({
    userId: 'user-id-123',
    displayName: 'Updated Name',
    description: 'Updated bio',
    avatarFileId: 'avatar-file-id',
    metadata: { theme: 'dark' },
  })
  if (error) {
    console.error('Failed to update user:', error)
  } else {
    console.log('User updated:', updatedUser?.displayName)
  }
}
```

</CodeGroup>
