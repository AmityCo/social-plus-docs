{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-get-list | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void messageGetList() {
    final controller = PagingController<AmityMessage>(
      pageFuture: (token) => AmityChatClient.newMessageRepository()
          .getMessages(channelId: 'channelId')
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    );
    controller.fetchNextPage();
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function messageGetList(channelId: string) {
  const { data: messages, onNextPage } = await MessageRepository.getMessages({
    channelId,
    limit: 20,
  })
  console.log('Messages:', messages)
  return { messages, onNextPage }
}
```

</CodeGroup>
