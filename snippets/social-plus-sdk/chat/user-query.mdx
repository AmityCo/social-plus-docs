{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-query | updated: 2026-04-30 */}

<CodeGroup>

```kotlin Android
fun queryUsers(userRepository: AmityUserRepository) {
        userRepository.getUsers()
            .sortBy(sortOption = AmityUserSortOption.DISPLAYNAME) // optional
            .build()
            .query()
            .doOnNext { users: PagingData<AmityUser> ->
                // PagingData<AmityUser>
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let userRepository = AmityUserRepository(client: client)
        var token: AmityNotificationToken?
        token = userRepository.getUsers(.displayName).observe { collection, change, error in
            // Handle collection updates
        }
```

```dart Flutter
List<AmityUser> _users = [];
  late PagingController<AmityUser> _controller;
  final scrollController = ScrollController();

  void queryUsers(String keyword) {
    _controller = PagingController(
      pageFuture: (token) => AmityCoreClient.newUserRepository()
          .searchUserByDisplayName(keyword)
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    )..addListener(() {
        if (_controller.error == null) {
          _users
            ..clear()
            ..addAll(_controller.loadedItems);
        }
      });

    scrollController.addListener(() {
      if (scrollController.position.pixels ==
              scrollController.position.maxScrollExtent &&
          _controller.hasMoreItems) {
        _controller.fetchNextPage();
      }
    });
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function queryUsers() {
  const liveCollection = UserRepository.getUsers({ sortBy: 'displayName' })
  liveCollection.on('dataUpdated', (users) => {
    console.log(users)
  })
}
```

</CodeGroup>
