{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_user-unread | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getUserUnread() {
        // Retrieve the total unread count across all channels for the current user
        AmityChatClient.newChannelRepository()
            .getTotalUnreadCount()
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(io.reactivex.rxjava3.android.schedulers.AndroidSchedulers.mainThread())
            .subscribe({ totalUnread ->
                // Update badge with totalUnread count
            }, { error ->
                // Handle error
            })
    }
```

```dart Flutter
void getUserUnread() {
    // TODO: implement SDK operation
    throw UnimplementedError();
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function getUserUnread() {
  // Observe total unread count across all channels for the current user
  const liveObject = ChannelRepository.getTotalUnreadCount()
  const unsubscribe = liveObject.subscribe(({ data, loading }) => {
    if (!loading && data) {
      console.log('User total unread:', data.unreadCount)
    }
  })
  return unsubscribe
}
```

</CodeGroup>
