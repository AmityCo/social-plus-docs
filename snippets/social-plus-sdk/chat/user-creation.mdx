{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-creation | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun userCreation() {
        com.amityco.socialcloud.sdk.AmityCoreClient.login("userId123")
            .displayName("John Doe")
            .build()
            .subscribe()
    }
```

```swift iOS
AmityManager.shared.client?.login(userId: "newUserId", displayName: "New User", authToken: nil, sessionHandler: nil) { success, error in
            if success {
                print("User session created successfully")
            } else if let error = error {
                print("User creation failed: \(error)")
            }
        }
```

```dart Flutter
void createUser() async {
    await AmityCoreClient.login('userId')
        .displayName('userDisplayName')
        .submit();
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function userCreation() {
  const client = createClient('YOUR_API_KEY')
  await client.login({
    userId: 'userId',
    displayName: 'Display Name',
    authToken: 'authToken',
  })
  console.log('User created and logged in')
}
```

</CodeGroup>
