{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-channel | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getChannel() {
        AmityChatClient.newChannelRepository()
            .getChannel("channelId123")
            .subscribe { channel ->
                println("Channel: \${channel.getDisplayName()}")
            }
    }
```

```swift iOS
func getChannel() {
        let client = AmityClient.shared
        let channelRepo = AmityChannelRepository(client: client)
        let token = channelRepo.getChannel(withChannelId: "channelId")
            .observe { liveObject, error in
                guard let channel = liveObject.object else { return }
                print("Channel display name: \(channel.displayName ?? "")")
            }
    }
```

```dart Flutter
void getChannel() {
    AmityChatClient.newChannelRepository()
        .getChannel('channelId')
        .asStream()
        .listen((AmityChannel channel) {
      print('Channel: \${channel.displayName}, members: \${channel.memberCount}');
    });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

function getChannel() {
  ChannelRepository.getChannel('channelId', ({ data: channel }) => {
    console.log('Channel:', channel)
  })
}
```

</CodeGroup>
