{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-create-live | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void channelCreateLive() {
    AmityChatClient.newChannelRepository()
        .createChannel()
        .liveType()
        .withChannelId('channelId')
        .withDisplayName('Live Channel')
        .create()
        .then((channel) { })
        .onError((error, stackTrace) { });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function createLiveChannel() {
  try {
    const { data: channel } = await ChannelRepository.createChannel({
      channelId: 'my-live-channel',
      displayName: 'Live Event Channel',
      type: 'live',
      metadata: { eventName: 'Product Launch' },
    });
    console.log('Live channel created:', channel.channelId);
    return channel;
  } catch (error) {
    console.error('Failed to create live channel:', error);
  }
}
```

</CodeGroup>
