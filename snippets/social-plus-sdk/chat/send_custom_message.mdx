{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: send_custom_message | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void sendCustomMessage() {
    final messageRepo = AmityChatClient.newMessageRepository();
    messageRepo
        .createMessage('channelId')
        .custom(data: {'type': 'custom', 'payload': 'value'})
        .send()
        .then((AmityMessage message) {
          print('Custom message sent: \${message.messageId}');
        });
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function sendCustomMessage() {
  const messageCreator = MessageRepository.createMessage('channelId', {
    dataType: 'custom',
    data: { customField: 'customValue' },
  });
  messageCreator.on('dataUpdated', (message) => {
    console.log('Custom message sent:', message);
  });
}
```

</CodeGroup>
