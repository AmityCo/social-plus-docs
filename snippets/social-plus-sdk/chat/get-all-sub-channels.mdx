{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-all-sub-channels | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getAllSubChannels() {
        AmityChatClient.newSubChannelRepository()
            .getSubChannels("channelId123")
            .subscribe { subChannels ->
                println("Sub-channels: \${subChannels.size}")
            }
    }
```

```swift iOS
func getAllSubChannels() {
        let client = AmityClient.shared
        let subChannelRepo = AmitySubChannelRepository(client: client)
        let queryOptions = AmitySubChannelQueryOptions(channelId: "channelId")
        let collection = subChannelRepo.getSubChannels(options: queryOptions)
    }
```

```typescript TypeScript
import { SubChannelRepository } from '@amityco/ts-sdk'

function getAllSubChannels() {
  const liveCollection = SubChannelRepository.getSubChannels({ channelId: 'channelId' })

  liveCollection.on('dataUpdated', (subChannels) => {
    console.log('Sub-channels:', subChannels)
  })

  liveCollection.on('dataError', (error) => {
    console.error('Failed to get sub-channels:', error)
  })

  return () => liveCollection.dispose()
}
```

</CodeGroup>
