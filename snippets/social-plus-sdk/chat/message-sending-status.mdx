{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-sending-status | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getMessageState(message: AmityMessage) {
        when (message.getState()) {
            AmityMessage.State.CREATED -> {
                // saved message into local cache, but not sending to server yet
            }
            AmityMessage.State.UPLOADING -> {
                // sent request to server, uploading the image/file/audio/video
            }
            AmityMessage.State.SYNCING -> {
                // uploaded image/file/audio/video to server, creating a message
            }
            AmityMessage.State.SYNCED -> {
                // message is successfully created and synced
            }
            AmityMessage.State.FAILED -> {
                // send request to server failed
                // or upload image/file/audio/video to server failed
            }
        }
    }
```

```swift iOS
let messageRepo = AmityMessageRepository(client: AmityManager.shared.client!)
        let liveObject = messageRepo.getMessage("messageId123")
        liveObject.observe { liveObject, error in
            guard let message = liveObject.object else { return }
            print("Sending status: \(message.syncState.rawValue)")
        }
```

```dart Flutter
void messageSendingStatus() {
    final channelId = 'channelId';
    final message = await AmityCoreClient.newChannelRepository()
        .message(channelId)
        .text()
        .text('Hello')
        .send();
    print('Sending status: \${message.syncState}');
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

function getMessageSendingStatus(message) {
  switch (message.syncState) {
    case 'syncing':
      console.log('Message is uploading or syncing to server')
      break
    case 'synced':
      console.log('Message successfully created and synced')
      break
    case 'error':
      console.log('Message failed to send')
      break
    default:
      console.log('Message state:', message.syncState)
  }
}
```

</CodeGroup>
