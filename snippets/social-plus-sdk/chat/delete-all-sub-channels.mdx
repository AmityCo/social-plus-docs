{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: delete-all-sub-channels | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun deleteAllSubChannels(channelId: String) {
        AmityChatClient.newSubChannelRepository()
            .getSubChannels(channelId = channelId)
            .build()
            .query()
            .firstElement()
            .doOnSuccess { subChannels ->
                subChannels.forEach { subChannel ->
                    AmityChatClient.newSubChannelRepository()
                        .deleteSubChannel(subChannelId = subChannel.getSubChannelId())
                        .subscribe()
                }
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let channelRepository = AmityChannelRepository()
        let subChannelQuery = channelRepository.getSubChannels(channelId: "channelId")
        subChannelQuery.observe { liveCollection, _, error in
            guard error == nil else { return }
            let subChannels = liveCollection.allObjects()
            subChannels.forEach { subChannel in
                channelRepository.deleteSubChannel(subChannelId: subChannel.subChannelId) { deleteError in
                    if let deleteError = deleteError {
                        print("Failed to delete sub-channel: \(deleteError.localizedDescription)")
                    }
                }
            }
        }
```

```typescript TypeScript
import { SubChannelRepository } from '@amityco/ts-sdk'

async function deleteAllSubChannels() {
  try {
    await SubChannelRepository.deleteAllSubChannels('channelId')
    console.log('All sub-channels deleted')
  } catch (error) {
    console.error('Failed to delete all sub-channels:', error)
  }
}
```

</CodeGroup>
