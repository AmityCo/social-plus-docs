{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: send_audio_message | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun sendAudioMessage() {
        val audioUri = android.net.Uri.parse("content://path/to/audio.mp3")
        AmityChatClient.newMessageRepository()
            .createMessage(channelId = "channelId")
            .with()
            .audio(audioUri)
            .build()
            .send()
            .subscribe()
    }
```

```swift iOS
let messageRepository = AmityMessageRepository(client: AmityManager.shared.client!)
        let audioURL = URL(fileURLWithPath: "path/to/audio.m4a")
        let createOptions = AmityMessageCreateOptions(channelId: "channelId", audioFileURL: audioURL)
        messageRepository.createMessage(with: createOptions) { message, error in
            print("Audio message sent: \(message?.messageId ?? "")")
        }
```

```dart Flutter
void sendAudioMessage() {
    final messageRepo = AmityChatClient.newMessageRepository();
    // Upload audio file first, then attach fileId
    messageRepo
        .createMessage('channelId')
        .audio(fileId: 'uploadedAudioFileId')
        .send()
        .then((AmityMessage message) {
          print('Audio message sent: \${message.messageId}');
        });
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

function sendAudioMessage() {
  Send an audio message to a channel using MessageRepository.createMessage with audio data type and file ID.
}
```

</CodeGroup>
