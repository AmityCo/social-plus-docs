{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: chat-new-channel-repository | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun newChannelRepository() {
        val channelRepository = AmityCoreClient.newChannelRepository()
    }
```

```swift iOS
let channelRepository = AmityChannelRepository()
        print("New channel repository created: \(channelRepository)")
```

```dart Flutter
void newChannelRepository() {
    final channelRepository = AmityChatClient.newChannelRepository();
    // use channelRepository to create, get, or query channels
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

function queryChannels() {
  const liveCollection = ChannelRepository.queryChannels({ membership: 'member' })

  liveCollection.on('dataUpdated', (channels) => {
    console.log('Channels updated:', channels)
  })

  liveCollection.on('dataError', (error) => {
    console.error('Query channels error:', error)
  })

  return () => liveCollection.dispose()
}
```

</CodeGroup>
