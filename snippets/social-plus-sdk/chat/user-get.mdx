{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-get | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getUser(
        userRepository: AmityUserRepository
    ) {
        userRepository.getUser(userId = "brian3291")
            .doOnNext { user: AmityUser ->
                // AmityUser
                val displayname = user.getDisplayName()
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let userRepository = AmityUserRepository(client: client)
        var token: AmityNotificationToken?
        token = userRepository.getUser("<user-id>").observe { liveObject, error in
            guard let user = liveObject.object else { return }
            print("User: \(user.displayName ?? "")")
        }
```

```dart Flutter
void userGet(String userId) {
    AmityCoreClient.newUserRepository()
        .getUser(userId)
        .then((AmityUser user) {
          print('User retrieved: ${user.displayName}');
          print('User ID: ${user.userId}');
          print('Description: ${user.description}');
        })
        .onError((error, stackTrace) {
          print('Failed to get user: $error');
        });
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

function getUser(userId: string) {
  const unsubscribe = UserRepository.getUser(userId, ({ data: user, loading, error }) => {
    if (error) {
      console.error('Error fetching user:', error)
      return
    }
    if (!loading && user) {
      console.log('User:', user.displayName, user.userId)
      unsubscribe()
    }
  })
}
```

</CodeGroup>
