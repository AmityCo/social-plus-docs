{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-moderation | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun channelModeration() {
        // TODO: implement channel moderation
    }
```

```swift iOS
func channelModeration() {
        let client = AmityClient.shared
        let channelModeration = AmityChannelModeration(client: client, channelId: "channel_id")
        channelModeration.addRole("moderator", userIds: ["user_id"]) { success, error in
            if success {
                print("Role assigned to channel member")
            }
        }
    }
```

```dart Flutter
void channelModeration() {
    // Perform moderation actions on a channel (ban/mute/role management)
    final moderation = AmityChatClient.newChannelRepository()
        .moderation('channelId');

    moderation
        .banMembers(userIds: ['userId'])
        .then((_) => print('Moderation action applied'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function channelModeration() {
  const client = createClient('apiKey', 'region')
  await client.login({ userId: 'user123', displayName: 'User 123' })
  await client.channel.banMembers({ channelId: 'channel123', userIds: ['user456'] })
  const members = await client.channel.queryMembers({ channelId: 'channel123', filter: 'banned' })
  members.on('dataUpdated', (banned) => console.log('Banned members:', banned.length))
}
```

</CodeGroup>
