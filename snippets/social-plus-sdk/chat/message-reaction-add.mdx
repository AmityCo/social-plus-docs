{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-reaction-add | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun addReaction(
        reactionRepository: AmityReactionRepository,
        message: AmityMessage
    ) {
        // Add a reaction
        reactionRepository.addReaction(
            reactionReference = AmityReactionReference.MESSAGE(messageId = message.getMessageId()),
            reactionName = "like"
        )
            .doOnComplete {
                // Void
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let reactionRepository = AmityReactionRepository(client: client)
        do {
            let result = try await reactionRepository.addReaction("like", referenceId: "<message-id>", referenceType: .message)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void addMessageReaction(AmityMessage message) {
    message.react()
        .addReaction('like')
        .then((value) {
          // success
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { ReactionRepository } from '@amityco/ts-sdk'

async function addMessageReaction() {
  await ReactionRepository.addReaction('message', 'messageId', 'like')
}
```

</CodeGroup>
