{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-reaction-add | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun messageReactionAdd() {
        // TODO: implement message reaction add
    }
```

```swift iOS
func messageReactionAdd() {
        let client = AmityClient.shared
        let reactionRepository = AmityReactionRepository(client: client)
        reactionRepository.addReaction("love", referenceId: "message_id", referenceType: .message) { success, error in
            if success {
                print("Message reaction added")
            }
        }
    }
```

```dart Flutter
void addMessageReaction(AmityMessage message) {
    message.react()
        .addReaction('like')
        .then((value) {
          // success
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { ReactionRepository } from '@amityco/ts-sdk'

async function addMessageReaction() {
  await ReactionRepository.addReaction('message', 'messageId', 'like')
}
```

</CodeGroup>
