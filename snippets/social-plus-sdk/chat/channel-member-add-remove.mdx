{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-member-add-remove | updated: 2026-05-02 */}

<CodeGroup>

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function addChannelMembers(channelId: string, userIds: string[]) {
  await ChannelRepository.addMembers(channelId, userIds)
  console.log(`Added ${userIds.length} member(s) to channel ${channelId}`)
}

async function removeChannelMembers(channelId: string, userIds: string[]) {
  await ChannelRepository.removeMembers(channelId, userIds)
  console.log(`Removed ${userIds.length} member(s) from channel ${channelId}`)
}

// Example usage
addChannelMembers('channelId', ['userId1', 'userId2'])
removeChannelMembers('channelId', ['userId1'])
```

</CodeGroup>
