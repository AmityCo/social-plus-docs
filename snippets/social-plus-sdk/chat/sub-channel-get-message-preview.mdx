{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: sub-channel-get-message-preview | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun subChannelGetMessagePreview() {
        // TODO: implement sub channel get message preview
    }
```

```swift iOS
func subChannelGetMessagePreview() {
        let client = AmityClient.shared
        let subChannelRepository = AmitySubChannelRepository(client: client)
        let liveObject = subChannelRepository.getSubChannel("sub_channel_id")
        liveObject.observe { subChannel, error in
            if let sc = subChannel, let preview = sc.messagePreview {
                print("SubChannel last message: \(preview.text ?? "")")
            }
        }
    }
```

```dart Flutter
void subChannelGetMessagePreview() {
    AmityChatClient.newSubChannelRepository()
        .getSubChannel(subChannelId: 'subChannelId')
        .then((subChannel) {
      final preview = subChannel.messagePreview;
      // Use preview to display latest message
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function subChannelGetMessagePreview() {
  const channelRepository = new ChannelRepository();
  const preview = await channelRepository.getMessagePreview('subChannelId');
  console.log('Message preview:', preview);
}
```

</CodeGroup>
