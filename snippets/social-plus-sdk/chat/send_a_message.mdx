{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: send_a_message | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void sendAMessage() {
    final messageRepo = AmityChatClient.newMessageRepository();
    messageRepo
        .createMessage('channelId')
        .text('Hello, World!')
        .send()
        .then((AmityMessage message) {
          print('Message sent: \${message.messageId}');
        })
        .onError((error, stackTrace) {
          print('Error: \$error');
        });
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function sendMessageUnderscore() {
  const messageData = {
    subChannelId: 'subChannelId123',
    dataType: 'text' as const,
    data: { text: 'Hello, world!' },
  };

  const liveMessage = MessageRepository.createMessage(messageData);
  liveMessage.on('dataUpdated', message => console.log('Message sent:', message.messageId));
  liveMessage.on('dataError', err => console.error('Error sending message:', err));
}
```

</CodeGroup>
