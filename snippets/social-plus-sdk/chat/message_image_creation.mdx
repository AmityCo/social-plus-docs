{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message_image_creation | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void messageImageCreation() async {
    final fileInfo = await AmityCoreClient.newFileRepository()
        .uploadImage('path/to/image.jpg');
    await AmityChatClient.newMessageRepository()
        .createMessage('channelId')
        .image(fileInfo)
        .send();
    print('Image message created');
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function messageImageCreation(channelId: string, imageFileId: string, caption?: string) {
  const message = await MessageRepository.createMessage({
    channelId,
    dataType: 'image',
    data: {
      fileId: imageFileId,
      caption,
    },
  })
  console.log('Image message created:', message.messageId)
  return message
}
```

</CodeGroup>
