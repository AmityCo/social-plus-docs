{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create_custom_message | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createCustomMessage() {
        // Send a custom-type message to a channel
        AmityChatClient.newMessageRepository()
            .createMessage(channelId = "channelId")
            .with()
            .custom(data = com.google.gson.JsonObject())
            .build()
            .subscribe()
    }
```

```swift iOS
var data: [String: Any]!
        
        // Sending an integer
        data = ["anykey": 10]
        
        // Sending an array
        data = ["anykey": [10, 14]]
        
        // Sending a dictionary of an array of integer
        data = ["anykey": ["anykey": [10, 12]]]
        
        let customMessageCreateOptions = AmityCustomMessageCreateOptions(subChannelId: "subChannel-123", data: data, tags: nil, parentId: nil)
        
        do {
            let message = try await messageRepository.createCustomMessage(options: customMessageCreateOptions)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void createCustomMessage() {
    AmityChatClient.newMessageRepository()
        .createMessage(channelId: 'channelId')
        .getCustom()
        .data({'key': 'value', 'type': 'custom'})
        .build()
        .send()
        .then((AmityMessage message) {
          // Custom message sent
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function createCustomMessage() {
  const message = await MessageRepository.createMessage({
    subChannelId: 'channelId',
    dataType: 'custom',
    data: { type: 'location', lat: 13.756, lng: 100.501 },
  })
  console.log('Custom message created:', message)
}
```

</CodeGroup>
