{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-member-add | updated: 2026-05-01 */}

<CodeGroup>

```kotlin Android
fun addMembers(channelId: String, userIds: List<String>) {
        AmityChatClient.newChannelRepository()
            .membership(channelId = channelId)
            .addMembers(userIds = userIds)
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let channelMembership = AmityChannelMembership(client: client, andChannel: "<channel-id>")
        do {
            let success = try await channelMembership.addMembers(["<user-id>"])
        } catch {
            // Handle error here
        }
```

```dart Flutter
void addMembers(String channelId) {
    AmityChatClient.newChannelRepository()
        .membership(channelId)
        .addMembers(['userId1', 'userId2'])
        .then((value) {
          // handle result
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function addChannelMembers() {
  await ChannelRepository.addMembers('channelId', ['userId1', 'userId2'])
}
```

</CodeGroup>
