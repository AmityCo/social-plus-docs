{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create-sub-channel | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createSubChannel(channelId: String) {
        AmityChatClient.newSubChannelRepository()
            .createSubChannel(channelId = channelId, displayName = "General")
            .doOnSuccess { subChannel: AmitySubChannel ->
                // AmitySubChannel created
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let channelRepository = AmityChannelRepository()
        let builder = AmitySubChannelCreationDataBuilder()
        builder.setChannelId("parentChannelId")
        builder.setDisplayName("General")
        channelRepository.createSubChannel(builder) { subChannel, error in
            if let error = error {
                print("Failed to create sub-channel: \(error.localizedDescription)")
            } else {
                print("Sub-channel created: \(subChannel?.subChannelId ?? "")")
            }
        }
```

```dart Flutter
void createSubChannel(String channelId, {String displayName = ''}) {
    AmityChatClient.newSubChannelRepository()
        .createSubChannel(channelId: channelId)
        .displayName(displayName)
        .create()
        .then((AmitySubChannel subChannel) {
          // sub-channel created
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { SubChannelRepository } from '@amityco/ts-sdk'

async function createSubChannel() {
  try {
    const { data: subChannel } = await SubChannelRepository.createSubChannel({
      channelId: 'channelId',
      displayName: 'General',
    })
    console.log('Sub-channel created:', subChannel)
  } catch (error) {
    console.error('Failed to create sub-channel:', error)
  }
}
```

</CodeGroup>
