{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-channel-members | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getChannelMembers(channelId: String) {
        AmityChatClient.newChannelRepository()
            .membership(channelId)
            .getMembers()
            .build()
            .query()
            .doOnNext { pagedList ->
                // Handle paged list of channel members
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let membershipRepository = AmityChannelMembershipRepository(client: AmityManager.shared.client!)
        let liveCollection = membershipRepository.getMembers(channelId: "channelId")
        liveCollection.observe { collection, _, error in
            guard error == nil else { return }
            print("Members count: \(collection.count())")
        }
```

```dart Flutter
void getChannelMembers() async {
    AmityChatClient.newChannelRepository()
        .membership(channelId: 'channelId')
        .getMembers()
        .getPagingData(token: null, limit: 20)
        .asStream()
        .listen((result) {
      final members = result.data;
    });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

function getChannelMembers() {
  const channelId = 'channel123'
  const liveCollection = ChannelRepository.getMembers({ channelId, limit: 20 })
  liveCollection.on('dataUpdated', (members) => {
    console.log('Channel members:', members)
  })
}
```

</CodeGroup>
