{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create_audio_message | updated: 2026-05-02 */}

<CodeGroup>

```swift iOS
let audioMessageCreateOptions = AmityAudioMessageCreateOptions(subChannelId: "subChannel-123", attachment: .localURL(url: URL(string: "local-audio-path")!), fileName: nil, tags: nil, parentId: nil)
        
        do {
            let message = try await messageRepository.createAudioMessage(options: audioMessageCreateOptions)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void createAudioMessage() {
    AmityChatClient.newMessageRepository()
        .createMessage(channelId: 'channelId')
        .getAudio()
        .attachment(AmityUploadedAudio(fileId: 'fileId'))
        .build()
        .send()
        .then((AmityMessage message) {
          // Audio message sent
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function createAudioMessage() {
  const message = await MessageRepository.createMessage({
    subChannelId: 'channelId',
    dataType: 'audio',
    data: { fileId: 'uploadedAudioFileId' },
  })
  console.log('Audio message created:', message)
}
```

</CodeGroup>
