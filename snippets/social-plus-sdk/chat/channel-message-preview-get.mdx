{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-message-preview-get | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getChannelMessagePreview(channel: AmityChannel) {
        val messagePreview: AmityMessagePreview? = channel.getMessagePreview()
        messagePreview?.let { preview->
            val data: AmityMessage.Data = preview.getData()
            val dataType: AmityMessage.DataType = preview.getDataType()
            val user: AmityUser? = preview.getUser()
        }
    }
```

```swift iOS
let liveObject = channelRepository.getChannel("channelId")
        liveObject.observe { liveObject, error in
            guard let channel = liveObject.object else { return }
            if let previewMessage = channel.latestMessage {
                print("Preview: \(previewMessage.data?["text"] as? String ?? "")")
            }
        }
```

```dart Flutter
void getMessagePreview(AmityChannel channel) {
    final preview = channel.messagePreview;
    if (preview != null) {
      final dataType = preview.dataType;
      final creatorId = preview.creatorId;
      // display preview in channel list
    }
  }
```

</CodeGroup>
