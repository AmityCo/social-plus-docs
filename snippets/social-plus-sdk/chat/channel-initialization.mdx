{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-initialization | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun initChannelRepository() {
        val channelRepository = AmityChatClient.newChannelRepository()
    }
```

```swift iOS
let builder = AmityCommunityChannelBuilder()
        builder.setId("my-channel-id")
        builder.setDisplayName("My Channel")
        channelRepository.createChannel(with: builder) { channel, error in
            if let error = error {
                print("Error creating channel: \(error)")
            } else {
                print("Channel created: \(channel?.channelId ?? "")")
            }
        }
```

```dart Flutter
void initChannelRepository() {
    final channelRepository = AmityChatClient.newChannelRepository();
    // use channelRepository to create, query, or manage channels
  }
```

</CodeGroup>
