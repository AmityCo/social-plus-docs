{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-total-channels-unread-info | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getTotalChannelsUnreadInfo() {
        AmityChatClient.newChannelRepository()
            .getTotalUnreadCount()
            .doOnNext { totalUnread ->
                val unreadCount = totalUnread.getUnreadCount()
                val mentionCount = totalUnread.getUnreadMentionCount()
                val channelCount = totalUnread.getChannelCount()
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
AmityManager.shared.client.getChannelUnreadInfo { result in
            switch result {
            case .success(let info):
                print("Total unread channels: \(info.totalUnreadChannelCount)")
                print("Total unread messages: \(info.totalUnreadMessageCount)")
            case .failure(let error):
                print("Error: \(error)")
            }
        }
```

```typescript TypeScript
import { getUserUnread } from '@amityco/ts-sdk'

function getTotalChannelsUnreadInfo() {
  const unsubscribe = getUserUnread(({ data, loading, error }) => {
    if (loading) return
    if (error) {
      console.error('Error fetching unread info:', error)
      return
    }

    console.log('Total unread count:', data.unreadCount)
    console.log('Total mention count:', data.mentionCount)
    console.log('Channels with unread:', data.channelCount)
  })

  return unsubscribe
}
```

</CodeGroup>
