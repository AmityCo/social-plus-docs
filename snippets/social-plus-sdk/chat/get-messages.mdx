{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-messages | updated: 2026-05-02 */}

<CodeGroup>

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

function getMessages() {
  const liveCollection = MessageRepository.queryMessages({ subChannelId: 'subChannelId' })

  liveCollection.on('dataUpdated', (messages) => {
    console.log('Messages:', messages)
  })

  liveCollection.on('dataError', (error) => {
    console.error('Failed to get messages:', error)
  })

  return () => liveCollection.dispose()
}
```

</CodeGroup>
