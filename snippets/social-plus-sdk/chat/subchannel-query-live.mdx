{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: subchannel-query-live | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun subchannelQueryLive() {
        AmityChatClient.newSubChannelRepository()
            .getSubChannels(channelId = "channelId")
            .build()
            .getLiveCollection()
            .observe(lifecycleOwner) { pagingData ->
                // Handle live sub-channel list updates
            }
    }
```

```swift iOS
let subChannelRepository = AmitySubChannelRepository(client: client)
        var token: AmityNotificationToken?
        token = subChannelRepository.getSubChannels(channelId: "<channel-id>").observe { collection, change, error in
            guard error == nil else { return }
            print("Subchannels: \(collection.count())")
        }
```

```dart Flutter
void subchannelQueryLive() {
    AmityChatClient.newSubChannelRepository()
        .observeSubChannels(channelId: 'channelId')
        .listen((subChannels) {
      // Handle live sub-channel updates
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function subchannelQueryLive() {
  const channelRepository = new ChannelRepository();
  const liveCollection = channelRepository.getSubChannels('channelId');
  liveCollection.on('dataUpdated', (subChannels) => {
    console.log('Live sub-channels:', subChannels);
  });
}
```

</CodeGroup>
