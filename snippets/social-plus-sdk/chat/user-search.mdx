{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-search | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun userSearch() {
        // TODO: implement user search using Social Plus SDK
    }
```

```swift iOS
func userSearch() {
        let userRepository = AmityUserRepository(client: AmityManager.shared.client!)
        let liveCollection = userRepository.searchUser("searchKeyword", sortBy: .displayName)
        
        let token = liveCollection.observe { collection, changes, error in
            guard error == nil else { return }
            let users = collection.allObjects()
            print("Found \(users.count) users")
            for user in users {
                print("User: \(user.displayName ?? ""), id: \(user.userId)")
            }
        }
        // Retain token as long as observation is needed
    }
```

```dart Flutter
void userSearch() {
    AmitySocialClient.newUserRepository()
        .searchUserByDisplayName('searchKeyword')
        .query()
        .then((List<AmityUser> users) {
          for (final user in users) {
            print('User found: \${user.displayName}');
          }
        });
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function searchUsers() {
  const unsubscribe = UserRepository.getUsers(
    { displayName: 'John', sortBy: 'displayName' },
    (result) => {
      if (result.data) {
        console.log('Search results:', result.data);
      }
    },
  );
  return unsubscribe;
}
```

</CodeGroup>
