{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: start_and_stop_reading | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun startAndStopReading() {
        // TODO: Implement Start and stop reading
    }
```

```swift iOS
let channelRepository = AmityChannelRepository(client: AmityManager.shared.client!)
        channelRepository.startReading(channelId: "channelId")
        // ... user reads messages ...
        channelRepository.stopReading(channelId: "channelId")
        print("Started and stopped reading channel")
```

```dart Flutter
void startAndStopReading() {
    // Start reading — mark messages as read in the channel
    await AmityCoreClient.newChatRepository()
        .channel
        .startReading(channelId: 'channelId123');
    print('Started reading channel');

    // Stop reading when the user leaves the channel
    await AmityCoreClient.newChatRepository()
        .channel
        .stopReading(channelId: 'channelId123');
    print('Stopped reading channel');
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function startAndStopReading() {
  import { ChannelRepository } from '@amityco/ts-sdk';

  // Start tracking read status
  const stopReading = ChannelRepository.startReading('channel-id-123');
  console.log('Started reading channel');

  // Stop tracking read status after some time
  setTimeout(() => {
    stopReading();
    console.log('Stopped reading channel');
  }, 5000);
}
```

</CodeGroup>
