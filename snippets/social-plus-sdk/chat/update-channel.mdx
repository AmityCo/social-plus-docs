{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: update-channel | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun updateChannel() {
        // Update operation for update-channel
        AmityChatClient.newChannelRepository()
            .updateChannel("channelId")
            .build()
            .update()
            .subscribe({ channel -> }, { error -> })
    }
```

```swift iOS
let channelRepository = AmityChannelRepository()
        let builder = AmityChannelUpdateBuilder()
        builder.setDisplayName("Updated Channel Name")
        channelRepository.updateChannel(withId: "channelId", builder: builder) { channel, error in
            if let error = error {
                print("Error updating channel: \(error)")
            } else {
                print("Channel updated: \(channel?.displayName ?? "")")
            }
        }
```

```dart Flutter
void updateChannel() {
    AmityChatClient.newChannelRepository()
        .updateChannel('channelId')
        .displayName('Updated Channel Name')
        .update()
        .then((AmityChannel channel) {
          print('Channel updated: \${channel.channelId}');
        })
        .onError((error, stackTrace) {
          print('Error: \$error');
        });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function updateChannel() {
  const updatedChannel = await ChannelRepository.updateChannel('channelId', {
    displayName: 'Updated Channel Name',
    metadata: { topic: 'General Discussion' },
  });
  console.log('Channel updated:', updatedChannel);
}
```

</CodeGroup>
