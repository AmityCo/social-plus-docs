{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: ban-unban-channel-member | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void banMember(String channelId, String userId) {
    AmityChatClient.newChannelRepository()
        .moderation(channelId)
        .banMembers([userId])
        .then((value) {
          // member banned
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }

  void unbanMember(String channelId, String userId) {
    AmityChatClient.newChannelRepository()
        .moderation(channelId)
        .unbanMembers([userId])
        .then((value) {
          // member unbanned
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function banChannelMember(channelId: string, userId: string) {
  await ChannelRepository.banMembers(channelId, [userId])
  console.log(`User ${userId} banned from channel ${channelId}`)
}

async function unbanChannelMember(channelId: string, userId: string) {
  await ChannelRepository.unbanMembers(channelId, [userId])
  console.log(`User ${userId} unbanned from channel ${channelId}`)
}
```

</CodeGroup>
