{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-reaction-query | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun messageReactionQuery() {
        AmityChatClient.newMessageRepository()
            .getMessage("messageId")
            .subscribe { message ->
                message.react()
                    .getReactions()
                    .subscribe()
            }
    }
```

```swift iOS
func messageReactionQuery() {
        let reactionRepository = AmityReactionRepository(client: AmityManager.shared.client!)
        let collection = reactionRepository.getReactions(
            referenceId: "messageId123",
            referenceType: .message,
            reactionName: nil
        )
        collection.observe { collection, _, error in
            guard error == nil else { return }
            collection.snapshots.forEach { reaction in
                print("Reaction: \(reaction.reactionName)")
            }
        }
    }
```

```dart Flutter
void queryMessageReactions(AmityMessage message) {
    message.react()
        .getReactions('like')
        .then((value) {
      // Handle reaction list
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { ReactionRepository } from '@amityco/ts-sdk'

function queryMessageReactions() {
  const liveCollection = ReactionRepository.queryReactions({
    referenceType: 'message',
    referenceId: 'messageId',
    reactionName: 'like',
  })
  liveCollection.on('dataUpdated', (reactions) => {
    console.log(reactions)
  })
  return liveCollection
}
```

</CodeGroup>
