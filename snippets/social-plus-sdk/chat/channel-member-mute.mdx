{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-member-mute | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun muteChannelMember(channelId: String, userId: String, mutePeriod: Long) {
        AmityCoreClient.newChannelRepository()
            .moderation(channelId)
            .muteMembers(userIds = listOf(userId), mutePeriod = mutePeriod)
            .doOnComplete { }
            .doOnError { }
            .subscribe()
    }
```

```swift iOS
let moderation = channelRepository.moderation(forChannel: "channelId")
        moderation.muteMembers(["userId"], mutePeriod: .forever) { success, error in
            if let error = error {
                print("Error muting member: \(error)")
            } else {
                print("Member muted: \(success)")
            }
        }
```

```dart Flutter
void muteChannelUsers() {
    final userIds = List.of(['user1', 'user2']);
    AmityChatClient.newChannelRepository()
        .moderation('channelId')
        .muteMembers(userIds)
        .then((value) => {
              //handle result
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function muteChannelMember(channelId: string, userId: string, muteDuration: number) {
  // muteDuration is in milliseconds; pass -1 to mute indefinitely
  await ChannelRepository.muteMembers(channelId, [userId], muteDuration)
  console.log(`User ${userId} muted in channel ${channelId} for ${muteDuration}ms`)
}

// Example: mute for 1 hour
muteChannelMember('channelId', 'userId', 60 * 60 * 1000)
```

</CodeGroup>
