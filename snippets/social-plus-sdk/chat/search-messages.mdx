{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: search-messages | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun searchMessages() {
        // TODO: Implement Search messages
    }
```

```swift iOS
func searchMessages() {
        let client = AmityClient.shared
        let messageRepo = AmityMessageRepository(client: client)
        let queryOptions = AmityMessageQueryOptions(
            subChannelId: "subChannelId",
            filterByParentId: false,
            parentId: nil,
            limit: 20
        )
        let collection = messageRepo.getMessages(options: queryOptions)
    }
```

```dart Flutter
void searchMessages() async {
    final messageRepository = AmityChatClient.newMessageRepository();
    messageRepository
        .getMessages('channelId')
        .keyword('searchTerm')
        .asStream()
        .listen((messages) {
      print('Messages found: \${messages.length}');
    });
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function searchMessages() {
  const params = {
    subChannelId: 'subChannelId123',
    keyword: 'hello',
    sortBy: 'segmentDesc' as const,
  };

  const liveCollection = MessageRepository.getMessages(params);
  liveCollection.on('dataUpdated', messages => {
    console.log('Message search results:', messages.map(m => m.messageId));
  });
  liveCollection.on('dataError', err => console.error('Error searching messages:', err));
}
```

</CodeGroup>
