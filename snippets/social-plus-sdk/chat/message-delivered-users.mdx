{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-delivered-users | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getMessageDeliveredUsers(message: AmityMessage) {
		message.getDeliveredUsers()
			.doOnNext {
				// PagingData<AmityUser>
			}
			.doOnError {
				// Exception
			}
			.subscribe()
		
		/*
		Filtering the delivered users by membership status
		with MessageDeliveredMembershipFilter enum
		MEMBER, BANNED, MUTED, NOT_MEMBER, DELETED
		 */
		message.getDeliveredUsers(
			memberships = listOf(
				MessageDeliveredMembershipFilter.MEMBER,
				MessageDeliveredMembershipFilter.BANNED,
				MessageDeliveredMembershipFilter.MUTED,
				MessageDeliveredMembershipFilter.NOT_MEMBER,
				MessageDeliveredMembershipFilter.DELETED)
			)
			.doOnNext {
				// PagingData<AmityUser>
			}
			.doOnError {
				// Exception
			}
			.subscribe()
	}
```

```swift iOS
let messageRepo = AmityMessageRepository(client: AmityManager.shared.client!)
        messageRepo.markMessageAsDelivered(messageId: "messageId123") { success, error in
            if let error = error { print("Error: \(error)"); return }
            print("Message marked as delivered")
        }
```

```dart Flutter
void messageDeliveredUsers() {
    final messageId = 'messageId';
    final channelId = 'channelId';
    AmityCoreClient.newChannelRepository()
        .message(channelId)
        .getDeliveredUsers(messageId: messageId)
        .then((users) => print('Delivered to: \${users.length} users'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function getMessageDeliveredUsers() {
  const { data: users } = await MessageRepository.getDeliveredUsers({
    messageId: 'messageId',
    memberships: ['member'],
  })
  console.log(users)
}
```

</CodeGroup>
