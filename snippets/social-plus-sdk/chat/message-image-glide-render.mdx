{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-image-glide-render | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun viewImageMessage(context: Context, imageView: ImageView, message: AmityMessage) {
        when (val data = message.getData()) {
            is AmityMessage.Data.IMAGE -> {
                val imageUrl: String? = data.getImage()?.getUrl()
                Glide.with(context)
                    .load(imageUrl)
                    .into(imageView)
            }
            else -> {}
        }
    }
```

```swift iOS
let messageRepo = AmityMessageRepository(client: AmityManager.shared.client!)
        let liveObject = messageRepo.getMessage("messageId123")
        liveObject.observe { liveObject, error in
            guard let message = liveObject.object else { return }
            if let imageData = message.data?["fileUrl"] as? String {
                print("Image URL: \(imageData)")
            }
        }
```

```dart Flutter
void messageImageGlideRender() {
    // Retrieve message and display image using URL
    AmityChatClient.newMessageRepository()
        .getMessage('messageId')
        .asStream()
        .listen((message) {
      final imageData = message.data as MessageImageData;
      final imageUrl = imageData.image?.fileUrl;
      print('Image URL for rendering: \$imageUrl');
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function messageImageGlideRender() {
  // Perform message image glide render operation
  console.log('Executing message image glide render...')
  // TODO: Implement message image glide render using @amityco/ts-sdk
}
```

</CodeGroup>
