{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-create | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createChannel(channelId: String, displayName: String, userIds: List<String>) {
        AmityCoreClient.newChannelRepository()
            .createChannel(channelId = channelId)
            .displayName(displayName = displayName)
            .userIds(userIds = userIds)
            .build()
            .create()
            .doOnSuccess { channel: AmityChannel -> }
            .doOnError { }
            .subscribe()
    }
```

```swift iOS
let channelRepository = AmityChannelRepository(client: client)
        let builder = AmityCommunityChannelCreateOptions()
        builder.setDisplayName("My Channel")
        builder.setUserIds(["user1", "user2"])
        do {
            let channel = try await channelRepository.createChannel(with: builder)
            print("Channel created: \(channel.channelId)")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void channelCreate() async {
    final channelRepo = AmityChatClient.newChannelRepository();
    final channel = await channelRepo
        .createChannel()
        .publicChannel()
        .displayName('My Channel')
        .create();
    print('Channel created: \${channel.channelId}');
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function channelCreate(displayName: string, userIds: string[]) {
  const channel = await ChannelRepository.createChannel({
    displayName,
    type: 'community',
    userIds,
  })
  console.log('Channel created:', channel.data.channelId)
}
```

</CodeGroup>
