{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: send_message | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun sendMessage() {
        // TODO: Implement Send message
    }
```

```swift iOS
let messageRepository = AmityMessageRepository(client: AmityManager.shared.client!)
        let createOptions = AmityMessageCreateOptions(subChannelId: "subChannelId", data: ["text": "Hello!"], type: .text, attachments: nil, mentionees: nil, metadata: nil, parentId: nil)
        messageRepository.createMessage(createOptions) { message, error in
            if let message = message {
                print("Message created with ID: \(message.messageId)")
            } else if let error = error {
                print("Failed to send message: \(error)")
            }
        }
```

```dart Flutter
void sendMessage() {
    AmityCoreClient.newChatRepository()
        .message
        .createMessage(channelId: 'channelId123')
        .text('Hello, World!')
        .send()
        .then((message) => print('Message sent: \${message.messageId}'))
        .onError((error, _) => print('Error sending message: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function sendMessage() {
  import { MessageRepository } from '@amityco/ts-sdk';

  const message = await MessageRepository.createMessage({
    subChannelId: 'channel-id-123',
    dataType: 'text',
    data: { text: 'Hello, World!' },
  });
  console.log('Message sent:', message);
}
```

</CodeGroup>
