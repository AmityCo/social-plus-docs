{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: sub-channel-delete | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun deleteSubChannel(
        subChannelRepository: AmitySubChannelRepository,
        subChannelId: String
    ) {
        // soft-delete
        subChannelRepository.softDeleteSubChannel(subChannelId = subChannelId)
            .doOnComplete {
                // Void
            }
            .doOnError {
                // Exception
            }
            .subscribe()

        // hard-delete
        subChannelRepository.hardDeleteSubChannel(subChannelId = subChannelId)
            .doOnComplete {
                // Void
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```dart Flutter
void softDeleteSubChannel(String subChannelId) {
    AmityChatClient.newSubChannelRepository()
        .softDeleteSubChannel(subChannelId: subChannelId)
        .then((_) {
          // sub-channel soft deleted
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }

  void hardDeleteSubChannel(String subChannelId) {
    AmityChatClient.newSubChannelRepository()
        .hardDeleteSubChannel(subChannelId: subChannelId)
        .then((_) {
          // sub-channel hard deleted
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

</CodeGroup>
