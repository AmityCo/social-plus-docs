{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: send-video-message | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void sendVideoMessage() async {
    final message = await AmityChatClient.newMessageRepository()
        .createMessage('subChannelId')
        .video(filePath: '/path/to/video.mp4')
        .send();
    print('Sent video message: \${message.messageId}');
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function sendVideoMessage(subChannelId: string, videoFileId: string) {
  const message = await MessageRepository.createMessage({
    subChannelId,
    dataType: 'video',
    data: { fileId: videoFileId },
  })

  console.log('Video message sent:', message.messageId)
  return message
}
```

</CodeGroup>
