{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: delete-message | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun deleteMessage(messageId: String) {
        AmityChatClient.newMessageRepository()
            .softDeleteMessage(messageId = messageId)
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let messageRepository = AmityMessageRepository()
        messageRepository.softDeleteMessage(withId: "messageId") { error in
            if let error = error {
                print("Failed to delete message: \(error.localizedDescription)")
            } else {
                print("Message deleted successfully")
            }
        }
```

```dart Flutter
void deleteMessage(String messageId) {
    AmityChatClient.newMessageRepository()
        .softDeleteMessage(messageId: messageId)
        .then((_) {
          // message deleted
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

</CodeGroup>
