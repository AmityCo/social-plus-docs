{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-sub-channel-unread | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getSubChannelUnread(subChannelId: String) {
        AmityChatClient.newSubChannelRepository()
            .getSubChannel(subChannelId = subChannelId)
            .doOnNext { subChannel: AmitySubChannel ->
                val unreadCount = subChannel.getUnreadCount()
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let subChannelRepository = AmitySubChannelRepository(client: AmityManager.shared.client!)
        let liveObject = subChannelRepository.getSubChannel(withId: "subChannelId")
        liveObject.observe { liveObject, error in
            guard let subChannel = liveObject.object else { return }
            print("Sub-channel unread: \(subChannel.unreadCount)")
        }
```

```typescript TypeScript
import { SubChannelRepository } from '@amityco/ts-sdk'

function getSubChannelUnread() {
  const liveObject = SubChannelRepository.getSubChannel('subChannelId')

  liveObject.on('dataUpdated', (subChannel) => {
    console.log('Unread count:', subChannel.unreadCount)
  })

  liveObject.on('dataError', (error) => {
    console.error('Failed to get sub-channel unread:', error)
  })

  return () => liveObject.dispose()
}
```

</CodeGroup>
