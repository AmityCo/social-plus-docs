{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_channel_member_with_roles | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void getChannelMemberWithRoles() async {
    AmityChatClient.newChannelRepository()
        .membership(channelId: 'channelId')
        .getMembers()
        .roles(['moderator', 'member'])
        .getPagingData(token: null, limit: 20)
        .asStream()
        .listen((result) {
      final members = result.data;
    });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

function getChannelMemberWithRoles() {
  const channelId = 'channel123'
  const roles = ['moderator']
  const liveCollection = ChannelRepository.getMembers({ channelId, roles, limit: 20 })
  liveCollection.on('dataUpdated', (members) => {
    console.log('Channel members with roles:', members)
  })
}
```

</CodeGroup>
