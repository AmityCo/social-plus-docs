{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: remove_reaction_message | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun removeReactionMessage() {
        // perform remove reaction message operation
        AmityChatClient.newMessageRepository()
            .addReaction("messageId", "like")
            .subscribe()
    }
```

```dart Flutter
void removeReactionMessage() {
    AmityCoreClient.newChatRepository()
        .message
        .react()
        .message(messageId: 'messageId123')
        .removeReaction('like')
        .then((_) => print('Reaction removed'))
        .onError((error, _) => print('Error removing reaction: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function removeReactionMessage() {
  await MessageRepository.removeReaction({
    messageId: 'messageId123',
    reactionName: 'like',
  });
  console.log('Reaction removed from message successfully');
}
```

</CodeGroup>
