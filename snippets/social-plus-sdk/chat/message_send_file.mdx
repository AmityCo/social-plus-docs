{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message_send_file | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void messageSendFile() async {
    final fileInfo = await AmityCoreClient.newFileRepository()
        .uploadFile('path/to/document.pdf');
    await AmityChatClient.newMessageRepository()
        .createMessage('channelId')
        .file(fileInfo)
        .send();
    print('File message sent');
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function messageSendFile(channelId: string, fileId: string, fileName: string) {
  const message = await MessageRepository.createMessage({
    channelId,
    dataType: 'file',
    data: {
      fileId,
      fileName,
    },
  })
  console.log('File message sent:', message.messageId)
  return message
}
```

</CodeGroup>
