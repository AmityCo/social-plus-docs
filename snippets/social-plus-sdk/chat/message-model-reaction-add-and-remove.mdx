{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-model-reaction-add-and-remove | updated: 2026-04-30 */}

<CodeGroup>

```kotlin Android
fun addAndRemoveReaction(
        reactionRepository: AmityReactionRepository,
        message: AmityMessage
    ) {
        // Add a reaction
        reactionRepository.addReaction(
            reactionReference = AmityReactionReference.MESSAGE(messageId = message.getMessageId()),
            reactionName = "like"
        )
            .doOnComplete {
                // Void
            }
            .doOnError {
                // Exception
            }
            .subscribe()
        // Remove a reaction
        reactionRepository.removeReaction(
            reactionReference = AmityReactionReference.MESSAGE(messageId = message.getMessageId()),
            reactionName = "like"
        )
            .doOnComplete {
                // Void
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

</CodeGroup>
