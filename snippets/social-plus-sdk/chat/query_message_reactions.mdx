{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: query_message_reactions | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun queryMessageReactions() {
        // Query reactions on a post
        AmitySocialClient.newReactionRepository()
            .getReactions(referenceType = AmityReactionReferenceType.POST, referenceId = "postId")
            .asLiveData()
    }
```

```dart Flutter
late PagingController<AmityReaction> _reactionController;

    void queryMessageReactions(String messageId) {
      _reactionController = PagingController(
        pageFuture: (token) => AmityChatClient.newMessageRepository()
            .getReactions(messageId: messageId)
            .getPagingData(token: token, limit: 20),
        pageSize: 20,
      )..addListener(() {
          if (_reactionController.error == null) {
            // Update UI with _reactionController.loadedItems
          } else {
            // Handle error
          }
        });
    }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function queryMessageReactions() {
  const unsub = runQuery(
    MessageRepository.queryMessageReactions({
      messageId: 'messageId123',
      reactionName: undefined,
    }),
    ({ data: reactions }) => {
      console.log('Message reactions:', reactions.map((r) => r.reactionName));
    },
  );
  return unsub;
}
```

</CodeGroup>
