{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-create-community | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void channelCreateCommunity() {
    AmityChatClient.newChannelRepository()
        .createChannel()
        .communityType()
        .withChannelId('channelId')
        .withDisplayName('Community Channel')
        .create()
        .then((channel) { })
        .onError((error, stackTrace) { });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function createCommunityChannel() {
  try {
    const { data: channel } = await ChannelRepository.createChannel({
      channelId: 'my-community-channel',
      displayName: 'My Community Channel',
      type: 'community',
      metadata: { description: 'A community discussion channel' },
    });
    console.log('Community channel created:', channel.channelId);
    return channel;
  } catch (error) {
    console.error('Failed to create community channel:', error);
  }
}
```

</CodeGroup>
