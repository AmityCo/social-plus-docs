{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: edit-message | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun editMessage(messageId: String, newText: String) {
        AmityChatClient.newMessageRepository()
            .editMessage(messageId = messageId)
            .text(text = newText)
            .build()
            .apply()
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```dart Flutter
void editMessage() {
    AmityChatClient.newMessageRepository()
        .editMessage(messageId: 'messageId')
        .text('Updated message text')
        .build()
        .apply()
        .then((_) {
          // Message edited
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function editMessage() {
  const updated = await MessageRepository.editMessage({
    messageId: 'messageId',
    data: { text: 'Edited message content' },
  })
  console.log('Message edited:', updated)
}
```

</CodeGroup>
