{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create_live_channel | updated: 2026-05-02 */}

<CodeGroup>

```swift iOS
do {
            let builder = AmityLiveChannelCreateOptions()
            builder.setUserIds(["steve", "john"])
            builder.setDisplayName("Live Channel")
            
            let channel = try await channelRepository.createChannel(with: builder)
            print("A channel is created: \(channel.channelId)")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void createLiveChannel() {
    AmityChatClient.newChannelRepository()
        .createChannel()
        .liveType()
        .withChannelId('liveChannelId')
        .withDisplayName('Live Channel')
        .create()
        .then((AmityChannel channel) {
          final channelId = channel.channelId;
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function createLiveChannel() {
  const channel = await ChannelRepository.createChannel({
    displayName: 'Live Chat',
    type: 'live',
  })
  console.log('Live channel created:', channel)
}
```

</CodeGroup>
