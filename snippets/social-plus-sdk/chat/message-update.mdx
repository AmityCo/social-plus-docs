{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-update | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun messageUpdate() {
        // Update operation via builder pattern
        AmityChatClient.newMessageRepository()
            .editMessage(messageId = "messageId")
            .text("Updated message content")
            .build()
            .apply()
            .subscribe()
    }
```

```swift iOS
let messageRepo = AmityMessageRepository(client: AmityManager.shared.client!)
        let builder = AmityMessageUpdateBuilder(messageId: "messageId123")
        builder.setText("Updated message content")
        messageRepo.updateMessage(builder) { success, error in
            if let error = error { print("Error: \(error)"); return }
            print("Message updated successfully")
        }
```

```dart Flutter
void updateMessage() {
    // update message
    AmityChatClient.newMessageRepository()
        .editTextMessage('messageId')
        .text('updated message')
        .tags(['tag1', 'tag2'])
        .metadata({'key': 'value'})
        .update()
        .then((AmityMessage message) => {
              //handle result
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function updateMessage() {
  const { data: message } = await MessageRepository.updateMessage('messageId', {
    data: { text: 'Updated message text' },
  })
  console.log(message)
}
```

</CodeGroup>
