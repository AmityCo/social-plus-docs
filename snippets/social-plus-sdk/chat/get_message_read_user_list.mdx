{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_message_read_user_list | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getMessageReadUserList() {
        // Query users who delivered/read a specific message
        AmityChatClient.newMessageRepository()
            .getMessage("messageId")
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(io.reactivex.rxjava3.android.schedulers.AndroidSchedulers.mainThread())
            .subscribe({ message ->
                // Access delivery/read receipts from message
            }, { error ->
                // Handle error
            })
    }
```

```dart Flutter
void getMessageReadUserList() {
    AmityChannelParticipation('channelId')
        .getMessageReadUserList('messageId')
        .getPagingData()
        .listen((users) {
      for (final user in users) {
        print('Read by: \${user.userId}');
      }
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function getMessageReadUserList() {
  const liveCollection = MessageRepository.getMessageReadByUsers('messageId123');
  liveCollection.on('dataUpdated', (users) => {
    console.log('Message read by:', users.map((u: any) => u.userId));
  });
}
```

</CodeGroup>
