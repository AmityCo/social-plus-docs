{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: mark_message_as_delivered | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun markMessageAsDelivered() {
        // Mark a message as delivered to the current user
        AmityChatClient.newMessageRepository()
            .markMessageAsDelivered("messageId")
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(io.reactivex.rxjava3.android.schedulers.AndroidSchedulers.mainThread())
            .subscribe({
                // Message marked as delivered
            }, { error ->
                // Handle error
            })
    }
```

```dart Flutter
void markMessageAsDelivered() async {
    final messageRepo = AmityChatClient.newMessageRepository();
    await messageRepo.markMessageAsDelivered(
      channelId: 'channelId123',
      messageId: 'messageId123',
    );
    print('Message marked as delivered.');
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function markMessageAsDelivered() {
  await MessageRepository.markMessageAsDelivered('messageId')
  console.log('Message marked as delivered')
}
```

</CodeGroup>
