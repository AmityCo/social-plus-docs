{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: send_video_message | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun sendVideoMessage() {
        val videoUri = android.net.Uri.parse("content://path/to/video.mp4")
        AmityChatClient.newMessageRepository()
            .createMessage(channelId = "channelId")
            .with()
            .video(videoUri)
            .build()
            .send()
            .subscribe()
    }
```

```swift iOS
let messageRepository = AmityMessageRepository(client: AmityManager.shared.client!)
        let videoURL = URL(fileURLWithPath: "path/to/video.mp4")
        let createOptions = AmityMessageCreateOptions(channelId: "channelId", videoFileURL: videoURL)
        messageRepository.createMessage(with: createOptions) { message, error in
            print("Video message sent: \(message?.messageId ?? "")")
        }
```

```dart Flutter
void sendVideoMessage() {
    final messageRepo = AmityChatClient.newMessageRepository();
    messageRepo
        .createMessage('channelId')
        .video(fileId: 'uploadedVideoFileId')
        .send()
        .then((AmityMessage message) {
          print('Video message sent: \${message.messageId}');
        });
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function sendVideoMessage() {
  const messageCreator = MessageRepository.createMessage('channelId', {
    dataType: 'video',
    fileId: 'uploaded-video-id',
  });
  messageCreator.on('dataUpdated', (message) => {
    console.log('Video message sent:', message);
  });
}
```

</CodeGroup>
