{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: leave_channel | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun leaveChannel() {
        AmityChatClient.newChannelRepository()
            .leaveChannel("channelId")
            .subscribe()
    }
```

```swift iOS
func leaveChannel() {
        let channelRepository = AmityChannelRepository(client: AmityManager.shared.client!)
        channelRepository.leaveChannel(withId: "channelId123") { success, error in
            if success {
                print("Successfully left channel")
            }
        }
    }
```

```dart Flutter
void leaveChannel() {
    AmityChatClient.newChannelRepository()
        .leaveChannel('channelId');
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function leaveChannel() {
  const channelId = 'channel123'
  ChannelRepository.leaveChannel(channelId)
    .then(() => console.log('Left channel'))
    .catch(console.error)
}
```

</CodeGroup>
