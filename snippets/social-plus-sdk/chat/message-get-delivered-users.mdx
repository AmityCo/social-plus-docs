{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-get-delivered-users | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun messageGetDeliveredUsers() {
        // Chat message operation
        AmityChatClient.newMessageRepository()
            .getDeliveredUsers(messageId = "messageId")
            .build()
            .query()
            .subscribe { users ->
                // handle delivered users list
            }
    }
```

```swift iOS
let messageRepo = AmityMessageRepository(client: AmityManager.shared.client!)
        messageRepo.markMessageAsDelivered(messageId: "messageId123") { success, error in
            if let error = error { print("Error: \(error)"); return }
            print("Message marked as delivered")
        }
```

```dart Flutter
void messageGetDeliveredUsers() {
    final messageId = 'messageId';
    final channelId = 'channelId';
    AmityCoreClient.newChannelRepository()
        .message(channelId)
        .getDeliveredUsers(messageId: messageId)
        .then((users) => print('Delivered to: \${users.length} users'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function getDeliveredUsers() {
  const { data: users } = await MessageRepository.getDeliveredUsers({ messageId: 'messageId' })
  console.log(users)
}
```

</CodeGroup>
