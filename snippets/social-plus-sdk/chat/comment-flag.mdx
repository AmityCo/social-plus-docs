{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: comment-flag | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun flagComment(
        commentRepository: AmityCommentRepository,
        comment: AmityComment,
        reason: AmityContentFlagReason,
    ) {
        // Flag a comment
        commentRepository.flagComment(
            commentId = comment.getCommentId(),
            reason = reason
        )
            .subscribe()
        //
        // Unflag a comment
        commentRepository.unflagComment(commentId = comment.getCommentId())
            .subscribe()
        //
        // Get comment's total flag count
        val flagCount = comment.getFlagCount()
        //
        // Check whether comment has been flagged by the active user
        val isFlaggedByMe = comment.isFlaggedByMe()
    }
```

```swift iOS
let commentRepository = AmityCommentRepository(client: client)
        do {
            let success = try await commentRepository.flagComment(withId: "<comment-id>")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void commentFlag() async {
    final commentRepo = AmitySocialClient.newCommentRepository();
    final comment = await commentRepo.getComment('commentId').first;
    await comment.report().flag();
    print('Comment flagged');
  }
```

```typescript TypeScript
import { CommentRepository } from '@amityco/ts-sdk'

async function commentFlag(commentId: string) {
  await CommentRepository.flagComment(commentId)
  console.log('Comment flagged')
}
```

</CodeGroup>
