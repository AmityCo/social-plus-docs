{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: sub-channel-query-options-is-deleted | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun subChannelQueryOptionsIsDeleted() {
        AmityChatClient.newSubChannelRepository()
            .getSubChannels(channelId = "channelId")
            .isDeleted(false)
            .build()
            .query()
            .subscribe({ pagingData ->
                // Handle sub-channels filtered by deleted status
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
let repo = AmitySubChannelRepository()
        let options = AmitySubChannelQueryOptions(channelId: "<channel-id>", isDeleted: true)
        let collection = repo.getSubChannels(options: options)
        print("Include deleted: \(options.isDeleted)")
```

```dart Flutter
void subChannelQueryOptionsIsDeleted() {
    final _subChannels = <AmitySubChannel>[];
    late PagingController<AmitySubChannel> _controller;

    _controller = PagingController(
      pageFuture: (token) => AmityChatClient.newSubChannelRepository()
          .getSubChannels(channelId: 'channelId')
          .isDeleted(false)
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    )..addListener(() {
        if (_controller.error == null) {
          _subChannels
            ..clear()
            ..addAll(_controller.loadedItems);
        }
      });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function subChannelQueryOptionsIsDeleted() {
  const channelRepository = new ChannelRepository();
  const liveCollection = channelRepository.getSubChannels('channelId', {
    isDeleted: false,
  });
  liveCollection.on('dataUpdated', (subChannels) => {
    console.log('Sub-channels:', subChannels);
  });
}
```

</CodeGroup>
