{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: view_custom_message | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun viewCustomMessage() {
        // Observe and display a custom message payload from a channel
        AmityChatClient.newMessageRepository()
            .getMessage(messageId = "messageId")
            .subscribe { message ->
                val customData = message.getData() as? com.amityco.socialcloud.sdk.model.AmityMessage.Data.CUSTOM
                val rawData = customData?.getRawData()
            }
    }
```

```swift iOS
let messageObject = messageRepository.getMessage(messageId)
        
        token = messageObject.observe { messageObject, error in
            if let message = messageObject.object {
                if let data = message.data {
                    // Handle custom message data
                }
            } else {
                // Handle error.
            }
        }
```

```dart Flutter
void viewCustomMessage() {
    AmityChatClient.newMessageRepository()
        .getMessages(subChannelId: 'subChannelId')
        .asStream()
        .listen((messages) {
          for (final message in messages) {
            if (message.type == AmityMessageDataType.CUSTOM) {
              final customData = message.data as AmityMessageCustomData?;
              print('Custom data: \${customData?.rawData}');
            }
          }
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function viewCustomMessage() {
  const messageId = 'message-id';
  const { data: message } = await MessageRepository.getMessage(messageId);
  const customData = message.data;
  console.log('Custom message data:', customData);
}
```

</CodeGroup>
