{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: report-channel-member | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun reportChannelMember() {
        // perform report channel member operation
        AmityChatClient.newMessageRepository()
            .flagMessage("messageId")
            .subscribe()
    }
```

```swift iOS
let channelRepository = AmityChannelRepository(client: AmityUIKitManagerInternal.shared.client)
        channelRepository.report(userId: "userId", channelId: "channelId") { success, error in
            if success {
                print("Channel member reported")
            } else if let error = error {
                print("Failed to report member: \(error.localizedDescription)")
            }
        }
```

```dart Flutter
void reportChannelMember() {
    AmityCoreClient.newChatRepository()
        .channel
        .report('channelId123')
        .flag()
        .then((_) => print('Channel member reported'))
        .onError((error, _) => print('Error reporting member: \$error'));
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function reportChannelMember(channelId: string, userId: string) {
  await ChannelRepository.flagMember(channelId, userId)
  console.log(`Member ${userId} reported in channel ${channelId}`)
}

// Example usage
reportChannelMember('channelId', 'userId')
```

</CodeGroup>
