{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-edit | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun editMessage(
        messageRepository: AmityMessageRepository,
        message: AmityMessage
    ) {
        when (val data = message.getData()) {
            is AmityMessage.Data.TEXT -> {
                messageRepository.editTextMessage(messageId = message.getMessageId())
                    .text(text = "text to edit")
                    .build()
                    .apply()
                    .doOnComplete {
                        //success
                    }
                    .subscribe()
            }
            is AmityMessage.Data.CUSTOM -> {
                messageRepository.editCustomMessage(messageId = message.getMessageId())
                    .data(data = JsonObject())
                    .build()
                    .apply()
                    .doOnComplete {
                        //success
                    }
                    .subscribe()
            }
            else -> {}
        }
    }
```

```swift iOS
let messageRepository = AmityMessageRepository()
        messageRepository.editTextMessage(withId: "messageId", text: "Updated message text") { message, error in
            if let error = error {
                print("Error editing message: \(error)")
            } else {
                print("Message edited: \(message?.messageId ?? "")")
            }
        }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function editMessage() {
  const { data: message } = await MessageRepository.updateMessage('messageId', {
    data: { text: 'Edited message' },
  })
  console.log(message)
}
```

</CodeGroup>
