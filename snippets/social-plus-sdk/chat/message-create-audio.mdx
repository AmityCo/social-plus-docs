{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-create-audio | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createAudioMessage(subChannelId: String, attachment: AmityMessageAttachment) {
        AmityChatClient.newMessageRepository()
            .createAudioMessage(
                subChannelId = subChannelId,
                attachment = attachment
            )
            .build()
            .send()
            .doOnComplete { /* message sent */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let messageRepository = AmityMessageRepository(client: client)
        let audioURL = URL(string: "file:///path/to/audio.m4a")!
        let options = AmityAudioMessageCreateOptions(subChannelId: "<sub-channel-id>", attachment: .localURL(url: audioURL), fileName: nil, tags: nil, parentId: nil)
        do {
            let message = try await messageRepository.createAudioMessage(options: options)
        } catch {
            // Handle error here
        }
```

</CodeGroup>
