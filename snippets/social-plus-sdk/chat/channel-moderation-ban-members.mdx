{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-moderation-ban-members | updated: 2026-04-30 */}

<CodeGroup>

```kotlin Android
fun banMembers(channelId: String, userIds: List<String>) {
        AmityChatClient.newChannelRepository()
            .moderation(channelId = channelId)
            .banMembers(userIds = userIds)
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let channelModeration = AmityChannelModeration(client: client, andChannel: "<channel-id>")
        do {
            let success = try await channelModeration.banMembers(["<user-id>"])
        } catch {
            // Handle error here
        }
```

```dart Flutter
void banMembers(String channelId) {
    AmityChatClient.newChannelRepository()
        .moderation(channelId)
        .banMembers(['userId1', 'userId2'])
        .then((value) {
          // handle result
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function banChannelMembers() {
  await ChannelRepository.banMembers('channelId', ['userId1', 'userId2'])
}
```

</CodeGroup>
