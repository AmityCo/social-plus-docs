{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: comment-unflag | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun unflagComment(commentId: String) {
        AmitySocialClient.newCommentRepository()
            .unflagComment(commentId = commentId)
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let commentRepository = AmityCommentRepository(client: client)
        do {
            let success = try await commentRepository.unflagComment(withId: "<comment-id>")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void commentUnflag() async {
    final commentRepo = AmitySocialClient.newCommentRepository();
    final comment = await commentRepo.getComment('commentId').first;
    await comment.report().unflag();
    print('Comment unflagged');
  }
```

```typescript TypeScript
import { CommentRepository } from '@amityco/ts-sdk'

async function commentUnflag(commentId: string) {
  await CommentRepository.unflagComment(commentId)
  console.log('Comment unflagged')
}
```

</CodeGroup>
