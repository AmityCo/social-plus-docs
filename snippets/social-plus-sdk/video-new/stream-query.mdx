{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: stream-query | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun queryStreams() {
        AmityVideoClient.newStreamRepository()
            .getStreams()
            .build()
            .query()
            .doOnNext { streams: PagedList<AmityStream> ->
                // handle streams list
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let streamRepository = AmityStreamRepository(client: client)
        var token: AmityNotificationToken?
        let query = AmityStreamCollectionQuery()
        query.includeStatus(.live)
        token = streamRepository.getStreams(from: query).observe { collection, change, error in
            // Handle collection updates
        }
```

```dart Flutter
final _streams = <AmityStream>[];
  late PagingController<AmityStream> _controller;

  void queryStreams() {
    _controller = PagingController(
      pageFuture: (token) => AmityVideoClient.newStreamRepository()
          .getStreams()
          .status([AmityStreamStatus.live])
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    )..addListener(() {
        if (_controller.error == null) {
          _streams
            ..clear()
            ..addAll(_controller.loadedItems);
        }
      });
  }
```

```typescript TypeScript
import { StreamRepository } from '@amityco/ts-sdk'

async function queryStreams() {
  const liveCollection = StreamRepository.queryStreams({ status: ['live', 'recorded'] })
  liveCollection.on('dataUpdated', (streams) => {
    console.log(streams)
  })
}
```

</CodeGroup>
