{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: stop-broadcasting | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun stopBroadcasting() {
        // TODO: implement Stop broadcasting
    }
```

```swift iOS
// Call stop on the broadcaster instance from AmityVideoBroadcasterKit
        // broadcaster.stopPublish { error in
        //     if let error = error {
        //         print("Stop broadcast error: \(error)")
        //     } else {
        //         print("Broadcast stopped")
        //     }
        // }
        print("Broadcast stopped and stream ended")
```

```dart Flutter
void stopBroadcasting() async {
    await AmityStreamRepository()
        .getStream('streamId')
        .then((stream) => stream.stopBroadcasting());
    print('Broadcasting stopped');
  }
```

```typescript TypeScript
import { StreamRepository } from '@amityco/ts-sdk'

async function stopBroadcasting(streamId: string) {
  await StreamRepository.stopStream(streamId)
  console.log(`Broadcasting stopped for stream ${streamId}`)
}
```

</CodeGroup>
