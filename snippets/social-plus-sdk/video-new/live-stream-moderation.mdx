{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: live-stream-moderation | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun liveStreamModeration() {
        // TODO: implement Live stream moderation
    }
```

```swift iOS
let streamRepository = AmityStreamRepository(client: AmityClient.shared)
        streamRepository.terminateStream(withId: "streamId") { success, error in
            if success {
                print("Stream terminated by moderator")
            } else if let error = error {
                print("Moderation action failed: \(error)")
            }
        }
```

```typescript TypeScript
import { StreamRepository } from '@amityco/ts-sdk'

function getLiveStreamModerationLabels(streamId: string) {
  const unsubscribe = StreamRepository.getStream(streamId, ({ data: stream, loading, error }) => {
    if (loading) return
    if (error) {
      console.error('Error fetching stream:', error)
      return
    }

    console.log('Stream ID:', stream.streamId)
    console.log('Moderation labels:', stream.moderationLabels)
  })

  return unsubscribe
}

// Example usage
getLiveStreamModerationLabels('streamId')
```

</CodeGroup>
