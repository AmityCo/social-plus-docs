{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: watch-stream | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void watchStream() {
    final streamRepository = AmityVideoClient.newStreamRepository();
    streamRepository
        .getStream('streamId')
        .then((stream) {
          // Use stream.watcherUrl to view the live stream
        });
  }
```

```typescript TypeScript
import { StreamRepository } from '@amityco/ts-sdk'

async function watchStream() {
  const streamId = 'stream123'
  const unsubscribe = StreamRepository.getStreamById(streamId, (stream, error) => {
    if (error) return console.error(error)
    console.log('Stream status:', stream?.status)
    console.log('Stream URL:', stream?.watcherData?.url)
  })
  return unsubscribe
}
```

</CodeGroup>
