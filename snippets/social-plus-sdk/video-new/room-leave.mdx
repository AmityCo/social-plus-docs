{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: room-leave | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun roomLeave() {
        // perform room leave operation
        AmitySocialClient.newVideoRoomRepository()
            .leaveRoom("roomId")
            .subscribe()
    }
```

```swift iOS
let streamRepository = AmityStreamRepository(client: AmityUIKitManagerInternal.shared.client)
        streamRepository.stopStream(streamId: "streamId") { success, error in
            if success {
                print("Left stream successfully")
            } else if let error = error {
                print("Failed to leave stream: \(error.localizedDescription)")
            }
        }
```

```dart Flutter
void leaveRoom(String roomId) {
    AmityVideoClient.newRoomRepository()
        .leaveRoom(roomId: roomId)
        .then((_) {
          // left the room successfully
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { RoomRepository } from '@amityco/ts-sdk'

async function leaveRoom() {
  await RoomRepository.leaveRoom('roomId')
}
```

</CodeGroup>
