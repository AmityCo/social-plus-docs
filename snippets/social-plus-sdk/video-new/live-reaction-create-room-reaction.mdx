{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: live-reaction-create-room-reaction | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createRoomReaction(
        roomId: String,
        referenceId: String,
        referenceType: AmityLiveReactionReferenceType,
        reactionName: String
    ) {
        AmityCoreClient.newLiveReactionRepository()
            .createRoomReaction(
                roomId = roomId,
                referenceId = referenceId,
                referenceType = referenceType,
                reactionName = reactionName
            )
    }
```

```typescript TypeScript
import { LiveStreamRepository } from '@amityco/ts-sdk'

async function createRoomReaction(roomId: string, reactionName: string) {
  await LiveStreamRepository.createReaction({
    roomId,
    reactionName,
    referenceId: roomId,
    referenceType: 'room',
  })
  console.log(`Reaction "${reactionName}" created for room ${roomId}`)
}

// Example usage
createRoomReaction('roomId', 'heart')
```

</CodeGroup>
