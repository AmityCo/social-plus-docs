{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: stream-broadcaster-start-publish | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun streamBroadcasterStartPublish() {
        // Use the AmityStreamBroadcaster to start live streaming
        val streamRepository = com.amityco.socialcloud.sdk.api.video.AmityVideoClient.newStreamRepository()
        streamRepository.createStream(
            title = "My Live Stream",
            description = "Streaming now"
        ).subscribe()
    }
```

```swift iOS
let streamRepository = AmityStreamRepository(client: client)
        do {
            let stream = try await streamRepository.createStream(title: "My Broadcast", description: nil, thumbnailImage: nil, resolution: .HD)
            // AmityBroadcaster is part of AmityVideoBroadcasterKit
            // broadcaster.startPublish(stream: stream) { result in
            //     print("Broadcast started: \(result)")
            // }
            print("Stream created for broadcast: \(stream.streamId)")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void streamBroadcasterStartPublish() {
    AmityVideoClient.newStreamRepository()
        .getStream(streamId: 'streamId')
        .then((stream) {
      // Start broadcasting
      stream.startPublish();
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function streamBroadcasterStartPublish() {
  const streamRepository = new StreamRepository();
  const broadcaster = streamRepository.getBroadcaster();
  await broadcaster.startPublish('streamId');
  console.log('Broadcasting started');
}
```

</CodeGroup>
