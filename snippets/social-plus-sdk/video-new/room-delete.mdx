{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: room-delete | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun roomDelete() {
        // perform room delete operation
        AmitySocialClient.newVideoRoomRepository()
            .deleteRoom("roomId")
            .subscribe()
    }
```

```swift iOS
let streamRepository = AmityStreamRepository(client: AmityUIKitManagerInternal.shared.client)
        streamRepository.deleteStream(streamId: "streamId") { success, error in
            if success {
                print("Stream deleted successfully")
            } else if let error = error {
                print("Failed to delete stream: \(error.localizedDescription)")
            }
        }
```

```dart Flutter
void deleteRoom(String roomId) {
    AmityVideoClient.newRoomRepository()
        .deleteRoom(roomId: roomId)
        .then((_) {
          // room deleted successfully
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { RoomRepository } from '@amityco/ts-sdk'

async function deleteRoom() {
  await RoomRepository.deleteRoom('roomId')
}
```

</CodeGroup>
