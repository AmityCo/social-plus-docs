{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: compose-video-camera-sample | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
@Composable
    fun AmityComposeCameraView(
        modifier: Modifier = Modifier,
        callback: (AmityCameraView) -> Unit
    ) {
        AndroidView(
            factory = { context: Context ->
                AmityCameraView(context)
            },
            update = { view ->
                callback(view)
            },
            modifier = modifier
                .fillMaxSize()
                .aspectRatio(9 / 16f) // set aspect ratio to 16:9
        )
    }

    @Composable
    fun StreamBroadcaster(
        modifier: Modifier = Modifier
    ) {
        AmityComposeCameraView(
            modifier = modifier
        ) { amityCamera ->
            broadcaster = AmityStreamBroadcaster.Builder(amityCamera)
                .setConfiguration(configuration)
                .build()
        }
    }
```

</CodeGroup>
