{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: stream-dispose | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun disposeStream(streamId: String) {
        AmityVideoClient.newStreamRepository()
            .disposeStream(streamId = streamId)
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let streamRepository = AmityStreamRepository(client: client)
        do {
            let success = try await streamRepository.disposeStream(streamId: "<stream-id>")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void streamDispose() {
    // Dispose the stream player/broadcaster when done
    AmityVideoClient.newStreamRepository()
        .getStream(streamId: 'streamId')
        .then((stream) {
      // Release resources
      // broadcaster?.dispose();
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function streamDispose() {
  const streamRepository = new StreamRepository();
  await streamRepository.disposeStream('streamId');
  console.log('Stream disposed');
}
```

</CodeGroup>
