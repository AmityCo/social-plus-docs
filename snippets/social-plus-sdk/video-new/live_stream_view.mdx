{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: live_stream_view | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun liveStreamView() {
        AmitySocialClient.newStreamRepository()
            .getStream("streamId")
            .subscribe { stream ->
                // Stream retrieved, start viewing
            }
    }
```

```swift iOS
func liveStreamView() {
        let streamRepository = AmityStreamRepository(client: AmityManager.shared.client!)
        let liveObject = streamRepository.getStream(withId: "streamId123")
        liveObject.observe { liveObject, error in
            guard let stream = liveObject.object else { return }
            print("Stream status: \(stream.status.rawValue)")
        }
    }
```

```dart Flutter
void liveStreamView() {
    final streamRepository = AmityStreamRepository();
    streamRepository
        .getStreamById('streamId')
        .asStream()
        .listen((stream) {
      print('Stream status: \${stream.status}');
      print('Watch URL: \${stream.watcherUrl}');
    });
  }
```

```typescript TypeScript
import { StreamRepository } from '@amityco/ts-sdk'

async function liveStreamView(streamId: string) {
  const liveObject = StreamRepository.getStreamById(streamId)
  liveObject.on('dataUpdated', (stream) => {
    if (stream.status === 'live') {
      console.log('Watch URL:', stream.watcherUrl)
    }
  })
  return liveObject
}
```

</CodeGroup>
