{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-get-live-room-posts | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getLiveRoomPosts(userId: String) {
        AmitySocialClient.newPostRepository()
            .getPosts()
            .targetUser(userId = userId)
            .types(listOf(AmityPost.DataType.LIVE_STREAM))
            .build()
            .query()
            .doOnNext { posts: PagingData<AmityPost> ->
                // PagingData<AmityPost> for live room posts
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let feedRepository = AmityFeedRepository(client: AmityClient.shared)
        let communityId = "community_id"
        let liveCollection = feedRepository.getCommunityFeed(
            withCommunityId: communityId,
            sortBy: .lastCreated,
            includeDeleted: false,
            feedType: .published
        )
        liveCollection.observe { collection, _, error in
            if let error = error {
                print("Failed to get live room posts: \(error.localizedDescription)")
                return
            }
            let posts = collection.allObjects().filter { $0.dataType == "liveStream" }
            print("Live room posts count: \(posts.count)")
        }
```

```dart Flutter
void getLiveRoomPosts(String streamId) {
      AmitySocialClient.newPostRepository()
          .getPosts()
          .targetStream(streamId)
          .build()
          .query()
          .listen((List<AmityPost> posts) {
        // Handle live room posts
      }).onError((error, stackTrace) {
        // Handle error
      });
    }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function postGetLiveRoomPosts() {
  const liveCollection = PostRepository.getPosts({ targetId: 'userId123', targetType: 'user', dataTypes: ['liveStream'] });
  liveCollection.on('dataUpdated', (posts) => {
    console.log('Live room posts:', posts);
  });
}
```

</CodeGroup>
