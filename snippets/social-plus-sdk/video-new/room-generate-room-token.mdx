{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: room-generate-room-token | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun roomGenerateRoomToken() {
        // perform room generate room token operation
        AmitySocialClient.newVideoRoomRepository()
            .getRoom("roomId")
            .subscribe { room ->
                // handle room object
            }
    }
```

```swift iOS
do {
            let tokenData = try await roomRepository.generateRoomToken(withId: "<room-id>")
            if let token = tokenData?["token"] as? String {
                print("Room token: \(token)")
            }
        } catch {
            // Handle error here
        }
```

```dart Flutter
void generateRoomToken(String roomId) {
    AmityVideoClient.newRoomRepository()
        .generateRoomToken(roomId: roomId)
        .then((AmityRoomToken token) {
          final accessToken = token.token;
          // use accessToken to connect to the room via a video SDK
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function roomGenerateRoomToken() {
  const { token } = await RoomRepository.generateRoomToken({
    roomId: 'room-id-123',
  });
  console.log('Room token:', token);
}
```

</CodeGroup>
