{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: room-get-broadcaster-data | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getBroadcasterData(streamId: String) {
        AmityVideoClient.newStreamRepository()
            .getStream(streamId = streamId)
            .doOnNext { stream: AmityStream ->
                val broadcasterClient = stream.getStreamerUrl()
                val streamKey = stream.getStreamerKey()
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```dart Flutter
void getBroadcasterData(String streamId) {
    AmityVideoClient.newStreamRepository()
        .getStream(streamId)
        .then((AmityStream stream) {
          final streamKey = stream.streamKey;
          final ingestUrl = stream.ingestUrl;
          final resolution = stream.resolution;
          // use streamKey and ingestUrl with a broadcasting SDK
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { RoomRepository } from '@amityco/ts-sdk'

async function getBroadcasterData() {
  const liveObject = RoomRepository.getBroadcasterData('roomId')
  liveObject.on('dataUpdated', (data) => {
    console.log(data)
  })
}
```

</CodeGroup>
