{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: stream-live-query | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun streamLiveQuery() {
        AmitySocialClient.newStreamRepository()
            .getStreams()
            .streamStatus(listOf(AmityStream.Status.LIVE))
            .build()
            .query()
            .subscribe({ pagingData ->
                // Handle live streams list
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
let streamRepository = AmityStreamRepository(client: client)
        var token: AmityNotificationToken?
        let query = AmityStreamCollectionQuery()
        query.includeStatus(.live)
        token = streamRepository.getStreams(from: query).observe { collection, change, error in
            guard error == nil else { return }
            print("Live streams: \(collection.count())")
        }
```

```dart Flutter
void streamLiveQuery() {
    final _amityStreams = <AmityStream>[];
    late PagingController<AmityStream> _controller;

    _controller = PagingController(
      pageFuture: (token) => AmityVideoClient.newStreamRepository()
          .getStreams()
          .isLive(true)
          .status([AmityStreamStatus.live])
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    )..addListener(() {
        if (_controller.error == null) {
          _amityStreams
            ..clear()
            ..addAll(_controller.loadedItems);
        }
      });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function streamLiveQuery() {
  const streamRepository = new StreamRepository();
  const liveCollection = streamRepository.getLiveStreams({ status: 'live' });
  liveCollection.on('dataUpdated', (streams) => {
    console.log('Live streams:', streams);
  });
}
```

</CodeGroup>
