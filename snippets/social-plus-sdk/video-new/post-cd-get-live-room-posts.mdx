{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-cd-get-live-room-posts | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun postCdGetLiveRoomPosts() {
        // Query live posts as a live collection
        AmitySocialClient.newPostRepository()
            .getPosts()
            .targetMe()
            .build()
            .query()
            .subscribe({ pagingData ->
                // Observe live post updates
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
let postRepository = AmityPostRepository(client: client)
        var token: AmityNotificationToken?
        token = postRepository.getPosts(targetId: nil, targetType: .global, sortBy: .lastCreated, includeDeleted: false, feedType: .liveStream).observe { collection, change, error in
            guard error == nil else { return }
            print("Live room posts: \(collection.count())")
        }
```

```dart Flutter
void postCdGetLiveRoomPosts() async {
        AmitySocialClient.newPostRepository()
            .getPosts()
            .types([AmityDataType.liveStream])
            .getLiveCollection()
            .asStream()
            .listen((posts) {
              print('Live room posts: \${posts.length}');
              for (final post in posts) {
                print('Post ID: \${post.postId}');
              }
            });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function postCdGetLiveRoomPosts() {
  const liveCollection = PostRepository.getPosts({ targetId: 'userId123', targetType: 'user', dataTypes: ['liveStream'] });
  liveCollection.on('dataUpdated', (posts) => {
    console.log('Live room posts:', posts);
  });
}
```

</CodeGroup>
