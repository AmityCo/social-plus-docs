{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-stream-from-post | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getStreamFromPost(post: AmityPost) {
        val liveStreamData = post.getData() as? AmityPost.Data.LIVESTREAM
        liveStreamData?.getStreamId()?.let { streamId ->
            AmityVideoClient.newStreamRepository()
                .getStream(streamId = streamId)
                .doOnNext { stream: AmityStream ->
                    val status = stream.getStatus()
                    val watcherCount = stream.getWatcherCount()
                    // handle stream data
                }
                .doOnError {
                    // handle error
                }
                .subscribe()
        }
    }
```

```swift iOS
// Extract stream ID from a post and get the stream
        let postRepository = AmityPostRepository(client: AmityManager.shared.client!)
        let streamRepository = AmityStreamRepository(client: AmityManager.shared.client!)
        var postToken: AmityNotificationToken?
        var streamToken: AmityNotificationToken?
        let liveObject = postRepository.getPost(withPostId: "postId")
        postToken = liveObject.observe { post, error in
            if let error = error {
                print("Failed to get post: \(error.localizedDescription)")
                return
            }
            guard let post = post.object,
                  let streamData = post.data as? [String: Any],
                  let streamId = streamData["streamId"] as? String else { return }
            let streamObject = streamRepository.getStream(withStreamId: streamId)
            streamToken = streamObject.observe { stream, _ in
                print("Stream from post: \(stream.object?.title ?? "")")
            }
        }
```

```dart Flutter
void getStreamFromPost() {
    final post = await AmitySocialClient.newPostRepository()
        .getPost('postId');
    final liveStreamData = post.data as AmityLivestreamData;
    final stream = liveStreamData.stream;
    print('Stream ID: \${stream?.streamId}, status: \${stream?.status}');
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function getStreamFromPost() {
  const liveObject = PostRepository.getPost('postId123');
  liveObject.on('dataUpdated', (post) => {
    const streamChild = post.children?.find((c: any) => c.dataType === 'liveStream');
    if (streamChild) {
      console.log('Stream ID:', streamChild.data?.streamId, 'status:', streamChild.data?.status);
    }
  });
}
```

</CodeGroup>
