{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: room-update-cohost-permission | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun updateCohostPermission(
        roomId: String,
        cohostId: String,
        canManageProductTags: Boolean
    ) {
        AmityVideoClient.newRoomRepository()
            .updateCohostPermission(
                roomId = roomId,
                cohostId = cohostId,
                canManageProductTags = canManageProductTags
            )
            .doOnComplete { /* permission updated */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let streamRepository = AmityStreamRepository(client: AmityUIKitManagerInternal.shared.client)
        streamRepository.updateCohostPermission(streamId: "streamId", userId: "userId", permission: .coHost) { success, error in
            if success {
                print("Co-host permission updated")
            } else if let error = error {
                print("Failed to update co-host permission: \(error.localizedDescription)")
            }
        }
```

```dart Flutter
void roomUpdateCohostPermission() {
    AmityCoreClient.newVideoRepository()
        .room(roomId: 'roomId123')
        .updateCohostPermission(
          canInviteParticipants: true,
          canMuteParticipants: false,
        )
        .then((_) => print('Cohost permission updated'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function roomUpdateCohostPermission() {
  await RoomRepository.updateCoHostPermission({
    roomId: 'room-id-123',
    userId: 'user-id-456',
    permission: 'coHost',
  });
  console.log('Co-host permission updated');
}
```

</CodeGroup>
