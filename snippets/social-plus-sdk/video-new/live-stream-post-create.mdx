{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: live-stream-post-create | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createLiveStreamPost(targetId: String, streamId: String) {
        AmitySocialClient.newPostRepository()
            .createPost()
            .targetCommunity(communityId = targetId)
            .liveStream(streamId = streamId)
            .build()
            .post()
            .doOnSuccess { post: AmityPost ->
                // handle live stream post
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let postRepository = AmityPostRepository(client: client)
        let streamRepository = AmityStreamRepository(client: client)
        do {
            let stream = try await streamRepository.createStream(title: "Live Stream 101", description: nil, thumbnailImage: nil, resolution: .HD)
            let builder = AmityLivestreamPostBuilder()
            builder.setStreamId(stream.streamId)
            let post = try await postRepository.createLivestreamPost(builder, targetId: "<community-id>", targetType: .community, metadata: nil, mentionees: nil)
            print("Live post created: \(post.postId)")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void liveStreamPostCreate() async {
    final postRepo = AmitySocialClient.newPostRepository();
    final post = await postRepo
        .createPost()
        .targetCommunity('communityId123')
        .liveStream(streamId: 'streamId123')
        .post();
    print('Live stream post created: \${post.postId}');
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function createLiveStreamPost(communityId: string, streamId: string) {
  const post = await PostRepository.createPost({
    targetType: 'community',
    targetId: communityId,
    dataType: 'liveStream',
    data: { streamId },
  })

  console.log('Live stream post created:', post.postId)
  return post
}

// Example usage
createLiveStreamPost('communityId', 'streamId')
```

</CodeGroup>
