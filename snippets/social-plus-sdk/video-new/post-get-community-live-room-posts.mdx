{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-get-community-live-room-posts | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getCommunityLiveRoomPosts(communityId: String) {
        AmitySocialClient.newPostRepository()
            .getPosts()
            .targetCommunity(communityId = communityId)
            .types(listOf(AmityPost.DataType.LIVE_STREAM))
            .build()
            .query()
            .doOnNext { posts: PagingData<AmityPost> ->
                // PagingData<AmityPost> for community live room posts
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let feedRepository = AmityFeedRepository(client: AmityClient.shared)
        let communityId = "community_id"
        let liveCollection = feedRepository.getCommunityFeed(
            withCommunityId: communityId,
            sortBy: .lastCreated,
            includeDeleted: false,
            feedType: .published
        )
        liveCollection.observe { collection, _, error in
            if let error = error {
                print("Failed to get live room posts: \(error.localizedDescription)")
                return
            }
            let posts = collection.allObjects().filter { $0.dataType == "liveStream" }
            print("Live room posts count: \(posts.count)")
        }
```

```dart Flutter
void postGetCommunityLiveRoomPosts() async {
        AmitySocialClient.newPostRepository()
            .getPosts()
            .targetCommunity('communityId')
            .types([AmityDataType.liveStream])
            .getLiveCollection()
            .asStream()
            .listen((posts) {
              print('Community live room posts: \${posts.length}');
              for (final post in posts) {
                print('Live post ID: \${post.postId}');
              }
            });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function postGetCommunityLiveRoomPosts() {
  const liveCollection = PostRepository.getPosts({ targetId: 'communityId123', targetType: 'community', dataTypes: ['liveStream'] });
  liveCollection.on('dataUpdated', (posts) => {
    console.log('Community live room posts:', posts);
  });
}
```

</CodeGroup>
