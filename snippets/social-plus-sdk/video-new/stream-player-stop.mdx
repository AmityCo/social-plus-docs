{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: stream-player-stop | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun stopStream(streamId: String) {
        AmityVideoClient.newStreamRepository()
            .disposeStream(streamId = streamId)
            .doOnComplete {
                // stream disposed
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
// AmityVideoPlayer is part of AmityVideoPlayerKit
        // player.stop()
        print("Stream player stopped")
```

```dart Flutter
void streamPlayerStop() {
    // Stop stream playback and release player resources
    // videoPlayerController?.pause();
    // videoPlayerController?.dispose();
    AmityVideoClient.newStreamRepository()
        .getStream(streamId: 'streamId')
        .then((stream) {
      // Stop the player referencing this stream
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function streamPlayerStop() {
  const streamRepository = new StreamRepository();
  const player = streamRepository.getPlayer();
  await player.stop();
  console.log('Playback stopped');
}
```

</CodeGroup>
