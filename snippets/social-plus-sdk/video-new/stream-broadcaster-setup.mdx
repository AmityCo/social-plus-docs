{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: stream-broadcaster-setup | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun setupBroadcaster(title: String, description: String) {
        AmityVideoClient.newStreamRepository()
            .createStream(
                title = title,
                description = description,
                resolution = AmityBroadcastResolution.HD_720P,
                isSecure = true
            )
            .doOnSuccess { stream: AmityStream ->
                val streamerUrl = stream.getStreamerUrl()
                val streamerKey = stream.getStreamerKey()
                // use these to configure your RTMP broadcaster
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
// AmityBroadcaster is part of AmityVideoBroadcasterKit
        // let broadcaster = AmityBroadcaster(client: client)
        // broadcaster.renderView = yourRenderView
        // broadcaster.setup()
        print("Broadcaster set up")
```

```dart Flutter
void streamBroadcasterSetup() {
    final broadcaster = await AmityCoreClient.newVideoRepository()
        .createBroadcaster(streamId: 'streamId123');
    await broadcaster.setup(
      resolution: AmityStreamResolution.HD,
      bitrate: 2000,
    );
    print('Broadcaster setup complete');
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function streamBroadcasterSetup() {
  const streamRepository = new StreamRepository();
  const broadcaster = await streamRepository.createBroadcaster({
    title: 'My Live Stream',
    thumbnailFileId: 'thumbnailId',
  });
  console.log('Broadcaster setup:', broadcaster);
}
```

</CodeGroup>
