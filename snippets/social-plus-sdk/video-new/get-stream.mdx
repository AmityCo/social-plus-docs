{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-stream | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getStream() {
        // Retrieve a live stream by its ID
        AmityVideoClient.newStreamRepository()
            .getStream(streamId = "streamId")
            .subscribe { stream -> /* handle stream */ }
    }
```

```swift iOS
// Get a live stream by its stream ID
        let streamRepository = AmityStreamRepository(client: AmityManager.shared.client!)
        var token: AmityNotificationToken?
        let liveObject = streamRepository.getStream(withStreamId: "streamId")
        token = liveObject.observe { stream, error in
            if let error = error {
                print("Failed to get stream: \(error.localizedDescription)")
                return
            }
            guard let stream = stream.object else { return }
            print("Stream: \(stream.streamId), status: \(stream.status.rawValue)")
        }
```

```dart Flutter
void getStream() async {
    final stream = await AmitySocialClient.newStreamRepository()
        .getStream(streamId: 'streamId');
  }
```

```typescript TypeScript
import { StreamRepository } from '@amityco/ts-sdk'

function getStream() {
  const streamId = 'stream123'
  const liveObject = StreamRepository.getStream(streamId)
  liveObject.on('dataUpdated', (stream) => {
    console.log('Stream:', stream)
  })
}
```

</CodeGroup>
