{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: stream-broadcaster-stop-publish | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun streamBroadcasterStopPublish() {
        // Use the AmityStreamBroadcaster to start live streaming
        val streamRepository = com.amityco.socialcloud.sdk.api.video.AmityVideoClient.newStreamRepository()
        streamRepository.createStream(
            title = "My Live Stream",
            description = "Streaming now"
        ).subscribe()
    }
```

```swift iOS
// AmityBroadcaster is part of AmityVideoBroadcasterKit
        // broadcaster.stopPublish { result in
        //     print("Broadcast stopped")
        // }
        let streamRepository = AmityStreamRepository(client: client)
        do {
            try await streamRepository.endStream(streamId: "<stream-id>")
            print("Stream ended")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void streamBroadcasterStopPublish() {
    AmityVideoClient.newStreamRepository()
        .getStream(streamId: 'streamId')
        .then((stream) {
      stream.stopPublish();
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function streamBroadcasterStopPublish() {
  const streamRepository = new StreamRepository();
  const broadcaster = streamRepository.getBroadcaster();
  await broadcaster.stopPublish();
  console.log('Broadcasting stopped');
}
```

</CodeGroup>
