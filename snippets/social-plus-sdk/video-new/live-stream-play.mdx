{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: live-stream-play | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun liveStreamPlay() {
        // TODO: implement Live stream play
    }
```

```swift iOS
// AmityStreamPlayer is provided by AmityVideoPlayerKit
        // Retrieve the stream URL and pass it to the player
        let streamRepository = AmityStreamRepository(client: AmityClient.shared)
        let liveObject = streamRepository.getStream(withId: "streamId")
        liveObject.observe { liveStream, error in
            guard let stream = liveStream.object, error == nil else { return }
            print("Stream status: \(stream.status.rawValue)")
            // player.play(url: stream.watcherUrl)
        }
```

```dart Flutter
void liveStreamPlay() async {
    final streamRepo = AmityVideoClient.newStreamRepository();
    final stream = await streamRepo.getStream('streamId123');
    final watchUrl = stream.watcherUrl;
    print('Play stream at: \$watchUrl');
  }
```

```typescript TypeScript
import { StreamRepository } from '@amityco/ts-sdk'

function playLiveStream(streamId: string) {
  const unsubscribe = StreamRepository.getStream(streamId, ({ data: stream, loading, error }) => {
    if (loading) return
    if (error) {
      console.error('Error fetching stream:', error)
      return
    }

    if (stream.status === 'live') {
      console.log('Stream is live, play URL:', stream.watcherUrl)
    } else {
      console.log('Stream is not live, status:', stream.status)
    }
  })

  return unsubscribe
}

// Example usage
playLiveStream('streamId')
```

</CodeGroup>
