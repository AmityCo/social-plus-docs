{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: child-stream-access | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun accessChildStream(
        stream: AmityStream,
    ) {
        if(stream.isParent()) {
            val children = stream.getChildStreams() // List<AmityStream>
        }
    }
```

```swift iOS
let roomRepository = AmityRoomRepository(client: AmityClient.shared)
        let token = roomRepository.getRoom(withId: "roomId").observe { liveObject, error in
            guard let room = liveObject.object else { return }
            print("Child streams: \(room.streamIds)")
        }
```

```dart Flutter
void accessChildStream(AmityStream stream) {
    if (stream.isParent()) {
      final children = stream.getChildStreams();
      // children is a List<AmityStream>
    }
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function childStreamAccess() {
  const streamId = 'stream-id-here';
  const liveObject = StreamRepository.getStream(streamId);
  liveObject.on('dataUpdated', (stream) => {
    console.log('Stream status:', stream.status);
    console.log('Video URL:', stream.videoURL);
  });
}
```

</CodeGroup>
