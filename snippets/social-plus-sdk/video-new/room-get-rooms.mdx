{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: room-get-rooms | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
final List<AmityRoom> _rooms = [];
  late PagingController<AmityRoom> _controller;
  final scrollController = ScrollController();

  void getRooms() {
    _controller = PagingController(
      pageFuture: (token) => AmityVideoClient.newRoomRepository()
          .getRooms()
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    )..addListener(() {
        if (_controller.error == null) {
          _rooms
            ..clear()
            ..addAll(_controller.loadedItems);
          // update UI
        } else {
          // handle error
        }
      });

    scrollController.addListener(() {
      if (scrollController.position.pixels ==
              scrollController.position.maxScrollExtent &&
          _controller.hasMoreItems) {
        _controller.fetchNextPage();
      }
    });
  }
```

```typescript TypeScript
import { RoomRepository } from '@amityco/ts-sdk'

async function getRooms() {
  const liveCollection = RoomRepository.getRooms({ targetType: 'community', targetId: 'communityId' })
  liveCollection.on('dataUpdated', (rooms) => {
    console.log(rooms)
  })
}
```

</CodeGroup>
