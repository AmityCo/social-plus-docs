{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: stop_broadcasting | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun stopBroadcasting() {
        // Use the AmityStreamBroadcaster to start live streaming
        val streamRepository = com.amityco.socialcloud.sdk.api.video.AmityVideoClient.newStreamRepository()
        streamRepository.createStream(
            title = "My Live Stream",
            description = "Streaming now"
        ).subscribe()
    }
```

```swift iOS
let streamRepository = AmityStreamRepository(client: AmityManager.shared.client!)
        let broadcaster = streamRepository.createBroadcaster()
        broadcaster.stopPublishing { error in
            print("Broadcasting stopped, error: \(String(describing: error))")
        }
```

```dart Flutter
void stopBroadcasting() async {
    await AmityStreamRepository()
        .getStream('streamId')
        .then((stream) => stream.stopBroadcasting());
    print('Broadcasting stopped');
  }
```

```typescript TypeScript
import { StreamRepository } from '@amityco/ts-sdk'

async function stopBroadcasting(streamId: string) {
  await StreamRepository.stopStream(streamId)
  console.log(`Broadcasting stopped for stream ${streamId}`)
}
```

</CodeGroup>
