{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: online-users-count | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getOnlineUsersCount() {
        AmityCoreClient.newUserPresenceRepository()
            .getOnlineUsersCount()
            .doOnSuccess { count: Int ->
                //  Online Users count
            }
            .doOnError {
                //  Handle Exception
            }
            .subscribe()

    }
```

```swift iOS
let streamId = "stream_id"
        let streamRepository = AmityStreamRepository(client: AmityClient.shared)
        streamRepository.getOnlineUserCount(streamId: streamId) { count, error in
            if let error = error {
                print("Failed to get online user count: \(error.localizedDescription)")
                return
            }
            print("Online users count: \(count)")
        }
```

```dart Flutter
void onlineUsersCount() async {
        final count = await AmityCoreClient.getOnlineUsersCount(streamId: 'streamId');
        print('Online users count: \$count');
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function getOnlineUsersCount() {
  const { data: count } = await UserRepository.getOnlineUsersCount()
  console.log('Online users count:', count)
}
```

</CodeGroup>
