{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: live-reaction-create-reaction | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createReaction(
        liveStreamId: String,
        referenceId: String,
        referenceType: AmityLiveReactionReferenceType,
        reactionName: String
    ) {
        AmityCoreClient.newLiveReactionRepository()
            .createReaction(
                liveStreamId = liveStreamId,
                referenceId = referenceId,
                referenceType = referenceType,
                reactionName = reactionName
            )
    }
```

```swift iOS
let reactionRepository = AmityReactionRepository(client: AmityUIKitManagerInternal.shared.client)
        let contentId = "content123"
        reactionRepository.addReaction("like", referenceId: contentId, referenceType: .content) { success, error in
            print("Reaction created: \(success)")
        }
```

```dart Flutter
void liveReactionCreateReaction() async {
    final streamRepo = AmityVideoClient.newStreamRepository();
    await streamRepo.createReaction(
      streamId: 'streamId123',
      reactionType: 'like',
    );
    print('Reaction sent to live stream.');
  }
```

```typescript TypeScript
import { LiveStreamRepository } from '@amityco/ts-sdk'

async function createLiveReaction(liveStreamId: string, reactionName: string) {
  await LiveStreamRepository.createReaction({
    liveStreamId,
    reactionName,
    referenceId: liveStreamId,
    referenceType: 'liveStream',
  })
  console.log(`Reaction "${reactionName}" created for live stream ${liveStreamId}`)
}

// Example usage
createLiveReaction('liveStreamId', 'like')
```

</CodeGroup>
