{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: room-get | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getRoom(streamId: String) {
        AmityVideoClient.newStreamRepository()
            .getStream(streamId = streamId)
            .doOnNext { stream: AmityStream ->
                val title = stream.getTitle()
                val status = stream.getStatus()
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let streamRepository = AmityStreamRepository(client: client)
        var token: AmityNotificationToken?
        token = streamRepository.getStream(streamId: "<stream-id>").observe { liveObject, error in
            guard let stream = liveObject.object else { return }
            print("Stream title: \(stream.title ?? "")")
        }
```

```dart Flutter
void getRoom(String roomId) {
    AmityVideoClient.newRoomRepository()
        .getRoom(roomId: roomId)
        .then((AmityRoom room) {
          final title = room.title;
          final status = room.status;
          // use room data
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { RoomRepository } from '@amityco/ts-sdk'

function watchRoom() {
  const unsubscribe = RoomRepository.getRoom('roomId', ({ data: room }) => {
    console.log(room)
  })
  return unsubscribe
}
```

</CodeGroup>
