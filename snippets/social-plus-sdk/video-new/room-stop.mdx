{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: room-stop | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun roomStop() {
        // perform room stop operation
        AmitySocialClient.newVideoRoomRepository()
            .stopRoom("roomId")
            .subscribe()
    }
```

```swift iOS
let streamRepository = AmityStreamRepository(client: AmityUIKitManagerInternal.shared.client)
        streamRepository.stopStream(streamId: "streamId") { success, error in
            if success {
                print("Stream stopped successfully")
            } else if let error = error {
                print("Failed to stop stream: \(error.localizedDescription)")
            }
        }
```

```dart Flutter
void roomStop() {
    AmityCoreClient.newVideoRepository()
        .room(roomId: 'roomId123')
        .stopBroadcasting()
        .then((_) => print('Room broadcasting stopped'))
        .onError((error, _) => print('Error stopping room: \$error'));
  }
```

```typescript TypeScript
import { RoomRepository } from '@amityco/ts-sdk'

async function stopRoom() {
  await RoomRepository.stopRoom('roomId')
}
```

</CodeGroup>
