{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: recorded-stream-play | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun playRecordedStream(
        stream: AmityStream,
        player: AmityVideoPlayer
    ) {
        player.play(streamId = stream.getStreamId())
    }
```

```swift iOS
let streamRepository = AmityStreamRepository(client: AmityUIKitManagerInternal.shared.client)
        let object = streamRepository.getStream(streamId: "streamId")
        object.observe { liveObject, error in
            guard error == nil, let stream = liveObject.snapshot else { return }
            if let recordedUrl = stream.recordedUrl {
                print("Recorded stream URL: \(recordedUrl)")
                // Use recordedUrl with AVPlayer or equivalent to play back
            }
        }
```

```dart Flutter
void playRecordedStream(String streamId) {
    AmityVideoClient.newStreamRepository()
        .getStream(streamId)
        .then((AmityStream stream) {
          final recordedUrl = stream.recordingData?.url;
          if (recordedUrl != null) {
            // use recordedUrl with a video player widget to play the recorded stream
          }
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function recordedStreamPlay() {
  const unsub = StreamRepository.getStreamById('streamId123').subscribe({
    next: ({ data: stream }) => {
      if (stream.status === 'recorded' && stream.url) {
        console.log('Playing recorded stream at:', stream.url);
        // Pass stream.url to your video player component
      } else {
        console.log('Stream is not yet available for playback');
      }
    },
  });
  return unsub;
}
```

</CodeGroup>
