{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: room-presence-get-room-online-users | updated: 2026-05-01 */}

<CodeGroup>

```swift iOS
let presenceRepository = AmityRoomPresenceRepository(roomId: "<room-id>")
        do {
            let onlineUsers = try await presenceRepository.getRoomOnlineUsers()
            for user in onlineUsers {
                print("Online user: \(user.userId)")
            }
        } catch {
            // Handle error here
        }
```

```typescript TypeScript
import { RoomPresenceRepository } from '@amityco/ts-sdk'

async function getRoomOnlineUsers() {
  const liveCollection = RoomPresenceRepository.getRoomOnlineUsers('roomId')
  liveCollection.on('dataUpdated', (users) => {
    console.log(users)
  })
}
```

</CodeGroup>
