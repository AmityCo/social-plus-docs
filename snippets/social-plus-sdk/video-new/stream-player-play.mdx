{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: stream-player-play | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun playStream(streamId: String) {
        AmityVideoClient.newStreamRepository()
            .getStream(streamId = streamId)
            .firstOrError()
            .doOnSuccess { stream: AmityStream ->
                val watcherUrl = stream.getWatcherUrl()
                // use watcherUrl to play the stream with your media player
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
// AmityVideoPlayer is part of AmityVideoPlayerKit
        // let player = AmityVideoPlayer(client: client)
        // player.renderView = yourRenderView
        // player.play(stream) { result in
        //     switch result {
        //     case .success: print("Playback started")
        //     case .failure(let error): print("Error: \(error)")
        //     }
        // }
        print("Stream player started")
```

```dart Flutter
void streamPlayerPlay() {
    // Initialize and start stream playback
    AmityVideoClient.newStreamRepository()
        .getStream(streamId: 'streamId')
        .then((stream) {
      final watcherUrl = stream.watcherUrl;
      // Pass watcherUrl to video player controller and call play()
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function streamPlayerPlay() {
  const streamRepository = new StreamRepository();
  const player = streamRepository.getPlayer();
  await player.play('streamId');
  console.log('Playback started');
}
```

</CodeGroup>
