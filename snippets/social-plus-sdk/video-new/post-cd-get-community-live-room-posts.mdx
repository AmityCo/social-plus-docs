{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-cd-get-community-live-room-posts | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun postCdGetCommunityLiveRoomPosts() {
        // Query live posts as a live collection
        AmitySocialClient.newPostRepository()
            .getPosts()
            .targetMe()
            .build()
            .query()
            .subscribe({ pagingData ->
                // Observe live post updates
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
let postRepository = AmityPostRepository(client: client)
        var token: AmityNotificationToken?
        token = postRepository.getPosts(targetId: "<community-id>", targetType: .community, sortBy: .lastCreated, includeDeleted: false, feedType: .liveStream).observe { collection, change, error in
            guard error == nil else { return }
            print("Community live room posts: \(collection.count())")
        }
```

```dart Flutter
void postCdGetCommunityLiveRoomPosts() async {
        AmitySocialClient.newPostRepository()
            .getPosts()
            .targetCommunity('communityId')
            .types([AmityDataType.liveStream])
            .getLiveCollection()
            .asStream()
            .listen((posts) {
              print('Community live room posts: \${posts.length}');
              for (final post in posts) {
                print('Live post: \${post.postId}');
              }
            });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function postCdGetCommunityLiveRoomPosts() {
  const liveCollection = PostRepository.getPosts({
    targetId: 'communityId123',
    targetType: 'community',
    dataTypes: ['liveStream'],
  });
  liveCollection.on('dataUpdated', (posts) => {
    console.log('Community live room posts:', posts);
  });
}
```

</CodeGroup>
