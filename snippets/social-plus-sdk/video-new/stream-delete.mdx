{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: stream-delete | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun streamDelete() {
        AmitySocialClient.newStreamRepository()
            .deleteStream("streamId")
            .subscribe({
                // Stream deleted successfully
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
let streamRepository = AmityStreamRepository(client: AmityManager.shared.client!)
        streamRepository.deleteStream(streamId: "streamId") { success, error in
            print("Stream deleted: \(success)")
        }
```

```dart Flutter
void streamDelete() {
    AmityVideoClient.newStreamRepository()
        .deleteStream(streamId: 'streamId')
        .then((_) {
      // Stream deleted successfully
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { StreamRepository } from '@amityco/ts-sdk'

async function deleteStream() {
  await StreamRepository.deleteStream('streamId')
}
```

</CodeGroup>
