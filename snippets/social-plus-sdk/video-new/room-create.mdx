{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: room-create | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun roomCreate() {
        // perform room create operation
        // Create a new video room
        AmitySocialClient.newVideoRoomRepository()
            .createRoom()
            .title("My Live Stream")
            .build()
            .create()
            .subscribe { room ->
                // handle created room
            }
    }
```

```swift iOS
let streamRepository = AmityStreamRepository(client: AmityUIKitManagerInternal.shared.client)
        let builder = AmityStreamCreateDataBuilder()
        builder.setTitle("My Live Stream")
        builder.setDescription("A live stream description")
        streamRepository.createStream(with: builder) { stream, error in
            if let stream = stream {
                print("Stream created: \(stream.streamId)")
            } else if let error = error {
                print("Failed to create stream: \(error.localizedDescription)")
            }
        }
```

```dart Flutter
void createRoom(String title) {
    AmityVideoClient.newRoomRepository()
        .createRoom(title: title)
        .then((AmityRoom room) {
          final roomId = room.roomId;
          // use roomId to join or manage the room
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { RoomRepository } from '@amityco/ts-sdk'

async function createRoom() {
  const { data: room } = await RoomRepository.createRoom({
    title: 'My Live Room',
    isPrivate: false,
  })
  console.log(room)
}
```

</CodeGroup>
