{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: room-presence-room-id | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun buildPresenceForRoom(roomId: String): AmityRoomPresenceRepository {
        return AmityRoomPresenceRepository.Builder()
            .roomId(roomId = roomId)
    }
```

```swift iOS
let streamRepository = AmityStreamRepository(client: AmityUIKitManagerInternal.shared.client)
        let roomId = "roomId"
        let object = streamRepository.getStream(streamId: roomId)
        object.observe { liveObject, error in
            guard error == nil, let stream = liveObject.snapshot else { return }
            print("Presence for room \(roomId): \(stream.watcherCount) online users")
        }
```

```dart Flutter
void initRoomPresence(String roomId) {
    final presenceRepository = AmityRoomPresenceRepository(roomId: roomId);
    // use presenceRepository to get online users, count, or start heartbeat
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function roomPresenceRoomId() {
  const unsubscribe = RoomRepository.getPresenceByRoomId(
    { roomId: 'room-id-123' },
    ({ data: presence, loading, error }) => {
      if (!loading && presence) {
        console.log('Presence for room:', presence);
      }
    },
  );
  return unsubscribe;
}
```

</CodeGroup>
