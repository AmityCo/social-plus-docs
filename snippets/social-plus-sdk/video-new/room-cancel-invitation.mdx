{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: room-cancel-invitation | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun roomCancelInvitation() {
        // perform room cancel invitation operation
        AmitySocialClient.newVideoRoomRepository()
            .createInvitation("roomId")
            .users(listOf("userId"))
            .submit()
            .subscribe()
    }
```

```swift iOS
let streamRepository = AmityStreamRepository(client: client)
        do {
            try await streamRepository.cancelInvitation(streamId: "<stream-id>", userIds: ["<user-id>"])
            print("Invitation cancelled")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void cancelRoomInvitation(String streamId, List<String> userIds) {
    AmityVideoClient.newStreamRepository()
        .cancelInvitation(streamId: streamId, userIds: userIds)
        .then((_) {
          // invitations cancelled
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function roomCancelInvitation() {
  await StreamRepository.cancelRoomInvitation({
    roomId: 'roomId123',
    userId: 'userId123',
  });
  console.log('Video room invitation cancelled');
}
```

</CodeGroup>
