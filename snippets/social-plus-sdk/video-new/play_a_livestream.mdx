{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: play_a_livestream | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun playALivestream() {
        // Play a live stream using the stream repository
        AmitySocialClient.newStreamRepository()
            .getStream(streamId = "stream123")
            .subscribe({ stream ->
                // Observe live stream state and play URL
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
// AmityVideoPlayer is the part of AmityVideoPlayerKit.
    // Please import AmityVideoPlayerKit to use it.
    func playLivestream(client: AmityClient, stream: AmityStream) {
        player = AmityVideoPlayer(client: client)
        player.renderView = yourViewToRenderVideo
        
        player?.play(stream, completion: { [weak self] result in
            guard let self else {
                return
            }
            
            switch result {
            case .failure(let error):
                print("Error: \(error.localizedDescription)")
            case .success:
                break
            }
        })
    }
```

```dart Flutter
void playALivestream() async {
        final streamController = AmityStreamController();
        await streamController.play(streamId: 'streamId');
        print('Livestream playback started');

        // To stop playback
        // await streamController.stop();
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function playALivestream() {
  const streamPost = await PostRepository.getPost('postId123').model;
  const streamId = streamPost?.children?.[0];
  if (streamId) {
    console.log('Playing livestream with stream ID:', streamId);
  }
}
```

</CodeGroup>
