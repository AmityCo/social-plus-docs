{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: sdk-connect | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun sdkConnect() {
        // TODO: implement sdk connect using Social Plus SDK
    }
```

```swift iOS
func sdkConnect() {
        let client = try! AmityClient(apiKey: "your-api-key")
        
        client.login(userId: "userId", displayName: "Display Name", authToken: nil) { success, error in
            if let error = error {
                print("SDK connection failed: \(error)")
                return
            }
            print("SDK connected successfully")
        }
    }
```

```dart Flutter
void sdkConnect() {
    AmityCoreClient.setup(
      option: AmityCoreClientOption(apiKey: 'YOUR_API_KEY'),
    );
    await AmityCoreClient.registerDevice(
      userId: 'userId123',
      displayName: 'John Doe',
    );
    print('SDK connected');
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function sdkConnect() {
  const client = createClient('YOUR_API_KEY');

  await client.login({
    userId: 'user-id-123',
    displayName: 'John Doe',
    authToken: 'secure-auth-token',
  });
  console.log('SDK connected successfully');
}
```

</CodeGroup>
