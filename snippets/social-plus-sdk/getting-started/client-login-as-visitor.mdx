{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: client-login-as-visitor | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun clientLoginAsVisitor() {
        AmityCoreClient.login(userId = "visitor_${System.currentTimeMillis()}")
            .displayName("Guest")
            .submit()
            .subscribe(
                { println("Visitor login successful") },
                { error -> println("Visitor login failed: ${error.message}") }
            )
    }
```

```swift iOS
do {
            try await client.loginAsVisitor()
        } catch {
            // Handle error here
        }
```

```dart Flutter
Future<void> loginAsVisitor() async {
    await AmityCoreClient.loginAsVisitor().submit();
  }
```

```typescript TypeScript
import { Client } from '@amityco/ts-sdk'

async function loginAsVisitor() {
  const client = Client.createClient('apiKey', 'us')
  await client.loginAsVisitor()
}
```

</CodeGroup>
