{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: client-is-connected | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void checkConnection() {
    final isConnected = AmityCoreClient.isConnected();
    if (isConnected) {
      // Client is connected to Amity servers
    } else {
      // Client is disconnected
    }
  }
```

```typescript TypeScript
import { isConnected } from '@amityco/ts-sdk'

function checkConnection() {
  const connected = isConnected()
  console.log('Connected:', connected)
}
```

</CodeGroup>
