{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: client-set-access-token-handler | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun setAccessTokenHandler(userId: String) {
        val sessionHandler = object : SessionHandler {
            override fun sessionWillRenewAccessToken(renewal: AccessTokenRenewal) {
                // Fetch a new auth token from your server and call renewWithAuthToken
                renewal.renewWithAuthToken(authToken = "newAuthToken")
            }
        }
        AmityCoreClient.login(userId = userId, sessionHandler = sessionHandler)
            .build()
            .submit()
            .subscribe()
    }
```

```swift iOS
client.setAccessTokenHandler { handler in
            // Fetch a new token from your server and call handler with it
            handler.renewAccessToken(withTokens: "<new-auth-token>")
        }
```

```dart Flutter
Future<void> loginWithTokenHandler(String userId) async {
    await AmityCoreClient.login(userId, sessionHandler: (AccessTokenRenewal renewal) async {
      try {
        final newToken = await fetchNewAuthTokenFromServer();
        renewal.renewWithAuthToken(newToken);
      } catch (error) {
        renewal.unableToRetrieveAuthToken();
      }
    }).submit();
  }

  Future<String> fetchNewAuthTokenFromServer() async {
    // Retrieve a fresh auth token from your backend
    return Future.value('newAuthToken');
  }
```

```typescript TypeScript
import { Client } from '@amityco/ts-sdk'

function setAccessTokenHandler() {
  const client = Client.getActiveClient()
  client.on('tokenExpired', async (renewToken) => {
    const newAuthToken = await fetchNewAuthToken()
    await renewToken({ authToken: newAuthToken })
  })
}
```

</CodeGroup>
