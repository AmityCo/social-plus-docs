{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: product-get | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getProduct() {
        AmityCoreClient.getNetworkSettings()
            .doOnSuccess { settings ->
                // access tier/product settings
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let productRepository = AmityProductRepository(client: client)
        var token: AmityNotificationToken?
        token = productRepository.getProduct("<product-id>").observe { liveObject, error in
            guard let product = liveObject.object else { return }
            print("Product: \(product.productId)")
        }
```

```typescript TypeScript
import { ProductRepository } from '@amityco/ts-sdk'

async function getProduct() {
  const liveObject = ProductRepository.getProduct('productId')
  liveObject.on('dataUpdated', (product) => {
    console.log(product)
  })
}
```

</CodeGroup>
