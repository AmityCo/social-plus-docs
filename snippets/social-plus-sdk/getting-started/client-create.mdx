{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: client-create | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createClient(context: Context, apiKey: String) {
        AmityCoreClient.setup(
            apiKey = apiKey,
            context = context
        )
    }
```

```swift iOS
let client = try? AmityClient(apiKey: "YOUR_API_KEY", region: .global)
        print("Client created: \(client != nil)")
```

```dart Flutter
Future<void> createClient() async {
    await AmityCoreClient.setup(
      option: AmityCoreClientOption(
        apiKey: 'YOUR_API_KEY',
        httpEndpoint: AmityRegionalHttpEndpoint.SG,
        mqttEndpoint: AmityRegionalMqttEndpoint.SG,
      ),
    );
  }
```

```typescript TypeScript
import { createClient } from '@amityco/ts-sdk'

function initClient() {
  const client = createClient('apiKey', 'sg')
  client.use()
  return client
}
```

</CodeGroup>
