{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: client-get-link-preview-metadata | updated: 2026-05-02 */}

<CodeGroup>

```swift iOS
do {
            let metadata = try await client.getLinkPreviewMetadata(url: "https://example.com/article")
            print("Title: \(metadata.title ?? "")")
            print("Description: \(metadata.description ?? "")")
        } catch {
            // Handle error here
        }
```

```typescript TypeScript
import { getLinkPreviewMetadata } from '@amityco/ts-sdk'

async function fetchLinkPreview() {
  const { title, description, imageUrl } = await getLinkPreviewMetadata('https://www.example.com/')
  console.log(title, description, imageUrl)
}
```

</CodeGroup>
