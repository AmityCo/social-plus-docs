{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: client-marker-sync | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun clientMarkerSync() {
        AmityCoreClient.syncMarkers()
            .subscribe(
                { println("Markers synced") },
                { error -> println("Sync failed: ${error.message}") }
            )
    }
```

```swift iOS
let client = AmityClient.shared
        // Marker sync occurs automatically when observing live objects
        print("Client connected: \(client.connectionStatus == .connected)")
```

```dart Flutter
void syncMarkers(DateTime lastSyncDate) {
    AmityCoreClient.syncMarkers(lastSyncAt: lastSyncDate)
        .then((_) {
          // Markers synced successfully
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }
```

```typescript TypeScript
import { markerSync } from '@amityco/ts-sdk'

async function syncMarkers() {
  const { data, hasMore } = await markerSync(new Date().toISOString())
  console.log('Has more:', hasMore)
}
```

</CodeGroup>
