{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: client-connect | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun connectClient(userId: String, displayName: String) {
        AmityCoreClient.login(userId = userId)
            .displayName(displayName = displayName)
            .submit()
            .doOnComplete { }
            .doOnError { }
            .subscribe()
    }
```

```swift iOS
AmityUIKitManager.setup(apiKey: "YOUR_API_KEY", region: .global)
```

```typescript TypeScript
import { Client } from '@amityco/ts-sdk'

async function connectClient(apiKey: string, userId: string, displayName: string) {
  const client = Client.createClient(apiKey, 'sg')

  await client.login({ userId, displayName })
  console.log('Client connected for user:', userId)

  return client
}

// Example usage
connectClient('your-api-key', 'user123', 'John Doe')
```

</CodeGroup>
