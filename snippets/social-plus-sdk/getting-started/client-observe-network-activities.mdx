{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: client-observe-network-activities | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun clientObserveNetworkActivities() {
        AmityCoreClient.observeNetworkActivities()
            .subscribe(
                { activity -> println("Network activity: $activity") },
                { error -> println("Error: ${error.message}") }
            )
    }
```

```swift iOS
client.observeNetworkActivities { request, response, data in
            if let request = request {
                print("Request URL: \(request.url?.absoluteString ?? "")")
            }
            if let response = response {
                print("Response status: \(response.statusCode)")
            }
        }
```

```dart Flutter
StreamSubscription? subscription;

  void observeNetworkActivities() {
    subscription = AmityCoreClient.observeNetworkActivities()
        .listen((AmityNetworkActivity activity) {
          final requestUrl = activity.requestUrl;
          final responseCode = activity.responseCode;
          // Log or monitor network activity
        })
        ..onError((error) {
          // Handle error
        });
  }

  void dispose() {
    subscription?.cancel();
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function clientObserveNetworkActivities() {
  const client = Client.getActiveClient();
  client.on('connectionStatusChanged', (status) => {
    console.log('Network status:', status); // 'connected' | 'connecting' | 'disconnected'
  });
}
```

</CodeGroup>
