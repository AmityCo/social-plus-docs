{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: client-setup-login-subscriptions | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
StreamSubscription? sessionSubscription;

  Future<void> setupAfterLogin(String userId) async {
    await AmityCoreClient.login(userId).submit();

    sessionSubscription = AmityCoreClient.observeSessionState()
        .listen((AmitySessionState state) {
          switch (state) {
            case AmitySessionState.established:
              // Session is established, set up feature subscriptions
              break;
            case AmitySessionState.notLoggedIn:
              // Handle logout state
              break;
            default:
              break;
          }
        });
  }

  void dispose() {
    sessionSubscription?.cancel();
  }
```

```typescript TypeScript
import { setupLoginSubscriptions } from '@amityco/ts-sdk'

function initLoginSubscriptions(unsubWatcher) {
  const subscriptions = setupLoginSubscriptions(unsubWatcher)
  return () => subscriptions.forEach(fn => fn())
}
```

</CodeGroup>
