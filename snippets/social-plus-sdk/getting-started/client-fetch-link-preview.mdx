{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: client-fetch-link-preview | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun fetchLinkPreview(url: String) {
        AmityCoreClient.fetchLinkPreview(url = url)
            .doOnSuccess { preview: AmityLinkPreview ->
                val title = preview.getTitle()
                val description = preview.getDescription()
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
AmityClient.shared.getLinkPreviewMetadata(url: "https://example.com") { metadata, error in
            if let metadata = metadata {
                print("Title: \(metadata.title ?? "")")
            }
        }
```

```dart Flutter
Future<void> fetchLinkPreview(String url) async {
    final preview = await AmityCoreClient.fetchLinkPreview(url: url);
    final title = preview.title;
    final description = preview.description;
    // use title and description to render a link preview card
  }
```

</CodeGroup>
