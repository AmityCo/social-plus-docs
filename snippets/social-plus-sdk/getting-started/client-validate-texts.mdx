{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: client-validate-texts | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun clientValidateTexts(texts: List<String>) {
        AmityCoreClient.validateTexts(texts = texts)
            .subscribe(
                { result -> println("Validation result: $result") },
                { error -> println("Validation error: ${error.message}") }
            )
    }
```

```swift iOS
do {
            let texts = ["Hello world", "Sample message content"]
            try await client.validateTexts(texts: texts)
            // All texts passed validation
        } catch {
            // Handle validation error here
        }
```

```dart Flutter
void validateTexts(List<String> texts) {
    AmityCoreClient.validateTexts(texts)
        .then((List<AmityTextValidationResult> results) {
          for (final result in results) {
            final text = result.text;
            final isBlocked = result.isBlocked;
            if (isBlocked) {
              // Text contains blocked content
            }
          }
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }
```

```typescript TypeScript
import { validateTexts } from '@amityco/ts-sdk'

async function checkTexts() {
  const isValid = await validateTexts(['hello', 'world'])
  console.log('All texts valid:', isValid)
}
```

</CodeGroup>
