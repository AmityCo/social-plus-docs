{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: notification-registration | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun notificationRegistration() {
        AmitySocialClient.notifications()
            .registerDeviceToken("fcmToken")
            .subscribe({
                // Token registered
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
func notificationRegistration() {
        let notificationManager = AmityNotificationManager(client: AmityManager.shared.client!)
        let deviceToken = "DEVICE_PUSH_TOKEN"
        notificationManager.registerDeviceToken(forPushNotification: deviceToken) { success, error in
            if success {
                print("Push notification token registered")
            }
        }
    }
```

```typescript TypeScript
import { Client } from '@amityco/ts-sdk'

async function registerNotification() {
  await Client.registerDeviceForPushNotification('deviceToken')
  console.log('Push notification registered')
}
```

</CodeGroup>
