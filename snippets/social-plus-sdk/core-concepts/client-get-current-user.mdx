{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: client-get-current-user | updated: 2026-04-30 */}

<CodeGroup>

```kotlin Android
fun getCurrentUser() {
        AmityCoreClient.newUserRepository()
            .getUser(AmityCoreClient.getUserId())
            .doOnNext { user: AmityUser ->
                val userId = user.getUserId()
                val displayName = user.getDisplayName()
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let userRepository = AmityUserRepository(client: client)
        let liveObject = userRepository.getUser(client.currentUserId ?? "")
        let token = liveObject.observe { liveObject, error in
            guard let user = liveObject.object else { return }
            print("userId: \(user.userId), displayName: \(user.displayName ?? "")")
        }
```

```dart Flutter
void getCurrentUser() {
    final AmityUser currentUser = AmityCoreClient.getCurrentUser();
    // use currentUser.userId, currentUser.displayName, etc.
  }
```

```typescript TypeScript
import { Client } from '@amityco/ts-sdk'

async function getCurrentUser() {
  const client = Client.getActiveClient()
  const currentUser = client.currentUser
  console.log(currentUser)
}
```

</CodeGroup>
