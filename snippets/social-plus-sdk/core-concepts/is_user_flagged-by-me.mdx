{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: is_user_flagged-by-me | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun isUserFlaggedByMe() {
        // Check whether the current user has flagged a specific user
        AmityCoreClient.newUserRepository()
            .getUser("userId")
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(io.reactivex.rxjava3.android.schedulers.AndroidSchedulers.mainThread())
            .subscribe({ user ->
                val isFlagged = user.isFlaggedByMe()
                // Update UI to reflect flag status
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
do {
            let isFlaggedByMe = try await userRepository.isUserFlaggedByMe(withId: "<user-id>")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void isUserFlaggedByMe() async {
    final userRepo = AmityCoreClient.newUserRepository();
    final user = await userRepo.getUser('userId123');
    final isFlagged = user.isFlaggedByMe ?? false;
    print('User flagged by me: \$isFlagged');
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function isUserFlaggedByMe() {
  const userId = 'user-id-to-check'
  const liveObject = UserRepository.getUser(userId)
  const unsubscribe = liveObject.subscribe(({ data: user, loading }) => {
    if (!loading && user) {
      console.log(`Is user ${userId} flagged by me:`, user.isFlaggedByMe)
    }
  })
  return unsubscribe
}
```

</CodeGroup>
