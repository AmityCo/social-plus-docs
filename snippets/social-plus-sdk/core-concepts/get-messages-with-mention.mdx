{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-messages-with-mention | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getMessagesWithMention() {
        AmityChatClient.newMessageRepository()
            .createMessage("channelId")
            .with()
            .text("Hello @user!")
            .mentionUsers(listOf("userId"))
            .build()
            .send()
            .subscribe()
    }
```

```swift iOS
func getMessagesWithMention() {
        let messageRepository = AmityMessageRepository(client: AmityManager.shared.client!)
        let liveCollection = messageRepository.getMessages(channelId: "channelId", stack: .default)
        liveCollection.observe { collection, _, error in
            if let error = error { print("Error: \(error)"); return }
            let messages = collection.allObjects()
            let mentioned = messages.filter { $0.hasMentioned }
            print("Messages with mentions: \(mentioned.count)")
        }
    }
```

```dart Flutter
void getMessagesWithMention() async {
    AmityChatClient.newMessageRepository()
        .getMessages(subChannelId: 'subChannelId')
        .filterByMention(userId: AmityCoreClient.getUserId())
        .getPagingData(token: null, limit: 20)
        .asStream()
        .listen((result) {
      final messages = result.data;
    });
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

function getMessagesWithMention() {
  const subChannelId = 'subChannel123'
  const liveCollection = MessageRepository.getMessages({ subChannelId, limit: 20 })
  liveCollection.on('dataUpdated', (messages) => {
    const mentionMessages = messages.filter((msg) => msg.mentionees && msg.mentionees.length > 0)
    console.log('Messages with mentions:', mentionMessages)
  })
}
```

</CodeGroup>
