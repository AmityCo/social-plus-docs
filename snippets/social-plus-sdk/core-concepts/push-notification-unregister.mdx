{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: push-notification-unregister | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun pushNotificationUnregister() {
        // TODO: implement push notification unregister using Social Plus SDK
    }
```

```swift iOS
func pushNotificationUnregister() {
        AmityManager.shared.client?.unregisterDeviceForPushNotification { success, error in
            if let error = error {
                print("Push unregistration failed: \(error)")
                return
            }
            print("Push notification unregistered successfully: \(success)")
        }
    }
```

```dart Flutter
void unregisterPushNotification() {
      AmityCoreClient.unregisterDeviceNotification()
          .then((_) {
        // Device unregistered from push notifications
      }).onError((error, stackTrace) {
        // Handle error
      });
    }
```

```typescript TypeScript
import { Client } from '@amityco/ts-sdk'

async function unregisterPushNotification(deviceToken: string) {
  try {
    await Client.unregisterPushNotification(deviceToken)
    console.log('Device unregistered from push notifications')
  } catch (error) {
    console.error('Failed to unregister push notification:', error)
  }
}
```

</CodeGroup>
