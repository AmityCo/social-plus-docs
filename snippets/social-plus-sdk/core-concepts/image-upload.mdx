{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: image-upload | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun uploadImage(imageUri: Uri) {
        AmityCoreClient.newFileRepository()
            .uploadImage(uri = imageUri)
            .doOnNext { uploadResult ->
                val image: AmityImage = uploadResult
                val imageUrl = image.getUrl()
                // use uploaded image
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let fileRepository = AmityFileRepository()
        guard let image = UIImage(named: "sample"), let data = image.jpegData(compressionQuality: 0.8) else { return }
        fileRepository.uploadImage(data, filename: "sample.jpg", mimeType: "image/jpeg") { progress in
            print("Upload progress: \(progress)")
        } completion: { result in
            switch result {
            case .success(let imageData):
                print("Uploaded image fileId: \(imageData.fileId)")
            case .failure(let error):
                print("Error: \(error)")
            }
        }
```

```dart Flutter
void uploadImage(File uploadingImage) async {
    AmityCoreClient.newFileRepository().uploadImage(uploadingImage).stream.listen((amityUploadResult) {
      amityUploadResult.when(
        progress: (uploadInfo, cancelToken) {
          int progress = uploadInfo.getProgressPercentage();
        },
        complete: (file) {
          //check if the upload result is complete

          final AmityImage uploadedImage = file;
          //proceed result with uploadedImage
        },
        error: (error) {
          final AmityException amityException = error;
          //handle error
        },
        cancel: () {
          //upload is cancelled
        },
      );
    });
  }
```

```typescript TypeScript
import { FileRepository } from '@amityco/ts-sdk'

const uploadImage = async (imageFile: File) => {
  const formData = new FormData()
  formData.append('files', imageFile)

  const result = await FileRepository.uploadImage(formData, (percent) => {
    if (percent === 100) {
      console.log('Image upload complete')
    } else {
      console.log(`Image upload progress: ${percent}%`)
    }
  })

  console.log('Uploaded image:', result)
  return result
}
```

</CodeGroup>
