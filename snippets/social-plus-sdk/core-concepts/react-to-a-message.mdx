{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: react-to-a-message | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun reactToAMessage() {
        // TODO: Implement React to a message
    }
```

```swift iOS
func reactToAMessage() {
        let client = AmityClient.shared
        let reactionRepository = AmityReactionRepository(client: client)
        reactionRepository.addReaction("like", referenceId: "message_id", referenceType: .message) { success, error in
            if success {
                print("Reacted to message successfully")
            }
        }
    }
```

```dart Flutter
void reactToAMessage() async {
    final reactionRepository = AmitySocialClient.newReactionRepository();
    await reactionRepository
        .addReaction(
          referenceType: AmityReactionReferenceType.MESSAGE,
          referenceId: 'messageId',
          reactionName: 'like',
        );
  }
```

```typescript TypeScript
import { ReactionRepository } from '@amityco/ts-sdk'

async function reactToMessage() {
  const reactionData = {
    referenceId: 'messageId123',
    referenceType: 'message' as const,
    reactionName: 'like',
  };

  await ReactionRepository.addReaction(reactionData);
  console.log('Reaction added to message');
}
```

</CodeGroup>
