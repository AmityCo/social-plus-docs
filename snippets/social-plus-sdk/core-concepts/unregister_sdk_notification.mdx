{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: unregister_sdk_notification | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun unregisterSdkNotification() {
        // Perform unregister_sdk_notification operation
        AmityCoreClient.registerDeviceForPushNotification("fcmToken")
            .subscribe({ /* registered */ }, { error -> })
    }
```

```dart Flutter
void unregisterSdkNotification() {
    AmityCoreClient.unregisterDevicePushNotification()
        .then((_) {
      // SDK push notifications unregistered
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function unregisterSdkNotification() {
  await NotificationRepository.unregisterPushNotification({ deviceToken: 'deviceToken' })
  console.log('SDK push notification unregistered')
}
```

</CodeGroup>
