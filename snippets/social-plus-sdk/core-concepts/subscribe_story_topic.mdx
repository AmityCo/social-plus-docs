{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: subscribe_story_topic | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun subscribeStoryTopic() {
        val topic = AmityTopicBuilder.topic(AmityStory::class.java)
            .forCommunity("communityId")
            .build()
        AmityCoreClient.subscribeTopic(topic)
            .subscribe({
                // Subscribed to story real-time events
            }, { error ->
                // Handle error
            })
    }
```

```dart Flutter
void subscribeStoryTopic() {
    final topic = TopicBuilder.community(communityId: 'communityId')
        .story()
        .build();
    AmityCoreClient.subscribeTopic(topic).then((_) {
      // Subscribed to story events
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function subscribeStoryTopic() {
  const { subscribeTopic } = await import('@amityco/ts-sdk');
  const topic = getStoryTopic({ targetType: 'community', targetId: 'communityId' });
  const unsubscribe = subscribeTopic(topic, (event) => {
    console.log('Story event:', event);
  });
  return unsubscribe;
}
```

</CodeGroup>
