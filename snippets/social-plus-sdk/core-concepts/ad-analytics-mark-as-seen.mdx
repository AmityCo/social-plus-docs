{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: ad-analytics-mark-as-seen | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun markAdAsSeen(ad: AmityAd) {
        AmityCoreClient.newAdRepository()
            .markAsSeen(adId = ad.getAdId())
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
var ad: AmityAd!
        ad.analytics.markAsSeen(placement: .feed)
```

```dart Flutter
void markAdAsSeen(AmityAd ad) {
    ad.getAnalyticsEngine().markAsSeen();
  }
```

```typescript TypeScript
import { AdRepository } from '@amityco/ts-sdk'

async function markAdAsSeen() {
  const networkAds = await AdRepository.getNetworkAds()
  const ad = networkAds.ads[0]
  if (ad) {
    ad.analytics.markAsSeen('feed')
  }
}
```

</CodeGroup>
