{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: event-observe-user | updated: 2026-05-02 */}

<CodeGroup>

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

function observeUserEvents() {
  const unsubscribeUpdated = UserRepository.onUserUpdated((user) => {
    console.log('User updated:', user.userId)
  })

  const unsubscribeDeleted = UserRepository.onUserDeleted((user) => {
    console.log('User deleted:', user.userId)
  })

  return () => {
    unsubscribeUpdated()
    unsubscribeDeleted()
  }
}
```

</CodeGroup>
