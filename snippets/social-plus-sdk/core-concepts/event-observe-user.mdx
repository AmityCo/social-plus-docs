{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: event-observe-user | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun eventObserveUser(user: AmityUser) {
        user.subscription(events = AmityUserEvents.USER)
            .subscribeTopic()
            .subscribe()
    }
```

```swift iOS
let userRepository = AmityUserRepository()
        let liveObject = userRepository.getUser("userId")
        liveObject.observe { user, error in
            guard error == nil else { return }
            print("User updated: \(user.object?.displayName ?? "")")
        }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

function observeUserEvents() {
  const unsubscribeUpdated = UserRepository.onUserUpdated((user) => {
    console.log('User updated:', user.userId)
  })

  const unsubscribeDeleted = UserRepository.onUserDeleted((user) => {
    console.log('User deleted:', user.userId)
  })

  return () => {
    unsubscribeUpdated()
    unsubscribeDeleted()
  }
}
```

</CodeGroup>
