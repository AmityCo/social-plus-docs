{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: push-notification-enable | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun pushNotificationEnable() {
        // Enable push notifications for the current device
        AmityCoreClient.registerPushNotification(deviceToken = "fcmDeviceToken")
            .subscribe(
                { /* push notifications enabled */ },
                { error -> /* handle error */ }
            )
    }
```

```swift iOS
let client = AmityClient.shared
        let notificationRepository = AmityNotificationRepository(client: client)
        notificationRepository.enablePushNotification { success, error in
            if let error = error { print("Error: \(error)"); return }
            print("Push notifications enabled: \(success)")
        }
```

```dart Flutter
void enablePushNotifications(String fcmToken) {
      AmityCoreClient.registerDeviceNotification(fcmToken)
          .then((_) {
        // Push notifications enabled
      }).onError((error, stackTrace) {
        // Handle error
      });
    }
```

```typescript TypeScript
import { Client } from '@amityco/ts-sdk'

async function enablePushNotification(deviceToken: string) {
  await Client.registerPushNotification(deviceToken)
  console.log('Push notifications enabled for device:', deviceToken)
}
```

</CodeGroup>
