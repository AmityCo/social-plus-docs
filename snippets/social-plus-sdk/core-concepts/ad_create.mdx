{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: ad_create | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun adCreate() {
        AmitySocialClient.newAdRepository()
            .createAd(
                advertiserName = "My Brand",
                headline = "Discover our product",
                description = "Check out amazing features",
                callToAction = "Learn More",
                callToActionUrl = "https://example.com"
            )
            .subscribe()
    }
```

```swift iOS
let adRepository = AmityAdRepository(client: AmityManager.shared.client!)
        let builder = AmityAdBuilder()
        builder.setName("Sponsored Ad")
        builder.setBody("Check out our latest product!")
        builder.setCallToActionUrl("https://example.com/product")
        adRepository.createAd(builder: builder) { ad, error in
            if let error = error { print("Error creating ad: \(error)"); return }
            print("Ad created with ID: \(ad?.adId ?? "")")
        }
```

```dart Flutter
void adCreate() async {
    final ad = await AmityAdRepository()
        .createAd(
          adName: 'My Ad',
          adType: AmityAdType.BANNER,
          placement: AmityAdPlacement.FEED,
          callToAction: AmityAdCallToAction.LEARN_MORE,
          callToActionUrl: 'https://example.com',
        );
    print('Ad created: \${ad.adId}');
  }
```

```typescript TypeScript
import { Client } from '@amityco/ts-sdk'

async function createAd() {
  const ad = await Client.getAdRepository().createAd({
    name: 'My Campaign Ad',
    adType: 'banner',
    placement: 'feed',
    callToAction: { text: 'Learn More', url: 'https://example.com' },
    imageFileId: 'fileId123',
  })

  console.log('Ad created:', ad.adId)
  return ad
}
```

</CodeGroup>
