{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: file-upload-image | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun uploadImage(imageUri: Uri) {
        AmityCoreClient.newFileRepository()
            .uploadImage(imageUri)
            .doOnNext { result: AmityUploadResult<AmityImage> ->
                when (result) {
                    is AmityUploadResult.PROGRESS -> {
                        val progress = result.getUploadInfo().getProgressPercentage()
                    }
                    is AmityUploadResult.COMPLETE -> {
                        val image: AmityImage = result.getFile()
                    }
                    is AmityUploadResult.ERROR -> {
                        val error = AmityError.from(result.getError())
                    }
                    is AmityUploadResult.CANCELLED -> { }
                }
            }
            .subscribe()
    }
```

```swift iOS
let fileRepository = AmityFileRepository(client: client)
        do {
            let imageURL = URL(fileURLWithPath: "<image-file-path>")
            let imageData = try Data(contentsOf: imageURL)
            let result = try await fileRepository.uploadImage(imageData, filename: "image.jpg", progressHandler: nil)
            print("Image uploaded with fileId: \(result.fileId)")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void uploadImage(File imageFile) {
    AmityCoreClient.newFileRepository()
        .uploadImage(imageFile)
        .stream
        .listen((AmityUploadResult<AmityImage> result) {
      result.when(
        progress: (uploadInfo, cancelToken) {
          int progress = uploadInfo.getProgressPercentage();
        },
        complete: (image) {
          final AmityImage uploadedImage = image;
          //proceed with uploadedImage
        },
        error: (error) {
          //handle error
        },
        cancel: () {
          //upload cancelled
        },
      );
    });
  }
```

```typescript TypeScript
import { FileRepository } from '@amityco/ts-sdk'

async function uploadImage(imageFile) {
  const { data: file } = await FileRepository.uploadImage(imageFile)
  console.log(file)
}
```

</CodeGroup>
