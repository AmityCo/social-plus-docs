{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: comment-get-all-reactions | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun commentGetAllReactions(commentId: String) {
        AmitySocialClient.newReactionRepository()
            .getReactions(
                referenceType = AmityReactionReferenceType.COMMENT,
                referenceId = commentId
            )
            .subscribe(
                { reactions -> println("Reactions: ${reactions.size}") },
                { error -> println("Error: ${error.message}") }
            )
    }
```

```swift iOS
let reactionRepository = AmityReactionRepository(client: AmityClient.shared)
        let token = reactionRepository.getReactions(
            referenceId: "commentId",
            referenceType: .comment,
            reactionName: nil
        ).observe { collection, _, error in
            guard error == nil else { return }
            print("Total reactions: \(collection.count())")
        }
```

```dart Flutter
void commentGetAllReactions() {
    final query = AmitySocialClient.newCommentRepository()
        .getCommentReactions(commentId: 'commentId')
        .build();
    final controller = PagingController(pageFuture: (token) => query.getPagingData(token: token, limit: 20), pageSize: 20);
  }
```

```typescript TypeScript
import { ReactionRepository } from '@amityco/ts-sdk'

function getAllCommentReactions(referenceId: string) {
  const liveCollection = ReactionRepository.getReactions(
    { referenceId, referenceType: 'comment' },
    ({ data: reactions, loading, error, hasNextPage, onNextPage }) => {
      if (error) { console.error(error); return; }
      if (!loading) {
        reactions.forEach(r => console.log(r.reactionName, 'by', r.userId));
        if (hasNextPage) onNextPage();
      }
    }
  );
  return liveCollection;
}
```

</CodeGroup>
