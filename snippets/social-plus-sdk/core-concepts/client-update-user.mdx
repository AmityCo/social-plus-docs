{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: client-update-user | updated: 2026-04-30 */}

<CodeGroup>

```kotlin Android
fun updateUser(displayName: String, description: String) {
        AmityCoreClient.newUserRepository()
            .updateUser()
            .displayName(displayName = displayName)
            .description(description = description)
            .build()
            .update()
            .doOnSuccess { user: AmityUser ->
                // updated user
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let userRepository = AmityUserRepository(client: client)
        let builder = AmityUserUpdateBuilder()
        builder.setDisplayName("New Display Name")
        builder.setAvatar(nil)
        do {
            let user = try await userRepository.updateUser(builder)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void updateCurrentUser() {
    AmityCoreClient.newUserRepository()
        .updateUser(AmityCoreClient.getUserId())
        .displayName('New Display Name')
        .description('Updated bio')
        .update()
        .then((AmityUser user) {
          // handle result
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function updateUser() {
  const { data: user } = await UserRepository.updateUser('userId', {
    displayName: 'New Display Name',
    description: 'My updated bio',
  })
  console.log(user)
}
```

</CodeGroup>
