{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: access_mention_status | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun accessMentionStatus() {
        AmitySocialClient.newFeedRepository()
            .getPost("postId")
            .subscribe { post ->
                val mentionees = post.getMentionees()
                val isMentioned = mentionees.any { it.getUserId() == "currentUserId" }
            }
    }
```

```dart Flutter
void accessMentionStatus() {
    // Check if the current user has been mentioned in content
    final isMentioned = AmityMentionMetadata.isMentioned(
      metadata: {},
      userId: AmityCoreClient.getUserId(),
    );
    print('Mentioned: \$isMentioned');
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function accessMentionStatus() {
  const client = createClient('apiKey', 'region')
  await client.login({ userId: 'user123', displayName: 'User 123' })
  const mentionees = [{ type: 'user' as const, userIds: ['user456'] }]
  await client.post.createPost({
    targetType: 'community',
    targetId: 'community789',
    dataType: 'text',
    data: { text: 'Hello @user456!' },
    mentionees,
  })
}
```

</CodeGroup>
