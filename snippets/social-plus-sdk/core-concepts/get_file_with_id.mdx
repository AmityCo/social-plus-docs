{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_file_with_id | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getFileWithId() {
        // Retrieve a file by its file ID
        AmityCoreClient.newFileRepository()
            .getFile(fileId = "fileId")
            .subscribe { file -> /* handle file */ }
    }
```

```swift iOS
let fileRepository = AmityFileRepository(client: client)
        
        do {
            // Get AmityRawFile
            let rawFile = try await fileRepository.getFile(fileId: "<file-id>")
            
            // Transform AmityRawFile to specific AmityFileData.
            // If AmityRawFile is not the type `file`, it will return nil.
            if rawFile.type == .file {
                guard let fileData = rawFile.mapToFileData() else {
                    return
                }
                
                // Download the file by using downloadFile api or
                // You can use any network client to download the file.
                let path = try await fileRepository.downloadFile(fromURL: fileData.fileURL)
                print("File downloaded to local path: \(path)")
            }
        } catch {
            // Handle error here
        }
```

```dart Flutter
void getFileWithId() {
    AmitySocialClient.newFileRepository()
        .getFile('fileId')
        .asStream()
        .listen((file) {
      print('File name: \${file.attributes?.name}');
      print('File URL: \${file.fileUrl}');
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function getFileWithId() {
  const liveObject = FileRepository.getFile('fileId123');
  liveObject.on('dataUpdated', (file) => {
    console.log('File name:', file.attributes?.name, 'size:', file.attributes?.size, 'url:', file.fileUrl);
  });
}
```

</CodeGroup>
