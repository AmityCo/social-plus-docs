{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: react-comment | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun reactComment() {
        // TODO: implement React comment
    }
```

```swift iOS
let reactionRepository = AmityReactionRepository(client: AmityClient.shared)
        reactionRepository.addReaction("like", referenceId: "commentId", referenceType: .comment) { success, error in
            if success {
                print("Reaction added to comment")
            } else if let error = error {
                print("React failed: \(error)")
            }
        }
```

```dart Flutter
void reactToComment(AmityComment comment, String reactionName) {
      comment.react()
          .addReaction(reactionName)
          .then((_) {
        // Reaction added to comment
      }).onError((error, stackTrace) {
        // Handle error
      });
    }

    void removeCommentReaction(AmityComment comment, String reactionName) {
      comment.react()
          .removeReaction(reactionName)
          .then((_) {
        // Reaction removed from comment
      }).onError((error, stackTrace) {
        // Handle error
      });
    }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function reactComment() {
  await CommentRepository.addReaction('commentId123', 'like');
  console.log('Reaction added to comment successfully');
}
```

</CodeGroup>
