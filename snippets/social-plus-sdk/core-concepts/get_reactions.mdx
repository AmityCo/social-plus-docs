{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_reactions | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getReactions() {
        AmitySocialClient.newPostRepository()
            .getPost("postId")
            .subscribe { post ->
                AmitySocialClient.newReactionRepository()
                    .getReactions(post)
                    .build()
                    .query()
                    .subscribe()
            }
    }
```

```swift iOS
func getReactions() {
        let reactionRepository = AmityReactionRepository(client: AmityManager.shared.client!)
        let collection = reactionRepository.getReactions(
            referenceId: "postId123",
            referenceType: .post,
            reactionName: nil
        )
        collection.observe { collection, _, error in
            guard error == nil else { return }
            collection.snapshots.forEach { reaction in
                print("Reaction: \(reaction.reactionName) by \(reaction.userId)")
            }
        }
    }
```

```dart Flutter
void getReactions() {
    AmitySocialClient.newPostRepository()
        .getReactions(
          referenceId: 'postId',
          referenceType: AmityReactionReferenceType.POST,
        )
        .query();
  }
```

```typescript TypeScript
import { ReactionRepository } from '@amityco/ts-sdk'

async function getReactions() {
  const unsubscribe = ReactionRepository.getReactions(
    { referenceId: 'post123', referenceType: 'post' },
    (reactions, error) => {
      if (error) return console.error(error)
      console.log('Reactions:', reactions)
      unsubscribe()
    }
  )
}
```

</CodeGroup>
