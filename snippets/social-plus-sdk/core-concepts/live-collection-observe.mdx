{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: live-collection-observe | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun liveCollectionObserve() {
        // TODO: implement live collection observe using Social Plus SDK
    }
```

```swift iOS
func liveCollectionObserve() {
        let communityRepository = AmityCommunityRepository(client: AmityManager.shared.client!)
        let queryOptions = AmityCommunityQueryOptions()
        let liveCollection = communityRepository.getCommunities(with: queryOptions)
        
        let token = liveCollection.observe { collection, changes, error in
            if let error = error {
                print("Live collection error: \(error)")
                return
            }
            print("Collection updated, count: \(collection.count())")
            if let changes = changes {
                print("Deletions: \(changes.deletions.count), Insertions: \(changes.insertions.count)")
            }
        }
        // Retain token as long as observation is needed
    }
```

</CodeGroup>
