{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: access-mention-status | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void checkMentionStatus(AmityChannel channel) {
    final isMentioned = channel.isMentioned;
    // isMentioned is true if the current user has an unread mention
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

function checkMentionStatus(channelId: string) {
  const unsubscribe = ChannelRepository.getChannel(channelId, ({ data: channel, loading }) => {
    if (loading) return

    const { isMentioned } = channel
    if (isMentioned) {
      console.log('You have been mentioned in this channel')
    } else {
      console.log('No unread mentions in this channel')
    }
  })

  return unsubscribe
}
```

</CodeGroup>
