{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: sync_user_presence | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun syncUserPresence() {
        AmityCoreClient.newUserRepository()
            .syncUserPresence("userId")
            .subscribe({ userPresence ->
                // Handle user presence state
            }, { error ->
                // Handle error
            })
    }
```

```dart Flutter
void syncUserPresence() {
    AmityCoreClient.newUserRepository()
        .syncPresence(userId: 'userId')
        .then((_) {
      // User presence synced
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function syncUserPresence() {
  const userRepository = new UserRepository();
  await userRepository.syncUserPresence('userId');
  console.log('User presence synced');
}
```

</CodeGroup>
