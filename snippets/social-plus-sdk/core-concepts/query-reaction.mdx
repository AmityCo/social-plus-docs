{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: query-reaction | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
final _reactions = <AmityReaction>[];
  late PagingController<AmityReaction> _controller;

  void queryReactions(AmityPost post) {
    _controller = PagingController(
      pageFuture: (token) => AmitySocialClient.newReactionRepository()
          .getReactions(AmityReactionReferenceType.POST, post.postId!)
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    )..addListener(
        () {
          if (_controller.error == null) {
            _reactions.clear();
            _reactions.addAll(_controller.loadedItems);
            // update UI
          } else {
            // handle error
          }
        },
      );
  }
```

</CodeGroup>
