{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: realtime_event_message | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun realtimeEventMessage() {
        // perform realtime event message operation
        AmityChatClient.newMessageRepository()
            .getMessages("channelId")
            .build()
            .query()
            .subscribe { messages ->
                // handle messages
            }
    }
```

```swift iOS
AmityUIKitManagerInternal.shared.client.subscribeTopic(AmityTopicCreator.channelTopic(forChannelId: "channelId")) { error in
            print(error == nil ? "Subscribed to channel topic" : "Failed: \(error!.localizedDescription)")
        }
        let messageRepository = AmityMessageRepository(client: AmityUIKitManagerInternal.shared.client)
        let collection = messageRepository.getMessages(channelId: "channelId", includingTags: [], excludingTags: [], filterByParentId: false, parentId: nil, reverse: true)
        let token = collection.observe { liveCollection, _, error in
            guard error == nil else { return }
            print("Message RTE update, count: \(liveCollection.snapshots.count)")
        }
```

```dart Flutter
void realtimeEventMessage() async {
    final channelRepository = AmityChatClient.newChannelRepository();
    final channel = await channelRepository.getChannel('channelId');
    await channel
        .subscription(AmityChannelEvents.MESSAGE)
        .subscribeTopic();
    print('Subscribed to message events');
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function realtimeEventMessageUnderscore() {
  const liveCollection = MessageRepository.getMessages({
    subChannelId: 'subChannelId123',
    sortBy: 'segmentDesc' as const,
    limit: 20,
  });

  liveCollection.on('dataUpdated', messages => {
    console.log('Messages updated, count:', messages.length);
  });
}
```

</CodeGroup>
