{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: poll-get | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun pollGet() {
        // TODO: implement poll get using Social Plus SDK
    }
```

```swift iOS
func pollGet() {
        let pollRepository = AmityPollRepository(client: AmityManager.shared.client!)
        let liveObject = pollRepository.getPoll(withId: "pollId")
        
        let token = liveObject.observe { poll, error in
            guard let poll = poll.object else { return }
            print("Poll: \(poll.question), answers: \(poll.answers.count)")
        }
        // Retain token as long as observation is needed
    }
```

```dart Flutter
void pollGet() async {
    final poll = await AmitySocialClient.newPollRepository()
        .getPoll(pollId: 'pollId')
        .getSingleFuture();
    print('Poll question: \${poll.question}');
  }
```

```typescript TypeScript
import { PollRepository } from '@amityco/ts-sdk'

function getPoll(pollId: string) {
  const disposer = PollRepository.getPoll(pollId, ({ data: poll, loading, error }) => {
    if (loading) return
    if (error) {
      console.error('Failed to fetch poll', error)
      return
    }
    console.log('Poll:', poll)
  })

  return disposer
}
```

</CodeGroup>
