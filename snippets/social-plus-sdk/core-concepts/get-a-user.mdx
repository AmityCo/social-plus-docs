{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-a-user | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getAUser() {
        AmitySocialClient.newUserRepository()
            .getUser("userId")
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(AndroidSchedulers.mainThread())
            .subscribe({ user ->
                val displayName = user.getDisplayName()
                val avatar = user.getAvatar()
            }, { error ->
                error.printStackTrace()
            })
    }
```

```swift iOS
func getAUser() {
        let userRepository = AmityUserRepository(client: AmityManager.shared.client!)
        let liveCollection = userRepository.getUsers("", sortBy: .displayName)
        liveCollection.observe { collection, _, error in
            if let error = error { print("Error: \(error)"); return }
            print("Users: \(collection.count())")
        }
    }
```

```dart Flutter
void getAUser() async {
    final user = await AmitySocialClient.newUserRepository()
        .getUser(userId: 'userId');
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

function getAUser() {
  const userId = 'user123'
  const liveObject = UserRepository.getUser(userId)
  liveObject.on('dataUpdated', (user) => {
    console.log('User:', user)
  })
}
```

</CodeGroup>
