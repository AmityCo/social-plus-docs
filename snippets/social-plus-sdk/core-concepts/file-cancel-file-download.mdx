{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: file-cancel-file-download | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun cancelFileDownload(fileUri: Uri) {
        val disposable: Disposable = AmityCoreClient.newFileRepository()
            .uploadFile(fileUri)
            .doOnNext { result: AmityUploadResult<AmityFile> ->
                when (result) {
                    is AmityUploadResult.PROGRESS -> {
                        val progress = result.getUploadInfo().getProgressPercentage()
                    }
                    is AmityUploadResult.COMPLETE -> {
                        val file: AmityFile = result.getFile()
                    }
                    else -> { }
                }
            }
            .subscribe()
        // Cancel by disposing
        disposable.dispose()
    }
```

```swift iOS
fileRepository.cancelFileDownload("https://example.com/document.pdf")
```

```dart Flutter
void fileCancelFileDownload() {
    final fileRepo = AmityCoreClient.newFileRepository();
    final cancelToken = CancelToken();
    fileRepo.downloadFile(
      'fileId',
      cancelToken: cancelToken,
    );
    // Cancel the download
    cancelToken.cancel('Download cancelled by user');
    print('File download cancelled');
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function fileCancelFileDownload() {
  const controller = new AbortController();
  const downloadPromise = FileRepository.downloadFile('fileId123', { signal: controller.signal });
  // Cancel the download
  controller.abort();
  console.log('File download cancelled');
}
```

</CodeGroup>
