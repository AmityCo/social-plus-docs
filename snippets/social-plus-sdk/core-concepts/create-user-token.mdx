{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create-user-token | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createUserToken(userId: String, displayName: String) {
        AmityCoreClient.login(userId = userId)
            .displayName(displayName = displayName)
            .build()
            .submit()
            .doOnComplete { /* logged in */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```dart Flutter
void createUserToken() {
    AmityClient.shared
        .login(
          userId: 'userId',
          displayName: 'User Display Name',
          authToken: 'userAuthToken',
        )
        .then((_) {
          // Login successful, token managed by SDK
        })
        .onError((error, stackTrace) {
          // Handle error
        });
  }
```

```typescript TypeScript
import { Client } from '@amityco/ts-sdk'

async function createUserToken() {
  const client = Client.createClient('apiKey')
  await client.login({
    userId: 'userId',
    displayName: 'User Name',
    authToken: 'secureAuthToken',
  })
  console.log('User logged in with secure token')
}
```

</CodeGroup>
