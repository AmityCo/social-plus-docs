{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: story-react | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun storyReact() {
        // TODO: Implement Story react
    }
```

```swift iOS
let reactionManager = AmityReactionManager(client: AmityManager.shared.client!)
        reactionManager.addReaction(reaction: "like", referenceId: "storyId", referenceType: .story) { success, error in
            if success {
                print("Story reaction added successfully")
            } else if let error = error {
                print("Error adding story reaction: \(error)")
            }
        }
```

```dart Flutter
void storyReact() {
    AmitySocialClient.newStoryRepository()
        .react(storyId: 'storyId', reactionName: 'like')
        .then((_) {
          print('Story reacted successfully.');
        });
  }
```

```typescript TypeScript
import { ReactionRepository } from '@amityco/ts-sdk'

async function reactToStory() {
  await ReactionRepository.addReaction('story', 'storyId', 'like');
  console.log('Reaction added to story');
}
```

</CodeGroup>
