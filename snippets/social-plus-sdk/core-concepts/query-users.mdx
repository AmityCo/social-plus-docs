{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: query-users | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun queryUsers() {
        // TODO: Implement Query users
    }
```

```swift iOS
func queryUsers() {
        let userRepository = AmityUserRepository(client: AmityClient.shared)
        let collection = userRepository.searchUsers("john", sortBy: .displayName)
        token = collection.observe { liveCollection, change, error in
            print("Users found: \(liveCollection.count())")
        }
    }
```

```dart Flutter
void queryUsers() async {
    final userRepository = AmitySocialClient.newUserRepository();
    userRepository
        .searchUser('keyword')
        .sortBy(AmityUserSortOption.DISPLAY_NAME)
        .asStream()
        .listen((users) {
      print('Users found: \${users.length}');
    });
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function queryUsers() {
  const params = {
    sortBy: 'displayName' as const,
    filter: 'all' as const,
  };

  const liveCollection = UserRepository.getUsers(params);
  liveCollection.on('dataUpdated', users => {
    console.log('Users:', users.map(u => u.userId));
  });
  liveCollection.on('dataError', err => console.error('Error querying users:', err));
}
```

</CodeGroup>
