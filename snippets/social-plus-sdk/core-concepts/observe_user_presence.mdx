{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: observe_user_presence | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun observeUserPresence() {
        // Search for users with a live collection
        AmityCoreClient.newUserRepository()
            .searchUsers(keyword = "john")
            .build()
            .query()
            .subscribe({ pagingData ->
                // Handle user search results
            }, { error ->
                // Handle error
            })
    }
```

```dart Flutter
void observeUserPresence() async {
        AmityCoreClient.newUserRepository()
            .observeUserPresence(userId: 'userId')
            .listen((presence) {
              print('User online status: \${presence.isOnline}');
              print('Last seen: \${presence.lastSeenAt}');
            });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function observeUserPresence() {
  const liveObject = UserRepository.getUser('userId123');
  liveObject.on('dataUpdated', (user) => {
    console.log('User presence state:', user.presenceState);
  });
}
```

</CodeGroup>
