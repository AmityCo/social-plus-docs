{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: story-topic-subscribe | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun storyTopicSubscribe() {
        // TODO: implement story topic subscribe
    }
```

```swift iOS
func storyTopicSubscribe() {
        let client = AmityClient.shared
        let storyRepository = AmityStoryRepository(client: client)
        let topic = AmityStoryTopic(targetId: "community_id", targetType: .community)
        let token = storyRepository.subscribeTopic(topic) { error in
            if error == nil {
                print("Story topic subscribed successfully")
            }
        }
    }
```

```dart Flutter
void storyTopicSubscribe() {
    final topic = AmityStoryTopic.community(communityId: 'communityId123');
    AmityCoreClient.newSocialRepository()
        .story
        .subscribeTopic(topic)
        .then((_) => print('Subscribed to story topic'))
        .onError((error, _) => print('Error subscribing: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function storyTopicSubscribe() {
  const { subscribeTopic } = await import('@amityco/ts-sdk');
  const topic = getStoryTopic({ targetType: 'community', targetId: 'communityId' });
  const unsubscribe = subscribeTopic(topic);
  return unsubscribe;
}
```

</CodeGroup>
