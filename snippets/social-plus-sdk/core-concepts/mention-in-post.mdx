{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: mention-in-post | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void mentionInPost() {
    AmitySocialClient.newPostRepository()
        .createPost()
        .targetMe()
        .text(
          'Hello @userId',
          mentionees: [AmityMentionee.user('userId')],
        )
        .build()
        .post();
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function mentionInPost() {
  const unsubscribe = PostRepository.createPost({
    targetType: 'community',
    targetId: 'community123',
    dataType: 'text',
    data: { text: 'Hello @jane, welcome!' },
    mentionees: [{ type: 'user', userIds: ['user456'] }],
  }, (post, error) => {
    if (error) return console.error(error)
    console.log('Post with mention created:', post)
    unsubscribe()
  })
}
```

</CodeGroup>
