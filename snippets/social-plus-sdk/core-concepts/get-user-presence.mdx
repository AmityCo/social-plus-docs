{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-user-presence | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getUserPresence() {
        AmitySocialClient.newUserRepository()
            .getUserPresence("userId123")
            .subscribe { presence ->
                println("User online: \${presence.isOnline()}")
            }
    }
```

```swift iOS
func getUserPresence() {
        let client = AmityClient.shared
        let presenceManager = client.presenceManager
        presenceManager.getUserPresence(userId: "userId") { presence, error in
            if let presence = presence {
                print("User is online: \(presence.isOnline)")
                print("Last seen: \(String(describing: presence.lastSeen))")
            }
        }
    }
```

</CodeGroup>
