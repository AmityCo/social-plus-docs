{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: query_messages_with_mention | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun queryMessagesWithMention() {
        // Query messages containing mentions
        AmityChatClient.newMessageRepository()
            .getMessages(channelId = "channelId")
            .mentionType(AmityMentionType.USER)
            .build()
            .query()
            .asLiveData()
    }
```

```swift iOS
let messageRepository = AmityMessageRepository(client: AmityUIKitManagerInternal.shared.client)
        let collection = messageRepository.getMessages(
            channelId: "channelId",
            includingTags: [],
            excludingTags: [],
            filterByParentId: false,
            parentId: nil,
            reverse: true
        )
        collection.observe { liveCollection, _, error in
            guard error == nil else { return }
            let messagesWithMentions = liveCollection.snapshots.filter { message in
                guard let metadata = message.metadata,
                      let mentionees = metadata["mentionees"] as? [[String: Any]] else { return false }
                return !mentionees.isEmpty
            }
            print("Loaded \(messagesWithMentions.count) messages with mentions")
        }
```

```dart Flutter
void queryMessagesWithMention() async {
    final messageRepository = AmityChatClient.newMessageRepository();
    messageRepository
        .getMessages('channelId')
        .filterByMentionUserId('userId')
        .asStream()
        .listen((messages) {
      print('Messages with mention: \${messages.length}');
    });
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function queryMessagesWithMentionUnderscore() {
  const params = {
    subChannelId: 'subChannelId123',
    sortBy: 'segmentDesc' as const,
    limit: 20,
  };

  const liveCollection = MessageRepository.getMessages(params);
  liveCollection.on('dataUpdated', messages => {
    const mentionMessages = messages.filter(m => m.mentionees && m.mentionees.length > 0);
    console.log('Messages with mentions:', mentionMessages.map(m => m.messageId));
  });
}
```

</CodeGroup>
