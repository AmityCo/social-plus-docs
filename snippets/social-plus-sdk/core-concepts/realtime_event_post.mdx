{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: realtime_event_post | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun realtimeEventPost() {
        // perform realtime event post operation
        AmitySocialClient.newPostRepository()
            .getPost("postId")
            .asFlowable()
            .subscribe { post ->
                // handle post updates
            }
    }
```

```swift iOS
AmityUIKitManagerInternal.shared.client.subscribeTopic(AmityTopicCreator.postTopic(forPostId: "postId")) { error in
            print(error == nil ? "Subscribed to post topic" : "Failed: \(error!.localizedDescription)")
        }
        let postRepository = AmityPostRepository(client: AmityUIKitManagerInternal.shared.client)
        let object = postRepository.getPost(withPostId: "postId")
        let token = object.observe { liveObject, error in
            guard error == nil, let post = liveObject.snapshot else { return }
            print("Post RTE update: \(post.postId)")
        }
```

```dart Flutter
void realtimeEventPost() async {
    final postRepository = AmitySocialClient.newPostRepository();
    final post = await postRepository.getPost('postId');
    await post
        .subscription(AmityPostEvents.COMMENT)
        .subscribeTopic();
    print('Subscribed to post events');
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function realtimeEventPostUnderscore() {
  const livePost = PostRepository.getPost('postId123');

  livePost.on('dataUpdated', post => {
    console.log('Post updated:', post.postId, 'reactions:', post.reactions);
  });

  livePost.on('dataError', err => console.error('Post realtime error:', err));
}
```

</CodeGroup>
