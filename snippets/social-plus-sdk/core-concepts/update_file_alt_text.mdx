{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: update_file_alt_text | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun updateFileAltText() {
        // Update operation for update_file_alt_text
        // TODO: implement ${className} operation
        AmitySocialClient.newPostRepository()
            .getPost("postId")
            .asFlowable()
            .subscribe({ result -> }, { error -> })
    }
```

```swift iOS
do {
            try await fileRepository.updateAltText(fileId: "<file-id>", altText: "Alt text")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void updateFileAltText() {
    // Update alt text via post/comment update builder
    // Alt text is set as metadata on the media object
    print('Alt text update is handled through post update with metadata');
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function updateFileAltText() {
  const { data: file } = await FileRepository.updateFile({
    fileId: 'fileId',
    attributes: { altText: 'New alt text description' },
  })
  console.log('File alt text updated:', file)
}
```

</CodeGroup>
