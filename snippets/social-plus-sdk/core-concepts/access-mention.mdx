{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: access-mention | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun accessMention() {
        val commentId = "commentId"
        AmitySocialClient.newCommentRepository()
            .getComment(commentId)
            .subscribe { comment ->
                val mentionees = comment.getMentionees()
                val metadata = comment.getMetadata()
            }
    }
```

```swift iOS
let postRepository = AmityPostRepository()
        let liveObject = postRepository.getPost(withId: "postId")
        liveObject.observe { liveObject, error in
            guard let post = liveObject.object else { return }
            if let metadata = post.metadata,
               let mentioned = metadata["mentioned"] as? [[String: Any]] {
                for mention in mentioned {
                    let type = mention["type"] as? String ?? ""
                    let userId = mention["userId"] as? String ?? ""
                    print("Mention type: \(type), userId: \(userId)")
                }
            }
        }
```

```dart Flutter
void accessMention() {
    // Access mention metadata from a post or message text
    final mentions = AmityMentionMetadata.fromText(
      text: "Hello @userId!",
      metadata: {},
    );
    print(mentions);
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function accessMention() {
  const client = createClient('apiKey', 'region')
  await client.login({ userId: 'user123', displayName: 'User 123' })
  const mentionees = [{ type: 'user' as const, userIds: ['user456'] }]
  await client.post.createPost({
    targetType: 'community',
    targetId: 'community789',
    dataType: 'text',
    data: { text: 'Hello @user456!' },
    mentionees,
  })
}
```

</CodeGroup>
