{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-presence-sync | updated: 2026-04-30 */}

<CodeGroup>

```kotlin Android
fun syncUserPresence(
        userPresenceRepository: AmityUserPresenceRepository,
        userId: String
    ) {
        userPresenceRepository.syncUserPresence(
            userId = userId,
            viewId = "" // optional, can use hash code of the View Holder class as view id
            //  recommended to use view id due to the Recycler View item update behavior
        )
    }

    //  Example usage about syncing user presence inside RecyclerView Adapter
    fun onViewAttachedToWindow(holder: UserViewHolder) {
        //  when View Holder is attached to adapter, start syncing presence for attached user
        holder.syncUserPresence()
    }

    class UserViewHolder(view: View) : RecyclerView.ViewHolder(view) {

        private var userId: String = ""

        fun bind(user: AmityUser) {
            userId = user.getUserId()
        }

        fun syncUserPresence() {
            AmityCoreClient.newUserPresenceRepository()
                .syncUserPresence(
                    userId = userId,
                    viewId = hashCode().toString()
                )
        }
    }
```

</CodeGroup>
