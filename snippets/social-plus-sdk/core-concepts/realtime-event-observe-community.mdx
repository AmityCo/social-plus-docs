{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: realtime-event-observe-community | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun realtimeEventObserveCommunity() {
        // perform realtime event observe community operation
        AmitySocialClient.newCommunityRepository()
            .getCommunity("communityId")
            .asFlowable()
            .subscribe { community ->
                // handle community object
            }
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository(client: AmityUIKitManagerInternal.shared.client)
        let object = communityRepository.getCommunity(withId: "communityId")
        let token = object.observe { liveObject, error in
            guard error == nil, let community = liveObject.snapshot else { return }
            print("Community updated: \(community.displayName ?? "")")
        }
```

```dart Flutter
StreamSubscription? _subscription;

  void observeCommunity(String communityId) {
    _subscription = AmitySocialClient.newCommunityRepository()
        .getCommunity(communityId)
        .asStream()
        .listen(
          (AmityCommunity community) {
            // handle community update event
          },
          onError: (error) {
            // handle error
          },
        );
  }

  void dispose() {
    _subscription?.cancel();
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function realtimeEventObserveCommunity() {
  const unsub = CommunityRepository.getCommunity('communityId123').subscribe({
    next: ({ data: community }) => {
      console.log('Community updated:', community.displayName, 'Members:', community.membersCount);
    },
    error: (err) => console.error('Community observe error:', err),
  });
  return unsub;
}
```

</CodeGroup>
