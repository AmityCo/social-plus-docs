{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: notification-update-settings | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun updateNotificationSettings(enabled: Boolean) {
        AmityCoreClient.notifications()
            .enable()
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let client = AmityClient.shared
        let settings = AmityUserNotificationSettings()
        settings.isEnabled = true
        client.notificationManager.setUserNotificationSettings(settings) { success, error in
            if let error = error {
                print("Failed to update notification settings: \(error.localizedDescription)")
                return
            }
            print("Notification settings updated successfully")
        }
```

```dart Flutter
void notificationUpdateSettings() async {
        await AmityCoreClient.updateNotificationSettings(
          AmityUserNotificationSettings(
            isEnabled: true,
            modules: [
              AmityUserNotificationModule.social(),
              AmityUserNotificationModule.chat(),
            ],
          ),
        );
        print('Notification settings updated');
  }
```

```typescript TypeScript
import { NotificationRepository } from '@amityco/ts-sdk'

async function updateNotificationSettings() {
  await NotificationRepository.setUserNotificationSettings({ isEnabled: true })
  console.log('Notification settings updated')
}
```

</CodeGroup>
