{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-delete | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun userDelete() {
        // Delete operation for user-delete
        AmityCoreClient.newUserRepository()
            .getUser("userId")
            .asFlowable()
            .subscribe({ user -> }, { error -> })
    }
```

```swift iOS
let userId = "userId"
        AmitySDK.deleteUser(userId: userId) { success, error in
            if let error = error {
                print("Failed to delete user: \(error)")
                return
            }
            print("User deleted successfully")
        }
```

```dart Flutter
void userDelete() {
    AmityCoreClient.newUserRepository()
        .deleteUser(userId: 'userId')
        .then((_) => print('User deleted'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function deleteUser() {
  await UserRepository.deleteUser('userId')
}
```

</CodeGroup>
