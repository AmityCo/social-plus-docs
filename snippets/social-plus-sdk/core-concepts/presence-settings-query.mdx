{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: presence-settings-query | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun queryPresenceSettings() {
        AmityCoreClient.presence()
            .isEnabled()
            .doOnSuccess { isEnabled: Boolean ->
                //  Check if presence feature is enabled
            }
            .doOnError {
                //  Handle Exception
            }
            .subscribe()
    }
```

```swift iOS
let client = AmityClient.shared
        let presenceRepository = AmityPresenceRepository(client: client)
        presenceRepository.getPresenceSettings { settings, error in
            if let error = error { print("Error: \(error)"); return }
            print("Presence settings: \(settings?.isEnabled ?? false)")
        }
```

```dart Flutter
void queryPresenceEnabled() {
    AmityCoreClient.presence()
        .isEnabled()
        .then((bool isEnabled) {
          if (isEnabled) {
            // presence is currently enabled
          } else {
            // presence is currently disabled
          }
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function presenceSettingsQuery() {
  const { PresenceRepository } = Amity;
  const settings = await PresenceRepository.getSettings();
  console.log('Presence settings:', settings);
  console.log('Presence enabled:', settings.enabled);
}
```

</CodeGroup>
