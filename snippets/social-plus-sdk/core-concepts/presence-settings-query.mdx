{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: presence-settings-query | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun queryPresenceSettings() {
        AmityCoreClient.presence()
            .isEnabled()
            .doOnSuccess { isEnabled: Boolean ->
                //  Check if presence feature is enabled
            }
            .doOnError {
                //  Handle Exception
            }
            .subscribe()
    }
```

```dart Flutter
void queryPresenceEnabled() {
    AmityCoreClient.presence()
        .isEnabled()
        .then((bool isEnabled) {
          if (isEnabled) {
            // presence is currently enabled
          } else {
            // presence is currently disabled
          }
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

</CodeGroup>
