{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: ads-analytics | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun adsAnalytics() {
        // TODO: implement ads analytics using Social Plus SDK
    }
```

```swift iOS
func adsAnalytics() {
        let adRepository = AmityAdRepository(client: AmityManager.shared.client!)
        
        adRepository.getAdAnalytics(adId: "adId") { analytics, error in
            if let error = error {
                print("Failed to get ad analytics: \(error)")
                return
            }
            guard let analytics = analytics else { return }
            print("Impressions: \(analytics.impressions), Clicks: \(analytics.clicks)")
        }
    }
```

```dart Flutter
void adsAnalytics() {
    // Track analytics event for an ad impression
    AmityAdRepository()
        .trackAdEvent(
          adId: 'adId',
          event: AmityAdEvent.impression,
        )
        .then((_) => print('Analytics tracked'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function adsAnalytics() {
  const client = createClient('apiKey', 'region')
  await client.login({ userId: 'user123', displayName: 'User 123' })
  const analytics = await client.ad.getAdAnalytics({ adId: 'ad123' })
  console.log('Impressions:', analytics.impressions, 'Clicks:', analytics.clicks)
}
```

</CodeGroup>
