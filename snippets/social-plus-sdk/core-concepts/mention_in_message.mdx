{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: mention_in_message | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun mentionInMessage() {
        // Build mention metadata for content
        val mentionMetadata = com.amityco.socialcloud.sdk.model.AmityMentionMetadata.User(
            userId = "userId",
            displayName = "User Name",
            index = 0,
            length = 9
        )
        AmityChatClient.newMessageRepository()
            .createMessage(channelId = "channelId")
            .with()
            .text("Hello @UserName")
            .mentionUsers(listOf(mentionMetadata))
            .build()
            .send()
            .subscribe()
    }
```

```swift iOS
let mentionee = AmityUserMention(userId: "user123", length: 5, index: 0)
        let builder = AmityMessageCreationBuilder()
        builder.setText("Hello @user123")
        builder.setMentionees([mentionee])
```

```dart Flutter
void mentionInMessage() {
    AmityChatClient.newMessageRepository()
        .createMessage('channelId')
        .text('Hello @userId')
        .mentionUser(['userId'])
        .send();
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function mentionInMessage(subChannelId: string) {
  const mentionedUserIds = ['user1', 'user2']

  const newMessage = await MessageRepository.createMessage({
    subChannelId,
    data: { text: 'Hello @user1 and @user2!' },
    dataType: 'text',
    mentionees: [{ type: 'user', userIds: mentionedUserIds }],
    metadata: {},
  })

  return newMessage
}
```

</CodeGroup>
