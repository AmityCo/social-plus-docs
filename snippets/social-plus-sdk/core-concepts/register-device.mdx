{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: register-device | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun registerDevice() {
        // TODO: Implement Register device
    }
```

```swift iOS
let deviceToken = "your_apns_device_token_data"
        AmityManager.shared.client?.registerDeviceForPushNotification(deviceToken) { success, error in
            if success {
                print("Device registered for push notifications")
            } else if let error = error {
                print("Registration failed: \(error)")
            }
        }
```

```typescript TypeScript
import { Client } from '@amityco/ts-sdk'

async function registerDevice(deviceToken: string) {
  try {
    await Client.registerPushNotification(deviceToken)
    console.log('Device registered for push notifications:', deviceToken)
  } catch (error) {
    console.error('Failed to register device:', error)
  }
}
```

</CodeGroup>
