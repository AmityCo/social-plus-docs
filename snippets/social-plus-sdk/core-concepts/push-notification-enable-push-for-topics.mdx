{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: push-notification-enable-push-for-topics | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun pushNotificationEnablePushForTopics() {
        // Enable push notifications for specific topics
        AmityCoreClient.registerPushNotification(
            deviceToken = "fcmDeviceToken",
            topics = listOf("community:communityId", "channel:channelId")
        )
        .subscribe(
            { /* push notifications enabled for topics */ },
            { error -> /* handle error */ }
        )
    }
```

```swift iOS
let client = AmityClient.shared
        let notificationRepository = AmityNotificationRepository(client: client)
        let topics: [AmityNotificationTopic] = [.newPost, .newComment, .newReaction]
        notificationRepository.enablePushNotification(for: topics) { success, error in
            if let error = error { print("Error: \(error)"); return }
            print("Push enabled for topics: \(success)")
        }
```

```dart Flutter
Future<void> enablePushForTopics(List<AmityNotificationTopic> topics) async {
    try {
      await AmityCoreClient.enablePushNotificationForTopics(topics);
      // push enabled for topics
    } catch (error) {
      // handle error
    }
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function pushNotificationEnablePushForTopics() {
  const { NotificationRepository } = Amity;
  await NotificationRepository.enableTopics({
    topics: ['community.postCreated', 'channel.messageCreated'],
  });
  console.log('Push notifications enabled for topics');
}
```

</CodeGroup>
