{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: realtime-subscription-post | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun realtimeSubscriptionPost() {
        // TODO: implement realtime subscription post
    }
```

```swift iOS
let postRepository = AmityPostRepository()
        let liveCollection = postRepository.getPosts(
            targetId: "communityId",
            targetType: .community,
            sortBy: .lastCreated,
            includeDeleted: false,
            feedType: .published
        )
        liveCollection.observe { liveCollection, error in
            let posts = liveCollection.allObjects() ?? []
            print("Posts updated in real-time, count: \(posts.count)")
        }
```

```dart Flutter
StreamSubscription? _postSubscription;

    void subscribeToPostRealtimeEvents(String postId) {
      AmitySocialClient.newPostRepository()
          .getPost(postId)
          .first
          .asStream()
          .asyncExpand((post) => post.subscription(AmityPostEvent.POST))
          .listen((_) {
        // Handle real-time post subscription events
      });
    }

    void unsubscribeFromPostRealtimeEvents() {
      _postSubscription?.cancel();
      _postSubscription = null;
    }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function realtimeSubscriptionPost() {
  const unsubscribe = subscribeTopic(
    PostTopic.post('postId123'),
    () => {
      console.log('Post subscription event received');
    },
  );
  return unsubscribe;
}
```

</CodeGroup>
