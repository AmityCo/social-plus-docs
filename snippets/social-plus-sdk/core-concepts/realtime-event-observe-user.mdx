{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: realtime-event-observe-user | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun realtimeEventObserveUser() {
        // perform realtime event observe user operation
        AmityCoreClient.newUserRepository()
            .getUser("userId")
            .asFlowable()
            .subscribe { user ->
                // handle real-time user updates
            }
    }
```

```swift iOS
let userRepository = AmityUserRepository(client: AmityUIKitManagerInternal.shared.client)
        let object = userRepository.getUser(userId: "userId")
        let token = object.observe { liveObject, error in
            guard error == nil, let user = liveObject.snapshot else { return }
            print("User updated: \(user.userId)")
        }
```

```dart Flutter
StreamSubscription? _subscription;

  void observeUser(String userId) {
    _subscription = AmityCoreClient.newUserRepository()
        .getUser(userId)
        .asStream()
        .listen(
          (AmityUser user) {
            // handle user update event
          },
          onError: (error) {
            // handle error
          },
        );
  }

  void dispose() {
    _subscription?.cancel();
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function realtimeEventObserveUser() {
  const unsub = UserRepository.getUser('userId123').subscribe({
    next: ({ data: user }) => {
      console.log('User updated:', user.displayName, 'Followers:', user.followersCount);
    },
    error: (err) => console.error('User observe error:', err),
  });
  return unsub;
}
```

</CodeGroup>
