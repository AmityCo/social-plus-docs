{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: rte-unsubscription | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun rteUnsubscription() {
        // perform rte unsubscription operation
        AmityChatClient.newChannelRepository()
            .getChannel("channelId")
            .asFlowable()
            .subscribe { channel ->
                // handle real-time updates
            }
    }
```

```swift iOS
// Unsubscribe from all realtime event topics
        AmityEventBus.shared.unsubscribeAllEvents(for: self)
```

```dart Flutter
Future<void> unsubscribeFromPostEvents(AmityPost post) async {
    await post
        .subscription(AmityPostEvents.POST)
        .unsubscribeTopic();
    // realtime updates for this post are stopped
  }

  Future<void> unsubscribeFromCommunityEvents(AmityCommunity community) async {
    await community
        .subscription(AmityCommunityEvents.POSTS)
        .unsubscribeTopic();
    // realtime updates for community posts are stopped
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function rteUnsubscription() {
  // Store the unsubscribe function returned from any subscription call
  const unsubscribe = PostRepository.subscribePost(
    { postId: 'post-id-123', level: 'post' as any },
    () => {},
  );

  // Call unsubscribe to stop receiving real-time events
  unsubscribe();
  console.log('Unsubscribed from real-time events');
}
```

</CodeGroup>
