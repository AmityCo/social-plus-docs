{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-reactions | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getReactions(postId: String) {
        AmitySocialClient.newReactionRepository()
            .getReactions(
                referenceType = AmityReactionReferenceType.POST,
                referenceId = postId
            )
            .build()
            .query()
            .doOnNext { pagedList ->
                // Handle paged list of reactions
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let reactionRepository = AmityReactionRepository(client: AmityManager.shared.client!)
        let liveCollection = reactionRepository.getReactions(referenceId: "postId", referenceType: .post)
        liveCollection.observe { collection, _, error in
            guard error == nil else { return }
            print("Reactions: \(collection.count())")
            for i in 0..<collection.count() {
                if let reaction = collection.object(at: i) {
                    print("  \(reaction.userId): \(reaction.reactionName)")
                }
            }
        }
```

```dart Flutter
void getReactions() {
    AmitySocialClient.newPostRepository()
        .getReactions(
          referenceId: 'postId',
          referenceType: AmityReactionReferenceType.POST,
        )
        .query();
  }
```

```typescript TypeScript
import { ReactionRepository } from '@amityco/ts-sdk'

async function getReactions() {
  const unsubscribe = ReactionRepository.getReactions(
    { referenceId: 'post123', referenceType: 'post' },
    (reactions, error) => {
      if (error) return console.error(error)
      console.log('Reactions:', reactions)
      unsubscribe()
    }
  )
}
```

</CodeGroup>
