{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-reactions | updated: 2026-05-02 */}

<CodeGroup>

```typescript TypeScript
import { ReactionRepository } from '@amityco/ts-sdk'

function getPostReactions(postId: string) {
  const liveCollection = ReactionRepository.queryReactions({
    referenceType: 'post',
    referenceId: postId,
  })

  liveCollection.on('dataUpdated', (reactions) => {
    console.log(`Reactions on post ${postId}:`, reactions.length)
    reactions.forEach((reaction) => {
      console.log(' -', reaction.reactionName, 'by', reaction.userId)
    })
  })

  liveCollection.on('dataError', (error) => {
    console.error('Error fetching reactions:', error)
  })
}
```

</CodeGroup>
