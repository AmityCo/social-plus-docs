{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: remove-reaction | updated: 2026-05-02 */}

<CodeGroup>

```swift iOS
let reactionRepository = AmityReactionRepository()
        reactionRepository.removeReaction("like", referenceId: "postId", referenceType: .post) { error in
            if let error = error {
                print("Error removing reaction: \(error)")
            } else {
                print("Reaction removed")
            }
        }
```

```dart Flutter
void removeReactionFromPost(AmityPost post, String reactionName) {
    post.react()
        .removeReaction(reactionName)
        .then((value) {
          // reaction removed
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { ReactionRepository } from '@amityco/ts-sdk'

async function removeReactionFromPost(postId: string, reactionName: string) {
  await ReactionRepository.removeReaction('post', postId, reactionName)
  console.log(`Reaction '${reactionName}' removed from post ${postId}`)
}

// Example usage
removeReactionFromPost('postId', 'like')
```

</CodeGroup>
