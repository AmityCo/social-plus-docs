{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-live-collection | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getLiveCollection() {
        // TODO: implement get live collection using Social Plus SDK
    }
```

```swift iOS
func getLiveCollection() {
        let communityRepository = AmityCommunityRepository(client: AmityManager.shared.client!)
        let queryOptions = AmityCommunityQueryOptions()
        queryOptions.filter = .all
        queryOptions.sortBy = .displayName
        
        let liveCollection = communityRepository.getCommunities(with: queryOptions)
        print("Live collection dataStatus: \(liveCollection.dataStatus)")
        // Observe changes
        let token = liveCollection.observe { collection, changes, error in
            guard error == nil else { return }
            print("Communities count: \(collection.count())")
        }
        // Retain token as long as observation is needed
    }
```

</CodeGroup>
