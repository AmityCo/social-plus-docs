{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: subscribe_a_topic | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun subscribeATopic() {
        val topic = AmityTopicBuilder.topic(AmityPost::class.java)
            .forPost("postId")
            .build()
        AmityCoreClient.subscribeTopic(topic)
            .subscribe({
                // Subscribed to topic successfully
            }, { error ->
                // Handle error
            })
    }
```

```dart Flutter
void subscribeATopic() {
    // Subscribe to a real-time topic
    final topic = TopicBuilder.community(communityId: 'communityId')
        .post()
        .build();
    AmityCoreClient.subscribeTopic(topic).then((_) {
      // Successfully subscribed to topic
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function subscribeATopic() {
  const { subscribeTopic } = await import('@amityco/ts-sdk');
  const topic = getCommunityTopic('communityId');
  const unsubscribe = subscribeTopic(topic);
  return unsubscribe;
}
```

</CodeGroup>
