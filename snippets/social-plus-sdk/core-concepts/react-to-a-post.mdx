{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: react-to-a-post | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun reactToAPost() {
        // TODO: Implement React to a post
    }
```

```swift iOS
func reactToAPost() {
        let reactionRepository = AmityReactionRepository(client: AmityClient.shared)
        reactionRepository.addReaction("like", referenceId: "postId", referenceType: .post) { success, error in
            if let error = error {
                print("Failed to react to post: \(error.localizedDescription)")
                return
            }
            print("Reaction added to post successfully")
        }
    }
```

```dart Flutter
void reactToAPost() async {
    final reactionRepository = AmitySocialClient.newReactionRepository();
    await reactionRepository
        .addReaction(
          referenceType: AmityReactionReferenceType.POST,
          referenceId: 'postId',
          reactionName: 'like',
        );
  }
```

```typescript TypeScript
import { ReactionRepository } from '@amityco/ts-sdk'

async function reactToPost() {
  const reactionData = {
    referenceId: 'postId123',
    referenceType: 'post' as const,
    reactionName: 'like',
  };

  await ReactionRepository.addReaction(reactionData);
  console.log('Reaction added to post');
}
```

</CodeGroup>
