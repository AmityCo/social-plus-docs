{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: realtime-event-reaction | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun realtimeEventReaction() {
        // TODO: implement realtime event reaction using Social Plus SDK
    }
```

```swift iOS
func realtimeEventReaction() {
        let postId = "postId"
        let reactionRepository = AmityReactionRepository(client: AmityManager.shared.client!)
        let liveCollection = reactionRepository.getReactions(referenceId: postId, referenceType: .post, reactionName: nil)
        
        let token = liveCollection.observe { collection, changes, error in
            guard error == nil else { return }
            print("Reactions updated, count: \(collection.count())")
        }
        // Retain token as long as observation is needed
    }
```

</CodeGroup>
