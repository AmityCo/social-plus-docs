{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-get-viewed | updated: 2026-05-01 */}

<CodeGroup>

```dart Flutter
final _viewedUsers = <AmityUser>[];
  late PagingController<AmityUser> _controller;

  void getViewedUsers(String postId) {
    _controller = PagingController(
      pageFuture: (token) => AmityCoreClient.newUserRepository()
          .getViewedUsers(
              viewedType: AmityViewedType.POST, viewedId: postId)
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    )..addListener(() {
        if (_controller.error == null) {
          _viewedUsers.clear();
          _viewedUsers.addAll(_controller.loadedItems);
          // update widgets
        } else {
          // handle error
        }
      });
  }
```

</CodeGroup>
