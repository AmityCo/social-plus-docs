{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: realtime-subscription-message | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun realtimeSubscriptionMessage() {
        // TODO: implement realtime subscription message
    }
```

```swift iOS
let messageRepository = AmityMessageRepository()
        let liveCollection = messageRepository.getMessages(
            subChannelId: "subChannelId",
            includeDeleted: false,
            filterByParentId: false,
            parentId: nil,
            orderBy: .descending
        )
        liveCollection.observe { liveCollection, error in
            let messages = liveCollection.allObjects() ?? []
            print("Messages updated in real-time, count: \(messages.count)")
            if let latest = messages.first {
                print("Latest message: \(latest.data?["text"] ?? "")")
            }
        }
```

</CodeGroup>
