{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: file-download-image | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun downloadImage(fileId: String) {
        AmityCoreClient.newFileRepository()
            .getFile(fileId = fileId)
            .doOnSuccess { rawFile: AmityRawFile ->
                if (rawFile.getFileType() == AmityFileType.IMAGE) {
                    val image: AmityImage? = rawFile.asAmityImage()
                    image?.let {
                        val imageUrl = it.getUrl(AmityImage.Size.MEDIUM)
                    }
                }
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
fileRepository.downloadImage(fromURL: "https://example.com/image.png", size: .full) { fileURL, error in
            if let error = error {
                // Handle error here
                print("Error: \(error)")
                return
            }
            if let fileURL = fileURL {
                print("Image downloaded to: \(fileURL.path)")
            }
        }
```

```dart Flutter
void fileDownloadImage() {
    final fileRepo = AmityCoreClient.newFileRepository();
    fileRepo
        .downloadImage('imageFileId', size: AmityImageSize.large)
        .then((file) => print('Image downloaded to: \${file.path}'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { FileRepository } from '@amityco/ts-sdk'

function downloadImage(fileUrl: string) {
  const imageUrl = FileRepository.fileUrlWithSize(fileUrl, 'medium')
  console.log('Image URL:', imageUrl)
  return imageUrl
}
```

</CodeGroup>
