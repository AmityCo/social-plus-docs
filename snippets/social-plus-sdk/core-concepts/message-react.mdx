{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-react | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun messageReact() {
        // Add or query reactions
        AmityChatClient.newMessageRepository()
            .addReaction(messageId = "messageId", reactionName = "like")
            .subscribe()
    }
```

```swift iOS
let reactionRepo = AmityReactionRepository(client: AmityManager.shared.client!)
        reactionRepo.addReaction("like", referenceId: "messageId123", referenceType: .message) { success, error in
            if let error = error { print("Error: \(error)"); return }
            print("Reaction added")
        }
```

```dart Flutter
void messageReact() async {
    await AmityChatClient.newMessageRepository()
        .getMessage('messageId')
        .then((message) async {
      await message.react().addReaction('like');
      print('Reaction added to message');
    });
  }
```

```typescript TypeScript
import { ReactionRepository } from '@amityco/ts-sdk'

async function messageReact(referenceId: string, reactionName: string) {
  await ReactionRepository.addReaction({
    referenceType: 'message',
    referenceId,
    reactionName,
  })
  console.log(`Reaction '${reactionName}' added to message:`, referenceId)
}
```

</CodeGroup>
