{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-react | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void messageReact() async {
    await AmityChatClient.newMessageRepository()
        .getMessage('messageId')
        .then((message) async {
      await message.react().addReaction('like');
      print('Reaction added to message');
    });
  }
```

```typescript TypeScript
import { ReactionRepository } from '@amityco/ts-sdk'

async function messageReact(referenceId: string, reactionName: string) {
  await ReactionRepository.addReaction({
    referenceType: 'message',
    referenceId,
    reactionName,
  })
  console.log(`Reaction '${reactionName}' added to message:`, referenceId)
}
```

</CodeGroup>
