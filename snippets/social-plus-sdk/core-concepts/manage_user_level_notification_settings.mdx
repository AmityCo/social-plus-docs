{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: manage_user_level_notification_settings | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun manageUserLevelNotificationSettings() {
        // Manage user-level push notification settings
        AmityCoreClient.notifications()
            .enable()
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(io.reactivex.rxjava3.android.schedulers.AndroidSchedulers.mainThread())
            .subscribe({
                // User-level notifications enabled
            }, { error ->
                // Handle error
            })
    }
```

```dart Flutter
void manageUserLevelNotificationSettings() async {
    final notifRepo = AmityCoreClient.newUserRepository();
    await notifRepo.notification().enable(
      events: [
        AmityUserNotificationEvent.NEW_MESSAGE,
        AmityUserNotificationEvent.NEW_COMMENT,
      ],
    );
    print('User notification settings updated.');
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function manageUserLevelNotificationSettings() {
  await NotificationRepository.setUserNotificationSettings({
    isPushNotificationEnabled: true,
    modules: [
      { moduleType: 'post', isEnabled: true },
      { moduleType: 'comment', isEnabled: false },
    ],
  })
  console.log('User notification settings updated')
}
```

</CodeGroup>
