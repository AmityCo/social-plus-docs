{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: unsync_user_presence | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun unsyncUserPresence() {
        // Perform unsync_user_presence operation
        AmityCoreClient.newUserRepository()
            .getUser("userId")
            .asFlowable()
            .subscribe({ user -> }, { error -> })
    }
```

```dart Flutter
void unsyncUserPresence() {
    AmityCoreClient.newUserRepository()
        .unsyncPresence(userId: 'userId')
        .then((_) {
      // User presence unsynced
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function unsyncUserPresence() {
  UserRepository.Presence.unsyncUserPresence({ userId: 'userId' })
}
```

</CodeGroup>
