{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: observe-community-topic | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun observeCommunityTopic() {
        AmitySocialClient.newCommunityRepository()
            .getCommunity("communityId")
            .asLiveData()
            .observe(lifecycleOwner) { community ->
                // Handle community topic update
            }
    }
```

```swift iOS
func observeCommunityTopic() {
        let topic = AmityCommunityTopic(communityId: "communityId")
        token = AmityEventBus.shared.subscribe(topic) { event in
            print("Community event received: \(event)")
        }
    }
```

```dart Flutter
void observeCommunityTopic() async {
        final subscription = AmityEventBus.subscribe(
          AmityCommunityTopic(communityId: 'communityId'),
          handler: (event) {
            print('Community event received: \$event');
          },
        );

        // To unsubscribe: subscription.cancel();
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function observeCommunityTopic() {
  const topic = getCommunityTopic({ communityId: 'communityId123' });
  const unsubscribe = subscribeTopic(topic, () => {
    console.log('Community topic event received');
  });
  return unsubscribe;
}
```

</CodeGroup>
