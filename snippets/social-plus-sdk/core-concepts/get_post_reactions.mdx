{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_post_reactions | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void getPostReactions() async {
    AmitySocialClient.newPostRepository()
        .getReactions(postId: 'postId')
        .getPagingData(token: null, limit: 20)
        .asStream()
        .listen((result) {
      final reactions = result.data;
    });
  }
```

```typescript TypeScript
import { ReactionRepository } from '@amityco/ts-sdk'

function getPostReactions() {
  const referenceId = 'post123'
  const liveCollection = ReactionRepository.getReactions({ referenceId, referenceType: 'post', limit: 20 })
  liveCollection.on('dataUpdated', (reactions) => {
    console.log('Post reactions:', reactions)
  })
}
```

</CodeGroup>
