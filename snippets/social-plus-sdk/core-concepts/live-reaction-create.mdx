{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: live-reaction-create | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createLiveReaction(
        liveStreamId: String,
        referenceId: String,
        reactionName: String
    ) {
        AmityCoreClient.newLiveReactionRepository()
            .createReaction(
                liveStreamId = liveStreamId,
                referenceId = referenceId,
                referenceType = AmityLiveReactionReferenceType.POST,
                reactionName = reactionName
            )
    }
```

```swift iOS
let reactionRepository = AmityReactionRepository()
        reactionRepository.addReaction("like", referenceId: "postId", referenceType: .post) { error in
            if let error = error {
                print("Error adding live reaction: \(error)")
            } else {
                print("Live reaction added")
            }
        }
```

```dart Flutter
void liveReactionCreate() async {
    final streamRepo = AmityVideoClient.newStreamRepository();
    await streamRepo.createReaction(
      streamId: 'streamId123',
      reactionType: 'heart',
    );
    print('Live reaction created.');
  }
```

```typescript TypeScript
import { LiveReactionRepository } from '@amityco/ts-sdk'

async function createLiveReaction() {
  await LiveReactionRepository.createLiveReaction({
    referenceId: 'roomId',
    referenceType: 'room',
    reaction: 'heart',
  })
}
```

</CodeGroup>
