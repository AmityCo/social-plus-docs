{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: upload_image | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun uploadImage() {
        // Upload operation for upload_image
        AmityCoreClient.newFileRepository()
            .uploadImage(imageUri)
            .subscribe({ uploadResult -> 
                // Image uploaded: ${uploadResult.getFile().getFileId()}
            }, { error -> })
    }
```

```swift iOS
// Note: `imageUrl` must be a local file url.
    func uploadImageExample(imageUrl: URL) {
        fileRepository.uploadImage(with: imageUrl, isFullImage: true, progress: { progress in
            print("uploading progress: \(progress)")
        }, completion: { image, error in
            if let error = error {
                print("uploading finish with error: \(error)")
                return
            }
            print("uploading finish successfully: fileId \(String(describing: image?.fileId))")
        })
    }
```

```dart Flutter
void uploadImage() {
    final fileRepo = AmityCoreClient.newFileRepository();
    fileRepo
        .uploadImage(AmityUploadableFile.fromPath('/path/to/image.jpg'))
        .then((AmityImageInfo imageInfo) {
          print('Image uploaded. FileId: \${imageInfo.fileId}');
        })
        .onError((error, stackTrace) {
          print('Error: \$error');
        });
  }
```

```typescript TypeScript
import { Client } from '@amityco/ts-sdk'

async function uploadImage() {
  const client = Client.getActiveClient();
  const formData = new FormData();
  formData.append('file', imageBlob, 'photo.jpg');
  const uploadedImage = await client.uploadImage(formData);
  console.log('Image uploaded, ID:', uploadedImage.fileId);
}
```

</CodeGroup>
