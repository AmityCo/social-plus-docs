{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: ad_delete | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun adDelete() {
        AmitySocialClient.newAdRepository()
            .deleteAd(adId = "adId")
            .subscribe()
    }
```

```swift iOS
let adRepository = AmityAdRepository(client: AmityManager.shared.client!)
        adRepository.deleteAd(adId: "adId") { success, error in
            if let error = error { print("Error deleting ad: \(error)"); return }
            print("Ad deleted: \(success)")
        }
```

```dart Flutter
void adDelete() async {
    await AmityAdRepository()
        .deleteAd(adId: 'adId');
    print('Ad deleted');
  }
```

```typescript TypeScript
import { Client } from '@amityco/ts-sdk'

async function deleteAd(adId: string) {
  await Client.getAdRepository().deleteAd(adId)
  console.log(`Ad ${adId} has been deleted.`)
}
```

</CodeGroup>
