{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: subscribe_comment_topic | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun subscribeCommentTopic() {
        val topic = AmityTopicBuilder.topic(AmityComment::class.java)
            .forPost("postId")
            .build()
        AmityCoreClient.subscribeTopic(topic)
            .subscribe({
                // Subscribed to comment topic for the post
            }, { error ->
                // Handle error
            })
    }
```

```dart Flutter
void subscribeCommentTopic() {
    final topic = TopicBuilder.community(communityId: 'communityId')
        .comment()
        .build();
    AmityCoreClient.subscribeTopic(topic).then((_) {
      // Subscribed to comment events
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function subscribeCommentTopic() {
  const { subscribeTopic } = await import('@amityco/ts-sdk');
  const topic = getCommentTopic('commentId');
  const unsubscribe = subscribeTopic(topic, (event) => {
    console.log('Comment event:', event);
  });
  return unsubscribe;
}
```

</CodeGroup>
