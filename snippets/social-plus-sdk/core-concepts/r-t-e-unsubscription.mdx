{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: r-t-e-unsubscription | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
val subscriptions: MutableList<AmityTopicSubscription> = mutableListOf()
    override fun onViewCreated(view: View, savedInstanceState: Bundle?) {
        super.onViewCreated(view, savedInstanceState)
        observeCommunity() // observe via LiveObect
        subscribeCommunity(community = community) // subscribe to a community
    }
    //
    private fun subscribeCommunity(community: AmityCommunity) {
        val topicSubscription = community.subscription(events = AmityCommunityEvents.COMMUNITY)
        topicSubscription
            .subscribeTopic()
            .doOnComplete {
                // keep ref of topicSubscription for disconnection
                subscriptions.add(topicSubscription)
            }
            .subscribe()
    }
    //
    override fun onDestroy() {
        // Unsubscribe topics before leaving each screen
        subscriptions.forEach { topicSubscription ->
            topicSubscription.unsubscribeTopic()
                .subscribe()
        }
        super.onDestroy()
    }
```

```swift iOS
AmityUIKitManagerInternal.shared.client.unsubscribeTopic(AmityTopicCreator.postTopic(forPostId: "postId")) { error in
            print(error == nil ? "Unsubscribed from topic" : "Unsubscribe failed: \(error!.localizedDescription)")
        }
```

```dart Flutter
StreamSubscription? _subscription;

    void unsubscribeFromRealtimeEvents() {
      _subscription?.cancel();
      _subscription = null;
    }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function rTEUnsubscription() {
  const unsubscribe = subscribeTopic(
    PostTopic.post('postId123'),
    () => {
      console.log('Post realtime event received');
    },
  );

  // Later, stop receiving events
  unsubscribe();
  console.log('Unsubscribed from realtime events');
}
```

</CodeGroup>
