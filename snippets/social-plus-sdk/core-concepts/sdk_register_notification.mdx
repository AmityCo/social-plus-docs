{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: sdk_register_notification | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun sdkRegisterNotification() {
        // perform sdk register notification operation
        AmityCoreClient.login("userId")
            .displayName("User Display Name")
            .build()
            .submit()
            .subscribe()
    }
```

```dart Flutter
void sdkRegisterNotification() {
    final fcmToken = await FirebaseMessaging.instance.getToken();
    if (fcmToken != null) {
      await AmityCoreClient.registerDeviceNotification(fcmToken);
      print('Push notification registered: \$fcmToken');
    }
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function sdkRegisterNotification() {
  import { NotificationRepository } from '@amityco/ts-sdk';

  await NotificationRepository.registerDevicePushNotification({
    deviceToken: 'device-push-token-abc123',
  });
  console.log('Device registered for push notifications');
}
```

</CodeGroup>
