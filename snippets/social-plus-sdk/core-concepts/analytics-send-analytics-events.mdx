{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: analytics-send-analytics-events | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun sendAnalyticsEvents(syncPriority: AnalyticsEventSyncPriority) {
        AnalyticsRepository()
            .sendAnalyticsEvents(syncPriority = syncPriority)
            .doOnComplete { /* events sent */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let analyticsManager = AmityAnalyticsManager()
        analyticsManager.sendEvents { error in
            if let error = error {
                print("Error sending events: \(error)")
            } else {
                print("Analytics events sent successfully")
            }
        }
```

```dart Flutter
void analyticsSendAnalyticsEvents() async {
    await AmityAnalyticsClient.sendEvents(
      events: [
        AmityAnalyticsEvent(
          eventType: 'ad_impression',
          properties: {'adId': 'adId', 'placement': 'feed'},
        ),
      ],
    );
  }
```

```typescript TypeScript
import { AdRepository } from '@amityco/ts-sdk'

async function sendAnalyticsEvents(adId: string) {
  try {
    await AdRepository.markAsSeen({ adId });
    console.log('Ad marked as seen:', adId);
    await AdRepository.markLinkAsClicked({ adId });
    console.log('Ad link marked as clicked:', adId);
  } catch (error) {
    console.error('Failed to send analytics event:', error);
  }
}
```

</CodeGroup>
