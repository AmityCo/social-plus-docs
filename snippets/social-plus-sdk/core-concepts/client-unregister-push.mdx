{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: client-unregister-push | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun unregisterPush() {
        AmityCoreClient.unregisterPushNotification()
            .doOnComplete {
                // success
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
do {
            try await client.unregisterPushNotification()
        } catch {
            // Handle error here
        }
```

```dart Flutter
void unregisterPush() {
    AmityCoreClient.unregisterDeviceNotification()
        .then((value) => {
              //success
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

```typescript TypeScript
import { Client } from '@amityco/ts-sdk'

async function unregisterPushNotification(deviceToken: string) {
  await Client.unregisterPushNotification(deviceToken)
  console.log('Push notification unregistered successfully')
}
```

</CodeGroup>
