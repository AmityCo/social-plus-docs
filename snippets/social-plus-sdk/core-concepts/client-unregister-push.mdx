{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: client-unregister-push | updated: 2026-04-30 */}

<CodeGroup>

```kotlin Android
fun unregisterPush() {
        AmityCoreClient.unregisterPushNotification()
            .doOnComplete {
                // success
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
do {
            try await client.unregisterPushNotification()
        } catch {
            // Handle error here
        }
```

```dart Flutter
void unregisterPush() {
    AmityCoreClient.unregisterDeviceNotification()
        .then((value) => {
              //success
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

</CodeGroup>
