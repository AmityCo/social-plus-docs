{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-mention-in-comment | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun userMentionInComment() {
        val postId = "postId"
        val mentionedUserId = "userId"
        AmitySocialClient.newCommentRepository()
            .createComment()
            .post(postId)
            .with()
            .text("Great post @user!")
            .mentionUsers(listOf(mentionedUserId))
            .build()
            .comment()
            .subscribe()
    }
```

```swift iOS
let commentRepository = AmityCommentRepository()
        let builder = AmityCommentCreateOptions()
        builder.setText("Nice post @jane!")
        let metadata: [String: Any] = [
            "mentioned": [
                ["type": "user", "userId": "janeUserId", "length": 5, "index": 10]
            ]
        ]
        let mentionees = AmityMentionees()
        mentionees.mentionUsers(userIds: ["janeUserId"])
        commentRepository.createComment(
            builder,
            referenceId: "postId",
            referenceType: .post,
            metadata: metadata,
            mentionees: mentionees
        ) { comment, error in
            if let comment = comment {
                print("Comment created with mention: \(comment.commentId)")
            }
        }
```

```dart Flutter
void userMentionInComment() {
    final mentionees = AmityMentioneesBuilder()
        .users(['targetUserId'])
        .build();
    AmitySocialClient.newCommentRepository()
        .createComment()
        .post(postId: 'postId')
        .create()
        .text('@targetUser great point!')
        .mentionees(mentionees)
        .send()
        .then((comment) => print('Comment with mention created'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function userMentionInComment() {
  const { data: comment } = await CommentRepository.createComment({
    referenceId: 'postId',
    referenceType: 'post',
    data: { text: 'Hello @userId!' },
    metadata: {
      mentioned: [{ userId: 'userId', index: 6, length: 7 }],
    },
  })
  console.log('Comment with mention created:', comment.commentId)
}
```

</CodeGroup>
