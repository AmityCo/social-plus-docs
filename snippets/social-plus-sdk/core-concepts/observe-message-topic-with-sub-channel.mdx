{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: observe-message-topic-with-sub-channel | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun observeMessageTopicWithSubChannel() {
        // Observe messages in a channel as a live collection
        AmityChatClient.newMessageRepository()
            .getMessages(channelId = "channel123")
            .build()
            .query()
            .subscribe({ pagingData ->
                // Handle real-time message updates
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
let channelRepository = AmityChannelRepository()
        let subChannelLiveObject = channelRepository.getSubChannel(withId: "subChannelId")
        subChannelLiveObject.observe { subChannel, error in
            guard let subChannel = subChannel.snapshot else { return }
            let topic = AmityMessageTopic(subChannelId: subChannel.subChannelId)
            AmityEventBus.shared.observe(topic: topic) { event in
                print("Message event for sub-channel: \(event)")
            }
        }
```

```dart Flutter
void observeMessageTopicWithSubChannel() async {
        final subscription = AmityEventBus.subscribe(
          AmityMessageTopic(
            channelId: 'channelId',
            subChannelId: 'subChannelId',
          ),
          handler: (event) {
            print('Message event in sub-channel: \$event');
          },
        );

        // To unsubscribe: subscription.cancel();
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function observeMessageTopicWithSubChannel() {
  const topic = getMessageTopic({ subChannelId: 'subChannelId123' });
  const unsubscribe = subscribeTopic(topic, () => {
    console.log('Sub-channel message topic event received');
  });
  return unsubscribe;
}
```

</CodeGroup>
