{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: observe-message-topic-with-sub-channel | updated: 2026-05-02 */}

<CodeGroup>

```swift iOS
let channelRepository = AmityChannelRepository()
        let subChannelLiveObject = channelRepository.getSubChannel(withId: "subChannelId")
        subChannelLiveObject.observe { subChannel, error in
            guard let subChannel = subChannel.snapshot else { return }
            let topic = AmityMessageTopic(subChannelId: subChannel.subChannelId)
            AmityEventBus.shared.observe(topic: topic) { event in
                print("Message event for sub-channel: \(event)")
            }
        }
```

</CodeGroup>
