{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-user-by-id | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getUserById() {
        // Retrieve a user profile by user ID
        AmityCoreClient.newUserRepository()
            .getUser(userId = "userId")
            .subscribe { user -> /* handle user */ }
    }
```

```swift iOS
// Get a user profile by their user ID
        let userRepository = AmityUserRepository(client: AmityManager.shared.client!)
        var token: AmityNotificationToken?
        let liveObject = userRepository.getUser("userId")
        token = liveObject.observe { user, error in
            if let error = error {
                print("Failed to get user: \(error.localizedDescription)")
                return
            }
            guard let user = user.object else { return }
            print("User: \(user.userId), displayName: \(user.displayName ?? "")")
        }
```

```dart Flutter
void getUserById() {
    AmitySocialClient.newUserRepository()
        .searchUserByDisplayName('displayName')
        .query();
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function getUserById() {
  const unsubscribe = UserRepository.getUser('user123', (user, error) => {
    if (error) return console.error(error)
    console.log('User:', user?.displayName)
    unsubscribe()
  })
}
```

</CodeGroup>
