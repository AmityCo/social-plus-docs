{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: story-reaction-query | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun queryReactions(story: AmityStory) {
        AmityCoreClient.newReactionRepository()
            .getReactions(
                reactionReference = AmityReactionReference.STORY(storyId = story.getStoryId())
            )
            .doOnNext { reactions: PagedList<AmityReaction> ->
                // handle reactions list
            }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let reactionRepository = AmityReactionRepository(client: client)
        var token: AmityNotificationToken?
        token = reactionRepository.getReactions("<story-id>", referenceType: .story, reactionName: nil)
            .observe { collection, change, error in
                // Handle collection updates
            }
```

```dart Flutter
final _reactions = <AmityReaction>[];
  late PagingController<AmityReaction> _controller;

  void queryStoryReactions(AmityStory story) {
    _controller = PagingController(
      pageFuture: (token) => AmitySocialClient.newReactionRepository()
          .getReactions(AmityReactionReferenceType.STORY, story.storyId)
          .getPagingData(token: token, limit: 20),
      pageSize: 20,
    )..addListener(
        () {
          if (_controller.error == null) {
            _reactions.clear();
            _reactions.addAll(_controller.loadedItems);
            // update UI
          } else {
            // handle error
          }
        },
      );
  }
```

</CodeGroup>
