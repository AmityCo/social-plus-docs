{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: combine_property_access | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun combinePropertyAccess() {
        AmitySocialClient.newUserRepository()
            .getUser("userId")
            .subscribe { user ->
                val displayName = user.getDisplayName()
                val avatarUrl = user.getAvatar()?.getUrl()
                val description = user.getDescription()
            }
    }
```

```dart Flutter
void combinePropertyAccess() {
    // Access multiple properties from a combined live object result
    final livePost = AmitySocialClient.newPostRepository().getPost('postId');
    final liveCommunity = AmitySocialClient.newCommunityRepository()
        .getCommunity('communityId');

    Rx.combineLatest2(
      livePost.stream,
      liveCommunity.stream,
      (post, community) {
        print('Post: \${post.postId}, Community: \${community.displayName}');
        return (post: post, community: community);
      },
    ).listen((_) {});
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function combinePropertyAccess() {
  const liveObject = PostRepository.getPost('post-id-here');
  liveObject.on('dataUpdated', (post) => {
    const { postId, data, reactions, myReactions } = post;
    console.log('Post ID:', postId);
    console.log('Reactions:', reactions);
  });
}
```

</CodeGroup>
