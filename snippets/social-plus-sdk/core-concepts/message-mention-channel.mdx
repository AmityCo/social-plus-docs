{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-mention-channel | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createTextMessageWithChannelMention(
        messageRepository: AmityMessageRepository
    ) {
        // mention to the whole channel
        messageRepository
            .createTextMessage(
                subChannelId = "subChannelId",
                text = "hello @all how are you guys doing?"
            )
            .metadata(metadata = JsonObject()) // flexible data structure for highlighting text
            .mentionChannel()
            .build()
            .send()
            .doOnComplete {
                // Void
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```dart Flutter
void messageMentionChannel() async {
    await AmityChatClient.newMessageRepository()
        .createMessage('channelId')
        .text('Attention @channel everyone!')
        .mentionChannel()
        .send();
    print('Message with channel mention sent');
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function messageMentionChannel(channelId: string) {
  const message = await MessageRepository.createMessage({
    channelId,
    dataType: 'text',
    data: { text: 'Hey @channel, check this out!' },
    mentionees: [{ type: 'channel' }],
    metadata: {},
  })
  console.log('Channel mention message sent:', message.messageId)
  return message
}
```

</CodeGroup>
