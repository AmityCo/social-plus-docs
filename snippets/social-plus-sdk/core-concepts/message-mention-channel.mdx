{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-mention-channel | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun messageMentionChannel() {
        AmityChatClient.newMessageRepository()
            .createMessage("subChannelId123")
            .text("Hello @channel everyone")
            .mentionChannel()
            .build()
            .send()
            .subscribe()
    }
```

```swift iOS
func messageMentionChannel() {
        let client = AmityClient.shared
        let messageRepo = AmityMessageRepository(client: client)
        let builder = AmityMessageCreationBuilder()
        builder.setSubChannelId("subChannelId")
        builder.setText("Attention @all members!")
        let mentionees = AmityMentionees()
        mentionees.mentionChannel()
        builder.setMentionees(mentionees)
        messageRepo.createMessage(with: builder) { message, error in
            if let message = message {
                print("Channel mention message created: \(message.messageId)")
            }
        }
    }
```

```dart Flutter
void messageMentionChannel() async {
    await AmityChatClient.newMessageRepository()
        .createMessage('channelId')
        .text('Attention @channel everyone!')
        .mentionChannel()
        .send();
    print('Message with channel mention sent');
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function messageMentionChannel(channelId: string) {
  const message = await MessageRepository.createMessage({
    channelId,
    dataType: 'text',
    data: { text: 'Hey @channel, check this out!' },
    mentionees: [{ type: 'channel' }],
    metadata: {},
  })
  console.log('Channel mention message sent:', message.messageId)
  return message
}
```

</CodeGroup>
