{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-get-reaction | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun messageGetReaction() {
        // Add or query reactions
        AmityChatClient.newMessageRepository()
            .addReaction(messageId = "messageId", reactionName = "like")
            .subscribe()
    }
```

```swift iOS
let reactionRepo = AmityReactionRepository(client: AmityManager.shared.client!)
        let collection = reactionRepo.getReactions(referenceId: "messageId123", referenceType: .message, reactionName: nil)
        collection.observe { liveCollection, _, error in
            guard error == nil else { return }
            let reactions = (0..<liveCollection.count()).compactMap { liveCollection.object(at: $0) }
            print("Reactions: \(reactions.map { $0.reactionName })")
        }
```

```dart Flutter
void getMessageReaction(String messageId) {
    final reactionQueryBuilder = AmityChatClient.newMessageRepository()
        .getReaction(messageId: messageId);
    // use reactionQueryBuilder to query reactions on the message
  }
```

```typescript TypeScript
import { ReactionRepository } from '@amityco/ts-sdk'

function getMessageReaction() {
  const liveCollection = ReactionRepository.queryReactions({
    referenceType: 'message',
    referenceId: 'messageId',
  })
  liveCollection.on('dataUpdated', (reactions) => {
    console.log(reactions)
  })
  return liveCollection
}
```

</CodeGroup>
