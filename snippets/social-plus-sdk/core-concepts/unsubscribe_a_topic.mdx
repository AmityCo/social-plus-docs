{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: unsubscribe_a_topic | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun unsubscribeATopic() {
        // Perform unsubscribe_a_topic operation
        AmityCoreClient.newUserRepository()
            .getUser(AmityCoreClient.getUserId())
            .asFlowable()
            .subscribe({ user -> }, { error -> })
    }
```

```dart Flutter
void unsubscribeATopic() {
    final topic = TopicBuilder.community(communityId: 'communityId')
        .post()
        .build();
    AmityCoreClient.unsubscribeTopic(topic).then((_) {
      // Successfully unsubscribed from topic
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function unsubscribeATopic() {
  SubscriptionRepository.unsubscribeTopic({ topic: 'topicName' })
  console.log('Unsubscribed from topic')
}
```

</CodeGroup>
