{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: upload-file | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun uploadFile() {
        // Upload operation for upload-file
        AmityCoreClient.newFileRepository()
            .uploadFile(fileUri)
            .subscribe({ uploadResult -> 
                // File uploaded: ${uploadResult.getFile().getFileId()}
            }, { error -> })
    }
```

```swift iOS
let fileURL = URL(fileURLWithPath: "/path/to/document.pdf")
        AmitySDK.uploadFile(fileURL: fileURL) { fileData, error in
            if let error = error {
                print("Failed to upload file: \(error)")
                return
            }
            print("File uploaded with ID: \(fileData?.fileId ?? "")")
        }
```

```dart Flutter
void uploadFile() {
    final fileRepo = AmityCoreClient.newFileRepository();
    fileRepo
        .uploadFile(AmityUploadableFile.fromPath('/path/to/file.pdf'))
        .then((AmityFileInfo fileInfo) {
          print('File uploaded. FileId: \${fileInfo.fileId}');
        })
        .onError((error, stackTrace) {
          print('Error: \$error');
        });
  }
```

```typescript TypeScript
import { Client } from '@amityco/ts-sdk'

async function uploadFile() {
  const client = Client.getActiveClient();
  const formData = new FormData();
  formData.append('file', fileBlob, 'document.pdf');
  const uploadedFile = await client.uploadFile(formData);
  console.log('File uploaded, ID:', uploadedFile.fileId);
}
```

</CodeGroup>
