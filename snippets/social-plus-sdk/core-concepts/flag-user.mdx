{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: flag-user | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun flagUser(userId: String) {
        AmityCoreClient.newUserRepository()
            .flagUser(userId = userId)
            .doOnComplete {
                // User flagged successfully
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let userRepository = AmityUserRepository(client: AmityManager.shared.client!)
        userRepository.flagUser(withId: "userId") { _, error in
            if let error = error {
                print("Error flagging user: \(error)")
            } else {
                print("User flagged successfully")
            }
        }
```

```dart Flutter
void flagUser(String userId) {
    AmityCoreClient.newUserRepository()
        .flagUser(userId)
        .then((_) {
          // user flagged
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

</CodeGroup>
