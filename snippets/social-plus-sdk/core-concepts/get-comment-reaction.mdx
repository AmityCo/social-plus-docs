{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-comment-reaction | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getCommentReaction(commentId: String) {
        AmitySocialClient.newCommentRepository()
            .reaction(commentId = commentId)
            .getReactions()
            .build()
            .query()
            .doOnNext { reactions ->
                // PagingData<AmityReaction>
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
// Get reactions for a specific comment
        let reactionRepository = AmityReactionRepository(client: AmityManager.shared.client!)
        var token: AmityNotificationToken?
        let collection = reactionRepository.getReactions(
            referenceId: "commentId",
            referenceType: .comment,
            reactionName: "like"
        )
        token = collection.observe { liveCollection, _, error in
            if let error = error {
                print("Failed to get comment reactions: \(error.localizedDescription)")
                return
            }
            print("Comment reactions count: \(liveCollection.count())")
        }
```

```dart Flutter
void getCommentReaction() {
    final liveCollection = AmitySocialClient.newReactionRepository()
        .getReactions(
          referenceId: 'commentId',
          referenceType: AmityReactionReferenceType.COMMENT,
        )
        .getPagingData();
    liveCollection.asStream().listen((List<AmityReaction> reactions) {
      print('Comment reactions: \${reactions.length}');
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function getCommentReaction() {
  const liveCollection = runQuery(
    ReactionRepository.getReactions({ referenceId: 'commentId123', referenceType: 'comment' })
  );
  liveCollection.on('dataUpdated', (reactions) => console.log('Comment reactions:', reactions));
}
```

</CodeGroup>
