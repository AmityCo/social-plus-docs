{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: react_to_a_message | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun reactToAMessage() {
        // TODO: Implement React to a message
    }
```

```swift iOS
let reactionManager = AmityMessageReactionManager(channelId: "channelId", subChannelId: "subChannelId")
        reactionManager.addReaction(reaction: "like", messageId: "messageId") { success, error in
            if success {
                print("Reaction added to message successfully")
            } else if let error = error {
                print("Failed to add reaction: \(error)")
            }
        }
```

```dart Flutter
void reactToAMessage() async {
    final reactionRepository = AmitySocialClient.newReactionRepository();
    await reactionRepository
        .addReaction(
          referenceType: AmityReactionReferenceType.MESSAGE,
          referenceId: 'messageId',
          reactionName: 'like',
        );
  }
```

```typescript TypeScript
import { ReactionRepository } from '@amityco/ts-sdk'

async function reactToMessageUnderscore() {
  const reactionData = {
    referenceId: 'messageId123',
    referenceType: 'message' as const,
    reactionName: 'like',
  };

  await ReactionRepository.addReaction(reactionData);
  console.log('Reaction added to message');
}
```

</CodeGroup>
