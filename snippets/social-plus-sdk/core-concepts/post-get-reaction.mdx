{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-get-reaction | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun postGetReaction() {
        // Query posts as a live collection with real-time updates
        AmitySocialClient.newPostRepository()
            .getPosts()
            .targetMe()
            .build()
            .query()
            .subscribe({ pagingData ->
                // Handle paged post data
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
let reactionRepository = AmityReactionRepository(client: AmityClient.shared)
        let postId = "post_id"
        let liveCollection = reactionRepository.getReactions(
            referenceId: postId,
            referenceType: .post,
            reactionName: nil
        )
        liveCollection.observe { collection, _, error in
            if let error = error {
                print("Failed to get reactions: \(error.localizedDescription)")
                return
            }
            print("Reactions count: \(collection.count())")
        }
```

```dart Flutter
void getPostReaction(String postId) {
    final reactionQueryBuilder = AmitySocialClient.newPostRepository()
        .getReaction(postId: postId);
    // use reactionQueryBuilder to query reactions on the post
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function postGetReaction() {
  const liveCollection = ReactionRepository.getReactions({ referenceType: 'post', referenceId: 'postId123' });
  liveCollection.on('dataUpdated', (reactions) => {
    console.log('Post reactions:', reactions);
  });
}
```

</CodeGroup>
