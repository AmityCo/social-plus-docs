{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: poll-unvote | updated: 2026-05-01 */}

<CodeGroup>

```kotlin Android
fun unvotePoll(pollId: String, answerIds: List<String>) {
        AmitySocialClient.newPollRepository()
            .votePoll(pollId = pollId, answerIds = answerIds)
            .doOnComplete {
                // success
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let pollRepository = AmityPollRepository(client: client)
        do {
            try await pollRepository.deletePollVote(pollId: "<poll-id>", answerIds: ["<answer-id>"])
            print("Vote removed")
        } catch {
            // Handle error here
        }
```

```typescript TypeScript
import { PollRepository } from '@amityco/ts-sdk'

async function unvotePoll() {
  await PollRepository.deletePollVote('pollId', ['answerId'])
}
```

</CodeGroup>
