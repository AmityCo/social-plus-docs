{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: query_presence_settings | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun queryPresenceSettings() {
        // Get a user's current presence status
        AmityCoreClient.newUserRepository()
            .getUserPresence(userId = "userId")
            .asLiveData()
    }
```

```dart Flutter
void queryPresenceSettings() {
      AmityCoreClient.newUserRepository()
          .getPresenceSettings()
          .then((AmityPresenceSettings settings) {
        final isPresenceEnabled = settings.isEnabled;
        // Use presence settings
      }).onError((error, stackTrace) {
        // Handle error
      });
    }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function queryPresenceSettings() {
  const settings = await UserRepository.getPresenceSettings();
  console.log('Presence enabled:', settings.isEnabled);
  console.log('Presence timeout:', settings.timeoutSeconds);
}
```

</CodeGroup>
