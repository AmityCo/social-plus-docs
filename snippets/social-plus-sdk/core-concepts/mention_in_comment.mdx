{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: mention_in_comment | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun mentionInComment() {
        // Build mention metadata for content
        val mentionMetadata = com.amityco.socialcloud.sdk.model.AmityMentionMetadata.User(
            userId = "userId", displayName = "User Name", index = 0, length = 9
        )
        AmitySocialClient.newCommentRepository()
            .createComment()
            .post(postId = "postId")
            .with()
            .text("Hello @UserName")
            .mentionUsers(listOf(mentionMetadata))
            .build()
            .comment()
            .subscribe()
    }
```

```swift iOS
let mentionees: [AmityMentioneesBuilder] = [AmityMentioneesBuilder()]
        let metadata: [String: Any] = [:]
        let builder = AmityCommentCreationBuilder()
        builder.setMentionees(mentionees)
        builder.setMetadata(metadata)
```

```dart Flutter
void mentionInComment() {
    AmitySocialClient.newCommentRepository()
        .createComment()
        .post('postId')
        .create(
          text: 'Hello @userId',
          mentionees: [AmityMentionee.user('userId')],
        );
  }
```

```typescript TypeScript
import { CommentRepository } from '@amityco/ts-sdk'

async function mentionInComment(referenceId: string, referenceType: Amity.CommentReferenceType) {
  const mentionedUserIds = ['user1', 'user2']

  const newComment = await CommentRepository.createComment({
    referenceId,
    referenceType,
    data: { text: 'Hey @user1 and @user2, check this out!' },
    mentionees: [{ type: 'user', userIds: mentionedUserIds }],
    metadata: {},
  })

  return newComment
}
```

</CodeGroup>
