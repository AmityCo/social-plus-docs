{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-mention | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun messageMention() {
        // Build mention metadata for content
        val mentionMetadata = com.amityco.socialcloud.sdk.model.AmityMentionMetadata.User(
            userId = "userId",
            displayName = "User Name",
            index = 0,
            length = 9
        )
        AmityChatClient.newMessageRepository()
            .createMessage(channelId = "channelId")
            .with()
            .text("Hello @UserName")
            .mentionUsers(listOf(mentionMetadata))
            .build()
            .send()
            .subscribe()
    }
```

```swift iOS
let messageRepository = AmityMessageRepository()
        let mentionees = AmityMentioneesBuilder()
        mentionees.mentionUsers(userIds: ["userId1", "userId2"])
        messageRepository.createTextMessage(withChannelId: "channelId", subChannelId: "subChannelId", text: "@user1 Hello!", tags: nil, parentId: nil, mentionees: mentionees.build()) { message, error in
            if let error = error {
                print("Error creating mention message: \(error)")
            } else {
                print("Message with mention created")
            }
        }
```

```dart Flutter
void sendMessageWithMention(String channelId, String userId) {
    final metadata = AmityMentionMetadata.builder()
        .mentionUser(index: 0, length: userId.length + 1, userId: userId)
        .build();

    AmityChatClient.newMessageRepository()
        .createMessage(channelId)
        .text('@$userId Hey there!')
        .metadata(metadata.toMap())
        .mentionUsers([userId])
        .send()
        .then((AmityMessage message) {
          // message with mention sent
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function messageMention() {
  const mentionMessage = {
    subChannelId: 'channelId',
    dataType: 'text' as const,
    data: { text: 'Hello @john!' },
    mentionees: [{ type: 'user' as const, userIds: ['userId'] }],
    metadata: {},
  }
  
  const { data: message } = await MessageRepository.createMessage(mentionMessage)
  console.log('Mention message sent:', message.messageId)
}
```

</CodeGroup>
