{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: realtime_event_community | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun realtimeEventCommunity() {
        // perform realtime event community operation
        AmitySocialClient.newCommunityRepository()
            .getCommunity("communityId")
            .asFlowable()
            .subscribe { community ->
                // handle community object
            }
    }
```

```swift iOS
AmityUIKitManagerInternal.shared.client.subscribeTopic(AmityTopicCreator.communityTopic(forCommunityId: "communityId")) { error in
            print(error == nil ? "Subscribed to community topic" : "Failed: \(error!.localizedDescription)")
        }
        let communityRepository = AmityCommunityRepository(client: AmityUIKitManagerInternal.shared.client)
        let object = communityRepository.getCommunity(withId: "communityId")
        let token = object.observe { liveObject, error in
            guard error == nil, let community = liveObject.snapshot else { return }
            print("Community RTE update: \(community.communityId)")
        }
```

```dart Flutter
void realtimeEventCommunity() async {
    final communityRepository = AmitySocialClient.newCommunityRepository();
    final community = await communityRepository.getCommunity('communityId');
    await community
        .subscription(AmityCommunityEvents.POST)
        .subscribeTopic();
    print('Subscribed to community events');
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function realtimeEventCommunityUnderscore() {
  const liveCommunity = CommunityRepository.getCommunity('communityId123');

  liveCommunity.on('dataUpdated', community => {
    console.log('Community updated:', community.communityId, community.displayName);
  });

  liveCommunity.on('dataError', err => console.error('Community realtime error:', err));
}
```

</CodeGroup>
