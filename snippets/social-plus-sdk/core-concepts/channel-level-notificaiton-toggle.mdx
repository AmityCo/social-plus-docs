{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: channel-level-notificaiton-toggle | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun toggleChannelLevelNotification(channelId: String, enable: Boolean) {
        AmityCoreClient.notifications()
            .channel(channelId = channelId)
            .enable()
            .doOnComplete { }
            .doOnError { }
            .subscribe()
    }
```

```swift iOS
let notificationManager = AmityNotificationManager()
        notificationManager.setChannelNotificationSettings(channelId: "channelId", isEnabled: true) { error in
            if let error = error {
                print("Error toggling notifications: \(error)")
            } else {
                print("Channel notifications toggled")
            }
        }
```

```dart Flutter
void toggleChannelNotification(String channelId, bool enable) {
    final notificationManager = AmityChatClient.newChannelRepository()
        .notificationManagerForChannel(channelId: channelId);

    if (enable) {
      notificationManager
          .enable()
          .then((_) {
            // channel notification enabled
          })
          .onError((error, stackTrace) {
            // handle error
          });
    } else {
      notificationManager
          .disable()
          .then((_) {
            // channel notification disabled
          })
          .onError((error, stackTrace) {
            // handle error
          });
    }
  }
```

</CodeGroup>
