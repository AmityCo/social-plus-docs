{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create_observe_sync_state | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createObserveSyncState() {
        val liveObject = AmitySocialClient.newPostRepository().getPost("postId")
        liveObject
            .map { it.getSyncState() }
            .distinctUntilChanged()
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(AndroidSchedulers.mainThread())
            .subscribe({ syncState ->
                when (syncState) {
                    com.amityco.socialcloud.sdk.model.core.post.AmityPost.SyncState.SYNCED -> { /* synced */ }
                    com.amityco.socialcloud.sdk.model.core.post.AmityPost.SyncState.SYNCING -> { /* syncing */ }
                    else -> { /* error */ }
                }
            }, { error ->
                error.printStackTrace()
            })
    }
```

```swift iOS
func createObserveSyncState() {
        let postRepository = AmityPostRepository(client: AmityManager.shared.client!)
        let liveCollection = postRepository.getPosts(targetId: "communityId", targetType: .community, sortBy: .lastCreated, deletedOption: .notDeleted)
        liveCollection.observe { collection, changes, error in
            if let error = error { print("Error: \(error)"); return }
            print("Collection updated. Count: \(collection.count()), Sync state: \(collection.syncState)")
        }
    }
```

```dart Flutter
void createObserveSyncState() {
    AmityCoreClient.observeSyncState().listen((syncState) {
      switch (syncState) {
        case AmitySyncState.synced:
          print('SDK is synced');
          break;
        case AmitySyncState.syncing:
          print('SDK is syncing');
          break;
        default:
          print('Sync state: \$syncState');
      }
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function createObserveSyncState() {
  const liveObject = runQuery(PostRepository.postForPostId('postId123'));
  liveObject.on('dataUpdated', (post) => {
    console.log('Sync state:', post.syncState);
  });
}
```

</CodeGroup>
