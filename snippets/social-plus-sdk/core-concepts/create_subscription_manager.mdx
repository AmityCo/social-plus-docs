{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create_subscription_manager | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createSubscriptionManager() {
        // Create a subscription manager for real-time events
        val subscriptionManager = AmitySocialClient.newSubscriptionManager()
    }
```

```dart Flutter
void createSubscriptionManager() {
    final subscriptionManager = AmityCoreClient.newSubscriptionManager();
    subscriptionManager
        .subscribeTopic(AmityTopic.channel('channelId'))
        .onError((error, _) => print('Subscription error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function createSubscriptionManager() {
  const subscription = SubChannelRepository.subscribeSubChannel('subChannelId123', (event) => {
    console.log('Real-time event:', event);
  });
  // Unsubscribe when done
  return () => subscription.unsubscribe();
}
```

</CodeGroup>
