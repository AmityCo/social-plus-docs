{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: event-observe-channel | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun eventObserveChannel(channel: AmityChannel) {
        channel.subscription(events = AmityChannelEvents.CHANNEL)
            .subscribeTopic()
            .subscribe()
    }
```

```swift iOS
NotificationCenter.default.addObserver(
            forName: NSNotification.Name("AmityChannelDidUpdate"),
            object: nil,
            queue: .main
        ) { notification in
            if let channel = notification.object as? AmityChannel {
                print("Channel updated: \(channel.channelId)")
            }
        }
```

```dart Flutter
void eventObserveChannel() {
    final channelRepo = AmityChatClient.newChannelRepository();
    channelRepo
        .channel('channelId')
        .subscription(AmityChannelEvent())
        .subscribeTopic()
        .onError((error, _) => print('Subscription error: \$error'));
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

function observeChannelEvents() {
  const unsubscribeCreated = ChannelRepository.onChannelCreated((channel) => {
    console.log('Channel created:', channel.channelId)
  })

  const unsubscribeUpdated = ChannelRepository.onChannelUpdated((channel) => {
    console.log('Channel updated:', channel.channelId)
  })

  const unsubscribeDeleted = ChannelRepository.onChannelDeleted((channel) => {
    console.log('Channel deleted:', channel.channelId)
  })

  return () => {
    unsubscribeCreated()
    unsubscribeUpdated()
    unsubscribeDeleted()
  }
}
```

</CodeGroup>
