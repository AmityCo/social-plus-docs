{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: presence-heartbeat-start | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun startHeartbeat() {
        AmityCoreClient.presence()
            .startHeartbeat()
            .doOnComplete {
                //  Starting heartbeat syncing success
            }
            .doOnError {
                //  Handle Exception
            }
            .subscribe()

    }
```

```swift iOS
let client = AmityClient.shared
        let presenceRepository = AmityPresenceRepository(client: client)
        presenceRepository.startHeartbeat()
        print("Presence heartbeat started")
```

```dart Flutter
void startPresenceHeartbeat() {
    AmityCoreClient.presence()
        .startHeartbeat()
        .then((_) {
          // heartbeat started, user is marked as online
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function presenceHeartbeatStart() {
  const { PresenceRepository } = Amity;
  await PresenceRepository.startHeartbeat({ interval: 30000 });
  console.log('Presence heartbeat started');
}
```

</CodeGroup>
