{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: r-t-e-comment-subscription | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun subscribeComment(comment: AmityComment) {
        // ex. subscribing comment topic
        comment.subscription(events = AmityCommentEvents.COMMENT)
            .subscribeTopic()
            .subscribe()
    }
```

```swift iOS
let commentRepository = AmityCommentRepository(client: AmityUIKitManagerInternal.shared.client)
        let object = commentRepository.getComment(withCommentId: "commentId")
        let token = object.observe { liveObject, error in
            guard error == nil, let comment = liveObject.snapshot else { return }
            print("Comment updated: \(comment.commentId)")
        }
        AmityUIKitManagerInternal.shared.client.subscribeTopic(AmityTopicCreator.commentTopic(forCommentId: "commentId")) { error in
            print(error == nil ? "Subscribed to comment topic" : "Subscribe failed: \(error!.localizedDescription)")
        }
```

```dart Flutter
StreamSubscription? _commentSubscription;

    void subscribeToCommentEvents(AmityComment comment) {
      _commentSubscription = comment
          .subscription(AmityCommentEvent.COMMENT)
          .listen((_) {
        // Handle real-time comment events
      });
    }

    void unsubscribeFromCommentEvents() {
      _commentSubscription?.cancel();
      _commentSubscription = null;
    }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function rTECommentSubscription() {
  const { subscribeTopic } = CommentRepository;
  const unsubscribe = subscribeTopic(
    CommentTopic.comment('postId123'),
    () => {
      console.log('Comment realtime event received');
    },
  );
  return unsubscribe;
}
```

</CodeGroup>
