{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: event-observe-community-member | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun eventObserveCommunityMember(community: AmityCommunity) {
        community.subscription(events = AmityCommunityEvents.MEMBERS)
            .subscribeTopic()
            .subscribe()
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository()
        let collection = communityRepository.getMembers(communityId: "communityId", filter: .member, roles: [])
        collection.observe { liveCollection, changes, error in
            guard error == nil else { return }
            print("Community members updated: \(liveCollection.count())")
        }
```

```dart Flutter
void eventObserveCommunityMember() {
    final communityRepo = AmitySocialClient.newCommunityRepository();
    communityRepo
        .community('communityId')
        .subscription(AmityCommunityMemberEvent())
        .subscribeTopic()
        .onError((error, _) => print('Subscription error: \$error'));
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

function observeCommunityMemberEvents() {
  const unsubscribeMemberAdded = CommunityRepository.Membership.onMemberAdded((member) => {
    console.log('Member added to community:', member.userId)
  })

  const unsubscribeMemberRemoved = CommunityRepository.Membership.onMemberRemoved((member) => {
    console.log('Member removed from community:', member.userId)
  })

  return () => {
    unsubscribeMemberAdded()
    unsubscribeMemberRemoved()
  }
}
```

</CodeGroup>
