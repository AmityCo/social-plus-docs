{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: presence-settings-enable | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun enablePresence() {
        AmityCoreClient.presence()
            .enable()
            .doOnComplete {
                //  Enabling success
            }
            .doOnError {
                //  Handle Exception
            }
            .subscribe()
    }
```

```swift iOS
let client = AmityClient.shared
        let presenceRepository = AmityPresenceRepository(client: client)
        presenceRepository.enablePresence { success, error in
            if let error = error { print("Error: \(error)"); return }
            print("Presence enabled: \(success)")
        }
```

```dart Flutter
void enablePresence() {
    AmityCoreClient.presence()
        .enable()
        .then((_) {
          // presence enabled
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function presenceSettingsEnable() {
  const { PresenceRepository } = Amity;
  await PresenceRepository.updateSettings({ enabled: true });
  console.log('Presence settings enabled');
}
```

</CodeGroup>
