{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message_unreact | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun messageUnreact() {
        AmityChatClient.newMessageRepository()
            .getMessage("messageId")
            .flatMap { message ->
                message.react()
                    .removeReaction("like")
            }
            .subscribe({
                // Reaction removed
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
func messageUnreact() {
        let reactionRepository = AmityReactionRepository(client: AmityManager.shared.client!)
        reactionRepository.removeReaction(
            "heart",
            referenceId: "messageId123",
            referenceType: .message
        ) { success, error in
            if success {
                print("Reaction removed")
            }
        }
    }
```

</CodeGroup>
