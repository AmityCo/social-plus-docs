{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: ad-get-my-ads | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun adGetMyAds() {
        AmitySocialClient.newAdRepository()
            .getMyAds()
            .subscribe { ads ->
                ads.forEach { ad -> ad.getAdId() }
            }
    }
```

```swift iOS
let adRepository = AmityAdRepository(client: AmityManager.shared.client!)
        let liveCollection = adRepository.getMyAds()
        liveCollection.observe { collection, _, error in
            guard error == nil else { return }
            print("My ads count: \(collection.count())")
            for i in 0..<collection.count() {
                if let ad = collection.object(at: i) { print("My ad: \(ad.adId)") }
            }
        }
```

```dart Flutter
void adGetMyAds() {
    AmityAdRepository()
        .getMyAds()
        .query();
  }
```

```typescript TypeScript
import { AdRepository } from '@amityco/ts-sdk'

function getMyAds() {
  const liveCollection = AdRepository.getMyAds({
    sortBy: 'lastCreated',
  })

  const unsub = liveCollection.on('dataUpdated', (ads) => {
    console.log('My ads:', ads)
  })

  return unsub
}
```

</CodeGroup>
