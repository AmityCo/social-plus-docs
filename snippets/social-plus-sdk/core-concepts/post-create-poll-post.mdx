{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-create-poll-post | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createPollPost(targetCommunityId: String, poll: AmityPoll, text: String = "") {
        val target = AmityPost.Target.COMMUNITY.create(communityId = targetCommunityId)
        AmitySocialClient.newPostRepository()
            .createPollPost(target = target, poll = poll, text = text)
            .doOnSuccess { post: AmityPost ->
                // AmityPost
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let pollRepository = AmityPollRepository(client: client)
        let postRepository = AmityPostRepository(client: client)
        do {
            let pollBuilder = AmityPollBuilder()
            pollBuilder.setQuestion("What is your favorite color?")
            pollBuilder.setAnswers(["Red", "Blue", "Green"])
            pollBuilder.setTimeToClosePoll(86400)
            let poll = try await pollRepository.createPoll(pollBuilder)
            let postBuilder = AmityTextPostBuilder()
            postBuilder.setText("Vote on my poll!")
            let post = try await postRepository.createTextPost(postBuilder, targetId: "<community-id>", targetType: .community, metadata: ["pollId": poll.pollId], mentionees: nil)
            print("Created poll post: \(post.postId)")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void createPollPost(String targetId, AmityPostTargetType targetType) async {
    // Step 1: Create the poll
    final poll = await AmitySocialClient.newPollRepository()
        .createPoll(
          question: 'What is your favourite color?',
          answers: ['Red', 'Green', 'Blue'],
          answerType: AmityPollAnswerType.SINGLE,
          closedIn: 604800000, // 7 days in milliseconds
        );

    // Step 2: Create the post with poll
    AmitySocialClient.newPostRepository()
        .createPost()
        .targetType(targetType)
        .targetId(targetId)
        .poll(poll.pollId!)
        .post()
        .then((AmityPost post) => {
              //handle result
            })
        .onError((error, stackTrace) => {
              //handle error
            });
  }
```

```typescript TypeScript
import { PostRepository, PollRepository } from '@amityco/ts-sdk'

async function createPollPost() {
  const { data: poll } = await PollRepository.createPoll({
    question: 'Best programming language?',
    answers: [{ dataType: 'text', data: 'TypeScript' }, { dataType: 'text', data: 'Python' }],
    answerType: 'single',
  })
  const { data: post } = await PostRepository.createPost({
    targetType: 'community',
    targetId: 'communityId',
    dataType: 'poll',
    data: { pollId: poll.pollId },
  })
  console.log(post)
}
```

</CodeGroup>
