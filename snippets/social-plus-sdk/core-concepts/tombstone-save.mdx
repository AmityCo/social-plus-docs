{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: tombstone-save | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun saveTombstone(
        objectId: String,
        tombstoneModelType: TombstoneModelType,
        amityError: AmityError
    ) {
        TombstoneRepository()
            .saveTombstone(
                objectId = objectId,
                tombstoneModelType = tombstoneModelType,
                amityError = amityError
            )
            .doOnComplete { /* tombstone saved */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let contentId = "contentId"
        AmitySDK.saveTombstone(contentId: contentId) { success, error in
            if let error = error {
                print("Failed to save tombstone: \(error)")
                return
            }
            print("Tombstone saved successfully")
        }
```

```dart Flutter
void tombstoneSave() {
    AmitySocialClient.newAdRepository()
        .saveTombstone(
          tombstoneId: 'tombstoneId',
          referenceId: 'referenceId',
          referenceType: 'post',
        )
        .then((_) {
      // Tombstone saved
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function tombstoneSave() {
  const adRepository = new AdRepository();
  await adRepository.saveTombstone('adId', { seen: true });
  console.log('Ad tombstone saved');
}
```

</CodeGroup>
