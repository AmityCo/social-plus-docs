{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: live_collection_with_table_view | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun liveCollectionWithTableView() {
        val liveCollection = AmitySocialClient.newPostRepository()
            .getPostCollection()
            .build()
            .query()
        liveCollection.observe(androidx.lifecycle.ProcessLifecycleOwner.get()) { pagedList ->
                // Handle live collection updates in RecyclerView
            }
    }
```

```swift iOS
func liveCollectionWithTableView() {
        let communityRepository = AmityCommunityRepository(client: AmityManager.shared.client!)
        let query = AmityCommunityQuery()
        let collection = communityRepository.getCommunities(with: query)
        collection.observe { [weak self] collection, change, error in
            guard error == nil else { return }
            self?.tableView.reloadData()
        }
    }
```

</CodeGroup>
