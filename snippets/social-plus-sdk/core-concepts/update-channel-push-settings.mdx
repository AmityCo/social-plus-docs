{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: update-channel-push-settings | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun updateChannelNotificationSettings(channel: AmityChannel) {
        // To enable notification on this channel
        AmityCoreClient.notifications()
            .channel(channelId = channel.getChannelId())
            .enable()
            .doOnComplete {
                // Complete
            }
            .doOnError {
                // Exception
            }
            .subscribe()

        // To disable notification on this chanel
        AmityCoreClient.notifications()
            .channel(channelId = channel.getChannelId())
            .disable()
            .doOnComplete {
                // Complete
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let channelId = "channelId"
        AmitySDK.updateChannelPushSettings(channelId: channelId, enabled: true) { success, error in
            if let error = error {
                print("Failed to update push settings: \(error)")
                return
            }
            print("Channel push settings updated")
        }
```

```dart Flutter
void updateChannelPushSettings() {
    AmityChatClient.newChannelRepository()
        .channel('channelId')
        .notification
        .enable()
        .then((_) => print('Push notifications enabled'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function updateChannelPushSettings() {
  await NotificationRepository.updateChannelPushSettings({
    channelId: 'channelId',
    isPushEnabled: true,
  })
  console.log('Channel push settings updated')
}
```

</CodeGroup>
