{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: presence-heartbeat-stop | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun stopHeartbeat() {
        AmityCoreClient.presence()
            .stopHeartbeat()
    }
```

```swift iOS
let client = AmityClient.shared
        let presenceRepository = AmityPresenceRepository(client: client)
        presenceRepository.stopHeartbeat()
        print("Presence heartbeat stopped")
```

```dart Flutter
void stopPresenceHeartbeat() {
    AmityCoreClient.presence().stopHeartbeat();
    // heartbeat stopped, user presence will time out
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function presenceHeartbeatStop() {
  const { PresenceRepository } = Amity;
  await PresenceRepository.stopHeartbeat();
  console.log('Presence heartbeat stopped');
}
```

</CodeGroup>
