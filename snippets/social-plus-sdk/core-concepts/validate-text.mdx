{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: validate-text | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun validateText(
        client: AmityCoreClient,
        text: String
    ) {
        client.validateTexts(listOf(text))
            .doOnComplete {
                // Passes validation
            }
            .doOnError {
                // Fails validation
            }
            .subscribe()
    }
```

```swift iOS
let client = AmityClient.shared
        let contentValidator = AmityContentValidator(client: client)
        contentValidator.validateText("Sample text content") { isValid, error in
            print("Text validation result: \(isValid)")
        }
```

```dart Flutter
void validateText() {
    AmityCoreClient.newContentModerationRepository()
        .validateText(text: 'Sample text to validate')
        .then((result) => print('Text valid: \${result.isValid}'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function validateText() {
  const text = 'Sample text to validate';
  const result = await AdRepository.validateText({ text });
  console.log('Text validation result:', result);
}
```

</CodeGroup>
