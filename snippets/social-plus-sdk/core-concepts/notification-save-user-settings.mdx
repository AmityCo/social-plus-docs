{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: notification-save-user-settings | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun notificationSaveUserSettings() {
        AmitySocialClient.notifications()
            .setUserSettings(notificationSettings)
            .subscribe({
                // User notification settings saved
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
func notificationSaveUserSettings() {
        let notificationManager = AmityNotificationManager()
        let settings = AmityUserNotificationSettings()
        notificationManager.saveUserNotificationSettings(settings) { success, error in
            if let error = error {
                print("Failed to save user notification settings: \(error.localizedDescription)")
                return
            }
            print("User notification settings saved")
        }
    }
```

```dart Flutter
Future<void> saveUserNotificationSettings(AmityUserNotificationSettings settings) async {
    try {
      await AmityCoreClient.saveUserNotificationSettings(settings);
      // settings saved successfully
    } catch (error) {
      // handle error
    }
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function notificationSaveUserSettings() {
  await NotificationRepository.setUserNotificationSettings({
    isPushNotificationEnabled: true,
    modules: [
      { moduleType: 'post', isEnabled: true },
      { moduleType: 'comment', isEnabled: true },
    ],
  })
  console.log('User notification settings saved')
}
```

</CodeGroup>
