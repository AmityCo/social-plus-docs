{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: subscribe_and_observe_changes | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun subscribeAndObserveChanges() {
        val topic = AmityTopicBuilder.topic(AmityPost::class.java)
            .forPost("postId")
            .build()
        AmityCoreClient.subscribeTopic(topic)
            .flatMap {
                AmitySocialClient.newPostRepository().getPost("postId")
            }
            .subscribe({ post ->
                // Observe and handle real-time post changes
            }, { error ->
                // Handle error
            })
    }
```

```dart Flutter
void subscribeAndObserveChanges() {
    // Subscribe to a topic and observe incoming data changes
    final topic = TopicBuilder.user(userId: 'userId').post().build();
    AmityCoreClient.subscribeTopic(topic).then((_) {
      // Now observe the live object/collection for changes
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function subscribeAndObserveChanges() {
  const { subscribeTopic } = await import('@amityco/ts-sdk');
  const topic = getPostTopic('postId');
  const unsubscribe = subscribeTopic(topic, (event) => {
    console.log('Change event:', event);
  });
  return unsubscribe;
}
```

</CodeGroup>
