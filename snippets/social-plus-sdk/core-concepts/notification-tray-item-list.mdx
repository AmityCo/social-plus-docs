{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: notification-tray-item-list | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun notificationTrayItemList() {
        // Configure notification settings
        AmityCoreClient.newNotificationRepository()
            .getNotifications()
            .build()
            .query()
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(io.reactivex.rxjava3.android.schedulers.AndroidSchedulers.mainThread())
            .subscribe { pagingData ->
                // bind notification list to RecyclerView
            }
    }
```

```swift iOS
let notificationTrayRepository = AmityNotificationTrayRepository(client: AmityClient.shared)
        let liveCollection = notificationTrayRepository.getNotificationTrayItems()
        liveCollection.observe { collection, _, error in
            if let error = error {
                print("Failed to get notification tray items: \(error.localizedDescription)")
                return
            }
            let items = collection.allObjects()
            for item in items {
                print("Notification item: \(item.notificationId), isRead: \(item.isRead)")
            }
        }
```

```dart Flutter
void notificationTrayItemList() {
    AmityPushNotification.getTrayNotifications()
        .query();
  }
```

```typescript TypeScript
import { Client } from '@amityco/ts-sdk'

function notificationTrayItemList() {
  const disposer = Client.getNotificationTray().getItems(
    { limit: 20 },
    ({ data: items, loading, error }) => {
      if (loading) return
      if (error) {
        console.error('Failed to fetch notification tray items', error)
        return
      }
      console.log('Notification tray items:', items)
    },
  )

  return disposer
}
```

</CodeGroup>
