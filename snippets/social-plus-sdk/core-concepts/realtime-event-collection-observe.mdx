{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: realtime-event-collection-observe | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun realtimeEventCollectionObserve() {
        // perform realtime event collection observe operation
        AmityChatClient.newChannelRepository()
            .getChannel("channelId")
            .asFlowable()
            .subscribe { channel ->
                // handle real-time updates
            }
    }
```

```swift iOS
let postRepository = AmityPostRepository(client: AmityUIKitManagerInternal.shared.client)
        let collection = postRepository.getPosts(sortBy: .lastCreated, includeDeleted: false, feedType: .published, targetId: nil, targetType: .community)
        let token = collection.observe { liveCollection, change, error in
            guard error == nil else { return }
            print("Collection updated, dataStatus: \(liveCollection.dataStatus)")
        }
        // Retain token to keep observing; invalidate to stop:
        // token.invalidate()
```

```dart Flutter
void observeCollectionRealtimeEvents(String communityId) {
      final LiveCollectionStream<AmityPost> collections =
          AmitySocialClient.newPostRepository()
              .getPosts()
              .targetCommunity(communityId)
              .getLiveCollection(limit: 20);

      collections.getStream().listen((event) {
        final List<AmityPost> posts = event.data;
        // React to real-time collection updates
      });
    }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

function observeLiveCollection() {
  const liveCollection = ChannelRepository.queryChannels({
    types: ['community'],
    membership: 'all',
  })

  liveCollection.on('dataUpdated', (channels) => {
    console.log('Channels updated:', channels)
  })

  liveCollection.on('dataError', (error) => {
    console.error('Collection error:', error)
  })

  return () => liveCollection.dispose()
}
```

</CodeGroup>
