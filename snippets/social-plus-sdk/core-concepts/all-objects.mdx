{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: all-objects | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void allObjects() {
    final liveCollection = AmitySocialClient.newPostRepository()
        .getPosts()
        .targetMe()
        .build()
        .query();
    liveCollection.stream.listen((posts) {
      // Handle all posts
    });
  }
```

```typescript TypeScript
import { Client } from '@amityco/ts-sdk'

async function allObjects() {
  const unsubscribe = PostRepository.getPosts(
    { targetType: 'community', targetId: 'community123' },
    (posts, error) => {
      if (error) return console.error(error)
      console.log('Total posts:', posts?.length)
    }
  )
  return unsubscribe
}
```

</CodeGroup>
