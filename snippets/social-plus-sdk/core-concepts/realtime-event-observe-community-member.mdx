{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: realtime-event-observe-community-member | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun realtimeEventObserveCommunityMember() {
        // perform realtime event observe community member operation
        AmitySocialClient.newCommunityRepository()
            .membership("communityId")
            .removeMembers(listOf("userId1", "userId2"))
            .subscribe()
    }
```

```swift iOS
let communityRepository = AmityCommunityRepository(client: AmityUIKitManagerInternal.shared.client)
        let collection = communityRepository.getMembers(
            communityId: "communityId",
            filter: .all,
            sortBy: .displayName,
            roles: []
        )
        let token = collection.observe { liveCollection, change, error in
            guard error == nil else { return }
            print("Members updated, count: \(liveCollection.snapshots.count)")
        }
```

```dart Flutter
StreamSubscription? _subscription;
  late CommunityMemberLiveCollection liveCollection;

  void observeCommunityMembers(String communityId) {
    liveCollection = AmitySocialClient.newCommunityRepository()
        .membership(communityId)
        .getMembers()
        .getLiveCollection();

    _subscription = liveCollection.getStreamController().stream.listen(
      (List<AmityCommunityMember> members) {
        // handle real-time member updates
      },
      onError: (error) {
        // handle error
      },
    );
    liveCollection.loadNext();
  }

  void dispose() {
    _subscription?.cancel();
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function realtimeEventObserveCommunityMember() {
  const unsub = runQuery(
    CommunityRepository.queryMembers({
      communityId: 'communityId123',
      membership: 'member',
    }),
    ({ data: members }) => {
      console.log('Community members updated:', members.map((m) => m.userId));
    },
  );
  return unsub;
}
```

</CodeGroup>
