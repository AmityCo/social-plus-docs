{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: mute_a_channel | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void muteAChannel() async {
    await AmityChatClient.newChannelRepository()
        .muteChannel(channelId: 'channelId', period: AmityMutePeriod.FOREVER);
    print('Channel muted');
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function muteAChannel(channelId: string, mutePeriodMs: number) {
  await ChannelRepository.muteChannel({
    channelId,
    mutePeriod: mutePeriodMs,
  })
  console.log('Channel muted:', channelId, 'for', mutePeriodMs, 'ms')
}
```

</CodeGroup>
