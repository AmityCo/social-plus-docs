{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get-user | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getUser(userId: String) {
        AmityCoreClient.newUserRepository()
            .getUser(userId = userId)
            .doOnNext { user: AmityUser ->
                val displayName = user.getDisplayName()
                val avatarUrl = user.getAvatarUrl()
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let userRepository = AmityUserRepository()
        let liveObject = userRepository.getUser("userId")
        liveObject.observe { user, error in
            if let user = user.snapshot {
                print("Display name: \(user.displayName ?? "")")
            }
        }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

function observeUser(userId: string) {
  const unsubscribe = UserRepository.getUser(userId, ({ data: user, loading, error }) => {
    if (loading) return
    if (error) {
      console.error('Error fetching user:', error)
      return
    }

    console.log('User ID:', user.userId)
    console.log('Display Name:', user.displayName)
    console.log('Description:', user.description)
    console.log('Avatar URL:', user.avatarUrl)
  })

  return unsubscribe
}
```

</CodeGroup>
