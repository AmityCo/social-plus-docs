{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: subscribe_follow_topic | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun subscribeFollowTopic() {
        val topic = AmityTopicBuilder.topic(AmityFollow::class.java)
            .forUser("userId")
            .build()
        AmityCoreClient.subscribeTopic(topic)
            .subscribe({
                // Subscribed to follow topic for the user
            }, { error ->
                // Handle error
            })
    }
```

```dart Flutter
void subscribeFollowTopic() {
    final topic = TopicBuilder.user(userId: 'userId').follow().build();
    AmityCoreClient.subscribeTopic(topic).then((_) {
      // Subscribed to follow/unfollow events
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function subscribeFollowTopic() {
  const { subscribeTopic } = await import('@amityco/ts-sdk');
  const topic = getFollowTopic('userId');
  const unsubscribe = subscribeTopic(topic, (event) => {
    console.log('Follow event:', event);
  });
  return unsubscribe;
}
```

</CodeGroup>
