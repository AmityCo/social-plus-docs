{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-unflag | updated: 2026-05-01 */}

<CodeGroup>

```kotlin Android
fun unflagUser(userId: String) {
        AmityCoreClient.newUserRepository()
            .unflagUser(userId = userId)
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let userRepository = AmityUserRepository(client: client)
        do {
            let success = try await userRepository.unflagUser(withId: "<user-id>")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void unflagUser(AmityUser user) {
    user.report()
        .unflag()
        .then((value) {
          // success
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function unflagUser() {
  await UserRepository.unflagUser('userId')
}
```

</CodeGroup>
