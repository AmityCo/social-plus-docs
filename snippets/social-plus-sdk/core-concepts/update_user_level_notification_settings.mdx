{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: update_user_level_notification_settings | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun updateUserLevelNotificationSettings() {
        // Update operation for update_user_level_notification_settings
        AmityCoreClient.newUserRepository()
            .updateUser()
            .displayName("New Display Name")
            .build()
            .update()
            .subscribe({ user -> }, { error -> })
    }
```

```dart Flutter
void updateUserLevelNotificationSettings() {
    final settings = AmityUserNotificationSettings(
      isEnabled: true,
      modules: [
        AmityUserNotificationModule.social(
          isEnabled: true,
          roleFilter: AmityRoleFilter.all(),
        ),
      ],
    );
    AmityCoreClient.newUserRepository()
        .notification
        .enable(settings)
        .then((_) => print('User notification settings updated'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function updateUserLevelNotificationSettings() {
  const { data: settings } = await NotificationRepository.updateUserNotificationSettings({
    isEnabled: true,
    networkSettings: [],
  })
  console.log('User notification settings updated:', settings)
}
```

</CodeGroup>
