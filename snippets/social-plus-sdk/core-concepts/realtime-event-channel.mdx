{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: realtime-event-channel | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void realtimeEventChannel() async {
    final channelRepository = AmityChatClient.newChannelRepository();
    final subscription = channelRepository
        .getChannel('channelId')
        .then((channel) {
      channel.subscription(AmityChannelEvents.POST)
          .subscribeTopic()
          .then((_) => print('Subscribed to channel events'));
    });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

async function realtimeEventChannel() {
  const liveChannel = ChannelRepository.getChannel('channelId123');

  liveChannel.on('dataUpdated', channel => {
    console.log('Channel updated:', channel.channelId, channel.displayName);
  });

  liveChannel.on('dataError', err => console.error('Channel realtime error:', err));

  // Subscribe to channel events
  const disposer = ChannelRepository.subscribeToChannel('channelId123', () => {
    console.log('Channel event received');
  });
}
```

</CodeGroup>
