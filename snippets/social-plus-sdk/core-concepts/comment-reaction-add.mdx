{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: comment-reaction-add | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun addReaction(comment: AmityComment, reactionName: String) {
        AmityCoreClient.newReactionRepository()
            .addReaction(
                reactionReference = AmityReactionReference.COMMENT(commentId = comment.getCommentId()),
                reactionName = reactionName
            )
            .doOnComplete { /* success */ }
            .doOnError { /* handle error */ }
            .subscribe()
    }
```

```swift iOS
let reactionRepository = AmityReactionRepository(client: client)
        do {
            let result = try await reactionRepository.addReaction("like", referenceId: "<comment-id>", referenceType: .comment)
        } catch {
            // Handle error here
        }
```

```dart Flutter
void addCommentReaction(AmityComment comment) {
    comment.react()
        .addReaction('like')
        .then((value) {
          // success
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { ReactionRepository } from '@amityco/ts-sdk'

async function addCommentReaction() {
  await ReactionRepository.addReaction('comment', 'commentId', 'like')
}
```

</CodeGroup>
