{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: get_messages_with_mention | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void getMessagesWithMention() async {
    AmityChatClient.newMessageRepository()
        .getMessages(subChannelId: 'subChannelId')
        .filterByMention(userId: AmityCoreClient.getUserId())
        .getPagingData(token: null, limit: 20)
        .asStream()
        .listen((result) {
      final messages = result.data;
    });
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

function getMessagesWithMention() {
  const subChannelId = 'subChannel123'
  const liveCollection = MessageRepository.getMessages({ subChannelId, limit: 20 })
  liveCollection.on('dataUpdated', (messages) => {
    const mentionMessages = messages.filter((msg) => msg.mentionees && msg.mentionees.length > 0)
    console.log('Messages with mentions:', mentionMessages)
  })
}
```

</CodeGroup>
