{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: add-reaction | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void addReactionToPost(AmityPost post) {
    post.react()
        .addReaction('like')
        .then((value) {
          // reaction added
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { ReactionRepository } from '@amityco/ts-sdk'

async function addReactionToPost(postId: string, reactionName: string) {
  await ReactionRepository.addReaction('post', postId, reactionName)
  console.log(`Reaction '${reactionName}' added to post ${postId}`)
}

// Example usage
addReactionToPost('postId', 'like')
```

</CodeGroup>
