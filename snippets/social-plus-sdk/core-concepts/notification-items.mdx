{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: notification-items | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun notificationItems() {
        // Configure notification settings
        AmityCoreClient.newNotificationRepository()
            .getNotifications()
            .build()
            .query()
            .subscribe { pagingData ->
                // handle notification items
            }
    }
```

```swift iOS
let notificationTrayRepository = AmityNotificationTrayRepository(client: AmityClient.shared)
        let liveCollection = notificationTrayRepository.getNotificationTrayItems()
        liveCollection.observe { collection, _, error in
            if let error = error {
                print("Failed to observe notification items: \(error.localizedDescription)")
                return
            }
            let items = collection.allObjects()
            print("Notification items count: \(items.count)")
        }
```

```dart Flutter
void notificationItems() {
    final controller = PagingController<AmityNotification>(pageSize: 20);
    controller.addPageRequestListener((pageKey) {
      AmityCoreClient.newNotificationRepository()
          .getNotifications()
          .getPagingData(token: pageKey, limit: 20)
          .then((data) => controller.appendPage(data.data, data.token))
          .onError((error, _) => controller.error = error);
    });
  }
```

```typescript TypeScript
import { notificationTray } from '@amityco/ts-sdk'

function getNotificationItems() {
  const unsubscribe = notificationTray.getNotificationTrayItems(
    (response) => {
      if (response.error) throw response.error
      if (!response.loading) {
        console.log('Notification items:', response.data)
      }
    },
  )
  return unsubscribe
}
```

</CodeGroup>
