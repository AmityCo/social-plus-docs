{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: mention-highlight | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun mentionHighlight() {
        val postId = "postId"
        AmitySocialClient.newPostRepository()
            .getPost(postId)
            .subscribe { post ->
                val mentionees = post.getMentionees()
                val metadata = post.getMetadata()
            }
    }
```

```swift iOS
// Parse mention metadata to highlight mentions in UI
        let text = "Hello @john and @jane!"
        let metadata: [String: Any] = [
            "mentioned": [
                ["type": "user", "userId": "johnUserId", "length": 5, "index": 6],
                ["type": "user", "userId": "janeUserId", "length": 5, "index": 16]
            ]
        ]
        if let mentionedArray = metadata["mentioned"] as? [[String: Any]] {
            let attributedString = NSMutableAttributedString(string: text)
            for mention in mentionedArray {
                if let index = mention["index"] as? Int,
                   let length = mention["length"] as? Int {
                    let range = NSRange(location: index, length: length)
                    attributedString.addAttribute(.foregroundColor,
                        value: UIColor.systemBlue, range: range)
                }
            }
            print("Highlighted text: \(attributedString.string)")
        }
```

```dart Flutter
void mentionHighlight() {
    final text = 'Hello @user1, check this out!';
    final mentionees = <AmityMentionee>[];
    final metadata = AmityMentionMetadataCreator(mentionees).create();
    // Apply highlight to mentioned users in the text widget
    print('Mention metadata: \$metadata');
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function mentionHighlight() {
  // Parse and render mention annotations from message metadata
  function renderMentions(text: string, mentionees: Amity.Mentionee[]) {
    let rendered = text
    mentionees.forEach(({ userId, displayName }) => {
      rendered = rendered.replace(`@${userId}`, `<strong>@${displayName}</strong>`)
    })
    return rendered
  }
  
  const unsubscribe = MessageRepository.getMessage('messageId', ({ data: message }) => {
    if (message) {
      const html = renderMentions(message.data.text, message.mentionees ?? [])
      console.log('Rendered:', html)
    }
  })
  
  return unsubscribe
}
```

</CodeGroup>
