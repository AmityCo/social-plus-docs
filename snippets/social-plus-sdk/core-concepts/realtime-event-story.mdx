{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: realtime-event-story | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun realtimeEventStory() {
        // TODO: implement realtime event story using Social Plus SDK
    }
```

```swift iOS
func realtimeEventStory() {
        let communityId = "communityId"
        let storyRepository = AmityStoryRepository(client: AmityManager.shared.client!)
        let liveCollection = storyRepository.getStories(communityId: communityId, options: .init())
        
        let token = liveCollection.observe { collection, changes, error in
            guard error == nil else { return }
            print("Stories updated, count: \(collection.count())")
        }
        // Retain token as long as observation is needed
    }
```

```dart Flutter
void observeStoryRealtimeEvents(String communityId) {
      AmitySocialClient.newStoryRepository()
          .getActiveStoriesByTarget(
            targetId: communityId,
            targetType: AmityStoryTargetType.community,
          )
          .listen((List<AmityStory> stories) {
        // React to real-time story events
      }).onError((error, stackTrace) {
        // Handle error
      });
    }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function realtimeEventStory() {
  const unsubscribe = subscribeTopic(
    StoryTopic.story('communityId123'),
    () => {
      console.log('Story realtime event received');
    },
  );
  return unsubscribe;
}
```

</CodeGroup>
