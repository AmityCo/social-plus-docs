{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: manage_community_notification_settings | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun manageCommunityNotificationSettings() {
        // Manage push notification settings for a community
        AmitySocialClient.newCommunityRepository()
            .notification("communityId")
            .enable()
            .subscribeOn(io.reactivex.rxjava3.schedulers.Schedulers.io())
            .observeOn(io.reactivex.rxjava3.android.schedulers.AndroidSchedulers.mainThread())
            .subscribe({
                // Community notifications enabled
            }, { error ->
                // Handle error
            })
    }
```

```dart Flutter
void manageCommunityNotificationSettings() async {
    final notifRepo = AmitySocialClient.newCommunityRepository();
    await notifRepo.notification('communityId123').enable(
      events: [
        AmityCommunityNotificationEvent.POST_CREATED,
        AmityCommunityNotificationEvent.COMMENT_CREATED,
      ],
    );
    print('Community notification settings updated.');
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function manageCommunityNotificationSettings() {
  await NotificationRepository.setCommunityNotificationSettings({
    communityId: 'communityId',
    isPushNotificationEnabled: true,
    modules: [
      { moduleType: 'post', isEnabled: true, roleFilter: { roles: ['community-moderator'], type: 'only' } },
    ],
  })
  console.log('Community notification settings updated')
}
```

</CodeGroup>
