{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: search-users | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun searchUsers() {
        AmityCoreClient.newUserRepository()
            .searchUsers(keyword = "keyword")
            .build()
            .query()
            .subscribe()
    }
```

```swift iOS
let userRepository = AmityUserRepository()
        let liveCollection = userRepository.getUsers(keyword: "searchKeyword", sortBy: .displayName)
        liveCollection.observe { collection, _, error in
            collection.snapshots.forEach { user in
                print("User: \(user.displayName ?? "")")
            }
        }
```

```dart Flutter
void searchUsers() async {
    final userRepository = AmitySocialClient.newUserRepository();
    userRepository
        .searchUser('displayName')
        .sortBy(AmityUserSortOption.DISPLAY_NAME)
        .asStream()
        .listen((users) {
      print('Users found: \${users.length}');
    });
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function searchUsers() {
  const params = {
    keyword: 'john',
    sortBy: 'displayName' as const,
    filter: 'all' as const,
  };

  const liveCollection = UserRepository.getUsers(params);
  liveCollection.on('dataUpdated', users => {
    console.log('User search results:', users.map(u => u.displayName));
  });
  liveCollection.on('dataError', err => console.error('Error searching users:', err));
}
```

</CodeGroup>
