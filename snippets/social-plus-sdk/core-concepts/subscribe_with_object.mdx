{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: subscribe_with_object | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun subscribeWithObject() {
        val post = AmityPost() // obtained from repository
        AmityCoreClient.subscribeTopic(AmityTopicBuilder.fromObject(post))
            .subscribe({
                // Subscribed using the object itself
            }, { error ->
                // Handle error
            })
    }
```

```dart Flutter
void subscribeWithObject() {
    // Subscribe using an Amity object directly
    AmityCoreClient.subscribeTopic(
      TopicBuilder.community(communityId: 'communityId').post().build(),
    ).then((_) {
      // Subscribed via object reference
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function subscribeWithObject() {
  const { subscribeTopic } = await import('@amityco/ts-sdk');
  const topic = getPostTopic('postId');
  const unsubscribe = subscribeTopic(topic, (event) => {
    console.log('Object event:', event);
  });
  return unsubscribe;
}
```

</CodeGroup>
