{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: update-user-information | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun updateUserInformation() {
        // TODO: Implement Update user information
    }
```

```swift iOS
let userRepository = AmityUserRepository(client: AmityManager.shared.client!)
        let builder = AmityUserUpdateBuilder()
        builder.setDisplayName("New Display Name")
        builder.setUserDescription("Updated bio")
        userRepository.updateUser(builder) { user, error in
            if let user = user {
                print("User updated: \(user.displayName ?? "")")
            } else if let error = error {
                print("Error updating user: \(error)")
            }
        }
```

```dart Flutter
void updateUserInformation() async {
    final userRepo = AmitySocialClient.newUserRepository();
    await userRepo
        .updateUser('userId')
        .displayName('New Display Name')
        .description('Updated bio')
        .update();
    print('User information updated');
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function updateUserInformation(userId: string, displayName: string, description: string) {
  const updated = await UserRepository.updateUser(userId, {
    displayName,
    description,
  })
  console.log('User updated:', updated.data.userId)
}
```

</CodeGroup>
