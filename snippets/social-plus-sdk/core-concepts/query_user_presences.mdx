{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: query_user_presences | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun queryUserPresences() {
        // perform query user presences operation
        AmityCoreClient.newUserRepository()
            .searchUser("keyword")
            .build()
            .query()
            .subscribe { users ->
                // handle users collection
            }
    }
```

```dart Flutter
void queryUserPresences(List<String> userIds) {
      AmityCoreClient.newUserRepository()
          .getUserPresences(userIds: userIds)
          .listen((List<AmityUserPresence> presences) {
        for (final presence in presences) {
          final userId = presence.userId;
          final isOnline = presence.isOnline;
          // Update UI with each user's presence
        }
      }).onError((error, stackTrace) {
        // Handle error
      });
    }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function queryUserPresences() {
  const unsub = runQuery(
    UserRepository.queryUserPresences({ userIds: ['userId1', 'userId2', 'userId3'] }),
    ({ data: presences }) => {
      presences.forEach((p) => {
        console.log(`User ${p.userId}: ${p.isOnline ? 'online' : 'offline'}`);
      });
    },
  );
  return unsub;
}
```

</CodeGroup>
