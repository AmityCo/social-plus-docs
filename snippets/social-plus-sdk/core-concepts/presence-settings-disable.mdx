{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: presence-settings-disable | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun disablePresence() {
        AmityCoreClient.presence()
            .disable()
            .doOnComplete {
                //  Disabling success
            }
            .doOnError {
                //  Handle Exception
            }
            .subscribe()
    }
```

```swift iOS
let client = AmityClient.shared
        let presenceRepository = AmityPresenceRepository(client: client)
        presenceRepository.disablePresence { success, error in
            if let error = error { print("Error: \(error)"); return }
            print("Presence disabled: \(success)")
        }
```

```dart Flutter
void disablePresence() {
    AmityCoreClient.presence()
        .disable()
        .then((_) {
          // presence disabled, user will appear offline
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function presenceSettingsDisable() {
  const { PresenceRepository } = Amity;
  await PresenceRepository.updateSettings({ enabled: false });
  console.log('Presence settings disabled');
}
```

</CodeGroup>
