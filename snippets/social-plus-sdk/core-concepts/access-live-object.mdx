{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: access-live-object | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun accessLiveObject(communityId: String) {
        AmitySocialClient.newCommunityRepository()
            .getCommunity(communityId = communityId)
            .subscribeOn(Schedulers.io())
            .observeOn(AndroidSchedulers.mainThread())
            .doOnSuccess { community: AmityCommunity ->
                // Live object delivers real-time updates
            }
            .doOnError { }
            .subscribe()
    }
```

```swift iOS
let liveObject = communityRepository.getCommunity(withId: "communityId")
        liveObject.observe { liveObject, error in
            if let error = error {
                print("Error: \(error)")
                return
            }
            guard let community = liveObject.object else { return }
            print("Community updated: \(community.displayName ?? "")")
        }
```

```dart Flutter
void accessLiveObject() {
    final liveObject = AmitySocialClient.newCommunityRepository()
        .getCommunity('communityId');
    liveObject.stream.listen((community) {
      // Handle live updates
    });
  }
```

```typescript TypeScript
import { Client } from '@amityco/ts-sdk'

async function accessLiveObject() {
  const unsubscribe = UserRepository.getUser('user123', (user, error) => {
    if (error) return console.error(error)
    // This callback fires on every update (live object)
    console.log('Live user data:', user?.displayName)
  })
  // Unsubscribe when done
  return unsubscribe
}
```

</CodeGroup>
