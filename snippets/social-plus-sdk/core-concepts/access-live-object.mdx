{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: access-live-object | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun accessLiveObject(communityId: String) {
        AmitySocialClient.newCommunityRepository()
            .getCommunity(communityId = communityId)
            .subscribeOn(Schedulers.io())
            .observeOn(AndroidSchedulers.mainThread())
            .doOnSuccess { community: AmityCommunity ->
                // Live object delivers real-time updates
            }
            .doOnError { }
            .subscribe()
    }
```

```swift iOS
let liveObject = communityRepository.getCommunity(withId: "communityId")
        liveObject.observe { liveObject, error in
            if let error = error {
                print("Error: \(error)")
                return
            }
            guard let community = liveObject.object else { return }
            print("Community updated: \(community.displayName ?? "")")
        }
```

```dart Flutter
void observeChannel(String channelId) {
    AmityChatClient.newChannelRepository()
        .getChannel(channelId)
        .asStream()
        .listen((AmityChannel channel) {
          // handle live channel updates
        });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

function observeChannel(channelId: string) {
  const unsubscribe = ChannelRepository.getChannel(channelId, ({ data: channel, loading, error }) => {
    if (loading) return
    if (error) {
      console.error('Error observing channel:', error)
      return
    }
    console.log('Channel updated:', channel.displayName)
  })

  // Call unsubscribe() when done to stop receiving updates
  return unsubscribe
}
```

</CodeGroup>
