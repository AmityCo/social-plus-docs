{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post_react | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void postReact() async {
    final postRepo = AmitySocialClient.newPostRepository();
    final post = await postRepo.getPost('postId').first;
    await post.react().addReaction('like');
    print('Reaction added to post');
  }
```

```typescript TypeScript
import { ReactionRepository } from '@amityco/ts-sdk'

async function postReact(postId: string, reactionName: string) {
  await ReactionRepository.addReaction('post', postId, reactionName)
  console.log('Reaction added to post')
}
```

</CodeGroup>
