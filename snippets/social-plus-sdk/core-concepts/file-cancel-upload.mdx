{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: file-cancel-upload | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun cancelUpload(uploadId: String) {
        AmityCoreClient.newFileRepository()
            .cancelUpload(uploadId = uploadId)
    }
```

```swift iOS
let fileRepository = AmityFileRepository()
        // Start an upload task and retain the task to cancel it
        let imageData = Data()
        let uploadTask = fileRepository.uploadImage(imageData, progress: { progress in
            print("Upload progress: \(progress)")
        }) { imageData, error in
            print("Upload completed or cancelled")
        }
        // Cancel the upload
        uploadTask?.cancel()
        print("Upload cancelled")
```

```dart Flutter
void cancelUpload(String uploadId) {
    AmityCoreClient.newFileRepository().cancelUpload(uploadId);
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function fileCancelUpload() {
  const uploadTask = FileRepository.uploadFile(new File(['content'], 'test.txt'));
  uploadTask.cancel();
  console.log('File upload cancelled');
}
```

</CodeGroup>
