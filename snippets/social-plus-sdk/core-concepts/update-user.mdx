{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: update-user | updated: 2026-05-02 */}

<CodeGroup>

```swift iOS
let userRepository = AmityUserRepository()
        let builder = AmityUserUpdateBuilder()
        builder.setDisplayName("New Display Name")
        builder.setDescription("Updated bio")
        userRepository.updateCurrentUser(builder: builder) { user, error in
            if let error = error {
                print("Error updating user: \(error)")
            } else {
                print("User updated: \(user?.displayName ?? "")")
            }
        }
```

```dart Flutter
void updateUser() {
    AmityCoreClient.newUserRepository()
        .updateUser(AmityCoreClient.getUserId())
        .displayName('New Display Name')
        .description('Updated profile description')
        .update()
        .then((AmityUser user) {
          print('User updated: \${user.userId}');
        })
        .onError((error, stackTrace) {
          print('Error: \$error');
        });
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function updateUser() {
  const updatedUser = await UserRepository.updateUser('userId', {
    displayName: 'Updated Display Name',
    description: 'Updated user bio',
    avatarFileId: 'avatar-file-id',
  });
  console.log('User updated:', updatedUser);
}
```

</CodeGroup>
