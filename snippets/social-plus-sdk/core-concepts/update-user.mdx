{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: update-user | updated: 2026-05-02 */}

<CodeGroup>

```swift iOS
let userRepository = AmityUserRepository()
        let builder = AmityUserUpdateBuilder()
        builder.setDisplayName("New Display Name")
        builder.setDescription("Updated bio")
        userRepository.updateCurrentUser(builder: builder) { user, error in
            if let error = error {
                print("Error updating user: \(error)")
            } else {
                print("User updated: \(user?.displayName ?? "")")
            }
        }
```

```dart Flutter
Future<void> updateUser({
    required String displayName,
    String? description,
    String? avatarUrl,
  }) async {
    try {
      final user = await AmityCoreClient.newUserRepository()
          .updateUser()
          .displayName(displayName)
          .description(description ?? '')
          .build()
          .update();
      // user updated successfully
    } catch (error) {
      // handle error
    }
  }
```

```typescript TypeScript
import { UserRepository } from '@amityco/ts-sdk'

async function updateUserProfile(userId: string, displayName: string, description: string) {
  const { data: user } = await UserRepository.updateUser(userId, { displayName, description })
  console.log('User profile updated:', user.displayName)
  return user
}

// Example usage
updateUserProfile('userId', 'New Name', 'My updated bio')
```

</CodeGroup>
