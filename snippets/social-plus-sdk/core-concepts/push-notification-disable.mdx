{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: push-notification-disable | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun pushNotificationDisable() {
        // Disable push notifications for the current device
        AmityCoreClient.unregisterPushNotification()
            .subscribe(
                { /* push notifications disabled */ },
                { error -> /* handle error */ }
            )
    }
```

```swift iOS
let client = AmityClient.shared
        let notificationRepository = AmityNotificationRepository(client: client)
        notificationRepository.disablePushNotification { success, error in
            if let error = error { print("Error: \(error)"); return }
            print("Push notifications disabled: \(success)")
        }
```

```dart Flutter
void disablePushNotifications() {
      AmityCoreClient.unregisterDeviceNotification()
          .then((_) {
        // Push notifications disabled
      }).onError((error, stackTrace) {
        // Handle error
      });
    }
```

```typescript TypeScript
import { Client } from '@amityco/ts-sdk'

async function disablePushNotification(deviceToken: string) {
  await Client.unregisterPushNotification(deviceToken)
  console.log('Push notifications disabled for device:', deviceToken)
}
```

</CodeGroup>
