{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: access-local-object | updated: 2026-05-02 */}

<CodeGroup>

```dart Flutter
void getLocalChannel(String channelId) {
    AmityChatClient.newChannelRepository()
        .getChannel(channelId)
        .then((AmityChannel channel) {
          // access locally cached channel data
          final channelDisplayName = channel.displayName;
          final memberCount = channel.memberCount;
        });
  }
```

```typescript TypeScript
import { ChannelRepository } from '@amityco/ts-sdk'

function accessLocalChannel(channelId: string) {
  const unsubscribe = ChannelRepository.getChannel(channelId, ({ data: channel, loading }) => {
    if (loading) return

    // Access locally cached channel data fields
    const { channelId: id, displayName, memberCount, unreadCount, metadata } = channel
    console.log('Channel ID:', id)
    console.log('Display Name:', displayName)
    console.log('Member Count:', memberCount)
    console.log('Unread Count:', unreadCount)
    console.log('Metadata:', metadata)
  })

  return unsubscribe
}
```

</CodeGroup>
