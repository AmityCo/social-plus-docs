{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: notification-disable-community-for-topics | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun notificationDisableCommunityForTopics() {
        // Configure notification settings
        AmityCoreClient.newNotificationRepository()
            .disableNotificationForCommunity(communityId = "communityId", topics = listOf("posts", "comments"))
            .subscribe()
    }
```

```swift iOS
let notificationManager = AmityNotificationManager(client: AmityManager.shared.client!)
        notificationManager.disableCommunityNotification(communityId: "communityId123") { success, error in
            if let error = error { print("Error: \(error)"); return }
            print("Community notifications disabled for all topics")
        }
```

```dart Flutter
void disableCommunityNotifications(String communityId) {
    AmityCoreClient.notifications()
        .community(communityId: communityId)
        .disable()
        .then((_) {
          // community notifications disabled
        })
        .onError((error, stackTrace) {
          // handle error
        });
  }
```

```typescript TypeScript
import { CommunityRepository } from '@amityco/ts-sdk'

async function disableCommunityNotifications(communityId: string) {
  await CommunityRepository.disableNotification(communityId)
  console.log(`Community notifications disabled for community ${communityId}`)
}

// Example usage
disableCommunityNotifications('communityId')
```

</CodeGroup>
