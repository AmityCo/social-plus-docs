{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: unsync_all_user_presence | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun unsyncAllUserPresence() {
        // Perform unsync_all_user_presence operation
        AmityCoreClient.newUserRepository()
            .getUser("userId")
            .asFlowable()
            .subscribe({ user -> }, { error -> })
    }
```

```swift iOS
override func viewWillDisappear(_ animated: Bool) {
        super.viewWillDisappear(animated)
        
        // Stop syncing presence for all users as user
        // is navigating to another screen.
        userPresenceRepo.unsyncAllUserPresence()
    }
```

```dart Flutter
void unsyncAllUserPresence() {
    AmityCoreClient.newUserRepository()
        .unsyncAllPresence()
        .then((_) {
      // All user presences unsynced
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function unsyncAllUserPresence() {
  const { data: liveObject } = await runQuery(
    UserRepository.Presence.syncAllUserPresence()
  )
}
```

</CodeGroup>
