{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: r-t-e-post-subscription | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun subscribePost(post: AmityPost) {
        // ex. subscribing post topic
        post.subscription(events = AmityPostEvents.POST)
            .subscribeTopic()
            .subscribe()
        // ex. subscribing comment topic
        post.subscription(events = AmityPostEvents.COMMENTS)
            .subscribeTopic()
            .subscribe()
    }
```

```swift iOS
AmityUIKitManagerInternal.shared.client.subscribeTopic(AmityTopicCreator.postTopic(forPostId: "postId")) { error in
            print(error == nil ? "Subscribed to post topic" : "Subscribe failed: \(error!.localizedDescription)")
        }
```

```dart Flutter
StreamSubscription? _postSubscription;

    void subscribeToPostEvents(AmityPost post) {
      _postSubscription = post
          .subscription(AmityPostEvent.POST)
          .listen((_) {
        // Handle real-time post events
      });
    }

    void unsubscribeFromPostEvents() {
      _postSubscription?.cancel();
      _postSubscription = null;
    }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function rTEPostSubscription() {
  const unsubscribe = subscribeTopic(
    PostTopic.post('postId123'),
    () => {
      console.log('Post realtime event received');
    },
  );
  return unsubscribe;
}
```

</CodeGroup>
