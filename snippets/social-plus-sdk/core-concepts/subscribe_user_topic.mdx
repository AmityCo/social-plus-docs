{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: subscribe_user_topic | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun subscribeUserTopic() {
        val topic = AmityTopicBuilder.topic(AmityUser::class.java)
            .forUser("userId")
            .build()
        AmityCoreClient.subscribeTopic(topic)
            .subscribe({
                // Subscribed to user real-time events
            }, { error ->
                // Handle error
            })
    }
```

```dart Flutter
void subscribeUserTopic() {
    final topic = TopicBuilder.user(userId: 'userId').build();
    AmityCoreClient.subscribeTopic(topic).then((_) {
      // Subscribed to user events
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function subscribeUserTopic() {
  const { subscribeTopic } = await import('@amityco/ts-sdk');
  const topic = getUserTopic('userId');
  const unsubscribe = subscribeTopic(topic, (event) => {
    console.log('User event:', event);
  });
  return unsubscribe;
}
```

</CodeGroup>
