{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: observe_channel_presence | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun observeChannelPresence() {
        AmityChatClient.newChannelRepository()
            .getChannel("channelId")
            .asLiveData()
            .observe(lifecycleOwner) { channel ->
                val isOnline = channel.isOnline()
                // Handle presence update
            }
    }
```

```swift iOS
func observeChannelPresence() {
        let channelRepository = AmityChannelRepository(client: AmityClient.shared)
        let liveChannel = channelRepository.getChannel("channelId")
        token = liveChannel.observe { liveObject, error in
            guard let channel = liveObject.object else { return }
            print("Channel presence updated for: \(channel.channelId)")
        }
    }
```

</CodeGroup>
