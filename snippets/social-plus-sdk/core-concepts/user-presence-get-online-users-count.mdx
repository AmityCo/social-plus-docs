{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-presence-get-online-users-count | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getOnlineUsersCount() {
        AmityCoreClient.newUserPresenceRepository()
            .getOnlineUsersCount()
            .doOnSuccess { count: Int ->
                // count of online users
            }
            .doOnError {
                // handle error
            }
            .subscribe()
    }
```

```swift iOS
let userPresenceRepo = AmityUserPresenceRepository(client: client)
        do {
            let count = try await userPresenceRepo.getOnlineUsersCount()
            print("Online users count: \(count)")
        } catch {
            // Handle error here
        }
```

```dart Flutter
void userPresenceGetOnlineUsersCount() {
    AmityCoreClient.presence
        .getOnlineUsersCount()
        .then((count) => print('Online users: \$count'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function userPresenceGetOnlineUsersCount() {
  const { data } = await runQuery(
    UserRepository.Presence.getOnlineUsersCount()
  )
  console.log('Online users count:', data)
}
```

</CodeGroup>
