{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: post-unreact | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun postUnreact() {
        AmitySocialClient.newPostRepository()
            .getPost("postId")
            .flatMap { post ->
                post.react()
                    .removeReaction("like")
            }
            .subscribe({
                // Reaction removed
            }, { error ->
                // Handle error
            })
    }
```

```swift iOS
func postUnreact() {
        let reactionRepository = AmityReactionRepository(client: AmityClient.shared)
        reactionRepository.removeReaction("like", referenceId: "postId", referenceType: .post) { success, error in
            if let error = error {
                print("Failed to remove reaction: \(error.localizedDescription)")
                return
            }
            print("Reaction removed successfully")
        }
    }
```

```dart Flutter
void postUnreact() async {
    await AmitySocialClient.newPostRepository()
        .getPost('postId')
        .react()
        .removeReaction('like');
  }
```

```typescript TypeScript
import { ReactionRepository } from '@amityco/ts-sdk'

async function unreactToPost(postId: string, reactionName: string = 'like') {
  await ReactionRepository.removeReaction('post', postId, reactionName)
  console.log(`Removed reaction "${reactionName}" from post ${postId}`)
}
```

</CodeGroup>
