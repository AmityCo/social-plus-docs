{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: create_user_token | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun createUserToken() {
        // Generate a user authentication token
        AmityCoreClient.createUserToken(userId = "userId")
            .subscribe({ token -> println(token) }, { error -> error.printStackTrace() })
    }
```

```swift iOS
// 1. create AmityUserTokenManager instance.
    let userTokenManager = AmityUserTokenManager(apiKey: "<api-key>", region: .SG)
    
    func createNewUserTokenExample() async {
        // 2. call `createUserToken` on AmityUserTokenManager.
        do {
            let auth = try await userTokenManager.createUserToken(
                userId: "<user-id>",
                displayName: "<(optional)-display-name>",
                authToken: "<(optional)-auth-token>"
            )
            print("auth.accessToken: \(auth.accessToken)")
        } catch {
            print("unable to create a new user token: \(error.localizedDescription)")
        }
    }
```

```dart Flutter
void createUserToken() {
    await AmityClient.shared.login(
      userId: 'userId',
      displayName: 'User Display Name',
      authToken: 'secureUserAuthToken',
    );
    // SDK manages the session token automatically
  }
```

```typescript TypeScript
import { Client } from '@amityco/ts-sdk'

async function createUserToken() {
  const client = Client.createClient('apiKey')
  await client.login({
    userId: 'userId',
    displayName: 'User Name',
    authToken: 'secureAuthToken',
  })
  console.log('User authenticated with token')
}
```

</CodeGroup>
