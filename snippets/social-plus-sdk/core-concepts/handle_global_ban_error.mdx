{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: handle_global_ban_error | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun handleGlobalBanError() {
        // Handle errors globally, including ban errors, from the SDK client
        AmityCoreClient.setErrorHandler { throwable ->
            when (throwable) {
                is com.amityco.socialcloud.sdk.core.exception.AmityException -> {
                    // Handle specific SDK exception (e.g., global ban)
                    val code = throwable.code
                }
                else -> {
                    // Handle unexpected errors
                }
            }
        }
    }
```

```dart Flutter
void handleGlobalBanError() {
    try {
      await AmitySocialClient.newPostRepository()
          .createPost()
          .targetMe()
          .text('Hello world')
          .post();
    } on AmityException catch (e) {
      if (e.code == 400311) {
        print('User is globally banned');
      } else {
        print('Error: \${e.message}');
      }
    }
  }
```

```typescript TypeScript
import { createClient } from '@amityco/ts-sdk'

async function handleGlobalBanError() {
  const client = createClient('your-api-key')
  client.onError = (error) => {
    if (error.code === 400301) {
      console.warn('User is globally banned. Logging out.')
      client.logout()
    } else {
      console.error('SDK error:', error.message)
    }
  }
  await client.login({ userId: 'user-id-123', displayName: 'John Doe' })
}
```

</CodeGroup>
