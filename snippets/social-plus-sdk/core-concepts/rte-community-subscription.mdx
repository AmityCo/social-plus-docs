{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: rte-community-subscription | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun rteCommunitySubscription() {
        // perform rte community subscription operation
        AmitySocialClient.newCommunityRepository()
            .getCommunity("communityId")
            .asFlowable()
            .subscribe { community ->
                // handle community object
            }
    }
```

```swift iOS
AmityUIKitManagerInternal.shared.client.subscribeTopic(AmityTopicCreator.communityTopic(forCommunityId: "communityId")) { error in
            print(error == nil ? "Subscribed to community RTE topic" : "Failed: \(error!.localizedDescription)")
        }
```

```dart Flutter
Future<void> subscribeToCommunityEvents(AmityCommunity community) async {
    await community
        .subscription(AmityCommunityEvents.COMMUNITY)
        .subscribeTopic();

    await community
        .subscription(AmityCommunityEvents.POSTS)
        .subscribeTopic();

    await community
        .subscription(AmityCommunityEvents.COMMENTS)
        .subscribeTopic();
    // now receiving realtime updates for community, posts, and comments
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function rteCommunitySubscription() {
  import { SubscriptionLevels, CommunityRepository } from '@amityco/ts-sdk';

  const unsubscribe = CommunityRepository.subscribeCommunity(
    { communityId: 'community-id-123', level: SubscriptionLevels.COMMUNITY },
    () => {
      console.log('Community updated via RTE');
    },
  );
  return unsubscribe;
}
```

</CodeGroup>
