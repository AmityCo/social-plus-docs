{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user_mention | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun userMention() {
        // Create a post with user mention metadata
        AmitySocialClient.newPostRepository()
            .createPost()
            .targetUser(userId = "userId")
            .text(text = "Hello @username")
            .build()
            .post()
            .subscribe { post -> }
    }
```

```swift iOS
let client = AmityClient.shared
        let repository = AmityUserRepository(client: client)
        let metadata: [String: Any] = [
            "mentionees": [["type": "user", "userIds": ["userId1", "userId2"]]]
        ]
        print("Mention metadata prepared: \(metadata)")
```

```dart Flutter
void userMention() {
    AmitySocialClient.newPostRepository()
        .createPost()
        .targetMe()
        .text('Hello @mention!')
        .mentionUsers(['userId1', 'userId2'])
        .post()
        .then((AmityPost post) {
          print('Post with mention created: \${post.postId}');
        });
  }
```

```typescript TypeScript
import { PostRepository } from '@amityco/ts-sdk'

async function mentionUser() {
  const post = await PostRepository.createPost({
    targetType: 'community',
    targetId: 'communityId',
    dataType: 'text',
    data: { text: 'Hello @john!' },
    mentionees: [{ type: 'user', userIds: ['john-user-id'] }],
    metadata: {
      mentioned: [{ type: 'user', userId: 'john-user-id', index: 6, length: 5 }],
    },
  });
  console.log('Post with mention created:', post.postId);
}
```

</CodeGroup>
