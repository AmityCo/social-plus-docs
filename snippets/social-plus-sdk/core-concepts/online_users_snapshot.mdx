{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: online_users_snapshot | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun onlineUsersSnapshot() {
        // Perform the operation using the appropriate SDK repository
        AmitySocialClient.newPostRepository()
            .getPosts()
            .targetMe()
            .build()
            .query()
            .subscribe({ pagingData ->
                // Handle live collection updates
            }, { error ->
                // Handle error
            })
    }
```

```dart Flutter
void onlineUsersSnapshot() async {
        final snapshot = await AmityCoreClient.getOnlineUsersSnapshot(streamId: 'streamId');
        print('Online users: \${snapshot.count}');
        print('Retrieved at: \${snapshot.timestamp}');
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function onlineUsersSnapshot() {
  const snapshot = await UserRepository.getOnlineUsers({ limit: 20 });
  console.log('Online users:', snapshot.data);
}
```

</CodeGroup>
