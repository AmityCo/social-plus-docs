{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: reaction-summary | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun reactionSummary() {
        // TODO: Implement Reaction summary
    }
```

```swift iOS
let postRepository = AmityPostRepository(client: AmityManager.shared.client!)
        let liveObject = postRepository.getPost("postId")
        liveObject.observe { liveObject, error in
            guard let post = liveObject.object, error == nil else { return }
            let reactionSummary = post.myReactions
            print("My reactions: \(reactionSummary)")
            print("Reaction count: \(post.reactionsCount)")
        }
```

```dart Flutter
void getReactionSummary(AmityPost post) {
      final reactionsCount = post.reactionsCount ?? 0;
      final reactionMap = post.reactionMap ?? {};
      final myReactions = post.myReactions ?? [];

      // Display reaction summary
      reactionMap.forEach((reaction, count) {
        // Show reaction type and count
      });
    }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function reactionSummary() {
  const unsub = ReactionRepository.getReactionSummary({
    referenceType: 'post',
    referenceId: 'postId123',
  }).subscribe({
    next: ({ data: summary }) => {
      console.log('Total reactions:', summary?.totalCount);
      summary?.reactionCounts?.forEach(({ reactionName, count }) => {
        console.log(`  ${reactionName}: ${count}`);
      });
    },
  });
  return unsub;
}
```

</CodeGroup>
