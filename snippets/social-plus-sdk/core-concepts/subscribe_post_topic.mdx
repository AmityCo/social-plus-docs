{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: subscribe_post_topic | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun subscribePostTopic() {
        val topic = AmityTopicBuilder.topic(AmityPost::class.java)
            .forPost("postId")
            .build()
        AmityCoreClient.subscribeTopic(topic)
            .subscribe({
                // Subscribed to post real-time events
            }, { error ->
                // Handle error
            })
    }
```

```dart Flutter
void subscribePostTopic() {
    final topic = TopicBuilder.community(communityId: 'communityId')
        .post()
        .build();
    AmityCoreClient.subscribeTopic(topic).then((_) {
      // Subscribed to post events
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function subscribePostTopic() {
  const { subscribeTopic } = await import('@amityco/ts-sdk');
  const topic = getPostTopic('postId');
  const unsubscribe = subscribeTopic(topic, (event) => {
    console.log('Post event:', event);
  });
  return unsubscribe;
}
```

</CodeGroup>
