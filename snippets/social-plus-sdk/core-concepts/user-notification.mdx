{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-notification | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun userNotification() {
        // Perform user-notification operation
        AmityCoreClient.registerDeviceForPushNotification("fcmToken")
            .subscribe({ /* registered */ }, { error -> })
    }
```

```swift iOS
AmityManager.shared.client.notificationForCurrentUser().enable { error in
            if let error = error {
                print("Error enabling notifications: \(error)")
            } else {
                print("User notifications enabled")
            }
        }
```

```dart Flutter
void userNotification() {
    AmityCoreClient.newUserRepository()
        .notification
        .getSettings()
        .then((settings) => print('Notifications enabled: \${settings.isEnabled}'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function userNotification() {
  const { data: settings } = await NotificationRepository.getUserNotificationSettings()
  console.log('User notification settings:', settings)
}
```

</CodeGroup>
