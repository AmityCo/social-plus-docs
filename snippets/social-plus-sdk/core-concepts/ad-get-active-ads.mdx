{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: ad-get-active-ads | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun adGetActiveAds() {
        AmitySocialClient.newAdRepository()
            .getActiveAds()
            .subscribe { ads ->
                ads.forEach { ad -> ad.getAdId() }
            }
    }
```

```swift iOS
let adRepository = AmityAdRepository(client: AmityManager.shared.client!)
        let liveCollection = adRepository.getAds(placement: .feedBanner)
        liveCollection.observe { collection, _, error in
            guard error == nil else { return }
            print("Active ads count: \(collection.count())")
            for i in 0..<collection.count() {
                if let ad = collection.object(at: i) { print("Ad: \(ad.adId)") }
            }
        }
```

```dart Flutter
void adGetActiveAds() {
    AmityAdRepository()
        .getAds(adStatus: AmityAdStatus.active)
        .query();
  }
```

```typescript TypeScript
import { AdRepository } from '@amityco/ts-sdk'

function getActiveAds(placement: string) {
  const liveCollection = AdRepository.getActiveAds({ placement })

  const unsub = liveCollection.on('dataUpdated', (ads) => {
    console.log('Active ads:', ads)
  })

  return unsub
}
```

</CodeGroup>
