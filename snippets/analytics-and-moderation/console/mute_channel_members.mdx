{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: mute_channel_members | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun muteChannelMembers() {
        // Mute/unmute operation
        AmityChatClient.newChannelRepository()
            .muteMembers(channelId = "channelId", userIds = listOf("userId"), period = com.amityco.socialcloud.sdk.chat.channel.AmityMutePeriod.FOREVER)
            .subscribe()
    }
```

```dart Flutter
void muteChannelMembers() {
    final channelId = 'channelId';
    final userIds = ['userId1', 'userId2'];
    final mutePeriod = 300; // seconds
    AmityCoreClient.newChannelRepository()
        .moderation(channelId)
        .muteMembers(userIds, mutePeriod)
        .then((_) => print('Members muted'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function muteChannelMembers() {
  // Mute members
  await ChannelRepository.muteMembers('channelId', ['userId1', 'userId2'], 60000)
  console.log('Members muted for 60 seconds')
  
  // Unmute members
  await ChannelRepository.unmuteMembers('channelId', ['userId1'])
  console.log('Member unmuted')
}
```

</CodeGroup>
