{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-flag-count | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun getMessageFlagCount(message: AmityMessage) {
        val totalFlagCount = message.getFlagCount()
    }
```

```swift iOS
let messageRepo = AmityMessageRepository(client: AmityManager.shared.client!)
        let liveObject = messageRepo.getMessage("messageId123")
        liveObject.observe { liveObject, error in
            guard let message = liveObject.object else { return }
            print("Flag count: \(message.flagCount)")
        }
```

```dart Flutter
void messageFlagCount() {
    final messageId = 'messageId';
    AmityCoreClient.newChannelRepository()
        .message('')
        .messageId(messageId)
        .getFlagCount()
        .then((count) => print('Flag count: \$count'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

function getMessageFlagCount(message) {
  const totalFlagCount = message.flagCount
  console.log('Flag count:', totalFlagCount)
}
```

</CodeGroup>
