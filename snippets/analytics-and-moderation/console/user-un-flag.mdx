{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-un-flag | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun unflagUser(
        userRepository: AmityUserRepository,
        user: AmityUser
    ) {
        userRepository.unflagUser(userId = user.getUserId())
            .doOnComplete {
                //success
            }
            .subscribe()
    }
```

```swift iOS
let client = AmityClient.shared
        let moderationRepository = AmityModerationRepository(client: client)
        moderationRepository.unflagUser(withUserId: "userId") { success, error in
            if success { print("User unflagged successfully") }
        }
```

```dart Flutter
void unflagUser(AmityUser user) {
    user.report().unflag().then((value) {
      //success
    }).onError((error, stackTrace) {
      //handle error
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function userUnFlag() {
  const userId = 'user-id-to-unflag';
  await UserRepository.unflagUser(userId);
  console.log('User unflagged:', userId);
}
```

</CodeGroup>
