{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-flag-check | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun isMessageFlaggedByMe(message: AmityMessage) {
        val isFlaggedByMe = message.isFlaggedByMe()
    }
```

```swift iOS
let messageRepo = AmityMessageRepository(client: AmityManager.shared.client!)
        messageRepo.isFlaggedByMe(messageId: "messageId123") { isFlagged, error in
            if let error = error { print("Error: \(error)"); return }
            print("Message flagged by me: \(isFlagged)")
        }
```

```dart Flutter
void messageFlagCheck() {
    final messageId = 'messageId';
    AmityCoreClient.newChannelRepository()
        .message('')
        .messageId(messageId)
        .getFlagCount()
        .then((count) => print('Flag count: \$count'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { MessageRepository } from '@amityco/ts-sdk'

async function checkMessageFlaggedByMe() {
  const isFlagged = await MessageRepository.isMessageFlaggedByMe('messageId')
  console.log('Is flagged by me:', isFlagged)
}
```

</CodeGroup>
