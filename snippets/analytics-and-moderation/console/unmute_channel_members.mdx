{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: unmute_channel_members | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun unmuteChannelMembers() {
        // Perform unmute_channel_members operation
        AmityChatClient.newChannelRepository()
            .muteChannel("channelId", true)
            .subscribe({ /* muted */ }, { error -> })
    }
```

```dart Flutter
void unmuteChannelMembers() {
    AmityChatClient.newChannelRepository()
        .moderation('channelId')
        .unmuteMembers(userIds: ['userId1', 'userId2'])
        .then((_) {
      // Members unmuted
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function unmuteChannelMembers() {
  await ChannelRepository.unmuteMembers({
    channelId: 'channelId',
    userIds: ['userId1', 'userId2'],
  })
  console.log('Channel members unmuted')
}
```

</CodeGroup>
