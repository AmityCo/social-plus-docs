{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: message-un-flag | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun unFlagMessage(
        messageRepository: AmityMessageRepository,
        message: AmityMessage
    ) {
        messageRepository.unflagMessage(messageId = message.getMessageId())
            .doOnComplete {
                // Void
            }
            .doOnError {
                // Exception
            }
            .subscribe()
    }
```

```swift iOS
let messageRepo = AmityMessageRepository(client: AmityManager.shared.client!)
        messageRepo.unflagMessage(messageId: "messageId123") { success, error in
            if let error = error { print("Error: \(error)"); return }
            print("Message unflagged successfully")
        }
```

```dart Flutter
void messageUnFlag() {
    final messageId = 'messageId';
    AmityCoreClient.newChannelRepository()
        .message('')
        .messageId(messageId)
        .unflag()
        .then((_) => print('Message unflagged'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function messageUnFlag() {
  await FlagRepository.unflagMessage('messageId')
  console.log('Message unflagged successfully')
}
```

</CodeGroup>
