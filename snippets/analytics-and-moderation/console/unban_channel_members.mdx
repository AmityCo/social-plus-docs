{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: unban_channel_members | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun unbanChannelMembers() {
        AmityChatClient.newChannelRepository()
            .moderation("channelId")
            .unbanMembers(listOf("userId1", "userId2"))
            .subscribe({
                // Channel members unbanned
            }, { error ->
                // Handle error
            })
    }
```

```dart Flutter
void unbanChannelMembers() {
    AmityChatClient.newChannelRepository()
        .moderation('channelId')
        .unbanMembers(userIds: ['userId1', 'userId2'])
        .then((_) {
      // Members unbanned successfully
    }).onError((error, stackTrace) {
      // Handle error
    });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function unbanChannelMembers() {
  const channelRepository = new ChannelRepository();
  await channelRepository.unbanChannelMembers('channelId', ['userId1', 'userId2']);
  console.log('Channel members unbanned');
}
```

</CodeGroup>
