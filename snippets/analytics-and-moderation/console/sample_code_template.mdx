{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: sample_code_template | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun sampleCodeTemplate() {
        // perform sample code template operation
        // TODO: implement SDK operation
        AmitySocialClient.newPostRepository()
            .getPosts()
            .targetMe()
            .build()
            .query()
            .subscribe { result ->
                // handle result
            }
    }
```

```swift iOS
let x = "add sample code here"
```

```dart Flutter
void sampleCodeTemplate() {
    // Sample code template for Social Plus SDK
    AmityCoreClient.setup(
      option: AmityCoreClientOption(apiKey: 'YOUR_API_KEY'),
    );
    print('SDK setup complete');
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function sampleCodeTemplate() {
  const client = createClient('YOUR_API_KEY');

  // Example: query a live object
  const unsubscribe = client.subscribe('some-topic', (data) => {
    console.log('Received data:', data);
  });

  // Cleanup subscription
  return unsubscribe;
}
```

</CodeGroup>
