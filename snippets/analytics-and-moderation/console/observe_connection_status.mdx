{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: observe_connection_status | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun observeConnectionStatus() {
        // Observe SDK connection status changes
        AmityCoreClient.observeConnectionStatus()
            .subscribe({ status ->
                // React to connection state changes
            }, { error ->
                // Handle error
            })
    }
```

```dart Flutter
void observeConnectionStatus() async {
        AmityCoreClient.observeConnectionStatus().listen((status) {
          switch (status) {
            case AmityConnectionStatus.connected:
              print('Connected');
              break;
            case AmityConnectionStatus.disconnected:
              print('Disconnected');
              break;
            default:
              print('Status: \$status');
          }
        });
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function observeConnectionStatus() {
  const client = createClient('YOUR_API_KEY');
  client.on('connectionStatusChanged', (status) => {
    console.log('Connection status:', status);
  });
}
```

</CodeGroup>
