{/* AUTO-GENERATED by harness gendocs — do not edit manually */}
{/* function: user-flag-check | updated: 2026-05-02 */}

<CodeGroup>

```kotlin Android
fun flagUser(user: AmityUser) {
        // check whether we already have flagged this user
        val isFlaggedByMe = user.isFlaggedByMe()
        // get the number of people who already have flagged this user
        val totalFlagCount = user.getFlagCount()
    }
```

```swift iOS
let userId = "userId"
        AmitySDK.isUserFlagged(userId: userId) { isFlagged, error in
            if let error = error {
                print("Failed to check flag status: \(error)")
                return
            }
            print("User is flagged: \(isFlagged)")
        }
```

```dart Flutter
void userFlagCheck() {
    AmitySocialClient.newUserRepository()
        .isFlaggedByMe(userId: 'userId')
        .then((flagged) => print('Flag status: \$flagged'))
        .onError((error, _) => print('Error: \$error'));
  }
```

```typescript TypeScript
import { Client, createClient } from '@amityco/ts-sdk'

async function userFlagCheck() {
  const { data: user } = await runQuery(
    UserRepository.getUser({ userId: 'userId' })
  )
  console.log('Flag count:', user?.flagCount)
}
```

</CodeGroup>
