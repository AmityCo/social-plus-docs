# AUTO-GENERATED by harness genmanifests — fill in snippets: with GendocsKeys
# GendocsKey = DeriveKey(filename): strip 'Amity' prefix, PascalCase → kebab-case
# Example: AmityClientLogin.kt → client-login
sections:
    implementation-pattern:
        heading: '### Implementation Pattern'
        snippets:
            - notification-tray-items-query
            - notification-tray-query-items
    notification-tray-item:
        heading: '### Notification Tray Item'
        snippets:
            - notification-tray-items-query
    notification-tray-seen-state:
        heading: '### Notification Tray Seen State'
        snippets:
            - notification-tray-get-notification-tray-seen
            - notification-tray-seen-get
            - notification-tray-mark-all-as-seen
            - notification-tray-mark-seen
