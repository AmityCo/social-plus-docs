---
title: "Event RSVP"
description: "Manage event responses and track attendance with comprehensive RSVP functionality"
---

# Event RSVP

Handle event attendance through RSVP responses, track attendees, and manage user participation in scheduled events.

## Overview

The RSVP system allows users to respond to event invitations with three status types:
- **Going**: Confirmed attendance
- **Interested**: Considering attendance
- **Not Going**: Declined invitation

Each event tracks RSVP counts and provides APIs to manage responses and query attendees.

## RSVP Response Types

| Status | Description | Impact |
|--------|-------------|--------|
| **Going** | User will attend | Counted in `rsvpCount`, included in attendee list |
| **Interested** | User might attend | Counted in `interestedCount`, separate from attendees |
| **Not Going** | User won't attend | Recorded but not counted in attendance |

## Creating an RSVP

### RSVP as Going

<CodeGroup>
```swift iOS
Task { @MainActor in
    do {
        let event = try await AmityEventRepository.shared.getEvent(id: "event-123")
        let rsvp = try await event.createRSVP(status: .going)
        print("RSVP created: \(rsvp.status)")
    } catch {
        print("Failed to RSVP: \(error)")
    }
}
```

```kotlin Android
AmityEventRepository.getEvent(id = "event-123")
    .observe()
    .firstOrError()
    .flatMap { event ->
        event.createRSVP(status = AmityEventResponseStatus.GOING)
    }
    .subscribe(
        onSuccess = { rsvp ->
            Log.d("RSVP", "Created: ${rsvp.status}")
        },
        onError = { error ->
            Log.e("RSVP", "Failed to create", error)
        }
    )
```

```typescript TypeScript
try {
    const event = await eventRepository.getEvent("event-123");
    const rsvp = await event.createRSVP(AmityEventResponseStatus.GOING);
    console.log("RSVP created:", rsvp.status);
} catch (error) {
    console.error("Failed to RSVP:", error);
}
```
</CodeGroup>

### RSVP as Interested

<CodeGroup>
```swift iOS
let event = try await AmityEventRepository.shared.getEvent(id: "event-123")
let rsvp = try await event.createRSVP(status: .interested)
```

```kotlin Android
val event = AmityEventRepository.getEvent(id = "event-123").blockingFirst()
val rsvp = event.createRSVP(status = AmityEventResponseStatus.INTERESTED).blockingGet()
```

```typescript TypeScript
const event = await eventRepository.getEvent("event-123");
const rsvp = await event.createRSVP(AmityEventResponseStatus.INTERESTED);
```
</CodeGroup>

### RSVP as Not Going

<CodeGroup>
```swift iOS
let event = try await AmityEventRepository.shared.getEvent(id: "event-123")
let rsvp = try await event.createRSVP(status: .notGoing)
```

```kotlin Android
val event = AmityEventRepository.getEvent(id = "event-123").blockingFirst()
val rsvp = event.createRSVP(status = AmityEventResponseStatus.NOT_GOING).blockingGet()
```

```typescript TypeScript
const event = await eventRepository.getEvent("event-123");
const rsvp = await event.createRSVP(AmityEventResponseStatus.NOT_GOING);
```
</CodeGroup>

## Updating an RSVP

Change existing RSVP response:

<CodeGroup>
```swift iOS
Task { @MainActor in
    do {
        let event = try await AmityEventRepository.shared.getEvent(id: "event-123")
        let updatedRSVP = try await event.updateRSVP(status: .notGoing)
        print("RSVP updated to: \(updatedRSVP.status)")
    } catch {
        print("Failed to update RSVP: \(error)")
    }
}
```

```kotlin Android
AmityEventRepository.getEvent(id = "event-123")
    .observe()
    .firstOrError()
    .flatMap { event ->
        event.updateRSVP(status = AmityEventResponseStatus.NOT_GOING)
    }
    .subscribe(
        onSuccess = { rsvp ->
            Log.d("RSVP", "Updated to: ${rsvp.status}")
        },
        onError = { error ->
            Log.e("RSVP", "Failed to update", error)
        }
    )
```

```typescript TypeScript
try {
    const event = await eventRepository.getEvent("event-123");
    const updatedRSVP = await event.updateRSVP(AmityEventResponseStatus.NOT_GOING);
    console.log("RSVP updated to:", updatedRSVP.status);
} catch (error) {
    console.error("Failed to update RSVP:", error);
}
```
</CodeGroup>

## Getting My RSVP

Check current user's RSVP status for an event:

<CodeGroup>
```swift iOS
Task { @MainActor in
    do {
        let event = try await AmityEventRepository.shared.getEvent(id: "event-123")
        let myRSVP = try await event.getMyRSVP()
        print("My RSVP status: \(myRSVP.status)")
        print("Responded at: \(myRSVP.respondedAt)")
    } catch {
        print("Failed to get RSVP: \(error)")
    }
}
```

```kotlin Android
AmityEventRepository.getEvent(id = "event-123")
    .observe()
    .firstOrError()
    .flatMap { event ->
        event.getMyRSVP()
    }
    .subscribe(
        onSuccess = { rsvp ->
            Log.d("RSVP", "My status: ${rsvp.status}")
            Log.d("RSVP", "Responded at: ${rsvp.respondedAt}")
        },
        onError = { error ->
            if (error is AmityException.NotFound) {
                Log.d("RSVP", "No RSVP yet")
            } else {
                Log.e("RSVP", "Failed to get", error)
            }
        }
    )
```

```typescript TypeScript
try {
    const event = await eventRepository.getEvent("event-123");
    const myRSVP = await event.getMyRSVP();
    
    if (myRSVP) {
        console.log("My RSVP status:", myRSVP.status);
        console.log("Responded at:", myRSVP.respondedAt);
    } else {
        console.log("No RSVP yet");
    }
} catch (error) {
    console.error("Failed to get RSVP:", error);
}
```
</CodeGroup>

## Querying Event Attendees

### Get All Going Attendees

<CodeGroup>
```swift iOS
let event = try await AmityEventRepository.shared.getEvent(id: "event-123")
let attendees = event.getRSVPs(status: .going)

attendees.observe { responses in
    responses.forEach { rsvp in
        print("Attendee: \(rsvp.user.displayName)")
        print("Responded at: \(rsvp.respondedAt)")
    }
    print("Total going: \(responses.count)")
}
```

```kotlin Android
val event = AmityEventRepository.getEvent(id = "event-123").blockingFirst()
val attendees = event.getRSVPs(status = AmityEventResponseStatus.GOING)

attendees.observe()
    .subscribe { responses ->
        responses.forEach { rsvp ->
            Log.d("Attendee", "Name: ${rsvp.user.displayName}")
            Log.d("Attendee", "Responded at: ${rsvp.respondedAt}")
        }
        Log.d("Attendee", "Total going: ${responses.size}")
    }
```

```typescript TypeScript
const event = await eventRepository.getEvent("event-123");
const attendees = event.getRSVPs(AmityEventResponseStatus.GOING);

attendees.on("dataUpdated", (responses) => {
    responses.forEach(rsvp => {
        console.log("Attendee:", rsvp.user.displayName);
        console.log("Responded at:", rsvp.respondedAt);
    });
    console.log("Total going:", responses.length);
});
```
</CodeGroup>

### Get Interested Users

<CodeGroup>
```swift iOS
let event = try await AmityEventRepository.shared.getEvent(id: "event-123")
let interested = event.getRSVPs(status: .interested)

interested.observe { responses in
    print("Interested count: \(responses.count)")
    responses.forEach { rsvp in
        print("- \(rsvp.user.displayName)")
    }
}
```

```kotlin Android
val event = AmityEventRepository.getEvent(id = "event-123").blockingFirst()
val interested = event.getRSVPs(status = AmityEventResponseStatus.INTERESTED)

interested.observe()
    .subscribe { responses ->
        Log.d("RSVP", "Interested count: ${responses.size}")
        responses.forEach { rsvp ->
            Log.d("RSVP", "- ${rsvp.user.displayName}")
        }
    }
```

```typescript TypeScript
const event = await eventRepository.getEvent("event-123");
const interested = event.getRSVPs(AmityEventResponseStatus.INTERESTED);

interested.on("dataUpdated", (responses) => {
    console.log("Interested count:", responses.length);
    responses.forEach(rsvp => {
        console.log("-", rsvp.user.displayName);
    });
});
```
</CodeGroup>

### Get Not Going Users

<CodeGroup>
```swift iOS
let event = try await AmityEventRepository.shared.getEvent(id: "event-123")
let notGoing = event.getRSVPs(status: .notGoing)

notGoing.observe { responses in
    print("Not going count: \(responses.count)")
}
```

```kotlin Android
val event = AmityEventRepository.getEvent(id = "event-123").blockingFirst()
val notGoing = event.getRSVPs(status = AmityEventResponseStatus.NOT_GOING)

notGoing.observe()
    .subscribe { responses ->
        Log.d("RSVP", "Not going count: ${responses.size}")
    }
```

```typescript TypeScript
const event = await eventRepository.getEvent("event-123");
const notGoing = event.getRSVPs(AmityEventResponseStatus.NOT_GOING);

notGoing.on("dataUpdated", (responses) => {
    console.log("Not going count:", responses.length);
});
```
</CodeGroup>

## RSVP Data Model

The `AmityEventResponse` object contains:

```typescript
interface AmityEventResponse {
  status: AmityEventResponseStatus; // going | interested | not_going
  eventId: string;
  event: AmityEvent; // Linked event object
  userId: string; // User who RSVP'd
  user: AmityUser; // Linked user object
  respondedAt: string; // ISO 8601 timestamp
}
```

## Common Use Cases

### Display RSVP Status Button

```typescript
// Get current user's RSVP and display appropriate button
const event = await eventRepository.getEvent(eventId);
const myRSVP = await event.getMyRSVP();

if (!myRSVP) {
    // Show RSVP options
    displayRSVPOptions();
} else if (myRSVP.status === AmityEventResponseStatus.GOING) {
    // Show "Going" button, allow change
    displayGoingButton(myRSVP);
} else if (myRSVP.status === AmityEventResponseStatus.INTERESTED) {
    // Show "Interested" button, allow change
    displayInterestedButton(myRSVP);
} else {
    // Show "Not Going" button, allow change
    displayNotGoingButton(myRSVP);
}
```

### Attendee List with Real-Time Updates

```typescript
// Display attendee list that updates in real-time
const event = await eventRepository.getEvent(eventId);
const attendees = event.getRSVPs(AmityEventResponseStatus.GOING);

attendees.on("dataUpdated", (responses) => {
    const attendeeList = responses.map(rsvp => ({
        userId: rsvp.userId,
        name: rsvp.user.displayName,
        avatar: rsvp.user.avatar,
        respondedAt: rsvp.respondedAt
    }));
    
    updateAttendeeUI(attendeeList);
    updateCountBadge(responses.length);
});
```

### RSVP Summary

```typescript
// Get comprehensive RSVP summary
const event = await eventRepository.getEvent(eventId);

console.log(`RSVP Summary for: ${event.title}`);
console.log(`Going: ${event.rsvpCount}`);
console.log(`Interested: ${event.interestedCount}`);

// Optionally get detailed lists
const going = event.getRSVPs(AmityEventResponseStatus.GOING);
const interested = event.getRSVPs(AmityEventResponseStatus.INTERESTED);
```

### Toggle RSVP Status

```typescript
// Toggle between Going and Not Going
async function toggleRSVP(eventId: string) {
    const event = await eventRepository.getEvent(eventId);
    const myRSVP = await event.getMyRSVP();
    
    if (!myRSVP || myRSVP.status !== AmityEventResponseStatus.GOING) {
        // Create or update to Going
        if (!myRSVP) {
            await event.createRSVP(AmityEventResponseStatus.GOING);
        } else {
            await event.updateRSVP(AmityEventResponseStatus.GOING);
        }
        showNotification("You're going to this event!");
    } else {
        // Update to Not Going
        await event.updateRSVP(AmityEventResponseStatus.NOT_GOING);
        showNotification("RSVP updated");
    }
}
```

## Best Practices

<AccordionGroup>
  <Accordion title="UI State Management" icon="toggle-on">
    Maintain UI state based on RSVP status:
    
    ```typescript
    // Check RSVP status before showing options
    const myRSVP = await event.getMyRSVP();
    
    if (myRSVP) {
        // User has RSVP'd - show update options
        showRSVPUpdateUI(myRSVP.status);
    } else {
        // User hasn't RSVP'd - show create options
        showRSVPCreateUI();
    }
    ```
  </Accordion>
  
  <Accordion title="Real-Time Updates" icon="arrows-rotate">
    Subscribe to attendee lists for live updates:
    
    - Use live collections for attendee lists
    - Update counts automatically
    - Unsubscribe when view is dismissed
    - Handle network disconnections gracefully
  </Accordion>
  
  <Accordion title="Error Handling" icon="triangle-exclamation">
    Handle RSVP errors appropriately:
    
    ```typescript
    try {
        await event.createRSVP(status);
    } catch (error) {
        if (error.code === 'ALREADY_EXISTS') {
            // User already RSVP'd, use update instead
            await event.updateRSVP(status);
        } else if (error.code === 'EVENT_ENDED') {
            showError("This event has ended");
        } else {
            showError("Failed to RSVP");
        }
    }
    ```
  </Accordion>
  
  <Accordion title="Performance Optimization" icon="gauge-high">
    Optimize attendee list queries:
    
    - Implement pagination for large attendee lists
    - Cache frequently viewed lists
    - Load counts separately from full lists
    - Use appropriate query limits
  </Accordion>
</AccordionGroup>

## Notifications

RSVP actions may trigger notifications:

| Action | Notification Trigger |
|--------|---------------------|
| User RSVPs "Going" | Event creator notified of new attendee |
| User changes to "Not Going" | Event creator may be notified |
| Event updates | All "Going" attendees notified of changes |
| Event reminder | All "Going" and "Interested" users notified |

<Note>
**Notification Settings**: Notification behavior is configurable at the network level through the Admin Console.
</Note>

## Error Handling

<CodeGroup>
```swift iOS
do {
    try await event.createRSVP(status: .going)
} catch AmityError.alreadyExists {
    // RSVP already exists, use update instead
    try await event.updateRSVP(status: .going)
} catch AmityError.eventEnded {
    showError("Cannot RSVP to ended event")
} catch AmityError.eventCancelled {
    showError("This event has been cancelled")
} catch {
    showError("Failed to RSVP: \(error.localizedDescription)")
}
```

```kotlin Android
event.createRSVP(status = AmityEventResponseStatus.GOING)
    .subscribe(
        onSuccess = { /* Success */ },
        onError = { error ->
            when (error) {
                is AmityException.AlreadyExists -> {
                    // Use update instead
                    event.updateRSVP(status = AmityEventResponseStatus.GOING)
                }
                is AmityException.EventEnded -> {
                    showError("Cannot RSVP to ended event")
                }
                is AmityException.EventCancelled -> {
                    showError("This event has been cancelled")
                }
                else -> {
                    showError("Failed to RSVP: ${error.message}")
                }
            }
        }
    )
```

```typescript TypeScript
try {
    await event.createRSVP(AmityEventResponseStatus.GOING);
} catch (error) {
    if (error.code === 'ALREADY_EXISTS') {
        // Use update instead
        await event.updateRSVP(AmityEventResponseStatus.GOING);
    } else if (error.code === 'EVENT_ENDED') {
        showError("Cannot RSVP to ended event");
    } else if (error.code === 'EVENT_CANCELLED') {
        showError("This event has been cancelled");
    } else {
        showError(`Failed to RSVP: ${error.message}`);
    }
}
```
</CodeGroup>

## Related Topics

<CardGroup cols={3}>
  <Card title="Create Events" href="./create-event" icon="plus">
    Schedule new events
  </Card>
  <Card title="Manage Events" href="./manage-events" icon="gear">
    Update and query events
  </Card>
  <Card title="Events Overview" href="./overview" icon="book">
    Learn about event concepts
  </Card>
</CardGroup>
