# AUTO-GENERATED by harness genmanifests — fill in snippets: with GendocsKeys
# GendocsKey = DeriveKey(filename): strip 'Amity' prefix, PascalCase → kebab-case
# Example: AmityClientLogin.kt → client-login
sections:
    connection-methods:
        heading: '### Connection Methods'
        snippets:
            - follow-send-request
            - follow-accept
            - follow-decline
            - follow-withdraw-request
    enable-disable-follow-requests:
        heading: '### Enable/Disable Follow Requests'
        snippets:
            - follow-accept
            - follow-decline
    error-recovery-pattern:
        heading: '### Error Recovery Pattern'
        snippets:
            - error-conversion
            - error-global-ban-login-handling
            - error-global-ban-subscription
    implementation-pattern:
        heading: '### Implementation Pattern'
        snippets:
            - follow-send-request
            - follow-accept
            - follow-decline
            - follow-withdraw-request
    implementation-strategy:
        heading: '### Implementation Strategy'
        snippets:
            - follow-self-get
            - follow-user-get
