---
title: "Web Development"
description: "TypeScript and JavaScript SDK installation for modern web applications and frameworks."
---

# Web Development

Build powerful web applications with Social Plus SDK's TypeScript/JavaScript implementation. Perfect for React, Vue, Angular, and vanilla JavaScript applications.

## Available SDKs

<Card title="TypeScript SDK" icon="code" href="/social-plus-sdk/getting-started/platform-setup/web/install-web">
Modern TypeScript SDK with full type safety and framework support
</Card>

## Framework Compatibility

The Social Plus TypeScript SDK works seamlessly with popular web frameworks:

<CardGroup cols={2}>
  <Card title="React Applications" icon="react">
    - Create React App  
    - Next.js (client-side)
    - Vite React
    - Custom React setups
  </Card>
  
  <Card title="Vue Applications" icon="vuejs">
    - Vue 3 composition API
    - Vue 2 options API
    - Nuxt.js (client-side)
    - Vite Vue
  </Card>
  
  <Card title="Angular Applications" icon="angular">
    - Angular 12+
    - Angular CLI projects
    - Nx workspaces
    - Custom Angular setups
  </Card>
  
  <Card title="Other Frameworks" icon="javascript">
    - Vanilla JavaScript/TypeScript
    - Svelte/SvelteKit
    - Solid.js
    - Lit Framework
  </Card>
</CardGroup>

## Web-Specific Features

### Real-time Communication
- **WebSocket Connections**: Persistent real-time connections
- **Event-Driven Architecture**: Reactive data updates
- **Connection Management**: Automatic reconnection and error handling
- **Offline Detection**: Graceful handling of network changes

### Browser Capabilities
- **Modern Browser Support**: Chrome 38+, Firefox 42+, Safari 9+, Edge 13+
- **Progressive Web App**: PWA-ready features and service worker support
- **Responsive Design**: Adaptive layouts for all screen sizes
- **Accessibility**: WCAG compliant components and interactions

### Development Experience
- **TypeScript Support**: Full type definitions and IntelliSense
- **Tree Shaking**: Optimized bundle sizes with webpack/Vite
- **Hot Module Replacement**: Fast development iteration
- **Source Maps**: Easy debugging in development

## Browser Requirements

<Tabs>
<Tab title="Supported Browsers">
### Minimum Browser Versions
- **Chrome**: 38+ (2014)
- **Firefox**: 42+ (2015)
- **Safari**: 9+ (2015)
- **Microsoft Edge**: 13+ (2015)
- **Opera**: 25+ (2014)

### Recommended Browsers
- **Chrome**: Latest stable
- **Firefox**: Latest stable
- **Safari**: Latest stable
- **Edge**: Latest stable (Chromium-based)
</Tab>

<Tab title="Unsupported">
### Not Supported
- **Internet Explorer**: All versions (including IE 11)
- **Legacy Edge**: EdgeHTML-based versions
- **Chrome**: Below version 38
- **Safari**: Below version 9

### Why These Limitations?
The SDK requires modern JavaScript features like:
- WebSockets for real-time communication
- Promises and async/await for asynchronous operations
- ES6+ syntax and modules
- Modern DOM APIs
</Tab>
</Tabs>

## Installation Methods

<CardGroup cols={3}>
  <Card title="npm" icon="npm">
    ```bash
    npm install @amityco/ts-sdk
    ```
  </Card>
  
  <Card title="yarn" icon="package">
    ```bash
    yarn add @amityco/ts-sdk
    ```
  </Card>
  
  <Card title="pnpm" icon="box">
    ```bash
    pnpm add @amityco/ts-sdk
    ```
  </Card>
</CardGroup>

## Web Development Best Practices

### Performance Optimization
- **Bundle Splitting**: Load SDK components on-demand
- **Connection Pooling**: Reuse WebSocket connections efficiently
- **Caching Strategy**: Implement smart caching for API responses
- **Lazy Loading**: Load features as users need them

### User Experience
- **Loading States**: Show progress during data fetching
- **Error Boundaries**: Graceful error handling and recovery
- **Offline Support**: Cache critical data for offline access
- **Responsive Design**: Optimize for all device types

### Security Considerations
- **Environment Variables**: Store API keys securely
- **HTTPS Only**: Ensure secure communication
- **Content Security Policy**: Implement CSP headers
- **Input Validation**: Sanitize user inputs properly

## Framework-Specific Guides

### React Integration
```typescript
import { Client } from '@amityco/ts-sdk';
import { useEffect, useState } from 'react';

function App() {
  const [client, setClient] = useState<Client | null>(null);

  useEffect(() => {
    const initClient = async () => {
      const sdkClient = Client.create({
        apiKey: process.env.REACT_APP_API_KEY!,
        region: 'sg'
      });
      
      await sdkClient.login({
        userId: 'user123',
        displayName: 'John Doe'
      });
      
      setClient(sdkClient);
    };

    initClient();
  }, []);

  return client ? <SocialApp client={client} /> : <Loading />;
}
```

### Vue Integration
```typescript
import { ref, onMounted } from 'vue';
import { Client } from '@amityco/ts-sdk';

export default {
  setup() {
    const client = ref<Client | null>(null);
    
    onMounted(async () => {
      const sdkClient = Client.create({
        apiKey: process.env.VUE_APP_API_KEY!,
        region: 'sg'
      });
      
      await sdkClient.login({
        userId: 'user123',
        displayName: 'John Doe'
      });
      
      client.value = sdkClient;
    });
    
    return { client };
  }
};
```

## Development Tools

### Debugging
- **Browser DevTools**: Use network tab for API inspection
- **Console Logging**: Enable SDK debug logs in development
- **React DevTools**: Inspect React component state and props
- **Vue DevTools**: Debug Vue component reactivity

### Testing
- **Jest**: Unit testing with mocking capabilities
- **Cypress**: End-to-end testing for user workflows
- **Testing Library**: Component testing with user-centric approach
- **MSW**: Mock Service Worker for API mocking

## Deployment Considerations

### Build Optimization
- **Environment Variables**: Configure different environments
- **Bundle Analysis**: Monitor and optimize bundle size
- **Code Splitting**: Split code by routes and features
- **Asset Optimization**: Compress images and other assets

### Production Setup
- **CDN Deployment**: Serve static assets from CDN
- **Caching Headers**: Configure appropriate cache policies
- **Monitoring**: Implement error tracking and performance monitoring
- **Security Headers**: Set up security headers and CSP

## Getting Started

1. **Check Requirements**: Ensure browser compatibility
2. **Install SDK**: Add to your project using npm/yarn/pnpm
3. **Configure Environment**: Set up API key and environment variables
4. **Initialize Client**: Create and authenticate SDK client
5. **Build Features**: Start implementing social functionality

Ready to build for the web? Follow our [detailed installation guide](/social-plus-sdk/getting-started/platform-setup/web/install-web) to get started.
