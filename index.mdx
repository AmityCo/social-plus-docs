---
title: "Home"
description: "Welcome to the Social+ documentation"
mode: "custom"
---

<div className="hero-section">
  <div className="hero-content">
    <div className="hero-text">
      <h1 className="hero-title">Build with Social+</h1>
      <p className="hero-description">
        Get started with Social+ SDKs, UIKits, and APIs to integrate powerful social features into 
        your application - fast, flexible, and built for scale.
      </p>
    </div>
    
    <div className="search-container">
      <div className="search-box" onClick="window.dispatchEvent(new KeyboardEvent('keydown', { key: 'k', ctrlKey: true, metaKey: true }))">
        <span className="search-icon">🔍</span>
        <input type="text" placeholder="Search..." className="search-input" readOnly />
        <span className="search-shortcut">Ctrl K</span>
      </div>
    </div>
    
    <script>
      {`
        document.addEventListener('DOMContentLoaded', function() {
          const searchBox = document.querySelector('.search-box');
          const searchInput = document.querySelector('.search-input');
          
          function triggerGlobalSearch() {
            // Try multiple methods to trigger Mintlify search
            // Method 1: Keyboard shortcut simulation
            const event = new KeyboardEvent('keydown', {
              key: 'k',
              code: 'KeyK',
              ctrlKey: true,
              metaKey: true,
              bubbles: true
            });
            document.dispatchEvent(event);
            
            // Method 2: Try to find and click Mintlify search button
            const searchButton = document.querySelector('[data-search-button]') || 
                                document.querySelector('.search-trigger') ||
                                document.querySelector('[aria-label*="search" i]');
            if (searchButton) {
              searchButton.click();
            }
            
            // Method 3: Try to focus on Mintlify search input
            setTimeout(() => {
              const mintlifySearch = document.querySelector('input[type="search"]') ||
                                   document.querySelector('input[placeholder*="search" i]');
              if (mintlifySearch && mintlifySearch !== searchInput) {
                mintlifySearch.focus();
              }
            }, 100);
          }
          
          if (searchBox) {
            searchBox.addEventListener('click', triggerGlobalSearch);
          }
          
          if (searchInput) {
            searchInput.addEventListener('click', triggerGlobalSearch);
            searchInput.addEventListener('focus', triggerGlobalSearch);
          }
        });
      `}
    </script>
  
  <div className="cards-section">
    <CardGroup cols={3}>
    <Card title="SDKs" icon="layer-plus" href="/social-plus-sdk/overview">
      Integrate Social+ with platform-specific SDKs for auth, real-time events, data, and 
      core services.
    </Card>
    <Card title="UIKits" icon="palette" href="/uikit/overview">
      Use prebuilt UI components for feeds, profiles, groups, chat, and live streaming features.
    </Card>
    <Card title="API Reference" icon="code" href="/api-reference/introduction">
      Explore complete API docs with endpoints, parameters, auth methods, and response formats.
    </Card>
    <Card title="Social+ Portal" icon="building" href="/analytics-and-moderation/social+-portal/overview">
      Manage apps, regions, billing, usage, and account settings from one centralized platform.
    </Card>
    <Card title="Social+ Console" icon="terminal" href="/analytics-and-moderation/console/overview">
      Monitor network data, configure settings, and manage moderation across your Social+ applications.
    </Card>
    <Card title="Social+ Dashboard" icon="chart-bar" href="/analytics-and-moderation/console/analytics">
      Analyze chat, community, and live stream data to track engagement and platform performance.
    </Card>
  </CardGroup>
  </div>

  </div>
</div>

