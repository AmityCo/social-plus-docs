{
  "id": "0053",
  "title": "Fix AmitySocialClient→AmityChatClient in Flutter flag-unflag snippet",
  "type": "doc-fix",
  "owner_hint": "either",
  "priority": "medium",
  "target_files": [
    "social-plus-sdk/chat/messaging-features/messages/flag-unflag-a-message.mdx"
  ],
  "context_files": [
    ".docs-ops/sdk-surface/flutter.json"
  ],
  "description": "The Flutter Dart code blocks in flag-unflag-a-message.mdx use `AmitySocialClient.newMessageRepository()` (lines ~832, ~1221) but the SDK surface shows `newMessageRepository` belongs to `AmityChatClient`, not `AmitySocialClient`. Fix both occurrences. The blocks are currently marked with doc-as-test skip markers (class-body-fragment reason) — that skip reason should remain valid; only fix the AmitySocialClient reference in the source.",
  "success_criteria": {
    "validators": [
      "grep -rn 'AmitySocialClient.newMessageRepository' social-plus-sdk/chat/messaging-features/messages/flag-unflag-a-message.mdx | wc -l | grep -q '^0$'"
    ]
  }
}
