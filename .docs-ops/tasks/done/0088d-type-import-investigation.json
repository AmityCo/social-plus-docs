{
  "id": "0088d-type-import-investigation",
  "tier": "C",
  "type": "content",
  "owner_hint": "copilot",
  "target_files": [
    ".docs-ops/evals/0087-fence-fix-triage.md",
    ".docs-ops/evals/sdk-tickets-to-file.md"
  ],
  "context_files": [
    ".docs-ops/evals/0087-fence-fix-triage.md",
    ".docs-ops/sdk-surface/typescript.json",
    ".docs-ops/extractors/typescript-hybrid-extractor.py",
    ".docs-ops/ci/check-drift.py"
  ],
  "instructions": "Cleanup cohort 4 of 5 from the 0087 FENCE_RE-fix triage. **Heavy investigation cohort** — ~30 drift pairs are type imports that aren't in the current surface. Three possible reasons per type:\n  - **(A) Type lives in the `Amity.*` global namespace** (e.g. `Amity.Post`, `Amity.Reaction`) — fix is to remove the `import` and use the qualified `Amity.X` form in the snippet body, OR to import the global types correctly per the SDK's documented pattern.\n  - **(B) Type is genuinely unexported / removed from the public API** — fix is either to remove the import (and the snippet line that uses it) or to add a `doc-as-test: skip` marker per [CI_GATE.md](.docs-ops/CI_GATE.md).\n  - **(C) Hybrid extractor missed an export** — file as a `TICKET-0088D-VALIDATOR-*` for extractor refinement; in the meantime add to a temporary allowlist OR skip.\n\n## Types to investigate\n\nFrom the triage §4, count by symbol:\n\n| Count | Symbol | Likely category |\n|---|---|---|\n| 4 | `AmityPost` | (A) likely `Amity.Post` |\n| 3 | `CommunityPostSetting` | (A) or (B) — investigate |\n| 3 | `LiveCollection` | (B) likely — generic utility type, may not be a public export |\n| 2 | `Reaction` | (A) likely `Amity.Reaction` |\n| 2 | `ReactionReferenceType` | (A) likely `Amity.ReactionReferenceType` |\n| 1 | `FeedType` | (A) or (B) — investigate |\n\nAlso likely in this cohort (verify from triage): any remaining `import X` lines for type-only purposes.\n\n## Steps\n\n1. For each type, search `.docs-ops/sdk-surface/typescript.json` for the bare name (e.g. `\"Post\"`, `\"Reaction\"`) and check whether it exists under the `Amity` namespace (global types pass).\n2. **If type exists as `Amity.X`** → in each affected file, replace `import { X }` with the appropriate `Amity` import per SDK convention (likely the SDK uses ambient global, so just remove the import and qualify usages as `Amity.X`), OR confirm what the SDK's documented import pattern actually is by reading TS SDK examples.\n3. **If type does NOT exist anywhere in the SDK source** → confirm by greping `AmityTypescriptSDK/`. If confirmed absent:\n   - For snippets where the type is illustrative (just a name in a function signature) → remove the import line\n   - For snippets where the type is load-bearing (e.g. used in actual logic) → add `{/* doc-as-test: skip (type not in public surface) */}` immediately before the fence\n4. **If type IS in the SDK source but missing from surface JSON** → file ticket `TICKET-0088D-VALIDATOR-<name>` for extractor refinement, and use the skip-marker approach for now.\n5. Run gate. Aim for ~30 pairs cleaned up (this is the largest cohort).\n\n## Out of scope\n\n- Repository/method renames (other cohorts).\n- Fixing the extractor itself (file tickets only).\n\n## Success criteria\n\n- All type-import drift pairs resolved (rewrite or skip-marker).\n- No new drift pairs introduced.\n- Per-type decision documented in result notes: A/B/C classification.\n- Any extractor gaps filed as tickets.\n\nReport in `notes`: per-type investigation results (which category); count of files touched; skip-markers added vs imports rewritten; tickets filed.",
  "success_criteria": {
    "validators": [
      {
        "name": "drift_reduced",
        "command": "python3 .docs-ops/ci/check-drift.py --quiet 2>&1 | grep -oE 'candidate=[0-9]+' | head -1",
        "expect_in_stdout": "candidate="
      }
    ],
    "rubric_must_not_regress": false
  },
  "expected_outputs": {
    "report_path": ".docs-ops/evals/sdk-tickets-to-file.md"
  },
  "blocked_by": [],
  "created_by": "claude-opus-4-7",
  "created_at": "2026-05-20T16:00:00Z"
}
