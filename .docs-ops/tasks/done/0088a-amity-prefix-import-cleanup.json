{
  "id": "0088a-amity-prefix-import-cleanup",
  "tier": "C",
  "type": "content",
  "owner_hint": "copilot",
  "target_files": [
    ".docs-ops/evals/0087-fence-fix-triage.md"
  ],
  "context_files": [
    ".docs-ops/evals/0087-fence-fix-triage.md",
    ".docs-ops/sdk-surface/typescript.json",
    ".docs-ops/ci/check-drift.py"
  ],
  "instructions": "Cleanup cohort 1 of 5 from the 0087 FENCE_RE-fix triage. Mechanical rename: the old `Amity`-prefix root-export style is gone from the current TS SDK surface. Replace the old import names with the current namespace names. ~15 drift pairs across ~12 files.\n\n## Symbols to rename\n\n| Old (drift) | New (current surface) | Notes |\n|---|---|---|\n| `AmityUserRepository` | `UserRepository` | namespace |\n| `AmityNotificationTrayRepository` | `notificationTray` | namespace, also case change |\n| `AmityClient` | `Client` | namespace |\n| `AmityPost` | (verify — likely no direct equivalent; may be `Amity.Post` type ref) | type — investigate |\n\n## Steps\n\n1. Run `python3 .docs-ops/ci/check-drift.py --json-out /tmp/drift-before-0088a.json` to capture the current drift baseline.\n2. For each symbol above:\n   - Grep the docs (`social-plus-sdk/`, `uikit/`, `use-cases/`) for the old name in import statements specifically (`import { AmityX }` or `import AmityX from`).\n   - Replace with the new name. For `AmityPost`: check if the snippet uses it as a TYPE (Amity.Post lives in the global namespace) or as a runtime value. If a type-only usage, change to `Amity.Post` (TS namespace ref). If a runtime usage, investigate per-case.\n   - Update any DOWNSTREAM usages in the same code block (e.g. `AmityUserRepository.getUser` → `UserRepository.getUser`).\n3. Re-run the gate. Confirm at least 15 pairs cleaned up (target: ~15 from this cohort, but other cohorts may overlap if shared files).\n\n## Out of scope\n\n- Symbols in other cohorts (0088b–e).\n- Non-import drift items (member-method renames) — those are 0088c.\n- Skip markers — none needed here per the triage classification.\n\n## Success criteria\n\n- Drift count drops by at least 12 pairs vs the baseline captured in step 1.\n- No new (file, ref) pairs introduced.\n- All `import AmityUserRepository`, `import AmityClient`, `import AmityNotificationTrayRepository` references replaced.\n\nReport in `notes`: per-symbol count of files changed, drift count before/after.",
  "success_criteria": {
    "validators": [
      {
        "name": "drift_reduced",
        "command": "python3 .docs-ops/ci/check-drift.py --quiet 2>&1 | tee /tmp/0088a-gate.txt | grep -oE 'candidate=[0-9]+' | head -1",
        "expect_in_stdout": "candidate="
      }
    ],
    "rubric_must_not_regress": false
  },
  "expected_outputs": {
    "report_path": ".docs-ops/evals/0087-fence-fix-triage.md"
  },
  "blocked_by": [],
  "created_by": "claude-opus-4-7",
  "created_at": "2026-05-20T16:00:00Z"
}
