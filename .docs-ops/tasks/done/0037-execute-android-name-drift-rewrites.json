{
  "id": "0037-execute-android-name-drift-rewrites",
  "tier": "A",
  "type": "sdk_signature_diff",
  "owner_hint": "copilot",
  "target_files": [],
  "context_files": [
    ".docs-ops/evals/android-fabrication-decisions.json",
    ".docs-ops/sdk-surface/android.json",
    ".docs-ops/evals/android-accuracy-drift.json"
  ],
  "instructions": "Execute the auto-fixable rewrites identified in `.docs-ops/evals/android-fabrication-decisions.json` — analog of task 0021 (TS NAME_DRIFT) for Android.\n\nONLY act on rows where `classification == \"NAME_DRIFT_ANDROID_RENAMED\"`. Use the `recommended_action` field on each row (format: `REWRITE_TO:<target>`) as authoritative — these targets are surface-verified by 0035's triage. Do NOT act on TRUE_FABRICATION_NOWHERE or ANDROID_GAP_OTHER_PLATFORMS_HAVE rows — those are for the Android SDK-lead meeting.\n\n**target_files is intentionally empty** — discover from each row's `locations[]` in the decision sheet.\n\nExpected 13 rows / 26 occurrences. The patterns you'll encounter:\n\n**Pattern 1 — Method rename within same type** (e.g. `AmityCommentRepository.get` → `getComment`, `AmityRoomRepository.getBroadcastData` → `getBroadcasterData`, `AmityRoomRepository.stop` → `stopRoom`):\n  - Find the call site `Type.oldMethod(...)`.\n  - Replace with `Type.newMethod(...)` preserving args verbatim.\n  - No import changes (the type is already imported).\n\n**Pattern 2 — Enum case rename (UPPER_SNAKE → PascalCase)** for `AmityContentFlagReason`:\n  - 6 cases: `OTHERS`→`Others`, `HARASSMENT_OR_BULLYING`→`HarassmentOrBullying`, `SPAM_OR_SCAMS`→`SpamOrScams`, `COMMUNITY_GUIDELINES`→`CommunityGuidelines`, `VIOLENCE_OR_THREATENING_CONTENT`→`ViolenceOrThreateningContent`, `SEXUAL_CONTENT_OR_NUDITY`→`SexualContentOrNudity`, `FALSE_INFORMATION`→`FalseInformation`.\n  - Find `AmityContentFlagReason.<UPPER_SNAKE>` in Kotlin/Java blocks.\n  - Replace with `AmityContentFlagReason.<PascalCase>` per the decision sheet's recommended_action.\n  - No import changes.\n\n**Pattern 3 — Other type-rename** (e.g. `AmityError.NOT_FOUND` → `AmityError.ITEM_NOT_FOUND`, `AmityFileRepository.<old>` → `AmityFileRepository.getFile`):\n  - Same textual substitution per recommended_action.\n\n**Rules** (consistent with 0006 / 0010 / 0021):\n  - Apply only inside ```kotlin / ```java fenced code blocks.\n  - Preserve all arguments, generic params, chained calls verbatim.\n  - Watch for indented blocks inside MDX components (`<Step>`, `<Tab>`, `<Accordion>`) — handle manually if FENCE_RE misses them.\n  - Do NOT modify prose, frontmatter, or unrelated code blocks.\n  - Do NOT touch rows that aren't `NAME_DRIFT_ANDROID_RENAMED` — those are meeting-gated.\n  - If a call site doesn't fit cleanly (signature appears to have changed beyond rename), STOP that occurrence and report `needs_signature_review` in notes.\n\nAfter your edits, re-run the Android validator. The 13 NAME_DRIFT refs should NO LONGER appear in the drift report. The 37+1 remaining decisions (DELETE + FLAG_FOR_SDK) are still expected — they're held for the meeting.\n\nReport in `notes`: per-file (line, old, new) replacements; per-row occurrences fixed vs blocked; any `needs_signature_review` cases.",
  "success_criteria": {
    "validators": [
      {
        "name": "android_name_drift_refs_gone",
        "command": "python3 .docs-ops/extractors/android-extractor.py > /dev/null && python3 .docs-ops/validators/android-accuracy-validator.py > /dev/null && python3 -c \"import json; d=json.load(open('.docs-ops/evals/android-fabrication-decisions.json')); r=json.load(open('.docs-ops/evals/android-accuracy-drift.json')); name_drift_refs={row['ref'] for row in d['rows'] if row['classification']=='NAME_DRIFT_ANDROID_RENAMED'}; flagged={i.get('ref') for f,iss in r['issues_by_file'].items() for i in iss if i.get('ref') in name_drift_refs}; print('FAIL: still flagging '+str(flagged)) if flagged else print('OK'); import sys; sys.exit(1 if flagged else 0)\"",
        "expect_in_stdout": "OK"
      }
    ],
    "rubric_must_not_regress": true
  },
  "blocked_by": [],
  "created_by": "claude-opus-4-7",
  "created_at": "2026-05-17T16:30:00Z"
}
