# TypeScript SDK Gap Tickets

_Generated by task 0026 from fabrication-decisions.json FLAG_FOR_SDK rows._
_Each entry represents a real feature available on ≥1 other SDK platform that is absent from the TypeScript SDK._

---

## TICKET-TS-001 · `FeedRepository.getCommunityFeed`

**Priority**: P1 (iOS + Android + Flutter all expose this)
**Affected docs**: `social-plus-sdk/social/content-management/posts/moderation/post-review.mdx`, `snippets/social/feeds/get-user-feed.mdx`
**Description**: `FeedRepository` in TS only exposes `queryGlobalFeed`. iOS, Android, and Flutter all have `getCommunityFeed(communityId, options)`. Customers building community post feeds in TypeScript have no equivalent.
**Suggested API**: `FeedRepository.getCommunityFeed(communityId: string, params: { sortBy, dataTypes, limit }, callback): Amity.Unsubscriber`

---

## TICKET-TS-002 · `ChannelRepository.markAsRead`

**Priority**: P1 (iOS + Android)
**Affected docs**: `use-cases/chat/unread-counts-and-read-receipts.mdx`
**Description**: iOS and Android expose `markAsRead(channelId)` to sync the read position. TS uses `Client.markerSync` for a different model but does not have a per-channel mark-as-read that matches the mobile API.
**Suggested API**: `ChannelRepository.markAsRead(channelId: string): Promise<void>`

---

## TICKET-TS-003 · `PostRepository.createPollPost`

**Priority**: P1 (iOS + Android + Flutter)
**Affected docs**: `use-cases/social/polls-and-interactive-content.mdx`, `use-cases/social/rich-content-creation.mdx`
**Description**: iOS, Android, and Flutter have a single `createPollPost` convenience method. In TS, users must manually chain `PollRepository.createPoll` + `PostRepository.createPost`. A unified helper would close the cross-platform parity gap.
**Suggested API**: `PostRepository.createPollPost(params: { pollId, targetType, targetId, text? }): Promise<Amity.Post>`

---

## TICKET-TS-004 · `Client.registerPushNotification`

**Priority**: P1 (iOS + Android)
**Affected docs**: `use-cases/social/notifications-and-engagement.mdx`
**Description**: iOS and Android have SDK-level push token registration. The TS SDK has no `registerPushNotification` method; web push requires external Firebase Messaging wiring outside the SDK.
**Suggested API**: `Client.registerPushNotification(params: { token: string, platform: 'web' }): Promise<void>`

---

## TICKET-TS-005 · `AdRepository.markAdSeen`

**Priority**: P2 (Flutter internal use-case)
**Affected docs**: `use-cases/social/advertising-and-monetization.mdx`
**Description**: Flutter `AmityAdRepository` exposes impression tracking. TS `AdRepository` has `getNetworkAds` but no `markAdSeen`.
**Suggested API**: `AdRepository.markAdSeen(adId: string): Promise<void>`

---

## TICKET-TS-006 · `AdRepository.markAdClicked`

**Priority**: P2 (Flutter internal use-case)
**Affected docs**: `use-cases/social/advertising-and-monetization.mdx`
**Description**: Flutter `AmityAdRepository` exposes click tracking. TS `AdRepository` has no `markAdClicked`.
**Suggested API**: `AdRepository.markAdClicked(adId: string): Promise<void>`

---

## TICKET-TS-007 · `UserRepository.getViewedUsers`

**Priority**: P2 (Android + Flutter)
**Affected docs**: `social-plus-sdk/social/content-management/posts/analytics/post-impressions.mdx`
**Description**: Android and Flutter expose post viewer tracking via `getViewedUsers(postId)`. No TS equivalent.
**Suggested API**: `UserRepository.getViewedUsers(params: { postId: string, limit?: number }, callback): Amity.Unsubscriber`

---

## TICKET-TS-008 · `AmityCommentQuery` / `AmityCommentSortOption`

**Priority**: P2 (Android + Flutter)
**Affected docs**: `social-plus-sdk/social/comments/query-comment.mdx`
**Description**: Android and Flutter expose typed query objects for comment filtering. TS `CommentRepository.getComments` accepts inline params but has no exported `AmityCommentQuery` interface or `AmityCommentSortOption` enum.
**Suggested API**: `export type AmityCommentQuery = { referenceId: string; referenceType: Amity.CommentReferenceType; filterByParentId?: boolean; parentId?: string; }` + `export enum AmityCommentSortOption`

---

## TICKET-TS-009 · `AmityDataType` unified enum

**Priority**: P2 (Android + Flutter)
**Affected docs**: `social-plus-sdk/social/posts/viewing-post-content.mdx`
**Description**: Android and Flutter have a unified `AmityDataType` enum covering post types. TS splits this across `FeedDataType` and `PollDataType` string literals. A unified enum would ease cross-platform code sharing.
**Suggested API**: `export enum AmityDataType { TEXT = 'text', IMAGE = 'image', VIDEO = 'video', FILE = 'file', POLL = 'poll', LIVE_STREAM = 'liveStream', CUSTOM = 'custom' }`

---

## TICKET-TS-010 · `AmityFeedRepository` (user feed class)

**Priority**: P2 (iOS + Android + Flutter)
**Affected docs**: `snippets/social/feeds/get-user-feed.mdx`
**Description**: iOS, Android, and Flutter expose `AmityFeedRepository` with user feed + community feed queries. TS has `FeedRepository.queryGlobalFeed` only — no user-scoped feed query.
**Suggested API**: `FeedRepository.getUserFeed(userId: string, params, callback): Amity.Unsubscriber`

---

## TICKET-TS-011 · `StreamBroadcaster` class

**Priority**: P3 (Android only)
**Affected docs**: `social-plus-sdk/video-new/platform-specific/platform-comparison.mdx`
**Description**: Android has `AmityStreamBroadcaster` for RTMP push. TS/Web uses WebRTC and `StreamRepository.createStream` with browser MediaRecorder. This is legitimately platform-specific but the doc incorrectly shows `StreamBroadcaster` as a TS import.
**Suggested action**: Clarify in docs that TS streaming is WebRTC-based; update platform comparison table. No TS SDK ticket needed unless RTMP support is planned.

---

## TICKET-TS-012 · `AmityCommunityMemberStatusFilter` (named export)

**Priority**: P2 (Android + Flutter)
**Affected docs**: `social-plus-sdk/social/discovery-engagement/search/intelligent-search-community.mdx`, `use-cases/social/search-and-discovery.mdx`
**Description**: The type exists as `Amity.CommunityMemberStatusFilter` (ambient namespace type) and as the runtime enum `AmityCommunityMemberStatusFilter` exported via `@amityco/ts-sdk`. However, some TS examples use `CommunityRepository.AmityCommunityMemberStatusFilter` (nested access) which is incorrect. Confirm top-level export.
**Status**: Fixed in docs (task 0024) — using `AmityCommunityMemberStatusFilter` from top-level import. SDK ticket: verify the enum is intentionally re-exported in the public surface.

---

_Total tickets: 12_
_Updated: task 0026 execution_
